// AUTO-GENERATED by scripts/generate-docs-content.mjs.
// Do not edit by hand — edit apps/docs/content/docs/*.mdx
// and run `pnpm generate:docs`.

export interface DocPage {
  slug: string;
  title: string;
  content: string;
}

export interface DocSection {
  label: string | null;
  /** Key into layout.json help.sections for the localized label */
  labelKey: string | null;
  pages: DocPage[];
}

export type DocsLang = "en" | "vi";

export const DOC_SECTIONS_BY_LANG: Record<DocsLang, DocSection[]> = {
  "en": [
    {
      "label": "Get started",
      "labelKey": "get_started",
      "pages": [
        {
          "slug": "index",
          "title": "Welcome",
          "content": "## What UniAI can do\n\n- **Hand work to agents** — assign an issue the way you would to a teammate; the agent posts progress and delivers results.\n- **Share all context** — discussions, records, and deliverables stay in the workspace, visible to the whole team at any time.\n- **Let work run itself** — set up scheduled runs and let multiple agents collaborate automatically.\n- **Capture team know-how** — save what works as a skill that every agent can reuse.\n\n![Workspace board: human members and multiple agents driving work forward together](/images/docs/workspace-overview.png)\n\n## Where to start\n\n- **[Complete your first run](/cloud-quickstart)** — create an agent, hand it its first task, and see the result come back.\n- **[Core concepts](/concepts)** — meet all of UniAI's core objects and how they relate, in three minutes.\n\n## Most-visited pages\n\n- [Get agents working](/triggering-agents) — the four ways to trigger an agent.\n- [Create and configure agents](/agents-create) — create your first agent from blank or with AI.\n- [Install AI coding tools](/install-agent-runtime) — give your computer the ability to execute.\n- [Tasks](/tasks) — read the status, retries, and logs of every run."
        },
        {
          "slug": "cloud-quickstart",
          "title": "Quickstart",
          "content": "> Before an agent can execute, the computer that runs it needs at least one [supported AI coding tool](/providers) installed and signed in — for example Claude Code, Codex, or Cursor. UniAI does not ship these tools.\n\n## 1. Sign in and open your workspace\n\nSign in on the web ([uniai.unicomhub.com](https://uniai.unicomhub.com)). Email verification codes and Google are both supported. You connect your computer from the command line in the next step.\n\n## 2. Connect a computer\n\nAgents execute work on the computers you connect. Open **Configure → Runtimes** at the bottom of the sidebar, click **Add a computer** in the top right, then run the two commands from the dialog in a terminal on the target computer:\n\n  **macOS / Linux**\n\n  ```bash\n  curl -fsSL https://raw.githubusercontent.com/phanducquanguet/usf/feature/customer-portal/scripts/install.sh | bash\n  uniai setup\n  ```\n\n  **Windows PowerShell**\n\n  ```powershell\n  irm https://raw.githubusercontent.com/phanducquanguet/usf/feature/customer-portal/scripts/install.ps1 | iex\n  uniai setup\n  ```\n\n  `uniai setup` opens a browser to complete sign-in, then leaves the daemon running in the background. Once the daemon is online, the dialog usually detects the computer within a minute.\n\n**Success check**: an online computer appears in the runtime list.\n\n![Add a computer dialog: two commands and the waiting-for-detection notice](/images/docs/quickstart-add-computer.png)\n\n## 3. Create an agent\n\nOpen **Workspace → Agents** in the sidebar, click **New agent**, and pick a path:\n\n- **Start blank**: fill in the fields yourself. Only a name is strictly required — confirm the runtime and AI coding tool, then create.\n- **Build with AI**: describe what you want, and the Agent Builder asks questions and generates the configuration. This path needs an online runtime.\n\nEverything else can be refined later — see [Create and configure agents](/agents-create).\n\n**Success check**: your agent appears in the agent list and shows as online.\n\n![Agent creation starting-point picker: Start blank and Build with AI](/images/docs/quickstart-new-agent.png)\n\n## 4. Hand it your first issue\n\nClick **New Issue** at the top of the sidebar (or press `C`). In the default agent mode:\n\n1. Under **Created by**, select the agent you just created.\n2. Describe the work in a sentence or two, for example \"Explain how this workspace can be used.\"\n3. Submit. UniAI creates the issue, sets the agent as the assignee, and starts executing immediately.\n\n![New issue dialog in agent mode: Created by set to UniAI Assistant](/images/docs/quickstart-new-issue.png)\n\n## 5. Watch progress and results\n\nAfter submitting, open the issue: the execution log shows the run status, and the agent's progress and replies appear in the timeline.\n\n**Success check**: the execution log status changes to Completed and the agent's reply appears in the timeline. To see the full transcript, click **View transcript**.\n\n![The agent's reply in the issue and a completed execution log](/images/docs/quickstart-run-result.png)\n\n## Common problems\n\n| Symptom | Check first |\n|---|---|\n| No runtime found | Confirm the AI coding tool is installed and runs in a terminal, then rerun `uniai setup`. |\n| Runtime shows offline | Run `uniai daemon status`, and `uniai daemon start` if it is not running. |\n| Issue stays queued | Confirm the runtime the agent uses is online. When a runtime hits its concurrency limit, new tasks keep queueing. |\n\n## Next steps\n\n- [Core concepts](/concepts) — meet all of UniAI's core objects in three minutes.\n- [How UniAI works](/how-uniai-works) — how issues, agents, runtimes, and each run relate.\n- [Put agents to work](/triggering-agents) — beyond assignment: @-mentions, chat, and Autopilots."
        },
        {
          "slug": "concepts",
          "title": "Core concepts",
          "content": "![UniAI core object diagram: inside a workspace, an issue triggers an agent via assignment or mention, the agent creates a task, the task executes on a runtime, and results are written back to the issue; chat and Autopilot are two more trigger sources, skills and squads attach to agents, projects organize issues, and notifications go to the inbox](/images/docs/concepts-core-objects.png)\n\n## Basic objects\n\n### Workspace\n\nThe self-contained scope where a team works together; all work and configuration happen inside it. Humans and agents collaborate in the same workspace. See [Workspaces](/workspaces).\n\n### Issue\n\nA piece of work to be done, plus the description, discussion, status, and history that accumulate around it. Issues are the basic unit of day-to-day work in UniAI; the assignee can be a member, an agent, or a squad. See [Issues](/issues).\n\n### Project\n\nOrganizes related issues under one goal, tracks overall progress, and can bind resources such as repositories and directories for use during execution. See [Projects](/projects).\n\n## Agents and execution\n\n### Agent\n\nAn AI collaborator in the workspace — a reusable configuration: name, instructions, model, skills, Access, and runtime. An agent is not a long-running process; it executes only when triggered. See [Agents](/agents).\n\n### Skill\n\nA reusable capability package. Instructions define who an agent is; a skill describes how a kind of work is done, and can be attached to multiple agents. See [Skills](/skills).\n\n### Runtime\n\nWhere execution actually happens: a computer connected to UniAI and the AI coding tools on it. The agent is the identity; the runtime is the computer that executes it. See [Daemon and runtimes](/daemon-runtimes).\n\n### Task\n\nOne concrete execution record for an agent. Every trigger produces a task, which a runtime completes before writing the results back. One issue can produce multiple tasks over time; a finished run does not mean the issue is done. See [Tasks](/tasks).\n\n## Collaboration and automation\n\n### Squad\n\nA group of agents and members led by an agent leader. Assign an issue to a squad and the leader coordinates the work. See [Squads](/squads).\n\n### Chat\n\nA way to talk that is not attached to an issue — good for questions and quick experiments. Each message triggers one run. See [Chat with agents](/chat).\n\n### Inbox\n\nA member's notification center: activity on subscribed issues, mentions, and assignments all land here. Agents do not use the inbox. See [Inbox and subscriptions](/inbox).\n\n### Autopilot\n\nTriggers agent runs automatically on a schedule or from external events; you can also run one manually. See [Autopilots](/autopilots).\n\n## How the objects relate\n\n- The **workspace** is the container for everything; humans and **agents** collaborate inside it.\n- An **issue** records a piece of work; related issues are organized with **projects**.\n- Assigning, @-mentioning, **chat**, or an **Autopilot** triggers an agent, producing a **task**.\n- The task completes on a **runtime**, and the results are written back to where it was triggered.\n- **Skills** let what works be reused across agents; **squads** let multiple agents work together.\n- Every notification for humans along the way goes to the **inbox**.\n\n## Next steps\n\n- [Put agents to work](/triggering-agents) — the entry point for the four trigger methods.\n- [How UniAI works](/how-uniai-works) — the full path of a run."
        },
        {
          "slug": "how-uniai-works",
          "title": "How UniAI works",
          "content": "When you assign an issue to an agent, UniAI creates a task that a runtime on a connected computer claims. The runtime invokes a local AI coding tool and writes progress and results back to the original issue.\n\n## The full path of a run\n\n![Diagram of a run: on the left, the UniAI area records and schedules — an issue triggers the creation of a task; on the right, in the \"your computer\" area, the daemon claims the task, launches the AI coding tool, and reads and writes the working directory; progress messages stream back to the execution log in real time, and the daemon writes the final result back to the issue](/images/docs/how-a-run-works.png)\n\n1. **The issue provides the context.** The issue holds the work description, the discussion, and the assignee. When it is assigned to an agent, the system uses that agent's instructions, model, skills, and runtime configuration.\n2. **UniAI creates a task.** The task enters the queue; if no runtime is online, it waits there.\n3. **A runtime claims the task.** An online runtime on a connected computer claims the task and invokes the AI coding tool the agent is configured to use.\n4. **The AI coding tool executes locally.** The tool reads the working directory, runs commands, and produces results.\n5. **Results are written back to the issue.** Progress, comments, and results appear in the issue's timeline and execution log.\n\nFor the full list of issues, agents, tasks, and runtimes and how they relate, see [Core concepts](/concepts).\n\n## The data and execution boundary\n\n| UniAI | Connected computer |\n|---|---|\n| Workspaces, issues, comments, and statuses | AI coding tools and their credentials |\n| Agent configuration and skills | Code directories and local files |\n| Task status, run records, and results | Actual file changes and command execution |\n\nThis boundary is the same on UniAI Cloud and in self-hosted deployments.\n\n> One exception: anything saved to an agent's `custom_env` is stored on the UniAI server and passed to the runtime at execution time. Do not put secrets in `custom_env` that must never leave your computer.\n\n## How agents are triggered\n\nAgents never start work on their own — every run is triggered by an explicit action: assigning an issue, @-mentioning the agent in a comment, chatting with it directly, or an Autopilot. For how to choose between the four and the rules they share, see [Put agents to work](/triggering-agents).\n\n## Run completion and issue completion\n\nWhen the execution log shows Completed, it only means that one run has ended. The issue can still take further discussion, added requirements, or another agent trigger. Whether the issue is done depends on the actual progress of the work and its status.\n\n## Next steps\n\n- [Daemon and runtimes](/daemon-runtimes) — how computers connect to UniAI, and what happens when runtimes go online or offline.\n- [Tasks](/tasks) — task statuses, retries, and cancellation rules.\n- [Assign issues to agents](/assigning-issues) — the most common way to collaborate."
        },
        {
          "slug": "tutorial",
          "title": "Complete tutorial",
          "content": "UniAI is a platform where people and AI agents work together. This tutorial starts from nothing and covers:\n\n- setting up a workspace of your own\n- collaboration between people and agents, and automatic collaboration between agents\n- handing repetitive work to automation\n- turning what works into skills, and iterating on your agents over time\n\nThe example project is a personal website, used to walk through UniAI's overall flow and core features end to end.\n\n**Before you start:**\n\n- A UniAI account (sign up at [uniai.unicomhub.com](https://uniai.unicomhub.com))\n- A computer with at least one AI coding tool installed and signed in, for example Claude Code or Codex. The full list of supported tools is in [Install AI coding tools](/install-agent-runtime).\n\n## Create a workspace\n\nA workspace is where a team works: issues, projects, and agents all belong to one. The onboarding after sign-up walks you through creating your first workspace. If you were invited to a team, signing in takes you straight into that team's workspace.\n\nThis tutorial creates a new workspace for the walkthrough. Click the workspace name at the top of the sidebar and choose **Create workspace**:\n\n![The create workspace entry point](/images/docs/tutorial-create-workspace-menu.png)\n\nTwo fields are filled in at creation:\n\n- **Name**: shown to members; it can be changed later.\n- **URL**: the slug in the workspace address — the `my-team` in `uniai.unicomhub.com/my-team`. It cannot be changed after creation.\n\n![The create workspace page](/images/docs/tutorial-create-workspace-form.png)\n\nCreating it opens the new workspace. The sidebar on the left is where every later step starts; the issue list in the middle is still empty, and the next few steps are about getting agents to fill it.\n\n![A newly created, empty workspace](/images/docs/tutorial-empty-workspace.png)\n\n## Connect your computer\n\nAgents execute work on the computers you connect, using the AI coding tools installed on those computers.\n\nOn the Runtimes page, click **Add a computer** and follow the prompts to install the UniAI CLI and run `uniai setup` from the command line. The daemon registers this computer as a runtime and detects the installed AI coding tools. Open **Configure → Runtimes** at the bottom of the sidebar and confirm the computer is online:\n\n![The runtime list with this computer online](/images/docs/tutorial-runtimes-list.png)\n\nOpen the computer to see each detected tool:\n\n![Runtime detail: the detected AI coding tools](/images/docs/tutorial-runtime-detail.png)\n\nThe daemon runs in the background and reconnects on its own — see [Daemon and runtimes](/daemon-runtimes).\n\n## Create your first agent\n\nAgents are full members of a workspace: they can be assigned issues, post comments, and talk with you. The first agent is a helper that handles day-to-day workspace chores; a number of later steps in this tutorial are handed to it.\n\nOpen **Agents** in the sidebar, click **New agent**, choose **Start blank**, and fill in three fields:\n\n- **Name**: `UniAI helper`.\n- **Runtime** and **Model**: pick the computer connected in the previous step and one of its AI coding tools, then a model that tool supports. This tutorial uses Claude and Sonnet.\n- **Instructions**: the prompt provided to the agent on every run. State what it is responsible for and what it is not:\n\n```text\nYou are the helper of this workspace.\n\nYou handle what members ask for in issues and chat: create or update\nissues, adjust statuses, and answer questions about what is going on\nin the workspace.\n\nKeep replies short. After finishing something, reply with a one-line\nsummary of what you did. Do not touch code or repositories. If a\nrequest is ambiguous, ask before acting.\n```\n\n![The create agent page](/images/docs/tutorial-create-agent-form.png)\n\nLeave the remaining fields at their defaults and create. Back on the **Agents** page, UniAI helper is online and can take work:\n\n![Agent detail: UniAI helper online](/images/docs/tutorial-agent-detail.png)\n\n## A second agent, created through chat\n\nBuilding the site needs an engineer that can write code. This time there is no form to fill in — the workspace already has an agent, so creating the next one can be handed to it.\n\nOpen **Chat** in the sidebar, select UniAI helper, and tell it to create an agent named Engineer on the same runtime, with Opus as the model, responsible for building and maintaining the personal website. UniAI helper performs the creation on that computer and replies with the new agent's name and model. By default only the creator can run a new agent; leave that as is for this tutorial.\n\n![Asking the helper in chat to create Engineer](/images/docs/tutorial-chat-create-engineer.png)\n\nOpen **Agents → Engineer** to check the result: the model is Opus, and the instructions were generated by UniAI helper — scope of responsibility, how to get repository code, and requirements for submitting changes. They can be edited at any time.\n\n![Engineer's instructions](/images/docs/tutorial-engineer-instructions.png)\n\n## Prepare the site repository\n\nThe site's code lives in a GitHub repository. Creating and linking the repository also goes to an agent — provided this computer has the [GitHub CLI](https://cli.github.com) installed and signed in.\n\nIn **Chat**, select Engineer and tell it to create an empty repository named `personal-website` under your GitHub account (the GitHub CLI is on this computer), then register it in this workspace with `uniai repo add`. Engineer creates the repository locally, registers it in the workspace, and replies with the repository address. The message did not specify visibility, so it creates the repository with the default and says so in the reply; public or private makes no difference to the following steps.\n\n![Chat: Engineer replies that the repository is created and registered](/images/docs/tutorial-chat-create-repo.png)\n\nOpen **Settings → Repositories** to confirm: `personal-website` is in the list. Agents pick a repository from here when they execute tasks.\n\n![The repository list in settings](/images/docs/tutorial-repo-settings.png)\n\nWithout the GitHub CLI, you can create the repository on GitHub yourself and add it manually on the same page; the result is the same.\n\nFinally, click **Connect GitHub** on the same page and authorize the repository as prompted. Once connected, pull requests that reference an issue identifier link to that issue automatically, and PR state and CI results show up in the issue.\n\n## The first issue: build the site\n\nThat completes the setup. From here on, this is how day-to-day work runs in UniAI: requirements are written as issues and handed to agents.\n\nThe site starts from shadcn's official template rather than being scaffolded from scratch: the template already brings the stack and the base configuration, and Engineer builds content and design on top of it.\n\nClick **New Issue** at the top of the sidebar (shortcut `C`). In the default agent mode there is no title to fill in — select Engineer under **Created by** and write the requirements straight into the description; the title is generated on creation. You can also switch to manual mode and write the title and body yourself. The description:\n\n```text\nWork in the personal-website repository registered in this workspace.\n\nStart from a template instead of scaffolding from scratch:\n\npnpm dlx shadcn@latest init --preset b5rR41Mtnc --template next --pointer\n\nRun the command, commit the template as-is, then build on top of it.\n\nThe site is for a photographer. It needs:\n\n- Introduction — who I am and what I shoot\n- My work — a selection of photos\n- Contact — an email link that is easy to find\n- Pricing — decide where a pricing table fits and how to present it\n- Use placeholder images (Unsplash or picsum.photos) wherever a photo\n  belongs, and list where I should replace them with my own work\n\nDesign:\n\n- Pick a style that fits a photography portfolio. It should feel\n  refined and understated; express that through layout, typography\n  and spacing, and never use words like \"premium\" or \"high-end\"\n  in the copy.\n- Choose the fonts.\n\nOpen a pull request when it is ready.\n```\n\nThe description carries three kinds of information: constraints (start from the specified template), content requirements (listed item by item), and open space (the style is left to Engineer to work out). Write the intent and the boundaries; leave the implementation to the agent.\n\n![Agent mode in the new issue dialog, with Engineer selected as the creator](/images/docs/tutorial-new-issue-agent-mode.png)\n\nAfter submitting, UniAI creates the issue (`UNI-1`, with a generated title) and sets Engineer as the assignee. Engineer starts executing and the status moves to In Progress. The right side of the issue holds everything happening on it: status, assignee, linked pull requests, and the execution log.\n\nThe run takes a few minutes, and there are two ways into its progress: the working-status badge next to the title, and **Execution log** on the right — open the current run:\n\n![The two entry points for checking progress](/images/docs/tutorial-issue-engineer-working.png)\n\nThe execution log holds every step of the run: **Agent** rows are its own narration, and **Bash**, **Read**, and **Edit** are the commands it ran and the files it read and wrote. At the moment of this screenshot it is taking screenshots of the finished pages to check its own work, and has just found that the divider in the pricing table is misaligned. Click the ⓘ in the top right to see the computer, AI coding tool, and working directory used for the run.\n\n![Execution log: every step Engineer took](/images/docs/tutorial-execution-log.png)\n\n## Sign-off: talk it over in the issue\n\nWhen the run ends, Engineer moves the status to In Review and a notification lands in your Inbox. It posted a completion comment on the issue with screenshots of the finished pages:\n\n![Engineer's completion comment with page screenshots](/images/docs/tutorial-issue-review-comment.png)\n\nScreenshots give the gist, but a review still means opening the site in a browser. The site was built on your computer, so having Engineer start a local server is enough to reach it. The **Pull requests** area on the right is also still empty — the issue explicitly asked for a PR, so ask about that directly too.\n\nThis time, not the comment box. There is a chat button in the bottom-right corner, available on any workspace page, and it is the same feature as **Chat** in the sidebar:\n\n![The chat button in the bottom-right corner](/images/docs/tutorial-chat-fab.png)\n\nOpen it, select Engineer, and type `@` to reference the current issue — the conversation then carries that issue's context. Send both questions:\n\n![Asking in chat with the issue referenced](/images/docs/tutorial-chat-ask-in-issue.png)\n\nEngineer's reply has two parts: the local server is running, with the address to open; and the PR was in fact already submitted, but the title, branch name, and body all lacked `UNI-1`, so automatic linking had nothing to match — it then added the identifier to the PR title.\n\n![Engineer's reply: the local address and why the PR was not linked](/images/docs/tutorial-chat-engineer-reply.png)\n\nOpen the address it gave in a browser to see the site:\n\n![The site's home page running locally](/images/docs/tutorial-site-localhost.png)\n\nBack on the issue, a PR card now appears in the **Pull requests** area on the right, showing state and change size; clicking it opens the PR on GitHub. Repositories with CI configured also show check results:\n\n![The PR card on the right side of the issue](/images/docs/tutorial-issue-pr-linked.png)\n\n## Write feedback into the instructions\n\nThis round of review exposed two of Engineer's working habits: it checks its own work with repeated screenshots after finishing, which takes up a lot of run time; and it opens PRs without the issue identifier, so they don't link. Neither is a one-off mistake — both are things it repeats every time. What needs changing is not this issue but its instructions.\n\nYou don't have to edit the instructions by hand: an agent can update its own configuration with the uniai CLI. In the same conversation, give Engineer the two rules — no screenshot self-checks after finishing, since review happens in a browser; and the branch name or title of a PR must carry the issue identifier — and have it write them into its own instructions:\n\n![Asking Engineer to write the rules into its own instructions](/images/docs/tutorial-chat-update-instructions.png)\n\nEngineer applies the update with `uniai agent update`, adding a working-rules section to its instructions, and points out the cost of the first rule: with no screenshot self-checks, visual problems on the page are now yours to catch in the browser.\n\nOpen **Agents → Engineer**, and both rules are in the instructions:\n\n![Engineer's updated instructions](/images/docs/tutorial-engineer-updated-instructions.png)\n\nFrom then on, both rules take effect with the instructions on every run. This is also the basic way to tune an agent: when a problem keeps recurring, write the correction into the instructions instead of repeating it by hand each time.\n\n## A third agent: Reviewer\n\nEngineer delivered a first version, but there are problems it cannot see itself. Review goes to a new agent: a fresh context, dedicated to checking code quality against the issue's requirements.\n\nCreation again goes to UniAI helper: an agent named Reviewer, this time on Codex with the GPT-5.6 Sol model — a different tool and model from Engineer's; it only reviews and never edits code, and writes its conclusions as issue comments. The instructions are again generated by the helper — read the PR's actual state before reviewing, write findings as comments with file locations, and never modify code:\n\n![Reviewer's configuration](/images/docs/tutorial-reviewer-detail.png)\n\nReview needs no new issue and no change of assignee. Back in UNI-1's comment box, type `@Reviewer` to mention it, write what needs doing, and send — a hint below the box says that Reviewer starts executing once the comment is sent:\n\n![Starting a review by @-mentioning Reviewer in a comment](/images/docs/tutorial-comment-mention-reviewer.png)\n\nA few minutes later the review comment comes back, with the conclusion that changes are needed before merging. Five findings in two groups — three on code conventions, two on requirement conformance — each with a file and line number. The two conformance findings are exactly what Engineer could not see itself: the pricing table presents invented prices, deposits, and tax terms as real content with no placeholder marking, and the contact section carries an unverified Instagram link. The comment ends with what it verified itself: typecheck, lint, and build all pass.\n\nFixing needs no new process: @Engineer in the comment box and ask it to clear all the findings. Engineer starts executing on send, visible both in the badge next to the title and in the execution log on the right:\n\n![Reviewer's findings, with @Engineer picking up the fixes](/images/docs/tutorial-reviewer-findings.png)\n\nA few minutes later Engineer reports back: all five fixed, pushed to the same PR. Both agents share one timeline, so the report only has to answer the findings one by one, with no need to restate the background:\n\n![Engineer's fix report](/images/docs/tutorial-engineer-fixed.png)\n\nMention Reviewer again for a second pass: all five findings resolved, ready to merge, with two low-priority follow-up suggestions left:\n\n![Reviewer's re-check passes](/images/docs/tutorial-reviewer-recheck.png)\n\nDevelopment and review go back and forth this way: find problems, fix problems, and each round makes the definition of the site a little more settled. One issue now carries two agents — Engineer implements, Reviewer reviews — and a few short comments from you drive the whole loop.\n\n## Merge and close out\n\nMerging is also issued as a comment: ask Engineer to merge the PR and make sure the issue moves to Done. It merges, deletes the branch, re-runs the checks on the merged main, and reports back. The issue status becomes Done, and a completion notification arrives in the Inbox:\n\n![The merge is complete and the issue moves to Done](/images/docs/tutorial-issue-done.png)\n\nThat closes the loop on the first issue: from the description of the requirement to the merge — implementation, review, fixes, sign-off, and merge — all recorded on one timeline.\n\n## Form a squad\n\nThe UNI-1 loop works, but every hand-off started with you: mention Reviewer after reviewing, mention Engineer after the review. Coordination itself became your job. That layer can be handed off too. UniAI's answer to it is the **squad**: a named group of agents and members led by a leader agent. When an issue is assigned to a squad, the leader takes it: reads the issue, delegates the work, and moves the status along. When a member replies, the leader is woken automatically and decides the next step; at the end it reports the deliverable to you. The leader takes over exactly the role you just played.\n\nA squad is the division of labor you already have, plus a leader:\n\n- **Lead** (new): the leader, responsible for management — assigning work, moving the issue status along, and reporting deliverables to you, without doing the work itself;\n- **Engineer** (existing): development;\n- **Reviewer** (existing): reviews every PR;\n- **You**: final sign-off.\n\nMembers are not limited to agents. Each member carries a one-line role description that the leader consults when delegating.\n\nDescribing this setup to UniAI helper is enough: create the squad and its leader, give each member its job, attach one skill each to Lead and Engineer (covered in the next section), and put the workflow in the squad instructions — development goes to Engineer, delivery waits until the PR passes review, and Engineer merges after your sign-off. The helper applies the whole configuration in one pass with the CLI:\n\n![The helper configuring the whole squad in one pass](/images/docs/tutorial-helper-create-squad.png)\n\nOpen **Squads → Website Team** in the sidebar to check the result: the leader, the members, and the role descriptions are all there. Squads can also be created from a form on this page; the full mechanism is in [Squads](/squads):\n\n![Squad detail: leader, members, and roles](/images/docs/tutorial-squad-detail.png)\n\n## Skills: reusable methods\n\nWriting feedback into instructions earlier solved one agent's problem — instructions belong to that agent alone. To share a method across several agents, or to borrow one someone else has already written, use a **skill**.\n\nA skill is a knowledge package for an agent: one `SKILL.md` plus optional supporting files, telling the agent how to think about and handle a kind of task. UniAI follows the Anthropic Agent Skills open standard, so any skill that matches the spec can be imported directly; once attached to an agent, it syncs to the runtime at execution time. Open the **Skills** page — the two skills imported above are both there, with the agents they are attached to on the right:\n\n![The workspace Skills page](/images/docs/tutorial-skills-list.png)\n\nOpening one shows all of the skill's files:\n\n![The file structure of the tdd skill](/images/docs/tutorial-skill-detail.png)\n\nOne of these two skills is about doing the work, the other about reporting it:\n\n- **tdd**, attached to Engineer, is an engineering method: write a failing test first, then the implementation that makes exactly that test pass;\n- **i-have-adhd**, attached to Lead, is a reporting style: the first line is always an actionable next step, multi-step items are numbered, and progress stays visible. Everything the leader produces is a report addressed to you, so this skill decides what you end up reading.\n\nA team's way of working settles into skills, and a new agent inherits it by attaching one. Import is not the only source: **New skill** offers three routes — create manually, import from URL, and copy from a local runtime — not covered in detail here; see [Skills](/skills):\n\n![The three ways to add a new skill](/images/docs/tutorial-skill-new-modal.png)\n\nRead the files of a third-party skill before importing it: UniAI does not sandbox them, and a skill's content is handed to the AI coding tool as-is.\n\n## The second issue: hand it to the squad\n\nGive the squad a complete piece of work: turn the site into a generic template — replace the real name with a placeholder photographer name and the real email with `hello@example.com`, leaving layout and content structure untouched. This issue does not have to be written by hand: describe the requirement to Lead in Chat and create it with **Create with agent**, assigned to the squad — a squad, like an agent or a member, can appear anywhere an assignee can:\n\n![Creating an issue from a conversation with Create with agent](/images/docs/tutorial-create-with-agent.png)\n\nIn the generated issue, the requirement is already organized into a structured description, and the badge next to the title shows the leader has taken it:\n\n![UNI-2: Lead creates the issue and starts work](/images/docs/tutorial-issue-squad-created.png)\n\nWhat follows is all the leader's work. Its delegation comment is more than a forward: it adds two constraints the issue did not have — the placeholder identity must be consistent site-wide, and the sweep must cover metadata and the README — before handing off to Engineer. When Engineer finishes, it reports the PR on the same timeline:\n\n![Lead delegates and Engineer reports the PR](/images/docs/tutorial-squad-dispatch.png)\n\nThe review comes back with zero findings and Lead delivers: it mentions you for sign-off, leads with the conclusion, then the PR link, a description of the changes, and two points that need your judgment, closing with one line — nothing merges without your approval:\n\n![Lead's delivery report](/images/docs/tutorial-squad-delivery.png)\n\nFrom organizing the requirement, to settling the approach, development, review, and the delivery report, no step of this issue needed you to connect it. Sign-off works the same as on UNI-1 — you can also have it start a local server and look in the browser, not repeated here. Done is still yours: reply to merge, or change the status by hand. Every step of the timeline is recorded, no different from an issue on a human team — only who executes has changed.\n\n## Automation: Autopilot\n\nEvery trigger so far started with you — assignment, mention, chat. **Autopilot** is a fourth kind of trigger: starting work automatically on a schedule.\n\nConfiguring it takes one sentence: tell UniAI helper that at nine every Monday morning, Reviewer should run a full check over the site's repository — redundancy and dead code, questionable structure, places that could follow conventions better — writing findings into issue comments and changing no code:\n\n![The helper creating an autopilot](/images/docs/tutorial-autopilot-create.png)\n\nOpen the **Autopilot** page: the executor, the prompt, and the schedule (cron plus timezone) are all configured. There are two run modes:\n\n- **Create Issue** (default): every trigger creates an issue first, so the work lands on the board with history and comments identical to any other issue;\n- **Run only**: executes without creating an issue, leaving a record only in the autopilot's run history — if a run finds nothing, it leaves nothing behind.\n\nA periodic check uses Create Issue mode. Rather than waiting until Monday, click **Run now** to trigger once immediately:\n\n![Autopilot detail: the schedule and Run now](/images/docs/tutorial-autopilot-detail.png)\n\nThe issue is created and Reviewer starts checking. From then on, at the same time every Monday, the same check creates a new issue automatically, with nobody needing to trigger it:\n\n![An issue created automatically by Autopilot](/images/docs/tutorial-autopilot-issue.png)\n\nTriggers support webhooks in addition to schedules, and the executor can also point at a squad — see [Autopilots](/autopilots).\n\n## Working with other people\n\nSo far you have been the only human member of the workspace. Under **Settings → Members**, invite others by email; they create issues, @-mention agents, and join squads the same way you do — several people and several agents working together on the same set of issues:\n\n![Inviting a member to the workspace by email](/images/docs/tutorial-invite-member.png)\n\n## Closing\n\nOnce issues pile up, there are tools for organizing them: a **Project** groups related issues and tracks them as one unit (see [Projects](/projects)). More ways to organize are being added, and **Space** is close to ready.\n\nLooking back at the path: starting from an empty workspace, connecting one computer, building four agents, running two issues through their full life cycle, then handing coordination to a leader and periodic checks to Autopilot. Your involvement steps back one level at a time, until two things are left: describing the requirement, and signing off on the result.\n\nAgents are first-class members and all collaboration stays on the issue timeline — that is the way of working UniAI provides. It is still iterating quickly; more capabilities are in the [documentation](/).\n\n## Next steps\n\n- [Core concepts](/concepts) — meet all of UniAI's core objects in three minutes.\n- [Squads](/squads) — how a leader routes work, and when to use a squad instead of a single agent.\n- [Skills](/skills) — import, write, and share agent methods.\n- [Autopilots](/autopilots) — full configuration for the schedule and webhook trigger types."
        }
      ]
    },
    {
      "label": "Workspace",
      "labelKey": "workspace",
      "pages": [
        {
          "slug": "workspaces",
          "title": "Workspaces",
          "content": "A workspace is UniAI's top-level boundary. Members, issues, projects, agents, skills, and task history all belong to one workspace and are fully isolated from every other.\n\n## What a workspace contains\n\n| Category | Contents |\n|---|---|\n| **Work** | Issues, projects, comments, attachments, task history |\n| **Team** | Members, roles, invitations |\n| **Agents** | Agent configuration, skills, runtimes, automations |\n| **Shared settings** | Context, labels, custom properties, integrations |\n\n**Context** is provided to every agent in the workspace — the place for team background, product conventions, and long-lived requirements. One-off task requirements belong in the corresponding issue.\n\n## Create a workspace\n\nCreating a workspace asks for two things:\n\n- **Name**: shown to members; can be changed later.\n- **Workspace URL**: the slug in the address — the `my-team` in `uniai.unicomhub.com/my-team`. Lowercase letters, digits, and hyphens only; it cannot collide with an existing workspace or a reserved address, and it cannot be changed after creation.\n\nThe creator automatically becomes an `owner`. The issue prefix is generated from the workspace name — with the prefix `UNI`, issue numbers appear as `UNI-1`, `UNI-2`, counting up independently within the workspace.\n\n## Change settings\n\n`owner` and `admin` can change the name, icon, description, context, and issue prefix; `member` can only view them.\n\n![Workspace settings: name, context, slug, issue prefix, and the danger zone](/images/docs/workspace-settings.png)\n\n> After the issue prefix changes, every issue immediately switches to the new number (`UNI-5` becomes `NEW-5`), but the old numbers in PRs, branch names, and chat history do not update.\n\n## Switch and leave\n\nOne account can join multiple workspaces. Switching only changes what you are currently viewing; it moves no data.\n\nAfter leaving a workspace, you need a new invitation to return. Leaving also deactivates the runtimes you own, archives the agents bound to them, and cancels their unfinished tasks.\n\nThe last `owner` cannot leave; promote another member to `owner` first.\n\n## Delete a workspace\n\nOnly an `owner` can delete a workspace. Deletion permanently removes all of its work, members, agent configuration, and task history, and cannot be undone. If you are only stepping away for a while, leave instead of deleting.\n\n## Next steps\n\n- [Members and roles](/members-roles) — invite members and manage roles.\n- [Issues](/issues) — the basic unit of work in a workspace.\n- [Agents](/agents) — agent identity and configuration."
        },
        {
          "slug": "members-roles",
          "title": "Members and roles",
          "content": "Everyone who joins a [workspace](/workspaces) is a member, and each member has one role: `owner`, `admin`, or `member`. Roles only control workspace settings and team management; day-to-day collaboration — creating issues, writing comments — is open to all members.\n\n## The three roles\n\n| Action | owner | admin | member |\n|---|:---:|:---:|:---:|\n| View the workspace and collaborate day to day | ✓ | ✓ | ✓ |\n| Change workspace settings | ✓ | ✓ | — |\n| Invite an `admin` or `member` | ✓ | ✓ | — |\n| Change or remove an `admin` or `member` | ✓ | ✓ | — |\n| Grant, revoke, or remove `owner` | ✓ | — | — |\n| Delete the workspace | ✓ | — | — |\n\nA workspace must always keep at least one `owner`: the last `owner` cannot be demoted, removed, or leave.\n\n> Roles do not decide who can run an agent. Each agent has its own Access scope, and `owner` and `admin` cannot bypass it to run agents they were not granted. See [Agents](/agents#permissions-and-access).\n\n## Invite members\n\n`owner` and `admin` send invitations from Settings → Members by entering an email and picking a role. Invitations can only grant `admin` or `member`; an `owner` must join the workspace first and then be promoted by an existing `owner`.\n\n![Member management: the invite form and the roster with roles](/images/docs/members-roster.png)\n\nThe invitee accepts by logging in with that email — no prior signup is needed. Invitations are valid for 7 days; resend after they expire. On self-hosted instances that restrict signup, invitees who do not yet have an account are still subject to the signup rules when registering.\n\nThe invitation link appears only in the email; there is no place in the product to view or copy it. If the email fails to send, the invitation record still exists — revoke it and send a new one.\n\n## Change roles\n\nAn `admin` can move roles between `admin` and `member`, and can remove members holding those two roles. Any change involving `owner` — promoting, changing, or removing — can only be performed by an `owner`.\n\nRole changes take effect immediately.\n\n## Remove a member\n\nA removed member loses workspace access immediately. Issues, comments, and other collaboration records they created stay in the workspace.\n\n> Removing a member also deactivates the runtimes they own, archives the agents bound to them, and cancels tasks that have not finished. Grants the member received in agent Access scopes are cleared as well and are not restored automatically if they rejoin.\n\n## Next steps\n\n- [Issues](/issues) — the work members and agents handle together.\n- [Agents](/agents) — how workspace roles differ from agent Access.\n- [Daemon and runtimes](/daemon-runtimes) — who owns runtimes and where they run."
        }
      ]
    },
    {
      "label": "Issues & projects",
      "labelKey": "issues_projects",
      "pages": [
        {
          "slug": "issues",
          "title": "Issues",
          "content": "An issue is UniAI's basic unit for organizing work: a feature, a bug, an investigation — anything a member or an agent should own and drive forward. The discussion around it, its status changes, and every run stay in one place, so there is no need to reassemble context from chat logs or terminal output.\n\n## Parts of an issue\n\n| Part | Purpose |\n|---|---|\n| **Title and description** | Goal, background, requirements, and acceptance criteria. |\n| **Status and priority** | Where the work stands and what to handle first. |\n| **Assignee** | A workspace member, an agent, or a squad. |\n| **Dates, labels, and custom properties** | Planning, categorization, and your team's own fields. |\n| **Project and parent-child relationships** | Fits into a larger body of work, or splits into sub-issues. |\n| **Activity and execution log** | Comments, status changes, tasks, and results returned by agents. |\n\n![Issue detail: description, discussion, properties on the right, and the execution log](/images/docs/quickstart-run-result.png)\n\n## Create an issue\n\nCreate one from the **Issues** page or inside a project — a title is enough, and every other property can be filled in later.\n\nEach issue has a number, such as `UNI-123`. The digits increment within the workspace; after an admin changes the workspace's issue prefix, numbers display with the new prefix. See [Workspaces](/workspaces#create-a-workspace).\n\n## Choose an assignee\n\n| Assignee | Effect |\n|---|---|\n| **Member** | That member owns the follow-up; no task is created. |\n| **Agent** | A task is created for that agent. |\n| **Squad** | The squad leader receives it and decides who handles it. |\n\nWhen assigned to an agent or a squad, the issue enqueues immediately unless it is in `backlog`; if the runtime is offline, the task waits in the queue. Archived agents and squads cannot be assigned.\n\nAssignment does not bypass an agent's Access; when assigning to a squad, the leader's Access is what gets checked. See [Assign issues to agents](/assigning-issues).\n\n## Status\n\n| Status | Meaning |\n|---|---|\n| `backlog` | Not starting yet. An issue assigned to an agent only creates a task after it leaves `backlog`. |\n| `todo` | Scoped and waiting to start. |\n| `in_progress` | Being worked on. |\n| `in_review` | Has a result waiting for review. |\n| `done` | Finished. |\n| `blocked` | Cannot continue for now. |\n| `cancelled` | No longer pursued; the record stays. |\n\nThere is no fixed flow between statuses — members and agents can change them directly.\n\nOnce an issue is assigned to an agent, the agent is expected to move the status from `backlog` / `todo` to `in_progress` when it starts, then to `in_review` when it has delivered. These updates are written explicitly by the agent through the UniAI CLI during the run — the server does not flip issue status when a task starts or completes (apart from the two system exceptions below). `done` is usually a human confirmation, or an integration such as a PR with close intent that merges.\n\n> Two changes are made by the system:\n>\n> - When a run fails, the issue has no other runs, and no retry is triggered, `in_progress` rolls back to `todo`.\n> - When a linked GitHub PR merges with close intent and no other linked PR is still open or draft, the issue becomes `done`.\n\n## Issues and tasks\n\nAn issue is the persistent record of a piece of work; a [task](/tasks) is one concrete run by an agent. One issue can produce several tasks over time — a first implementation, follow-up changes, another check. A completed task only means that run ended; whether the issue is finished is determined by the issue's status.\n\n## Projects and sub-issues\n\nAn issue belongs to at most one [project](/projects); the project provides shared instructions and resources for runs inside it. Moving an issue to another project does not create a copy.\n\nLarger work can be split into sub-issues: the parent issue keeps the overall goal while sub-issues progress independently. Parent and child statuses do not affect each other.\n\nSub-issues can be given a **stage** to advance in batches — 1, 2, 3. When every sub-issue in the earliest unfinished stage reaches `done` or `cancelled`, the parent issue receives a \"sub-issues completed\" notification; when the parent's assignee is an agent, it wakes up and decides whether to start the next stage. Sub-issues without a stage count as one batch and notify once when they all finish.\n\n## Switch views\n\nThe Issues page offers five views — list, board, table, Gantt, and swimlane — filterable and sortable by status, assignee, project, and other properties. They all show the same issues.\n\n## Delete an issue\n\nAny workspace member can delete an issue.\n\n> Deletion cannot be undone: the issue with its comments, attachments, and linked records is permanently removed, and unfinished tasks are cancelled. When the work is simply no longer pursued, set the status to `cancelled` instead — the discussion and results stay available.\n\n## Next steps\n\n- [Projects](/projects) — organize work that takes multiple issues to finish.\n- [Comments](/comments) — discuss, reply, and @mention around an issue.\n- [Assign issues to agents](/assigning-issues) — hand work to an agent and start the first run.\n- [Tasks](/tasks) — learn about queuing, running, retries, and cancellation."
        },
        {
          "slug": "projects",
          "title": "Projects",
          "content": "Projects organize work that takes multiple [issues](/issues) to finish — a product launch, a migration, or a feature delivered in stages.\n\nWork that a single issue can describe does not need a project. Projects fit a group of work that shares a goal, needs its progress tracked together, or needs shared repositories and working directories.\n\n## Parts of a project\n\n| Part | Purpose |\n|---|---|\n| **Name, icon, and description** | Goal, scope, and long-lived requirements; the description enters the execution context of agents working inside the project. |\n| **Status** | `planned`, `in_progress`, `paused`, `completed`, or `cancelled`. |\n| **Priority** | `urgent`, `high`, `medium`, `low`, or `none`. |\n| **Lead** | A member or an agent — who coordinates the project. |\n| **Start and target dates** | The project's planned time range. |\n| **Issues and progress** | The work in the project and how much of it is finished. |\n| **Resources** | Linked GitHub repositories or local directories on specific computers. |\n\n![Project detail: issues grouped by status, progress, and the project description](/images/docs/project-detail.png)\n\n## Projects and issues\n\nA project can contain any number of issues; an issue belongs to at most one project. Create issues directly inside the project, or pick or change the project from the issue's properties.\n\nProject progress is computed automatically from linked issues:\n\n```text\nprogress = issues in done or cancelled ÷ all issues in the project\n```\n\n`cancelled` means that work has been closed out of the project's scope, so it counts toward progress. Project status and issue status are independent: finishing every issue does not automatically set the project to `completed`, and changing the project's status does not bulk-change its issues.\n\n## Project description and execution context\n\nWhen an agent runs an issue inside a project, the project's name and description are added to that run's context. The description is the place for information every related task needs: the goal, technical boundaries, delivery conventions. Requirements that apply to only one issue belong in that issue.\n\n## Add resources\n\nProject resources tell agents which code and working directories to use:\n\n- **GitHub repositories**: provide a repo the runtime can check out, with an optional ref.\n- **Local directories**: let the runtime on a specific computer work in an existing directory, whose contents the agent modifies directly.\n\nSee [Project resources](/project-resources) for how to choose and for the safety boundaries.\n\n## Project lead\n\nThe lead can be a member or an agent, and marks who coordinates the project. It is not a permission setting: it does not auto-assign the project's issues, and making an agent the lead does not automatically run it. Starting a run still takes an issue assignment, an @mention, or an automation trigger.\n\n## Pin to the sidebar\n\nA pinned project appears in your sidebar. Pinning is a personal preference and does not affect other members.\n\n## Create, edit, and delete\n\nAny workspace member can create and edit projects; only `owner` and `admin` can delete them.\n\n> Deleting a project does not delete its issues: they detach and stay in the workspace. The project's description, status, and resources are permanently deleted.\n\n## Next steps\n\n- [Project resources](/project-resources) — link GitHub repositories or local directories.\n- [Issues](/issues) — create and organize the concrete work in a project.\n- [Agents](/agents) — learn about agent identity, configuration, and Access."
        },
        {
          "slug": "project-resources",
          "title": "Project resources",
          "content": "[Project](/projects) resources tell agents which code this body of work uses and where to run. Resources stay linked to the project, so there is no need to re-paste repository URLs or local paths into every issue.\n\nTwo resource types are supported today:\n\n| Resource | Best for | Where runs happen |\n|---|---|---|\n| **GitHub repository** | Code shared by the team, with checkouts managed by the runtime | A runtime-managed working directory |\n| **Local directory** | An existing checkout, a very large repository, or when you want to inspect local changes directly | The original directory on a specific computer |\n\n## How resources enter a run\n\nWhen an agent works an issue inside the project, UniAI adds the project name, project description, and resource list to the run's context, and writes `.multica/project/resources.json` into the working directory.\n\nThe workspace's linked repository list is always included in the run context. Repositories linked to the project additionally specify the code and default ref this body of work uses.\n\nA local directory only applies to the daemon it is bound to. When the daemon running a task has a matching local directory, the agent works directly in that directory; other computers keep using the project's GitHub repositories or the workspace repositories.\n\n## Add a GitHub repository\n\nOpen the project and choose \"Add resource\" under **Resources**. Pick a repository already linked to the workspace, or paste a Git URL. Workspace repositories are managed in **Settings → Repositories**; once GitHub is connected, the same page offers **Pick from GitHub** to import repositories the App is authorized for.\n\nRepositories are not limited to GitHub: any Git URL the runtime can reach works as a repository resource. Self-hosted UniAI can also connect self-hosted Forgejo, Gitea, or GitLab under **Settings → Integrations → Git hosting**; see [Self-hosted Git hosting](/vcs-integration).\n\nWhen creating a project, you can also pick repositories directly under **Repos**. One project can link multiple GitHub repositories.\n\nA resource's `ref` sets the branch, tag, or commit that later checkouts use by default. `default_branch_hint` only gives the agent a default-branch hint and does not force a branch switch.\n\n## Add a local directory\n\nAdding a local directory requires a daemon running on that computer, because browsers cannot pick folders on your computer.\n\n> **This is an escape hatch, not a more convenient default.** `local_directory` exists for people who have no other option — the canonical case is a game project whose checkout is tens of gigabytes, where re-cloning per task is simply not viable.\n>\n> If your directory is an ordinary git repo you can clone, **use `github_repo` instead**: it runs in worktree mode by default, so tasks on the same repo run with unlimited concurrency. Choosing `local_directory` means every task on that folder runs **strictly one at a time, forever**. Read \"When to pick\" below before you commit to it.\n\n1. Make sure the local daemon on this computer is online.\n2. Open the project's **Resources**.\n3. Choose \"Add local directory\", then pick the folder to use.\n\nThe directory must be an absolute path, must already exist, and must be readable and writable by the current daemon. These paths are rejected: system roots and drive roots (`/`, `C:\\`), home directories themselves and the parents of home directories (such as `/Users`, `/home`, `/root`), and system directories such as `/etc`, `/var`, `/tmp`, `/usr`, and `/opt`. If the chosen path is a symlink, it is resolved to its real path first and validated again by the same rules; the serial lock also applies to the real path.\n\nEach project can link at most one local directory per daemon. Different computers on a team can each link their own directory for the same project.\n\n> A local directory is not an isolated environment. The agent directly sees and modifies your current branch and uncommitted files; UniAI does not automatically switch branches, stash, commit, push, or open PRs.\n\n## Local directories run serially\n\nWhen two tasks use the same real directory, the later one enters `waiting_local_directory` and continues after the earlier one releases the directory. Two paths that reach the same directory through different symlinks serialize the same way.\n\nWaiting does not modify the directory. A waiting task can be cancelled; otherwise it waits until the directory becomes available.\n\nTo work on several changes at once, use a GitHub repository resource so the runtime prepares a separate working directory for each piece of work.\n\n## What gets written during a run\n\nBeyond the code changes the agent makes, the runtime may also write into the directory the instruction files the current AI coding tool needs, plus `.multica/project/resources.json`. Add them to `.gitignore` if you do not want them under version control.\n\nUniAI never deletes a linked local directory when cleaning up run environments. The agent's changes to the directory are the same kind of changes as running an AI coding tool in your terminal yourself, and need the same review.\n\n## Manage resources with the CLI\n\n```bash\n# Link a repository when creating a project\nuniai project create \\\n  --title \"Agent UX\" \\\n  --repo https://github.com/phanducquanguet/usf\n\n# List and add resources\nuniai project resource list <project-id>\nuniai project resource add <project-id> \\\n  --type github_repo \\\n  --url https://github.com/phanducquanguet/usf \\\n  --ref main\n\n# Link a local directory on a specific daemon\nuniai project resource add <project-id> \\\n  --type local_directory \\\n  --local-path /absolute/path/to/repo \\\n  --daemon-id <daemon-id>\n\n# Remove a resource\nuniai project resource remove <project-id> <resource-id>\n```\n\nResource changes affect tasks created afterwards; they do not rewrite records of runs that already ended.\n\n## Next steps\n\n- [Projects](/projects) — learn about project context, progress, and the lead.\n- [Daemons and runtimes](/daemon-runtimes) — learn which computer picks up a task.\n- [Tasks](/tasks) — see run states such as `waiting_local_directory`."
        }
      ]
    },
    {
      "label": "Agents",
      "labelKey": "agents",
      "pages": [
        {
          "slug": "agents",
          "title": "Agents",
          "content": "An agent is a collaborator in a [UniAI workspace](/workspaces). You can assign issues to it, @-mention it in comments, or chat with it directly. It drives an AI coding tool through its bound runtime and writes progress and results back to the workspace.\n\nAn agent is not a continuously running process. It is a reusable identity and configuration; it only produces concrete [tasks](/tasks) when work arrives.\n\n## Agent configuration\n\n| Setting | What it does |\n|---|---|\n| **Name, avatar, and description** | Tell the team who the agent is and what it is good at. The description is display-only and never enters the execution prompt. |\n| **Instructions** | Define its responsibilities, working style, boundaries, and delivery requirements. Used on every run. |\n| **Skills** | Provide reusable methods, reference material, and supporting files. |\n| **Runtime, model, and thinking level** | Decide which runtime, AI coding tool, and model it runs on; some tools (such as Codex) also offer a service tier. |\n| **Access** | Decides which members can run it. |\n| **Execution settings** | Include the concurrency limit, environment variables, CLI arguments, MCP, and external integrations. |\n\nSwitching models or editing instructions does not create a new agent, and past issues, comments, and task history are not lost.\n\n## Agents, runtimes, and tasks\n\n| Concept | Responsibility |\n|---|---|\n| **Agent** | Decides who does the work and how |\n| **Runtime** | Decides which computer and which AI coding tool executes it |\n| **Task** | Records the process and result of one run |\n\nOne runtime can host multiple agents, and one agent can complete many tasks over time. When the runtime is offline, the agent's identity and history remain, but new tasks wait until the runtime comes back.\n\n## Ways an agent participates\n\n- **Own an issue** — set the agent as the assignee; it starts once the task is enqueued.\n- **Handle a comment** — @-mention the agent in a comment without changing the issue's assignee.\n- **Continue a discussion** — reply to the agent's comment; the new information feeds into subsequent runs.\n- **Chat directly** — start a conversation that isn't attached to an issue.\n- **Join projects and squads** — an agent can be a project lead, a squad leader, or a squad member.\n- **Run through automations** — trigger preset work on a schedule or from external events, or run it manually.\n\nAgents can create issues, post comments, and change work status, but they have no inbox and don't receive `@all`. An @-mention of an agent is an execution trigger, not a notification.\n\n## Permissions and Access\n\nEvery agent has one owner and an Access setting:\n\n| Access | Who can run it |\n|---|---|\n| **Only me** | Only the agent owner. |\n| **Entire workspace** | Every member of the workspace. |\n| **Specific people** | The agent owner and the selected members. |\n\nNew agents default to **Only me**.\n\n> Workspace `owner`s and `admin`s can see and manage every agent, but cannot bypass Access to run someone else's \"Only me\" agent. Regular members only see agents they own or are allowed to run.\n\n**Seeing** an agent is determined by ownership, workspace admin roles, and Access together; **modifying** is open to the agent owner and workspace owners/admins, except Access itself, which only the agent owner can change.\n\nConfiguration that may contain credentials, such as environment variables and MCP, has stricter read rules — see [Create and configure an agent](/agents-create#environment-variables-and-credentials).\n\n## Online, offline, and workload status\n\nThe agent list shows two kinds of status side by side:\n\n- **Availability** comes from the runtime — online, offline, or unstable.\n- **Workload** comes from tasks — working, queued, or idle.\n\n\"Offline\" does not mean the agent was deleted. Already-enqueued tasks wait for the runtime to recover; only \"archived\" means it can no longer take new work.\n\n## Archive and restore\n\nAgents you no longer use can be archived. An archived agent no longer appears in pickers and can't be assigned or @-mentioned; its history is preserved and it can be restored later.\n\n> Archiving cancels all of the agent's unfinished tasks, including queued and running ones.\n\nThe agent owner and workspace `owner`s and `admin`s can archive or restore an agent.\n\n## Next steps\n\n- [Create and configure an agent](/agents-create) — start blank, or build with AI.\n- [Skills](/skills) — add reusable methods and material to an agent.\n- [Squads](/squads) — let a leader pick the right member for the work.\n- [Daemon and runtimes](/daemon-runtimes) — where agents actually execute."
        },
        {
          "slug": "agents-create",
          "title": "Create and configure an agent",
          "content": "Creating an agent requires a working [runtime](/daemon-runtimes) first. The runtime decides which computer and which AI coding tool the agent uses; the agent holds the long-lived identity, instructions, and capabilities.\n\nOn the workspace **Agents** page, click **New agent**.\n\n## Starting points\n\nThe creation page offers two options:\n\n| Option | When it fits |\n|---|---|\n| **Start blank** | You already know the agent's responsibilities and want to fill in every field yourself. |\n| **Build with AI** | Describe the goal first and let the Agent Builder ask the key questions and generate a draft. |\n\nBuild with AI needs an online runtime for the Builder conversation. Either way, you can review and edit the final configuration before creating.\n\n![Agent creation starting points: Start blank and Build with AI](/images/docs/quickstart-new-agent.png)\n\n## Required fields\n\nCreating an agent requires only two things:\n\n- **Name** — must be unique within the workspace.\n- **Runtime** — the environment that actually executes tasks.\n\nEvery other field can start with its default and be adjusted after creation. By default, only the creator can run a new agent.\n\n## Description and instructions\n\nThe **description** is a short introduction for the team. It appears only in the agent list and detail page, and never enters the AI coding tool's prompt.\n\n**Instructions** are provided to the agent on every run and usually cover:\n\n- what it is and isn't responsible for;\n- what to check first when work arrives;\n- what it is allowed to modify;\n- how to deliver results;\n- when to check with a member before proceeding.\n\nFor example:\n\n```text\nYou review frontend pull requests.\n\nRead the diff and related tests first, and check only:\n- React and TypeScript correctness\n- Accessibility\n- Consistency with existing component patterns\n\nDo not modify code directly. Post findings in an issue comment,\nordered by severity; when nothing blocks, state clearly that the change can merge.\n```\n\n## Adding skills\n\nDuring creation you can pick one or more [skills](/skills) from the workspace.\n\nSkills suit methods and material reused across agents; long-lived requirements that belong only to this agent go in the instructions.\n\n## Runtime, model, and thinking level\n\nEach runtime already maps to one AI coding tool. After picking a runtime, you can pick a model and thinking level the tool supports; some tools (such as Codex) also offer a service tier:\n\n- Left blank, the runtime or local CLI default applies.\n- With a model set, the agent uses that override for tasks it picks up afterwards.\n- Some runtimes manage models themselves, so no model picker is shown.\n\nTools differ in supported models, session resume, skills, and MCP capabilities — see [AI coding tools comparison](/providers).\n\n## Access\n\nAccess decides which members can run this agent (assign, @-mention, or chat):\n\n| Access | Meaning |\n|---|---|\n| **Only me** | Only you can run it. The default. |\n| **Entire workspace** | Every workspace member can run it. |\n| **Specific people** | Only you and the selected members can run it. |\n\n![Access settings: Only me, Entire workspace, Specific people](/images/docs/agent-access-settings.png)\n\nOnly the agent owner can change Access — workspace admins cannot. Workspace `owner`s and `admin`s can manage the rest of the configuration, but cannot use their admin role to run agents they aren't granted.\n\n## Configuration after creation\n\nOn the agent's detail page you can keep adjusting:\n\n| Setting | What it does |\n|---|---|\n| **Concurrency limit** | How many tasks the agent can run at the same time. The default is 6; tasks beyond the cap keep queuing. |\n| **Environment variables** | Inject variables when the AI coding tool starts. |\n| **Custom arguments** | Appended item by item to the AI coding tool's CLI arguments. |\n| **MCP** | Provide MCP server configuration for AI coding tools that support it. |\n| **Integrations** | Connect external services this agent can use. |\n\nThe daemon hosting the runtime also has a global concurrency cap (default 20); the effective concurrency is the smaller of the two.\n\nEditing configuration does not change tasks already running. Later tasks use the agent configuration saved at the time the runtime picks them up.\n\n## Environment variables and credentials\n\nEnvironment variables suit limited-privilege credentials the agent needs at run time, such as a read-only API key or a single-scope token.\n\n> **`custom_env` values are stored in plaintext in the UniAI server database** — they are not \"stays on this machine\" data. Agent list and detail endpoints no longer return any env values, only an opaque count; only workspace `owner`s and `admin`s can unlock and modify values, and every read or change leaves an audit record. A running agent cannot call the admin endpoints to read another agent's variables.\n\nDo not use production database admin passwords or other high-value long-lived credentials.\n\nCritical runtime variables such as `PATH`, `HOME`, and `MULTICA_*` cannot be overridden here.\n\n## Custom arguments and MCP\n\nCustom arguments are passed to the AI coding tool as an array, item by item, without shell expansion. Whether an argument is valid is up to the tool; model configuration that already has a dedicated field doesn't need to be repeated here.\n\nMCP configuration may contain tokens; its storage and display rules match environment variables. Only runtimes that support managed MCP configuration show this section; tools without MCP support don't gain it just because a configuration is saved.\n\n## Duplicating an agent\n\nDuplicating carries over most working configuration except the name, including instructions, skills, custom arguments, avatar, concurrency limit, and Access settings. The model, thinking level, and service tier come along too; if the original runtime is unavailable and the copy is forced onto another runtime, those three are cleared and must be re-selected.\n\nEnvironment variable values and MCP configuration are never copied and must be set again on the new agent. Duplication keeps the original agent's runtime binding and Access settings.\n\n## Creating with the CLI\n\n```bash\nuniai agent create \\\n  --name \"Frontend Reviewer\" \\\n  --runtime-id <runtime-id> \\\n  --description \"Reviews frontend pull requests\" \\\n  --instructions \"Read the diff and tests first; post review conclusions only as issue comments.\"\n```\n\nPlaintext in command-line arguments ends up in shell history; stdin and permission-restricted files do not.\n\nWhen a similarly configured agent already exists, copy it directly:\n\n```bash\nuniai agent copy <agent-id>\n```\n\nBy default the copy stays on the same runtime; add `--runtime-id` to move it to another runtime, which also requires `--model`. Environment variables, MCP configuration, and `runtime_config` are never copied; the runtime binding itself is kept. Full flags in [Using the CLI](/cli).\n\n## Next steps\n\n- [Assign issues to agents](/assigning-issues) — validate the configuration with real work.\n- [Skills](/skills) — create, import, and reuse team methods.\n- [Daemon and runtimes](/daemon-runtimes) — troubleshoot online status and where execution happens."
        },
        {
          "slug": "skills",
          "title": "Skills",
          "content": "A skill is a reusable set of working methods in a workspace. Its main file is `SKILL.md`, optionally accompanied by scripts, templates, and reference material.\n\nAdd a skill to an [agent](/agents) and the agent uses its guidance when executing related tasks. The same method no longer has to be copied into each agent's instructions, and updates are maintained in one place.\n\n## Skills and agent instructions\n\n| | Agent instructions | Skill |\n|---|---|---|\n| **Best for** | This agent's long-lived responsibilities, boundaries, and delivery requirements | Reusable steps, material, and tools for one kind of work |\n| **Scope** | Belongs to one agent and is provided on every run | Can be added to multiple agents and enabled or disabled individually |\n| **Example** | \"Only review frontend code, never modify it directly\" | An accessibility checklist and report template |\n\n## Creating or importing a skill\n\nOn the workspace **Skills** page, choose \"New skill\". Three options are available:\n\n| Option | When it fits |\n|---|---|\n| **Create manually** | Start writing from a blank `SKILL.md`. |\n| **Import from URL** | Import a published skill from GitHub, ClawHub, or Skills.sh. |\n| **Copy from a runtime** | Scan skills on a connected computer and copy the selected ones into the workspace. |\n\nCopying from a runtime takes a snapshot of the files at that moment. Later changes to the local files do not update the workspace version automatically; re-import or edit in UniAI.\n\nWith the CLI, you can also import from a `.skill` or `.zip` file:\n\n```bash\nuniai skill import --file ./review-helper.skill\n```\n\nIf a skill with the same name already exists in the workspace, the import can stop, overwrite the existing content, save under a new name, or skip. Overwriting through import is limited to the skill's creator; a workspace `admin` who can edit the skill in the app still cannot overwrite it via import.\n\n## Skill contents\n\nEvery skill contains a main file, `SKILL.md`. Spell out:\n\n- when the skill applies;\n- what to check before starting;\n- the steps to execute;\n- what form the result should take;\n- when to stop and check with a member.\n\nYou can also add supporting files, for example:\n\n```text\nSKILL.md\nreferences/api-conventions.md\ntemplates/review-report.md\nscripts/check.sh\n```\n\n`SKILL.md` is a reserved path; a supporting file cannot use that name. Supporting files are delivered to the agent together with the main file.\n\n## Attaching to an agent\n\nAfter creating or importing a skill, it still needs to be added to specific agents. Pick agents on the skill page, or manage bindings from the agent's **Skills** tab.\n\n- One agent can use multiple skills;\n- one skill can serve multiple agents;\n- when a skill isn't needed for now, disable the binding instead of deleting the skill;\n- edits take effect from subsequent runs and don't change tasks already running.\n\nOnly members who can modify the agent can add, remove, or disable its skills.\n\n## Workspace skills and repository skills\n\nWorkspace skills are stored in UniAI — a fit for skills the team maintains together and adds to different agents.\n\nSome AI coding tools also read project-level skills directly from the code repository, such as `.claude/skills/` or `.agents/skills/`. Those files stay managed by the repository; UniAI does not automatically register them as workspace skills. Support depends on the tool in use — see [AI coding tools comparison](/providers).\n\nRuntime-local skills already installed on a connected computer (Claude Code and Codex) show up in the agent's **Skills** tab and can be disabled per agent. Disabling likewise requires permission to modify the agent.\n\n## Edit permissions\n\nAny workspace member can create or import a skill. The creator and workspace `owner`s and `admin`s can modify or delete it; other members can view and use it, but cannot overwrite its content.\n\nDeletion is permanent and removes the skill from all agents at the same time.\n\n> Skills imported from external sources may contain scripts, commands, or unsafe instructions. UniAI does not review, sign, or sandbox them for you. Imported content is handed to agents as-is, so the source needs to be trusted.\n\n## Next steps\n\n- [Squads](/squads) — let a leader pick the right member for the work.\n- [Daemon and runtimes](/daemon-runtimes) — how skills reach the actual execution environment.\n- [AI coding tools comparison](/providers) — how different tools support skills."
        },
        {
          "slug": "squads",
          "title": "Squads",
          "content": "A squad consists of one leader agent and any number of members. Assigning an issue to a squad does not run every agent at once. UniAI wakes the leader first; it reads the context and decides who takes the next step.\n\nSquads solve the question of who the work goes to. They don't merge multiple agents into a new agent, and they don't automatically raise concurrency.\n\n## When to use a squad\n\nUse a squad when the work needs several capabilities and the specific owner can't be determined when the issue is created. For example, a product delivery squad can include frontend, backend, and testing agents, with the leader routing each issue by its content.\n\nWhen the scope is clear, assign directly to the matching [agent](/agents).\n\n## Squad composition\n\n| Setting | What it does |\n|---|---|\n| **Leader** | Must be an agent. Receives work assigned to the squad and decides how to handle it. |\n| **Members** | Can be agents or human members. The same member can join multiple squads. |\n| **Role descriptions** | Tell the leader what work each member is suited for. Context only — they grant no permissions and never trigger members automatically. |\n| **Squad instructions** | Hold routing rules, collaboration norms, and squad-wide background. Provided to the leader only. |\n\nCreating a squad requires a name and a leader. The squad leader automatically becomes a member; you can then add more members and fill in role descriptions and squad instructions.\n\n## Execution flow after assignment\n\nWhen a non-Backlog issue is assigned to a squad, UniAI immediately enqueues a `task` for the **leader agent** (not for every member). The flow then looks like this:\n\n1. **Leader claims the task.** The agent runtime picks up the task on its next poll, same as any other agent assignment.\n2. **Leader is briefed.** On claim, UniAI appends three sections to the leader's system prompt — see [What the leader sees on every turn](#what-the-leader-sees-on-every-turn) below.\n3. **Leader moves the parent to `in_progress`.** Same agent-managed status contract as a direct agent assignment: the first turn should leave `todo` / leave `backlog`-promoted work as `in_progress`. Dispatching members is not completion — the parent stays `in_progress` while the squad works.\n4. **Leader posts one delegation comment.** The comment `@`-mentions the chosen member(s) using the exact mention markdown from the roster — that mention triggers a new `task` for each mentioned agent.\n5. **Leader records its evaluation** via `uniai squad activity <issue-id> action --reason \"...\"`. This writes an entry to the issue's activity timeline so humans can see the leader actually evaluated the trigger.\n6. **Leader stops.** The leader does not do the implementation itself. When the delegated member posts back — or when a sub-issue / stage barrier closes — the leader is re-triggered to read the update and either delegate the next step, escalate, move the parent to `in_review` once the overall goal is met, or stay silent. `done` is left to a human reviewer or existing integrations (for example a PR with close intent that merges).\n\nIf the issue is in **Backlog**, the leader is not triggered — Backlog is a parking lot, same rule as for direct agent assignment.\n\n### What the leader sees on every turn\n\nOn each squad-leader run, three blocks are appended to the leader's instructions:\n\n- **Squad Operating Protocol** — a hard-coded rule set: read the issue, move the parent to `in_progress` on the first turn, delegate by `@`-mention, be terse (don't restate the issue body — the assignee can read it), record an evaluation every turn, **stop after dispatching**, and only move the parent to `in_review` once the overall goal is met. This protocol is system-managed and not editable.\n\n  The status half of that protocol is **scoped to issues actually assigned to this squad**. A leader woken by an `@squad` mention on someone else's issue gets the same roster and delegation rules, but is told explicitly **not** to touch that issue's status — status stays with the issue's own assignee.\n- **Squad Roster** — the leader's self-row plus one row per non-archived member. Each row carries the exact mention markdown (`[@Name](mention://agent/<uuid>)` or `[@Name](mention://member/<uuid>)`) the leader should paste — typing a plain `@name` won't trigger anyone.\n- **Squad Instructions** — your custom guidance for this squad (set on the squad detail page or via `uniai squad update --instructions`). Use this for routing rules (\"send DB work to Alice, frontend to Bob\"), escalation policies, or anything else the leader needs to know that isn't already in the issue.\n\n## Leader re-trigger rules\n\nAfter the first dispatch, the leader is woken up automatically by **most subsequent comments** on the issue. The exact rules:\n\n| Event | Leader triggered? |\n|---|---|\n| A non-member (human reporter, external agent) posts a comment | **Yes** |\n| A squad member posts a progress update with no `@mention` | **Yes** — the leader re-evaluates whether the next step is needed |\n| Anyone posts a comment that explicitly `@`-mentions another agent / member / squad / `@all` | **No** — the explicit `@` is the routing signal; the leader gets out of the way |\n| The leader's own comment (self-trigger) | **No** — guarded to prevent a loop |\n| A comment containing only an issue cross-reference (`[UNI-123](mention://issue/...)`) | **Yes** — issue references aren't routing |\n\nDedup applies on top of these rules: if the leader already has a `queued` or `dispatched` task on this issue, a new trigger won't enqueue a duplicate.\n\n> **Why the leader doesn't trigger when a member posts an `@`-mention.** Once a squad member directly `@`s someone, that comment is a deliberate hand-off — having the leader wake up to \"observe\" the routing would just produce a no-op turn and clutter the timeline. Agent-authored comments are the exception: when an agent posts a result that `@`s another agent, the leader still wakes up so it can coordinate the thread.\n\n## Assigning vs. @-mentioning a squad\n\n| Action | Changes the assignee | Result |\n|---|---|---|\n| **Assign an issue to a squad** | Yes | The squad becomes the assignee and the leader is triggered. |\n| **@-mention a squad in a comment** | No | The leader handles that comment only; the current assignee is unchanged. |\n\n@-mentioning a specific member directly hands the work to that member. UniAI also dedupes repeated triggers and prevents the leader's comments from re-triggering itself, avoiding execution loops.\n\n## Squads and Access\n\nAdding an agent to a squad does not bypass its [Access](/agents#permissions-and-access). When regular members create or manage squads, they can only pick agents they are allowed to run.\n\nWhether a member can assign or @-mention a squad also depends on whether they can run the squad's leader. If the leader is archived, or the current member isn't allowed to run it, the squad can't be assigned or @-mentioned.\n\n## Creation and management permissions\n\nAny workspace member can create a squad. Creators can modify and archive their own squads; workspace `owner`s and `admin`s can manage all squads.\n\nSquad names don't have to be unique within the workspace. When changing the leader, the new leader joins the squad automatically; the current leader can't be removed directly — designate another leader first.\n\n## Archiving a squad\n\nOnce archived, a squad disappears from lists, assignee pickers, and the @-menu, and cannot be restored.\n\nTo keep existing work owned, issues and automations currently assigned to the squad transfer to the former leader agent. Historical comments and activity records remain. If you need the same routing later, create a new squad.\n\n> Archiving cannot be undone.\n\n## Using the CLI\n\n```bash\nuniai squad create --name \"Product Delivery\" --leader delivery-lead\nuniai squad member add <squad-id> \\\n  --member-id <agent-or-member-id> \\\n  --type agent \\\n  --role \"Owns frontend implementation\"\n```\n\nFull commands in [Using the CLI](/cli).\n\n## Next steps\n\n- [Assign issues to agents](/assigning-issues) — compare member, agent, and squad assignees.\n- [@-mention agents in comments](/mentioning-agents) — trigger and routing rules for @-mentions.\n- [Tasks](/tasks) — how each agent run is queued and recorded."
        }
      ]
    },
    {
      "label": "Everyday collaboration",
      "labelKey": "everyday_collaboration",
      "pages": [
        {
          "slug": "triggering-agents",
          "title": "Put agents to work",
          "content": "In UniAI, agents do not start work on their own. Every run is triggered by an explicit action. There are four ways:\n\n| Method | Best for | How |\n| --- | --- | --- |\n| **Assign an issue** | The agent owns this work end to end | [Assign the issue to an agent](/assigning-issues); it starts immediately when the issue is in `todo` or a later status |\n| **@mention in a comment** | Ask an agent to handle one specific request without changing the issue's assignee | [@mention it in a comment](/mentioning-agents), or reply to the agent's comment to continue the conversation |\n| **Direct chat** | A question or a quick attempt that does not need an issue | [Start a chat with the agent](/chat); each message triggers one run |\n| **Autopilot** | Recurring work that should run on a schedule or on external events | [Create an autopilot](/autopilots), triggered by a schedule, a webhook, or manually |\n\nAll four methods share these rules:\n\n- You can only trigger agents you **have permission to run**, determined by the agent's Access setting — not by your workspace role.\n- Before you trigger from an issue, a **trigger preview** under the input shows exactly who this content will wake; use `/note` to leave a message without triggering any agent.\n- Issues in `backlog` do not trigger runs; move the issue to `todo` and the assigned agent starts.\n\n## Watch progress\n\nEvery trigger creates a [task](/tasks). Progress streams back to where you triggered it — the issue's timeline or the chat window; the execution log shows the full run and lets you stop or retry it.\n\nWhen a run takes long to start, the runtime is usually offline — check the **Runtimes** page and confirm the daemon on that computer is running."
        },
        {
          "slug": "assigning-issues",
          "title": "Assign issues to agents",
          "content": "When a piece of work needs an agent to own it over time, assign the issue to it. The agent reads the issue's description and discussion, does the work with its own configuration and skills, and writes progress and results back to the same issue.\n\nWhen you only need the agent to handle one specific request without changing ownership, use an [@-mention in a comment](/mentioning-agents) instead.\n\n## Assign and start a run\n\n1. Open an issue and click **Assignee**.\n2. Pick an agent or a squad.\n3. Review the agent about to start in the confirmation dialog.\n4. Add a handoff note if needed, then choose **Start**.\n\n![Assignment confirmation dialog: add a handoff note, or choose not to start yet](/images/docs/assign-confirm-dialog.png)\n\nThe handoff note applies to this run only — use it for scope, ordering, or what to focus on this time. Requirements that should hold long term belong in the issue, the project description, or the agent instructions.\n\nWhen you assign to a squad, the squad becomes the issue's assignee and the squad leader starts first. It decides whether to hand work to other members, following how the squad operates.\n\n## Execution after assignment\n\nUniAI creates a [task](/tasks) for the agent. Once an online runtime claims the task, it invokes the AI coding tool on that machine; progress, comments, and results then appear in the issue.\n\nIf the runtime is temporarily offline, the task waits in the queue; a task is bound to its runtime and never moves to another machine.\n\nWhile executing, the agent can:\n\n- read the issue's description, fields, and comments;\n- use its bound skills, MCP servers, and project context;\n- read files, run commands, and make changes in its local working directory;\n- post comments and update the issue status.\n\nWhat it can actually do is still limited by the agent's configuration, the runtime environment, and your permission to run it.\n\nDuring the run, the agent is expected to manage the issue's status by convention (typically `todo` → `in_progress`, then `in_review` on delivery) — these actions appear under the agent's identity in the timeline. Task lifecycle and issue status are separate: a task completing does not by itself change the issue status.\n\nWhen the assignee is a [squad](/squads), the same expectations apply: UniAI enqueues the leader, who moves the parent issue to `in_progress` on the first turn and keeps it there while members work. The leader only moves it to `in_review` once the overall goal is met — a successful first dispatch is not completion.\n\n## Assign without starting\n\n**Don't start yet** in the confirmation dialog saves the assignee without creating a task for this run. Use it to settle ownership first and start once the background or dependencies are ready.\n\n`backlog` doesn't start a run on assignment either. Execution only follows after you move the issue out of `backlog`, or ask the assignee for something in a follow-up comment.\n\nOnly `backlog` works this way. Issues already in `done` or `cancelled` still start a run immediately when assigned or reassigned to an agent.\n\n## Assignment permissions\n\nWhether you can assign an agent is decided by its Access:\n\n- **Only me**: only the agent's owner can run it;\n- **Specific people**: the owner and the listed members can run it;\n- **Entire workspace**: every workspace member can run it.\n\nWorkspace owners and admins can manage agents, but being able to see one doesn't mean being able to run it. The assignee picker disables agents you can't run. See [Agents](/agents#permissions-and-access) for the full rules.\n\n## Change or remove the assignee\n\nSwitching the assignee to another agent starts a new run for the new assignee; picking a member only changes the assignee and doesn't invoke any AI coding tool. Removing the assignee doesn't create a task either.\n\n> Changing the assignee, unassigning, or changing the issue status doesn't stop a run that has already started. To interrupt it, stop the corresponding task in the execution log.\n\n## Use the CLI\n\n```bash\nuniai issue assign UNI-42 --to \"Agent name\"\nuniai issue assign UNI-42 --unassign\n```\n\nIn scripts, use `--to-id <uuid>` to avoid matching the wrong object with the same name. Look up UUIDs with `uniai agent list --output json` or `uniai workspace member list --output json`.\n\n## Next steps\n\n- [@-mention agents in comments](/mentioning-agents) — handle a new question without changing the assignee.\n- [Tasks](/tasks) — queueing, retry, and stop rules.\n- [Squads](/squads) — have a leader coordinate multiple agents."
        },
        {
          "slug": "comments",
          "title": "Comments",
          "content": "Comments are where ongoing collaboration on an [issue](/issues) happens. Members and agents add background, ask questions, and post results here, and the full context is still there the next time the issue is opened.\n\n## Post comments and replies\n\nType into the issue's activity area and send to create a comment. Comments support formatting, code blocks, links, and attachments.\n\nReplying to a comment forms a discussion thread. Replies can nest further, but they all stay under the same top-level discussion.\n\n## Resolve a discussion\n\nWhen a question reaches a conclusion, mark the whole discussion as resolved, or set one reply as the conclusion; a discussion keeps only one conclusion at a time. Resolved discussions collapse but can still be expanded.\n\nReplying in a resolved discussion reopens it automatically.\n\n## @-mentions\n\nType `@` and pick a target from the list:\n\n| Target | Effect |\n|---|---|\n| **Member** | Gets a mention notification in the inbox; an @ in a comment does not create a subscription. |\n| **Agent** | Creates a task for that agent; agents have no inbox. |\n| **Squad** | Notifies the squad's members and triggers the squad leader. |\n| **Issue** | Inserts a link to another issue — no notification, no agent trigger. |\n| **@all** | Notifies every workspace member and triggers no agent. |\n\nTyping plain-text `@name` does not create a mention; pick from the editor's suggestion list. Typing a valid issue key converts it into an issue link automatically.\n\n> @-mentioning an agent in a comment does not change the issue's assignee or status. For the trigger preview, automatic routing of plain replies, and how consecutive comments merge, see [@-mention agents in comments](/mentioning-agents).\n\n## Subscriptions and the inbox\n\nMembers who comment subscribe to the issue automatically, and later activity and @-mentions land in the [inbox](/inbox); you can also subscribe or unsubscribe manually at the top right of the issue. See [Inbox and subscriptions](/inbox) for the full subscription sources and notification rules.\n\n## Reactions\n\nAdd an emoji reaction to a comment to acknowledge, agree, or give quick feedback. Reactions do not trigger agents and do not change issue status.\n\n## Edit and delete\n\nComment authors can edit or delete their own comments; workspace `owner`s and `admin`s can also edit and delete anyone's comments. For comments posted by agents, the UI only offers delete, not edit.\n\nAfter a comment is edited, UniAI recalculates triggers from the new content. @-mentions added during the edit create tasks too; the trigger preview shows under the input before you save.\n\n> Deleting a comment cannot be undone. Deleting a top-level comment deletes every reply under it. If the content is wrong, edit it instead.\n\n## Next steps\n\n- [@-mention agents in comments](/mentioning-agents) — how @-mentions, replies, and assignment differ.\n- [Inbox and subscriptions](/inbox) — manage the work activity you need to follow.\n- [Assign issues to agents](/assigning-issues) — make an agent the issue's assignee."
        },
        {
          "slug": "mentioning-agents",
          "title": "@-mention agents in comments",
          "content": "@-mention an agent in a comment when you want it to look at a new piece of information, answer a question, or help the current assignee. The comment becomes the starting point for the run; the issue's assignee and status stay unchanged.\n\n## Assigning vs. @-mentioning\n\n| | Assign to an agent | @-mention in a comment |\n|---|---|---|\n| Best for | Making the agent own the whole issue | Having the agent handle the current comment |\n| Changes the assignee | Yes | No |\n| Changes the status | No | No |\n| Targets per action | One agent or squad | Multiple agents or squads |\n| Focus of the run | The whole issue | The triggering comment, plus comments merged in the same batch |\n\nUse [assignment](/assigning-issues) when the issue needs a clear owner; use an @-mention to bring an agent into the discussion temporarily.\n\n## Trigger preview\n\nAfter you pick an agent or squad in the comment editor, a trigger preview appears under the input:\n\n- which agents will start working once you send;\n- whether each target comes from a direct @-mention, the issue assignee, or a squad leader;\n- whether a target is unavailable, or you lack permission to run it.\n\nUnticking an agent skips this one trigger only — you don't need to delete its name from the comment, and the agent's Access is not changed.\n\n![Trigger preview before sending: lists the agents that will start working once the comment is sent](/images/docs/mention-trigger-preview.png)\n\nOnce the comment is posted, an online runtime usually claims the task right away; if the runtime is offline, the task waits in the queue.\n\n## Automatic routing of replies\n\nA plain comment (containing no @ at all) is routed by discussion context:\n\n1. Replying to an agent's comment hands it to that agent.\n2. Replying inside a discussion an agent already participates in keeps that discussion's agent.\n3. Otherwise it goes to the issue's agent assignee; when the assignee is a squad, the leader handles it.\n\nWhen a comment explicitly @-mentions other targets (including members), the assignee fallback no longer applies.\n\nWhen a reply triggers an agent other than the assignee, UniAI schedules a delayed fallback run for the assignee: if the target agent replies within about 5 minutes, the fallback is cancelled automatically; otherwise the assignee starts a run too.\n\n## Coalescing consecutive comments\n\nWhen the same agent already has a waiting task on the issue, new consecutive comments merge into the pending task; if the agent is already running, later comments wait for the current run to finish and merge into a single follow-up run. You can keep adding information without waiting for the agent to reply.\n\nWhen one comment @-mentions several different agents, each agent gets its own task; multiple mentions resolving to the same agent keep only one.\n\n## Comments that don't trigger agents\n\nTwo ways:\n\n- Start the comment with `/note`: it's posted as a note between members and triggers no agent.\n- Use `@all`: it notifies every workspace member and switches off this comment's automatic trigger for the assignee.\n\nIf the trigger preview still lists an agent, untick it before sending.\n\n> `@all` does not include agents. Agents have no inbox; to put one to work, @-mention it explicitly, reply in its discussion, or assign the issue to it.\n\n## Access restrictions\n\nAn @-mention cannot bypass an agent's Access. When you lack permission to run the target, the target is archived, or it has no available runtime, the trigger preview explains that it won't start; the comment itself still posts normally. See [Agents](/agents#permissions-and-access) for the three Access levels.\n\n## Agents mentioning each other\n\nAgents can @-mention other agents in their replies. When A @-mentions B and B @-mentions A back, the system merges duplicate runs at the same moment, but it does not decide when the collaboration should end — write the stop condition into the agent instructions.\n\n## Next steps\n\n- [Comments](/comments) — replies, resolving discussions, notifications, and subscriptions.\n- [Agents](/agents) — configure identity, Access, and runtimes.\n- [Squads](/squads) — have a leader coordinate multiple agents."
        },
        {
          "slug": "chat",
          "title": "Chat with agents",
          "content": "Chat fits work that hasn't formed a clear issue yet: sorting out ideas, checking on the workspace, discussing an approach, or having an agent finish a small task first.\n\n**Chat is a one-to-one conversation between you and an [agent](/agents)** — stepping outside the [issue](/issues) board. The agent starts with **no issue context** — it isn't looking at your board and won't turn the conversation into issues on its own — and the entire conversation is **fully private** (nobody else in the [workspace](/workspaces), including admins, can see it). It fits discussing an approach with an agent, brainstorming, or asking a question that does not belong to any issue.\n\nA chat is **not bound to any issue**: the agent doesn't automatically receive an issue's description and comments, but it can still use the UniAI CLI to query workspaces, projects, issues, and skills within its permissions.\n\nA chat can also carry background. Click **+** at the bottom left of the input and choose **Project context** to attach a [project](/projects) to the conversation; when the agent runs, it receives that project's description, repositories, and other resources.\n\n![A chat with an agent: a question answered with the result organized into a table](/images/docs/chat-conversation.png)\n\n## Start a conversation\n\n1. Open **Chat** in the sidebar.\n2. Click new and pick an agent you have permission to run.\n3. Type a message; you can also attach images or files.\n4. After you send, UniAI creates a run for the message, and the reply comes back to the current conversation.\n\nIf the runtime is offline, the message waits until it comes back online. Once the agent is archived, or you lose permission to run it, you can't send new messages.\n\n## Chat vs. issues\n\n| Need | Better fit |\n|---|---|\n| Quick questions, exploring an approach, or private drafts | **Chat** |\n| A clear assignee, status, priority, and deliverable | **Issue** |\n| Letting teammates see the background and continue the discussion | **Issue** |\n| A quick look at workspace information with no work record needed | **Chat** |\n\nDirect chats created in UniAI are visible only to their creator; other workspace members and admins cannot read them. Conclusions the team needs to see together should be written into an issue, a project description, or a skill.\n\n## Multi-turn context\n\nA conversation tries to continue the original session in the AI coding tool, so later messages don't need to repeat earlier content. UniAI stores the session ID and routes subsequent runs to a runtime that can access that session.\n\nIf the local session no longer exists, the context can't be continued, or the previous error shouldn't be reused, the runtime starts from a fresh session. The conversation's message history is kept either way.\n\n## Watch and stop a run\n\nWhile the agent runs, the UI shows the current phase and tool activity. Expand **View details** to inspect the run in full.\n\nTo interrupt, click the stop button next to the input. Content already returned to the conversation is kept; input not yet processed is restored to the editor when possible.\n\n## Organize the conversation list\n\nConversations can be renamed, pinned, archived, and deleted:\n\n- **Pin** keeps frequently used conversations at the top of the list;\n- **Archive** moves a conversation to Archived and makes it read-only;\n- **Unarchive** lets you send messages again;\n- **Delete** permanently removes the conversation and its messages.\n\nIf a conversation is bound to an external channel (such as Slack), archiving breaks the binding; unarchiving does not restore it — bind again.\n\n> Deleting a conversation cannot be undone and stops any runs in it that haven't finished. Archive instead when you just don't need it for now.\n\n## Next steps\n\n- [Assign issues to agents](/assigning-issues) — turn a discussion into trackable work.\n- [Inbox and subscriptions](/inbox) — follow team activity that needs you.\n- [Providers matrix](/providers) — session and model capabilities per AI coding tool."
        },
        {
          "slug": "inbox",
          "title": "Inbox and subscriptions",
          "content": "The inbox collects the work activity that concerns you. It's not a full activity log — it flags where something changed that needs your attention; the full context stays on the corresponding issue.\n\n## What lands in the inbox\n\nCommon notifications include:\n\n- an issue is assigned to you, or moved away from you;\n- an issue you subscribe to gets a new comment, or its assignee, status, priority, or dates change;\n- someone @-mentions you in an issue description or comment;\n- an issue or comment you created gets a reaction;\n- an agent run fails on an issue you subscribe to;\n- creating an issue through an agent completes or fails;\n- an autopilot you subscribe to creates a new issue, or is paused after consecutive failures.\n\nYour own actions don't notify you. Multiple notifications on the same issue merge into one entry; open it to see the issue's recent changes.\n\n## Sources of automatic subscription\n\nThese participants subscribe to an issue automatically:\n\n- the issue's creator;\n- a new assignee;\n- anyone who comments;\n- anyone newly @-mentioned in the issue description;\n- subscribers preconfigured in an autopilot.\n\nChanging the assignee does not remove existing subscriptions. Subscribe or unsubscribe anytime on the issue page; after unsubscribing, ordinary updates stop reaching your inbox, while direct @-mentions still notify you individually.\n\n> When a sub-issue's status changes, subscribers of the parent issue are notified as well. Sub-issue comments, priority, and date changes do not bubble up to the parent.\n\n## Agents don't use the inbox\n\nThe inbox is only for workspace members. Even when an agent is the assignee or a subscriber, it never reads inbox notifications.\n\n@-mentioning an agent in a comment, assigning an issue to it, or triggering an autopilot creates a run directly instead of leaving the agent a notification. `@all` notifies members only.\n\n## Handle notifications\n\nSelecting a notification marks it as read. Archive it when you're done; archiving doesn't modify or delete the corresponding issue. Once an issue reaches `in_review`, `done`, or `cancelled`, its run-failure notifications archive automatically.\n\nRight-click a notification for **Mark as read** / **Mark as unread** and **Archive** (**Unarchive** in the Archived view). Mark as unread is the \"I looked, but I'm not done with this\" action: the notification goes back to unread and raises the unread count again, and it stays unread even while you keep it open — opening it again later marks it read as usual. Archived notifications keep their read state, so restoring one brings it back exactly as you left it.\n\nFrom the inbox menu you can also:\n\n- mark all as read;\n- archive all, or archive all read;\n- archive notifications for issues in `done` or `cancelled`;\n- open Archived and restore a notification.\n\n## Tune notification types\n\nOpen **Settings → Notifications** to toggle each group separately:\n\n- assignments;\n- status changes;\n- comments;\n- mentions;\n- priority and dates;\n- agent activity.\n\nTurning a group off doesn't stop the changes from being recorded on the issue — it only stops the inbox notifications. A few types, such as reactions, quick-create results, and autopilot pauses, sit outside these six groups and are always delivered. System and browser notifications control desktop banners and don't change the inbox itself.\n\n## Next steps\n\n- [Comments](/comments) — replies, mentions, and discussion threads.\n- [@-mention agents in comments](/mentioning-agents) — member notifications vs. agent runs.\n- [Autopilots](/autopilots) — configure automatic work creation and default subscribers."
        }
      ]
    },
    {
      "label": "Runtime & execution",
      "labelKey": "runtime_execution",
      "pages": [
        {
          "slug": "tasks",
          "title": "Tasks",
          "content": "Every time an agent starts working, UniAI creates a **task**. It records what triggered the run, which agent it went to, how far it has progressed, and whether it ultimately succeeded.\n\n## Issues and tasks\n\nAn issue holds the goal, discussion, assignee, and final status of a piece of work; a task records one agent run against that work.\n\n| | Issue | Task |\n| --- | --- | --- |\n| What it records | A piece of work moving forward | One agent run |\n| How long it lasts | Can be discussed, amended, and reassigned repeatedly | From trigger until completion, failure, or cancellation |\n| Cardinality | One issue can span many runs | Every run gets its own record |\n\nSo the same issue can be handed to different agents in turn, or run again after a failure. Each run produces a new task; earlier records are never overwritten.\n\n## Trigger sources\n\nAny of the following can trigger a run:\n\n- Assigning an issue to an agent or a squad.\n- Mentioning an agent in a comment.\n- Sending a message to an agent in chat.\n- An Autopilot firing on a schedule or from an external event.\n\nDifferent entry points provide different context, but execution works the same way: UniAI creates a task, a runtime claims it, and the runtime invokes the AI coding tool the agent is configured with.\n\n## Execution lifecycle\n\nA task typically moves through these states:\n\n| State | Meaning |\n| --- | --- |\n| `deferred` | Scheduled to fire later; enters the queue at the appointed time |\n| `queued` | Waiting for a runtime to claim it |\n| `dispatched` | A runtime has claimed it and is starting the AI coding tool |\n| `waiting_local_directory` | The target local directory is held by another run; waiting for the directory lock to release |\n| `running` | The AI coding tool is executing |\n| `completed` | The run finished normally |\n| `failed` | The run errored or was interrupted |\n| `cancelled` | The run was stopped manually |\n\nWhen the runtime is online, a new task usually starts quickly; if the runtime goes offline after the task enters the queue, the task stays queued until it recovers. A task left unclaimed for more than 2 hours ends as failed.\n\n> If the system already knows the target runtime is offline before triggering, some immediate actions report that the run cannot start right now instead of creating a task nothing can claim.\n\nA runtime with a healthy heartbeat can execute long tasks; the server never force-ends a run just because it has been running for a while. The runtime decides whether the process has stalled based on actual activity.\n\n## Viewing run history\n\nOpen an issue and go to the **Execution log** to see every task it has produced. Each row shows the trigger source, the executing agent, the state, and timing.\n\nFrom here you can:\n\n- Open a run record to see the agent's messages, tool calls, and error output.\n- Stop tasks that are deferred, queued, starting, waiting for a local directory, or running.\n- Retry a failed or cancelled task.\n\n![Run record: status timeline, agent summary, and individual tool calls](/images/docs/task-transcript.png)\n\n> Changing an issue's assignee or status does not stop a run that has already started. To interrupt one, stop the corresponding task in the execution log. Active tasks are cancelled along with an issue only when the issue is deleted.\n\n## Failures and automatic retries\n\nTransient faults — a runtime going briefly offline, a daemon restart, an execution timeout, or a network interruption in the AI coding tool — can trigger an automatic retry. A regular task runs at most twice by default; tool network interruptions get up to three attempts.\n\nErrors returned by the agent itself are usually not retried automatically. Expired credentials, exhausted quota, misconfiguration, or a model unable to complete the request all need the cause fixed first, followed by a manual retry.\n\nAn Autopilot's **run only** mode does not retry automatically, to avoid overlapping with the next scheduled run. The **create issue** mode produces regular issue tasks, so infrastructure failures still retry under the rules above. Both modes show their final result in the Autopilot run history.\n\nWhen an issue has no other active task and no new retry waiting to run, a failure moves an `in_progress` issue back to `todo`.\n\n## Failure reason reference\n\nThe failure reasons shown in the execution log and usage stats fall into two groups: unprefixed reason codes are recorded by the platform; `agent_error.*` codes are classified from the AI coding tool's own errors.\n\n**Platform side**\n\n| Reason | Meaning | What to do |\n| --- | --- | --- |\n| `runtime_offline` | The runtime went offline during the run | Restore the runtime and retry; see [Daemon and runtimes](/daemon-runtimes) |\n| `queued_expired` | Queued for over 2 hours with no runtime claiming it | Confirm the runtime is online, then retry |\n| `runtime_recovery` | The daemon reclaimed an interrupted run after restarting | Retry directly |\n| `cancelled` | Stopped manually, or cancelled along with an archive or delete | Nothing to do |\n| `timeout` | Exceeded the daemon's configured execution time limit | Narrow the issue scope, or adjust the daemon's `agent_timeout` |\n| `iteration_limit` | Hit the iteration cap for a run | Narrow the issue scope |\n| `agent_blocked` | The agent reported it cannot proceed | Provide what its comment asked for |\n| `api_invalid_request` | The platform API rejected an invalid request | Retry; report the problem if it recurs |\n| `codex_semantic_inactivity` | Codex produced no meaningful output for too long and was judged stalled | Retry, or adjust the Codex inactivity timeout |\n\n**Tool side (`agent_error.*`, prefix omitted)**\n\n| Reason | Meaning | What to do |\n| --- | --- | --- |\n| `provider_auth_or_access` | Model provider authentication failed or access denied (401/403) | Sign in again in that AI coding tool, or check the API key |\n| `provider_quota_limit` | Quota or balance exhausted (402) | Top up or switch accounts |\n| `provider_capacity_or_rate_limit` | Rate limited or out of capacity (429/529) | Retry later |\n| `provider_server_error` | Model provider server error (5xx) | Retry later |\n| `provider_network` | Network failure reaching the model provider | Retries automatically; check the executing machine's network if it persists |\n| `model_not_found_or_unavailable` | The model does not exist or is currently unavailable | Pick an available model in the agent settings |\n| `context_overflow` | Context exceeded the model window | Narrow the issue scope or reduce the input |\n| `missing_config` | Required configuration such as an API key is missing | Fill in the agent's environment variables or the tool configuration |\n| `runtime_missing_executable` | The AI coding tool's executable was not found | Reinstall the tool; see [Install AI coding tools](/install-agent-runtime) |\n| `runtime_version_unsupported` | The AI coding tool version is too old | Upgrade the tool |\n| `process_failure` | The tool process exited abnormally | Check the run record to locate the cause, then retry |\n| `empty_or_unparseable_output` | The tool produced no output, or output that could not be parsed | Retry; check the tool installation if it recurs |\n| `agent_timeout` | The tool was terminated after going unresponsive for too long | Retry or narrow the issue scope |\n| `unknown` | Unclassified failure | Check the raw error in the run record |\n\n## Manual retry\n\nClicking the retry button on a row in the execution log invokes **the agent that ran that task at the time**. Even if the issue has since been reassigned, the retry does not switch to the new assignee.\n\nA retry keeps the files the previous run already wrote to the local directory whenever possible. If the original session is still safe and the same runtime claims the retry, it also continues the previous session. Errors that poison the session, such as context overflow or invalid requests, start a new session on top of the original working directory instead. When the original directory no longer exists, a fresh working directory is used.\n\nYou can also rerun the current issue from the CLI:\n\n```bash\nuniai issue rerun <issue-id>\n```\n\nThis form does not point at a specific past task, so it uses the issue's current agent assignee and starts with a fresh session and working directory.\n\n## Task completion and issue completion\n\n`completed` only means this particular run ended normally — it does not confirm the issue's goal has been met. You can still review the result, keep discussing, add requirements, or trigger the agent again.\n\nWhether an issue is done is judged by the actual progress of the work and the issue's status.\n\n## State and timeout quick reference\n\nThe numbers below reflect the server's default configuration, for quick cross-checking while troubleshooting.\n\n| State | Meaning | Timeout and consequence |\n| --- | --- | --- |\n| `deferred` | Scheduled to fire later | Enters `queued` at the scheduled time, then the rules below apply |\n| `queued` | Waiting for a runtime to claim it | Fails after **2 hours** unclaimed; not retried automatically |\n| `dispatched` | Claimed; the tool is starting | Treated as failed after more than **5 minutes** in this state |\n| `waiting_local_directory` | Waiting for the local directory lock to release | No timeout of its own; returns to the startup flow once the directory is released |\n| `running` | The AI coding tool is executing | No fixed duration cap; liveness follows the runtime heartbeat — one every 15 seconds; a runtime that loses its heartbeat is marked offline within about 3 minutes at the latest, and its tasks fail with it |\n\nAutomatic retry covers only the transient faults below, and only applies to tasks attached to an issue or a chat (excluding the Autopilot **run only** mode):\n\n| Auto-retryable failure reason | Attempt ceiling |\n| --- | --- |\n| Runtime offline | 2 by default (first run + 1 retry) |\n| Reclaimed after a daemon restart | 2 by default |\n| Platform-judged execution timeout | 2 by default |\n| Codex stalled with no meaningful output | 2 by default |\n| Skill bundle download failure | 2 by default (the agent process has not started at this point; already-downloaded bundles come from the local cache) |\n| Tool network interruption | Up to 3; the final attempt starts after a delay of about 5 seconds |\n\nAll other failure reasons (auth, quota, configuration, model, and so on) never retry automatically; fix the cause first, then retry manually.\n\n## Next steps\n\n- [Daemon and runtimes](/daemon-runtimes) — which computer a task executes on.\n- [Assigning issues to agents](/assigning-issues) — trigger a run from an issue.\n- [Mentioning agents](/mentioning-agents) — add requirements in comments or bring in other agents."
        },
        {
          "slug": "daemon-runtimes",
          "title": "Daemon and runtimes",
          "content": "UniAI records and coordinates the work; connected computers execute it. The **daemon** on a computer claims tasks and invokes the AI coding tools installed on that machine.\n\n## Daemon vs runtime\n\n- The **daemon** is the UniAI background process running on one computer. It connects to the server, discovers local tools, claims tasks, and reports results back.\n- A **runtime** represents one concrete execution environment available to a workspace. It corresponds to one computer plus one AI coding tool — or one custom runtime profile — on that computer.\n\nFor example, a computer has both Claude Code and Codex installed and is connected to two workspaces. The daemon registers Claude Code and Codex runtimes for each workspace. Restarting the daemon updates the existing records; it does not keep creating new runtimes for the same combination.\n\n## Execution location and data boundaries\n\nThe AI coding tools a local runtime invokes, those tools' own login credentials, and your local code directories all stay on the connected computer. The UniAI server does not execute commands on behalf of local tools, and it does not automatically upload your entire working directory.\n\nSo the team can collaborate, the server stores issues, comments, agent configuration, task context, run records, and the results agents write back. That content can include code snippets or other project context an agent chose to read and include in its replies.\n\n> An agent's custom environment variables are stored server-side and sent to the runtime at execution time. Do not read \"local execution\" as \"every secret exists only on this machine\": custom environment variables and MCP configuration live on the server, and their display is limited by the sensitive-value rules.\n\n## Starting the daemon\n\n[Install the UniAI CLI](/cli) first, then run:\n\n```bash\nuniai daemon start\n```\n\nThe daemon runs in the background by default. Common commands:\n\n| Command | Purpose |\n| --- | --- |\n| `uniai daemon status` | Show daemon and connection status |\n| `uniai daemon logs -f` | Follow the logs |\n| `uniai daemon restart` | Restart the daemon and re-detect local tools |\n| `uniai daemon stop` | Stop the daemon |\n| `uniai daemon start --foreground` | Run in the current terminal, for debugging |\n\nOn startup, the daemon detects supported AI coding tools on `PATH` and registers runtimes for the workspaces you are allowed to connect. If a tool was just installed or signed in, restart the daemon to detect it again.\n\nThe daemon needs at least one built-in supported AI coding tool detected before it will start. Install methods and executable names are in [Install AI coding tools](/install-agent-runtime).\n\n## Dispatch and online status\n\nOnce registered, a runtime keeps a persistent connection. When a new task enters the queue, the server notifies the matching daemon; the daemon also polls periodically as a backstop after connection interruptions. So when a runtime is online with spare capacity, tasks usually start immediately.\n\nThe daemon sends a heartbeat every 15 seconds. The server combines heartbeats and connection state to decide whether a runtime is online; after a daemon exits unexpectedly, the runtime usually shows as offline within about 3 minutes at the latest.\n\n![Runtime details for one online computer: the same daemon registered 7 runtimes, one row per detected AI coding tool, each showing online status and CLI version](/images/docs/runtime-machine-detail.png)\n\nWhen a runtime is offline:\n\n- Tasks already queued wait for the runtime to recover, for up to 2 hours.\n- Tasks that were running fail; eligible issue or chat tasks can retry automatically.\n- When the daemon starts again, it re-registers its runtimes and reclaims tasks that did not end cleanly last time.\n- A runtime offline for more than 7 days with no agents bound to it (including archived ones) is cleaned up automatically.\n\nDetailed states and retry rules are in [Tasks](/tasks).\n\n## Concurrency limits\n\nOne daemon executes at most 20 tasks at a time by default; each agent at most 6. Effective concurrency is the smaller of the two.\n\nOnce a limit is reached, new tasks keep queueing. You can adjust a single agent's concurrency in the agent settings, and the machine-wide cap through `MULTICA_DAEMON_MAX_CONCURRENT_TASKS`. Parallel runs compete for machine capacity, tool account quota, and the same working directory all at once.\n\n## Private and public runtimes\n\nA local runtime is **private** by default: the runtime owner and the workspace's owners and admins can create agents on it.\n\nThe runtime owner or an admin can make it **public**. Other workspace members can then select this runtime as well; that does not share the underlying AI coding tool's login credentials — it only lets members route their agents' tasks to this computer.\n\n## Custom runtime profiles\n\nIf your team uses an internal wrapper, a version-pinned executable, or needs fixed extra arguments for a compatible tool, create a **custom runtime profile**.\n\nA custom profile does not add a new communication protocol. You still pick one of the protocol families UniAI already supports (the tool's integration protocol type; see [AI coding tools comparison](/providers)), and the command itself must be compatible with that family.\n\n### Creating a profile\n\nOnly workspace owners and admins can create, edit, or delete custom runtime profiles:\n\n1. Open **Runtimes** and go to a computer where the command is installed.\n2. Click **Add custom runtime**.\n3. Choose the protocol family the command is actually compatible with.\n4. Fill in the name, command, and fixed arguments, then save.\n\nThe profile is shared across the workspace. Each connected computer looks up the command on its own; only computers that can resolve it on `PATH` register the corresponding runtime. Creating a profile does not install the command, and it does not sign other members in to the tool.\n\nThe command field takes an executable and arguments, not a shell script. Plain arguments, quotes, and backslash escapes work; pipes, redirects, `&&`, `;`, backticks, and environment variable expansion do not. When you need those behaviors, put them in a wrapper script and use that script as the command.\n\nIf the daemon cannot find a command your terminal can run, set an absolute path for the current computer:\n\n```bash\nuniai runtime profile set-path <profile-id> --path /absolute/path/to/command\n```\n\nRemove the path override:\n\n```bash\nuniai runtime profile unset-path <profile-id>\n```\n\nEditing a profile only affects tasks claimed afterwards. Before deleting a profile, deal with the active agents still bound to its runtimes. Deleting only the runtime instance on one computer does not delete the profile — a running daemon will re-register it.\n\n## Troubleshooting an offline runtime\n\nCheck in this order:\n\n1. Run `uniai daemon status` to confirm the daemon is running.\n2. Run `uniai daemon logs -f` to look for login, network, or tool-detection errors.\n3. Run `command -v <tool-command>` in the same environment to confirm the daemon can find it.\n4. Open UniAI's **Runtimes** page and check whether the target computer and the matching AI coding tool show as online.\n5. After a tool install, a path change, or a profile update, run `uniai daemon restart`.\n\nIf the problem persists, check `uniai daemon logs -f` for the exact error the daemon reports.\n\n## Next steps\n\n- [Install AI coding tools](/install-agent-runtime) — install and verify a supported AI coding tool.\n- [AI coding tools comparison](/providers) — compare models, session resumption, MCP, and skill support.\n- [Tasks](/tasks) — queueing, stopping, and retries."
        },
        {
          "slug": "install-agent-runtime",
          "title": "Install AI coding tools",
          "content": "A local runtime has two parts: the UniAI daemon, and one AI coding tool it can invoke. Install only the tool you intend to use — you do not need all of them.\n\n## 1. Choose a tool\n\nA tool that is already installed and signed in needs no extra setup. Tools differ in models, MCP, session resumption, and skill support; the details are in [AI coding tools comparison](/providers).\n\nUniAI currently detects these commands:\n\n| Tool | Detected command | Official install guide |\n| --- | --- | --- |\n| Antigravity CLI | `agy` | [Antigravity install and sign-in](https://antigravity.google/docs/cli-install) |\n| Claude Code | `claude` | [Claude Code quickstart](https://code.claude.com/docs/en/quickstart) |\n| CodeBuddy Code | `codebuddy` | [CodeBuddy quickstart](https://www.codebuddy.ai/docs/cli/quickstart) |\n| Codex CLI | `codex` | [Codex CLI](https://developers.openai.com/codex/cli/) |\n| GitHub Copilot CLI | `copilot` | [Install Copilot CLI](https://docs.github.com/en/copilot/how-tos/copilot-cli/set-up-copilot-cli/install-copilot-cli) |\n| Cursor Agent | `cursor-agent` | [Install Cursor CLI](https://docs.cursor.com/en/cli/installation) |\n| DevEco Code | `deveco` | [DevEco Code](https://gitcode.com/openharmony-sig/deveco-code) |\n| Grok Build | `grok` | [Grok Build quickstart](https://docs.x.ai/build/overview) |\n| Hermes Agent | `hermes` | [Hermes quickstart](https://hermes-agent.nousresearch.com/docs/getting-started/quickstart/) |\n| Kimi CLI | `kimi` | Kimi CLI |\n| Kiro CLI | `kiro-cli` | [Install Kiro CLI](https://kiro.dev/docs/cli/installation/) |\n| OpenClaw | `openclaw` | [Install OpenClaw](https://docs.openclaw.ai/install) |\n| OpenCode | `opencode` | [OpenCode quickstart](https://opencode.ai/en/docs) |\n| Pi coding agent | `pi` | Pi coding agent |\n| Oh-My-Pi | `omp` | [Oh-My-Pi install](https://omp.sh) |\n| Qoder CLI | `qodercli` | [Qoder CLI quickstart](https://docs.qoder.com/en/cli/quick-start) |\n| Qoder CN CLI | `qoderclicn` | [Qoder CN CLI quickstart](https://help.aliyun.com/en/lingma/qodercli-cn/user-guide/qoder-cli-cn-get-started-quickly) |\n| Qwen Code | `qwen` | Qwen Code |\n| QwenPaw | `qwenpaw` | QwenPaw |\n| Reasonix | `reasonix` | Reasonix |\n| TRAE CLI | `traecli` | [TRAE official site](https://www.trae.cn/) |\n\n\"Supported\" means UniAI can invoke that CLI. It does not mean UniAI provides the tool's account, subscription, or model quota for you.\n\n## 2. Sign in on your machine\n\nAfter installing, launch the tool once in a terminal on its own and complete its sign-in or model provider setup. If the tool cannot complete requests in the terminal, the daemon's invocations fail the same way.\n\nFor Reasonix, run `reasonix setup` and configure a default provider and model before starting the daemon.\n\nFor QwenPaw, choose the provider and model in QwenPaw's own configuration. UniAI cannot override a QwenPaw model at run time.\n\nThose login credentials are stored locally by the tool itself. UniAI never receives login tokens from Claude, Codex, Cursor, or any other CLI.\n\n> Claude Code requires 2.0.0 or later, Codex 0.100.0 or later, Copilot 1.0.0 or later, Grok 0.2.89 or later, and Qwen Code 0.20.0 or later. Below the minimum version, the daemon does not register the corresponding runtime.\n\n## 3. Confirm the daemon can find the command\n\nOn macOS, Linux, or WSL, run:\n\n```bash\ncommand -v <command>\n<command> --version\n```\n\nIn Windows PowerShell, run:\n\n```powershell\nGet-Command <command>\n<command> --version\n```\n\nReplace `<command>` with a command from the table, such as `claude`, `codex`, or `cursor-agent`. The first command prints the executable path.\n\nIf the terminal finds the tool but the background daemon does not, the two are usually using different `PATH` values. Restart the daemon, or set an absolute path through the matching `MULTICA_<PROVIDER>_PATH` environment variable.\n\n## 4. Re-detect and confirm the runtime is online\n\nStart the daemon with the CLI:\n\n```bash\nuniai daemon start\n```\n\nIf the daemon is already running, restart it after installing a new tool:\n\n```bash\nuniai daemon restart\n```\n\nThen open UniAI's **Runtimes** page. The tool appears under the target computer and shows as online. You can now select this runtime when creating or editing an agent.\n\n> The daemon must detect at least one built-in supported CLI before it will start. Custom runtime profiles sync after the daemon is up, so they cannot be the sole startup condition on a blank machine.\n\n## No tools detected\n\nCheck in order:\n\n1. Run the tool itself in a terminal to confirm installation and sign-in are complete.\n2. Check the actual command name and path with `command -v` or `Get-Command`.\n3. Run `uniai daemon logs -f` and look for version, path, or authentication errors.\n4. Restart the daemon so it re-reads the current environment.\n5. Return to the **Runtimes** page and confirm the computer and the tool are online.\n\nIf the tool shows as online but runs still fail, open the issue's run record to see the exact error the CLI returned.\n\n## Next steps\n\n- [Create and configure an agent](/agents-create) — select the runtime you just connected.\n- [Daemon and runtimes](/daemon-runtimes) — online status, concurrency, and custom runtimes.\n- [AI coding tools comparison](/providers) — compare what each tool can do in UniAI."
        },
        {
          "slug": "providers",
          "title": "AI coding tools comparison",
          "content": "UniAI does not replace the AI coding tool you use. A runtime invokes the tool already installed and signed in on the computer, then writes progress and results back to the issue.\n\nIf you already use one of these tools, you can usually keep it as-is. Four things to weigh when choosing:\n\n- whether it is already installed on the computer;\n- which models your account can use;\n- whether UniAI needs to configure MCP for it;\n- which skills the team wants to reuse.\n\nInstall steps are in [Install AI coding tools](/install-agent-runtime).\n\n## Supported tools\n\n| Tool | Detected command | Session resumption | UniAI-managed MCP | Skill injection path |\n|---|---|---:|---:|---|\n| Antigravity | `agy` | ✓ | — | `.agents/skills/` |\n| Claude Code | `claude` | ✓ | ✓ | `.claude/skills/` |\n| CodeBuddy | `codebuddy` | ✓ | ✓ | `.codebuddy/skills/` |\n| Codex | `codex` | ✓ | ✓ | `$CODEX_HOME/skills/` |\n| GitHub Copilot CLI | `copilot` | ✓ | — | `.github/skills/` |\n| Cursor Agent | `cursor-agent` | ✓ | ✓ | `.cursor/skills/` |\n| DevEco Code | `deveco` | ✓ | — | `.deveco/skills/` |\n| Grok | `grok` | ✓ | ✓ | `.grok/skills/` |\n| Hermes | `hermes` | ✓ | ✓ | per-task `HERMES_HOME/skills/` |\n| Kimi CLI | `kimi` | ✓ | ✓ | `.kimi/skills/` |\n| Kiro CLI | `kiro-cli` | ✓ | ✓ | `.kiro/skills/` |\n| OpenClaw | `openclaw` | ✓ | ✓ | `skills/` |\n| OpenCode | `opencode` | ✓ | ✓ | `.opencode/skills/` |\n| Pi | `pi` | ✓ | — | `.pi/skills/` |\n| Oh-My-Pi | `omp` | ✓ | — | `.omp/skills/` |\n| Qoder CLI | `qodercli` | ✓ | ✓ | `.qoder/skills/` |\n| Qoder CN CLI | `qoderclicn` | ✓ | ✓ | `.qoder/skills/` |\n| Qwen Code | `qwen` | ✓ | ✓ | `.qwen/skills/` |\n| QwenPaw | `qwenpaw` | ✓ | ✓ | per-task workspace `skills/` |\n| Reasonix | `reasonix` | ✓ | ✓ | `.reasonix/skills/` |\n| Trae CLI | `traecli` | ✓ | ✓ | `.traecli/skills/` |\n\n\"UniAI-managed MCP\" means you can define MCP servers in the agent configuration and UniAI passes them to the tool before a run. It does not mean the tool has no other way to configure MCP.\n\n## Model sources\n\nWhen you create an agent, the model list comes from the corresponding runtime. Some tools expose a fixed set of model names; others return available models based on local configuration, the signed-in account, and subscription entitlements.\n\nIf you do not pick a model, UniAI uses the tool's own default. If the list is empty, confirm the runtime is online and the tool is signed in, then refresh the model list.\n\nQwenPaw is the one exception: it does not accept a model chosen from UniAI, so the picker is inert and shows \"Managed by runtime\". Choose the model in QwenPaw's own configuration instead. An empty model list is expected there and is not a sign that the runtime is offline.\n\n> Different tools may use different model names. After switching AI coding tools, the previous model value is invalid in the new tool and needs to be selected again.\n\n## Session resumption\n\nAll of these tools support continuing an existing session in a later run. The prerequisites are that the original session still exists and the task is executed by a runtime that can reach it.\n\nUniAI stores the session identifier the tool returns. The next time work continues on the same issue, the runtime tries to resume that session; when it cannot, it creates a new session and keeps going. Pi stores a local session file path, so it depends more on the original computer and file.\n\nRetry and session-resumption rules are in [Tasks](/tasks#manual-retry).\n\n## MCP configuration\n\nAntigravity, GitHub Copilot CLI, DevEco Code, and Pi do not read the MCP configuration on an agent yet, so the field is not shown when creating agents for them.\n\n> MCP server commands, arguments, and environment variables are saved server-side with the agent configuration. Do not write local credentials you have no need to share into the configuration.\n\n## Skill injection\n\nBefore a run starts, UniAI writes the skills bound to the agent into the paths in the table above, so each tool discovers them through its own native mechanism.\n\nSkills that already exist in the repository are never overwritten. On a name collision, UniAI uses a different directory name for the injected copy; after the run, it cleans up only the files it created.\n\nCodex, Hermes, and OpenClaw are handled slightly differently:\n\n- Codex uses a per-task `CODEX_HOME`, so skills are never written into the system-level Codex directory.\n- Hermes uses a per-task `HERMES_HOME`. Existing local configuration is still readable, but bound skills and session data are isolated per task. This isolation kicks in only when the agent has skills bound; tasks without bound skills use the machine's original Hermes home.\n- OpenClaw uses `skills/` under the current task's working directory, and the per-run configuration sets that directory as the workspace.\n\nFor creating and binding skills, see [Skills](/skills).\n\n## Next steps\n\n- [Create and configure an agent](/agents-create) — model and thinking-effort settings.\n- [Install AI coding tools](/install-agent-runtime) — install and detection steps.\n- [Daemon and runtimes](/daemon-runtimes) — runtime registration and concurrency."
        }
      ]
    },
    {
      "label": "Automation",
      "labelKey": "automation",
      "pages": [
        {
          "slug": "autopilots",
          "title": "Autopilots",
          "content": "Autopilots run work that happens over and over — a daily progress summary, a periodic dependency check, or an agent kicked off by an event from an external system.\n\nEach autopilot stores a Runbook, an assignee, and one or more triggers. When triggered, UniAI creates an issue or runs the agent directly, and keeps a record of every run.\n\n## Create an autopilot\n\nOpen **Autopilot** in the sidebar, pick a template or start from scratch, then configure:\n\n- **Name**: what this autopilot is responsible for;\n- **Runbook**: the goal, background, constraints, and steps the agent reads on every run;\n- **Assignee**: an agent or a squad;\n- **Project**: optional; puts auto-created issues into a specific project;\n- **Execution mode**: create an issue, or run only;\n- **Subscribers**: members to notify after an issue is auto-created;\n- **Triggers**: a schedule or a webhook.\n\nAn autopilot is enabled by default after saving; **Run now** manually executes the full flow once at any time.\n\n## Choose an execution mode\n\n| Mode | Behavior | Best for |\n|---|---|---|\n| **Create issue** | Each trigger first creates an issue, then assigns it to the assignee; discussion, status, and run records all live on the issue. | Work the team needs to review, confirm, or follow up on. |\n| **Run only** | Creates a task directly with no issue; results are visible only in the autopilot's run history. | Background runs that need no collaboration record. |\n\n**Create issue** mode uses the same task queue as regular issues: when the runtime is offline, the issue is still created and the task waits for the runtime to come online.\n\n**Run only** requires the runtime to be available at trigger time; otherwise the run shows as skipped, and no waiting issue is left behind.\n\n## Run on a schedule\n\nThe schedule editor lets you pick the run time, repeat days, time window, and timezone, and previews the upcoming run times. One autopilot can have multiple schedules; enabling or disabling an individual trigger is done with the CLI's `autopilot trigger-update` — see [Using the CLI](/cli) for the parameters.\n\nWhen you need more complex rules, edit the standard 5-field cron directly:\n\n```text\nminute hour day month weekday\n```\n\nFor example:\n\n| Cron | Timezone | Meaning |\n|---|---|---|\n| `0 9 * * 1-5` | `Asia/Shanghai` | 9:00 on weekdays |\n| `*/30 * * * *` | `UTC` | Every 30 minutes |\n| `0 3 * * *` | `UTC` | Every day at 3:00 |\n\nCron has no seconds field, and timezones use IANA names like `Asia/Shanghai`. Before saving, check the result against the \"next runs\" times shown on the page.\n\n![The autopilot editor: Runbook, schedule settings, and a preview of the next few runs](/images/docs/autopilot-schedule.png)\n\n## Run from a webhook\n\nAfter adding a webhook trigger, UniAI generates a unique URL. Send it a JSON object or array to trigger the autopilot:\n\n```bash\ncurl -X POST \"$UNIAI_WEBHOOK_URL\" \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Idempotency-Key: demo-001\" \\\n  -d '{\"event\":\"build.completed\",\"eventPayload\":{\"status\":\"success\"}}'\n```\n\nThe payload is saved on the delivery and run records and handed to the agent. In create-issue mode it is also appended to the issue description.\n\nConstraints on webhook requests:\n\n- the body must be a valid JSON object or array, up to 256 KiB;\n- `Idempotency-Key` prevents duplicate runs when the sender retries; GitHub deliveries are also deduplicated by `X-GitHub-Delivery`;\n- without a stable idempotency key, UniAI can't guarantee that repeated requests run only once;\n- when the trigger is disabled or the event doesn't match, the delivery is recorded as ignored and no run is created.\n\n### Filter events\n\nWhen one source sends several event types, add event filters to the trigger. Each row holds one event name plus an optional list of actions; a run fires if any row matches, and leaving all rows empty accepts every event.\n\nFor example, with `workflow_run` as the event name and `completed, failed` as the actions, only those two kinds of `workflow_run` results are accepted. UniAI recognizes the event and action from common request headers and payload fields, including GitHub's `X-GitHub-Event` and the `action` field in the body.\n\n### Protect the webhook URL\n\n> The token in the webhook URL is the calling credential. Don't put the full URL in public repositories, issues, or screenshots. If the URL leaks, click the **Rotate URL** button next to it and update the sender immediately; the old URL stops working right away.\n\nOnly the autopilot's creator, workspace owners/admins, and collaborators granted access can view the full URL. The UI hides the token in the URL by default, and copying doesn't require revealing it; click the URL or the eye icon to view the full address.\n\n### Webhook response reference\n\nWhen debugging a sender, use this table to interpret UniAI's responses:\n\n| HTTP status | Response status | Meaning |\n|---|---|---|\n| 200 | `accepted` | Accepted and a run was created; returns the delivery and run IDs. |\n| 200 | `skipped` | Accepted, but this run was skipped (for example, the runtime is offline in run-only mode); includes the reason. |\n| 200 | `ignored` | No run created: the trigger is disabled, the autopilot is paused or archived, or the event was filtered; the `reason` field explains why. |\n| 200 | `duplicate` | The idempotency key hit an existing delivery; returns the original delivery ID and doesn't run again. |\n| 400 | Error message | The body is empty, not valid JSON, or not a JSON object/array. |\n| 401 | `rejected` | The trigger has a signing secret configured, but the request is missing a signature or the signature doesn't match. |\n| 404 | Error message | The token in the URL is invalid or has been rotated. |\n| 413 | Error message | The body exceeds 256 KiB. |\n| 429 | Error message | Too many requests; retry later per the `Retry-After` response header. |\n| 500 | Error message | UniAI internal error; the sender can retry later. |\n\nBusiness-level ignores — pausing, archiving, and event filtering — return 200 rather than 4xx, so senders don't retry endlessly.\n\nEvent and action inference order:\n\n1. if the body has a string `event` field, it is used directly;\n2. otherwise the `X-GitHub-Event` request header, combined with the body's `action` into `github.<event>.<action>`;\n3. then the `X-Gitlab-Event` request header;\n4. then the `X-Event-Type` request header;\n5. then the body's `event`, `type`, and `action` fields;\n6. when all are missing, the event is recorded as `webhook.received`.\n\n## View runs and deliveries\n\nRun history shows the trigger source, time, status, the linked issue or task, and the reason for a failure or skip. Webhook triggers additionally keep separate delivery records, including the parsed event, the response, deduplication info, and the failure reason.\n\nA fully processed webhook delivery can be replayed from its detail view. A replay creates a new delivery and run without rewriting the original record; deliveries that failed signature verification or are still queued can't be replayed, and replays don't participate in deduplication.\n\n## Failures, pausing, and deletion\n\nA failed task in run-only mode is not retried automatically; the next schedule still fires as planned. Create-issue mode produces regular issue tasks, and infrastructure failures follow the rules in [Tasks](/tasks#failures-and-automatic-retries).\n\nUniAI periodically checks whether recent runs keep failing: when the past 7 days have at least 50 completed or failed runs with a failure rate of 90%, the system pauses the autopilot and notifies its creator; fix the cause, then resume it manually.\n\nPausing manually stops schedules, webhooks, and **Run now**. Deletion is actually archiving: future triggers stop, and run and delivery history is kept.\n\n## Permissions\n\n- Any workspace member can create autopilots;\n- the creator and workspace owners/admins can edit, run, delete, and manage triggers;\n- the creator and owners/admins can add collaborators to management access;\n- collaborators can edit, run, and manage triggers, but can't grant access to others;\n- being able to manage an autopilot doesn't guarantee being able to run its agent — agent Access still applies.\n\n## Use the CLI\n\n```bash\nuniai autopilot trigger <autopilot-id>\nuniai autopilot runs <autopilot-id>\nuniai autopilot trigger-rotate-url <autopilot-id> <trigger-id>\n```\n\nSee [Using the CLI](/cli) for the full parameters.\n\n## Next steps\n\n- [Assign issues to agents](/assigning-issues) — assignment and the confirmation window.\n- [Inbox and subscriptions](/inbox) — where run notifications arrive.\n- [Tasks](/tasks) — run states and failure handling."
        }
      ]
    },
    {
      "label": "Integrations",
      "labelKey": "integrations",
      "pages": [
        {
          "slug": "channels",
          "title": "Chat integrations",
          "content": "Chat integrations let the team ask an agent questions, @-mention it in a group chat, or create issues from the chat window — without opening UniAI.\n\nFeishu/Lark, Slack, DingTalk, and WeCom are supported today. They share the same session, identity, and execution mechanics but install differently.\n\n## Choose a platform\n\n| | Feishu / Lark | Slack | DingTalk | WeCom |\n|---|---|---|---|---|\n| Installation | Generate a QR code in UniAI and authorize by scanning it with Feishu | Create an app in Slack, then paste two tokens into UniAI | Create an internal app and Stream-mode robot, then paste its AppKey and AppSecret into UniAI | Create a smart bot with the long connection enabled in the WeCom admin console, then paste its Bot ID and Secret into UniAI |\n| DM the agent | Supported | Supported | Supported | Supported |\n| Groups or channels | Triggered by @-mentioning the Bot | Triggered by @-mentioning the Bot | Triggered by @-mentioning the Bot | Triggered by @-mentioning the Bot |\n| Create issues | `/issue` message command; creates the issue from your input as-is | `/issue` slash command; the agent writes up the description before creating | `/issue` message command; creates the issue from your input as-is | `/issue` message command; creates the issue from your input as-is |\n| Connection | Platform long connection | Socket Mode | Stream mode | Platform long connection |\n\nNew connections are currently open only for mainland-China Feishu; existing international Lark connections keep working and can still be managed.\n\nEach Bot is bound to one UniAI agent. To use several agents on the same chat platform, connect a separate Bot for each.\n\nDingTalk and WeCom are community-maintained: they ship in every release, but carry no official support SLA. Report problems in GitHub issues.\n\nWeCom handles text messages today. Voice, image, and file messages get a short reply explaining that, and are not passed to the agent.\n\nStep-by-step guides:\n\n- [Feishu / Lark Bot integration](/lark-bot-integration)\n- [Slack Bot integration](/slack-bot-integration)\n- [DingTalk Bot integration](/dingtalk-bot-integration)\n\n## How a message is processed\n\n1. UniAI resolves the Bot to its workspace and agent.\n2. In a group or channel, only messages that explicitly @-mention the Bot continue; DMs need no mention.\n3. UniAI verifies the sender's account binding and workspace membership.\n4. The message joins an [agent chat](/chat) session, and a task is created.\n5. The agent's reply goes back to the original DM or thread.\n\nChannel messages that don't @-mention the Bot never trigger the agent and are not added to its conversation context.\n\nOrdinary messages follow this flow. `/issue` is a command, not a chat turn: UniAI posts the result back to the originating platform but does not add the command to UniAI Chat. Slack's native slash command continues through its separate asynchronous issue-creation flow.\n\n## Session isolation\n\n- Feishu/Lark separates sessions by chat; later messages in the same chat continue the same session.\n- Slack separates DMs by channel; in a channel, each thread holds its own session.\n- DingTalk separates sessions by conversation; each DM or group continues its own session.\n- WeCom separates sessions by chat; each DM or group chat continues its own session.\n\nFollow-ups in a channel still need a fresh @-mention. The agent only receives messages addressed to it — it never reads the whole channel history automatically.\n\n## Account binding\n\nThe first time a member messages the Bot, they receive an account binding link. After signing in to UniAI, the platform account is linked to their membership in the current workspace.\n\nUniAI only runs the agent after binding completes. Every message re-checks account binding and workspace membership; after leaving a workspace, the Bot can no longer be used to reach it.\n\n> Account binding only confirms who the sender is. Other members on the chat platform are not automatically added to the UniAI workspace.\n\n## Manage connections\n\nWorkspace owners and admins can connect or disconnect Bots; for Feishu/Lark Bots, connecting and disconnecting is also open to the agent's owner. Regular members can view connected integrations and use the agents they have access to.\n\nAfter disconnecting, the Bot stops receiving new messages. Existing UniAI conversations and run records are kept.\n\n## Next steps\n\n- [Feishu / Lark Bot integration](/lark-bot-integration) — connect an agent by scanning a QR code.\n- [Slack Bot integration](/slack-bot-integration) — create a Slack app and paste its tokens into UniAI.\n- [DingTalk Bot integration](/dingtalk-bot-integration) — create a Stream-mode robot and paste its credentials into UniAI.\n- [Chat](/chat) — the conversation and execution model behind every Bot."
        },
        {
          "slug": "lark-bot-integration",
          "title": "Feishu Bot",
          "content": "Connect a UniAI agent as a Feishu Bot and team members can DM it in Feishu, @-mention it in a group, or use `/issue` to create issues. Conversations and run records are still saved to the UniAI workspace.\n\nEach Bot maps to exactly one agent. To use several agents in Feishu, connect a Bot for each of them.\n\n## Connect a Bot\n\nThe agent's owner and workspace owners and admins can start a connection:\n\n1. Open **Agents** and select the agent to connect to Feishu.\n2. Go to **Integrations** and click **Bind to Feishu**.\n3. Scan the QR code with Feishu and confirm the authorization.\n4. Return to UniAI. Once the page shows **Connected to Feishu**, the Bot is ready.\n\nThe QR code is a one-time credential; if it expires, generate a new one. An agent that is already connected no longer shows the scan entry point; to rename the Bot or adjust its permissions, click **Manage in Feishu**.\n\n> New connections are currently open only for mainland-China Feishu. Existing international Lark connections keep working and can still be managed.\n\n## First use\n\nThe first time a member messages the Bot, they receive an account binding link:\n\n1. Open the link and sign in to UniAI.\n2. Confirm the workspace account to link.\n3. Return to Feishu and send the message again.\n\nOnly after binding can UniAI confirm which workspace member a message comes from. Non-members cannot use the Bot.\n\n## Hand work to the agent in Feishu\n\n### Direct messages\n\nMessage the Bot directly. Consecutive messages stay in the same UniAI conversation, and the agent's replies go back to that conversation.\n\n### Group chats\n\nAdd the Bot to the group first, then `@Bot` and describe what you need. The Bot only responds to messages that explicitly @-mention it; when it runs, it draws on the recent context it can read in the current session.\n\n### Create an issue\n\nSend:\n\n```text\n/issue Fix the redirect after login\nSafari stays on the login page after a successful sign-in.\n```\n\nThe first line becomes the title and the rest becomes the description. Sending `/issue` alone creates nothing; the Bot replies with the required format instead.\n\n## Manage connections\n\nOpen **Settings → Integrations** to view the Bots connected in the workspace. Workspace owners and admins can disconnect them here; an agent's owner can also manage their own Bot from that agent's **Integrations** page.\n\nAfter disconnecting, the Bot stops receiving new messages. Existing conversations and audit records are not deleted.\n\n## Troubleshooting\n\n- **No bind button**: confirm the current account is the agent's owner or a workspace owner or admin.\n- **QR code expired**: close the dialog and start the connection again.\n- **The Bot asks for account binding**: open the binding link and sign in with the UniAI account for that workspace.\n- **The Bot doesn't run**: check whether the agent is archived and whether the [runtime](/daemon-runtimes) it uses is online.\n\n## Next steps\n\n- [Chat integrations](/channels) — platform comparison, session isolation, and account binding mechanics.\n- [Chat](/chat) — the conversation and execution model behind the Bot.\n- [Issues](/issues) — the unit of work `/issue` creates."
        },
        {
          "slug": "slack-bot-integration",
          "title": "Slack Bot",
          "content": "UniAI uses your own Slack app. Once connected, team members can DM the Bot, @-mention it in a channel, or use `/issue` to create issues.\n\nOne Slack app maps to one UniAI agent. To connect several agents, create a separate app for each.\n\n## Before you start\n\nThe connection must be completed by a workspace owner or admin, and ends with two credentials pasted into UniAI:\n\n- a Bot User OAuth Token, starting with `xoxb-`;\n- an App-level Token, starting with `xapp-`.\n\n## 1. Create the Slack app from a manifest\n\nOpen [Slack API Apps](https://api.slack.com/apps), click **Create New App → From a manifest**, pick the Slack workspace, then paste the following under **YAML**:\n\n```yaml\ndisplay_information:\n  name: UniAI\nfeatures:\n  app_home:\n    home_tab_enabled: false\n    messages_tab_enabled: true\n    messages_tab_read_only_enabled: false\n  bot_user:\n    display_name: UniAI\n    always_online: true\n  slash_commands:\n    - command: /issue\n      description: Create a UniAI issue\n      usage_hint: \"[description]\"\noauth_config:\n  scopes:\n    bot:\n      - app_mentions:read\n      - channels:history\n      - groups:history\n      - im:history\n      - mpim:history\n      - chat:write\n      - reactions:write\n      - users:read\n      - commands\nsettings:\n  event_subscriptions:\n    bot_events:\n      - app_mention\n      - message.im\n      - message.channels\n      - message.groups\n      - message.mpim\n  interactivity:\n    is_enabled: false\n  org_deploy_enabled: false\n  socket_mode_enabled: true\n  token_rotation_enabled: false\n```\n\nBefore creating, you can replace both occurrences of `UniAI` with the agent's name. Do not remove scopes or events from the manifest, or DMs, channel mentions, replies, or `/issue` may stop working.\n\nThe Bot connects out to Slack over Socket Mode, so no public Request URL or OAuth Redirect URL is needed.\n\n## 2. Get the two tokens\n\n### Bot token\n\n1. In the Slack app, open **Install App**.\n2. Click **Install to Workspace** and approve the permissions.\n3. Copy the **Bot User OAuth Token** — it starts with `xoxb-`.\n\n### App-level token\n\n1. Open **Basic Information → App-Level Tokens**.\n2. Click **Generate Token and Scopes**.\n3. Add the `connections:write` scope.\n4. Generate and copy the token starting with `xapp-`.\n\n## 3. Connect it to an agent\n\n1. In UniAI, open **Agents** and select the agent to connect to Slack.\n2. Go to **Integrations** and click **Connect Slack**.\n3. Paste the Bot token and the App-level token.\n4. Click **Connect**.\n\nUniAI verifies that the two tokens come from the same app. Once connected, the page shows **Connected to Slack**.\n\n> Both tokens are stored encrypted on the UniAI server. Tokens are credentials — don't put them in issues, comments, or code repositories. An app already connected to another agent or workspace can't be reused directly; disconnect the original connection first.\n\n## First use\n\nThe first time a member DMs or @-mentions the Bot, they receive an account binding link. Sign in with the UniAI account for that workspace, then return to Slack and send the message again.\n\nThe link is single-use and expires after 15 minutes. Once it expires, message the Bot again for a new one. Multiple Bots in the same UniAI workspace can usually reuse an existing binding if they come from the same Slack workspace.\n\nOnly members of the current UniAI workspace can use the Bot.\n\n## Hand work to the agent in Slack\n\n### Direct messages\n\nOpen the Bot from **Apps** in the Slack sidebar and message it directly — no @-mention needed.\n\n### Channels and group chats\n\nRun `/invite @your-bot` to add the Bot to the channel first, then send `@your-bot your request`. Each thread forms its own session; for follow-ups, keep @-mentioning the Bot in the original thread.\n\nThe Bot only responds to channel messages that explicitly @-mention it. When it runs, it reads the thread's recent context within the scopes its permissions allow.\n\n### Create issues with `/issue`\n\nIn a channel or DM, type:\n\n```text\n/issue Safari stays on the login page after a successful sign-in\n```\n\nThis command needs no @-mention. UniAI hands the description to the agent behind the current Bot, which writes up the title and body and creates the issue. Slack first shows an acknowledgment visible only to you; when the issue is created, you get a notification in your UniAI inbox.\n\n## Manage connections\n\nOpen **Settings → Integrations** to view all connected Slack Bots. Workspace owners and admins can disconnect them.\n\nAfter disconnecting, the Socket Mode connection stops and the Bot no longer receives new messages. Existing conversations and audit records are not deleted.\n\n## Troubleshooting\n\n- **Invalid token when connecting**: check the prefixes and confirm both tokens come from the same app.\n- **App can't be verified**: confirm the manifest includes `users:read`, then reinstall the app after updating permissions.\n- **No DM entry point**: confirm `app_home.messages_tab_enabled` is `true`.\n- **No reply in a channel**: confirm the Bot has been invited to the channel and the message @-mentions it.\n- **`/issue` doesn't exist**: confirm the manifest includes the slash command and the `commands` scope, then reinstall the app.\n- **The Bot doesn't run**: check whether the agent is archived and whether the [runtime](/daemon-runtimes) it uses is online.\n\n## Next steps\n\n- [Chat integrations](/channels) — platform comparison, session isolation, and account binding mechanics.\n- [Chat](/chat) — the conversation and execution model behind the Bot.\n- [Issues](/issues) — the unit of work `/issue` creates."
        },
        {
          "slug": "dingtalk-bot-integration",
          "title": "DingTalk Bot integration",
          "content": "Connect any [agent](/agents) to a DingTalk bot and your team can work with it from inside DingTalk — DM the bot, @-mention it in a group, send it screenshots, or type `/issue` to file a [UniAI issue](/issues) without opening the app.\n\nDingTalk support is community-maintained: it ships in every release, but it carries no official support SLA. Report problems in GitHub issues.\n\nDingTalk uses a **bring-your-own-app (BYO)** model: a workspace admin creates a DingTalk app, adds a Stream-mode robot to it, and pastes its credentials into UniAI. Each agent gets **its own** DingTalk app — so several agents can each have a distinct, separately @-mentionable bot in the same organization. (This differs from [Lark](/lark-bot-integration), where binding is a scan-to-install flow.)\n\nThe whole setup is below and takes about five minutes. You'll end up with two credentials to paste into UniAI:\n\n- an **AppKey** — the app's client id\n- an **AppSecret** — the app's client secret\n\n## Set up your DingTalk app\n\n### 1. Create the app and add a Stream-mode robot\n\n1. Go to the [DingTalk Open Platform](https://open-dev.dingtalk.com) and create an **enterprise internal app** (企业内部应用).\n2. Open the app and add the **robot** (机器人) capability.\n3. In the robot settings, set the **message-receiving mode** to **Stream mode** (Stream 模式 / 推送模式). This is what lets the bot connect out over a long-lived push connection instead of receiving a webhook.\n\nThis is everything UniAI needs at the platform level — it connects out over Stream mode, so you don't configure any public address:\n\n| Setting | Why it's there |\n|---|---|\n| **Enterprise internal app** | The app container that owns the robot and issues the AppKey / AppSecret credentials. |\n| **Robot capability** | Creates the bot identity that gets @-mentioned and posts replies. |\n| **Stream mode** | The bot connects out over a long-lived Stream connection — **no public webhook / URL needed**. |\n| **Robot send permission** | Lets the bot send messages back into DingTalk (the agent's replies and proactive messages). |\n| **Message-read scopes** | So the bot receives 1:1 messages and the group messages that @-mention it. |\n\nThere is **no webhook URL and no OAuth redirect URL**, because the robot runs over Stream mode and BYO doesn't use OAuth.\n\n> DingTalk has no native typing / reaction indicator, so — unlike Slack — the bot instead posts a short \"working on it\" acknowledgement when it starts, and the full reply follows once the agent is done. A quick burst of messages is coalesced into a single acknowledgement.\n\n### 2. Grant the robot its permissions\n\nGrant the robot the scopes it needs so it can **receive messages** and **send messages back** (the robot message-send permission). Without the send permission the agent can run but its reply can't be delivered.\n\n### 3. Copy the AppKey and AppSecret\n\nOpen the app's **凭证与基础信息** (Credentials & Basic Information) and copy:\n\n- the **AppKey** — this is your app's **client id**\n- the **AppSecret** — this is your app's **client secret**\n\n### 4. Connect it in UniAI\n\n1. Open the agent in **Agents → _your agent_** → the **Integrations** tab (or the **Integrations** section in the left sidebar).\n2. Click **Connect DingTalk**.\n3. Paste the **AppKey** and the **AppSecret**, then click **Connect**.\n4. The agent shows **Connected to DingTalk**. The bot is now listening over its own Stream connection.\n\n> Both credentials must be from the **same** DingTalk app, and that app maps to exactly **one** agent. Connecting an app that's already connected to a different agent or workspace is refused. To move an app to another agent, disconnect it first; re-connecting an agent with a **new** app updates that agent's bot in place.\n\n> Setting this up for **multiple agents**? Repeat the whole flow once per agent — each agent gets its own DingTalk app and its own AppKey / AppSecret, and they show up as separate bots in your organization.\n\n## What the integration does\n\n| Surface | Behavior |\n|---|---|\n| **Agent → Integrations** | Owners and admins see **Connect DingTalk**; once connected it flips to a **Connected to DingTalk** badge with a **Disconnect** control. |\n| **DM the bot** | A workspace member messages the bot in a 1:1 chat. The conversation becomes a UniAI [chat](/chat) session with the agent; every message is read. |\n| **@-mention in a group** | Add the bot to the group and @-mention it. Only the mentioning message is read — the bot does not listen to the whole group. |\n| **Send images** | Images in a DM, or sent alongside a group @-mention, land in the conversation for the agent to see — PNG, JPEG, GIF, WebP, or BMP, up to 4 images per message and 10 MB each. Each image is copied into UniAI storage, so it stays visible in the chat after DingTalk's temporary link expires. Images sent with `/issue` go to the created issue instead of the chat turn. Files and voice messages aren't supported. |\n| **`/issue` command** | Starting a message with `/issue <title>` creates a UniAI issue directly from your input, attributed to you, and posts the issue ID and title back into the same conversation. Put the description on following lines. Images sent in the same DingTalk message are attached to the created issue. The command itself does not appear in UniAI Chat; the created issue is the result recorded in UniAI. |\n| **`/new` command** | Starting a message with `/new <your message>` runs that message without prior conversation context. Sending `/new` by itself records the same fresh-start intent for your next non-empty message without creating an empty turn, and the bot confirms when that intent is ready. The pending intent remains active until a later chat task is successfully queued. The conversation history remains intact. |\n| **Reply** | The agent's answer is posted back into the same 1:1 chat or group. |\n\n## Use the bot (members)\n\n### First message: link your account\n\nThe first time you @-mention or DM the bot, it replies with a **link your account** prompt pointing at the in-product **/dingtalk/bind** page. Tap the link, sign in to UniAI, and your DingTalk identity is bound to your UniAI membership — this is what lets the agent act as you (e.g. `/issue` files under your name). The link is single-use and expires in about 15 minutes; just message the bot again for a fresh one.\n\n> Only **members of the workspace** can use the bot. If you aren't a member, or you skip the identity link, the bot won't run — your message is dropped (recorded for audit, without its contents).\n\n### Chat and commands\n\n- **In a group** — add the bot to the group, then `@your-bot <your message>`. Re-mention it for each follow-up (the bot only reads messages that mention it).\n- **In a 1:1 chat** — open the bot and message it directly; no mention needed, every message is read.\n- **Send images** — send screenshots or photos, with or without text; they show up in the conversation for the agent to work with. PNG, JPEG, GIF, WebP, or BMP; up to 4 images per message, 10 MB each.\n- **File an issue** — send `/issue the login redirect is broken on Safari`; add the description on following lines if needed. UniAI creates the issue synchronously, attaches images from that message to it, and posts its ID and title back into the chat.\n- **Start fresh** — send `/new <your message>` to run that message without prior context, or send `/new` by itself to apply the fresh start to your next non-empty message. A bare `/new` gets a confirmation, and its pending intent remains active until a later chat task is successfully queued. Neither form removes the existing conversation history.\n\n## Manage and disconnect\n\nWorkspace-wide management lives in **Settings → Integrations**:\n\n- **Connected bots** lists every bot in the workspace and the agent each is bound to (visible to all members).\n- **Disconnect** is **owner / admin only**. It stops the bot from receiving DingTalk messages and tears down its connection; the installation record is kept for audit, and you can re-connect later.\n\n## Permissions\n\n- **Connect / disconnect** require workspace **owner** or **admin**.\n- **Talking to the bot** requires being a workspace member with a linked DingTalk identity. Everyone else is dropped.\n- Message bodies for dropped messages are never stored — only a drop reason, for audit.\n\n## Next\n\n- [Chat integrations](/channels) — how the channel engine, sessions, and authorization work\n- [Agents](/agents) · [Chat](/chat) · [Issues](/issues)"
        },
        {
          "slug": "vcs-integration",
          "title": "Self-hosted Git providers",
          "content": "> **Self-hosted UniAI only.** This integration is available only when you run\n> UniAI yourself; it is not offered on UniAI Cloud. \"Self-hosted\" here means\n> **UniAI** is self-hosted — typically so it can reach a Git instance on your own\n> network. A server operator must enable it (`MULTICA_VCS_INTEGRATION_ENABLED=true`)\n> and set `MULTICA_VCS_SECRET_KEY`; until then the section does not appear in\n> Settings → Integrations.\n\nUniAI connects to a self-hosted Git provider per workspace: **Forgejo**, **Gitea**, or **GitLab**. Once connected, any pull request (GitLab: merge request) whose branch name, title, or body contains an issue identifier (for example `UNI-123`) is **auto-linked** to that [issue](/issues), appears under **Pull requests** in the issue sidebar, and — when merged with a closing keyword — moves the issue to **Done**. CI for the head commit shows as a checks bar on the card.\n\nThese providers run alongside GitHub: a workspace can use any combination.\n\n> Unlike GitHub, these providers have no \"App\" model. Each workspace stores its own instance URL plus an access token, and registers a webhook on its repository or organisation. The token and webhook secret are encrypted at rest.\n\n## Prerequisites (server)\n\nTurn the integration on for this deployment. It is off by default, so the section stays hidden until you set:\n\n```sh\nMULTICA_VCS_INTEGRATION_ENABLED=true\n```\n\nThe official self-hosted `docker compose` file (`docker-compose.selfhost.yml`) sets this for you.\n\nThen set a base64-encoded 32-byte key so the server can encrypt stored credentials. Without it, the connect form is disabled.\n\n```sh\nopenssl rand -base64 32\n```\n\n```sh\nMULTICA_VCS_SECRET_KEY=<base64 32-byte key>\n```\n\nBoth are required: the switch decides whether the feature is offered, and the key encrypts the stored token and webhook secret. Connect, webhook, and rotate all require both.\n\nSet `MULTICA_PUBLIC_URL` to the server's public base URL so UniAI can show a ready-to-paste webhook URL. Without it, the UI shows only the webhook path and you prepend your own origin.\n\n## Connect a workspace\n\n1. Create an access token in your provider with read access to the repositories you want mirrored:\n   - **Forgejo / Gitea**: Settings → Applications.\n   - **GitLab**: a personal (or group/project) access token with the `read_api` scope.\n2. In UniAI, open **Settings → Integrations → Git providers**.\n3. Choose the **provider**, enter the instance URL (for example `https://forgejo.example.com`) and the access token, then **Connect**. UniAI validates the token against the instance before saving.\n4. Copy the **webhook URL** and **webhook secret** shown after connecting.\n\n> The webhook secret is shown only once. Copy it before leaving the page. Reconnecting the same instance rotates the token and secret.\n\n## Register the webhook\n\nOn the repository (or organisation/group, to cover every repo):\n\n**Forgejo / Gitea** — Settings → Webhooks → Add Webhook → Forgejo/Gitea:\n\n- **Target URL**: the webhook URL from the previous step.\n- **HTTP method** `POST`, **content type** `application/json`.\n- **Secret**: the webhook secret (used to verify the `X-Gitea-Signature` HMAC).\n- **Trigger events**: select **Pull Request**, and **Commit Status** to mirror CI.\n\n**GitLab** — Settings → Webhooks:\n\n- **URL**: the webhook URL.\n- **Secret token**: the webhook secret (sent as `X-Gitlab-Token` and compared verbatim).\n- **Triggers**: enable **Merge request events**, and **Pipeline events** to mirror CI.\n\nUniAI authenticates every delivery against the stored secret, so a webhook without the matching secret is rejected.\n\n## What gets mirrored\n\n- **Pull / merge requests** — open, closed, merged, and draft state, author, branch, and (where the provider supplies them) diff stats.\n- **Issue links** — identifiers in the title, body, or branch link the PR to the issue. A closing keyword (`Closes`/`Fixes`/`Resolves UNI-123`) on a merged PR moves the issue to **Done** once no linked PR is still open.\n- **CI** — Forgejo/Gitea commit statuses and GitLab pipelines aggregate into a passed/failed/pending checks bar for the head commit.\n\n## Agents opening pull requests\n\nCreating PRs needs no provider configuration in UniAI. Agents check out repositories in the runtime and push branches and open PRs using the runtime host's own Git credentials. To let agents work against a provider, ensure the daemon host can authenticate to it — for example an SSH deploy key, or a token in the host's Git credential helper — and add the repository URL as usual. Repository checkout works with any Git URL, so no provider-specific wiring is required."
        }
      ]
    },
    {
      "label": "Self-hosting",
      "labelKey": "self_hosting",
      "pages": [
        {
          "slug": "security-model",
          "title": "Security model",
          "content": "When an agent picks up a task, the daemon spawns an AI coding tool (Codex, Claude Code, and so on) as a child process. Understanding what that process can touch is the whole security model.\n\n## The boundary is the daemon user\n\nBy default a task runs with the **full permissions of the operating-system user running the daemon**. It can read and write every file that user can, use that user's credentials, and reach the network without restriction.\n\nUniAI makes no filesystem-sandbox guarantee. One narrow exception exists today — Windows, where you have explicitly opted into Codex's native sandbox — but which platform and configuration combinations sandbox anything is a compatibility detail that moves with tool versions. Treat every task as unsandboxed and put the boundary outside the daemon.\n\n> UniAI does not sandbox the filesystem for you. If the daemon runs as your personal user account, a task can read your SSH keys, edit your shell profile, and delete your documents. Isolation has to come from the boundary you put the daemon in.\n\nThis is deliberate. Agents are asked to install dependencies, run builds, use your cloud CLIs, and drive tooling that expects a normal home directory. A partial filesystem sandbox breaks that work in ways that are hard to diagnose — the tool reports \"not logged in\" or silently uses the wrong account — while still not protecting the thing that matters most, since it cannot stop a task from reading credentials and sending them over the network.\n\nSo UniAI does not pretend to be the boundary. Put one around it.\n\n## Recommended setups\n\nPick whichever fits your infrastructure — they are listed from lightest to strongest:\n\n1. **Dedicated Unix user.** Create a `uniai` user, give it only the repositories and credentials the agents need, and run the daemon as that user. Your own account stays untouched.\n2. **Container.** Run the daemon in a container with only the mounts and secrets the agents need.\n3. **Virtual machine.** Full isolation, at the cost of provisioning a machine.\n\nWhichever you choose, treat the credentials reachable from that environment as credentials the agent may use: scope tokens narrowly, prefer per-purpose deploy keys over your personal SSH key, and avoid leaving unrelated production credentials in that user's home directory.\n\n## What UniAI does isolate\n\nThese are real, but they are conveniences and blast-radius reduction — not a security boundary against a task that actively tries to escape:\n\n- **Per-task working directory.** Each task gets its own workdir under `~/multica_workspaces/`, so concurrent tasks do not collide on the same checkout.\n- **Per-task agent state.** Codex tasks get a task-scoped `CODEX_HOME` holding config, sessions, and skills, so per-task settings do not pollute your `~/.codex/`.\n- **Task-scoped API tokens.** The `MULTICA_TOKEN` handed to a task is bound to that agent and that task by the server, so a task cannot act as you or as another agent through the UniAI API.\n\n## What is not a boundary\n\n- **The coding tool's own sandbox and approval settings.** UniAI runs agents unattended, so approval prompts are answered automatically. On the default path the filesystem sandbox is off as well: Codex runs with `sandbox_mode = \"danger-full-access\"` and Claude Code with `--permission-mode bypassPermissions`. The exception is Windows — if you explicitly configure Codex's native sandbox (`windows.sandbox = \"unelevated\"` or `\"elevated\"`), UniAI honors that opt-in and keeps `workspace-write` for those tasks. Worth doing where it fits, but it is one tool on one platform and it does not change how you should scope the daemon user.\n- **Your `HOME` directory layout.** Tasks inherit the daemon user's real `HOME` and `XDG_*` variables, which is what makes host CLIs (`gh`, `aws`, `kubectl`, `gcloud`, `glab`) work inside a task exactly as they do in your shell. It also means everything under that home is reachable.\n\n> On Linux, Codex tasks previously ran under the `workspace-write` sandbox with a redirected per-task `HOME`. That was removed: it left host CLIs unconfigured inside tasks, and because it restricted writes only, it never prevented a task from reading and exfiltrating credentials. Linux now matches the macOS and Windows default.\n\n## Checking what a task ran with\n\nThe daemon logs the effective sandbox mode at warn level when a task starts unsandboxed:\n\n```bash\nuniai daemon logs --lines 200 | grep \"codex sandbox\"\n```\n\nTo confirm the effective Codex configuration, read the managed block in the task's `config.toml` under the task's `CODEX_HOME` — the section between the `# BEGIN multica-managed` and `# END multica-managed` markers is written by the daemon on every run."
        }
      ]
    },
    {
      "label": "Reference",
      "labelKey": "reference",
      "pages": [
        {
          "slug": "cli",
          "title": "Using the CLI",
          "content": "The UniAI CLI both connects local runtimes and manages workspaces, issues, agents, and automations from the terminal. This page covers the common paths; the flags your installed version supports are always defined by each command's own `--help`.\n\n## Installation\n\n**macOS / Linux**\n\n```bash\ncurl -fsSL https://raw.githubusercontent.com/phanducquanguet/usf/feature/customer-portal/scripts/install.sh | bash\n```\n\n**Windows PowerShell**\n\n```powershell\nirm https://raw.githubusercontent.com/phanducquanguet/usf/feature/customer-portal/scripts/install.ps1 | iex\n```\n\nConfirm the install:\n\n```bash\nuniai version\n```\n\n## First connection\n\nConnect to UniAI Cloud:\n\n```bash\nuniai setup\n```\n\nConnect to a self-hosted instance:\n\n```bash\nuniai setup self-host \\\n  --server-url https://api.example.com \\\n  --app-url https://app.example.com\n```\n\n`setup` saves the server address, opens a browser to complete sign-in, and starts the daemon. When it finishes, check:\n\n```bash\nuniai auth status\nuniai daemon status\n```\n\nIf you only need to sign in again without overwriting other configuration, run `uniai login`. On a machine without a browser, create a personal access token in the web settings first, then enter it with:\n\n```bash\nuniai login --token\n```\n\nThe command prompts you to paste the token in the terminal, keeping the full value out of shell history.\n\n## Choosing a workspace\n\nList workspaces and switch the default:\n\n```bash\nuniai workspace list\nuniai workspace switch <slug>\n```\n\nSubsequent commands use this workspace. A single command can override it with `--workspace-id`, or you can set `MULTICA_WORKSPACE_ID`.\n\nInvite members:\n\n```bash\nuniai workspace member invite teammate@example.com\nuniai workspace member invite admin@example.com --role admin\n```\n\n## Common issue operations\n\n```bash\n# View and search\nuniai issue list\nuniai issue get UNI-123\nuniai issue search \"login failure\"\n\n# Create and update\nuniai issue create --title \"Fix login failure\"\nuniai issue status UNI-123 in_progress\nuniai issue assign UNI-123 --to \"Backend Agent\"\n\n# Comments and runs\nuniai issue comment list UNI-123\nuniai issue comment add UNI-123 --content \"Check the regression tests first\"\nuniai issue runs UNI-123\n```\n\nRead long descriptions or comments from stdin to avoid dealing with newlines and quoting:\n\n```bash\nuniai issue create --title \"Upgrade notes\" --description-stdin < notes.md\nuniai issue comment add UNI-123 --content-stdin < review.md\n```\n\nView the messages of a single run, or stop a task:\n\n```bash\nuniai issue run-messages <task-id> --issue UNI-123\nuniai issue cancel-task <task-id> --issue UNI-123\n```\n\n## Agents and skills\n\n```bash\nuniai agent list\nuniai agent get <agent-id>\nuniai agent create --help\nuniai agent update <agent-id> --help\n\nuniai skill list\nuniai skill get <skill-id>\nuniai skill import --url <skill-url>\nuniai agent skills add <agent-id> --skill-ids <skill-id>\n```\n\nWhen an import hits a skill with the same name, the default is to stop without modifying existing content. Pick by intent:\n\n```bash\nuniai skill import --url <skill-url> --on-conflict overwrite\nuniai skill import --url <skill-url> --on-conflict rename\nuniai skill import --url <skill-url> --on-conflict skip\n```\n\n`overwrite` is only allowed for the skill's creator and preserves the original ID and agent bindings.\n\n## Daemon and runtimes\n\n```bash\nuniai daemon start\nuniai daemon stop\nuniai daemon restart\nuniai daemon status --output json\nuniai daemon logs --follow\n\nuniai runtime list\nuniai runtime rename <runtime-id> \"Office Mac\"\nuniai runtime usage <runtime-id>\nuniai runtime activity <runtime-id>\n```\n\nDeleting a runtime that still has active agents bound to it is refused by default. `uniai runtime delete <id> --cascade` unbinds those agents, preserves their configuration and history, and cancels their active tasks.\n\nSee [Daemon and runtimes](/daemon-runtimes) for how it works and for custom profiles.\n\n## Command overview\n\n| Command | Purpose |\n| --- | --- |\n| `issue` | Create, update, assign, and search issues; manage comments, subscribers, labels, properties, and runs |\n| `project` | Manage projects and their resources |\n| `label`, `property` | Manage workspace labels and custom properties |\n| `agent`, `skill`, `squad` | Manage agents, skills, and squads |\n| `autopilot` | Manage automations, triggers, and run history |\n| `workspace` | Create, view, and switch workspaces, and invite members |\n| `repo` | Manage workspace repositories and check them out locally |\n| `daemon`, `runtime` | Start and stop the local daemon; view and manage runtimes |\n| `attachment` | Upload or download attachments |\n| `user profile` | View or update the current user's profile |\n| `auth`, `login`, `setup` | Log in, check auth status, and initialize a connection |\n| `config` | View or edit the current CLI profile's local configuration |\n| `update`, `version` | Update the CLI or print the version |\n\n`uniai chat` reads **the external chat session an agent is currently handling**; it is mainly for agents in chat integrations, not a general command for browsing arbitrary workspace conversations.\n\n## IDs and output formats\n\nIssues use keys like `UNI-123` or full UUIDs; short UUID prefixes are not accepted.\n\n`list` commands for other resources usually print copyable short IDs and support `--full-id` for the full UUID. When a short ID is ambiguous, the CLI asks for more characters or the full UUID. Short task IDs also require `--issue` to identify the owning issue.\n\nMany commands support structured output:\n\n```bash\nuniai issue list --output json\nuniai agent list --output json\n```\n\nScripts should use JSON output rather than parsing the terminal-oriented tables. The output formats and pagination flags each command supports are defined by `uniai <command> --help`.\n\n## Profiles and configuration\n\nThe default configuration lives in `~/.multica/config.json`. Use `--profile <name>` to isolate a separate set of server address, token, default workspace, and daemon state:\n\n```bash\nuniai setup self-host --profile staging \\\n  --server-url https://api.staging.example.com \\\n  --app-url https://app.staging.example.com\n\nuniai issue list --profile staging\n```\n\nNamed profiles live in `~/.multica/profiles/<name>/config.json`. Inspect the current values:\n\n```bash\nuniai config show\nuniai config show --profile staging\n```\n\nWhen the CLI runs inside a daemon-managed agent task, it does not load or modify these human-owned profile files. UniAI API commands authenticate with the task-scoped credential injected by the daemon. `config show` and `config set` use private task-local state; human/local commands such as `login`, `logout`, `setup`, `workspace switch`, local runtime profile path changes, `daemon start` / `stop` / `restart`, `daemon logs`, and `daemon probe-runtimes` are unavailable. `auth status` does not print token material in this context.\n\n`daemon status` and `daemon disk-usage` stay available so an agent can inspect the runtime hosting it, scoped to that runtime: `status` probes the health port the daemon injected, and `disk-usage` scans the workspaces root the daemon injected. Neither accepts `--profile`, `disk-usage` also rejects `--all-profiles` and `--workspaces-root`, and its STATUS column is blank because resolving it would require the human's stored token. Use `du` and `df` for anything outside that scope.\n\nThis protects implicit UniAI CLI profile resolution while preserving the normal `HOME` and XDG variables used by other provider tools. It is not an OS filesystem boundary: processes running as the same system user may still open an explicitly known path. Use a dedicated user, container, VM, or equivalent isolation when that stronger guarantee is required.\n\n> CLI configuration files contain tokens that can access UniAI as you. Don't commit them to a repository, upload them to logs, or share them with others.\n\n## Command reference\n\nThe tables below cover every current top-level command, grouped the way the CLI itself groups them. All commands accept the global flags `--server-url`, `--workspace-id`, `--profile`, and `--debug`. `--output` is registered per command, and the default format varies: `list` commands default to a table, while `get` and `create` commands mostly default to JSON. Full flags are defined by `uniai <command> --help`.\n\n### Core\n\n| Command | Subcommand | Purpose | Key flags |\n| --- | --- | --- | --- |\n| `issue` | `list` | List issues | `--status`, `--priority`, `--assignee`, `--project`, `--metadata` (repeatable), `--limit`, `--offset`, `--sort`, `--full-id` |\n| | `get <id>` | Show a single issue | |\n| | `create` | Create an issue | `--title` (required), `--description` / `--description-stdin` / `--description-file`, `--status`, `--priority`, `--assignee`, `--parent`, `--stage`, `--project`, `--start-date`, `--due-date`, `--attachment` (repeatable) |\n| | `update <id>` | Update issue fields | Same fields as `create`, plus `--position` |\n| | `assign <id>` | Assign or unassign | `--to` (fuzzy name match against members, agents, and squads), `--to-id`, `--unassign` |\n| | `status <id> <status>` | Change status | |\n| | `reorder <id>` | Move within a column | |\n| | `search <query>` | Search issues | `--limit`, `--include-closed` |\n| | `children <id>` | List sub-issues grouped by stage | |\n| | `pull-requests <id>` | List linked PRs | |\n| | `comment list/add/delete/resolve/unresolve` | Manage comments | `add`: `--content` / `--content-stdin` / `--content-file`, `--parent`, `--attachment`; `list`: `--since`, `--thread`, `--tail` |\n| | `subscriber list/add/remove <issue-id>` | Manage subscribers | `--user`, `--user-id` (defaults to the current user) |\n| | `label list/add/remove <issue-id>` | Manage labels on an issue | |\n| | `metadata list/get/set/delete <issue-id>` | Manage issue-level key-value metadata | |\n| | `property list/set/unset <issue-id>` | Set custom property values | `set`: `--name`, `--value` |\n| | `runs <issue-id>` | View run history | `--full-id` |\n| | `run-messages <task-id>` | View the messages of a single run | `--since`, `--issue` |\n| | `usage <issue-id>` | View aggregated token usage | |\n| | `rerun <id>` | Re-enqueue a run for the current assignee | |\n| | `cancel-task <task-id>` | Cancel a running or queued task | `--issue` |\n| `project` | `list/get/create/update/delete` | Manage projects | |\n| | `status <id> <status>` | Change project status | |\n| | `resource list/add/update/remove` | Manage project resources | `--type`, `--url` |\n| `label` | `list/get/create/update/delete` | Manage workspace labels | |\n| `property` | `list/get/create/update/archive/unarchive` | Manage workspace custom properties | `create`: `--name`, `--type`, `--option` (repeatable); `list`: `--include-archived`; the type cannot be changed after creation |\n| `agent` | `list/get/create/update/archive/restore` | Manage agents | `--name`, `--runtime-id` (required for `create`), `--instructions`, `--model`, `--thinking-level`, `--mcp-config`, `--permission-mode`, `--max-concurrent-tasks` |\n| | `copy <agent-id>` | Copy into a new agent; the original is untouched | `--name` (defaults to the original name plus ` (copy)`), `--runtime-id` (copying to another runtime also requires `--model`), `--no-skills`; secret configuration such as `custom_env`, `mcp_config`, and `runtime_config` is not copied — re-provide it with the same flags as `create` |\n| | `tasks <id>` | View an agent's tasks | |\n| | `avatar <id>` | Upload an avatar | |\n| | `env get/set <agent-id>` | Read and write custom environment variables (owner and admin only) | |\n| | `skills list/set/add <agent-id>` | Manage attached skills | `--skill-ids` (`set` replaces the full list, `add` appends) |\n| `autopilot` | `list/get/create/update/delete` | Manage automations | `create`: `--title`, `--agent`, `--mode` (all required), `--priority`, `--project`, `--subscriber` (repeatable) |\n| | `trigger <id>` | Trigger a run manually | |\n| | `runs <id>` | View run history | |\n| | `trigger-add/trigger-update/trigger-delete/trigger-rotate-url` | Manage schedule and webhook triggers | |\n| `workspace` | `list/get/create/update/switch` | Manage workspaces; `switch` sets the current profile's default workspace | |\n| | `member list/invite <email>` | View members, send invites | `invite`: `--role` (member or admin, default member) |\n| `repo` | `list/add/remove/checkout` | Manage workspace repositories and check them out locally | `--url` (repeatable); `checkout`: `--ref` |\n| `skill` | `list/get/create/update/delete` | Manage skills | |\n| | `import` | Import a skill from a URL or local file | `--url` / `--file`, `--on-conflict` (`fail`, `overwrite`, `rename`, `skip`; default `fail`) |\n| | `search <query>` | Search skills | |\n| | `files list/upsert/delete <skill-id>` | Manage the files inside a skill | |\n| `squad` | `list/get/create/update/delete` | Manage squads (`delete` archives) | |\n| | `member list/add/set-role/remove` | Manage squad members | |\n| | `activity <issue-id> <outcome>` | Record a leader evaluation | |\n| `chat` | `history`, `thread [id]` | Read the external chat session an agent is currently handling | `--limit`, `--before` |\n\n### Runtime\n\n| Command | Subcommand | Purpose | Key flags |\n| --- | --- | --- | --- |\n| `daemon` | `start` | Start the local daemon | `--foreground`, `--device-name`, `--runtime-name`, `--poll-interval`, `--heartbeat-interval`, `--agent-timeout` (`0` means no limit), `--max-concurrent-tasks`, `--no-auto-update`; each has a matching `MULTICA_*` environment variable |\n| | `stop` / `status` / `restart` | Stop, check status, restart (`restart` takes the same flags as `start`) | |\n| | `logs` | View daemon logs | `--follow`, `--lines` |\n| | `disk-usage` | View local disk usage | `--by-workspace`, `--by-task`, `--top` |\n| `runtime` | `list/usage/activity/update/rename/delete` | View and manage runtimes | `delete`: `--cascade` (unbinds bound agents, preserves their data, and cancels their tasks) |\n| | `profile list/create/update/delete` | Manage custom runtime profiles | |\n| | `profile set-path/unset-path <profile-id>` | Pin a local executable path (local only, never uploaded to the server) | |\n\n### Additional\n\n| Command | Subcommand | Purpose | Key flags |\n| --- | --- | --- | --- |\n| `auth` | `status` / `logout` | Check auth status; `logout` only deletes the locally saved token and does not revoke it on the server | |\n| `user` | `profile get/update` | View or update the current user's profile | |\n| `login` | — | Sign in via the browser and configure all workspaces automatically | `--token` (prompts interactively in the terminal when passed without a value) |\n| `setup` | `cloud` (default), `self-host` | Save the server address, complete sign-in, and start the daemon | `self-host`: `--server-url`, `--app-url`, `--port`, `--frontend-port` |\n| `attachment` | `download <attachment-id>`, `upload <path>` | Download or upload attachments | `download`: `--output-dir`; `upload`: `--task` |\n| `config` | `show`, `set <key> <value>` | View or edit the current profile's local configuration | Precedence: command-line flags > environment variables > `config.json` > built-in defaults; set an empty string to clear a value |\n| `update` | — | Update the CLI to the latest version | |\n| `version` | — | Print version information | `--output` (`text` or `json`) |\n\n## Next steps\n\n- [Authentication and tokens](/auth-tokens) — creating, renewing, and revoking PATs.\n- [Create and configure an agent](/agents-create) — the full semantics of every `agent create` field."
        },
        {
          "slug": "auth-tokens",
          "title": "Authentication and tokens",
          "content": "In day-to-day use of UniAI you mainly deal with two kinds of credentials: browser sign-in sessions and personal access tokens. Browser sessions serve the web app; personal access tokens serve the CLI, the daemon, scripts, and the API.\n\n## Browser sign-in sessions\n\nAfter signing in with an email verification code or Google, UniAI stores a JWT in an HttpOnly cookie named `multica_auth`. The browser sends it automatically; JavaScript cannot read it directly.\n\nSessions last 30 days by default. Self-hosting admins can adjust the lifetime of sessions issued from then on with `AUTH_TOKEN_TTL`. Logging out clears the auth and CSRF cookies in the current browser.\n\nBrowser cookies are not meant to be copied into scripts or the CLI; use a personal access token to access UniAI from a terminal.\n\n## Personal access tokens\n\nA personal access token (PAT) starts with `mul_` and represents your account. It can access every workspace and API you have access to, so guard it like a password.\n\nWhen creating a token under **Settings → API Token**, you provide a name and pick an expiry of 30 days, 90 days, 1 year, or never; 90 days is preselected. The full token is shown exactly once; after the dialog closes, UniAI keeps only:\n\n- the token's hash;\n- the first few characters, for identification;\n- the name, creation time, expiry, and last-used time.\n\nThe full value cannot be recovered. If you lose it, revoke the old token and create a new one.\n\n> Don't put a PAT in repositories, issues, comments, screenshots, or logs, and don't pass it directly in shell commands that get saved.\n\n## The CLI and PATs\n\nWhen you run `uniai login`, the CLI completes sign-in through the browser, then creates a PAT valid for 90 days and saves it to the current profile's config file:\n\n```text\n~/.multica/config.json\n~/.multica/profiles/<name>/config.json\n```\n\nThe daemon connects to UniAI with the same PAT. A `mul_` PAT with an expiry renews automatically when less than 7 days remain, extending it to 90 days from that moment. A failed renewal leaves the token unchanged; once a token has expired or been revoked, run `uniai login` again.\n\nOn a machine without a browser, create a PAT on the web first and let the CLI prompt for it safely:\n\n```bash\nuniai login --token\n```\n\n## Using a PAT in API requests\n\nPut the PAT in the `Authorization` header:\n\n```bash\nexport MULTICA_TOKEN='mul_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'\n\ncurl https://uniai.unicomhub.com/api/me \\\n  -H \"Authorization: Bearer $MULTICA_TOKEN\"\n```\n\nWorkspace-level API calls also need the workspace, as required by the endpoint:\n\n```bash\ncurl https://uniai.unicomhub.com/api/issues \\\n  -H \"Authorization: Bearer $MULTICA_TOKEN\" \\\n  -H \"X-Workspace-ID: $MULTICA_WORKSPACE_ID\"\n```\n\nIn scripts, take the token from a secret manager or a protected environment variable — never hardcode it. On self-hosted instances, replace the domain with your own public API address.\n\n## Logging out and revocation\n\n`uniai auth logout` only deletes the PAT saved in the current CLI profile; logging out on the web only deletes the current browser's cookies. Neither revokes the personal access token on the server.\n\nIf a token may have leaked, revoke it immediately under **Settings → API Token**. Once revoked, the PAT can never be used again, and any other machines and scripts that saved it lose access too.\n\n## Temporary tokens for agent runs\n\nWhen the daemon claims a task, the server creates a temporary token for that run, prefixed with `mat_`. It is bound to the current user, workspace, agent, and task, is valid for at most 24 hours, and is cleaned up when the task ends.\n\nThe daemon injects this temporary token into the AI coding tool instead of handing the user's PAT to the agent. Requests made by the agent are therefore recorded as agent actions, and the token can't be used to reach sensitive operations restricted to users or owners.\n\nThese tokens are created automatically by the server; users never need to save or manage them.\n\n## Other machine credentials\n\nThe server also recognizes two credentials used in internal or managed scenarios:\n\n| Prefix | Purpose | Managed by |\n| --- | --- | --- |\n| `mcn_` | UniAI Cloud Node connections | UniAI Cloud Fleet |\n| `mdt_` | Workspace-scoped daemon authentication protocol | Internal server flows |\n\nRegular Cloud and self-hosted installations never need to create them manually. The user-side CLI and daemon always use `mul_` PATs; don't construct tokens with other prefixes yourself.\n\n## Next steps\n\n- [Using the CLI](/cli) — `login`, `auth status`, and profile configuration.\n- [Daemon and runtimes](/daemon-runtimes) — where PAT renewal happens."
        }
      ]
    }
  ],
  "vi": [
    {
      "label": "Get started",
      "labelKey": "get_started",
      "pages": [
        {
          "slug": "index",
          "title": "Chào mừng",
          "content": "## UniAI làm được gì\n\n- **Giao việc cho agent** — giao issue như giao cho một đồng đội; agent đăng tiến độ và bàn giao kết quả.\n- **Chia sẻ toàn bộ ngữ cảnh** — thảo luận, bản ghi và sản phẩm bàn giao đều nằm trong không gian làm việc, cả đội xem được bất cứ lúc nào.\n- **Để công việc tự vận hành** — thiết lập lịch chạy và để nhiều agent tự động phối hợp với nhau.\n- **Đúc kết bí quyết của đội** — lưu cách làm hiệu quả thành skill mà mọi agent đều dùng lại được.\n\n![Bảng không gian làm việc: thành viên con người và nhiều agent cùng đẩy công việc tiến lên](/images/docs/workspace-overview.png)\n\n## Bắt đầu từ đâu\n\n- **[Hoàn thành lượt chạy đầu tiên](/cloud-quickstart)** — tạo một agent, giao task đầu tiên và xem kết quả trả về.\n- **[Khái niệm cốt lõi](/concepts)** — làm quen toàn bộ đối tượng cốt lõi của UniAI và cách chúng liên kết, trong ba phút.\n\n## Các trang được xem nhiều nhất\n\n- [Đưa agent vào việc](/triggering-agents) — bốn cách kích hoạt một agent.\n- [Tạo và cấu hình agent](/agents-create) — tạo agent đầu tiên từ trang trắng hoặc với AI.\n- [Cài đặt công cụ lập trình AI](/install-agent-runtime) — trao cho máy tính của bạn khả năng thực thi.\n- [Task](/tasks) — đọc trạng thái, số lần thử lại và log của mọi lượt chạy."
        },
        {
          "slug": "cloud-quickstart",
          "title": "Bắt đầu nhanh",
          "content": "> Trước khi agent có thể thực thi, máy tính chạy nó cần cài và đăng nhập sẵn ít nhất một [công cụ lập trình AI được hỗ trợ](/providers) — ví dụ Claude Code, Codex hoặc Cursor. UniAI không kèm sẵn các công cụ này.\n\n## 1. Đăng nhập và mở không gian làm việc\n\nĐăng nhập trên web ([uniai.unicomhub.com](https://uniai.unicomhub.com)). Hỗ trợ cả mã xác thực qua email lẫn Google. Bạn sẽ kết nối máy tính của mình từ dòng lệnh ở bước kế tiếp.\n\n## 2. Kết nối một máy tính\n\nAgent thực thi công việc trên những máy tính bạn kết nối. Mở **Cấu hình → Runtime** ở cuối thanh bên, bấm **Add a computer** ở góc trên bên phải, rồi chạy hai lệnh trong hộp thoại trên terminal của máy đích:\n\n  **macOS / Linux**\n\n  ```bash\n  curl -fsSL https://raw.githubusercontent.com/phanducquanguet/usf/feature/customer-portal/scripts/install.sh | bash\n  uniai setup\n  ```\n\n  **Windows PowerShell**\n\n  ```powershell\n  irm https://raw.githubusercontent.com/phanducquanguet/usf/feature/customer-portal/scripts/install.ps1 | iex\n  uniai setup\n  ```\n\n  `uniai setup` mở trình duyệt để hoàn tất đăng nhập, rồi để daemon chạy nền. Khi daemon đã online, hộp thoại thường phát hiện ra máy tính trong vòng một phút.\n\n**Dấu hiệu thành công**: một máy tính online xuất hiện trong danh sách runtime.\n\n![Hộp thoại Add a computer: hai lệnh và thông báo đang chờ phát hiện](/images/docs/quickstart-add-computer.png)\n\n## 3. Tạo agent\n\nMở **Không gian làm việc → Agent** trong thanh bên, bấm **New agent** và chọn một hướng:\n\n- **Start blank**: tự điền các trường. Chỉ tên là bắt buộc — xác nhận runtime và công cụ lập trình AI, rồi tạo.\n- **Build with AI**: mô tả điều bạn muốn, Agent Builder sẽ đặt câu hỏi và sinh cấu hình. Hướng này cần một runtime đang online.\n\nMọi thứ còn lại đều tinh chỉnh được về sau — xem [Tạo và cấu hình agent](/agents-create).\n\n**Dấu hiệu thành công**: agent của bạn xuất hiện trong danh sách agent và hiển thị là online.\n\n![Màn hình chọn điểm khởi đầu khi tạo agent: Start blank và Build with AI](/images/docs/quickstart-new-agent.png)\n\n## 4. Giao cho nó issue đầu tiên\n\nBấm **New Issue** ở đầu thanh bên (hoặc nhấn `C`). Ở chế độ agent mặc định:\n\n1. Ở mục **Created by**, chọn agent bạn vừa tạo.\n2. Mô tả công việc trong một hai câu, ví dụ \"Giải thích không gian làm việc này có thể dùng vào việc gì.\"\n3. Gửi. UniAI tạo issue, đặt agent làm người phụ trách và bắt đầu thực thi ngay.\n\n![Hộp thoại tạo issue ở chế độ agent: Created by đặt là UniAI Assistant](/images/docs/quickstart-new-issue.png)\n\n## 5. Theo dõi tiến độ và kết quả\n\nSau khi gửi, mở issue: nhật ký thực thi hiển thị trạng thái lượt chạy, còn tiến độ và phản hồi của agent xuất hiện trong dòng thời gian.\n\n**Dấu hiệu thành công**: trạng thái trong nhật ký thực thi chuyển thành Completed và phản hồi của agent xuất hiện trong dòng thời gian. Để xem toàn bộ bản ghi lượt chạy, bấm **View transcript**.\n\n![Phản hồi của agent trong issue và một nhật ký thực thi đã hoàn thành](/images/docs/quickstart-run-result.png)\n\n## Sự cố thường gặp\n\n| Triệu chứng | Kiểm tra trước tiên |\n|---|---|\n| Không tìm thấy runtime | Xác nhận công cụ lập trình AI đã cài và chạy được trong terminal, rồi chạy lại `uniai setup`. |\n| Runtime hiển thị offline | Chạy `uniai daemon status`, và `uniai daemon start` nếu daemon chưa chạy. |\n| Issue kẹt ở trạng thái xếp hàng | Xác nhận runtime mà agent dùng đang online. Khi runtime chạm giới hạn chạy đồng thời, task mới sẽ tiếp tục xếp hàng. |\n\n## Bước tiếp theo\n\n- [Khái niệm cốt lõi](/concepts) — gặp toàn bộ các đối tượng cốt lõi của UniAI trong ba phút.\n- [Cách UniAI hoạt động](/how-uniai-works) — issue, agent, runtime và mỗi lượt chạy liên hệ với nhau ra sao.\n- [Đưa agent vào việc](/triggering-agents) — không chỉ giao việc: @nhắc, chat và Tự động hóa."
        },
        {
          "slug": "concepts",
          "title": "Khái niệm cốt lõi",
          "content": "![Sơ đồ đối tượng cốt lõi của UniAI: bên trong một không gian làm việc, một issue kích hoạt agent qua giao việc hoặc @nhắc, agent tạo task, task thực thi trên một runtime, và kết quả được ghi ngược về issue; chat và Tự động hóa là hai nguồn kích hoạt nữa, skill và đội gắn vào agent, dự án tổ chức các issue, và thông báo đổ về hộp thư đến](/images/docs/concepts-core-objects.png)\n\n## Đối tượng cơ bản\n\n### Không gian làm việc\n\nPhạm vi khép kín nơi một nhóm cùng làm việc; mọi công việc và cấu hình đều diễn ra bên trong nó. Con người và agent cộng tác trong cùng một không gian làm việc. Xem [Không gian làm việc](/workspaces).\n\n### Issue\n\nMột phần việc cần làm, cùng phần mô tả, thảo luận, trạng thái và lịch sử tích lũy quanh nó. Issue là đơn vị cơ bản của công việc hằng ngày trong UniAI; người phụ trách có thể là một thành viên, một agent, hoặc một đội. Xem [Issue](/issues).\n\n### Dự án\n\nGom các issue liên quan dưới một mục tiêu, theo dõi tiến độ tổng thể, và có thể gắn tài nguyên như repo và thư mục để dùng trong lúc thực thi. Xem [Dự án](/projects).\n\n## Agent và thực thi\n\n### Agent\n\nMột cộng sự AI trong không gian làm việc — một cấu hình tái sử dụng được: tên, chỉ dẫn, model, skill, Access và runtime. Agent không phải một tiến trình chạy dài; nó chỉ thực thi khi được kích hoạt. Xem [Agent](/agents).\n\n### Skill\n\nMột gói năng lực tái sử dụng được. Chỉ dẫn định nghĩa agent là ai; skill mô tả một loại công việc được làm như thế nào, và gắn được vào nhiều agent. Xem [Skills](/skills).\n\n### Runtime\n\nNơi việc thực thi thực sự diễn ra: một máy tính kết nối với UniAI cùng các công cụ lập trình AI trên đó. Agent là danh tính; runtime là chiếc máy thực thi nó. Xem [Daemon và runtime](/daemon-runtimes).\n\n### Task\n\nMột bản ghi thực thi cụ thể của agent. Mỗi lần kích hoạt sinh ra một task, được một runtime hoàn thành rồi ghi kết quả ngược về. Một issue có thể sinh nhiều task theo thời gian; một lượt chạy xong không có nghĩa issue đã xong. Xem [Task](/tasks).\n\n## Cộng tác và tự động hóa\n\n### Đội\n\nMột nhóm agent và thành viên do một agent leader dẫn dắt. Giao issue cho đội và leader sẽ điều phối công việc. Xem [Đội](/squads).\n\n### Chat\n\nMột cách trò chuyện không gắn với issue nào — hợp với câu hỏi và thử nghiệm nhanh. Mỗi tin nhắn kích hoạt một lượt chạy. Xem [Chat với agent](/chat).\n\n### Hộp thư đến\n\nTrung tâm thông báo của thành viên: hoạt động trên các issue đang theo dõi, lượt @nhắc và giao việc đều đổ về đây. Agent không dùng hộp thư đến. Xem [Hộp thư đến và theo dõi](/inbox).\n\n### Tự động hóa\n\nTự động kích hoạt lượt chạy của agent theo lịch hoặc từ sự kiện bên ngoài; bạn cũng chạy tay được. Xem [Tự động hóa](/autopilots).\n\n## Các đối tượng liên kết ra sao\n\n- **Không gian làm việc** là chiếc hộp chứa tất cả; con người và **agent** cộng tác bên trong nó.\n- Một **issue** ghi lại một phần việc; các issue liên quan được tổ chức bằng **dự án**.\n- Giao việc, `@`nhắc, **chat**, hoặc một **Tự động hóa** kích hoạt agent, sinh ra một **task**.\n- Task hoàn thành trên một **runtime**, và kết quả được ghi ngược về nơi nó được kích hoạt.\n- **Skill** giúp tái sử dụng những gì hiệu quả giữa các agent; **đội** giúp nhiều agent phối hợp cùng nhau.\n- Mọi thông báo cho con người dọc đường đều về **hộp thư đến**.\n\n## Bước tiếp theo\n\n- [Đưa agent vào việc](/triggering-agents) — điểm khởi đầu cho bốn cách kích hoạt.\n- [Cách UniAI hoạt động](/how-uniai-works) — đường đi đầy đủ của một lượt chạy."
        },
        {
          "slug": "how-uniai-works",
          "title": "Cách UniAI hoạt động",
          "content": "Khi bạn giao một issue cho agent, UniAI tạo một task để một runtime trên máy tính đã kết nối nhận lấy. Runtime gọi một công cụ lập trình AI cục bộ và ghi tiến độ cùng kết quả ngược về issue ban đầu.\n\n## Trọn đường đi của một lượt chạy\n\n![Sơ đồ một lượt chạy: bên trái, vùng UniAI ghi nhận và lập lịch — một issue kích hoạt việc tạo task; bên phải, trong vùng \"máy tính của bạn\", daemon nhận task, khởi chạy công cụ lập trình AI và đọc ghi thư mục làm việc; các thông điệp tiến độ đổ về nhật ký thực thi theo thời gian thực, và daemon ghi kết quả cuối cùng ngược về issue](/images/docs/how-a-run-works.png)\n\n1. **Issue cung cấp ngữ cảnh.** Issue chứa mô tả công việc, phần thảo luận và người phụ trách. Khi issue được giao cho một agent, hệ thống dùng chỉ dẫn, model, skill và cấu hình runtime của agent đó.\n2. **UniAI tạo một task.** Task vào hàng đợi; nếu không có runtime nào online, nó chờ ở đó.\n3. **Một runtime nhận task.** Một runtime đang online trên máy tính đã kết nối nhận task và gọi công cụ lập trình AI mà agent được cấu hình dùng.\n4. **Công cụ lập trình AI thực thi cục bộ.** Công cụ đọc thư mục làm việc, chạy lệnh và tạo ra kết quả.\n5. **Kết quả được ghi ngược về issue.** Tiến độ, bình luận và kết quả xuất hiện trong dòng thời gian và nhật ký thực thi của issue.\n\nDanh sách đầy đủ về issue, agent, task, runtime và cách chúng liên hệ với nhau, xem [Khái niệm cốt lõi](/concepts).\n\n## Ranh giới dữ liệu và thực thi\n\n| UniAI | Máy tính đã kết nối |\n|---|---|\n| Không gian làm việc, issue, bình luận và trạng thái | Công cụ lập trình AI và thông tin xác thực của chúng |\n| Cấu hình agent và skill | Thư mục mã nguồn và file cục bộ |\n| Trạng thái task, bản ghi lượt chạy và kết quả | Thay đổi file và thực thi lệnh thực tế |\n\nRanh giới này giống nhau trên UniAI Cloud và trong các bản triển khai tự host.\n\n> Một ngoại lệ: mọi thứ lưu vào `custom_env` của agent được cất trên UniAI server và truyền cho runtime lúc thực thi. Đừng đặt vào `custom_env` những bí mật tuyệt đối không được rời khỏi máy bạn.\n\n## Agent được kích hoạt thế nào\n\nAgent không bao giờ tự bắt đầu làm việc — mỗi lượt chạy đều do một hành động tường minh kích hoạt: giao issue, @nhắc agent trong bình luận, chat trực tiếp với nó, hoặc một Tự động hóa. Cách chọn giữa bốn kiểu này và các quy tắc chung của chúng, xem [Đưa agent vào việc](/triggering-agents).\n\n## Lượt chạy hoàn tất và issue hoàn tất\n\nKhi nhật ký thực thi hiển thị Completed, điều đó chỉ có nghĩa một lượt chạy đã kết thúc. Issue vẫn có thể tiếp nhận thêm thảo luận, yêu cầu bổ sung, hoặc một lượt kích hoạt agent nữa. Issue xong hay chưa phụ thuộc vào tiến độ thực tế của công việc và trạng thái của nó.\n\n## Bước tiếp theo\n\n- [Daemon và runtime](/daemon-runtimes) — máy tính kết nối với UniAI ra sao, và điều gì xảy ra khi runtime online hay offline.\n- [Task](/tasks) — trạng thái task, quy tắc thử lại và hủy.\n- [Giao issue cho agent](/assigning-issues) — cách cộng tác phổ biến nhất."
        },
        {
          "slug": "tutorial",
          "title": "Hướng dẫn đầy đủ",
          "content": "UniAI là nền tảng nơi con người và AI agent làm việc cùng nhau. Hướng dẫn này bắt đầu từ con số không và đi qua:\n\n- tự dựng một không gian làm việc của riêng bạn\n- cộng tác giữa người và agent, và cộng tác tự động giữa các agent với nhau\n- giao việc lặp đi lặp lại cho tự động hóa\n- đúc kết những gì hiệu quả thành skill, và cải tiến agent của bạn theo thời gian\n\nDự án ví dụ là một website cá nhân, dùng để đi hết luồng làm việc tổng thể và các tính năng cốt lõi của UniAI từ đầu đến cuối.\n\n**Trước khi bắt đầu:**\n\n- Một tài khoản UniAI (đăng ký tại [uniai.unicomhub.com](https://uniai.unicomhub.com))\n- Một máy tính đã cài và đăng nhập ít nhất một công cụ lập trình AI, ví dụ Claude Code hoặc Codex. Danh sách đầy đủ các công cụ được hỗ trợ nằm ở [Cài đặt runtime cho agent](/install-agent-runtime).\n\n## Tạo không gian làm việc\n\nKhông gian làm việc là nơi một nhóm làm việc: issue, dự án và agent đều thuộc về một không gian làm việc. Luồng onboarding sau khi đăng ký sẽ dẫn bạn tạo không gian làm việc đầu tiên. Nếu bạn được mời vào một nhóm, đăng nhập sẽ đưa bạn thẳng vào không gian làm việc của nhóm đó.\n\nHướng dẫn này tạo một không gian làm việc mới để thực hành. Bấm vào tên không gian làm việc ở đầu thanh bên và chọn **Create workspace** (tạo không gian làm việc):\n\n![Lối vào tạo không gian làm việc](/images/docs/tutorial-create-workspace-menu.png)\n\nCó hai trường cần điền lúc tạo:\n\n- **Name**: tên hiển thị với thành viên; có thể đổi sau.\n- **URL**: slug trong địa chỉ của không gian làm việc — phần `my-team` trong `uniai.unicomhub.com/my-team`. Không đổi được sau khi tạo.\n\n![Trang tạo không gian làm việc](/images/docs/tutorial-create-workspace-form.png)\n\nTạo xong, không gian làm việc mới mở ra. Thanh bên bên trái là điểm xuất phát của mọi bước sau này; danh sách issue ở giữa vẫn trống, và vài bước tiếp theo chính là để agent lấp đầy nó.\n\n![Không gian làm việc vừa tạo, còn trống](/images/docs/tutorial-empty-workspace.png)\n\n## Kết nối máy tính của bạn\n\nAgent thực thi công việc trên những máy tính bạn kết nối, dùng các công cụ lập trình AI cài trên các máy đó.\n\nTrên trang Runtime, bấm **Add a computer** (thêm máy tính) và làm theo hướng dẫn để cài UniAI CLI rồi chạy `uniai setup` từ dòng lệnh. Daemon đăng ký máy tính này thành một runtime và phát hiện các công cụ lập trình AI đã cài. Mở **Cấu hình → Runtime** ở cuối thanh bên và xác nhận máy tính đang online:\n\n![Danh sách runtime với máy tính này đang online](/images/docs/tutorial-runtimes-list.png)\n\nMở máy tính đó ra để xem từng công cụ được phát hiện:\n\n![Chi tiết runtime: các công cụ lập trình AI được phát hiện](/images/docs/tutorial-runtime-detail.png)\n\nDaemon chạy nền và tự kết nối lại — xem [Daemon và runtime](/daemon-runtimes).\n\n## Tạo agent đầu tiên\n\nAgent là thành viên thực thụ của không gian làm việc: nó được giao issue, đăng bình luận và trò chuyện với bạn. Agent đầu tiên là một trợ lý lo các việc vặt thường ngày của không gian làm việc; nhiều bước sau trong hướng dẫn này sẽ được giao cho nó.\n\nMở **Agent** trong thanh bên, bấm **New agent**, chọn **Start blank** (bắt đầu từ trống), và điền ba trường:\n\n- **Name**: `UniAI helper`.\n- **Runtime** và **Model**: chọn máy tính đã kết nối ở bước trước và một trong các công cụ lập trình AI của nó, rồi chọn một model công cụ đó hỗ trợ. Hướng dẫn này dùng Claude và Sonnet.\n- **Chỉ dẫn**: prompt được cấp cho agent ở mỗi lượt chạy. Nêu rõ nó chịu trách nhiệm việc gì và không đụng vào việc gì:\n\n```text\nYou are the helper of this workspace.\n\nYou handle what members ask for in issues and chat: create or update\nissues, adjust statuses, and answer questions about what is going on\nin the workspace.\n\nKeep replies short. After finishing something, reply with a one-line\nsummary of what you did. Do not touch code or repositories. If a\nrequest is ambiguous, ask before acting.\n```\n\n![Trang tạo agent](/images/docs/tutorial-create-agent-form.png)\n\nCác trường còn lại để mặc định rồi tạo. Quay lại trang **Agent**, UniAI helper đã online và sẵn sàng nhận việc:\n\n![Chi tiết agent: UniAI helper đang online](/images/docs/tutorial-agent-detail.png)\n\n## Agent thứ hai, tạo qua chat\n\nXây website cần một kỹ sư biết viết code. Lần này không phải điền form nữa — không gian làm việc đã có một agent, nên việc tạo agent tiếp theo giao được cho nó.\n\nMở **Chat** trong thanh bên, chọn UniAI helper, và bảo nó tạo một agent tên Engineer trên cùng runtime, model là Opus, chịu trách nhiệm xây và bảo trì website cá nhân. UniAI helper thực hiện việc tạo trên máy tính đó và trả lời kèm tên cùng model của agent mới. Mặc định chỉ người tạo mới chạy được agent mới; trong hướng dẫn này cứ giữ nguyên như vậy.\n\n![Nhờ trợ lý trong chat tạo Engineer](/images/docs/tutorial-chat-create-engineer.png)\n\nMở **Agent → Engineer** để kiểm tra kết quả: model là Opus, và phần chỉ dẫn do UniAI helper sinh ra — phạm vi trách nhiệm, cách lấy code từ repo, và yêu cầu khi nộp thay đổi. Chỉ dẫn sửa được bất cứ lúc nào.\n\n![Chỉ dẫn của Engineer](/images/docs/tutorial-engineer-instructions.png)\n\n## Chuẩn bị repo cho website\n\nCode của website nằm trong một repo GitHub. Việc tạo và liên kết repo cũng giao cho agent — với điều kiện máy tính này đã cài và đăng nhập [GitHub CLI](https://cli.github.com).\n\nTrong **Chat**, chọn Engineer và bảo nó tạo một repo trống tên `personal-website` dưới tài khoản GitHub của bạn (máy này có sẵn GitHub CLI), rồi đăng ký repo vào không gian làm việc bằng `uniai repo add`. Engineer tạo repo tại máy, đăng ký vào không gian làm việc, và trả lời kèm địa chỉ repo. Tin nhắn không nói rõ public hay private, nên nó tạo theo mặc định và nói rõ điều đó trong câu trả lời; public hay private không ảnh hưởng gì đến các bước sau.\n\n![Chat: Engineer trả lời rằng repo đã được tạo và đăng ký](/images/docs/tutorial-chat-create-repo.png)\n\nMở **Cài đặt → Repositories** để xác nhận: `personal-website` đã nằm trong danh sách. Agent chọn repo từ đây khi thực thi task.\n\n![Danh sách repo trong cài đặt](/images/docs/tutorial-repo-settings.png)\n\nKhông có GitHub CLI, bạn có thể tự tạo repo trên GitHub rồi thêm thủ công ở cùng trang này; kết quả như nhau.\n\nCuối cùng, bấm **Connect GitHub** trên cùng trang và cấp quyền cho repo theo hướng dẫn. Kết nối xong, các pull request có nhắc tới mã issue sẽ tự liên kết với issue đó, và trạng thái PR cùng kết quả CI hiện ngay trong issue.\n\n## Issue đầu tiên: xây website\n\nVậy là xong phần thiết lập. Từ đây trở đi, đây chính là cách công việc thường ngày vận hành trong UniAI: yêu cầu được viết thành issue và giao cho agent.\n\nWebsite xuất phát từ template chính thức của shadcn thay vì dựng từ đầu: template đã mang sẵn stack và cấu hình nền, Engineer chỉ xây nội dung và thiết kế lên trên.\n\nBấm **New Issue** ở đầu thanh bên (phím tắt `C`). Ở chế độ agent mặc định không có ô tiêu đề để điền — chọn Engineer ở mục **Created by** và viết thẳng yêu cầu vào phần mô tả; tiêu đề được sinh ra lúc tạo. Bạn cũng có thể chuyển sang chế độ thủ công và tự viết tiêu đề lẫn nội dung. Phần mô tả:\n\n```text\nWork in the personal-website repository registered in this workspace.\n\nStart from a template instead of scaffolding from scratch:\n\npnpm dlx shadcn@latest init --preset b5rR41Mtnc --template next --pointer\n\nRun the command, commit the template as-is, then build on top of it.\n\nThe site is for a photographer. It needs:\n\n- Introduction — who I am and what I shoot\n- My work — a selection of photos\n- Contact — an email link that is easy to find\n- Pricing — decide where a pricing table fits and how to present it\n- Use placeholder images (Unsplash or picsum.photos) wherever a photo\n  belongs, and list where I should replace them with my own work\n\nDesign:\n\n- Pick a style that fits a photography portfolio. It should feel\n  refined and understated; express that through layout, typography\n  and spacing, and never use words like \"premium\" or \"high-end\"\n  in the copy.\n- Choose the fonts.\n\nOpen a pull request when it is ready.\n```\n\nPhần mô tả mang ba loại thông tin: ràng buộc (xuất phát từ template chỉ định), yêu cầu nội dung (liệt kê từng mục), và khoảng mở (phong cách để Engineer tự quyết). Viết ra ý định và ranh giới; phần triển khai để agent lo.\n\n![Chế độ agent trong hộp thoại tạo issue, với Engineer được chọn làm người tạo](/images/docs/tutorial-new-issue-agent-mode.png)\n\nSau khi gửi, UniAI tạo issue (`UNI-1`, tiêu đề tự sinh) và đặt Engineer làm người phụ trách. Engineer bắt đầu thực thi và trạng thái chuyển sang In Progress. Cột bên phải của issue chứa mọi thứ đang diễn ra trên nó: trạng thái, người phụ trách, các pull request liên kết, và nhật ký thực thi.\n\nLượt chạy mất vài phút, và có hai lối vào để xem tiến độ: huy hiệu trạng thái đang chạy cạnh tiêu đề, và **Execution log** (nhật ký thực thi) ở bên phải — mở lượt chạy hiện tại:\n\n![Hai lối vào để theo dõi tiến độ](/images/docs/tutorial-issue-engineer-working.png)\n\nNhật ký thực thi ghi lại từng bước của lượt chạy: các dòng **Agent** là lời tường thuật của chính nó, còn **Bash**, **Read**, **Edit** là các lệnh nó đã chạy và các file nó đã đọc, đã sửa. Tại thời điểm chụp màn hình, nó đang chụp ảnh các trang vừa hoàn thành để tự kiểm tra, và vừa phát hiện đường kẻ phân cách trong bảng giá bị lệch. Bấm biểu tượng ⓘ ở góc trên bên phải để xem máy tính, công cụ lập trình AI và thư mục làm việc của lượt chạy.\n\n![Nhật ký thực thi: từng bước Engineer đã làm](/images/docs/tutorial-execution-log.png)\n\n## Nghiệm thu: trao đổi ngay trong issue\n\nKhi lượt chạy kết thúc, Engineer chuyển trạng thái sang In Review và một thông báo rơi vào hộp thư đến của bạn. Nó đã đăng một bình luận hoàn thành trên issue kèm ảnh chụp các trang đã xong:\n\n![Bình luận hoàn thành của Engineer kèm ảnh chụp các trang](/images/docs/tutorial-issue-review-comment.png)\n\nẢnh chụp cho cái nhìn tổng quát, nhưng review đúng nghĩa vẫn là mở website trong trình duyệt. Website được xây ngay trên máy của bạn, nên chỉ cần Engineer khởi động một server cục bộ là vào được. Khu **Pull requests** bên phải cũng vẫn trống — issue đã yêu cầu rõ phải mở PR, nên hỏi thẳng luôn chuyện đó.\n\nLần này không dùng ô bình luận. Có một nút chat ở góc dưới bên phải, xuất hiện trên mọi trang của không gian làm việc, và nó chính là tính năng **Chat** trong thanh bên:\n\n![Nút chat ở góc dưới bên phải](/images/docs/tutorial-chat-fab.png)\n\nMở nó, chọn Engineer, và gõ `@` để tham chiếu issue hiện tại — cuộc trò chuyện từ đó mang theo ngữ cảnh của issue. Gửi cả hai câu hỏi:\n\n![Hỏi trong chat với issue được tham chiếu](/images/docs/tutorial-chat-ask-in-issue.png)\n\nCâu trả lời của Engineer có hai phần: server cục bộ đang chạy, kèm địa chỉ để mở; và PR thực ra đã được nộp từ trước, nhưng tiêu đề, tên nhánh lẫn nội dung đều thiếu `UNI-1`, nên cơ chế liên kết tự động không có gì để khớp — nó liền thêm mã issue vào tiêu đề PR.\n\n![Câu trả lời của Engineer: địa chỉ cục bộ và lý do PR chưa được liên kết](/images/docs/tutorial-chat-engineer-reply.png)\n\nMở địa chỉ nó đưa trong trình duyệt để xem website:\n\n![Trang chủ của website chạy cục bộ](/images/docs/tutorial-site-localhost.png)\n\nQuay lại issue, một thẻ PR giờ đã hiện trong khu **Pull requests** bên phải, cho biết trạng thái và độ lớn thay đổi; bấm vào sẽ mở PR trên GitHub. Repo có cấu hình CI còn hiển thị cả kết quả kiểm tra:\n\n![Thẻ PR ở cột bên phải của issue](/images/docs/tutorial-issue-pr-linked.png)\n\n## Viết phản hồi vào chỉ dẫn\n\nVòng review này lộ ra hai thói quen làm việc của Engineer: làm xong nó tự kiểm tra bằng cách chụp ảnh liên tục, ngốn khá nhiều thời gian chạy; và nó mở PR không kèm mã issue, nên PR không liên kết được. Cả hai đều không phải lỗi một lần — đều là việc nó lặp lại mỗi lượt. Thứ cần sửa không phải issue này mà là chỉ dẫn của nó.\n\nBạn không phải tự tay sửa chỉ dẫn: agent có thể tự cập nhật cấu hình của mình bằng uniai CLI. Trong cùng cuộc trò chuyện, đưa cho Engineer hai quy tắc — làm xong không tự kiểm tra bằng chụp ảnh nữa, vì review diễn ra trong trình duyệt; và tên nhánh hoặc tiêu đề PR phải mang mã issue — rồi bảo nó tự viết vào chỉ dẫn của chính nó:\n\n![Nhờ Engineer tự viết các quy tắc vào chỉ dẫn của nó](/images/docs/tutorial-chat-update-instructions.png)\n\nEngineer áp dụng cập nhật bằng `uniai agent update`, thêm một mục quy tắc làm việc vào chỉ dẫn, và chỉ ra cái giá của quy tắc thứ nhất: không còn tự kiểm tra bằng ảnh chụp, các vấn đề hiển thị trên trang giờ do bạn tự soi trong trình duyệt.\n\nMở **Agent → Engineer**, cả hai quy tắc đã nằm trong chỉ dẫn:\n\n![Chỉ dẫn đã cập nhật của Engineer](/images/docs/tutorial-engineer-updated-instructions.png)\n\nTừ đó về sau, cả hai quy tắc có hiệu lực cùng chỉ dẫn ở mỗi lượt chạy. Đây cũng là cách cơ bản để tinh chỉnh một agent: khi một vấn đề cứ lặp lại, viết điều chỉnh vào chỉ dẫn thay vì tự tay nhắc lại mỗi lần.\n\n## Agent thứ ba: Reviewer\n\nEngineer đã giao phiên bản đầu, nhưng có những vấn đề chính nó không tự thấy được. Việc review giao cho một agent mới: một ngữ cảnh tươi mới, chuyên kiểm tra chất lượng code theo yêu cầu của issue.\n\nViệc tạo lại giao cho UniAI helper: một agent tên Reviewer, lần này chạy trên Codex với model GPT-5.6 Sol — công cụ và model khác với Engineer; nó chỉ review và không bao giờ sửa code, viết kết luận thành bình luận trên issue. Chỉ dẫn cũng do trợ lý sinh ra — đọc trạng thái thực tế của PR trước khi review, viết phát hiện thành bình luận kèm vị trí file, và tuyệt đối không sửa code:\n\n![Cấu hình của Reviewer](/images/docs/tutorial-reviewer-detail.png)\n\nReview không cần issue mới, cũng không cần đổi người phụ trách. Quay lại ô bình luận của UNI-1, gõ `@Reviewer` để @nhắc nó, viết việc cần làm rồi gửi — dòng gợi ý dưới ô cho biết Reviewer bắt đầu thực thi ngay khi bình luận được gửi:\n\n![Bắt đầu review bằng cách @nhắc Reviewer trong bình luận](/images/docs/tutorial-comment-mention-reviewer.png)\n\nVài phút sau, bình luận review quay lại, với kết luận là cần sửa trước khi merge. Năm phát hiện chia hai nhóm — ba về quy ước code, hai về mức độ bám yêu cầu — mỗi cái kèm file và số dòng. Hai phát hiện về yêu cầu chính là thứ Engineer không tự thấy được: bảng giá trình bày giá tiền, tiền cọc và điều khoản thuế bịa ra như nội dung thật mà không đánh dấu placeholder, và mục liên hệ mang một liên kết Instagram chưa kiểm chứng. Bình luận kết lại bằng những gì nó đã tự xác minh: typecheck, lint và build đều đạt.\n\nSửa lỗi cũng không cần quy trình mới: @Engineer trong ô bình luận và bảo nó xử lý hết các phát hiện. Engineer bắt đầu thực thi ngay khi gửi, thấy được ở cả huy hiệu cạnh tiêu đề lẫn nhật ký thực thi bên phải:\n\n![Các phát hiện của Reviewer, với @Engineer nhận phần sửa](/images/docs/tutorial-reviewer-findings.png)\n\nVài phút sau Engineer báo lại: cả năm đã sửa xong, đẩy lên cùng PR. Hai agent chung một dòng thời gian, nên bản báo cáo chỉ cần trả lời từng phát hiện một, không phải kể lại bối cảnh:\n\n![Báo cáo sửa lỗi của Engineer](/images/docs/tutorial-engineer-fixed.png)\n\n@Nhắc Reviewer lần nữa để kiểm tra lại: cả năm phát hiện đã xử lý, sẵn sàng merge, kèm hai gợi ý theo dõi sau với độ ưu tiên thấp:\n\n![Lượt kiểm tra lại của Reviewer đạt](/images/docs/tutorial-reviewer-recheck.png)\n\nPhát triển và review cứ qua lại như vậy: tìm ra vấn đề, sửa vấn đề, và mỗi vòng làm định nghĩa của website vững thêm một chút. Một issue giờ mang hai agent — Engineer triển khai, Reviewer review — và vài bình luận ngắn của bạn kéo cả vòng lặp chạy.\n\n## Merge và khép lại\n\nMerge cũng ra lệnh bằng một bình luận: bảo Engineer merge PR và đảm bảo issue chuyển sang Done. Nó merge, xóa nhánh, chạy lại các bước kiểm tra trên main sau merge, rồi báo cáo. Trạng thái issue thành Done, và một thông báo hoàn thành đến hộp thư đến:\n\n![Merge hoàn tất và issue chuyển sang Done](/images/docs/tutorial-issue-done.png)\n\nVậy là khép trọn vòng đời của issue đầu tiên: từ bản mô tả yêu cầu đến lúc merge — triển khai, review, sửa lỗi, nghiệm thu và merge — tất cả ghi trên một dòng thời gian.\n\n## Lập một đội\n\nVòng lặp của UNI-1 chạy tốt, nhưng mọi lượt chuyền việc đều bắt đầu từ bạn: review xong thì @nhắc Reviewer, review xong lại @nhắc Engineer. Bản thân việc điều phối thành việc của bạn. Lớp đó cũng giao đi được. Câu trả lời của UniAI là **đội**: một nhóm có tên gồm các agent và thành viên, do một agent leader dẫn dắt. Khi issue được giao cho một đội, leader nhận lấy: đọc issue, phân việc, và đẩy trạng thái đi tiếp. Khi một thành viên trả lời, leader được đánh thức tự động và quyết định bước kế tiếp; cuối cùng nó báo cáo sản phẩm cho bạn. Leader đảm nhận đúng vai bạn vừa đóng.\n\nMột đội chính là cách phân công bạn đang có, cộng thêm một leader:\n\n- **Lead** (mới): leader, phụ trách quản lý — giao việc, đẩy trạng thái issue đi tiếp, và báo cáo sản phẩm cho bạn, không tự làm việc chuyên môn;\n- **Engineer** (có sẵn): phát triển;\n- **Reviewer** (có sẵn): review mọi PR;\n- **Bạn**: nghiệm thu cuối cùng.\n\nThành viên không giới hạn ở agent. Mỗi thành viên mang một dòng mô tả vai trò để leader tham khảo khi phân việc.\n\nChỉ cần mô tả cấu trúc này cho UniAI helper: tạo đội và leader của nó, giao cho mỗi thành viên phần việc, gắn cho Lead và Engineer mỗi bên một skill (nói ở mục sau), và đưa quy trình vào chỉ dẫn của đội — việc phát triển giao cho Engineer, chỉ bàn giao khi PR qua được review, và Engineer merge sau khi bạn nghiệm thu. Trợ lý áp toàn bộ cấu hình trong một lượt bằng CLI:\n\n![Trợ lý cấu hình cả đội trong một lượt](/images/docs/tutorial-helper-create-squad.png)\n\nMở **Đội → Website Team** trong thanh bên để kiểm tra kết quả: leader, các thành viên và mô tả vai trò đều đã có mặt. Đội cũng tạo được từ form trên trang này; cơ chế đầy đủ xem [Đội](/squads):\n\n![Chi tiết đội: leader, thành viên và vai trò](/images/docs/tutorial-squad-detail.png)\n\n## Skill: phương pháp tái sử dụng được\n\nViệc viết phản hồi vào chỉ dẫn lúc trước chỉ giải quyết vấn đề của một agent — chỉ dẫn thuộc riêng agent đó. Muốn chia sẻ một phương pháp cho nhiều agent, hoặc mượn một phương pháp ai đó đã viết sẵn, dùng **skill**.\n\nSkill là gói tri thức cho agent: một file `SKILL.md` cộng các file hỗ trợ tùy chọn, chỉ cho agent cách tư duy và xử lý một loại việc. UniAI theo chuẩn mở Anthropic Agent Skills, nên bất kỳ skill nào đúng chuẩn đều import thẳng được; gắn vào agent xong, nó đồng bộ xuống runtime lúc thực thi. Mở trang **Skills** — hai skill vừa import ở trên đều nằm đó, kèm các agent được gắn ở bên phải:\n\n![Trang Skills của không gian làm việc](/images/docs/tutorial-skills-list.png)\n\nMở một skill ra sẽ thấy toàn bộ file của nó:\n\n![Cấu trúc file của skill tdd](/images/docs/tutorial-skill-detail.png)\n\nTrong hai skill này, một cái nói về cách làm việc, cái kia về cách báo cáo:\n\n- **tdd**, gắn cho Engineer, là một phương pháp kỹ thuật: viết test thất bại trước, rồi mới viết phần triển khai làm đúng test đó đạt;\n- **i-have-adhd**, gắn cho Lead, là một phong cách báo cáo: dòng đầu luôn là bước tiếp theo làm được ngay, việc nhiều bước thì đánh số, và tiến độ luôn nhìn thấy được. Mọi thứ leader viết ra đều là báo cáo gửi cho bạn, nên skill này quyết định thứ bạn sẽ đọc.\n\nCách làm việc của một nhóm dần lắng thành skill, và agent mới thừa hưởng nó chỉ bằng một lần gắn. Import không phải nguồn duy nhất: **New skill** có ba đường — tạo thủ công, import từ URL, và chép từ runtime cục bộ — không đi sâu ở đây; xem [Skills](/skills):\n\n![Ba cách thêm một skill mới](/images/docs/tutorial-skill-new-modal.png)\n\nĐọc kỹ file của skill bên thứ ba trước khi import: UniAI không sandbox chúng, và nội dung skill được đưa nguyên vẹn cho công cụ lập trình AI.\n\n## Issue thứ hai: giao cho đội\n\nGiao cho đội một phần việc trọn vẹn: biến website thành một template chung — thay tên thật bằng một tên nhiếp ảnh gia placeholder và email thật bằng `hello@example.com`, giữ nguyên bố cục và cấu trúc nội dung. Issue này không phải tự tay viết: mô tả yêu cầu cho Lead trong Chat và tạo bằng **Create with agent** (tạo bằng agent), giao cho đội — một đội, cũng như agent hay thành viên, xuất hiện được ở bất cứ đâu một người phụ trách xuất hiện được:\n\n![Tạo issue từ cuộc trò chuyện bằng Create with agent](/images/docs/tutorial-create-with-agent.png)\n\nTrong issue được sinh ra, yêu cầu đã được sắp thành bản mô tả có cấu trúc, và huy hiệu cạnh tiêu đề cho thấy leader đã nhận việc:\n\n![UNI-2: Lead tạo issue và bắt đầu làm](/images/docs/tutorial-issue-squad-created.png)\n\nNhững gì diễn ra tiếp theo hoàn toàn là việc của leader. Bình luận phân việc của nó không chỉ là chuyển tiếp: nó thêm hai ràng buộc issue chưa có — danh tính placeholder phải nhất quán trên toàn site, và lượt rà soát phải phủ cả metadata lẫn README — rồi mới giao cho Engineer. Engineer xong việc thì báo PR trên cùng dòng thời gian:\n\n![Lead phân việc và Engineer báo cáo PR](/images/docs/tutorial-squad-dispatch.png)\n\nLượt review quay lại với không phát hiện nào và Lead bàn giao: nó @nhắc bạn để nghiệm thu, mở đầu bằng kết luận, rồi tới liên kết PR, mô tả thay đổi, hai điểm cần bạn cân nhắc, và khép lại bằng một dòng — không gì được merge khi chưa có bạn duyệt:\n\n![Báo cáo bàn giao của Lead](/images/docs/tutorial-squad-delivery.png)\n\nTừ sắp xếp yêu cầu, chốt hướng làm, phát triển, review đến báo cáo bàn giao, không bước nào của issue này cần bạn đứng ra nối. Nghiệm thu giống hệt UNI-1 — bạn cũng có thể bảo nó khởi động server cục bộ rồi xem trong trình duyệt, không nhắc lại ở đây. Done vẫn thuộc về bạn: trả lời để merge, hoặc tự tay đổi trạng thái. Mọi bước trên dòng thời gian đều được ghi lại, không khác gì issue của một nhóm toàn người — chỉ người thực thi là thay đổi.\n\n## Tự động hóa: Autopilot\n\nMọi kích hoạt đến giờ đều bắt đầu từ bạn — giao việc, @nhắc, chat. **Autopilot** là kiểu kích hoạt thứ tư: tự bắt đầu công việc theo lịch.\n\nCấu hình chỉ mất một câu: bảo UniAI helper rằng chín giờ sáng thứ Hai hằng tuần, Reviewer chạy một lượt kiểm tra toàn diện trên repo của website — code thừa và code chết, cấu trúc đáng ngờ, những chỗ có thể bám quy ước tốt hơn — viết phát hiện thành bình luận trên issue và không đổi dòng code nào:\n\n![Trợ lý tạo một autopilot](/images/docs/tutorial-autopilot-create.png)\n\nMở trang **Autopilot**: người thực thi, prompt và lịch chạy (cron cộng múi giờ) đều đã cấu hình xong. Có hai chế độ chạy:\n\n- **Create Issue** (mặc định): mỗi lần kích hoạt đều tạo issue trước, nên công việc nằm trên bảng với lịch sử và bình luận y như mọi issue khác;\n- **Run only**: thực thi mà không tạo issue, chỉ để lại dấu vết trong lịch sử chạy của autopilot — lượt chạy không tìm thấy gì thì không để lại gì.\n\nKiểm tra định kỳ dùng chế độ Create Issue. Thay vì chờ đến thứ Hai, bấm **Run now** để kích hoạt ngay một lượt:\n\n![Chi tiết autopilot: lịch chạy và Run now](/images/docs/tutorial-autopilot-detail.png)\n\nIssue được tạo và Reviewer bắt đầu kiểm tra. Từ đó trở đi, đúng giờ đó mỗi thứ Hai, cùng lượt kiểm tra ấy tự tạo issue mới, không cần ai kích hoạt:\n\n![Một issue do Autopilot tự tạo](/images/docs/tutorial-autopilot-issue.png)\n\nNgoài lịch chạy, trigger còn hỗ trợ webhook, và người thực thi cũng trỏ được vào một đội — xem [Tự động hóa](/autopilots).\n\n## Làm việc cùng người khác\n\nĐến giờ bạn vẫn là thành viên con người duy nhất của không gian làm việc. Vào **Cài đặt → Thành viên**, mời người khác qua email; họ tạo issue, @nhắc agent và tham gia đội y như bạn — nhiều người và nhiều agent cùng làm trên chung một tập issue:\n\n![Mời thành viên vào không gian làm việc qua email](/images/docs/tutorial-invite-member.png)\n\n## Lời kết\n\nKhi issue chất dần lên, đã có công cụ để tổ chức chúng: một **dự án** gom các issue liên quan và theo dõi như một khối (xem [Dự án](/projects)). Nhiều cách tổ chức khác đang được bổ sung, và **Space** cũng sắp sẵn sàng.\n\nNhìn lại chặng đường: xuất phát từ một không gian làm việc trống, kết nối một máy tính, dựng bốn agent, đưa hai issue đi trọn vòng đời, rồi giao việc điều phối cho leader và việc kiểm tra định kỳ cho Autopilot. Mức tham gia của bạn lùi lại từng nấc một, đến khi chỉ còn hai việc: mô tả yêu cầu, và nghiệm thu kết quả.\n\nAgent là thành viên hạng nhất và mọi cộng tác nằm trên dòng thời gian của issue — đó là cách làm việc UniAI mang lại. Nền tảng vẫn đang tiến hóa nhanh; thêm nhiều năng lực khác trong [tài liệu](/).\n\n## Bước tiếp theo\n\n- [Khái niệm cốt lõi](/concepts) — gặp toàn bộ các đối tượng cốt lõi của UniAI trong ba phút.\n- [Đội](/squads) — leader điều phối việc ra sao, và khi nào dùng đội thay vì một agent đơn lẻ.\n- [Skills](/skills) — import, viết và chia sẻ phương pháp cho agent.\n- [Tự động hóa](/autopilots) — cấu hình đầy đủ cho hai kiểu trigger lịch chạy và webhook."
        }
      ]
    },
    {
      "label": "Workspace",
      "labelKey": "workspace",
      "pages": [
        {
          "slug": "workspaces",
          "title": "Không gian làm việc",
          "content": "Không gian làm việc là ranh giới cấp cao nhất của UniAI. Thành viên, issue, dự án, agent, skill và lịch sử task đều thuộc về một không gian làm việc và cách ly hoàn toàn với mọi không gian làm việc khác.\n\n## Một không gian làm việc chứa những gì\n\n| Nhóm | Nội dung |\n|---|---|\n| **Công việc** | Issue, dự án, bình luận, tệp đính kèm, lịch sử task |\n| **Nhóm** | Thành viên, vai trò, lời mời |\n| **Agent** | Cấu hình agent, skill, runtime, Tự động hóa |\n| **Cài đặt chung** | Ngữ cảnh, nhãn, thuộc tính tùy chỉnh, tích hợp |\n\n**Ngữ cảnh** được cung cấp cho mọi agent trong không gian làm việc — chỗ cho bối cảnh của nhóm, quy ước sản phẩm và các yêu cầu lâu dài. Yêu cầu chỉ dùng một lần cho một task thì thuộc về issue tương ứng.\n\n## Tạo không gian làm việc\n\nTạo không gian làm việc chỉ hỏi hai thứ:\n\n- **Tên**: hiển thị cho thành viên; có thể đổi sau.\n- **URL không gian làm việc**: slug trong địa chỉ — phần `my-team` trong `uniai.unicomhub.com/my-team`. Chỉ chữ thường, chữ số và dấu gạch nối; không được trùng với không gian làm việc đã có hay một địa chỉ dành riêng, và không đổi được sau khi tạo.\n\nNgười tạo tự động trở thành `owner`. Tiền tố issue được sinh từ tên không gian làm việc — với tiền tố `UNI`, số issue hiển thị là `UNI-1`, `UNI-2`, đếm tăng độc lập trong từng không gian làm việc.\n\n## Đổi cài đặt\n\n`owner` và `admin` đổi được tên, biểu tượng, mô tả, ngữ cảnh và tiền tố issue; `member` chỉ xem được.\n\n![Cài đặt không gian làm việc: tên, ngữ cảnh, slug, tiền tố issue và vùng nguy hiểm](/images/docs/workspace-settings.png)\n\n> Sau khi đổi tiền tố issue, mọi issue lập tức chuyển sang số mới (`UNI-5` thành `NEW-5`), nhưng số cũ trong PR, tên nhánh và lịch sử chat không được cập nhật theo.\n\n## Chuyển và rời\n\nMột tài khoản có thể tham gia nhiều không gian làm việc. Chuyển qua lại chỉ thay đổi thứ bạn đang xem; không dữ liệu nào bị di chuyển.\n\nSau khi rời một không gian làm việc, bạn cần lời mời mới để quay lại. Rời đi cũng vô hiệu hóa các runtime bạn sở hữu, lưu trữ các agent gắn với chúng và hủy những task chưa hoàn thành của chúng.\n\n`owner` cuối cùng không rời được; hãy nâng một thành viên khác lên `owner` trước.\n\n## Xóa không gian làm việc\n\nChỉ một `owner` mới xóa được không gian làm việc. Xóa sẽ gỡ bỏ vĩnh viễn toàn bộ công việc, thành viên, cấu hình agent và lịch sử task của nó, và không thể hoàn tác. Nếu chỉ muốn tạm rời đi một thời gian, hãy rời thay vì xóa.\n\n## Bước tiếp theo\n\n- [Thành viên và vai trò](/members-roles) — mời thành viên và quản lý vai trò.\n- [Issue](/issues) — đơn vị công việc cơ bản trong không gian làm việc.\n- [Agent](/agents) — danh tính và cấu hình của agent."
        },
        {
          "slug": "members-roles",
          "title": "Thành viên và vai trò",
          "content": "Ai tham gia một [không gian làm việc](/workspaces) cũng là thành viên, và mỗi thành viên có một vai trò: `owner`, `admin` hoặc `member`. Vai trò chỉ chi phối cài đặt không gian làm việc và quản lý nhóm; cộng tác hằng ngày — tạo issue, viết bình luận — mở cho mọi thành viên.\n\n## Ba vai trò\n\n| Thao tác | owner | admin | member |\n|---|:---:|:---:|:---:|\n| Xem không gian làm việc và cộng tác hằng ngày | ✓ | ✓ | ✓ |\n| Đổi cài đặt không gian làm việc | ✓ | ✓ | — |\n| Mời một `admin` hoặc `member` | ✓ | ✓ | — |\n| Đổi hoặc gỡ một `admin` hoặc `member` | ✓ | ✓ | — |\n| Trao, thu hồi hoặc gỡ `owner` | ✓ | — | — |\n| Xóa không gian làm việc | ✓ | — | — |\n\nKhông gian làm việc luôn phải giữ ít nhất một `owner`: `owner` cuối cùng không thể bị hạ cấp, bị gỡ, hay tự rời đi.\n\n> Vai trò không quyết định ai chạy được agent. Mỗi agent có phạm vi Access riêng, và `owner` lẫn `admin` không thể vượt qua nó để chạy những agent mình không được cấp. Xem [Agent](/agents#permissions-and-access).\n\n## Mời thành viên\n\n`owner` và `admin` gửi lời mời từ Settings → Members bằng cách nhập email và chọn vai trò. Lời mời chỉ trao được `admin` hoặc `member`; một `owner` phải tham gia không gian làm việc trước, rồi được một `owner` hiện có nâng cấp.\n\n![Quản lý thành viên: form mời và danh sách thành viên kèm vai trò](/images/docs/members-roster.png)\n\nNgười được mời chấp nhận bằng cách đăng nhập với email đó — không cần đăng ký từ trước. Lời mời có hiệu lực 7 ngày; hết hạn thì gửi lại. Trên các bản self-hosted có hạn chế đăng ký, người được mời chưa có tài khoản vẫn phải tuân theo quy tắc đăng ký khi tạo tài khoản.\n\nLiên kết mời chỉ xuất hiện trong email; trong sản phẩm không có chỗ nào xem hay sao chép nó. Nếu email gửi thất bại, bản ghi lời mời vẫn tồn tại — hãy thu hồi và gửi một lời mời mới.\n\n## Đổi vai trò\n\nMột `admin` có thể chuyển vai trò qua lại giữa `admin` và `member`, và gỡ được các thành viên giữ hai vai trò đó. Mọi thay đổi liên quan đến `owner` — nâng cấp, thay đổi, hay gỡ bỏ — chỉ một `owner` mới thực hiện được.\n\nThay đổi vai trò có hiệu lực ngay lập tức.\n\n## Gỡ thành viên\n\nThành viên bị gỡ mất quyền truy cập không gian làm việc ngay lập tức. Issue, bình luận và các bản ghi cộng tác khác họ đã tạo vẫn ở lại trong không gian làm việc.\n\n> Gỡ một thành viên cũng vô hiệu hóa các runtime họ sở hữu, lưu trữ các agent gắn với chúng, và hủy những task chưa hoàn thành. Các quyền thành viên đó được cấp trong phạm vi Access của agent cũng bị xóa, và không tự khôi phục nếu họ tham gia lại.\n\n## Bước tiếp theo\n\n- [Issue](/issues) — phần việc mà thành viên và agent cùng xử lý.\n- [Agent](/agents) — vai trò trong không gian làm việc khác Access của agent thế nào.\n- [Daemon và runtime](/daemon-runtimes) — ai sở hữu runtime và chúng chạy ở đâu."
        }
      ]
    },
    {
      "label": "Issues & projects",
      "labelKey": "issues_projects",
      "pages": [
        {
          "slug": "issues",
          "title": "Issue",
          "content": "Issue là đơn vị cơ bản để tổ chức công việc trong UniAI: một tính năng, một bug, một cuộc điều tra — bất cứ việc gì mà một thành viên hay một agent nên nhận và theo đến cùng. Thảo luận xung quanh nó, các lần đổi trạng thái và mọi lượt chạy đều nằm ở một chỗ, nên không phải chắp nối lại bối cảnh từ log chat hay đầu ra terminal.\n\n## Các thành phần của issue\n\n| Thành phần | Vai trò |\n|---|---|\n| **Tiêu đề và mô tả** | Mục tiêu, bối cảnh, yêu cầu và tiêu chí nghiệm thu. |\n| **Trạng thái và độ ưu tiên** | Công việc đang ở đâu và nên xử lý gì trước. |\n| **Người phụ trách** | Một thành viên không gian làm việc, một agent, hoặc một đội. |\n| **Ngày, nhãn và thuộc tính tùy chỉnh** | Lập kế hoạch, phân loại và các trường riêng của nhóm bạn. |\n| **Dự án và quan hệ cha-con** | Nằm trong một khối công việc lớn hơn, hoặc tách thành các issue con. |\n| **Hoạt động và nhật ký thực thi** | Bình luận, thay đổi trạng thái, task và kết quả agent trả về. |\n\n![Trang chi tiết issue: mô tả, thảo luận, thuộc tính bên phải và nhật ký thực thi](/images/docs/quickstart-run-result.png)\n\n## Tạo issue\n\nTạo từ trang **Issues** hoặc ngay trong một dự án — chỉ cần tiêu đề là đủ, mọi thuộc tính khác có thể điền sau.\n\nMỗi issue có một mã số, ví dụ `UNI-123`. Phần số tăng dần trong không gian làm việc; sau khi admin đổi tiền tố issue của không gian làm việc, mã số sẽ hiển thị theo tiền tố mới. Xem [Không gian làm việc](/workspaces#create-a-workspace).\n\n## Chọn người phụ trách\n\n| Người phụ trách | Hiệu ứng |\n|---|---|\n| **Thành viên** | Thành viên đó chịu trách nhiệm theo dõi tiếp; không tạo task nào. |\n| **Agent** | Một task được tạo cho agent đó. |\n| **Đội** | Leader của đội nhận việc và quyết định ai xử lý. |\n\nKhi giao cho agent hoặc đội, issue được đưa vào hàng đợi ngay lập tức trừ khi nó đang ở `backlog`; nếu runtime offline, task chờ trong hàng đợi. Agent và đội đã lưu trữ thì không giao được.\n\nViệc giao issue không vượt qua được Quyền truy cập của agent; khi giao cho đội, Quyền truy cập của leader mới là thứ được kiểm tra. Xem [Giao issue cho agent](/assigning-issues).\n\n## Trạng thái\n\n| Trạng thái | Ý nghĩa |\n|---|---|\n| `backlog` | Chưa bắt đầu. Issue đã giao cho agent chỉ tạo task sau khi rời `backlog`. |\n| `todo` | Đã chốt phạm vi, chờ bắt đầu. |\n| `in_progress` | Đang làm. |\n| `in_review` | Có kết quả đang chờ review. |\n| `done` | Đã xong. |\n| `blocked` | Tạm thời không thể tiếp tục. |\n| `cancelled` | Không theo đuổi nữa; hồ sơ vẫn còn. |\n\nKhông có luồng cố định giữa các trạng thái — thành viên và agent đều đổi trực tiếp được.\n\nKhi issue được giao cho agent, agent được kỳ vọng chuyển trạng thái từ `backlog` / `todo` sang `in_progress` lúc bắt đầu, rồi sang `in_review` khi đã bàn giao kết quả. Các cập nhật này do agent chủ động ghi qua UniAI CLI trong lượt chạy — máy chủ không tự đổi trạng thái issue khi task bắt đầu hay hoàn tất (ngoài hai ngoại lệ hệ thống bên dưới). `done` thường là xác nhận của con người, hoặc một tích hợp như PR có ý định đóng issue được merge.\n\n> Hai thay đổi do hệ thống thực hiện:\n>\n> - Khi một lượt chạy thất bại, issue không còn lượt chạy nào khác và không có retry nào được kích hoạt, `in_progress` lùi về `todo`.\n> - Khi một GitHub PR được liên kết merge với ý định đóng issue và không còn PR liên kết nào khác đang mở hoặc ở dạng draft, issue chuyển thành `done`.\n\n## Issue và task\n\nIssue là hồ sơ lâu dài của một phần việc; [task](/tasks) là một lượt chạy cụ thể của agent. Một issue có thể sinh nhiều task theo thời gian — lần triển khai đầu, các chỉnh sửa tiếp theo, một lần kiểm tra lại. Task hoàn tất chỉ có nghĩa lượt chạy đó đã kết thúc; issue xong hay chưa do trạng thái của issue quyết định.\n\n## Dự án và issue con\n\nMột issue thuộc tối đa một [dự án](/projects); dự án cung cấp hướng dẫn và tài nguyên dùng chung cho các lượt chạy bên trong nó. Chuyển issue sang dự án khác không tạo bản sao.\n\nViệc lớn có thể tách thành các issue con: issue cha giữ mục tiêu tổng thể trong khi các issue con tiến triển độc lập. Trạng thái của cha và con không ảnh hưởng lẫn nhau.\n\nIssue con có thể được gán **giai đoạn** để tiến hành theo từng đợt — 1, 2, 3. Khi mọi issue con trong giai đoạn chưa xong sớm nhất đạt `done` hoặc `cancelled`, issue cha nhận thông báo \"issue con đã hoàn tất\"; khi người phụ trách issue cha là agent, nó được đánh thức và tự quyết định có bắt đầu giai đoạn tiếp theo hay không. Issue con không có giai đoạn được tính là một đợt và chỉ thông báo một lần khi tất cả hoàn tất.\n\n## Chuyển đổi chế độ xem\n\nTrang Issues có năm chế độ xem — danh sách, bảng kanban, bảng dữ liệu, Gantt và swimlane — lọc và sắp xếp được theo trạng thái, người phụ trách, dự án cùng các thuộc tính khác. Tất cả đều hiển thị cùng một tập issue.\n\n## Xóa issue\n\nBất kỳ thành viên nào của không gian làm việc cũng xóa được issue.\n\n> Xóa là không thể hoàn tác: issue cùng bình luận, tệp đính kèm và các bản ghi liên kết bị xóa vĩnh viễn, các task chưa xong bị hủy. Khi công việc đơn giản là không theo đuổi nữa, hãy đặt trạng thái `cancelled` thay vì xóa — thảo luận và kết quả vẫn còn đó.\n\n## Bước tiếp theo\n\n- [Dự án](/projects) — tổ chức công việc cần nhiều issue mới hoàn thành.\n- [Bình luận](/comments) — thảo luận, trả lời và @nhắc quanh một issue.\n- [Giao issue cho agent](/assigning-issues) — trao việc cho agent và bắt đầu lượt chạy đầu tiên.\n- [Task](/tasks) — tìm hiểu hàng đợi, thực thi, retry và hủy."
        },
        {
          "slug": "projects",
          "title": "Dự án",
          "content": "Dự án tổ chức những khối công việc cần nhiều [issue](/issues) mới xong — một đợt ra mắt sản phẩm, một đợt migration, hay một tính năng giao theo từng giai đoạn.\n\nViệc mà một issue mô tả được thì không cần dự án. Dự án hợp với một nhóm công việc có chung mục tiêu, cần theo dõi tiến độ chung, hoặc cần dùng chung repository và thư mục làm việc.\n\n## Các thành phần của dự án\n\n| Thành phần | Vai trò |\n|---|---|\n| **Tên, biểu tượng và mô tả** | Mục tiêu, phạm vi và các yêu cầu lâu dài; mô tả được đưa vào ngữ cảnh thực thi của các agent làm việc trong dự án. |\n| **Trạng thái** | `planned`, `in_progress`, `paused`, `completed` hoặc `cancelled`. |\n| **Độ ưu tiên** | `urgent`, `high`, `medium`, `low` hoặc `none`. |\n| **Lead** | Một thành viên hoặc một agent — người điều phối dự án. |\n| **Ngày bắt đầu và ngày mục tiêu** | Khoảng thời gian dự kiến của dự án. |\n| **Issue và tiến độ** | Công việc trong dự án và đã hoàn thành được bao nhiêu. |\n| **Tài nguyên** | Các repository GitHub hoặc thư mục cục bộ trên máy tính cụ thể được liên kết. |\n\n![Chi tiết dự án: issue nhóm theo trạng thái, tiến độ và mô tả dự án](/images/docs/project-detail.png)\n\n## Dự án và issue\n\nMột dự án chứa bao nhiêu issue cũng được; một issue thuộc tối đa một dự án. Bạn có thể tạo issue ngay trong dự án, hoặc chọn / đổi dự án từ phần thuộc tính của issue.\n\nTiến độ dự án được tính tự động từ các issue liên kết:\n\n```text\nprogress = issues in done or cancelled ÷ all issues in the project\n```\n\n`cancelled` nghĩa là phần việc đó đã được khép lại khỏi phạm vi dự án, nên vẫn tính vào tiến độ. Trạng thái dự án và trạng thái issue độc lập với nhau: hoàn thành mọi issue không tự đưa dự án về `completed`, và đổi trạng thái dự án cũng không đổi hàng loạt các issue của nó.\n\n## Mô tả dự án và ngữ cảnh thực thi\n\nKhi một agent chạy một issue trong dự án, tên và mô tả của dự án được thêm vào ngữ cảnh của lượt chạy đó. Mô tả là chỗ cho những thông tin mà mọi task liên quan đều cần: mục tiêu, ranh giới kỹ thuật, quy ước bàn giao. Yêu cầu chỉ áp dụng cho một issue thì thuộc về issue đó.\n\n## Thêm tài nguyên\n\nTài nguyên dự án cho agent biết dùng mã nguồn và thư mục làm việc nào:\n\n- **Repository GitHub**: cung cấp một repo mà runtime có thể checkout, kèm ref tùy chọn.\n- **Thư mục cục bộ**: để runtime trên một máy tính cụ thể làm việc trong một thư mục sẵn có, nơi agent sửa trực tiếp nội dung.\n\nXem [Tài nguyên dự án](/project-resources) để biết cách chọn và các ranh giới an toàn.\n\n## Lead của dự án\n\nLead có thể là thành viên hoặc agent, đánh dấu ai là người điều phối dự án. Đây không phải thiết lập phân quyền: nó không tự giao các issue của dự án, và đặt một agent làm lead cũng không tự chạy agent đó. Muốn bắt đầu một lượt chạy vẫn cần giao issue, @nhắc, hoặc một trigger Tự động hóa.\n\n## Ghim vào thanh bên\n\nDự án đã ghim xuất hiện trên thanh bên của bạn. Ghim là thiết lập cá nhân, không ảnh hưởng đến thành viên khác.\n\n## Tạo, sửa và xóa\n\nThành viên nào của không gian làm việc cũng tạo và sửa được dự án; chỉ `owner` và `admin` mới xóa được.\n\n> Xóa dự án không xóa các issue của nó: chúng được tách ra và ở lại trong không gian làm việc. Mô tả, trạng thái và tài nguyên của dự án bị xóa vĩnh viễn.\n\n## Bước tiếp theo\n\n- [Tài nguyên dự án](/project-resources) — liên kết repository GitHub hoặc thư mục cục bộ.\n- [Issue](/issues) — tạo và tổ chức phần việc cụ thể trong dự án.\n- [Agent](/agents) — tìm hiểu danh tính, cấu hình và Access của agent."
        },
        {
          "slug": "project-resources",
          "title": "Tài nguyên dự án",
          "content": "Tài nguyên của [dự án](/projects) cho agent biết phần việc này dùng mã nguồn nào và chạy ở đâu. Tài nguyên gắn liền với dự án, nên không cần dán lại URL repository hay đường dẫn cục bộ vào từng issue.\n\nHiện hỗ trợ hai kiểu tài nguyên:\n\n| Tài nguyên | Phù hợp khi | Chạy ở đâu |\n|---|---|---|\n| **Repository GitHub** | Mã nguồn cả nhóm dùng chung, checkout do runtime quản lý | Thư mục làm việc do runtime quản lý |\n| **Thư mục cục bộ** | Một bản checkout sẵn có, repository rất lớn, hoặc khi bạn muốn xem trực tiếp các thay đổi trên máy | Chính thư mục gốc trên một máy cụ thể |\n\n## Tài nguyên đi vào lần chạy như thế nào\n\nKhi agent xử lý issue trong dự án, UniAI thêm tên dự án, mô tả dự án và danh sách tài nguyên vào ngữ cảnh của lần chạy, đồng thời ghi `.multica/project/resources.json` vào thư mục làm việc.\n\nDanh sách repository gắn với không gian làm việc luôn có mặt trong ngữ cảnh của lần chạy. Repository gắn với dự án thì chỉ rõ thêm mã nguồn và ref mặc định mà phần việc này dùng.\n\nThư mục cục bộ chỉ áp dụng cho daemon nó được gắn. Khi daemon chạy task có thư mục cục bộ khớp, agent làm việc trực tiếp trong thư mục đó; các máy khác vẫn dùng repository GitHub của dự án hoặc repository của không gian làm việc.\n\n## Thêm repository GitHub\n\nMở dự án và chọn \"Add resource\" dưới mục **Resources**. Chọn một repository đã gắn với không gian làm việc, hoặc dán một URL Git. Repository của không gian làm việc được quản lý ở **Settings → Repositories**; sau khi kết nối GitHub, trang này có thêm **Pick from GitHub** để nhập các repository mà App được cấp quyền.\n\nRepository không giới hạn ở GitHub: mọi URL Git mà runtime truy cập được đều dùng được làm tài nguyên repository. Bản UniAI self-hosted còn kết nối được Forgejo, Gitea hoặc GitLab tự vận hành ở **Settings → Integrations → Git hosting**; xem [Git provider tự vận hành](/vcs-integration).\n\nKhi tạo dự án, bạn cũng chọn được repository ngay dưới mục **Repos**. Một dự án gắn được nhiều repository GitHub.\n\n`ref` của tài nguyên đặt nhánh, tag hoặc commit mà các lần checkout sau dùng làm mặc định. `default_branch_hint` chỉ gợi ý nhánh mặc định cho agent và không ép chuyển nhánh.\n\n## Thêm thư mục cục bộ\n\nThêm thư mục cục bộ cần một daemon đang chạy trên máy đó, vì trình duyệt không thể chọn thư mục trên máy của bạn.\n\n> **Đây là lối thoát hiểm, không phải một mặc định tiện hơn.** `local_directory` tồn tại cho những ai không còn lựa chọn khác — trường hợp điển hình là dự án game với bản checkout hàng chục gigabyte, nơi clone lại theo từng task đơn giản là bất khả thi.\n>\n> Nếu thư mục của bạn là một repo git bình thường clone được, **hãy dùng `github_repo`**: nó mặc định chạy ở chế độ worktree, nên các task trên cùng repo chạy song song không giới hạn. Chọn `local_directory` nghĩa là mọi task trên thư mục đó chạy **tuyệt đối tuần tự, mỗi lúc một task, mãi mãi**. Đọc kỹ phần \"khi nào chọn\" bên dưới trước khi quyết định.\n\n1. Đảm bảo daemon cục bộ trên máy này đang online.\n2. Mở mục **Resources** của dự án.\n3. Chọn \"Add local directory\", rồi chọn thư mục muốn dùng.\n\nThư mục phải là đường dẫn tuyệt đối, phải tồn tại sẵn, và daemon hiện tại phải đọc và ghi được. Các đường dẫn sau bị từ chối: gốc hệ thống và gốc ổ đĩa (`/`, `C:\\`), chính thư mục home và thư mục cha của các thư mục home (như `/Users`, `/home`, `/root`), cùng các thư mục hệ thống như `/etc`, `/var`, `/tmp`, `/usr` và `/opt`. Nếu đường dẫn được chọn là symlink, nó được phân giải về đường dẫn thật trước rồi kiểm tra lại theo cùng bộ quy tắc; khóa tuần tự cũng áp dụng trên đường dẫn thật.\n\nMỗi dự án gắn được tối đa một thư mục cục bộ cho mỗi daemon. Các máy khác nhau trong nhóm đều gắn được thư mục riêng của mình cho cùng một dự án.\n\n> Thư mục cục bộ không phải môi trường cách ly. Agent nhìn thấy và sửa trực tiếp nhánh hiện tại cùng các file chưa commit của bạn; UniAI không tự chuyển nhánh, stash, commit, push hay mở PR.\n\n## Thư mục cục bộ chạy tuần tự\n\nKhi hai task dùng cùng một thư mục thật, task đến sau vào trạng thái `waiting_local_directory` và tiếp tục sau khi task trước nhả thư mục. Hai đường dẫn dẫn tới cùng một thư mục qua các symlink khác nhau cũng tuần tự hóa như vậy.\n\nViệc chờ không đụng vào thư mục. Task đang chờ có thể bị hủy; nếu không, nó chờ tới khi thư mục sẵn sàng.\n\nMuốn làm nhiều thay đổi cùng lúc, hãy dùng tài nguyên repository GitHub để runtime chuẩn bị thư mục làm việc riêng cho từng phần việc.\n\n## Những gì được ghi trong lần chạy\n\nNgoài các thay đổi mã do agent thực hiện, runtime còn có thể ghi vào thư mục các file hướng dẫn mà công cụ lập trình AI hiện tại cần, cùng `.multica/project/resources.json`. Thêm chúng vào `.gitignore` nếu bạn không muốn đưa vào quản lý phiên bản.\n\nUniAI không bao giờ xóa một thư mục cục bộ đã gắn khi dọn dẹp môi trường chạy. Thay đổi của agent trong thư mục cũng giống hệt thay đổi khi bạn tự chạy một công cụ lập trình AI trong terminal, và cần được review y như vậy.\n\n## Quản lý tài nguyên bằng CLI\n\n```bash\n# Gắn repository khi tạo dự án\nuniai project create \\\n  --title \"Agent UX\" \\\n  --repo https://github.com/phanducquanguet/usf\n\n# Liệt kê và thêm tài nguyên\nuniai project resource list <project-id>\nuniai project resource add <project-id> \\\n  --type github_repo \\\n  --url https://github.com/phanducquanguet/usf \\\n  --ref main\n\n# Gắn thư mục cục bộ trên một daemon cụ thể\nuniai project resource add <project-id> \\\n  --type local_directory \\\n  --local-path /absolute/path/to/repo \\\n  --daemon-id <daemon-id>\n\n# Gỡ một tài nguyên\nuniai project resource remove <project-id> <resource-id>\n```\n\nThay đổi tài nguyên chỉ ảnh hưởng các task tạo sau đó; nó không viết lại hồ sơ của những lần chạy đã kết thúc.\n\n## Bước tiếp theo\n\n- [Dự án](/projects) — tìm hiểu ngữ cảnh dự án, tiến độ và lead.\n- [Daemon và runtime](/daemon-runtimes) — tìm hiểu máy nào nhận task.\n- [Task](/tasks) — xem các trạng thái chạy như `waiting_local_directory`."
        }
      ]
    },
    {
      "label": "Agents",
      "labelKey": "agents",
      "pages": [
        {
          "slug": "agents",
          "title": "Agent",
          "content": "Agent là một cộng sự trong [không gian làm việc UniAI](/workspaces). Bạn có thể giao issue cho nó, @nhắc nó trong bình luận, hoặc trò chuyện trực tiếp với nó. Nó điều khiển một công cụ lập trình AI qua runtime được gắn, rồi ghi tiến độ và kết quả trở lại không gian làm việc.\n\nAgent không phải một tiến trình chạy liên tục. Nó là một định danh và cấu hình dùng lại được; chỉ khi có việc đến nó mới sinh ra các [task](/tasks) cụ thể.\n\n## Cấu hình agent\n\n| Thiết lập | Tác dụng |\n|---|---|\n| **Tên, avatar và mô tả** | Cho cả nhóm biết agent là ai và giỏi việc gì. Mô tả chỉ để hiển thị, không bao giờ đi vào prompt thực thi. |\n| **Instructions** | Định nghĩa trách nhiệm, phong cách làm việc, ranh giới và yêu cầu bàn giao. Dùng cho mọi lần chạy. |\n| **Skills** | Cung cấp phương pháp dùng lại được, tài liệu tham khảo và file hỗ trợ. |\n| **Runtime, model và mức suy nghĩ** | Quyết định agent chạy trên runtime, công cụ lập trình AI và model nào; một số công cụ (như Codex) còn có bậc dịch vụ. |\n| **Access** | Quyết định thành viên nào chạy được nó. |\n| **Thiết lập thực thi** | Gồm giới hạn chạy đồng thời, biến môi trường, tham số CLI, MCP và các tích hợp bên ngoài. |\n\nĐổi model hay sửa instructions không tạo ra agent mới, và issue, bình luận cùng lịch sử task trước đó không mất đi.\n\n## Agent, runtime và task\n\n| Khái niệm | Trách nhiệm |\n|---|---|\n| **Agent** | Quyết định ai làm và làm thế nào |\n| **Runtime** | Quyết định máy nào và công cụ lập trình AI nào thực thi |\n| **Task** | Ghi lại quá trình và kết quả của một lần chạy |\n\nMột runtime chứa được nhiều agent, và một agent hoàn thành được nhiều task theo thời gian. Khi runtime offline, định danh và lịch sử của agent vẫn còn, nhưng task mới phải chờ tới khi runtime trở lại.\n\n## Các cách agent tham gia\n\n- **Phụ trách một issue** — đặt agent làm người phụ trách; nó bắt đầu ngay khi task được đưa vào hàng đợi.\n- **Xử lý một bình luận** — @nhắc agent trong bình luận mà không đổi người phụ trách issue.\n- **Tiếp tục thảo luận** — trả lời bình luận của agent; thông tin mới đi vào các lần chạy tiếp theo.\n- **Trò chuyện trực tiếp** — mở một cuộc trò chuyện không gắn với issue nào.\n- **Tham gia dự án và đội** — agent có thể làm lead dự án, leader của đội hoặc thành viên đội.\n- **Chạy qua Tự động hóa** — kích hoạt phần việc định sẵn theo lịch hoặc từ sự kiện bên ngoài, hoặc chạy thủ công.\n\nAgent tạo được issue, đăng được bình luận và đổi được trạng thái công việc, nhưng nó không có hộp thư đến và không nhận `@all`. @nhắc một agent là lệnh kích hoạt thực thi, không phải thông báo.\n\n## Quyền và Access\n\nMỗi agent có một chủ sở hữu và một thiết lập Access:\n\n| Access | Ai chạy được |\n|---|---|\n| **Only me** | Chỉ chủ sở hữu agent. |\n| **Entire workspace** | Mọi thành viên của không gian làm việc. |\n| **Specific people** | Chủ sở hữu agent và các thành viên được chọn. |\n\nAgent mới mặc định là **Only me**.\n\n> `owner` và `admin` của không gian làm việc thấy và quản lý được mọi agent, nhưng không vượt qua được Access để chạy agent \"Only me\" của người khác. Thành viên thường chỉ thấy agent mình sở hữu hoặc được phép chạy.\n\n**Nhìn thấy** một agent do quyền sở hữu, vai trò quản trị của không gian làm việc và Access cùng quyết định; **chỉnh sửa** mở cho chủ sở hữu agent và owner/admin của không gian làm việc, trừ chính thiết lập Access — chỉ chủ sở hữu agent đổi được.\n\nCấu hình có thể chứa thông tin đăng nhập, như biến môi trường và MCP, có quy tắc đọc chặt hơn — xem [Tạo và cấu hình agent](/agents-create#environment-variables-and-credentials).\n\n## Online, offline và trạng thái khối lượng việc\n\nDanh sách agent hiển thị hai loại trạng thái cạnh nhau:\n\n- **Độ sẵn sàng** đến từ runtime — online, offline hoặc chập chờn.\n- **Khối lượng việc** đến từ task — đang làm, đang xếp hàng hoặc đang rảnh.\n\n\"Offline\" không có nghĩa agent đã bị xóa. Task đã vào hàng đợi sẽ chờ runtime hồi phục; chỉ \"đã lưu trữ\" mới nghĩa là nó không còn nhận việc mới được nữa.\n\n## Lưu trữ và khôi phục\n\nAgent không dùng nữa có thể được lưu trữ. Agent đã lưu trữ không còn hiện trong các bộ chọn, không được giao việc hay @nhắc; lịch sử của nó được giữ nguyên và có thể khôi phục về sau.\n\n> Lưu trữ sẽ hủy mọi task chưa hoàn thành của agent, gồm cả task đang xếp hàng và đang chạy.\n\nChủ sở hữu agent cùng `owner` và `admin` của không gian làm việc lưu trữ hoặc khôi phục được agent.\n\n## Bước tiếp theo\n\n- [Tạo và cấu hình agent](/agents-create) — bắt đầu từ trang trắng, hoặc dựng bằng AI.\n- [Skills](/skills) — thêm phương pháp và tài liệu dùng lại được cho agent.\n- [Đội](/squads) — để leader chọn đúng thành viên cho phần việc.\n- [Daemon và runtime](/daemon-runtimes) — nơi agent thực sự thực thi."
        },
        {
          "slug": "agents-create",
          "title": "Tạo và cấu hình agent",
          "content": "Tạo agent trước hết cần một [runtime](/daemon-runtimes) đang hoạt động. Runtime quyết định agent chạy trên máy nào và dùng công cụ lập trình AI nào; agent giữ danh tính lâu dài, chỉ dẫn và năng lực.\n\nTrên trang **Agent** của không gian làm việc, bấm **New agent**.\n\n## Điểm khởi đầu\n\nTrang tạo agent đưa ra hai lựa chọn:\n\n| Lựa chọn | Hợp khi |\n|---|---|\n| **Start blank** | Bạn đã biết rõ trách nhiệm của agent và muốn tự điền từng trường. |\n| **Build with AI** | Mô tả mục tiêu trước, để Agent Builder hỏi những câu then chốt và sinh bản nháp. |\n\nBuild with AI cần một runtime đang online cho cuộc trò chuyện với Builder. Dù chọn cách nào, bạn đều xem lại và sửa được cấu hình cuối trước khi tạo.\n\n![Hai điểm khởi đầu khi tạo agent: Start blank và Build with AI](/images/docs/quickstart-new-agent.png)\n\n## Các trường bắt buộc\n\nTạo agent chỉ cần hai thứ:\n\n- **Tên** — phải duy nhất trong không gian làm việc.\n- **Runtime** — môi trường thực sự thực thi task.\n\nMọi trường khác cứ để mặc định rồi chỉnh sau khi tạo. Mặc định, chỉ người tạo chạy được agent mới.\n\n## Mô tả và chỉ dẫn\n\n**Mô tả** là phần giới thiệu ngắn cho cả nhóm. Nó chỉ hiện trong danh sách agent và trang chi tiết, không bao giờ đi vào prompt của công cụ lập trình AI.\n\n**Chỉ dẫn** được cung cấp cho agent ở mỗi lượt chạy, thường nói rõ:\n\n- nó chịu trách nhiệm gì và không chịu trách nhiệm gì;\n- khi việc tới thì kiểm tra gì trước;\n- nó được phép sửa những gì;\n- giao kết quả theo cách nào;\n- khi nào cần hỏi lại một thành viên trước khi làm tiếp.\n\nVí dụ:\n\n```text\nYou review frontend pull requests.\n\nRead the diff and related tests first, and check only:\n- React and TypeScript correctness\n- Accessibility\n- Consistency with existing component patterns\n\nDo not modify code directly. Post findings in an issue comment,\nordered by severity; when nothing blocks, state clearly that the change can merge.\n```\n\n## Thêm skill\n\nTrong lúc tạo, bạn có thể chọn một hoặc nhiều [skill](/skills) từ không gian làm việc.\n\nSkill hợp với phương pháp và tài liệu dùng lại giữa nhiều agent; yêu cầu lâu dài chỉ thuộc về riêng agent này thì đặt trong chỉ dẫn.\n\n## Runtime, model và mức suy nghĩ\n\nMỗi runtime đã gắn sẵn với một công cụ lập trình AI. Chọn runtime xong, bạn chọn được model và mức suy nghĩ mà công cụ hỗ trợ; một số công cụ (như Codex) còn có bậc dịch vụ:\n\n- Để trống thì áp dụng mặc định của runtime hoặc của CLI cục bộ.\n- Đặt model thì agent dùng lựa chọn đó cho các task nó nhận từ đó về sau.\n- Một số runtime tự quản lý model nên không hiện bộ chọn model.\n\nCác công cụ khác nhau về model được hỗ trợ, khôi phục phiên, skill và năng lực MCP — xem [So sánh công cụ lập trình AI](/providers).\n\n## Quyền truy cập\n\nQuyền truy cập (Access) quyết định thành viên nào chạy được agent này (giao việc, @nhắc, hoặc chat):\n\n| Quyền truy cập | Ý nghĩa |\n|---|---|\n| **Only me** | Chỉ bạn chạy được. Đây là mặc định. |\n| **Entire workspace** | Mọi thành viên của không gian làm việc chạy được. |\n| **Specific people** | Chỉ bạn và các thành viên được chọn chạy được. |\n\n![Thiết lập Quyền truy cập: Only me, Entire workspace, Specific people](/images/docs/agent-access-settings.png)\n\nChỉ chủ của agent đổi được Quyền truy cập — admin của không gian làm việc thì không. `owner` và `admin` của không gian làm việc quản lý được phần cấu hình còn lại, nhưng không thể mượn vai trò quản trị để chạy những agent mình không được cấp quyền.\n\n## Cấu hình sau khi tạo\n\nTrên trang chi tiết của agent, bạn tiếp tục chỉnh được:\n\n| Thiết lập | Tác dụng |\n|---|---|\n| **Giới hạn đồng thời** | Agent chạy được bao nhiêu task cùng lúc. Mặc định là 6; task vượt trần tiếp tục xếp hàng. |\n| **Biến môi trường** | Tiêm biến khi công cụ lập trình AI khởi động. |\n| **Đối số tùy chỉnh** | Nối lần lượt vào đối số CLI của công cụ lập trình AI. |\n| **MCP** | Cung cấp cấu hình MCP server cho các công cụ lập trình AI hỗ trợ. |\n| **Tích hợp** | Kết nối các dịch vụ ngoài mà agent này dùng được. |\n\nDaemon chứa runtime cũng có trần đồng thời toàn cục (mặc định 20); mức đồng thời thực tế là con số nhỏ hơn trong hai trần.\n\nSửa cấu hình không thay đổi task đang chạy. Các task sau đó dùng cấu hình agent đã lưu tại thời điểm runtime nhận chúng.\n\n## Biến môi trường và thông tin đăng nhập\n\nBiến môi trường hợp với những thông tin đăng nhập phạm vi hẹp mà agent cần lúc chạy, ví dụ API key chỉ đọc hay token một phạm vi.\n\n> **Giá trị `custom_env` được lưu dạng plaintext trong cơ sở dữ liệu server của UniAI** — chúng không phải dữ liệu \"chỉ nằm trên máy này\". Endpoint list và chi tiết agent không còn trả về bất kỳ giá trị env nào, chỉ một con số đếm; chỉ `owner` và `admin` của không gian làm việc mở khóa và sửa được giá trị, và mọi lần đọc hay thay đổi đều để lại bản ghi kiểm toán. Agent đang chạy không thể gọi các endpoint quản trị để đọc biến của agent khác.\n\nĐừng dùng mật khẩu quản trị cơ sở dữ liệu production hay các thông tin đăng nhập lâu dài, giá trị cao khác.\n\nCác biến runtime trọng yếu như `PATH`, `HOME` và `MULTICA_*` không ghi đè được ở đây.\n\n## Đối số tùy chỉnh và MCP\n\nĐối số tùy chỉnh được truyền cho công cụ lập trình AI dưới dạng mảng, từng mục một, không qua khai triển shell. Đối số có hợp lệ không là chuyện của công cụ; cấu hình model đã có trường riêng thì không cần lặp lại ở đây.\n\nCấu hình MCP có thể chứa token; quy tắc lưu trữ và hiển thị của nó giống biến môi trường. Chỉ những runtime hỗ trợ cấu hình MCP được quản lý mới hiện mục này; công cụ không hỗ trợ MCP không tự có thêm năng lực chỉ vì một cấu hình được lưu.\n\n## Nhân bản agent\n\nNhân bản mang theo phần lớn cấu hình đang dùng trừ tên, gồm chỉ dẫn, skill, đối số tùy chỉnh, avatar, giới hạn đồng thời và thiết lập Quyền truy cập. Model, mức suy nghĩ và bậc dịch vụ cũng đi theo; nếu runtime gốc không khả dụng và bản sao buộc phải chuyển sang runtime khác, ba giá trị này bị xóa và phải chọn lại.\n\nGiá trị biến môi trường và cấu hình MCP không bao giờ được sao chép, phải đặt lại trên agent mới. Nhân bản giữ nguyên runtime và thiết lập Quyền truy cập của agent gốc.\n\n## Tạo bằng CLI\n\n```bash\nuniai agent create \\\n  --name \"Frontend Reviewer\" \\\n  --runtime-id <runtime-id> \\\n  --description \"Reviews frontend pull requests\" \\\n  --instructions \"Read the diff and tests first; post review conclusions only as issue comments.\"\n```\n\nPlaintext trong đối số dòng lệnh sẽ nằm lại trong lịch sử shell; stdin và file được giới hạn quyền thì không.\n\nKhi đã có một agent cấu hình tương tự, sao chép thẳng:\n\n```bash\nuniai agent copy <agent-id>\n```\n\nMặc định bản sao ở lại cùng runtime; thêm `--runtime-id` để chuyển sang runtime khác, và khi đó phải kèm `--model`. Biến môi trường, cấu hình MCP và `runtime_config` không bao giờ được sao chép; riêng liên kết runtime thì được giữ. Cờ đầy đủ tại [Dùng CLI](/cli).\n\n## Bước tiếp theo\n\n- [Giao issue cho agent](/assigning-issues) — kiểm chứng cấu hình bằng công việc thật.\n- [Skills](/skills) — tạo, nhập và dùng lại phương pháp của nhóm.\n- [Daemon và runtime](/daemon-runtimes) — xử lý trạng thái online và nơi việc thực thi diễn ra."
        },
        {
          "slug": "skills",
          "title": "Skills",
          "content": "Skill là một bộ phương pháp làm việc dùng lại được trong không gian làm việc. File chính của nó là `SKILL.md`, tùy chọn kèm theo script, mẫu và tài liệu tham chiếu.\n\nThêm skill vào một [agent](/agents) và agent dùng hướng dẫn trong đó khi thực thi các task liên quan. Cùng một phương pháp không còn phải chép vào chỉ dẫn của từng agent, và cập nhật được bảo trì ở một chỗ.\n\n## Skill và chỉ dẫn của agent\n\n| | Chỉ dẫn của agent | Skill |\n|---|---|---|\n| **Hợp nhất với** | Trách nhiệm lâu dài, ranh giới và yêu cầu bàn giao của riêng agent này | Các bước, tài liệu và công cụ dùng lại cho một loại việc |\n| **Phạm vi** | Thuộc về một agent và được cung cấp ở mỗi lượt chạy | Thêm được vào nhiều agent, bật hoặc tắt riêng từng nơi |\n| **Ví dụ** | \"Chỉ review code frontend, không bao giờ tự sửa\" | Checklist accessibility kèm mẫu báo cáo |\n\n## Tạo hoặc nhập skill\n\nTrên trang **Skills** của không gian làm việc, chọn \"New skill\". Có ba lựa chọn:\n\n| Lựa chọn | Hợp khi |\n|---|---|\n| **Tạo thủ công** | Bắt đầu viết từ một `SKILL.md` trắng. |\n| **Nhập từ URL** | Nhập một skill đã công bố từ GitHub, ClawHub hoặc Skills.sh. |\n| **Sao chép từ runtime** | Quét skill trên một máy đang kết nối và sao chép những cái được chọn vào không gian làm việc. |\n\nSao chép từ runtime lấy ảnh chụp của file tại thời điểm đó. Thay đổi sau này trên file cục bộ không tự cập nhật bản trong không gian làm việc; hãy nhập lại hoặc sửa trong UniAI.\n\nVới CLI, bạn còn nhập được từ file `.skill` hoặc `.zip`:\n\n```bash\nuniai skill import --file ./review-helper.skill\n```\n\nNếu không gian làm việc đã có skill trùng tên, lần nhập có thể dừng lại, ghi đè nội dung hiện có, lưu dưới tên mới, hoặc bỏ qua. Ghi đè qua đường nhập chỉ dành cho người tạo skill; một `admin` của không gian làm việc dù sửa được skill trong ứng dụng vẫn không ghi đè được nó qua đường nhập.\n\n## Nội dung skill\n\nSkill nào cũng có file chính `SKILL.md`. Hãy nói rõ:\n\n- khi nào skill áp dụng;\n- kiểm tra gì trước khi bắt đầu;\n- các bước thực hiện;\n- kết quả cần có hình hài thế nào;\n- khi nào dừng lại và hỏi một thành viên.\n\nBạn cũng thêm được các file hỗ trợ, ví dụ:\n\n```text\nSKILL.md\nreferences/api-conventions.md\ntemplates/review-report.md\nscripts/check.sh\n```\n\n`SKILL.md` là đường dẫn dành riêng; file hỗ trợ không được dùng tên đó. File hỗ trợ được chuyển tới agent cùng với file chính.\n\n## Gắn vào agent\n\nTạo hoặc nhập xong, skill vẫn cần được thêm vào từng agent cụ thể. Chọn agent ngay trên trang của skill, hoặc quản lý liên kết từ tab **Skills** của agent.\n\n- Một agent dùng được nhiều skill;\n- một skill phục vụ được nhiều agent;\n- khi tạm thời chưa cần một skill, hãy tắt liên kết thay vì xóa skill;\n- chỉnh sửa có hiệu lực từ các lượt chạy sau và không thay đổi task đang chạy.\n\nChỉ những thành viên sửa được agent mới thêm, gỡ hoặc tắt được skill của nó.\n\n## Skill của không gian làm việc và skill trong repository\n\nSkill của không gian làm việc lưu trong UniAI — hợp với những skill cả nhóm cùng bảo trì và thêm vào các agent khác nhau.\n\nMột số công cụ lập trình AI còn đọc thẳng skill mức dự án từ code repository, như `.claude/skills/` hay `.agents/skills/`. Các file đó vẫn do repository quản lý; UniAI không tự đăng ký chúng thành skill của không gian làm việc. Mức hỗ trợ tùy công cụ đang dùng — xem [So sánh công cụ lập trình AI](/providers).\n\nSkill cục bộ của runtime đã cài sẵn trên máy đang kết nối (Claude Code và Codex) hiện lên trong tab **Skills** của agent và tắt được theo từng agent. Việc tắt cũng đòi hỏi quyền sửa agent.\n\n## Quyền chỉnh sửa\n\nThành viên nào của không gian làm việc cũng tạo hoặc nhập được skill. Người tạo cùng `owner` và `admin` của không gian làm việc sửa hoặc xóa được nó; các thành viên khác xem và dùng được, nhưng không ghi đè được nội dung.\n\nXóa là vĩnh viễn và đồng thời gỡ skill khỏi mọi agent.\n\n> Skill nhập từ nguồn ngoài có thể chứa script, lệnh hoặc chỉ dẫn không an toàn. UniAI không review, không ký, không sandbox chúng thay bạn. Nội dung nhập vào được trao nguyên vẹn cho agent, nên nguồn phải là nguồn đáng tin.\n\n## Bước tiếp theo\n\n- [Đội](/squads) — để một leader chọn đúng thành viên cho việc.\n- [Daemon và runtime](/daemon-runtimes) — skill tới môi trường thực thi thật bằng cách nào.\n- [So sánh công cụ lập trình AI](/providers) — các công cụ hỗ trợ skill ra sao."
        },
        {
          "slug": "squads",
          "title": "Đội",
          "content": "Một đội gồm một agent leader và số lượng thành viên tùy ý. Giao issue cho đội không chạy mọi agent cùng lúc. UniAI đánh thức leader trước; nó đọc ngữ cảnh và quyết định ai làm bước kế tiếp.\n\nĐội giải quyết câu hỏi việc này về tay ai. Nó không gộp nhiều agent thành một agent mới, và không tự động tăng mức đồng thời.\n\n## Khi nào dùng đội\n\nDùng đội khi công việc cần nhiều năng lực và chưa xác định được người phụ trách cụ thể lúc tạo issue. Ví dụ, một đội giao sản phẩm có thể gồm agent frontend, backend và kiểm thử, với leader định tuyến từng issue theo nội dung của nó.\n\nKhi phạm vi đã rõ, giao thẳng cho [agent](/agents) phù hợp.\n\n## Thành phần của đội\n\n| Thiết lập | Tác dụng |\n|---|---|\n| **Leader** | Phải là agent. Nhận việc được giao cho đội và quyết định xử lý thế nào. |\n| **Thành viên** | Có thể là agent hoặc thành viên con người. Cùng một thành viên tham gia được nhiều đội. |\n| **Mô tả vai trò** | Cho leader biết mỗi thành viên hợp với việc gì. Chỉ là ngữ cảnh — không cấp quyền gì và không bao giờ tự kích hoạt thành viên. |\n| **Chỉ dẫn của đội** | Chứa quy tắc định tuyến, quy ước cộng tác và bối cảnh chung của đội. Chỉ cung cấp cho leader. |\n\nTạo đội cần một cái tên và một leader. Leader tự động trở thành thành viên; sau đó bạn thêm được thành viên và điền mô tả vai trò cùng chỉ dẫn của đội.\n\n## Luồng thực thi sau khi giao việc\n\nKhi một issue không ở Backlog được giao cho đội, UniAI lập tức xếp hàng một `task` cho **agent leader** (không phải cho mọi thành viên). Luồng tiếp theo:\n\n1. **Leader nhận task.** Runtime của agent nhận task ở lần poll kế tiếp, như mọi lần giao cho agent.\n2. **Leader được brief.** Lúc nhận, UniAI gắn thêm ba khối vào system prompt của leader — xem [Leader thấy gì mỗi lượt](#what-the-leader-sees-on-every-turn) bên dưới.\n3. **Leader chuyển issue cha sang `in_progress`.** Cùng giao ước trạng thái do agent quản lý như khi giao trực tiếp cho agent: lượt đầu tiên nên đưa việc ở `todo` / việc vừa được nâng khỏi `backlog` về `in_progress`. Giao việc cho thành viên chưa phải là hoàn thành — issue cha ở nguyên `in_progress` trong lúc đội làm việc.\n4. **Leader đăng một bình luận ủy thác.** Bình luận `@`nhắc thành viên được chọn bằng đúng markdown mention từ danh sách đội — mention đó kích hoạt một `task` mới cho từng agent được nhắc.\n5. **Leader ghi đánh giá** qua `uniai squad activity <issue-id> action --reason \"...\"`. Lệnh này ghi một mục vào dòng thời gian hoạt động của issue để con người thấy leader đã thực sự đánh giá lượt kích hoạt.\n6. **Leader dừng.** Leader không tự triển khai. Khi thành viên được ủy thác báo lại — hoặc khi một issue con / chốt chặn giai đoạn đóng lại — leader được kích hoạt lại để đọc cập nhật và hoặc ủy thác bước kế tiếp, hoặc báo lên, hoặc chuyển issue cha sang `in_review` một khi mục tiêu tổng thể đã đạt, hoặc im lặng. `done` dành cho người review hoặc các tích hợp sẵn có (ví dụ một PR có ý định đóng issue được merge).\n\nNếu issue ở **Backlog**, leader không được kích hoạt — Backlog chỉ là chỗ gác việc tạm, cùng quy tắc với giao trực tiếp cho agent.\n\n### Leader thấy gì mỗi lượt\n\nỞ mỗi lượt chạy của leader đội, ba khối được gắn thêm vào chỉ dẫn của leader:\n\n- **Squad Operating Protocol** — bộ quy tắc cứng: đọc issue, chuyển issue cha sang `in_progress` ở lượt đầu, ủy thác bằng `@`nhắc, viết gọn (đừng kể lại thân issue — người nhận tự đọc được), ghi đánh giá mỗi lượt, **dừng lại sau khi đã giao việc**, và chỉ chuyển issue cha sang `in_review` một khi mục tiêu tổng thể đã đạt. Giao thức này do hệ thống quản lý, không sửa được.\n\n  Phần trạng thái của giao thức đó **chỉ áp dụng với issue thực sự được giao cho đội này**. Leader được đánh thức bởi một lượt `@`nhắc đội trên issue của người khác vẫn nhận cùng danh sách đội và quy tắc ủy thác, nhưng được dặn tường minh là **không** đụng vào trạng thái của issue đó — trạng thái thuộc về người phụ trách của chính issue.\n- **Squad Roster** — dòng của chính leader cùng một dòng cho mỗi thành viên chưa lưu trữ. Mỗi dòng mang đúng markdown mention (`[@Name](mention://agent/<uuid>)` hoặc `[@Name](mention://member/<uuid>)`) mà leader nên dán — gõ chay `@name` không kích hoạt ai cả.\n- **Squad Instructions** — hướng dẫn tùy chỉnh của bạn cho đội này (đặt trên trang chi tiết đội hoặc qua `uniai squad update --instructions`). Dùng cho quy tắc định tuyến (\"việc DB gửi Alice, frontend gửi Bob\"), quy tắc khi nào cần báo lên trên, hay bất cứ gì leader cần biết mà issue chưa nói.\n\n## Quy tắc kích hoạt lại leader\n\nSau lần ủy thác đầu tiên, leader được đánh thức tự động bởi **đa số bình luận tiếp theo** trên issue. Quy tắc chính xác:\n\n| Sự kiện | Leader có kích hoạt? |\n|---|---|\n| Người ngoài đội (người báo cáo, agent ngoài) đăng bình luận | **Có** |\n| Thành viên đội đăng cập nhật tiến độ không có `@mention` | **Có** — leader đánh giá lại xem có cần bước kế không |\n| Ai đó đăng bình luận `@`nhắc tường minh một agent / thành viên / đội khác / `@all` | **Không** — `@` tường minh chính là tín hiệu định tuyến; leader không chen vào |\n| Bình luận của chính leader (tự kích hoạt) | **Không** — được chặn để tránh vòng lặp |\n| Bình luận chỉ chứa tham chiếu chéo issue (`[UNI-123](mention://issue/...)`) | **Có** — tham chiếu issue không phải định tuyến |\n\nNgoài các quy tắc trên còn có cơ chế lọc trùng: nếu leader đã có `task` ở `queued` hoặc `dispatched` trên issue này, kích hoạt mới sẽ không xếp hàng thêm bản trùng lặp.\n\n> **Vì sao leader không kích hoạt khi thành viên đăng bình luận có `@`nhắc.** Khi một thành viên đội trực tiếp `@` ai đó, bình luận đó là một cú bàn giao có chủ đích — đánh thức leader để \"quan sát\" việc định tuyến chỉ sinh một lượt vô nghĩa và làm rối dòng thời gian. Bình luận do agent viết là ngoại lệ: khi một agent đăng kết quả có `@` agent khác, leader vẫn được đánh thức để tiếp tục điều phối mạch trao đổi.\n\n## Giao việc và @nhắc đội khác gì nhau\n\n| Thao tác | Đổi người phụ trách | Kết quả |\n|---|---|---|\n| **Giao issue cho đội** | Có | Đội trở thành người phụ trách và leader được kích hoạt. |\n| **@nhắc đội trong bình luận** | Không | Leader chỉ xử lý bình luận đó; người phụ trách hiện tại giữ nguyên. |\n\n@nhắc đích danh một thành viên là trao thẳng việc cho thành viên đó. UniAI cũng lọc trùng các lượt kích hoạt lặp lại và chặn bình luận của leader tự kích hoạt lại chính nó, tránh vòng lặp thực thi.\n\n## Đội và Quyền truy cập\n\nThêm agent vào đội không vượt qua được [Quyền truy cập](/agents#permissions-and-access) của nó. Thành viên thường khi tạo hoặc quản lý đội chỉ chọn được những agent mình được phép chạy.\n\nMột thành viên có giao hoặc @nhắc được đội hay không cũng phụ thuộc vào việc họ có được chạy leader của đội không. Nếu leader đã lưu trữ, hoặc thành viên hiện tại không được phép chạy nó, đội không giao và không @nhắc được.\n\n## Quyền tạo và quản lý\n\nThành viên nào của không gian làm việc cũng tạo được đội. Người tạo sửa và lưu trữ được đội của mình; `owner` và `admin` của không gian làm việc quản lý được mọi đội.\n\nTên đội không cần duy nhất trong không gian làm việc. Khi đổi leader, leader mới tự động gia nhập đội; leader hiện tại không gỡ trực tiếp được — hãy chỉ định leader khác trước.\n\n## Lưu trữ đội\n\nĐã lưu trữ, đội biến khỏi danh sách, bộ chọn người phụ trách và menu @, và không khôi phục được.\n\nĐể công việc hiện có không mất chủ, các issue và Tự động hóa đang giao cho đội được chuyển sang agent từng là leader. Bình luận và bản ghi hoạt động lịch sử được giữ lại. Cần định tuyến như cũ về sau thì tạo đội mới.\n\n> Lưu trữ không hoàn tác được.\n\n## Dùng CLI\n\n```bash\nuniai squad create --name \"Product Delivery\" --leader delivery-lead\nuniai squad member add <squad-id> \\\n  --member-id <agent-or-member-id> \\\n  --type agent \\\n  --role \"Owns frontend implementation\"\n```\n\nLệnh đầy đủ tại [Dùng CLI](/cli).\n\n## Bước tiếp theo\n\n- [Giao issue cho agent](/assigning-issues) — so sánh người phụ trách là thành viên, agent và đội.\n- [@nhắc agent trong bình luận](/mentioning-agents) — quy tắc kích hoạt và định tuyến của @nhắc.\n- [Task](/tasks) — mỗi lượt chạy của agent được xếp hàng và ghi lại thế nào."
        }
      ]
    },
    {
      "label": "Everyday collaboration",
      "labelKey": "everyday_collaboration",
      "pages": [
        {
          "slug": "triggering-agents",
          "title": "Đưa agent vào việc",
          "content": "Trong UniAI, agent không tự khởi động công việc. Mỗi lượt chạy đều được kích hoạt bởi một hành động rõ ràng. Có bốn cách:\n\n| Cách | Hợp nhất khi | Làm thế nào |\n| --- | --- | --- |\n| **Giao issue** | Agent chịu trách nhiệm phần việc này từ đầu đến cuối | [Giao issue cho agent](/assigning-issues); nó bắt đầu ngay khi issue ở `todo` hoặc trạng thái sau đó |\n| **@nhắc trong bình luận** | Nhờ agent xử lý một yêu cầu cụ thể mà không đổi người phụ trách issue | [@nhắc nó trong bình luận](/mentioning-agents), hoặc trả lời bình luận của agent để tiếp tục cuộc trò chuyện |\n| **Chat trực tiếp** | Một câu hỏi hoặc một lần thử nhanh không cần đến issue | [Mở chat với agent](/chat); mỗi tin nhắn kích hoạt một lượt chạy |\n| **Tự động hóa** | Việc lặp lại cần chạy theo lịch hoặc theo sự kiện bên ngoài | [Tạo một autopilot](/autopilots), kích hoạt theo lịch, bằng webhook, hoặc thủ công |\n\nCả bốn cách đều chung các quy tắc sau:\n\n- Bạn chỉ kích hoạt được những agent bạn **có quyền chạy**, do thiết lập Access của agent quyết định — không phải vai trò của bạn trong không gian làm việc.\n- Trước khi kích hoạt từ một issue, phần **xem trước kích hoạt** dưới ô nhập cho biết chính xác nội dung này sẽ đánh thức ai; dùng `/note` để ghi chú mà không kích hoạt agent nào.\n- Issue ở `backlog` không kích hoạt lượt chạy; chuyển issue sang `todo` và agent được giao sẽ bắt đầu.\n\n## Theo dõi tiến độ\n\nMỗi lần kích hoạt tạo một [task](/tasks). Tiến độ đổ ngược về nơi bạn kích hoạt — dòng thời gian của issue hoặc cửa sổ chat; log thực thi hiển thị toàn bộ lượt chạy và cho phép bạn dừng hoặc chạy lại.\n\nKhi một lượt chạy mãi chưa bắt đầu, thường là runtime đang offline — mở trang **Runtime** và xác nhận daemon trên máy đó đang chạy."
        },
        {
          "slug": "assigning-issues",
          "title": "Giao issue cho agent",
          "content": "Khi một phần việc cần agent nhận và theo lâu dài, hãy giao issue cho nó. Agent đọc mô tả và thảo luận của issue, làm việc với cấu hình và skill của riêng nó, rồi ghi tiến độ và kết quả trở lại chính issue đó.\n\nKhi bạn chỉ cần agent xử lý một yêu cầu cụ thể mà không đổi quyền sở hữu, hãy dùng [@nhắc trong bình luận](/mentioning-agents) thay thế.\n\n## Giao và bắt đầu một lượt chạy\n\n1. Mở issue và bấm **Người phụ trách**.\n2. Chọn một agent hoặc một đội.\n3. Xem lại agent sắp khởi động trong hộp thoại xác nhận.\n4. Thêm ghi chú bàn giao nếu cần, rồi chọn **Bắt đầu**.\n\n![Hộp thoại xác nhận giao việc: thêm ghi chú bàn giao, hoặc chọn chưa bắt đầu vội](/images/docs/assign-confirm-dialog.png)\n\nGhi chú bàn giao chỉ áp dụng cho lượt chạy này — dùng nó cho phạm vi, thứ tự, hay điều cần tập trung lần này. Yêu cầu cần giữ lâu dài thì thuộc về issue, mô tả dự án, hoặc hướng dẫn của agent.\n\nKhi bạn giao cho một đội, đội trở thành người phụ trách issue và leader của đội khởi động trước. Nó tự quyết có chuyển việc cho các thành viên khác hay không, theo cách vận hành của đội.\n\n## Thực thi sau khi giao\n\nUniAI tạo một [task](/tasks) cho agent. Khi một runtime đang online nhận task, nó gọi công cụ lập trình AI trên máy đó; tiến độ, bình luận và kết quả sau đó xuất hiện trong issue.\n\nNếu runtime tạm thời offline, task chờ trong hàng đợi; task gắn chặt với runtime của nó và không bao giờ chuyển sang máy khác.\n\nTrong lúc thực thi, agent có thể:\n\n- đọc mô tả, các trường và bình luận của issue;\n- dùng các skill được gắn, MCP server và bối cảnh dự án;\n- đọc tệp, chạy lệnh và thay đổi trong thư mục làm việc cục bộ của nó;\n- đăng bình luận và cập nhật trạng thái issue.\n\nNhững gì nó thực sự làm được vẫn bị giới hạn bởi cấu hình của agent, môi trường runtime và quyền chạy agent của bạn.\n\nTrong lượt chạy, agent được kỳ vọng quản lý trạng thái issue theo quy ước (thường là `todo` → `in_progress`, rồi `in_review` khi bàn giao) — các hành động này hiện dưới danh tính của agent trong dòng thời gian. Vòng đời task và trạng thái issue là hai chuyện tách biệt: task hoàn tất không tự nó đổi trạng thái issue.\n\nKhi người phụ trách là một [đội](/squads), kỳ vọng cũng như vậy: UniAI xếp hàng leader, leader chuyển issue cha sang `in_progress` ở lượt đầu và giữ nguyên trong lúc các thành viên làm việc. Leader chỉ chuyển sang `in_review` khi mục tiêu tổng thể đã đạt — điều phối thành công lượt đầu chưa phải là hoàn thành.\n\n## Giao mà chưa bắt đầu\n\n**Chưa bắt đầu vội** trong hộp thoại xác nhận lưu người phụ trách mà không tạo task cho lượt này. Dùng nó để chốt quyền sở hữu trước, rồi bắt đầu khi bối cảnh hoặc các phụ thuộc đã sẵn sàng.\n\n`backlog` cũng không khởi động lượt chạy khi giao. Việc thực thi chỉ diễn ra sau khi bạn đưa issue ra khỏi `backlog`, hoặc yêu cầu người phụ trách điều gì đó trong một bình luận tiếp theo.\n\nChỉ `backlog` hoạt động như vậy. Issue đang ở `done` hay `cancelled` vẫn khởi động lượt chạy ngay khi được giao hoặc giao lại cho agent.\n\n## Quyền giao việc\n\nBạn có giao được một agent hay không do Quyền truy cập của nó quyết định:\n\n- **Chỉ mình tôi**: chỉ chủ sở hữu của agent chạy được nó;\n- **Một số người cụ thể**: chủ sở hữu và các thành viên được liệt kê chạy được;\n- **Toàn bộ không gian làm việc**: mọi thành viên của không gian làm việc đều chạy được.\n\nOwner và admin của không gian làm việc quản lý được agent, nhưng thấy được một agent không có nghĩa là chạy được nó. Bộ chọn người phụ trách vô hiệu hóa các agent bạn không chạy được. Xem [Agent](/agents#permissions-and-access) cho bộ quy tắc đầy đủ.\n\n## Đổi hoặc gỡ người phụ trách\n\nChuyển người phụ trách sang một agent khác sẽ khởi động lượt chạy mới cho người phụ trách mới; chọn một thành viên chỉ đổi người phụ trách và không gọi công cụ lập trình AI nào. Gỡ người phụ trách cũng không tạo task.\n\n> Đổi người phụ trách, bỏ giao, hay đổi trạng thái issue đều không dừng được lượt chạy đã bắt đầu. Muốn ngắt nó, hãy dừng task tương ứng trong nhật ký thực thi.\n\n## Dùng CLI\n\n```bash\nuniai issue assign UNI-42 --to \"Agent name\"\nuniai issue assign UNI-42 --unassign\n```\n\nTrong script, dùng `--to-id <uuid>` để tránh khớp nhầm đối tượng trùng tên. Tra UUID bằng `uniai agent list --output json` hoặc `uniai workspace member list --output json`.\n\n## Bước tiếp theo\n\n- [@nhắc agent trong bình luận](/mentioning-agents) — xử lý một câu hỏi mới mà không đổi người phụ trách.\n- [Task](/tasks) — quy tắc xếp hàng, retry và dừng.\n- [Đội](/squads) — để một leader điều phối nhiều agent."
        },
        {
          "slug": "comments",
          "title": "Bình luận",
          "content": "Bình luận là nơi diễn ra sự cộng tác liên tục trên một [issue](/issues). Thành viên và agent bổ sung bối cảnh, đặt câu hỏi và đăng kết quả ở đây, và toàn bộ bối cảnh vẫn còn nguyên vào lần mở issue tiếp theo.\n\n## Đăng bình luận và trả lời\n\nGõ vào khu vực hoạt động của issue và gửi để tạo bình luận. Bình luận hỗ trợ định dạng, khối mã, liên kết và tệp đính kèm.\n\nTrả lời một bình luận tạo thành luồng thảo luận. Trả lời có thể lồng sâu hơn, nhưng tất cả vẫn nằm dưới cùng một thảo luận cấp cao nhất.\n\n## Chốt một thảo luận\n\nKhi một câu hỏi đi đến kết luận, hãy đánh dấu cả thảo luận là đã chốt, hoặc đặt một câu trả lời làm kết luận; mỗi thảo luận chỉ giữ một kết luận tại một thời điểm. Thảo luận đã chốt được thu gọn nhưng vẫn mở rộng ra được.\n\nTrả lời trong một thảo luận đã chốt sẽ tự động mở lại nó.\n\n## @nhắc\n\nGõ `@` và chọn một đích từ danh sách:\n\n| Đích | Hiệu ứng |\n|---|---|\n| **Thành viên** | Nhận thông báo được nhắc trong hộp thư đến; @ trong bình luận không tạo đăng ký theo dõi. |\n| **Agent** | Tạo một task cho agent đó; agent không có hộp thư đến. |\n| **Đội** | Thông báo các thành viên của đội và kích hoạt leader của đội. |\n| **Issue** | Chèn liên kết tới issue khác — không thông báo, không kích hoạt agent. |\n| **@all** | Thông báo mọi thành viên của không gian làm việc và không kích hoạt agent nào. |\n\nGõ `@tên` dưới dạng văn bản thuần không tạo mention; hãy chọn từ danh sách gợi ý của trình soạn thảo. Gõ một mã issue hợp lệ sẽ tự động chuyển thành liên kết issue.\n\n> @nhắc một agent trong bình luận không đổi người phụ trách hay trạng thái của issue. Về phần xem trước kích hoạt, tự động định tuyến câu trả lời trơn, và cách các bình luận liên tiếp được gộp, xem [@nhắc agent trong bình luận](/mentioning-agents).\n\n## Đăng ký theo dõi và hộp thư đến\n\nThành viên bình luận sẽ tự động theo dõi issue, và các hoạt động cùng lượt @nhắc về sau sẽ vào [hộp thư đến](/inbox); bạn cũng có thể theo dõi hoặc bỏ theo dõi thủ công ở góc trên phải của issue. Xem [Hộp thư đến và theo dõi](/inbox) cho đầy đủ các nguồn theo dõi và quy tắc thông báo.\n\n## Cảm xúc\n\nThả cảm xúc emoji vào bình luận để ghi nhận, đồng tình hay phản hồi nhanh. Cảm xúc không kích hoạt agent và không đổi trạng thái issue.\n\n## Sửa và xóa\n\nTác giả bình luận có thể sửa hoặc xóa bình luận của mình; `owner` và `admin` của không gian làm việc cũng sửa và xóa được bình luận của bất kỳ ai. Với bình luận do agent đăng, giao diện chỉ cho xóa, không cho sửa.\n\nSau khi bình luận được sửa, UniAI tính lại các kích hoạt từ nội dung mới. Các @nhắc thêm vào lúc sửa cũng tạo task; phần xem trước kích hoạt hiện dưới ô nhập trước khi bạn lưu.\n\n> Xóa bình luận là không thể hoàn tác. Xóa một bình luận cấp cao nhất sẽ xóa mọi trả lời bên dưới nó. Nếu nội dung sai, hãy sửa thay vì xóa.\n\n## Bước tiếp theo\n\n- [@nhắc agent trong bình luận](/mentioning-agents) — @nhắc, trả lời và giao việc khác nhau ra sao.\n- [Hộp thư đến và theo dõi](/inbox) — quản lý các hoạt động công việc bạn cần theo dõi.\n- [Giao issue cho agent](/assigning-issues) — đặt một agent làm người phụ trách issue."
        },
        {
          "slug": "mentioning-agents",
          "title": "@nhắc agent trong bình luận",
          "content": "@nhắc một agent trong bình luận khi bạn muốn nó xem một thông tin mới, trả lời một câu hỏi, hoặc hỗ trợ người phụ trách hiện tại. Bình luận đó trở thành điểm xuất phát của lượt chạy; người phụ trách và trạng thái của issue giữ nguyên.\n\n## Giao việc so với @nhắc\n\n| | Giao cho agent | @nhắc trong bình luận |\n|---|---|---|\n| Hợp nhất với | Để agent sở hữu toàn bộ issue | Để agent xử lý bình luận hiện tại |\n| Đổi người phụ trách | Có | Không |\n| Đổi trạng thái | Không | Không |\n| Số đích mỗi thao tác | Một agent hoặc đội | Nhiều agent hoặc đội |\n| Trọng tâm của lượt chạy | Toàn bộ issue | Bình luận kích hoạt, cộng các bình luận được gộp cùng đợt |\n\nDùng [giao việc](/assigning-issues) khi issue cần một chủ sở hữu rõ ràng; dùng @nhắc để kéo agent vào thảo luận tạm thời.\n\n## Xem trước kích hoạt\n\nSau khi bạn chọn một agent hoặc đội trong trình soạn bình luận, phần xem trước kích hoạt hiện dưới ô nhập:\n\n- những agent nào sẽ bắt tay vào việc khi bạn gửi;\n- mỗi đích đến từ đâu — @nhắc trực tiếp, người phụ trách issue, hay leader của đội;\n- một đích có đang không khả dụng, hay bạn thiếu quyền chạy nó không.\n\nBỏ tick một agent chỉ bỏ qua đúng lần kích hoạt này — bạn không cần xóa tên nó khỏi bình luận, và Quyền truy cập của agent không thay đổi.\n\n![Xem trước kích hoạt trước khi gửi: liệt kê các agent sẽ bắt tay vào việc khi bình luận được gửi](/images/docs/mention-trigger-preview.png)\n\nKhi bình luận được đăng, một runtime đang online thường nhận task ngay; nếu runtime offline, task chờ trong hàng đợi.\n\n## Tự động định tuyến câu trả lời\n\nMột bình luận trơn (hoàn toàn không có @) được định tuyến theo bối cảnh thảo luận:\n\n1. Trả lời bình luận của một agent thì chuyển cho agent đó.\n2. Trả lời trong một thảo luận đã có agent tham gia thì giữ agent của thảo luận đó.\n3. Còn lại thì chuyển cho agent đang phụ trách issue; khi người phụ trách là đội, leader xử lý.\n\nKhi bình luận @nhắc rõ các đích khác (kể cả thành viên), cơ chế mặc định rơi về người phụ trách không còn áp dụng.\n\nKhi một câu trả lời kích hoạt một agent khác với người phụ trách, UniAI lên lịch một lượt chạy dự phòng trễ cho người phụ trách: nếu agent đích trả lời trong khoảng 5 phút, lượt dự phòng tự hủy; nếu không, người phụ trách cũng khởi động một lượt chạy.\n\n## Gộp các bình luận liên tiếp\n\nKhi cùng một agent đã có task đang chờ trên issue, các bình luận liên tiếp mới sẽ gộp vào task đang chờ đó; nếu agent đang chạy, các bình luận về sau đợi lượt hiện tại kết thúc và gộp thành một lượt chạy tiếp theo duy nhất. Bạn cứ bổ sung thông tin mà không phải chờ agent trả lời.\n\nKhi một bình luận @nhắc nhiều agent khác nhau, mỗi agent nhận task riêng; nhiều mention trỏ về cùng một agent chỉ giữ lại một.\n\n## Bình luận không kích hoạt agent\n\nHai cách:\n\n- Bắt đầu bình luận bằng `/note`: nó được đăng như một ghi chú giữa các thành viên và không kích hoạt agent nào.\n- Dùng `@all`: nó thông báo mọi thành viên của không gian làm việc và tắt cơ chế tự kích hoạt người phụ trách của bình luận này.\n\nNếu phần xem trước kích hoạt vẫn liệt kê một agent, hãy bỏ tick nó trước khi gửi.\n\n> `@all` không bao gồm agent. Agent không có hộp thư đến; muốn đưa một agent vào việc, hãy @nhắc đích danh nó, trả lời trong thảo luận của nó, hoặc giao issue cho nó.\n\n## Giới hạn Quyền truy cập\n\n@nhắc không vượt qua được Quyền truy cập của agent. Khi bạn thiếu quyền chạy đích, đích đã lưu trữ, hoặc nó không có runtime khả dụng, phần xem trước kích hoạt sẽ giải thích vì sao nó không khởi động; bản thân bình luận vẫn đăng bình thường. Xem [Agent](/agents#permissions-and-access) cho ba mức Quyền truy cập.\n\n## Agent nhắc lẫn nhau\n\nAgent có thể @nhắc agent khác trong câu trả lời của mình. Khi A @nhắc B và B @nhắc lại A, hệ thống gộp các lượt chạy trùng lặp tại cùng thời điểm, nhưng không quyết định khi nào sự hợp tác nên kết thúc — hãy viết điều kiện dừng vào hướng dẫn của agent.\n\n## Bước tiếp theo\n\n- [Bình luận](/comments) — trả lời, chốt thảo luận, thông báo và đăng ký theo dõi.\n- [Agent](/agents) — cấu hình danh tính, Quyền truy cập và runtime.\n- [Đội](/squads) — để một leader điều phối nhiều agent."
        },
        {
          "slug": "chat",
          "title": "Chat với agent",
          "content": "Chat hợp với những việc chưa thành hình một issue rõ ràng: sắp xếp ý tưởng, kiểm tra tình hình không gian làm việc, bàn hướng làm, hay để agent làm xong một việc nhỏ trước đã.\n\n**Chat là cuộc trò chuyện một-một giữa bạn và một [agent](/agents)** — bước ra ngoài bảng [issue](/issues). Agent khởi đầu **không có ngữ cảnh issue** — nó không nhìn vào bảng của bạn và không tự biến cuộc trò chuyện thành issue — và toàn bộ cuộc trò chuyện **hoàn toàn riêng tư** (không ai khác trong [không gian làm việc](/workspaces), kể cả admin, thấy được). Chat hợp cho những lúc cần bàn hướng làm với agent, brainstorm, hay hỏi một câu không thuộc issue nào.\n\nCuộc trò chuyện **không gắn với issue nào**: agent không tự động nhận mô tả và bình luận của issue, nhưng vẫn có thể dùng CLI UniAI để truy vấn không gian làm việc, dự án, issue và skill trong phạm vi quyền của nó.\n\nCuộc trò chuyện cũng có thể mang theo bối cảnh. Bấm **+** ở góc dưới trái ô nhập và chọn **Project context** để gắn một [dự án](/projects) vào cuộc trò chuyện; khi agent chạy, nó nhận mô tả, repository và các tài nguyên khác của dự án đó.\n\n![Một cuộc chat với agent: câu hỏi được trả lời với kết quả sắp xếp thành bảng](/images/docs/chat-conversation.png)\n\n## Bắt đầu cuộc trò chuyện\n\n1. Mở **Chat** trên thanh bên.\n2. Bấm tạo mới và chọn một agent bạn có quyền chạy.\n3. Nhập tin nhắn; bạn cũng có thể đính kèm hình ảnh hoặc tệp.\n4. Sau khi gửi, UniAI tạo một lượt chạy cho tin nhắn, và câu trả lời quay về đúng cuộc trò chuyện hiện tại.\n\nNếu runtime đang offline, tin nhắn sẽ chờ đến khi nó trở lại. Một khi agent bị lưu trữ, hoặc bạn mất quyền chạy nó, bạn không gửi được tin nhắn mới nữa.\n\n## Chat hay issue?\n\n| Nhu cầu | Lựa chọn hợp hơn |\n|---|---|\n| Hỏi nhanh, thăm dò hướng làm, hoặc bản nháp riêng tư | **Chat** |\n| Cần người phụ trách, trạng thái, độ ưu tiên và sản phẩm bàn giao rõ ràng | **Issue** |\n| Muốn đồng đội thấy bối cảnh và tiếp tục cuộc thảo luận | **Issue** |\n| Xem nhanh thông tin không gian làm việc, không cần ghi lại thành công việc | **Chat** |\n\nChat trực tiếp tạo trong UniAI chỉ người tạo mới thấy; các thành viên khác và admin không đọc được. Kết luận mà cả nhóm cần cùng thấy nên được viết vào một issue, mô tả dự án, hoặc một skill.\n\n## Ngữ cảnh nhiều lượt\n\nCuộc trò chuyện cố tiếp nối phiên gốc trong công cụ lập trình AI, nên các tin nhắn sau không phải nhắc lại nội dung trước. UniAI lưu ID phiên và điều các lượt chạy kế tiếp đến một runtime truy cập được phiên đó.\n\nNếu phiên cục bộ không còn, ngữ cảnh không tiếp nối được, hoặc lỗi trước đó không nên dùng lại, runtime sẽ bắt đầu từ một phiên mới. Dù thế nào, lịch sử tin nhắn của cuộc trò chuyện vẫn được giữ.\n\n## Theo dõi và dừng lượt chạy\n\nTrong lúc agent chạy, giao diện hiển thị giai đoạn hiện tại và hoạt động công cụ. Mở rộng **View details** để xem toàn bộ lượt chạy.\n\nMuốn ngắt, bấm nút dừng cạnh ô nhập. Nội dung đã trả về cuộc trò chuyện được giữ lại; phần nhập chưa được xử lý sẽ được khôi phục vào trình soạn khi có thể.\n\n## Sắp xếp danh sách cuộc trò chuyện\n\nCuộc trò chuyện có thể được đổi tên, ghim, lưu trữ và xóa:\n\n- **Ghim** giữ các cuộc trò chuyện hay dùng ở đầu danh sách;\n- **Lưu trữ** chuyển cuộc trò chuyện vào mục Archived và biến nó thành chỉ đọc;\n- **Bỏ lưu trữ** cho phép gửi tin nhắn trở lại;\n- **Xóa** gỡ bỏ vĩnh viễn cuộc trò chuyện cùng các tin nhắn của nó.\n\nNếu cuộc trò chuyện đang gắn với một kênh bên ngoài (như Slack), lưu trữ sẽ cắt liên kết đó; bỏ lưu trữ không khôi phục lại — hãy gắn lại từ đầu.\n\n> Xóa cuộc trò chuyện là không thể hoàn tác và sẽ dừng mọi lượt chạy chưa xong trong đó. Nếu chỉ tạm thời không cần đến, hãy lưu trữ thay vì xóa.\n\n## Bước tiếp theo\n\n- [Giao issue cho agent](/assigning-issues) — biến cuộc thảo luận thành công việc theo dõi được.\n- [Hộp thư đến và theo dõi](/inbox) — bám sát hoạt động của nhóm cần đến bạn.\n- [Ma trận provider](/providers) — khả năng phiên và model của từng công cụ lập trình AI."
        },
        {
          "slug": "inbox",
          "title": "Hộp thư đến và theo dõi",
          "content": "Hộp thư đến gom các hoạt động công việc liên quan đến bạn. Nó không phải nhật ký hoạt động đầy đủ — nó chỉ ra chỗ có thay đổi cần bạn chú ý; ngữ cảnh đầy đủ vẫn nằm trên issue tương ứng.\n\n## Những gì đổ về hộp thư đến\n\nCác thông báo thường gặp gồm:\n\n- một issue được giao cho bạn, hoặc chuyển khỏi tay bạn;\n- issue bạn theo dõi có bình luận mới, hoặc đổi người phụ trách, trạng thái, độ ưu tiên hay ngày;\n- ai đó @nhắc bạn trong mô tả issue hoặc bình luận;\n- issue hoặc bình luận bạn tạo nhận được một lượt thả cảm xúc;\n- một lượt chạy agent thất bại trên issue bạn theo dõi;\n- việc tạo issue qua agent hoàn thành hoặc thất bại;\n- một Tự động hóa bạn theo dõi tạo issue mới, hoặc bị tạm dừng sau nhiều lần thất bại liên tiếp.\n\nHành động của chính bạn không tạo thông báo cho bạn. Nhiều thông báo trên cùng một issue được gộp thành một mục; mở nó để xem các thay đổi gần đây của issue.\n\n## Các nguồn tự động theo dõi\n\nNhững người tham gia sau được tự động theo dõi một issue:\n\n- người tạo issue;\n- người phụ trách mới;\n- bất kỳ ai bình luận;\n- bất kỳ ai vừa được @nhắc trong mô tả issue;\n- những người theo dõi được cấu hình sẵn trong một Tự động hóa.\n\nĐổi người phụ trách không gỡ các lượt theo dõi hiện có. Bạn theo dõi hoặc bỏ theo dõi bất cứ lúc nào trên trang issue; sau khi bỏ theo dõi, các cập nhật thông thường ngừng đến hộp thư của bạn, còn @nhắc trực tiếp vẫn thông báo riêng cho bạn.\n\n> Khi trạng thái của issue con thay đổi, người theo dõi issue cha cũng được báo. Bình luận, độ ưu tiên và thay đổi ngày của issue con không được báo lên issue cha.\n\n## Agent không dùng hộp thư đến\n\nHộp thư đến chỉ dành cho thành viên của không gian làm việc. Kể cả khi agent là người phụ trách hay người theo dõi, nó không bao giờ đọc thông báo hộp thư.\n\n@nhắc một agent trong bình luận, giao issue cho nó, hoặc kích hoạt một Tự động hóa sẽ tạo thẳng một lượt chạy thay vì để lại thông báo cho agent. `@all` chỉ thông báo cho thành viên.\n\n## Xử lý thông báo\n\nChọn một thông báo là đánh dấu đã đọc. Xong việc thì lưu trữ nó; lưu trữ không sửa hay xóa issue tương ứng. Khi issue đạt `in_review`, `done` hoặc `cancelled`, các thông báo lỗi lượt chạy của nó tự động được lưu trữ.\n\nChuột phải vào một thông báo để **Đánh dấu đã đọc** / **Đánh dấu chưa đọc** và **Lưu trữ** (**Bỏ lưu trữ** trong mục Archived). Đánh dấu chưa đọc là thao tác \"đã xem, nhưng chưa xử lý xong\": thông báo trở về trạng thái chưa đọc và tăng lại bộ đếm chưa đọc, và nó giữ nguyên chưa đọc kể cả khi bạn vẫn đang mở nó — lần mở sau sẽ đánh dấu đã đọc như bình thường. Thông báo đã lưu trữ giữ nguyên trạng thái đọc, nên khôi phục lại sẽ đưa nó về đúng như lúc bạn rời đi.\n\nTừ menu của hộp thư đến bạn cũng có thể:\n\n- đánh dấu tất cả đã đọc;\n- lưu trữ tất cả, hoặc lưu trữ tất cả thông báo đã đọc;\n- lưu trữ thông báo của các issue ở `done` hoặc `cancelled`;\n- mở mục Archived và khôi phục một thông báo.\n\n## Tinh chỉnh loại thông báo\n\nMở **Settings → Notifications** để bật tắt từng nhóm riêng:\n\n- giao việc;\n- thay đổi trạng thái;\n- bình luận;\n- lượt nhắc;\n- độ ưu tiên và ngày;\n- hoạt động của agent.\n\nTắt một nhóm không ngăn các thay đổi được ghi lại trên issue — nó chỉ dừng thông báo vào hộp thư. Một vài loại, như lượt thả cảm xúc, kết quả tạo nhanh và việc Tự động hóa bị tạm dừng, nằm ngoài sáu nhóm này và luôn được gửi. Thông báo hệ thống và trình duyệt chỉ điều khiển banner trên desktop, không thay đổi bản thân hộp thư đến.\n\n## Bước tiếp theo\n\n- [Bình luận](/comments) — trả lời, nhắc tên và các luồng thảo luận.\n- [@nhắc agent trong bình luận](/mentioning-agents) — thông báo cho thành viên khác gì lượt chạy của agent.\n- [Tự động hóa](/autopilots) — cấu hình tạo việc tự động và người theo dõi mặc định."
        }
      ]
    },
    {
      "label": "Runtime & execution",
      "labelKey": "runtime_execution",
      "pages": [
        {
          "slug": "tasks",
          "title": "Task",
          "content": "Mỗi khi một agent bắt đầu làm việc, UniAI tạo một **task**. Nó ghi lại điều gì kích hoạt lượt chạy, lượt chạy được giao cho agent nào, đã tiến đến đâu và cuối cùng có thành công hay không.\n\n## Issue và task\n\nIssue giữ mục tiêu, phần thảo luận, người phụ trách và trạng thái cuối cùng của một phần công việc; task ghi lại một lượt agent chạy trên phần công việc đó.\n\n| | Issue | Task |\n| --- | --- | --- |\n| Ghi lại điều gì | Một phần công việc đang tiến triển | Một lượt agent chạy |\n| Tồn tại bao lâu | Có thể được thảo luận, bổ sung và giao lại nhiều lần | Từ lúc kích hoạt đến khi hoàn thành, thất bại hoặc bị hủy |\n| Quan hệ số lượng | Một issue có thể trải qua nhiều lượt chạy | Mỗi lượt chạy có bản ghi riêng |\n\nVì thế cùng một issue có thể được lần lượt giao cho các agent khác nhau, hoặc chạy lại sau khi thất bại. Mỗi lượt chạy sinh một task mới; các bản ghi trước không bao giờ bị ghi đè.\n\n## Nguồn kích hoạt\n\nBất kỳ hành động nào sau đây đều có thể kích hoạt một lượt chạy:\n\n- Giao issue cho một agent hoặc một đội.\n- @nhắc agent trong bình luận.\n- Gửi tin nhắn cho agent trong chat.\n- Một Autopilot kích hoạt theo lịch hoặc từ sự kiện bên ngoài.\n\nMỗi điểm vào cung cấp ngữ cảnh khác nhau, nhưng cách thực thi giống hệt nhau: UniAI tạo task, một runtime nhận nó, rồi runtime gọi công cụ lập trình AI mà agent được cấu hình dùng.\n\n## Vòng đời thực thi\n\nMột task thường đi qua các trạng thái sau:\n\n| Trạng thái | Ý nghĩa |\n| --- | --- |\n| `deferred` | Được hẹn kích hoạt sau; vào hàng đợi đúng thời điểm hẹn |\n| `queued` | Chờ một runtime nhận |\n| `dispatched` | Một runtime đã nhận và đang khởi chạy công cụ lập trình AI |\n| `waiting_local_directory` | Thư mục cục bộ đích đang bị một lượt chạy khác giữ; chờ khóa thư mục được nhả |\n| `running` | Công cụ lập trình AI đang thực thi |\n| `completed` | Lượt chạy kết thúc bình thường |\n| `failed` | Lượt chạy gặp lỗi hoặc bị gián đoạn |\n| `cancelled` | Lượt chạy bị dừng thủ công |\n\nKhi runtime online, task mới thường bắt đầu nhanh; nếu runtime offline sau khi task đã vào hàng đợi, task nằm chờ ở hàng đợi cho tới khi runtime hồi phục. Task không được nhận trong hơn 2 giờ sẽ kết thúc ở trạng thái failed.\n\n> Nếu hệ thống đã biết runtime đích offline trước khi kích hoạt, một số hành động tức thời sẽ báo rằng hiện không thể bắt đầu lượt chạy, thay vì tạo một task không ai nhận được.\n\nRuntime có heartbeat khỏe mạnh có thể thực thi các task dài; server không bao giờ ép kết thúc một lượt chạy chỉ vì nó đã chạy lâu. Runtime tự quyết định tiến trình có bị treo hay không dựa trên hoạt động thực tế.\n\n## Xem lịch sử chạy\n\nMở một issue và vào **Nhật ký thực thi** để xem mọi task nó đã sinh ra. Mỗi dòng hiển thị nguồn kích hoạt, agent thực thi, trạng thái và thời gian.\n\nTừ đây bạn có thể:\n\n- Mở bản ghi lượt chạy để xem các tin nhắn của agent, các lần gọi công cụ và thông báo lỗi.\n- Dừng các task đang hẹn giờ, đang xếp hàng, đang khởi chạy, đang chờ thư mục cục bộ hoặc đang chạy.\n- Thử lại một task đã thất bại hoặc bị hủy.\n\n![Bản ghi lượt chạy: dòng thời gian trạng thái, tóm tắt của agent và từng lần gọi công cụ](/images/docs/task-transcript.png)\n\n> Đổi người phụ trách hoặc trạng thái của issue không dừng lượt chạy đã bắt đầu. Muốn ngắt, hãy dừng task tương ứng trong nhật ký thực thi. Task đang hoạt động chỉ bị hủy kèm theo issue khi issue bị xóa.\n\n## Lỗi và tự thử lại\n\nCác sự cố nhất thời — runtime offline chốc lát, daemon khởi động lại, timeout thực thi, hay công cụ lập trình AI bị gián đoạn mạng — có thể kích hoạt tự thử lại. Task thường chạy tối đa hai lần theo mặc định; gián đoạn mạng của công cụ được tối đa ba lần.\n\nLỗi do chính agent trả về thường không được tự thử lại. Thông tin đăng nhập hết hạn, cạn quota, cấu hình sai, hay mô hình không hoàn thành được yêu cầu đều cần sửa nguyên nhân trước, rồi thử lại thủ công.\n\nChế độ **chỉ chạy** của Autopilot không tự thử lại, để tránh chồng lên lần chạy kế theo lịch. Chế độ **tạo issue** sinh ra task issue thông thường, nên lỗi hạ tầng vẫn tự thử lại theo các quy tắc trên. Cả hai chế độ đều hiển thị kết quả cuối cùng trong lịch sử chạy của Autopilot.\n\nKhi issue không còn task nào khác đang hoạt động và không có lượt thử lại mới nào đang chờ chạy, một lần thất bại sẽ đưa issue đang `in_progress` lùi về `todo`.\n\n## Tra cứu lý do lỗi\n\nCác lý do lỗi hiển thị trong nhật ký thực thi và thống kê sử dụng chia làm hai nhóm: mã không có tiền tố do nền tảng ghi nhận; mã `agent_error.*` được phân loại từ lỗi của chính công cụ lập trình AI.\n\n**Phía nền tảng**\n\n| Lý do | Ý nghĩa | Cách xử lý |\n| --- | --- | --- |\n| `runtime_offline` | Runtime offline giữa lượt chạy | Khôi phục runtime rồi thử lại; xem [Daemon và runtime](/daemon-runtimes) |\n| `queued_expired` | Xếp hàng quá 2 giờ mà không runtime nào nhận | Xác nhận runtime online, rồi thử lại |\n| `runtime_recovery` | Daemon thu hồi lượt chạy bị gián đoạn sau khi khởi động lại | Thử lại trực tiếp |\n| `cancelled` | Bị dừng thủ công, hoặc bị hủy kèm khi lưu trữ hay xóa | Không cần làm gì |\n| `timeout` | Vượt giới hạn thời gian thực thi cấu hình trong daemon | Thu hẹp phạm vi issue, hoặc chỉnh `agent_timeout` của daemon |\n| `iteration_limit` | Chạm trần số vòng lặp của một lượt chạy | Thu hẹp phạm vi issue |\n| `agent_blocked` | Agent báo không thể tiếp tục | Cung cấp thứ mà bình luận của nó yêu cầu |\n| `api_invalid_request` | API nền tảng từ chối một yêu cầu không hợp lệ | Thử lại; báo lỗi nếu tái diễn |\n| `codex_semantic_inactivity` | Codex không tạo đầu ra có ý nghĩa quá lâu và bị coi là treo | Thử lại, hoặc chỉnh timeout không hoạt động của Codex |\n\n**Phía công cụ (`agent_error.*`, đã lược tiền tố)**\n\n| Lý do | Ý nghĩa | Cách xử lý |\n| --- | --- | --- |\n| `provider_auth_or_access` | Xác thực với nhà cung cấp mô hình thất bại hoặc bị từ chối truy cập (401/403) | Đăng nhập lại trong công cụ lập trình AI đó, hoặc kiểm tra API key |\n| `provider_quota_limit` | Cạn quota hoặc số dư (402) | Nạp thêm hoặc đổi tài khoản |\n| `provider_capacity_or_rate_limit` | Bị giới hạn tần suất hoặc hết dung lượng (429/529) | Thử lại sau |\n| `provider_server_error` | Lỗi máy chủ của nhà cung cấp mô hình (5xx) | Thử lại sau |\n| `provider_network` | Lỗi mạng khi kết nối tới nhà cung cấp mô hình | Tự thử lại; nếu kéo dài, kiểm tra mạng của máy thực thi |\n| `model_not_found_or_unavailable` | Mô hình không tồn tại hoặc hiện không khả dụng | Chọn mô hình khả dụng trong cài đặt agent |\n| `context_overflow` | Ngữ cảnh vượt cửa sổ của mô hình | Thu hẹp phạm vi issue hoặc giảm đầu vào |\n| `missing_config` | Thiếu cấu hình bắt buộc như API key | Điền biến môi trường của agent hoặc cấu hình công cụ |\n| `runtime_missing_executable` | Không tìm thấy file thực thi của công cụ lập trình AI | Cài lại công cụ; xem [Cài đặt công cụ lập trình AI](/install-agent-runtime) |\n| `runtime_version_unsupported` | Phiên bản công cụ lập trình AI quá cũ | Nâng cấp công cụ |\n| `process_failure` | Tiến trình của công cụ thoát bất thường | Xem bản ghi lượt chạy để tìm nguyên nhân, rồi thử lại |\n| `empty_or_unparseable_output` | Công cụ không tạo đầu ra, hoặc đầu ra không phân tích được | Thử lại; nếu tái diễn, kiểm tra cài đặt công cụ |\n| `agent_timeout` | Công cụ bị chấm dứt sau khi không phản hồi quá lâu | Thử lại hoặc thu hẹp phạm vi issue |\n| `unknown` | Lỗi chưa phân loại | Xem lỗi gốc trong bản ghi lượt chạy |\n\n## Thử lại thủ công\n\nBấm nút thử lại trên một dòng trong nhật ký thực thi sẽ gọi **agent đã chạy task đó vào thời điểm ấy**. Kể cả khi issue đã được giao lại sau đó, lượt thử lại không chuyển sang người phụ trách mới.\n\nLượt thử lại giữ lại các file mà lượt chạy trước đã ghi vào thư mục cục bộ khi có thể. Nếu phiên gốc vẫn an toàn và cùng runtime nhận lượt thử lại, nó cũng tiếp tục phiên trước. Những lỗi làm hỏng phiên, như tràn ngữ cảnh hoặc yêu cầu không hợp lệ, sẽ mở phiên mới trên chính thư mục làm việc gốc. Khi thư mục gốc không còn tồn tại, một thư mục làm việc mới được dùng.\n\nBạn cũng có thể chạy lại issue hiện tại từ CLI:\n\n```bash\nuniai issue rerun <issue-id>\n```\n\nDạng này không trỏ vào một task cụ thể trong quá khứ, nên nó dùng agent đang phụ trách issue và bắt đầu với phiên mới cùng thư mục làm việc mới.\n\n## Task hoàn thành và issue hoàn thành\n\n`completed` chỉ có nghĩa lượt chạy cụ thể này kết thúc bình thường — không xác nhận mục tiêu của issue đã đạt. Bạn vẫn có thể xem lại kết quả, tiếp tục thảo luận, bổ sung yêu cầu hoặc kích hoạt agent lần nữa.\n\nIssue đã xong hay chưa được đánh giá theo tiến độ thực tế của công việc và trạng thái của issue.\n\n## Bảng tra nhanh trạng thái và timeout\n\nCác con số dưới đây theo cấu hình mặc định của server, để đối chiếu nhanh khi chẩn đoán sự cố.\n\n| Trạng thái | Ý nghĩa | Timeout và hệ quả |\n| --- | --- | --- |\n| `deferred` | Được hẹn kích hoạt sau | Vào `queued` đúng thời điểm hẹn, sau đó áp dụng các quy tắc bên dưới |\n| `queued` | Chờ một runtime nhận | Thất bại sau **2 giờ** không được nhận; không tự thử lại |\n| `dispatched` | Đã được nhận; công cụ đang khởi chạy | Bị coi là thất bại khi ở trạng thái này quá **5 phút** |\n| `waiting_local_directory` | Chờ khóa thư mục cục bộ được nhả | Không có timeout riêng; quay lại quy trình khởi chạy khi thư mục được nhả |\n| `running` | Công cụ lập trình AI đang thực thi | Không có trần thời lượng cố định; sự sống theo heartbeat của runtime — mỗi 15 giây một nhịp; runtime mất heartbeat bị đánh dấu offline muộn nhất khoảng 3 phút, và task của nó thất bại theo |\n\nTự thử lại chỉ áp dụng cho các sự cố nhất thời dưới đây, và chỉ với task gắn với issue hoặc chat (không tính chế độ **chỉ chạy** của Autopilot):\n\n| Lý do lỗi tự thử lại được | Trần số lần chạy |\n| --- | --- |\n| Runtime offline | Mặc định 2 (lần chạy đầu + 1 lần thử lại) |\n| Bị thu hồi sau khi daemon khởi động lại | Mặc định 2 |\n| Timeout thực thi do nền tảng phán định | Mặc định 2 |\n| Codex treo, không có đầu ra có ý nghĩa | Mặc định 2 |\n| Tải gói skill thất bại | Mặc định 2 (lúc này tiến trình agent chưa khởi động; gói đã tải sẵn dùng từ cache cục bộ) |\n| Công cụ bị gián đoạn mạng | Tối đa 3; lần cuối bắt đầu sau khoảng 5 giây trễ |\n\nMọi lý do lỗi khác (xác thực, quota, cấu hình, mô hình, v.v.) không bao giờ tự thử lại; hãy sửa nguyên nhân trước, rồi thử lại thủ công.\n\n## Bước tiếp theo\n\n- [Daemon và runtime](/daemon-runtimes) — task thực thi trên máy nào.\n- [Giao issue cho agent](/assigning-issues) — kích hoạt lượt chạy từ một issue.\n- [@nhắc agent](/mentioning-agents) — bổ sung yêu cầu trong bình luận hoặc mời thêm agent khác."
        },
        {
          "slug": "daemon-runtimes",
          "title": "Daemon và runtime",
          "content": "UniAI ghi nhận và điều phối công việc; các máy tính được kết nối là nơi thực thi. **Daemon** trên một máy nhận task và gọi các công cụ lập trình AI cài trên chính máy đó.\n\n## Daemon vs runtime\n\n- **Daemon** là tiến trình nền của UniAI chạy trên một máy tính. Nó kết nối tới server, phát hiện công cụ cục bộ, nhận task và báo kết quả về.\n- **Runtime** đại diện cho một môi trường thực thi cụ thể mà một không gian làm việc dùng được. Nó ứng với một máy tính cộng một công cụ lập trình AI — hoặc một runtime profile tùy chỉnh — trên máy đó.\n\nVí dụ: một máy cài cả Claude Code lẫn Codex và được kết nối với hai không gian làm việc. Daemon đăng ký runtime Claude Code và Codex cho từng không gian làm việc. Khởi động lại daemon chỉ cập nhật các bản ghi hiện có; nó không tạo thêm runtime mới cho cùng một tổ hợp.\n\n## Vị trí thực thi và ranh giới dữ liệu\n\nCác công cụ lập trình AI mà runtime cục bộ gọi, thông tin đăng nhập riêng của các công cụ đó, và thư mục mã nguồn cục bộ của bạn đều ở lại trên máy được kết nối. UniAI server không thực thi lệnh thay cho công cụ cục bộ, và không tự động tải toàn bộ thư mục làm việc của bạn lên.\n\nĐể cả đội cộng tác được, server lưu issue, bình luận, cấu hình agent, ngữ cảnh task, bản ghi lượt chạy và kết quả mà agent ghi về. Nội dung đó có thể gồm đoạn mã hoặc ngữ cảnh dự án khác mà agent đã chọn đọc và đưa vào phần trả lời.\n\n> Biến môi trường tùy chỉnh của agent được lưu phía server và gửi xuống runtime lúc thực thi. Đừng hiểu \"thực thi cục bộ\" thành \"mọi bí mật chỉ tồn tại trên máy này\": biến môi trường tùy chỉnh và cấu hình MCP nằm trên server, và việc hiển thị chúng được giới hạn theo quy tắc giá trị nhạy cảm.\n\n## Khởi động daemon\n\n[Cài UniAI CLI](/cli) trước, rồi chạy:\n\n```bash\nuniai daemon start\n```\n\nDaemon mặc định chạy nền. Các lệnh thường dùng:\n\n| Lệnh | Mục đích |\n| --- | --- |\n| `uniai daemon status` | Xem trạng thái daemon và kết nối |\n| `uniai daemon logs -f` | Theo dõi log |\n| `uniai daemon restart` | Khởi động lại daemon và phát hiện lại công cụ cục bộ |\n| `uniai daemon stop` | Dừng daemon |\n| `uniai daemon start --foreground` | Chạy trong terminal hiện tại, để gỡ lỗi |\n\nKhi khởi động, daemon phát hiện các công cụ lập trình AI được hỗ trợ trên `PATH` và đăng ký runtime cho các không gian làm việc bạn được phép kết nối. Nếu vừa cài hoặc vừa đăng nhập một công cụ, hãy khởi động lại daemon để phát hiện lại.\n\nDaemon cần phát hiện được ít nhất một công cụ lập trình AI có sẵn được hỗ trợ thì mới khởi động. Cách cài và tên file thực thi xem tại [Cài đặt công cụ lập trình AI](/install-agent-runtime).\n\n## Điều phối và trạng thái online\n\nSau khi đăng ký, runtime giữ một kết nối bền vững. Khi task mới vào hàng đợi, server thông báo cho daemon phù hợp; daemon cũng poll định kỳ như lưới an toàn sau các lần gián đoạn kết nối. Vì thế khi runtime online và còn chỗ trống, task thường bắt đầu ngay lập tức.\n\nDaemon gửi heartbeat mỗi 15 giây. Server kết hợp heartbeat và trạng thái kết nối để quyết định runtime có online hay không; sau khi daemon thoát bất thường, runtime thường hiện offline muộn nhất khoảng 3 phút.\n\n![Chi tiết runtime của một máy đang online: cùng một daemon đăng ký 7 runtime, mỗi dòng ứng với một công cụ lập trình AI được phát hiện, kèm trạng thái online và phiên bản CLI](/images/docs/runtime-machine-detail.png)\n\nKhi runtime offline:\n\n- Task đã ở trong hàng đợi chờ runtime hồi phục, tối đa 2 giờ.\n- Task đang chạy thất bại; task issue hoặc chat đủ điều kiện có thể tự thử lại.\n- Khi daemon khởi động lại, nó đăng ký lại runtime và thu hồi các task lần trước chưa kết thúc gọn gàng.\n- Runtime offline quá 7 ngày và không gắn agent nào (kể cả agent đã lưu trữ) sẽ được tự động dọn dẹp.\n\nChi tiết trạng thái và quy tắc thử lại xem tại [Task](/tasks).\n\n## Giới hạn đồng thời\n\nMột daemon mặc định thực thi tối đa 20 task cùng lúc; mỗi agent tối đa 6. Mức đồng thời hiệu dụng là con số nhỏ hơn trong hai giới hạn.\n\nChạm trần thì task mới tiếp tục xếp hàng. Bạn có thể chỉnh mức đồng thời của từng agent trong cài đặt agent, và trần toàn máy qua `MULTICA_DAEMON_MAX_CONCURRENT_TASKS`. Các lượt chạy song song cạnh tranh cùng lúc về năng lực máy, quota tài khoản công cụ và cùng một thư mục làm việc.\n\n## Runtime riêng tư và công khai\n\nRuntime cục bộ mặc định là **riêng tư**: chủ runtime cùng các owner và admin của không gian làm việc có thể tạo agent trên nó.\n\nChủ runtime hoặc admin có thể chuyển nó thành **công khai**. Khi đó các thành viên khác trong không gian làm việc cũng chọn được runtime này; điều đó không chia sẻ thông tin đăng nhập của công cụ lập trình AI bên dưới — chỉ cho phép thành viên định tuyến task của agent họ về máy này.\n\n## Runtime profile tùy chỉnh\n\nNếu đội của bạn dùng một wrapper nội bộ, một file thực thi ghim phiên bản, hoặc cần đối số cố định thêm cho một công cụ tương thích, hãy tạo **runtime profile tùy chỉnh**.\n\nProfile tùy chỉnh không thêm giao thức giao tiếp mới. Bạn vẫn chọn một trong các họ giao thức UniAI đã hỗ trợ (loại giao thức tích hợp của công cụ; xem [So sánh công cụ lập trình AI](/providers)), và bản thân lệnh phải tương thích với họ đó.\n\n### Tạo profile\n\nChỉ owner và admin của không gian làm việc mới tạo, sửa hoặc xóa được runtime profile tùy chỉnh:\n\n1. Mở **Runtime** và vào một máy đã cài lệnh đó.\n2. Bấm **Thêm runtime tùy chỉnh**.\n3. Chọn họ giao thức mà lệnh thực sự tương thích.\n4. Điền tên, lệnh và các đối số cố định, rồi lưu.\n\nProfile được chia sẻ trong toàn không gian làm việc. Mỗi máy kết nối tự tra cứu lệnh trên máy mình; chỉ máy nào tìm được lệnh trên `PATH` mới đăng ký runtime tương ứng. Tạo profile không cài lệnh, cũng không đăng nhập công cụ thay các thành viên khác.\n\nTrường lệnh nhận file thực thi và đối số, không phải một shell script. Đối số thường, dấu nháy và escape bằng backslash dùng được; pipe, redirect, `&&`, `;`, backtick và mở rộng biến môi trường thì không. Khi cần các hành vi đó, hãy đưa chúng vào một script bọc ngoài và dùng script đó làm lệnh.\n\nNếu daemon không tìm thấy một lệnh mà terminal của bạn chạy được, hãy đặt đường dẫn tuyệt đối cho máy hiện tại:\n\n```bash\nuniai runtime profile set-path <profile-id> --path /absolute/path/to/command\n```\n\nGỡ ghi đè đường dẫn:\n\n```bash\nuniai runtime profile unset-path <profile-id>\n```\n\nSửa profile chỉ ảnh hưởng các task nhận sau đó. Trước khi xóa profile, hãy xử lý các agent đang hoạt động còn gắn với runtime của nó. Chỉ xóa thể hiện runtime trên một máy không xóa profile — daemon đang chạy sẽ đăng ký lại nó.\n\n## Chẩn đoán runtime offline\n\nKiểm tra theo thứ tự sau:\n\n1. Chạy `uniai daemon status` để xác nhận daemon đang chạy.\n2. Chạy `uniai daemon logs -f` để tìm lỗi đăng nhập, mạng hoặc phát hiện công cụ.\n3. Chạy `command -v <tool-command>` trong cùng môi trường để xác nhận daemon tìm thấy lệnh.\n4. Mở trang **Runtime** của UniAI và kiểm tra máy đích cùng công cụ lập trình AI tương ứng có hiện online không.\n5. Sau khi cài công cụ, đổi đường dẫn hoặc cập nhật profile, chạy `uniai daemon restart`.\n\nNếu vấn đề vẫn còn, xem `uniai daemon logs -f` để lấy đúng thông báo lỗi daemon báo ra.\n\n## Bước tiếp theo\n\n- [Cài đặt công cụ lập trình AI](/install-agent-runtime) — cài và kiểm chứng một công cụ lập trình AI được hỗ trợ.\n- [So sánh công cụ lập trình AI](/providers) — so sánh mô hình, khôi phục phiên, MCP và hỗ trợ skill.\n- [Task](/tasks) — xếp hàng, dừng và thử lại."
        },
        {
          "slug": "install-agent-runtime",
          "title": "Cài đặt công cụ lập trình AI",
          "content": "Một runtime cục bộ gồm hai phần: UniAI daemon và một công cụ lập trình AI mà daemon gọi được. Chỉ cần cài công cụ bạn định dùng — không cần đủ tất cả.\n\n## 1. Chọn công cụ\n\nCông cụ đã cài và đã đăng nhập thì không cần thiết lập gì thêm. Các công cụ khác nhau về model, MCP, khôi phục phiên và hỗ trợ skill; chi tiết xem [So sánh công cụ lập trình AI](/providers).\n\nUniAI hiện phát hiện các lệnh sau:\n\n| Công cụ | Lệnh được phát hiện | Hướng dẫn cài chính thức |\n| --- | --- | --- |\n| Antigravity CLI | `agy` | [Cài đặt và đăng nhập Antigravity](https://antigravity.google/docs/cli-install) |\n| Claude Code | `claude` | [Claude Code quickstart](https://code.claude.com/docs/en/quickstart) |\n| CodeBuddy Code | `codebuddy` | [CodeBuddy quickstart](https://www.codebuddy.ai/docs/cli/quickstart) |\n| Codex CLI | `codex` | [Codex CLI](https://developers.openai.com/codex/cli/) |\n| GitHub Copilot CLI | `copilot` | [Cài Copilot CLI](https://docs.github.com/en/copilot/how-tos/copilot-cli/set-up-copilot-cli/install-copilot-cli) |\n| Cursor Agent | `cursor-agent` | [Cài Cursor CLI](https://docs.cursor.com/en/cli/installation) |\n| DevEco Code | `deveco` | [DevEco Code](https://gitcode.com/openharmony-sig/deveco-code) |\n| Grok Build | `grok` | [Grok Build quickstart](https://docs.x.ai/build/overview) |\n| Hermes Agent | `hermes` | [Hermes quickstart](https://hermes-agent.nousresearch.com/docs/getting-started/quickstart/) |\n| Kimi CLI | `kimi` | Kimi CLI |\n| Kiro CLI | `kiro-cli` | [Cài Kiro CLI](https://kiro.dev/docs/cli/installation/) |\n| OpenClaw | `openclaw` | [Cài OpenClaw](https://docs.openclaw.ai/install) |\n| OpenCode | `opencode` | [OpenCode quickstart](https://opencode.ai/en/docs) |\n| Pi coding agent | `pi` | Pi coding agent |\n| Oh-My-Pi | `omp` | [Cài Oh-My-Pi](https://omp.sh) |\n| Qoder CLI | `qodercli` | [Qoder CLI quickstart](https://docs.qoder.com/en/cli/quick-start) |\n| Qoder CN CLI | `qoderclicn` | [Qoder CN CLI quickstart](https://help.aliyun.com/en/lingma/qodercli-cn/user-guide/qoder-cli-cn-get-started-quickly) |\n| Qwen Code | `qwen` | Qwen Code |\n| QwenPaw | `qwenpaw` | QwenPaw |\n| Reasonix | `reasonix` | Reasonix |\n| TRAE CLI | `traecli` | [Trang chính thức của TRAE](https://www.trae.cn/) |\n\n\"Được hỗ trợ\" nghĩa là UniAI gọi được CLI đó. Không có nghĩa UniAI cung cấp tài khoản, gói đăng ký hay quota model của công cụ cho bạn.\n\n## 2. Đăng nhập trên máy của bạn\n\nSau khi cài, tự chạy công cụ một lần trong terminal và hoàn tất đăng nhập hoặc thiết lập nhà cung cấp model. Nếu công cụ không hoàn thành được yêu cầu trong terminal, các lần gọi từ daemon cũng lỗi y như vậy.\n\nVới Reasonix, chạy `reasonix setup` và cấu hình provider cùng model mặc định trước khi khởi động daemon.\n\nVới QwenPaw, chọn provider và model trong cấu hình riêng của QwenPaw. UniAI không ghi đè được model của QwenPaw lúc chạy.\n\nCác thông tin đăng nhập đó do chính công cụ lưu cục bộ. UniAI không bao giờ nhận token đăng nhập từ Claude, Codex, Cursor hay bất kỳ CLI nào khác.\n\n> Claude Code yêu cầu từ 2.0.0 trở lên, Codex từ 0.100.0, Copilot từ 1.0.0, Grok từ 0.2.89 và Qwen Code từ 0.20.0. Dưới phiên bản tối thiểu, daemon không đăng ký runtime tương ứng.\n\n## 3. Xác nhận daemon tìm thấy lệnh\n\nTrên macOS, Linux hoặc WSL, chạy:\n\n```bash\ncommand -v <command>\n<command> --version\n```\n\nTrong Windows PowerShell, chạy:\n\n```powershell\nGet-Command <command>\n<command> --version\n```\n\nThay `<command>` bằng một lệnh trong bảng, ví dụ `claude`, `codex` hoặc `cursor-agent`. Lệnh đầu tiên in ra đường dẫn file thực thi.\n\nNếu terminal tìm thấy công cụ nhưng daemon chạy nền thì không, thường là hai bên đang dùng giá trị `PATH` khác nhau. Khởi động lại daemon, hoặc đặt đường dẫn tuyệt đối qua biến môi trường `MULTICA_<PROVIDER>_PATH` tương ứng.\n\n## 4. Quét lại và xác nhận runtime online\n\nKhởi động daemon bằng CLI:\n\n```bash\nuniai daemon start\n```\n\nNếu daemon đang chạy sẵn, khởi động lại nó sau khi cài công cụ mới:\n\n```bash\nuniai daemon restart\n```\n\nSau đó mở trang **Runtime** của UniAI. Công cụ hiện dưới máy tính tương ứng và ở trạng thái online. Giờ bạn có thể chọn runtime này khi tạo hoặc chỉnh sửa agent.\n\n> Daemon phải phát hiện được ít nhất một CLI được hỗ trợ sẵn thì mới khởi động. Các profile runtime tùy chỉnh chỉ đồng bộ sau khi daemon đã chạy, nên trên một máy trắng chúng không thể là điều kiện khởi động duy nhất.\n\n## Không phát hiện được công cụ nào\n\nKiểm tra theo thứ tự:\n\n1. Tự chạy công cụ trong terminal để xác nhận đã cài và đã đăng nhập xong.\n2. Kiểm tra tên lệnh và đường dẫn thực tế bằng `command -v` hoặc `Get-Command`.\n3. Chạy `uniai daemon logs -f` và tìm lỗi về phiên bản, đường dẫn hoặc xác thực.\n4. Khởi động lại daemon để nó đọc lại môi trường hiện tại.\n5. Quay lại trang **Runtime** và xác nhận máy tính lẫn công cụ đều online.\n\nNếu công cụ hiện online nhưng các lần chạy vẫn lỗi, mở bản ghi chạy của issue để xem chính xác lỗi mà CLI trả về.\n\n## Bước tiếp theo\n\n- [Tạo và cấu hình agent](/agents-create) — chọn runtime bạn vừa kết nối.\n- [Daemon và runtime](/daemon-runtimes) — trạng thái online, mức song song và runtime tùy chỉnh.\n- [So sánh công cụ lập trình AI](/providers) — so sánh mỗi công cụ làm được gì trong UniAI."
        },
        {
          "slug": "providers",
          "title": "So sánh công cụ lập trình AI",
          "content": "UniAI không thay thế công cụ lập trình AI bạn đang dùng. Runtime gọi công cụ đã cài và đã đăng nhập sẵn trên máy tính, rồi ghi tiến trình và kết quả ngược lại vào issue.\n\nNếu bạn đã dùng sẵn một trong các công cụ này, thường bạn cứ giữ nguyên. Bốn điều cần cân nhắc khi chọn:\n\n- công cụ đã được cài trên máy tính chưa;\n- tài khoản của bạn dùng được những model nào;\n- UniAI có cần cấu hình MCP cho nó không;\n- đội muốn tái sử dụng những skill nào.\n\nCác bước cài đặt nằm ở [Cài đặt công cụ lập trình AI](/install-agent-runtime).\n\n## Công cụ được hỗ trợ\n\n| Công cụ | Lệnh được phát hiện | Khôi phục phiên | MCP do UniAI quản lý | Vị trí chèn skill |\n|---|---|---:|---:|---|\n| Antigravity | `agy` | ✓ | — | `.agents/skills/` |\n| Claude Code | `claude` | ✓ | ✓ | `.claude/skills/` |\n| CodeBuddy | `codebuddy` | ✓ | ✓ | `.codebuddy/skills/` |\n| Codex | `codex` | ✓ | ✓ | `$CODEX_HOME/skills/` |\n| GitHub Copilot CLI | `copilot` | ✓ | — | `.github/skills/` |\n| Cursor Agent | `cursor-agent` | ✓ | ✓ | `.cursor/skills/` |\n| DevEco Code | `deveco` | ✓ | — | `.deveco/skills/` |\n| Grok | `grok` | ✓ | ✓ | `.grok/skills/` |\n| Hermes | `hermes` | ✓ | ✓ | `HERMES_HOME/skills/` theo từng task |\n| Kimi CLI | `kimi` | ✓ | ✓ | `.kimi/skills/` |\n| Kiro CLI | `kiro-cli` | ✓ | ✓ | `.kiro/skills/` |\n| OpenClaw | `openclaw` | ✓ | ✓ | `skills/` |\n| OpenCode | `opencode` | ✓ | ✓ | `.opencode/skills/` |\n| Pi | `pi` | ✓ | — | `.pi/skills/` |\n| Oh-My-Pi | `omp` | ✓ | — | `.omp/skills/` |\n| Qoder CLI | `qodercli` | ✓ | ✓ | `.qoder/skills/` |\n| Qoder CN CLI | `qoderclicn` | ✓ | ✓ | `.qoder/skills/` |\n| Qwen Code | `qwen` | ✓ | ✓ | `.qwen/skills/` |\n| QwenPaw | `qwenpaw` | ✓ | ✓ | `skills/` trong workspace theo từng task |\n| Reasonix | `reasonix` | ✓ | ✓ | `.reasonix/skills/` |\n| Trae CLI | `traecli` | ✓ | ✓ | `.traecli/skills/` |\n\n\"MCP do UniAI quản lý\" nghĩa là bạn có thể khai báo MCP server trong cấu hình agent và UniAI truyền chúng cho công cụ trước mỗi lần chạy. Không có nghĩa công cụ đó không còn cách nào khác để cấu hình MCP.\n\n## Nguồn model\n\nKhi bạn tạo agent, danh sách model lấy từ runtime tương ứng. Một số công cụ đưa ra một bộ tên model cố định; số khác trả về các model khả dụng dựa trên cấu hình cục bộ, tài khoản đã đăng nhập và quyền lợi gói đăng ký.\n\nNếu bạn không chọn model, UniAI dùng mặc định của chính công cụ. Nếu danh sách trống, xác nhận runtime đang online và công cụ đã đăng nhập, rồi làm mới danh sách model.\n\nQwenPaw là ngoại lệ duy nhất: nó không nhận model chọn từ UniAI, nên bộ chọn bị vô hiệu và hiển thị \"Managed by runtime\". Hãy chọn model trong cấu hình riêng của QwenPaw. Danh sách model trống ở đây là bình thường, không phải dấu hiệu runtime offline.\n\n> Các công cụ khác nhau có thể dùng tên model khác nhau. Sau khi đổi công cụ lập trình AI, giá trị model cũ không còn hợp lệ với công cụ mới và cần chọn lại.\n\n## Khôi phục phiên\n\nTất cả các công cụ này đều hỗ trợ tiếp tục phiên hiện có ở lần chạy sau. Điều kiện là phiên gốc vẫn còn và task được thực thi bởi một runtime với tới được nó.\n\nUniAI lưu định danh phiên mà công cụ trả về. Lần tiếp theo công việc tiếp tục trên cùng issue, runtime thử khôi phục phiên đó; khi không được, nó tạo phiên mới và chạy tiếp. Pi lưu đường dẫn file phiên cục bộ, nên phụ thuộc nhiều hơn vào máy tính và file gốc.\n\nQuy tắc thử lại và khôi phục phiên nằm ở [Task](/tasks#manual-retry).\n\n## Cấu hình MCP\n\nAntigravity, GitHub Copilot CLI, DevEco Code và Pi hiện chưa đọc cấu hình MCP trên agent, nên trường này không hiển thị khi tạo agent cho chúng.\n\n> Lệnh, tham số và biến môi trường của MCP server được lưu phía server cùng cấu hình agent. Đừng ghi vào cấu hình những thông tin xác thực cục bộ mà bạn không cần chia sẻ.\n\n## Chèn skill\n\nTrước khi một lần chạy bắt đầu, UniAI ghi các skill gắn với agent vào các đường dẫn trong bảng trên, để mỗi công cụ tự nhận diện chúng qua cơ chế native của mình.\n\nSkill đã có sẵn trong repository không bao giờ bị ghi đè. Khi trùng tên, UniAI dùng tên thư mục khác cho bản được chèn; sau lần chạy, nó chỉ dọn những file do chính nó tạo ra.\n\nCodex, Hermes và OpenClaw được xử lý hơi khác:\n\n- Codex dùng `CODEX_HOME` theo từng task, nên skill không bao giờ bị ghi vào thư mục Codex cấp hệ thống.\n- Hermes dùng `HERMES_HOME` theo từng task. Cấu hình cục bộ sẵn có vẫn đọc được, nhưng skill đã gắn và dữ liệu phiên được cô lập theo task. Cơ chế cô lập này chỉ kích hoạt khi agent có skill gắn kèm; task không có skill dùng Hermes home gốc của máy.\n- OpenClaw dùng `skills/` dưới thư mục làm việc của task hiện tại, và cấu hình mỗi lần chạy đặt thư mục đó làm workspace.\n\nVề tạo và gắn skill, xem [Skills](/skills).\n\n## Bước tiếp theo\n\n- [Tạo và cấu hình agent](/agents-create) — thiết lập model và mức thinking.\n- [Cài đặt công cụ lập trình AI](/install-agent-runtime) — các bước cài đặt và phát hiện.\n- [Daemon và runtime](/daemon-runtimes) — đăng ký runtime và mức song song."
        }
      ]
    },
    {
      "label": "Automation",
      "labelKey": "automation",
      "pages": [
        {
          "slug": "autopilots",
          "title": "Tự động hóa",
          "content": "Tự động hóa (autopilot) chạy những việc lặp đi lặp lại — bản tóm tắt tiến độ hằng ngày, một lượt kiểm tra dependency định kỳ, hay một agent được khởi động bởi sự kiện từ hệ thống bên ngoài.\n\nMỗi autopilot lưu một Runbook, một người phụ trách và một hoặc nhiều trigger. Khi được kích hoạt, UniAI tạo issue hoặc chạy agent trực tiếp, và ghi lại mọi lượt chạy.\n\n## Tạo autopilot\n\nMở **Autopilot** trong thanh bên, chọn một mẫu hoặc bắt đầu từ đầu, rồi cấu hình:\n\n- **Tên**: autopilot này chịu trách nhiệm việc gì;\n- **Runbook**: mục tiêu, bối cảnh, ràng buộc và các bước mà agent đọc ở mỗi lượt chạy;\n- **Người phụ trách**: một agent hoặc một đội;\n- **Dự án**: tùy chọn; đưa các issue tạo tự động vào một dự án cụ thể;\n- **Chế độ thực thi**: tạo issue, hoặc chỉ chạy;\n- **Người theo dõi**: các thành viên được thông báo sau khi issue được tạo tự động;\n- **Trigger**: một lịch chạy hoặc một webhook.\n\nAutopilot được bật mặc định sau khi lưu; **Run now** chạy tay trọn luồng một lần vào bất cứ lúc nào.\n\n## Chọn chế độ thực thi\n\n| Chế độ | Hành vi | Hợp với |\n|---|---|---|\n| **Tạo issue** | Mỗi lượt kích hoạt trước tiên tạo một issue, rồi giao cho người phụ trách; thảo luận, trạng thái và bản ghi chạy đều nằm trên issue. | Việc mà cả đội cần xem lại, xác nhận hoặc theo sát. |\n| **Chỉ chạy** | Tạo task trực tiếp, không có issue; kết quả chỉ thấy được trong lịch sử chạy của autopilot. | Các lượt chạy nền không cần dấu vết cộng tác. |\n\nChế độ **tạo issue** dùng chung hàng đợi task với issue thường: khi runtime offline, issue vẫn được tạo và task chờ tới khi runtime online.\n\nChế độ **chỉ chạy** yêu cầu runtime sẵn sàng đúng lúc kích hoạt; nếu không, lượt chạy hiển thị là skipped và không để lại issue nào chờ.\n\n## Chạy theo lịch\n\nTrình soạn lịch cho bạn chọn giờ chạy, các ngày lặp lại, khung giờ và múi giờ, đồng thời xem trước các lần chạy sắp tới. Một autopilot có thể có nhiều lịch; bật hoặc tắt từng trigger riêng lẻ được thực hiện bằng lệnh `autopilot trigger-update` của CLI — xem [Tham chiếu lệnh CLI](/cli) để biết tham số.\n\nKhi cần quy tắc phức tạp hơn, sửa trực tiếp cron 5 trường chuẩn:\n\n```text\nminute hour day month weekday\n```\n\nVí dụ:\n\n| Cron | Múi giờ | Ý nghĩa |\n|---|---|---|\n| `0 9 * * 1-5` | `Asia/Ho_Chi_Minh` | 9:00 các ngày trong tuần |\n| `*/30 * * * *` | `UTC` | Mỗi 30 phút |\n| `0 3 * * *` | `UTC` | Mỗi ngày lúc 3:00 |\n\nCron không có trường giây, và múi giờ dùng tên IANA như `Asia/Ho_Chi_Minh`. Trước khi lưu, đối chiếu kết quả với các mốc \"next runs\" hiển thị trên trang.\n\n![Trình soạn autopilot: Runbook, thiết lập lịch và bản xem trước vài lần chạy kế tiếp](/images/docs/autopilot-schedule.png)\n\n## Chạy từ webhook\n\nSau khi thêm trigger webhook, UniAI sinh một URL duy nhất. Gửi vào đó một object hoặc array JSON để kích hoạt autopilot:\n\n```bash\ncurl -X POST \"$UNIAI_WEBHOOK_URL\" \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Idempotency-Key: demo-001\" \\\n  -d '{\"event\":\"build.completed\",\"eventPayload\":{\"status\":\"success\"}}'\n```\n\nPayload được lưu trên bản ghi nhận webhook (delivery) và bản ghi chạy, rồi trao cho agent. Ở chế độ tạo issue, nó còn được nối vào mô tả issue.\n\nRàng buộc với request webhook:\n\n- body phải là object hoặc array JSON hợp lệ, tối đa 256 KiB;\n- `Idempotency-Key` ngăn chạy trùng khi bên gửi retry; delivery từ GitHub còn được khử trùng lặp theo `X-GitHub-Delivery`;\n- không có idempotency key ổn định thì UniAI không đảm bảo được request lặp lại chỉ chạy đúng một lần;\n- khi trigger đang tắt hoặc sự kiện không khớp, delivery được ghi là ignored và không tạo lượt chạy nào.\n\n### Lọc sự kiện\n\nKhi một nguồn gửi nhiều loại sự kiện, thêm bộ lọc sự kiện vào trigger. Mỗi dòng gồm một tên sự kiện cùng danh sách action tùy chọn; lượt chạy được kích hoạt nếu bất kỳ dòng nào khớp, và để trống toàn bộ các dòng thì nhận mọi sự kiện.\n\nVí dụ, với tên sự kiện là `workflow_run` và action là `completed, failed`, chỉ hai loại kết quả `workflow_run` đó được chấp nhận. UniAI nhận diện event và action từ các header request và trường payload phổ biến, bao gồm `X-GitHub-Event` của GitHub và trường `action` trong body.\n\n### Bảo vệ URL webhook\n\n> Token trong URL webhook chính là thông tin xác thực để gọi. Đừng đưa URL đầy đủ vào repo công khai, issue hay ảnh chụp màn hình. Nếu URL bị lộ, bấm nút **Rotate URL** ngay cạnh nó và cập nhật bên gửi lập tức; URL cũ ngừng hoạt động ngay.\n\nChỉ người tạo autopilot, chủ sở hữu/quản trị viên không gian làm việc và các cộng tác viên được cấp quyền mới xem được URL đầy đủ. UI mặc định ẩn token trong URL, và sao chép không cần hiển thị nó; bấm vào URL hoặc biểu tượng con mắt để xem địa chỉ đầy đủ.\n\n### Tra cứu phản hồi webhook\n\nKhi debug bên gửi, dùng bảng này để đọc phản hồi của UniAI:\n\n| Mã HTTP | Trạng thái phản hồi | Ý nghĩa |\n|---|---|---|\n| 200 | `accepted` | Đã nhận và tạo lượt chạy; trả về ID của delivery và lượt chạy. |\n| 200 | `skipped` | Đã nhận nhưng lượt chạy này bị bỏ qua (ví dụ runtime offline ở chế độ chỉ chạy); kèm lý do. |\n| 200 | `ignored` | Không tạo lượt chạy: trigger đang tắt, autopilot đang tạm dừng hoặc đã lưu trữ, hoặc sự kiện bị lọc; trường `reason` giải thích vì sao. |\n| 200 | `duplicate` | Idempotency key trùng với một delivery đã có; trả về ID delivery gốc và không chạy lại. |\n| 400 | Thông báo lỗi | Body rỗng, không phải JSON hợp lệ, hoặc không phải object/array JSON. |\n| 401 | `rejected` | Trigger có cấu hình signing secret nhưng request thiếu chữ ký hoặc chữ ký không khớp. |\n| 404 | Thông báo lỗi | Token trong URL không hợp lệ hoặc đã bị thay (rotate). |\n| 413 | Thông báo lỗi | Body vượt 256 KiB. |\n| 429 | Thông báo lỗi | Quá nhiều request; thử lại sau theo header phản hồi `Retry-After`. |\n| 500 | Thông báo lỗi | Lỗi nội bộ của UniAI; bên gửi có thể thử lại sau. |\n\nCác lượt bỏ qua ở mức nghiệp vụ — tạm dừng, lưu trữ và lọc sự kiện — trả 200 thay vì 4xx, để bên gửi không retry mãi.\n\nThứ tự suy ra event và action:\n\n1. nếu body có trường `event` dạng chuỗi, dùng trực tiếp giá trị đó;\n2. nếu không, dùng header request `X-GitHub-Event`, ghép với `action` trong body thành `github.<event>.<action>`;\n3. rồi tới header request `X-Gitlab-Event`;\n4. rồi tới header request `X-Event-Type`;\n5. rồi tới các trường `event`, `type` và `action` trong body;\n6. khi tất cả đều thiếu, event được ghi là `webhook.received`.\n\n## Xem lượt chạy và delivery\n\nLịch sử chạy hiển thị nguồn kích hoạt, thời gian, trạng thái, issue hoặc task liên kết, và lý do khi thất bại hay bị bỏ qua. Trigger webhook còn giữ riêng bản ghi delivery, gồm sự kiện đã phân tích, phản hồi, thông tin khử trùng lặp và lý do thất bại.\n\nMột delivery webhook đã xử lý xong có thể được phát lại (replay) từ trang chi tiết của nó. Phát lại tạo delivery và lượt chạy mới mà không ghi đè bản ghi gốc; delivery không qua được xác minh chữ ký hoặc còn đang xếp hàng thì không phát lại được, và các lượt phát lại không tham gia khử trùng lặp.\n\n## Thất bại, tạm dừng và xóa\n\nTask thất bại ở chế độ chỉ chạy không được tự thử lại; lịch kế tiếp vẫn chạy như dự kiến. Chế độ tạo issue sinh task issue thông thường, và lỗi hạ tầng tuân theo quy tắc trong [Task](/tasks#failures-and-automatic-retries).\n\nUniAI định kỳ kiểm tra xem các lượt chạy gần đây có thất bại liên tục không: khi 7 ngày qua có ít nhất 50 lượt chạy hoàn thành hoặc thất bại với tỷ lệ thất bại 90%, hệ thống tạm dừng autopilot và thông báo cho người tạo; sửa xong nguyên nhân thì bật lại thủ công.\n\nTạm dừng thủ công sẽ dừng lịch chạy, webhook và cả **Run now**. Xóa thực chất là lưu trữ: các lượt kích hoạt tương lai dừng lại, còn lịch sử chạy và delivery được giữ nguyên.\n\n## Quyền hạn\n\n- Mọi thành viên không gian làm việc đều tạo được autopilot;\n- người tạo và chủ sở hữu/quản trị viên không gian làm việc có thể sửa, chạy, xóa và quản lý trigger;\n- người tạo và chủ sở hữu/quản trị viên có thể thêm cộng tác viên vào quyền quản lý;\n- cộng tác viên có thể sửa, chạy và quản lý trigger, nhưng không cấp quyền cho người khác được;\n- quản lý được autopilot không đồng nghĩa với chạy được agent của nó — Quyền truy cập (Access) của agent vẫn áp dụng.\n\n## Dùng CLI\n\n```bash\nuniai autopilot trigger <autopilot-id>\nuniai autopilot runs <autopilot-id>\nuniai autopilot trigger-rotate-url <autopilot-id> <trigger-id>\n```\n\nXem [Tham chiếu lệnh CLI](/cli) để biết đầy đủ tham số.\n\n## Bước tiếp theo\n\n- [Giao issue cho agent](/assigning-issues) — giao việc và cửa sổ xác nhận.\n- [Hộp thư đến và theo dõi](/inbox) — nơi thông báo lượt chạy đổ về.\n- [Task](/tasks) — trạng thái lượt chạy và cách xử lý thất bại."
        }
      ]
    },
    {
      "label": "Integrations",
      "labelKey": "integrations",
      "pages": [
        {
          "slug": "channels",
          "title": "Tích hợp chat",
          "content": "Tích hợp chat cho phép nhóm hỏi agent, @nhắc nó trong chat nhóm, hoặc tạo issue ngay từ cửa sổ chat — mà không phải mở UniAI.\n\nHiện hỗ trợ Feishu/Lark, Slack, DingTalk và WeCom. Chúng dùng chung cơ chế phiên, danh tính và thực thi, nhưng cách cài đặt khác nhau.\n\n## Chọn nền tảng\n\n| | Feishu / Lark | Slack | DingTalk | WeCom |\n|---|---|---|---|---|\n| Cài đặt | Tạo mã QR trong UniAI và quét bằng Feishu để cấp quyền | Tạo một app trong Slack, rồi dán hai token vào UniAI | Tạo internal app và robot Stream mode, rồi dán AppKey và AppSecret của nó vào UniAI | Tạo smart bot có bật kết nối dài trong trang quản trị WeCom, rồi dán Bot ID và Secret của nó vào UniAI |\n| DM cho agent | Hỗ trợ | Hỗ trợ | Hỗ trợ | Hỗ trợ |\n| Nhóm hoặc kênh | Kích hoạt bằng cách @nhắc Bot | Kích hoạt bằng cách @nhắc Bot | Kích hoạt bằng cách @nhắc Bot | Kích hoạt bằng cách @nhắc Bot |\n| Tạo issue | Lệnh tin nhắn `/issue`; tạo issue đúng theo nội dung bạn nhập | Slash command `/issue`; agent viết lại mô tả trước khi tạo | Lệnh tin nhắn `/issue`; tạo issue đúng theo nội dung bạn nhập | Lệnh tin nhắn `/issue`; tạo issue đúng theo nội dung bạn nhập |\n| Kết nối | Kết nối dài của nền tảng | Socket Mode | Stream mode | Kết nối dài của nền tảng |\n\nHiện chỉ mở kết nối mới cho Feishu Trung Quốc đại lục; các kết nối Lark quốc tế hiện có vẫn hoạt động và vẫn quản lý được.\n\nMỗi Bot gắn với một agent UniAI. Muốn dùng nhiều agent trên cùng một nền tảng chat, kết nối một Bot riêng cho từng agent.\n\nDingTalk và WeCom do cộng đồng duy trì: chúng có mặt trong mọi bản phát hành nhưng không kèm SLA hỗ trợ chính thức. Báo lỗi tại GitHub issues.\n\nWeCom hiện chỉ xử lý tin nhắn văn bản. Tin nhắn thoại, hình ảnh và tệp nhận một câu trả lời ngắn giải thích điều đó, và không được chuyển cho agent.\n\nHướng dẫn từng bước:\n\n- [Tích hợp Bot Feishu / Lark](/lark-bot-integration)\n- [Tích hợp Slack Bot](/slack-bot-integration)\n- [Tích hợp Bot DingTalk](/dingtalk-bot-integration)\n\n## Một tin nhắn được xử lý thế nào\n\n1. UniAI phân giải Bot về không gian làm việc và agent của nó.\n2. Trong nhóm hoặc kênh, chỉ tin nhắn @nhắc Bot rõ ràng mới đi tiếp; DM không cần nhắc.\n3. UniAI kiểm tra liên kết tài khoản và tư cách thành viên của người gửi.\n4. Tin nhắn được nối vào một phiên [chat với agent](/chat), và một task được tạo.\n5. Câu trả lời của agent quay về đúng DM hoặc thread ban đầu.\n\nTin nhắn trong kênh không @nhắc Bot sẽ không bao giờ kích hoạt agent và không được thêm vào ngữ cảnh trò chuyện của nó.\n\nTin nhắn thường đi theo luồng này. `/issue` là một lệnh, không phải một lượt chat: UniAI gửi kết quả về đúng nền tảng gốc nhưng không thêm lệnh vào UniAI Chat. Slash command gốc của Slack tiếp tục qua luồng tạo issue bất đồng bộ riêng của nó.\n\n## Cách ly phiên\n\n- Feishu/Lark tách phiên theo cuộc chat; các tin nhắn sau trong cùng cuộc chat tiếp tục cùng một phiên.\n- Slack tách DM theo kênh; trong kênh, mỗi thread giữ một phiên riêng.\n- DingTalk tách phiên theo cuộc hội thoại; mỗi DM hoặc nhóm tiếp tục phiên của riêng nó.\n- WeCom tách phiên theo cuộc chat; mỗi DM hoặc chat nhóm tiếp tục phiên của riêng nó.\n\nHỏi tiếp trong kênh vẫn cần @nhắc lại. Agent chỉ nhận các tin nhắn gửi đích danh cho nó — nó không bao giờ tự đọc toàn bộ lịch sử kênh.\n\n## Liên kết tài khoản\n\nLần đầu một thành viên nhắn Bot, họ nhận một đường dẫn liên kết tài khoản. Sau khi đăng nhập UniAI, tài khoản trên nền tảng được nối với tư cách thành viên của họ trong không gian làm việc hiện tại.\n\nUniAI chỉ chạy agent sau khi liên kết hoàn tất. Mỗi tin nhắn đều kiểm tra lại liên kết tài khoản và tư cách thành viên; sau khi rời một không gian làm việc, không còn dùng Bot để truy cập nó được nữa.\n\n> Liên kết tài khoản chỉ xác nhận người gửi là ai. Các thành viên khác trên nền tảng chat không tự động được thêm vào không gian làm việc UniAI.\n\n## Quản lý kết nối\n\nOwner và admin của không gian làm việc có thể kết nối hoặc ngắt kết nối Bot; với Bot Feishu/Lark, owner của agent cũng kết nối và ngắt kết nối được. Thành viên thường có thể xem các tích hợp đã kết nối và dùng những agent họ có quyền truy cập.\n\nSau khi ngắt kết nối, Bot ngừng nhận tin nhắn mới. Các cuộc trò chuyện và bản ghi chạy hiện có trong UniAI được giữ lại.\n\n## Bước tiếp theo\n\n- [Tích hợp Bot Feishu / Lark](/lark-bot-integration) — kết nối một agent bằng cách quét mã QR.\n- [Tích hợp Slack Bot](/slack-bot-integration) — tạo một app Slack và dán token của nó vào UniAI.\n- [Tích hợp Bot DingTalk](/dingtalk-bot-integration) — tạo robot Stream mode và dán thông tin xác thực của nó vào UniAI.\n- [Chat](/chat) — mô hình trò chuyện và thực thi đứng sau mọi Bot."
        },
        {
          "slug": "lark-bot-integration",
          "title": "Feishu Bot",
          "content": "Kết nối một agent UniAI làm Bot Feishu (飞书) và thành viên trong nhóm có thể DM cho nó trong Feishu, @nhắc nó trong nhóm, hoặc dùng `/issue` để tạo issue. Các cuộc trò chuyện và bản ghi chạy vẫn được lưu về không gian làm việc UniAI.\n\nMỗi Bot ánh xạ tới đúng một agent. Muốn dùng nhiều agent trong Feishu, kết nối một Bot cho từng agent.\n\n## Kết nối một Bot\n\nOwner của agent cùng owner và admin của không gian làm việc có thể bắt đầu kết nối:\n\n1. Mở **Agents** và chọn agent cần nối với Feishu.\n2. Vào **Integrations** và bấm **Bind to Feishu**.\n3. Quét mã QR bằng Feishu và xác nhận cấp quyền.\n4. Quay lại UniAI. Khi trang hiện **Connected to Feishu**, Bot đã sẵn sàng.\n\nMã QR là thông tin xác thực dùng một lần; nếu hết hạn, tạo mã mới. Agent đã kết nối không còn hiện chỗ quét mã nữa; muốn đổi tên Bot hoặc chỉnh quyền của nó, bấm **Manage in Feishu**.\n\n> Hiện chỉ mở kết nối mới cho Feishu Trung Quốc đại lục. Các kết nối Lark quốc tế hiện có vẫn hoạt động và vẫn quản lý được.\n\n## Lần dùng đầu tiên\n\nLần đầu một thành viên nhắn Bot, họ nhận một đường dẫn liên kết tài khoản:\n\n1. Mở đường dẫn và đăng nhập UniAI.\n2. Xác nhận tài khoản không gian làm việc cần liên kết.\n3. Quay lại Feishu và gửi lại tin nhắn.\n\nChỉ sau khi liên kết, UniAI mới xác nhận được tin nhắn đến từ thành viên nào của không gian làm việc. Người ngoài không dùng được Bot.\n\n## Giao việc cho agent trong Feishu\n\n### Tin nhắn trực tiếp\n\nNhắn thẳng cho Bot. Các tin nhắn liên tiếp nằm trong cùng một cuộc trò chuyện UniAI, và câu trả lời của agent quay về đúng cuộc trò chuyện đó.\n\n### Chat nhóm\n\nThêm Bot vào nhóm trước, rồi `@Bot` và mô tả điều bạn cần. Bot chỉ trả lời các tin nhắn có @nhắc nó rõ ràng; khi chạy, nó dựa trên ngữ cảnh gần đây mà nó đọc được trong phiên hiện tại.\n\n### Tạo issue\n\nGửi:\n\n```text\n/issue Fix the redirect after login\nSafari stays on the login page after a successful sign-in.\n```\n\nDòng đầu thành tiêu đề, phần còn lại thành mô tả. Gửi mỗi `/issue` không thôi sẽ không tạo gì; thay vào đó, Bot trả lời kèm định dạng cần dùng.\n\n## Quản lý kết nối\n\nMở **Cài đặt → Integrations** để xem các Bot đã kết nối trong không gian làm việc. Owner và admin của không gian làm việc có thể ngắt kết nối tại đây; owner của agent cũng quản lý được Bot của mình từ trang **Integrations** của agent đó.\n\nSau khi ngắt kết nối, Bot ngừng nhận tin nhắn mới. Các cuộc trò chuyện và bản ghi kiểm toán hiện có không bị xóa.\n\n## Khắc phục sự cố\n\n- **Không thấy nút gắn**: xác nhận tài khoản hiện tại là owner của agent, hoặc là owner hay admin của không gian làm việc.\n- **Mã QR hết hạn**: đóng hộp thoại và bắt đầu lại kết nối.\n- **Bot yêu cầu liên kết tài khoản**: mở đường dẫn liên kết và đăng nhập bằng tài khoản UniAI của không gian làm việc đó.\n- **Bot không chạy**: kiểm tra agent có bị lưu trữ không và [runtime](/daemon-runtimes) nó dùng có trực tuyến không.\n\n## Bước tiếp theo\n\n- [Tích hợp chat](/channels) — so sánh nền tảng, cách ly phiên và cơ chế liên kết tài khoản.\n- [Chat](/chat) — mô hình trò chuyện và thực thi đứng sau Bot.\n- [Issue](/issues) — đơn vị công việc mà `/issue` tạo ra."
        },
        {
          "slug": "slack-bot-integration",
          "title": "Slack Bot",
          "content": "UniAI dùng app Slack của riêng bạn. Sau khi kết nối, thành viên trong nhóm có thể DM cho Bot, @nhắc nó trong kênh, hoặc dùng `/issue` để tạo issue.\n\nMột app Slack ánh xạ tới một agent UniAI. Muốn kết nối nhiều agent, tạo một app riêng cho từng agent.\n\n## Trước khi bắt đầu\n\nViệc kết nối phải do owner hoặc admin của không gian làm việc thực hiện, và kết thúc bằng hai thông tin xác thực dán vào UniAI:\n\n- một Bot User OAuth Token, bắt đầu bằng `xoxb-`;\n- một App-level Token, bắt đầu bằng `xapp-`.\n\n## 1. Tạo app Slack từ manifest\n\nMở [Slack API Apps](https://api.slack.com/apps), bấm **Create New App → From a manifest**, chọn Slack workspace, rồi dán đoạn sau vào tab **YAML**:\n\n```yaml\ndisplay_information:\n  name: UniAI\nfeatures:\n  app_home:\n    home_tab_enabled: false\n    messages_tab_enabled: true\n    messages_tab_read_only_enabled: false\n  bot_user:\n    display_name: UniAI\n    always_online: true\n  slash_commands:\n    - command: /issue\n      description: Create a UniAI issue\n      usage_hint: \"[description]\"\noauth_config:\n  scopes:\n    bot:\n      - app_mentions:read\n      - channels:history\n      - groups:history\n      - im:history\n      - mpim:history\n      - chat:write\n      - reactions:write\n      - users:read\n      - commands\nsettings:\n  event_subscriptions:\n    bot_events:\n      - app_mention\n      - message.im\n      - message.channels\n      - message.groups\n      - message.mpim\n  interactivity:\n    is_enabled: false\n  org_deploy_enabled: false\n  socket_mode_enabled: true\n  token_rotation_enabled: false\n```\n\nTrước khi tạo, bạn có thể thay cả hai chỗ `UniAI` bằng tên agent. Đừng xóa scope hay sự kiện nào khỏi manifest, nếu không DM, @nhắc trong kênh, trả lời, hoặc `/issue` có thể ngừng hoạt động.\n\nBot kết nối ra Slack qua Socket Mode, nên không cần Request URL công khai hay OAuth Redirect URL.\n\n## 2. Lấy hai token\n\n### Bot token\n\n1. Trong app Slack, mở **Install App**.\n2. Bấm **Install to Workspace** và phê duyệt các quyền.\n3. Chép **Bot User OAuth Token** — bắt đầu bằng `xoxb-`.\n\n### App-level token\n\n1. Mở **Basic Information → App-Level Tokens**.\n2. Bấm **Generate Token and Scopes**.\n3. Thêm scope `connections:write`.\n4. Generate rồi chép token bắt đầu bằng `xapp-`.\n\n## 3. Kết nối với một agent\n\n1. Trong UniAI, mở **Agents** và chọn agent cần nối với Slack.\n2. Vào **Integrations** và bấm **Connect Slack**.\n3. Dán Bot token và App-level token.\n4. Bấm **Connect**.\n\nUniAI xác minh hai token đến từ cùng một app. Kết nối xong, trang hiện **Connected to Slack**.\n\n> Cả hai token được lưu mã hóa trên server UniAI. Token là thông tin xác thực — đừng đưa chúng vào issue, bình luận hay kho mã. App đã nối với agent hoặc không gian làm việc khác không dùng lại trực tiếp được; hãy ngắt kết nối cũ trước.\n\n## Lần dùng đầu tiên\n\nLần đầu một thành viên DM hoặc @nhắc Bot, họ nhận một đường dẫn liên kết tài khoản. Đăng nhập bằng tài khoản UniAI của không gian làm việc đó, rồi quay lại Slack và gửi lại tin nhắn.\n\nĐường dẫn dùng một lần và hết hạn sau 15 phút. Khi hết hạn, nhắn Bot lần nữa để lấy đường dẫn mới. Nhiều Bot trong cùng một không gian làm việc UniAI thường dùng lại được liên kết sẵn có nếu chúng đến từ cùng một Slack workspace.\n\nChỉ thành viên của không gian làm việc UniAI hiện tại dùng được Bot.\n\n## Giao việc cho agent trong Slack\n\n### Tin nhắn trực tiếp\n\nMở Bot từ mục **Apps** ở thanh bên Slack và nhắn trực tiếp — không cần @nhắc.\n\n### Kênh và chat nhóm\n\nChạy `/invite @your-bot` để thêm Bot vào kênh trước, rồi gửi `@your-bot yêu cầu của bạn`. Mỗi thread là một phiên riêng; muốn hỏi tiếp, cứ @nhắc Bot trong đúng thread ban đầu.\n\nBot chỉ trả lời các tin nhắn trong kênh có @nhắc nó rõ ràng. Khi chạy, nó đọc ngữ cảnh gần đây của thread trong phạm vi các scope mà quyền của nó cho phép.\n\n### Tạo issue bằng `/issue`\n\nTrong kênh hoặc DM, gõ:\n\n```text\n/issue Safari stays on the login page after a successful sign-in\n```\n\nLệnh này không cần @nhắc. UniAI chuyển mô tả cho agent đứng sau Bot hiện tại; agent viết tiêu đề và nội dung rồi tạo issue. Slack trước tiên hiện một xác nhận chỉ mình bạn thấy; khi issue được tạo, bạn nhận thông báo trong hộp thư đến UniAI.\n\n## Quản lý kết nối\n\nMở **Cài đặt → Integrations** để xem mọi Slack Bot đã kết nối. Owner và admin của không gian làm việc có thể ngắt kết nối chúng.\n\nSau khi ngắt kết nối, kết nối Socket Mode dừng và Bot không nhận tin nhắn mới nữa. Các cuộc trò chuyện và bản ghi kiểm toán hiện có không bị xóa.\n\n## Khắc phục sự cố\n\n- **Token không hợp lệ khi kết nối**: kiểm tra tiền tố và xác nhận cả hai token đến từ cùng một app.\n- **Không xác minh được app**: xác nhận manifest có `users:read`, rồi cài lại app sau khi cập nhật quyền.\n- **Không có chỗ để DM**: xác nhận `app_home.messages_tab_enabled` là `true`.\n- **Không trả lời trong kênh**: xác nhận Bot đã được mời vào kênh và tin nhắn có @nhắc nó.\n- **`/issue` không tồn tại**: xác nhận manifest có slash command và scope `commands`, rồi cài lại app.\n- **Bot không chạy**: kiểm tra agent có bị lưu trữ không và [runtime](/daemon-runtimes) nó dùng có trực tuyến không.\n\n## Bước tiếp theo\n\n- [Tích hợp chat](/channels) — so sánh nền tảng, cách ly phiên và cơ chế liên kết tài khoản.\n- [Chat](/chat) — mô hình trò chuyện và thực thi đứng sau Bot.\n- [Issue](/issues) — đơn vị công việc mà `/issue` tạo ra."
        },
        {
          "slug": "dingtalk-bot-integration",
          "title": "Tích hợp DingTalk Bot",
          "content": "Nối bất kỳ [agent](/agents) nào với một bot DingTalk và nhóm của bạn làm việc với nó ngay trong DingTalk — DM cho bot, @nhắc nó trong nhóm, gửi ảnh chụp màn hình, hoặc gõ `/issue` để tạo một [issue UniAI](/issues) mà không phải mở app.\n\nHỗ trợ DingTalk do cộng đồng duy trì: nó có mặt trong mọi bản phát hành, nhưng không kèm SLA hỗ trợ chính thức. Báo lỗi tại GitHub issues.\n\nDingTalk dùng mô hình **bring-your-own-app (BYO)**: admin của không gian làm việc tạo một app DingTalk, thêm một robot chế độ Stream vào đó và dán thông tin xác thực của app vào UniAI. Mỗi agent có **app DingTalk riêng** — nên nhiều agent có thể có bot riêng, @nhắc được riêng từng bot, trong cùng một tổ chức. (Khác với [Lark](/lark-bot-integration), nơi việc gắn dùng luồng quét mã để cài.)\n\nToàn bộ thiết lập ở bên dưới, mất khoảng năm phút. Kết quả là hai thông tin xác thực để dán vào UniAI:\n\n- một **AppKey** — client id của app\n- một **AppSecret** — client secret của app\n\n## Thiết lập app DingTalk của bạn\n\n### 1. Tạo app và thêm robot chế độ Stream\n\n1. Vào [DingTalk Open Platform](https://open-dev.dingtalk.com) và tạo một **app nội bộ doanh nghiệp** (企业内部应用).\n2. Mở app và thêm khả năng **robot** (机器人).\n3. Trong phần cài đặt robot, đặt **chế độ nhận tin nhắn** thành **chế độ Stream** (Stream 模式 / 推送模式). Chính chế độ này cho phép bot kết nối ra ngoài qua một kết nối đẩy dài hạn thay vì nhận webhook.\n\nĐó là tất cả những gì UniAI cần ở tầng nền tảng — bot kết nối ra ngoài qua chế độ Stream, nên bạn không phải cấu hình địa chỉ công khai nào:\n\n| Mục | Vì sao cần |\n|---|---|\n| **App nội bộ doanh nghiệp** | Vỏ chứa app sở hữu robot và cấp cặp thông tin xác thực AppKey / AppSecret. |\n| **Khả năng robot** | Tạo danh tính bot được @nhắc và đăng trả lời. |\n| **Chế độ Stream** | Bot kết nối ra ngoài qua một kết nối Stream dài hạn — **không cần webhook / URL công khai**. |\n| **Quyền gửi tin của robot** | Cho bot gửi tin nhắn ngược vào DingTalk (câu trả lời và tin nhắn chủ động của agent). |\n| **Scope đọc tin nhắn** | Để bot nhận tin nhắn 1:1 và các tin nhắn nhóm có @nhắc nó. |\n\n**Không có webhook URL và không có OAuth redirect URL**, vì robot chạy qua chế độ Stream và BYO không dùng OAuth.\n\n> DingTalk không có chỉ báo đang gõ / cảm xúc gốc, nên — khác với Slack — bot thay vào đó đăng một lời xác nhận ngắn kiểu \"đang xử lý\" khi bắt đầu, và câu trả lời đầy đủ đến sau khi agent làm xong. Một loạt tin nhắn gửi dồn dập được gộp thành một lời xác nhận duy nhất.\n\n### 2. Cấp quyền cho robot\n\nCấp cho robot các scope nó cần để có thể **nhận tin nhắn** và **gửi tin nhắn trả lời** (quyền gửi tin nhắn của robot). Thiếu quyền gửi, agent vẫn chạy được nhưng câu trả lời của nó không thể chuyển đi.\n\n### 3. Chép AppKey và AppSecret\n\nMở phần **凭证与基础信息** (Thông tin xác thực & thông tin cơ bản) của app và chép:\n\n- **AppKey** — đây là **client id** của app\n- **AppSecret** — đây là **client secret** của app\n\n### 4. Kết nối trong UniAI\n\n1. Mở agent trong **Agents → _agent của bạn_** → tab **Integrations** (hoặc mục **Integrations** ở thanh bên trái).\n2. Bấm **Connect DingTalk**.\n3. Dán **AppKey** và **AppSecret**, rồi bấm **Connect**.\n4. Agent hiện **Connected to DingTalk**. Bot giờ đang nghe qua kết nối Stream của riêng nó.\n\n> Hai thông tin xác thực phải từ **cùng một** app DingTalk, và app đó ánh xạ tới đúng **một** agent. Kết nối một app đã nối với agent hoặc không gian làm việc khác sẽ bị từ chối. Muốn chuyển app sang agent khác, ngắt kết nối trước; kết nối lại một agent với một app **mới** sẽ cập nhật thẳng bot của agent đó.\n\n> Thiết lập cho **nhiều agent**? Lặp lại toàn bộ luồng cho từng agent — mỗi agent có app DingTalk riêng và cặp AppKey / AppSecret riêng, chúng hiện thành các bot tách biệt trong tổ chức của bạn.\n\n## Tích hợp làm gì\n\n| Vị trí | Hành vi |\n|---|---|\n| **Agent → Integrations** | Owner và admin thấy **Connect DingTalk**; nối xong nó chuyển thành huy hiệu **Connected to DingTalk** kèm nút **Disconnect**. |\n| **DM cho bot** | Một thành viên nhắn thẳng cho bot trong cuộc trò chuyện 1:1. Cuộc trò chuyện thành một phiên [chat](/chat) UniAI với agent; mọi tin nhắn đều được đọc. |\n| **@nhắc trong nhóm** | Thêm bot vào nhóm và @nhắc nó. Chỉ tin nhắn có nhắc mới được đọc — bot không nghe cả nhóm. |\n| **Gửi ảnh** | Ảnh trong DM, hoặc gửi kèm một @nhắc trong nhóm, xuất hiện trong cuộc trò chuyện để agent xem — PNG, JPEG, GIF, WebP, hoặc BMP, tối đa 4 ảnh mỗi tin nhắn và 10 MB mỗi ảnh. Mỗi ảnh được sao chép vào bộ lưu trữ UniAI, nên vẫn hiển thị trong chat sau khi liên kết tạm thời của DingTalk hết hạn. Ảnh gửi cùng `/issue` được đưa vào issue được tạo thay vì lượt chat. Tệp và tin nhắn thoại không được hỗ trợ. |\n| **Lệnh `/issue`** | Bắt đầu tin nhắn bằng `/issue <tiêu đề>` tạo một issue UniAI trực tiếp từ nội dung bạn nhập, đứng tên bạn, và đăng ID cùng tiêu đề issue trở lại đúng cuộc trò chuyện đó. Viết mô tả ở các dòng tiếp theo. Ảnh gửi trong cùng tin nhắn DingTalk được đính kèm vào issue được tạo. Bản thân lệnh không xuất hiện trong UniAI Chat; issue được tạo là kết quả được ghi lại trong UniAI. |\n| **Lệnh `/new`** | Bắt đầu tin nhắn bằng `/new <tin nhắn của bạn>` chạy tin nhắn đó mà không kèm ngữ cảnh trò chuyện trước. Gửi `/new` một mình ghi lại cùng ý định bắt đầu mới đó cho tin nhắn không rỗng kế tiếp của bạn mà không tạo lượt rỗng, và bot xác nhận khi ý định đó đã sẵn sàng. Ý định đang chờ vẫn có hiệu lực cho đến khi một chat task sau đó được xếp hàng thành công. Lịch sử trò chuyện vẫn giữ nguyên. |\n| **Trả lời** | Câu trả lời của agent được đăng lại vào đúng cuộc trò chuyện 1:1 hoặc nhóm. |\n\n## Dùng bot (thành viên)\n\n### Tin nhắn đầu tiên: liên kết tài khoản\n\nLần đầu bạn @nhắc hoặc DM bot, nó trả lời với lời nhắc **liên kết tài khoản** trỏ đến trang **/dingtalk/bind** trong sản phẩm. Bấm liên kết, đăng nhập UniAI, và danh tính DingTalk của bạn được nối với tài khoản thành viên UniAI — nhờ đó agent có thể hành động thay bạn (ví dụ `/issue` tạo issue đứng tên bạn). Liên kết dùng một lần và hết hạn sau khoảng 15 phút; nhắn bot lần nữa để lấy liên kết mới.\n\n> Chỉ **thành viên của không gian làm việc** dùng được bot. Nếu bạn không phải thành viên, hoặc bỏ qua bước liên kết danh tính, bot sẽ không chạy — tin nhắn của bạn bị bỏ qua (được ghi lại để kiểm toán, không kèm nội dung).\n\n### Chat và lệnh\n\n- **Trong nhóm** — thêm bot vào nhóm, rồi `@your-bot <tin nhắn của bạn>`. Mỗi câu tiếp theo phải @nhắc lại (bot chỉ đọc tin có nhắc nó).\n- **Trong chat 1:1** — mở bot và nhắn trực tiếp; không cần nhắc, mọi tin nhắn đều được đọc.\n- **Gửi ảnh** — gửi ảnh chụp màn hình hoặc ảnh chụp, kèm hoặc không kèm chữ; chúng xuất hiện trong cuộc trò chuyện để agent làm việc. PNG, JPEG, GIF, WebP, hoặc BMP; tối đa 4 ảnh mỗi tin nhắn, 10 MB mỗi ảnh.\n- **Tạo issue** — gửi `/issue the login redirect is broken on Safari`; thêm mô tả ở các dòng tiếp theo nếu cần. UniAI tạo issue đồng bộ, đính kèm ảnh từ tin nhắn đó vào issue, và đăng ID cùng tiêu đề trở lại chat.\n- **Bắt đầu mới** — gửi `/new <tin nhắn của bạn>` để chạy tin nhắn đó không kèm ngữ cảnh trước, hoặc gửi `/new` một mình để áp dụng việc bắt đầu mới cho tin nhắn không rỗng kế tiếp. Một `/new` trống nhận được xác nhận, và ý định đang chờ của nó vẫn có hiệu lực cho đến khi một chat task sau đó được xếp hàng thành công. Cả hai cách đều không xóa lịch sử trò chuyện hiện có.\n\n## Quản lý và ngắt kết nối\n\nQuản lý toàn không gian làm việc nằm ở **Cài đặt → Integrations**:\n\n- **Connected bots** liệt kê mọi bot trong không gian làm việc và agent mà từng bot gắn vào (mọi thành viên thấy được).\n- **Disconnect** chỉ dành cho **owner / admin**. Nó làm bot ngừng nhận tin DingTalk và gỡ kết nối; bản ghi installation giữ lại để kiểm toán, và bạn nối lại được về sau.\n\n## Quyền\n\n- **Kết nối / ngắt** cần **owner** hoặc **admin** của không gian làm việc.\n- **Nói chuyện với bot** yêu cầu là thành viên của không gian làm việc và đã liên kết danh tính DingTalk. Tin nhắn của những người khác đều bị bỏ qua.\n- Nội dung của tin nhắn bị bỏ qua không bao giờ được lưu — chỉ lưu lý do, để kiểm toán.\n\n## Tiếp theo\n\n- [Tích hợp chat](/channels) — bộ máy kênh, phiên và ủy quyền hoạt động ra sao\n- [Agent](/agents) · [Chat](/chat) · [Issue](/issues)"
        },
        {
          "slug": "vcs-integration",
          "title": "Git provider tự vận hành",
          "content": "> **Chỉ dành cho UniAI tự vận hành.** Tích hợp này chỉ có khi bạn tự chạy\n> UniAI; nó không được cung cấp trên UniAI Cloud. \"Tự vận hành\" ở đây nghĩa là\n> **UniAI** được tự vận hành — thường để nó với tới được một instance Git trong\n> mạng nội bộ của bạn. Người vận hành máy chủ phải bật nó (`MULTICA_VCS_INTEGRATION_ENABLED=true`)\n> và đặt `MULTICA_VCS_SECRET_KEY`; trước đó, mục này không xuất hiện trong\n> Cài đặt → Integrations.\n\nUniAI kết nối với một Git provider tự vận hành theo từng không gian làm việc: **Forgejo**, **Gitea**, hoặc **GitLab**. Kết nối xong, bất kỳ pull request nào (GitLab: merge request) có tên nhánh, tiêu đề, hoặc phần thân chứa một mã định danh issue (ví dụ `UNI-123`) sẽ được **tự động liên kết** với [issue](/issues) đó, xuất hiện dưới mục **Pull requests** ở thanh bên của issue, và — khi được merge kèm từ khóa đóng — chuyển issue sang **Done**. CI của commit đầu nhánh hiển thị thành thanh checks trên thẻ.\n\nCác provider này chạy song song với GitHub: một không gian làm việc dùng được bất kỳ tổ hợp nào.\n\n> Khác với GitHub, các provider này không có mô hình \"App\". Mỗi không gian làm việc lưu URL instance của riêng mình cùng một access token, và đăng ký một webhook trên repo hoặc tổ chức của mình. Token và webhook secret được mã hóa khi lưu.\n\n## Điều kiện tiên quyết (máy chủ)\n\nBật tích hợp cho deployment này. Nó tắt mặc định, nên mục này ẩn cho đến khi bạn đặt:\n\n```sh\nMULTICA_VCS_INTEGRATION_ENABLED=true\n```\n\nTệp `docker compose` tự vận hành chính thức (`docker-compose.selfhost.yml`) đã đặt sẵn biến này cho bạn.\n\nSau đó đặt một khóa 32 byte mã hóa base64 để máy chủ mã hóa được thông tin xác thực đã lưu. Thiếu nó, form kết nối bị vô hiệu hóa.\n\n```sh\nopenssl rand -base64 32\n```\n\n```sh\nMULTICA_VCS_SECRET_KEY=<base64 32-byte key>\n```\n\nCả hai đều bắt buộc: công tắc quyết định tính năng có được cung cấp hay không, còn khóa mã hóa token và webhook secret đã lưu. Kết nối, webhook, và xoay vòng đều cần cả hai.\n\nĐặt `MULTICA_PUBLIC_URL` thành base URL công khai của máy chủ để UniAI hiển thị được một webhook URL dán ngay được. Thiếu nó, giao diện chỉ hiện đường dẫn webhook và bạn tự ghép origin của mình vào trước.\n\n## Kết nối một không gian làm việc\n\n1. Tạo một access token trong provider của bạn với quyền đọc các repo bạn muốn phản chiếu:\n   - **Forgejo / Gitea**: Settings → Applications.\n   - **GitLab**: một personal (hoặc group/project) access token với scope `read_api`.\n2. Trong UniAI, mở **Cài đặt → Integrations → Git providers**.\n3. Chọn **provider**, nhập URL instance (ví dụ `https://forgejo.example.com`) và access token, rồi bấm **Connect**. UniAI xác thực token với instance trước khi lưu.\n4. Chép **webhook URL** và **webhook secret** hiện ra sau khi kết nối.\n\n> Webhook secret chỉ hiện đúng một lần. Chép nó trước khi rời trang. Kết nối lại cùng một instance sẽ xoay vòng token và secret.\n\n## Đăng ký webhook\n\nTrên repo (hoặc tổ chức/nhóm, để phủ mọi repo):\n\n**Forgejo / Gitea** — Settings → Webhooks → Add Webhook → Forgejo/Gitea:\n\n- **Target URL**: webhook URL từ bước trước.\n- **HTTP method** `POST`, **content type** `application/json`.\n- **Secret**: webhook secret (dùng để xác minh HMAC `X-Gitea-Signature`).\n- **Trigger events**: chọn **Pull Request**, và **Commit Status** để phản chiếu CI.\n\n**GitLab** — Settings → Webhooks:\n\n- **URL**: webhook URL.\n- **Secret token**: webhook secret (gửi dưới dạng `X-Gitlab-Token` và so sánh nguyên văn).\n- **Triggers**: bật **Merge request events**, và **Pipeline events** để phản chiếu CI.\n\nUniAI xác thực mọi lần gửi bằng secret đã lưu, nên webhook không có secret khớp sẽ bị từ chối.\n\n## Những gì được phản chiếu\n\n- **Pull / merge request** — trạng thái open, closed, merged, và draft, tác giả, nhánh, và (khi provider cung cấp) thống kê diff.\n- **Liên kết issue** — mã định danh trong tiêu đề, phần thân, hoặc nhánh liên kết PR với issue. Một từ khóa đóng (`Closes`/`Fixes`/`Resolves UNI-123`) trên PR đã merge chuyển issue sang **Done** khi không còn PR liên kết nào đang mở.\n- **CI** — commit status của Forgejo/Gitea và pipeline của GitLab được tổng hợp thành thanh checks passed/failed/pending cho commit đầu nhánh.\n\n## Agent mở pull request\n\nViệc tạo PR không cần cấu hình provider nào trong UniAI. Agent checkout repo trong runtime rồi đẩy nhánh và mở PR bằng chính thông tin xác thực Git của máy chạy runtime. Để agent làm việc được với một provider, hãy đảm bảo máy chạy daemon xác thực được với nó — ví dụ một SSH deploy key, hoặc một token trong Git credential helper của máy — và thêm URL repo như bình thường. Checkout repo hoạt động với mọi Git URL, nên không cần đấu nối gì riêng cho từng provider."
        }
      ]
    },
    {
      "label": "Self-hosting",
      "labelKey": "self_hosting",
      "pages": [
        {
          "slug": "security-model",
          "title": "Mô hình bảo mật",
          "content": "Khi agent nhận một task, daemon khởi chạy một công cụ lập trình AI (Codex, Claude Code, v.v.) dưới dạng tiến trình con. Hiểu tiến trình đó chạm tới được những gì chính là toàn bộ mô hình bảo mật.\n\n## Ranh giới là user chạy daemon\n\nMặc định, một task chạy với **toàn bộ quyền của user hệ điều hành đang chạy daemon**. Nó đọc và ghi được mọi file user đó đọc ghi được, dùng được thông tin đăng nhập của user đó, và truy cập mạng không giới hạn.\n\nUniAI không đưa ra bất kỳ cam kết sandbox filesystem nào. Hiện có đúng một ngoại lệ hẹp — Windows, khi bạn chủ động bật sandbox gốc của Codex — nhưng tổ hợp nền tảng và cấu hình nào sandbox được gì là chi tiết tương thích thay đổi theo phiên bản công cụ. Hãy coi mọi task là không có sandbox và đặt ranh giới ở bên ngoài daemon.\n\n> UniAI không sandbox filesystem giúp bạn. Nếu daemon chạy bằng tài khoản cá nhân của bạn, một task có thể đọc SSH key, sửa shell profile và xóa tài liệu của bạn. Sự cách ly phải đến từ ranh giới mà bạn đặt daemon vào.\n\nĐây là chủ ý thiết kế. Agent được yêu cầu cài dependency, chạy build, dùng các cloud CLI của bạn và điều khiển những bộ công cụ vốn trông đợi một thư mục home bình thường. Một sandbox filesystem nửa vời làm hỏng các công việc đó theo những cách khó chẩn đoán — công cụ báo \"not logged in\" hoặc âm thầm dùng nhầm tài khoản — trong khi vẫn không bảo vệ được thứ quan trọng nhất, vì nó không ngăn được task đọc thông tin đăng nhập rồi gửi qua mạng.\n\nVì thế UniAI không giả vờ làm ranh giới. Hãy dựng một ranh giới bao quanh nó.\n\n## Các cách dựng được khuyến nghị\n\nChọn cách hợp với hạ tầng của bạn — liệt kê từ nhẹ nhất đến mạnh nhất:\n\n1. **Unix user riêng.** Tạo một user `uniai`, chỉ cấp cho nó các repo và thông tin đăng nhập mà agent cần, rồi chạy daemon bằng user đó. Tài khoản của riêng bạn không bị đụng tới.\n2. **Container.** Chạy daemon trong container chỉ với các mount và secret mà agent cần.\n3. **Máy ảo.** Cách ly hoàn toàn, đổi lại phải cấp phát một máy.\n\nChọn cách nào cũng vậy, hãy coi mọi thông tin đăng nhập với tới được từ môi trường đó là thứ agent có thể dùng: giới hạn phạm vi token thật hẹp, ưu tiên deploy key theo từng mục đích thay vì SSH key cá nhân, và tránh để các thông tin đăng nhập production không liên quan nằm trong thư mục home của user đó.\n\n## Những gì UniAI có cách ly\n\nNhững điều sau là thật, nhưng chúng là tiện ích và biện pháp thu hẹp bán kính thiệt hại — không phải ranh giới bảo mật trước một task chủ động tìm cách thoát ra:\n\n- **Thư mục làm việc theo từng task.** Mỗi task có workdir riêng dưới `~/multica_workspaces/`, nên các task chạy song song không va nhau trên cùng một checkout.\n- **Trạng thái agent theo từng task.** Task của Codex có một `CODEX_HOME` riêng theo task chứa cấu hình, session và skill, nên thiết lập theo task không làm bẩn `~/.codex/` của bạn.\n- **API token theo phạm vi task.** `MULTICA_TOKEN` trao cho task được server ràng buộc với đúng agent đó và đúng task đó, nên một task không thể hành xử như bạn hay như agent khác qua API của UniAI.\n\n## Những gì không phải ranh giới\n\n- **Sandbox và thiết lập phê duyệt của chính công cụ lập trình AI.** UniAI chạy agent không cần người ngồi giám sát, nên các câu hỏi phê duyệt được trả lời tự động. Trên đường chạy mặc định, sandbox filesystem cũng tắt: Codex chạy với `sandbox_mode = \"danger-full-access\"` và Claude Code với `--permission-mode bypassPermissions`. Ngoại lệ là Windows — nếu bạn chủ động cấu hình sandbox gốc của Codex (`windows.sandbox = \"unelevated\"` hoặc `\"elevated\"`), UniAI tôn trọng lựa chọn đó và giữ `workspace-write` cho các task ấy. Đáng làm ở nơi phù hợp, nhưng đó là một công cụ trên một nền tảng và không thay đổi cách bạn nên giới hạn quyền cho user chạy daemon.\n- **Bố cục thư mục `HOME` của bạn.** Task kế thừa `HOME` thật và các biến `XDG_*` của user chạy daemon — chính điều này giúp các CLI trên máy (`gh`, `aws`, `kubectl`, `gcloud`, `glab`) hoạt động trong task y hệt trong shell của bạn. Nó cũng có nghĩa mọi thứ dưới home đó đều với tới được.\n\n> Trên Linux, task của Codex trước đây chạy dưới sandbox `workspace-write` với `HOME` chuyển hướng theo từng task. Cơ chế đó đã bị gỡ: nó khiến các CLI trên máy mất cấu hình bên trong task, và vì chỉ hạn chế ghi, nó chưa bao giờ ngăn được task đọc rồi tuồn thông tin đăng nhập ra ngoài. Linux giờ giống mặc định của macOS và Windows.\n\n## Kiểm tra task đã chạy với thiết lập nào\n\nDaemon ghi log chế độ sandbox hiệu lực ở mức warn khi một task khởi động không có sandbox:\n\n```bash\nuniai daemon logs --lines 200 | grep \"codex sandbox\"\n```\n\nĐể xác nhận cấu hình Codex hiệu lực, đọc khối được quản lý trong `config.toml` của task dưới `CODEX_HOME` của task đó — phần nằm giữa hai đánh dấu `# BEGIN multica-managed` và `# END multica-managed` được daemon ghi lại ở mỗi lượt chạy."
        }
      ]
    },
    {
      "label": "Reference",
      "labelKey": "reference",
      "pages": [
        {
          "slug": "cli",
          "title": "Sử dụng CLI",
          "content": "UniAI CLI vừa kết nối các runtime cục bộ, vừa quản lý không gian làm việc, issue, agent và Tự động hóa từ terminal. Trang này đi qua các luồng thường dùng; tập cờ mà phiên bản bạn cài hỗ trợ luôn do chính `--help` của từng lệnh quy định.\n\n## Cài đặt\n\n**macOS / Linux**\n\n```bash\ncurl -fsSL https://raw.githubusercontent.com/phanducquanguet/usf/feature/customer-portal/scripts/install.sh | bash\n```\n\n**Windows PowerShell**\n\n```powershell\nirm https://raw.githubusercontent.com/phanducquanguet/usf/feature/customer-portal/scripts/install.ps1 | iex\n```\n\nKiểm tra kết quả cài đặt:\n\n```bash\nuniai version\n```\n\n## Kết nối lần đầu\n\nKết nối UniAI Cloud:\n\n```bash\nuniai setup\n```\n\nKết nối một instance tự lưu trữ:\n\n```bash\nuniai setup self-host \\\n  --server-url https://api.example.com \\\n  --app-url https://app.example.com\n```\n\n`setup` lưu địa chỉ máy chủ, mở trình duyệt để hoàn tất đăng nhập và khởi động daemon. Khi xong, kiểm tra:\n\n```bash\nuniai auth status\nuniai daemon status\n```\n\nNếu bạn chỉ cần đăng nhập lại mà không ghi đè các cấu hình khác, chạy `uniai login`. Trên máy không có trình duyệt, hãy tạo personal access token trong phần cài đặt trên web trước, rồi nhập token bằng:\n\n```bash\nuniai login --token\n```\n\nLệnh này nhắc bạn dán token ngay trong terminal, để giá trị đầy đủ không lọt vào lịch sử shell.\n\n## Chọn không gian làm việc\n\nLiệt kê các không gian làm việc và đổi không gian mặc định:\n\n```bash\nuniai workspace list\nuniai workspace switch <slug>\n```\n\nCác lệnh sau đó dùng không gian làm việc này. Một lệnh đơn lẻ có thể ghi đè bằng `--workspace-id`, hoặc bạn đặt biến `MULTICA_WORKSPACE_ID`.\n\nMời thành viên:\n\n```bash\nuniai workspace member invite teammate@example.com\nuniai workspace member invite admin@example.com --role admin\n```\n\n## Thao tác issue thường dùng\n\n```bash\n# View and search\nuniai issue list\nuniai issue get UNI-123\nuniai issue search \"login failure\"\n\n# Create and update\nuniai issue create --title \"Fix login failure\"\nuniai issue status UNI-123 in_progress\nuniai issue assign UNI-123 --to \"Backend Agent\"\n\n# Comments and runs\nuniai issue comment list UNI-123\nuniai issue comment add UNI-123 --content \"Check the regression tests first\"\nuniai issue runs UNI-123\n```\n\nĐọc mô tả hoặc bình luận dài từ stdin để khỏi phải xử lý xuống dòng và dấu nháy:\n\n```bash\nuniai issue create --title \"Upgrade notes\" --description-stdin < notes.md\nuniai issue comment add UNI-123 --content-stdin < review.md\n```\n\nXem tin nhắn của một lượt chạy, hoặc dừng một task:\n\n```bash\nuniai issue run-messages <task-id> --issue UNI-123\nuniai issue cancel-task <task-id> --issue UNI-123\n```\n\n## Agent và skill\n\n```bash\nuniai agent list\nuniai agent get <agent-id>\nuniai agent create --help\nuniai agent update <agent-id> --help\n\nuniai skill list\nuniai skill get <skill-id>\nuniai skill import --url <skill-url>\nuniai agent skills add <agent-id> --skill-ids <skill-id>\n```\n\nKhi lệnh nhập gặp skill trùng tên, hành vi mặc định là dừng lại và không sửa nội dung sẵn có. Chọn theo ý định của bạn:\n\n```bash\nuniai skill import --url <skill-url> --on-conflict overwrite\nuniai skill import --url <skill-url> --on-conflict rename\nuniai skill import --url <skill-url> --on-conflict skip\n```\n\n`overwrite` chỉ được phép với người tạo skill và giữ nguyên ID gốc cùng các gắn kết agent.\n\n## Daemon và runtime\n\n```bash\nuniai daemon start\nuniai daemon stop\nuniai daemon restart\nuniai daemon status --output json\nuniai daemon logs --follow\n\nuniai runtime list\nuniai runtime rename <runtime-id> \"Office Mac\"\nuniai runtime usage <runtime-id>\nuniai runtime activity <runtime-id>\n```\n\nXóa một runtime vẫn còn agent hoạt động gắn kèm mặc định sẽ bị từ chối. `uniai runtime delete <id> --cascade` gỡ gắn kết các agent đó, giữ nguyên cấu hình và lịch sử của chúng, đồng thời hủy các task đang hoạt động.\n\nXem [Daemon và runtime](/daemon-runtimes) để hiểu cơ chế vận hành và runtime profile tùy chỉnh.\n\n## Tổng quan lệnh\n\n| Lệnh | Mục đích |\n| --- | --- |\n| `issue` | Tạo, cập nhật, giao và tìm issue; quản lý bình luận, người theo dõi, nhãn, thuộc tính và lượt chạy |\n| `project` | Quản lý dự án và tài nguyên của dự án |\n| `label`, `property` | Quản lý nhãn và thuộc tính tùy chỉnh của không gian làm việc |\n| `agent`, `skill`, `squad` | Quản lý agent, skill và đội |\n| `autopilot` | Quản lý Tự động hóa, trigger và lịch sử chạy |\n| `workspace` | Tạo, xem và chuyển không gian làm việc, mời thành viên |\n| `repo` | Quản lý repo của không gian làm việc và checkout về máy |\n| `daemon`, `runtime` | Khởi động và dừng daemon cục bộ; xem và quản lý runtime |\n| `attachment` | Tải lên hoặc tải xuống tệp đính kèm |\n| `user profile` | Xem hoặc cập nhật hồ sơ người dùng hiện tại |\n| `auth`, `login`, `setup` | Đăng nhập, kiểm tra trạng thái xác thực và khởi tạo kết nối |\n| `config` | Xem hoặc sửa cấu hình cục bộ của profile CLI hiện tại |\n| `update`, `version` | Cập nhật CLI hoặc in phiên bản |\n\n`uniai chat` đọc **phiên chat bên ngoài mà một agent đang xử lý**; lệnh này chủ yếu dành cho agent trong các tích hợp chat, không phải lệnh chung để duyệt hội thoại bất kỳ trong không gian làm việc.\n\n## ID và định dạng đầu ra\n\nIssue dùng mã kiểu `UNI-123` hoặc UUID đầy đủ; tiền tố UUID ngắn không được chấp nhận.\n\nLệnh `list` của các tài nguyên khác thường in ID ngắn chép-dán được và hỗ trợ `--full-id` để lấy UUID đầy đủ. Khi ID ngắn không đủ để xác định duy nhất, CLI sẽ yêu cầu thêm ký tự hoặc UUID đầy đủ. ID task ngắn còn cần `--issue` để xác định issue chứa nó.\n\nNhiều lệnh hỗ trợ đầu ra có cấu trúc:\n\n```bash\nuniai issue list --output json\nuniai agent list --output json\n```\n\nScript nên dùng đầu ra JSON thay vì phân tích các bảng vốn dành cho terminal. Định dạng đầu ra và cờ phân trang mà mỗi lệnh hỗ trợ do `uniai <command> --help` quy định.\n\n## Profile và cấu hình\n\nCấu hình mặc định nằm ở `~/.multica/config.json`. Dùng `--profile <name>` để tách riêng một bộ địa chỉ máy chủ, token, không gian làm việc mặc định và trạng thái daemon:\n\n```bash\nuniai setup self-host --profile staging \\\n  --server-url https://api.staging.example.com \\\n  --app-url https://app.staging.example.com\n\nuniai issue list --profile staging\n```\n\nCác profile có tên nằm ở `~/.multica/profiles/<name>/config.json`. Xem giá trị hiện tại:\n\n```bash\nuniai config show\nuniai config show --profile staging\n```\n\nKhi CLI chạy bên trong một task agent do daemon quản lý, nó không đọc hay sửa các file profile thuộc về con người này. Các lệnh gọi UniAI API xác thực bằng thông tin xác thực phạm vi task do daemon tiêm vào. `config show` và `config set` dùng trạng thái riêng cục bộ theo task; các lệnh dành cho con người/cục bộ như `login`, `logout`, `setup`, `workspace switch`, thay đổi đường dẫn runtime profile cục bộ, `daemon start` / `stop` / `restart`, `daemon logs` và `daemon probe-runtimes` không khả dụng. `auth status` không in nội dung token trong ngữ cảnh này.\n\n`daemon status` và `daemon disk-usage` vẫn khả dụng để agent kiểm tra runtime đang chứa nó, giới hạn trong runtime đó: `status` thăm dò cổng health do daemon tiêm vào, còn `disk-usage` quét thư mục gốc workspaces do daemon tiêm vào. Cả hai đều không nhận `--profile`, `disk-usage` còn từ chối `--all-profiles` và `--workspaces-root`, và cột STATUS của nó để trống vì việc phân giải trạng thái sẽ cần token đã lưu của con người. Dùng `du` và `df` cho mọi thứ ngoài phạm vi đó.\n\nCơ chế này bảo vệ việc phân giải profile ngầm của UniAI CLI trong khi vẫn giữ nguyên các biến `HOME` và XDG thông thường mà công cụ của các provider (công cụ lập trình AI) khác sử dụng. Đây không phải ranh giới hệ thống tệp ở cấp hệ điều hành: tiến trình chạy dưới cùng một người dùng hệ thống vẫn có thể mở một đường dẫn đã biết rõ. Hãy dùng người dùng riêng, container, VM hoặc mức cách ly tương đương khi cần bảo đảm mạnh hơn.\n\n> File cấu hình CLI chứa token có thể truy cập UniAI với danh nghĩa của bạn. Đừng commit chúng vào repo, đưa chúng lên log hay chia sẻ cho người khác.\n\n## Tham chiếu lệnh\n\nCác bảng dưới đây bao phủ mọi lệnh chính hiện có, nhóm theo đúng cách CLI tự nhóm. Mọi lệnh đều nhận các cờ toàn cục `--server-url`, `--workspace-id`, `--profile` và `--debug`. `--output` được đăng ký theo từng lệnh và định dạng mặc định khác nhau: lệnh `list` mặc định in bảng, còn lệnh `get` và `create` phần lớn mặc định JSON. Cờ đầy đủ do `uniai <command> --help` quy định.\n\n### Lõi\n\n| Lệnh | Lệnh con | Mục đích | Cờ chính |\n| --- | --- | --- | --- |\n| `issue` | `list` | Liệt kê issue | `--status`, `--priority`, `--assignee`, `--project`, `--metadata` (lặp lại được), `--limit`, `--offset`, `--sort`, `--full-id` |\n| | `get <id>` | Xem một issue | |\n| | `create` | Tạo issue | `--title` (bắt buộc), `--description` / `--description-stdin` / `--description-file`, `--status`, `--priority`, `--assignee`, `--parent`, `--stage`, `--project`, `--start-date`, `--due-date`, `--attachment` (lặp lại được) |\n| | `update <id>` | Cập nhật các trường của issue | Cùng các trường như `create`, thêm `--position` |\n| | `assign <id>` | Giao hoặc bỏ giao | `--to` (khớp tên gần đúng với thành viên, agent và đội), `--to-id`, `--unassign` |\n| | `status <id> <status>` | Đổi trạng thái | |\n| | `reorder <id>` | Di chuyển vị trí trong một cột | |\n| | `search <query>` | Tìm issue | `--limit`, `--include-closed` |\n| | `children <id>` | Liệt kê issue con nhóm theo giai đoạn | |\n| | `pull-requests <id>` | Liệt kê PR liên kết | |\n| | `comment list/add/delete/resolve/unresolve` | Quản lý bình luận | `add`: `--content` / `--content-stdin` / `--content-file`, `--parent`, `--attachment`; `list`: `--since`, `--thread`, `--tail` |\n| | `subscriber list/add/remove <issue-id>` | Quản lý người theo dõi | `--user`, `--user-id` (mặc định là người dùng hiện tại) |\n| | `label list/add/remove <issue-id>` | Quản lý nhãn trên một issue | |\n| | `metadata list/get/set/delete <issue-id>` | Quản lý metadata dạng khóa-giá trị ở cấp issue | |\n| | `property list/set/unset <issue-id>` | Đặt giá trị thuộc tính tùy chỉnh | `set`: `--name`, `--value` |\n| | `runs <issue-id>` | Xem lịch sử chạy | `--full-id` |\n| | `run-messages <task-id>` | Xem tin nhắn của một lượt chạy | `--since`, `--issue` |\n| | `usage <issue-id>` | Xem usage token tổng hợp | |\n| | `rerun <id>` | Đưa một lượt chạy mới vào hàng đợi cho bên đang phụ trách | |\n| | `cancel-task <task-id>` | Hủy task đang chạy hoặc đang xếp hàng | `--issue` |\n| `project` | `list/get/create/update/delete` | Quản lý dự án | |\n| | `status <id> <status>` | Đổi trạng thái dự án | |\n| | `resource list/add/update/remove` | Quản lý tài nguyên dự án | `--type`, `--url` |\n| `label` | `list/get/create/update/delete` | Quản lý nhãn của không gian làm việc | |\n| `property` | `list/get/create/update/archive/unarchive` | Quản lý thuộc tính tùy chỉnh của không gian làm việc | `create`: `--name`, `--type`, `--option` (lặp lại được); `list`: `--include-archived`; không thể đổi kiểu sau khi tạo |\n| `agent` | `list/get/create/update/archive/restore` | Quản lý agent | `--name`, `--runtime-id` (bắt buộc với `create`), `--instructions`, `--model`, `--thinking-level`, `--mcp-config`, `--permission-mode`, `--max-concurrent-tasks` |\n| | `copy <agent-id>` | Sao chép thành agent mới; agent gốc không đổi | `--name` (mặc định là tên gốc kèm ` (copy)`), `--runtime-id` (sao chép sang runtime khác còn cần `--model`), `--no-skills`; cấu hình bí mật như `custom_env`, `mcp_config` và `runtime_config` không được sao chép — cung cấp lại bằng cùng các cờ như `create` |\n| | `tasks <id>` | Xem task của một agent | |\n| | `avatar <id>` | Tải lên avatar | |\n| | `env get/set <agent-id>` | Đọc và ghi biến môi trường tùy chỉnh (chỉ owner và admin) | |\n| | `skills list/set/add <agent-id>` | Quản lý skill được gắn | `--skill-ids` (`set` thay toàn bộ danh sách, `add` nối thêm) |\n| `autopilot` | `list/get/create/update/delete` | Quản lý Tự động hóa | `create`: `--title`, `--agent`, `--mode` (đều bắt buộc), `--priority`, `--project`, `--subscriber` (lặp lại được) |\n| | `trigger <id>` | Kích hoạt một lượt chạy thủ công | |\n| | `runs <id>` | Xem lịch sử chạy | |\n| | `trigger-add/trigger-update/trigger-delete/trigger-rotate-url` | Quản lý trigger theo lịch và webhook | |\n| `workspace` | `list/get/create/update/switch` | Quản lý không gian làm việc; `switch` đặt không gian làm việc mặc định cho profile hiện tại | |\n| | `member list/invite <email>` | Xem thành viên, gửi lời mời | `invite`: `--role` (member hoặc admin, mặc định member) |\n| `repo` | `list/add/remove/checkout` | Quản lý repo của không gian làm việc và checkout về máy | `--url` (lặp lại được); `checkout`: `--ref` |\n| `skill` | `list/get/create/update/delete` | Quản lý skill | |\n| | `import` | Nhập skill từ URL hoặc file cục bộ | `--url` / `--file`, `--on-conflict` (`fail`, `overwrite`, `rename`, `skip`; mặc định `fail`) |\n| | `search <query>` | Tìm skill | |\n| | `files list/upsert/delete <skill-id>` | Quản lý các file bên trong một skill | |\n| `squad` | `list/get/create/update/delete` | Quản lý đội (`delete` là lưu trữ) | |\n| | `member list/add/set-role/remove` | Quản lý thành viên đội | |\n| | `activity <issue-id> <outcome>` | Ghi một đánh giá của leader | |\n| `chat` | `history`, `thread [id]` | Đọc phiên chat bên ngoài mà một agent đang xử lý | `--limit`, `--before` |\n\n### Runtime\n\n| Lệnh | Lệnh con | Mục đích | Cờ chính |\n| --- | --- | --- | --- |\n| `daemon` | `start` | Khởi động daemon cục bộ | `--foreground`, `--device-name`, `--runtime-name`, `--poll-interval`, `--heartbeat-interval`, `--agent-timeout` (`0` nghĩa là không giới hạn), `--max-concurrent-tasks`, `--no-auto-update`; mỗi cờ có biến môi trường `MULTICA_*` tương ứng |\n| | `stop` / `status` / `restart` | Dừng, kiểm tra trạng thái, khởi động lại (`restart` nhận cùng cờ với `start`) | |\n| | `logs` | Xem log daemon | `--follow`, `--lines` |\n| | `disk-usage` | Xem mức dùng đĩa cục bộ | `--by-workspace`, `--by-task`, `--top` |\n| `runtime` | `list/usage/activity/update/rename/delete` | Xem và quản lý runtime | `delete`: `--cascade` (gỡ gắn kết các agent, giữ dữ liệu của chúng và hủy task của chúng) |\n| | `profile list/create/update/delete` | Quản lý runtime profile tùy chỉnh | |\n| | `profile set-path/unset-path <profile-id>` | Ghim đường dẫn thực thi cục bộ (chỉ cục bộ, không bao giờ tải lên máy chủ) | |\n\n### Khác\n\n| Lệnh | Lệnh con | Mục đích | Cờ chính |\n| --- | --- | --- | --- |\n| `auth` | `status` / `logout` | Kiểm tra trạng thái xác thực; `logout` chỉ xóa token lưu cục bộ, không thu hồi token trên máy chủ | |\n| `user` | `profile get/update` | Xem hoặc cập nhật hồ sơ người dùng hiện tại | |\n| `login` | — | Đăng nhập qua trình duyệt và tự động cấu hình mọi không gian làm việc | `--token` (nhắc nhập tương tác trong terminal khi truyền không kèm giá trị) |\n| `setup` | `cloud` (mặc định), `self-host` | Lưu địa chỉ máy chủ, hoàn tất đăng nhập và khởi động daemon | `self-host`: `--server-url`, `--app-url`, `--port`, `--frontend-port` |\n| `attachment` | `download <attachment-id>`, `upload <path>` | Tải xuống hoặc tải lên tệp đính kèm | `download`: `--output-dir`; `upload`: `--task` |\n| `config` | `show`, `set <key> <value>` | Xem hoặc sửa cấu hình cục bộ của profile hiện tại | Thứ tự ưu tiên: cờ dòng lệnh > biến môi trường > `config.json` > mặc định tích hợp sẵn; đặt chuỗi rỗng để xóa một giá trị |\n| `update` | — | Nâng CLI lên phiên bản mới nhất | |\n| `version` | — | In thông tin phiên bản | `--output` (`text` hoặc `json`) |\n\n## Bước tiếp theo\n\n- [Xác thực và token](/auth-tokens) — tạo, gia hạn và thu hồi PAT.\n- [Tạo và cấu hình agent](/agents-create) — ngữ nghĩa đầy đủ của mọi trường trong `agent create`."
        },
        {
          "slug": "auth-tokens",
          "title": "Xác thực và token",
          "content": "Trong sử dụng hằng ngày với UniAI, bạn chủ yếu gặp hai loại thông tin xác thực: phiên đăng nhập trình duyệt và personal access token. Phiên trình duyệt phục vụ ứng dụng web; personal access token phục vụ CLI, daemon, script và API.\n\n## Phiên đăng nhập trình duyệt\n\nSau khi đăng nhập bằng mã xác thực qua email hoặc Google, UniAI lưu một JWT trong cookie HttpOnly tên `multica_auth`. Trình duyệt tự động gửi nó; JavaScript không đọc trực tiếp được.\n\nPhiên kéo dài 30 ngày theo mặc định. Quản trị viên tự host có thể chỉnh vòng đời của các phiên cấp từ đó về sau bằng `AUTH_TOKEN_TTL`. Đăng xuất sẽ xóa cookie xác thực và cookie CSRF trong trình duyệt hiện tại.\n\nCookie trình duyệt không dành để chép vào script hay CLI; hãy dùng personal access token để truy cập UniAI từ terminal.\n\n## Personal access token\n\nPersonal access token (PAT) bắt đầu bằng `mul_` và đại diện cho tài khoản của bạn. Nó truy cập được mọi không gian làm việc và API mà bạn có quyền, nên hãy giữ nó như giữ mật khẩu.\n\nKhi tạo token ở **Cài đặt → API Token**, bạn đặt tên và chọn hạn dùng 30 ngày, 90 ngày, 1 năm hoặc vĩnh viễn; 90 ngày được chọn sẵn. Token đầy đủ chỉ hiển thị đúng một lần; sau khi hộp thoại đóng, UniAI chỉ giữ lại:\n\n- hash của token;\n- vài ký tự đầu, để nhận diện;\n- tên, thời điểm tạo, hạn dùng và lần dùng cuối.\n\nGiá trị đầy đủ không thể khôi phục. Nếu làm mất, hãy thu hồi token cũ và tạo cái mới.\n\n> Đừng đặt PAT vào repo, issue, bình luận, ảnh chụp màn hình hay log, và đừng truyền nó trực tiếp trong các lệnh shell được lưu lại.\n\n## CLI và PAT\n\nKhi bạn chạy `uniai login`, CLI hoàn tất đăng nhập qua trình duyệt, rồi tạo một PAT có hạn 90 ngày và lưu vào file cấu hình của profile hiện tại:\n\n```text\n~/.multica/config.json\n~/.multica/profiles/<name>/config.json\n```\n\nDaemon kết nối tới UniAI bằng chính PAT này. PAT `mul_` có hạn dùng sẽ tự gia hạn khi còn dưới 7 ngày, kéo dài thành 90 ngày tính từ thời điểm đó. Gia hạn thất bại thì token giữ nguyên; một khi token đã hết hạn hoặc bị thu hồi, hãy chạy `uniai login` lại.\n\nTrên máy không có trình duyệt, hãy tạo PAT trên web trước rồi để CLI hỏi nhập nó một cách an toàn:\n\n```bash\nuniai login --token\n```\n\n## Dùng PAT trong request API\n\nĐặt PAT vào header `Authorization`:\n\n```bash\nexport MULTICA_TOKEN='mul_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'\n\ncurl https://uniai.unicomhub.com/api/me \\\n  -H \"Authorization: Bearer $MULTICA_TOKEN\"\n```\n\nCác lệnh gọi API mức không gian làm việc còn cần chỉ định không gian làm việc, theo yêu cầu của endpoint:\n\n```bash\ncurl https://uniai.unicomhub.com/api/issues \\\n  -H \"Authorization: Bearer $MULTICA_TOKEN\" \\\n  -H \"X-Workspace-ID: $MULTICA_WORKSPACE_ID\"\n```\n\nTrong script, hãy lấy token từ một secret manager hoặc biến môi trường được bảo vệ — không bao giờ hardcode. Trên bản tự host, thay domain bằng địa chỉ API công khai của bạn.\n\n## Đăng xuất và thu hồi\n\n`uniai auth logout` chỉ xóa PAT lưu trong profile CLI hiện tại; đăng xuất trên web chỉ xóa cookie của trình duyệt hiện tại. Cả hai đều không thu hồi personal access token trên server.\n\nNếu token có khả năng đã lộ, hãy thu hồi ngay ở **Cài đặt → API Token**. Sau khi thu hồi, PAT vĩnh viễn không dùng được nữa, và mọi máy hay script khác đã lưu nó cũng mất quyền truy cập.\n\n## Token tạm thời cho lượt chạy của agent\n\nKhi daemon nhận một task, server tạo một token tạm thời cho lượt chạy đó, tiền tố `mat_`. Nó bị gắn với người dùng, không gian làm việc, agent và task hiện tại, có hiệu lực tối đa 24 giờ, và được dọn dẹp khi task kết thúc.\n\nDaemon tiêm token tạm thời này vào công cụ lập trình AI thay vì trao PAT của người dùng cho agent. Nhờ vậy các request agent thực hiện được ghi nhận là hành động của agent, và token này không dùng được để chạm tới các thao tác nhạy cảm dành riêng cho người dùng hoặc owner.\n\nCác token này do server tự động tạo; người dùng không bao giờ cần lưu hay quản lý chúng.\n\n## Các thông tin xác thực máy khác\n\nServer còn nhận diện hai loại thông tin xác thực dùng trong các kịch bản nội bộ hoặc được quản lý hộ:\n\n| Tiền tố | Mục đích | Ai quản lý |\n| --- | --- | --- |\n| `mcn_` | Kết nối UniAI Cloud Node | UniAI Cloud Fleet |\n| `mdt_` | Giao thức xác thực daemon phạm vi không gian làm việc | Các luồng nội bộ của server |\n\nCác bản cài Cloud và tự host thông thường không bao giờ cần tự tạo chúng. CLI và daemon phía người dùng luôn dùng PAT `mul_`; đừng tự dựng token với tiền tố khác.\n\n## Bước tiếp theo\n\n- [Dùng CLI](/cli) — `login`, `auth status` và cấu hình profile.\n- [Daemon và runtime](/daemon-runtimes) — nơi diễn ra việc gia hạn PAT."
        }
      ]
    }
  ]
};
