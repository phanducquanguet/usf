// AUTO-GENERATED by scripts/generate-docs-content.mjs.
// Do not edit by hand — edit apps/docs/content/docs/*.mdx
// and run `pnpm generate:docs`.

export interface DocPage {
  slug: string;
  title: string;
  content: string;
}

export interface DocSection {
  label: string | null;
  /** Key into layout.json help.sections for the localized label */
  labelKey: string | null;
  pages: DocPage[];
}

export type DocsLang = "en" | "vi";

export const DOC_SECTIONS_BY_LANG: Record<DocsLang, DocSection[]> = {
  "en": [
    {
      "label": null,
      "labelKey": null,
      "pages": [
        {
          "slug": "index",
          "title": "Welcome",
          "content": "Multica is a task collaboration platform where humans and AI [agents](/agents) work together in the same [workspace](/workspaces). You can [assign an issue to an agent](/assigning-issues) the way you'd hand work to a teammate — it executes the work, reports progress, and replies in the comments. You can also [open a chat window and talk to it directly](/chat), asking it to draft an issue, answer a question, or handle a one-off request.\n\nThis page explains where agents run and the ways you can start using Multica.\n\n## Where agents run\n\nAgents do **not** execute tasks on Multica's servers. Multica currently supports one runtime model:\n\n- **Local [daemon](/daemon-runtimes)** — you run `multica daemon` on your own machine, and it drives the [AI coding tools](/providers) installed locally. Fourteen are built in today: [Antigravity](/providers#antigravity), [Claude Code](/providers#claude-code), [CodeBuddy](/providers#codebuddy), [Codex](/providers#codex), [Cursor](/providers#cursor), [Copilot](/providers#copilot), [Hermes](/providers#hermes), [Kimi](/providers#kimi), [Kiro CLI](/providers#kiro-cli), [OpenCode](/providers#opencode), [OpenClaw](/providers#openclaw), [Pi](/providers#pi), [Qoder](/providers#qoder), [Trae CLI](/providers#trae). Your API keys, toolchain, and code directories stay on your machine.\n\n> **Cloud runtimes are coming**, currently waitlist-only. Once live, you won't need a local daemon — agent tasks will execute on Multica Cloud directly.\n\n## Two ways to use Multica\n\nBoth cards are **backend choices** — where the Multica server runs.\n\n  **01 — [Multica Cloud](/cloud-quickstart) · Waitlist**\n\nManaged backend. Install the CLI, run the daemon locally, and connect to the Multica-hosted server. Takes about 5 minutes.\n  **02 — [Self-host](/self-host-quickstart) · Docker · Helm**\n\nRun the full backend on your own server with Docker Compose. Database, server, and storage all live on your infrastructure.\n\n## Next steps\n\n  \n    [How Multica works](/how-multica-works) — 30 seconds to read, and it settles the \"server doesn't run agents, agents run on your machine\" point once and for all.\n  \n  \n    Choose one of the two above — [Multica Cloud](/cloud-quickstart) is the fastest, up and running in about 5 minutes.\n  \n  \n    Create an [issue](/issues) and pick an agent as the assignee instead of a teammate. Wait for it to deliver."
        },
        {
          "slug": "how-multica-works",
          "title": "How Multica works",
          "content": "Multica is a **distributed** platform. The web interface you see is just the front of house — the real work is done by three components: the **Multica server** owns the data ([workspaces](/workspaces), [issues](/issues), [members](/members-roles), the [task](/tasks) queue, and so on); the **[daemon](/daemon-runtimes)** runs on your own machine, picks up tasks, and drives the AI coding tool; and the **[AI coding tool](/providers)** (Claude Code, Codex, and other local CLIs) is the component that actually writes code. This is the biggest difference between Multica and Linear or Jira — **[agents](/agents) don't run on our servers, they run on your machine**.\n\n## The three core components\n\n- **Multica server** — the workspaces, issue lists, and comment threads you see all live in its database. It's also a WebSocket hub that pushes real-time updates between you and your teammates. It does **not** execute any agent tasks.\n- **Daemon** — part of the Multica CLI, running on your own machine. On start it detects which AI coding tools are installed locally, registers with the server, and begins polling for tasks every 3 seconds and sending heartbeats every 15 seconds.\n- **AI coding tools** — one of the fourteen (or several in parallel): [Antigravity](/providers#antigravity), [Claude Code](/providers#claude-code), [CodeBuddy](/providers#codebuddy), [Codex](/providers#codex), [Cursor](/providers#cursor), [Copilot](/providers#copilot), [Hermes](/providers#hermes), [Kimi](/providers#kimi), [Kiro CLI](/providers#kiro-cli), [OpenCode](/providers#opencode), [OpenClaw](/providers#openclaw), [Pi](/providers#pi), [Qoder](/providers#qoder), [Trae CLI](/providers#trae). Once the daemon has picked up a task, it uses these tools to actually do the work.\n\nBecause the toolchain stays local, **your API keys, code directories, and authorized tools** are only ever used on your machine — the Multica server never sees any of them. This holds whether you self-host or use Cloud.\n\n## The lifecycle of a task\n\nTake the most common scenario — you assign an issue to an agent:\n\n1. You click assign in the web UI. The browser sends an HTTP request to the Multica server.\n2. The server sets the assignee on that issue to the agent and, at the same time, creates an execution task in the task queue with status `queued`.\n3. The daemon on your machine picks up the task on its next poll (within 3 seconds). Task status becomes `dispatched`.\n4. The daemon creates an isolated working directory locally and invokes the corresponding AI coding tool. Task status becomes `running`.\n5. The AI writes code locally, runs tests, and posts comments back to the server.\n6. Execution ends. The daemon reports the result (success / failure) to the server, and task status becomes `completed` or `failed`. You see the progress update in real time in the web UI (via WebSocket).\n\nFor the detailed mechanics, see [Daemon and runtimes](/daemon-runtimes) and [Tasks](/tasks).\n\n## Four ways to get an agent working\n\nIt's not only \"assign an issue\" — Multica has 4 triggers, one per collaboration style:\n\n| How | Typical scenario | Docs |\n|---|---|---|\n| **Assign an issue** | The most common. Assign an issue to an agent and it starts on its own | [Assigning issues](/assigning-issues) |\n| **@mention an agent in a comment** | \"Take a look at this one for me\" — don't change the assignee or status, just fire off a comment | [Mentioning agents](/mentioning-agents) |\n| **Direct chat** | Standalone conversation, not tied to an issue — ask questions, have it draft an issue | [Chat](/chat) |\n| **Autopilots (scheduled)** | Standing instructions — \"do a standup summary every Monday morning\" and the like | [Autopilots](/autopilots) |\n\n## Runtimes: where it runs, and how many tools\n\nA **runtime** is the pairing of \"daemon × one AI coding tool.\" If the daemon on one machine has both Claude Code and Codex installed and is joined to two workspaces, Multica registers 4 independent runtimes (2 workspaces × 2 tools).\n\nOnly the **local daemon** runtime model is supported today. Cloud runtimes (where you don't need your own machine running) are **coming soon**, currently waitlist-only.\n\n## Next steps\n\n- [Cloud Quickstart](/cloud-quickstart) — connect to Multica Cloud in 5 minutes\n- [Self-Host Quickstart](/self-host-quickstart) — run your own backend\n- [Daemon and runtimes](/daemon-runtimes) — a deep dive into the component the architecture rests on"
        },
        {
          "slug": "cloud-quickstart",
          "title": "Cloud quickstart",
          "content": "This page walks you end-to-end through Multica Cloud — **sign up → install the [CLI](/cli) → start the [daemon](/daemon-runtimes) → create an [agent](/agents) → assign your first [task](/tasks)**. Takes about 5 minutes.\n\nOne prerequisite: you already have at least one [AI coding tool](/providers) installed locally ([Antigravity](/providers#antigravity), [Claude Code](/providers#claude-code), [CodeBuddy](/providers#codebuddy), [Codex](/providers#codex), [Cursor](/providers#cursor), [Copilot](/providers#copilot), [Hermes](/providers#hermes), [Kimi](/providers#kimi), [Kiro CLI](/providers#kiro-cli), [OpenCode](/providers#opencode), [OpenClaw](/providers#openclaw), [Pi](/providers#pi), [Qoder](/providers#qoder), or [Trae CLI](/providers#trae)). The daemon auto-detects them on startup and refuses to start if none are present.\n\n## 1. Create an account\n\nSign up at [multica.ai](https://multica.ai). You can log in with email (6-digit verification code) or Google.\n\nAfter sign-up you're automatically placed in a default workspace (generated from your account name). You can rename it later, or create new workspaces.\n\n## 2. Install the Multica CLI\n\n**macOS / Linux (Homebrew recommended)**:\n\n```bash\nbrew install multica-ai/tap/multica\n```\n\n**macOS / Linux (no Homebrew)**:\n\n```bash\ncurl -fsSL https://raw.githubusercontent.com/multica-ai/multica/main/scripts/install.sh | bash\n```\n\n**Windows (PowerShell)**:\n\n```powershell\nirm https://raw.githubusercontent.com/multica-ai/multica/main/scripts/install.ps1 | iex\n```\n\nVerify the install:\n\n```bash\nmultica version\n```\n\n## 3. Log in + start the daemon\n\nA single command handles login and starts the daemon:\n\n```bash\nmultica setup\n```\n\n`multica setup` will:\n\n1. Configure the CLI to connect to Multica Cloud\n2. Open your browser for login (same email verification code / Google OAuth as the web)\n3. Store the generated PAT in `~/.multica/config.json`\n4. **Start the daemon automatically** — it begins polling for tasks every 3 seconds and sending heartbeats every 15 seconds\n\nVerify the daemon is running:\n\n```bash\nmultica daemon status\n```\n\n`online` means it has registered with the server.\n\n## 4. Verify the runtime is online\n\nIn the web UI, go to **Settings → Runtimes**. The daemon you just started should appear as one or more active runtimes — one per AI coding tool installed locally.\n\nIf it shows as offline, don't panic — see [Troubleshooting → Daemon can't connect to the server](/troubleshooting#daemon-cant-connect-to-the-server).\n\n## 5. Create an agent\n\nIn the web UI, go to **Settings → Agents** and click **New Agent**:\n\n- **Name** — the name shown for this agent on boards and in comments. Pick something you like\n- **Provider** — choose an AI coding tool you have installed locally (the dropdown only lists tools detected by your runtimes)\n- **Model** (optional) — the model selection inside that tool (a static list or dynamic discovery, depending on the provider)\n- **Instructions** (optional) — system prompt for this agent\n\nOnce created, the agent shows up in your workspace member list and can be assigned work like a human member.\n\n## 6. Assign your first task\n\nCreate an issue in the web UI, or from the CLI:\n\n```bash\nmultica issue create --title \"Add an ASCII architecture diagram to the README\"\n```\n\nAssign the issue to the agent you just created — click its avatar in the web UI, or use the CLI:\n\n```bash\nmultica issue assign MUL-1 --to my-agent-name\n```\n\n`--to` takes the **name** of an agent or member. A substring match works — if the agent is called `my-code-reviewer`, `reviewer` resolves to it. If your workspace has overlapping names, pass `--to-id <uuid>` instead (mutually exclusive with `--to`); look up the UUID via `multica agent list --output json` or `multica workspace member list --output json`.\n\n**What happens next from the daemon**:\n\n1. It picks up the task within 3 seconds (status goes from `queued` to `dispatched`)\n2. It invokes the matching AI coding tool to start work (status becomes `running`)\n3. The AI works locally — it may read your code directory, run commands, edit files\n4. When done, it reports the result back to Multica (status becomes `completed` or `failed`, depending on whether auto-retry kicks in)\n\nThe web UI updates in **real time** (via WebSocket) — no refresh needed.\n\n## Next steps\n\n- [Daemon and runtimes](/daemon-runtimes) — how the daemon operates and what runtimes mean\n- [Tasks](/tasks) — task lifecycle and retry rules\n- [AI coding tools compared](/providers) — capability differences across the 14 tools\n- [Self-host quickstart](/self-host-quickstart) — run your own backend"
        },
        {
          "slug": "self-host-quickstart",
          "title": "Self-host quickstart",
          "content": "This page walks you through running the Multica **server** (backend + frontend + PostgreSQL) on your own machine or server with Docker. When you're done, your data is fully under your control — including [workspaces](/workspaces), [issues](/issues), [comments](/comments), and [agent](/agents) configuration.\n\nAgent **execution** still relies on the [daemon](/daemon-runtimes) you run locally plus the [AI coding tools](/providers) installed on that machine — exactly like Cloud. Self-host swaps out the server layer, not the execution layer.\n\n## Prerequisites\n\n- **Docker** installed and able to run `docker compose`\n- **Git** (optional, but recommended so you can pull the source)\n- A machine that can stay up (local / internal network / cloud host all work)\n- At least one AI coding tool installed on **the machine running the daemon** (not necessarily the one running the server — your dev laptop works)\n\n## 1. Pull the project and start the backend\n\n> **Already on Kubernetes?** Skip Docker and use the Helm chart instead — jump to [Kubernetes deployment](#kubernetes-deployment-alternative) below, then come back to [Step 4](#4-first-login--create-a-workspace) for first login.\n\n```bash\ngit clone https://github.com/multica-ai/multica.git\ncd multica\nmake selfhost\n```\n\n`make selfhost` will:\n\n1. Generate a `.env` from `.env.example` if missing, with a **random JWT_SECRET and Postgres password**\n2. Pull the official Docker images (PostgreSQL, Multica backend, Multica frontend)\n3. Bring up every service using `docker-compose.selfhost.yml`\n4. Wait until the backend's `/health` endpoint is ready\n\nFor ongoing production probes after startup, use `/readyz` when you want the\ncheck to fail on database or migration problems.\n\nThe backend container **runs database migrations automatically** on startup (`docker/entrypoint.sh` runs `./migrate up` before the server starts) — you'll see the migration output in the backend logs. Version upgrades are handled the same way.\n\n> **Image not published yet?** If `make selfhost` fails to pull images, you may be on an unreleased version tag. Switch to a stable release, or build from source: `make selfhost-build`.\n\nOnce it's up:\n\n- **Frontend**: [http://localhost:3000](http://localhost:3000)\n- **Backend**: [http://localhost:8080](http://localhost:8080)\n\n> **Ports listen on `127.0.0.1` only.** `docker-compose.selfhost.yml` binds every published port to loopback — `ss -tlnp` will not show `0.0.0.0:8080`, and the services are unreachable from other machines by design. Secrets and Postgres credentials must never sit on the open internet. For cross-machine access, front the stack with a reverse proxy that terminates TLS — see [Step 5b — Cross-machine: front with a reverse proxy](#5b-cross-machine-front-with-a-reverse-proxy).\n\n## 2. Important: keep production safety on\n\n> **`docker-compose.selfhost.yml` sets `APP_ENV` to `production` by default** and leaves `MULTICA_DEV_VERIFICATION_CODE` empty, so there is no fixed code on public instances.\n>\n> Only set `MULTICA_DEV_VERIFICATION_CODE` for local or private test automation. If a fixed code is enabled while `APP_ENV` is non-production, anyone who can request a code can sign in with that fixed value. See [Auth setup → Fixed local testing codes](/auth-setup#fixed-local-testing-codes).\n>\n> Before any public deployment, make sure `.env` has `APP_ENV=production` and `MULTICA_DEV_VERIFICATION_CODE` is empty.\n\n## 3. Configure the email service (optional but recommended)\n\nWithout email configured, your users can't receive verification codes by email; the server prints generated codes to stdout instead.\n\nTwo delivery backends are supported — pick whichever fits your network:\n\n**Option A — Resend (cloud / public-internet deployments):**\n\n1. Sign up at [Resend](https://resend.com/) and get an API key\n2. Verify a sending domain you control\n3. Set these in `.env`:\n\n    ```bash\n    RESEND_API_KEY=re_xxxxxxxxxxxx\n    RESEND_FROM_EMAIL=noreply@yourdomain.com\n    ```\n\n**Option B — SMTP relay (internal networks / on-premise):**\n\nUse this when the deployment can't reach `api.resend.com`, or you already have an internal mail relay (Microsoft Exchange, Postfix, on-prem SendGrid, etc.). `SMTP_HOST` takes priority over Resend when both are set, so verification and invite mail stays on the internal relay. STARTTLS is upgraded automatically when advertised; port `465` (SMTPS / implicit TLS) auto-enables an immediate TLS handshake, and `SMTP_TLS=implicit` (aliases: `smtps`, `ssl`) forces it on a non-standard SMTPS port.\n\nFor **anonymous Exchange internal relay (port 25)** — the host is trusted by IP and submits without credentials:\n\n```bash\nSMTP_HOST=exchange.internal.example.com\nSMTP_PORT=25\nSMTP_USERNAME=\nSMTP_PASSWORD=\nSMTP_TLS_INSECURE=false\nRESEND_FROM_EMAIL=noreply@yourdomain.com  # reused as the From: header\n```\n\nFor **authenticated submission (port 587, STARTTLS)** — the relay requires a service account; STARTTLS is upgraded automatically when advertised:\n\n```bash\nSMTP_HOST=smtp.internal.example.com\nSMTP_PORT=587\nSMTP_USERNAME=multica\nSMTP_PASSWORD=...\nSMTP_TLS_INSECURE=false        # set true only for private CA / self-signed\nRESEND_FROM_EMAIL=noreply@yourdomain.com\n```\n\nFor **implicit TLS / SMTPS (port 465)** — providers like Aliyun / Tencent enterprise mail that don't advertise STARTTLS. Port `465` auto-enables implicit TLS, so `SMTP_TLS` is optional here:\n\n```bash\nSMTP_HOST=smtp.qiye.aliyun.com\nSMTP_PORT=465\nSMTP_USERNAME=multica@yourdomain.com\nSMTP_PASSWORD=...\nSMTP_TLS=implicit              # optional on 465; required on a non-standard SMTPS port\nRESEND_FROM_EMAIL=noreply@yourdomain.com\n```\n\nFor **strict public relays (e.g. Google Workspace `smtp-relay.gmail.com`)** that reject the default `localhost` greeting from a public IP, set `SMTP_EHLO_NAME` to the FQDN the relay expects — otherwise the connection is dropped and surfaces as an opaque `EOF` on a later command. It defaults to the container hostname, which is usually not a valid FQDN:\n\n```bash\nSMTP_HOST=smtp-relay.gmail.com\nSMTP_PORT=587\nSMTP_EHLO_NAME=mail.yourdomain.com   # FQDN the relay accepts; defaults to the (non-FQDN) container hostname\nRESEND_FROM_EMAIL=noreply@yourdomain.com\n```\n\nThen restart: `docker compose -f docker-compose.selfhost.yml restart backend`. On restart, the backend prints which provider it picked and the negotiated TLS mode (`EmailService: SMTP relay <host>:<port> (starttls|implicit-tls) from=…` / `Resend API` / `DEV mode`) — credentials are never logged, so this line is safe to share when asking for help.\n\nFor more auth configuration (OAuth, signup allowlist) and the full SMTP variable reference, see [Auth setup](/auth-setup) and [Environment variables → Email](/environment-variables#email-configuration).\n\n## 4. First login + create a workspace\n\nOpen [http://localhost:3000](http://localhost:3000):\n\n- Enter your email\n- Grab the verification code from your configured email backend (Resend or SMTP relay); if neither is configured, copy it from the server container stdout — look for the `[DEV] Verification code` line\n- Do not use `888888` unless you explicitly set `MULTICA_DEV_VERIFICATION_CODE=888888` on a non-production private instance\n- Log in and create your first workspace\n\n## 5. Point the CLI at your own server\n\nThe CLI install is the same as in [Cloud quickstart → 2. Install the CLI](/cloud-quickstart#2-install-the-multica-cli) — Homebrew / script / PowerShell, pick one.\n\n### 5a. Same machine\n\nIf the CLI and the server run on the same host, the defaults already work:\n\n```bash\nmultica setup self-host\n```\n\nThat points the CLI at `http://localhost:8080` (backend) and `http://localhost:3000` (frontend), takes you through browser login, stores the PAT locally, and **starts the daemon automatically**.\n\n### 5b. Cross-machine: front with a reverse proxy\n\nBecause the compose stack only listens on `127.0.0.1`, a daemon on a different machine cannot reach `http://<server-ip>:8080` directly — and you do not want it to, since server secrets would otherwise be reachable from the open internet. Put a reverse proxy on the server that terminates TLS and forwards to `127.0.0.1:8080` (backend) and `127.0.0.1:3000` (frontend), then point the CLI at the public HTTPS URL:\n\n```bash\nmultica setup self-host \\\n  --server-url https://<your-domain> \\\n  --app-url https://<your-domain>\n```\n\n> Prefer environment variables over flags? `setup self-host` reads `MULTICA_SERVER_URL` and `MULTICA_APP_URL` when the matching flag is omitted — a flag still takes precedence over the env var. `MULTICA_SERVER_URL` also accepts the `ws://…/ws` daemon form from [Environment variables](/environment-variables) and normalizes it to the HTTP base.\n\nA minimal Caddyfile that fronts both the frontend and the backend (with WebSocket support, which the daemon and the web app both need) on a single hostname:\n\n```nginx\nmultica.example.com {\n    # WebSocket route — must come before the catch-all\n    @ws path /ws /ws/*\n    handle @ws {\n        reverse_proxy 127.0.0.1:8080 {\n            flush_interval -1\n        }\n    }\n\n    # Backend API\n    handle /api/* {\n        reverse_proxy 127.0.0.1:8080\n    }\n\n    # Everything else → frontend\n    reverse_proxy 127.0.0.1:3000\n}\n```\n\nAfter bringing the proxy up, set `FRONTEND_ORIGIN=https://multica.example.com` in the server's `.env` and restart the backend — otherwise the WebSocket origin check will reject the browser ([Troubleshooting → WebSocket can't connect](/troubleshooting#websocket-cant-connect)).\n\n[Cloudflare Tunnel](https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/) is another solid option — it gives you TLS and a public hostname without exposing any port on the host at all. An Nginx equivalent (separate `app.` / `api.` hostnames, `proxy_set_header Upgrade` for WebSockets) works just as well; the key requirements are TLS termination and forwarding the `Upgrade` header on `/ws`.\n\n## 6. Create an agent + assign your first task\n\nSame flow as Cloud — see [Cloud quickstart → Steps 5-6](/cloud-quickstart#5-create-an-agent).\n\n## 7. Usage rollup (no operator action required)\n\n> The Usage / Runtime dashboards read from a derived `task_usage_hourly` table populated by `rollup_task_usage_hourly()`. As of MUL-2957 the backend runs this rollup in-process via the DB-backed scheduler — `pg_cron` is no longer required, and external cron / systemd timers are no longer the recommended setup. The bundled `pgvector/pgvector:pg17` image works without changes.\n\nThe in-process scheduler ticks every 30 seconds and claims a 5-minute UTC plan via the `sys_cron_executions` table. Multiple backend replicas are safe — the unique key `(job_name, scope_kind, scope_id, plan_time)` means only one wins each plan. No setup is needed for new deployments.\n\n**Compatibility — existing `pg_cron` registrations.** If you previously registered the rollup as a `pg_cron` job (`SELECT cron.schedule('rollup_task_usage_hourly', '*/5 * * * *', …)`), you do not need to remove it — the SQL function holds advisory lock 4246 internally, so the app scheduler and `pg_cron` cannot double-write. To drop the redundant entry:\n\n```sql\nSELECT cron.unschedule('rollup_task_usage_hourly')\n  FROM cron.job WHERE jobname = 'rollup_task_usage_hourly';\n```\n\n**Upgrade from `v0.3.4 → v0.3.5+`.** The previous release asked operators to run `cmd/backfill_task_usage_hourly` manually before applying migration 103, otherwise the migration's fail-closed guard would abort `migrate up`. As of MUL-2957 this is automatic: the migrate command runs an idempotent monthly-slice backfill (under advisory lock 4246) immediately before applying migration 103, then continues. You may still run the standalone backfill on a busy DB to throttle read pressure with `--sleep-between-slices=2s`, but it is no longer required.\n\nFull reference — including operations notes and the Kubernetes deployment shape — lives in the repo's `SELF_HOSTING_ADVANCED.md → Usage Dashboard Rollup`.\n\n## Kubernetes deployment (alternative)\n\nIf you already run a Kubernetes cluster, the repo also ships a Helm chart at `deploy/helm/multica/`. It's the equivalent of `make selfhost` for k8s — same backend image, frontend image, and `pgvector/pgvector:pg17` Postgres, packaged as Deployments / Services / Ingresses with one `ConfigMap` rendered from `values.yaml`. Authored against k3s + Traefik + `local-path` and should work on any cluster with an Ingress controller and a default `ReadWriteOnce` StorageClass.\n\nThe chart **does not template secret values**. It references a Secret named `multica-secrets` by name, so real JWT / DB / Resend / Google keys never need to live in git or in `values.yaml`. Create the namespace + Secret once with kubectl:\n\n```bash\nkubectl create namespace multica\n\nkubectl -n multica create secret generic multica-secrets \\\n  --from-literal=JWT_SECRET=\"$(openssl rand -hex 32)\" \\\n  --from-literal=POSTGRES_PASSWORD=\"$(openssl rand -hex 16)\" \\\n  --from-literal=RESEND_API_KEY=\"\" \\\n  --from-literal=GOOGLE_CLIENT_SECRET=\"\" \\\n  --from-literal=CLOUDFRONT_PRIVATE_KEY=\"\" \\\n  --from-literal=MULTICA_DEV_VERIFICATION_CODE=\"\"\n```\n\nThen install the chart:\n\n```bash\ngit clone https://github.com/multica-ai/multica.git\ncd multica\nhelm install multica deploy/helm/multica -n multica\n```\n\nDefaults assume the hostnames `multica.dev.lan` (web) and `api.multica.dev.lan` (backend). Add them to `/etc/hosts` (or local DNS) pointing at any node IP where your Ingress is reachable. To use different hostnames, copy `deploy/helm/multica/values.yaml`, edit `ingress.frontend.host` / `ingress.backend.host` and the matching `backend.config.appUrl` / `frontendOrigin` / `localUploadBaseUrl` / `googleRedirectUri`, then install with `-f my-values.yaml`.\n\nOn a cold cluster the backend can stay `Running` but not `Ready` for a few minutes while it waits on Postgres and runs migrations — a startupProbe absorbs this, so the pod should not restart. Once it's `Ready`:\n\n```bash\ncurl -H \"Host: api.multica.dev.lan\" http://<ingress-ip>/healthz\n# {\"status\":\"ok\",\"checks\":{\"db\":\"ok\",\"migrations\":\"ok\"}}\n```\n\nThen open `http://multica.dev.lan` and continue at [Step 4 — First login](#4-first-login--create-a-workspace) above. Point the CLI at your Ingress hostnames:\n\n```bash\nmultica setup self-host \\\n  --server-url http://api.multica.dev.lan \\\n  --app-url http://multica.dev.lan\n```\n\nTo pull the latest images without changing the chart, `kubectl -n multica rollout restart deploy/multica-backend deploy/multica-frontend`. To pin a specific Multica release, set `images.backend.tag` / `images.frontend.tag` in your values file and `helm upgrade`. `helm -n multica uninstall multica` removes the workloads but keeps the PVCs and Secret; `kubectl delete namespace multica` wipes everything.\n\nThe full reference — three login modes, the `backend` ExternalName workaround for the build-time-baked `REMOTE_API_URL` in the web image, resource limits, and TLS — lives in the repo's `SELF_HOSTING.md`.\n\n## Common issues\n\n- **Backend won't start**: check container logs with `docker compose -f docker-compose.selfhost.yml logs backend`; usually it's a bad `DATABASE_URL` or `JWT_SECRET` in `.env`\n- **Verification code not received**: no email backend is configured (neither Resend nor SMTP) → look for `[DEV] Verification code` in `docker compose logs backend`\n- **WebSocket won't connect**: for public deployments you must set `FRONTEND_ORIGIN` to your real frontend domain; see [Troubleshooting → WebSocket won't connect](/troubleshooting#websocket-wont-connect)\n- **Usage / Runtime dashboard stays at zero**: `rollup_task_usage_hourly()` isn't being scheduled — see [Step 7](#7-usage-rollup-no-operator-action-required) above and [Troubleshooting → Usage dashboard shows zero](/troubleshooting#usage-dashboard-stays-at-zero)\n- **`migrate up` fails with `refusing to drop legacy daily rollups`**: upgrade-path guard from `v0.3.4 → v0.3.5+`. As of MUL-2957 the migrate command runs the backfill automatically before applying migration 103 — see [Step 7](#7-usage-rollup-no-operator-action-required)\n\n## Next steps\n\n- [Environment variables](/environment-variables) — full env reference\n- [Auth setup](/auth-setup) — Resend / OAuth / signup allowlist in detail\n- [GitHub integration](/github-integration) — connect a GitHub App so PRs auto-link to issues and merging closes them\n- [Troubleshooting](/troubleshooting) — start here when things go wrong"
        }
      ]
    },
    {
      "label": "Workspace & team",
      "labelKey": "workspace_team",
      "pages": [
        {
          "slug": "workspaces",
          "title": "Workspaces",
          "content": "A workspace is **the self-contained space where a group collaborates in Multica** — every [issue](/issues), [member](/members-roles), [comment](/comments), and [agent](/agents) belongs to one. The issue list, member roster, and agent configuration you see after logging in are all scoped to the current workspace; **switching workspaces replaces the entire view**.\n\n## Creating a workspace\n\nThree things get decided when you create a workspace:\n\n- **Workspace name** — the display name members see. Spaces and non-ASCII characters are allowed. You can change it later.\n- **Slug** — the string used in the workspace URL. Lowercase letters and digits only (joined with `-`). **It cannot be changed after creation**, so pick carefully. If the slug is taken or hits a system-reserved word, the create screen will ask you to choose another.\n- **Issue prefix** — the prefix for every issue number in the workspace (the `MUL` in `MUL-123`). Uppercase letters and digits, up to 10 characters.\n\n> **Avoid changing the issue prefix.** Issue numbers are rendered with the current prefix — change it and `MUL-5` instantly becomes `NEW-5`. Every external link, Slack mention, and historical reference in comments breaks against the old number. Treat the issue prefix as \"set at creation, never touched.\"\n\nYou can create a workspace from the web UI or from the command line:\n\n```bash\nmultica workspace create\n```\n\n## Issue numbers\n\nEvery issue created in a workspace is automatically assigned a number in the format `<prefix>-<digits>` — `MUL-1`, `MUL-2`, `MUL-3`. A few properties:\n\n- **Sequential and unique within a workspace** — each workspace keeps its own counter; workspaces don't interfere with each other.\n- **Not manually assignable** — when you create an issue you only supply a title; the number is assigned by the system.\n- **Never reclaimed on delete** — delete `MUL-5` and the next new issue is `MUL-6`, not `MUL-5`.\n\n## Deleting a workspace\n\nOnly a workspace owner can delete the entire workspace. Deletion is **irreversible**.\n\n> Deleting a workspace wipes the following all at once:\n>\n> - Every issue, project, comment, and reaction\n> - Every attachment\n> - Every membership and pending invitation\n> - Every agent configuration along with its task history\n>\n> **Data cannot be recovered.** Export anything you need to keep before deleting.\n\nIf you're the last owner of a workspace and want to walk away from it, transfer the owner role to another member first, then have the new owner (or you) decide whether to delete. See [Members and roles](/members-roles).\n\n## Next\n\n- [Members and roles](/members-roles) — how to add people to a workspace, and what each of the three roles can do\n- [Issues and projects](/issues) — the core work objects inside a workspace"
        },
        {
          "slug": "members-roles",
          "title": "Members and roles",
          "content": "Everyone in a [workspace](/workspaces) has a role, and the role decides what they can do. Multica has three: **owner** (the workspace's owner), **admin**, and **member**. Most day-to-day work — creating [issues](/issues), writing [comments](/comments), using [agents](/agents) — is available to all three roles. **The differences cluster around team management.**\n\n## Permissions at a glance\n\nThe table below lists the most important differences across team-management actions:\n\n| Action | owner | admin | member |\n|---|---|---|---|\n| Invite a new admin or member | ✓ | ✓ | ✗ |\n| **Invite a new owner** | ✓ | ✗ | ✗ |\n| Demote / remove an admin or member | ✓ | ✓ | ✗ |\n| **Demote / remove another owner** | ✓ | ✗ | ✗ |\n| Delete the workspace | ✓ | ✗ | ✗ |\n\n**Members can't invite anyone** — inviting is an admin-tier permission. **Only owners can promote someone to owner** — admins can promote and demote members or other admins, but they can't create a new owner. Likewise, admins can remove members or other admins but **can't touch existing owners**. The point is to make sure the highest tier can only be granted by someone who already holds it — permissions don't leak upward.\n\n> Agent visibility comes in two flavors: \"workspace\" and \"private.\" Private agents can only be assigned to issues by owners and admins — this protects configurations meant for a specific set of people. See [Agents](/agents).\n\n## Inviting a new member\n\nMultica invites new members by email:\n\n1. On the workspace settings page, click **Invite member**, enter the email, and pick a role.\n2. Multica sends an invitation email containing a unique link.\n3. The recipient clicks the link, logs in (or signs up), and **accepts the invitation** to join the workspace.\n\nThe invited email **does not need to be registered with Multica in advance** — if no account exists, one is created when the invitation is accepted.\n\nIf the invitation email fails to deliver (wrong address, mail service hiccup), the invitation record is still retained; you can resend the email from workspace settings, or share the invitation link through another channel.\n\nInvitations are **valid for 7 days**. After that, clicking the link shows an \"expired\" message, and the inviter needs to send a new one.\n\n## Always at least one owner\n\nEvery workspace **must have at least one owner at all times**. This constraint automatically blocks two operations:\n\n- The last owner can't demote themselves.\n- Other owners or admins can't remove the last owner.\n\n> If you're the last owner and about to leave the team, **transfer the owner role to another member first**, then try to leave or hand off the workspace. Otherwise the operation will be rejected.\n\n## Removing a member\n\nOwners and admins can remove other members from a workspace. A removed member loses access immediately; issues, comments, and other content they created are retained in the workspace.\n\n## Next\n\n- [Issues and projects](/issues) — what members work on\n- [Comments and mentions](/comments) — collaborating under an issue"
        },
        {
          "slug": "issues",
          "title": "Issues and projects",
          "content": "An issue is a self-contained unit of work in Multica — a bug, a new feature, a thing that needs doing. Every issue has a **title**, a **description** (Markdown supported), a **status**, a **priority**, an **assignee**, and optionally belongs to a **project**. If you've used Linear or Jira, this is the same shape.\n\n**Multica's defining trait is that an issue's assignee can be a person or an [agent](/agents)** — which is where we'll start.\n\n## Assigning an issue to an agent\n\n[Assigning](/assigning-issues) an issue to an agent hands that work over to it. The agent **starts automatically** — executing within seconds, reporting progress in comments, and flipping the status to done when finished. The only difference from handing work to a teammate is that an agent doesn't go offline, doesn't need reminders, and is available 24/7.\n\n> For agent identity, configuration, and where they run, see [Agents](/agents).\n\nPrivate agents can only be assigned to issues by workspace owners and admins. For role permissions, see [Members and roles](/members-roles).\n\n## Status\n\nMultica has seven statuses. **Any status can move directly to any other** — Multica doesn't impose a workflow, and won't stop you from jumping from `backlog` straight to `done`.\n\n| Status | Meaning |\n|---|---|\n| `backlog` | Not scheduled yet |\n| `todo` | Scheduled, ready to start |\n| `in_progress` | Being worked on |\n| `in_review` | Awaiting review |\n| `done` | Completed |\n| `blocked` | Stuck on an external factor |\n| `cancelled` | Cancelled |\n\nOnce an issue is assigned to an agent, the agent automatically moves the status from `backlog` / `todo` to `in_progress`, then to `done` on completion. You can also change it manually at any time.\n\n## Priority\n\nPriority has five levels, used to order the default issue list:\n\n| Priority | Use |\n|---|---|\n| `No priority` | Not decided yet (default) |\n| `Urgent` | Urgent |\n| `High` | High |\n| `Medium` | Medium |\n| `Low` | Low |\n\n## Issue numbers\n\nEvery issue has a workspace-unique number in the format `<prefix>-<digits>` — for example `MUL-123`. The number is assigned by the system at creation time and **never changes**. See [Workspaces → Issue numbers](/workspaces#issue-numbers).\n\n## Comments\n\nThe comment thread under an issue is where collaboration happens — reply to a comment, `@` a person or agent, add a reaction.\n\n`@` an agent in a comment and **it triggers automatically** — this is the second way to start an agent, alongside \"assign to.\" See [Comments and mentions](/comments) and [Mentioning agents in comments](/mentioning-agents).\n\n## Deleting an issue\n\n> Deleting an issue **immediately** clears every comment, reaction, and attachment under it, along with any queued agent tasks (running tasks are cancelled). **It cannot be undone.**\n>\n> If you just want the issue out of sight, **changing the status to `cancelled` is safer than deleting** — the data stays, and you can pull it back later.\n\n## Projects\n\nA project is a container that groups multiple issues together. An issue belongs to at most one project, or to no project at all.\n\nProjects have their own **lead** — **just like an issue's assignee, a lead can be a person or an agent**.\n\nDeleting a project **does not delete the issues inside it**: those issues simply detach from the project and remain in the workspace.\n\n## Next\n\n- [Comments and mentions](/comments) — collaborating under an issue\n- [Agents](/agents) — understand how \"assign to an agent\" actually works"
        },
        {
          "slug": "projects",
          "title": "Projects",
          "content": "A **project** in Multica is a container for related [issues](/issues). Use it when a body of work is bigger than one issue but smaller than a full workspace — a launch, a migration, a feature with multiple parts, an investigation that branches into several threads.\n\nEach project has a name, an icon, a description, a **lead** (a member or an [agent](/agents)), a **status** (`planned` / `in_progress` / `paused` / `completed` / `cancelled`), a **priority** (`urgent` / `high` / `medium` / `low` / `none`), and a **progress** percentage that's auto-derived from the status of its linked issues.\n\n## How projects relate to issues\n\nProjects and issues are independent objects with a many-to-one relationship: an issue can belong to **at most one** project; a project holds **any number of** issues. Linking and unlinking is reversible at any time — drag in the board view, or use the project picker on the issue's right-side properties panel.\n\nThe progress bar on a project is computed from its linked issues — the more issues hit `done`, the further it fills. Issues that are `cancelled` are excluded from the count; issues in `backlog` count toward the denominator but not the numerator.\n\n## Pinning to the sidebar\n\nClick the pin icon in a project's top-right corner to add it to your sidebar's pinned list. Pinned projects stay one click away no matter where you are in the workspace; everyone on the team can pin independently — pins are personal.\n\nThe sidebar **Workspace → Projects** link always shows every project in the workspace; pinning is a personal shortcut on top of that.\n\n## Attaching resources\n\nEach project has a **Resources** section where you attach GitHub repositories. Once attached, any [agent](/agents) assigned to issues in this project can read and write to those repos when executing tasks — Multica passes the repo URLs as context to the [daemon](/daemon-runtimes).\n\nResources are per-project; if multiple projects share a repo, attach it to each one.\n\n## Deleting a project\n\nDeleting a project **does not delete its issues**. The linked issues are simply unlinked and revert to the workspace's flat issue list. This is intentional — work that was scoped to a project is rarely throwaway, even when the framing of the project changes.\n\n> If you want to delete the work too, archive or delete the issues first, then delete the project.\n\n## Project lead\n\nThe lead is the person — or agent — accountable for the project. It's a soft signal, not an access control: any workspace member can edit a project regardless of who's lead. A project's lead can be:\n\n- A workspace member (human teammate)\n- An [agent](/agents) — useful when the project's work is mostly delegated to an agent (e.g., \"Weekly bug triage\" led by a triage agent)\n\n## Next\n\n- [Issues](/issues) — the unit of work that lives inside projects\n- [Agents as project lead](/agents) — when an agent is the right owner\n- [How Multica works](/how-multica-works) — the broader picture"
        },
        {
          "slug": "comments",
          "title": "Comments and mentions",
          "content": "Every [issue](/issues) has a comment thread. Post comments, reply to someone, `@` a [member](/members-roles) or an [agent](/agents), add reactions — the same moves you make in any task manager you've used. The one difference: **mentioning an agent with `@` triggers it to start working.**\n\n## Posting a comment\n\nType into the input at the bottom of the issue detail page and hit **Send**. The comment appears in the thread immediately. Comments support Markdown — headings, lists, code blocks, links, all available.\n\n## Replying to a comment\n\nClick **Reply** on the top-right of any comment to open a nested input underneath it. Your reply is displayed as a child of that comment, forming a conversation thread. Replies can have their own replies, nesting as deep as you need.\n\nThe issue list shows only the top-level comment count; opening the issue reveals the full conversation tree.\n\n## Reactions\n\nEach comment has a reaction button in the top-right for quick signals (👍, 👀, 🎉) — no need to post a \"+1\" comment to agree.\n\n## `@` mentions\n\nTyping `@` in a comment opens a picker. Choose a member or an agent, and `@` plus the target's slug gets inserted (`@alice` or `@reviewer-bot`). The mentioned party gets a notification in their [inbox](/inbox).\n\n**If you mention an agent, it triggers automatically** — see [Mentioning agents in comments](/mentioning-agents).\n\nMentioning the same person multiple times in one comment still produces **only one** notification.\n\n### `@all` notifies the entire workspace\n\n`@all` is a special target: it pushes a notification to every member of the workspace. Both people and agents can use `@all` — which means an agent reporting progress could also `@all`, so remind agents in their instructions to use it sparingly.\n\n> **Use `@all` carefully.** In a larger workspace, a single `@all` generates that many inbox notifications instantly. Reserve it for things everyone genuinely needs to know — not day-to-day updates.\n\n## Referencing issues\n\nTo link another issue, choose it from the comment mention picker. Multica stores issue links as an explicit `[MUL-123](mention://issue/<uuid>)` mention link. Issue links are cross-references only: they do not notify people and they do not trigger agents.\n\nTyping a bare issue key, such as `MUL-123`, keeps it as plain text. This also keeps branch names and paths, such as `feature/MUL-123`, from being rewritten inside comments.\n\n> Markdown emphasis follows CommonMark rules. When bold text ends with punctuation or a closing quote and is immediately followed by a Korean particle, the closing `**` may not be recognized.\n>\n> Prefer moving the quote outside the bold span:\n>\n> ```markdown\n> \"**무엇을 먼저 정해두고 시작할지**\"가\n> ```\n>\n> instead of:\n>\n> ```markdown\n> **\"무엇을 먼저 정해두고 시작할지\"**가\n> ```\n\n## Editing and deleting a comment\n\nOnly the author of a comment can edit or delete it.\n\nDeleting a comment also **deletes every reply** under it (including replies to replies). To change content only, use edit instead.\n\n> **Adding an `@` while editing a comment does not trigger the agent.** The trigger fires the moment a comment is **created** — editing to add a new `@`, or changing the target, does not send a new notification or wake the agent. To summon an agent you missed, **post a new comment** that `@`s it.\n\n---\n\nEverything we've covered so far is \"the human world\" — workspaces, members, issues, projects, comments. If you've used Linear or Jira, none of it should feel unfamiliar.\n\nBut Multica's defining trait hasn't entered the picture yet: **treating agents as first-class members of a workspace**. That's what we turn to next.\n\n## Next\n\n- [Agents](/agents) — what they are, and how they differ from people\n- [Mentioning agents in comments](/mentioning-agents) — use `@` in a comment to start an agent"
        },
        {
          "slug": "project-resources",
          "title": "Project Resources",
          "content": "A **Project Resource** is a typed pointer — a Git repo URL, a path on your own machine, a Notion page tomorrow — attached to a [project](/workspaces). When an [agent](/agents) runs against an issue inside that project, the daemon automatically writes the project's resource list into the agent's working directory and into its [meta-skill](/skills) prompt.\n\nThe result: the agent knows which repo to check out (or which local directory to work in), and which docs are the \"primary references\" for this project, without anyone copy-pasting context into the issue body.\n\n## Mental model\n\nA project is no longer just a label. It is a small **resource container**:\n\n- A project has 0..N **resources**.\n- A resource has a `resource_type` (e.g. `github_repo`, `local_directory`) and a `resource_ref` (a JSON payload typed by `resource_type`).\n- New resource types add a string + a handler. **No schema migration. No frontend rewrite.**\n\nThis shape is intentional — it's the same pattern Multica already uses for agent providers: a `type` discriminator and a typed payload. It keeps the schema stable so adding \"Notion page\", \"Google Doc\", \"uploaded file\", or \"external URL\" later is a small, additive change.\n\nToday two resource types ship: [`github_repo`](#resource-type-github_repo) (clone-per-task into an isolated worktree) and [`local_directory`](#resource-type-local_directory) (run directly inside a folder on a specific daemon's machine).\n\n## Resource type: `github_repo`\n\nThe default resource type — checked out per task into an isolated worktree:\n\n```json\n{\n  \"resource_type\": \"github_repo\",\n  \"resource_ref\": {\n    \"url\": \"https://github.com/owner/repo\",\n    \"ref\": \"release/v2\",\n    \"default_branch_hint\": \"main\"\n  }\n}\n```\n\n`ref` is optional — if present, `multica repo checkout <url>` uses it as the default branch, tag, or commit for tasks in this project. An explicit `multica repo checkout <url> --ref <other-ref>` still wins for that one checkout.\n\n`default_branch_hint` is optional prompt context. It is not used for checkout; use `ref` when the project should pin a branch, tag, or SHA.\n\n## Resource type: `local_directory`\n\nFor repos that can't reasonably be re-cloned per task — multi-gigabyte game checkouts, large monorepos, or any project where the worktree-per-task model is painful — a project can instead point at an **existing directory on a specific [daemon](/daemon-runtimes)'s machine**. The agent runs **directly inside that folder**, with no clone, no copy, and no worktree.\n\n```json\n{\n  \"resource_type\": \"local_directory\",\n  \"resource_ref\": {\n    \"local_path\": \"/Users/me/code/big-game\",\n    \"daemon_id\": \"0001234e-…\",\n    \"label\": \"main checkout\"\n  }\n}\n```\n\nThe trade-off vs. `github_repo` is intentional: only the bound daemon can pick up tasks against the directory, and tasks on the same directory run **serially** instead of in parallel. In exchange you keep your existing checkout, your existing branch, your existing dirty state — Multica never re-clones it.\n\n### When to pick `local_directory` over `github_repo`\n\n| Concern | `github_repo` (worktree) | `local_directory` |\n| --- | --- | --- |\n| Checkout cost per task | Fresh clone + worktree | None — agent runs in place |\n| Concurrency on the same repo | Many tasks in parallel | One at a time per directory |\n| Branch / dirty state | Each task gets a fresh branch from the default | Whatever the directory currently has |\n| Where it can run | Any daemon | Exactly one daemon (the one bound) |\n| Disk footprint | One worktree per task | Zero overhead — your existing folder |\n\nPick `local_directory` when **either** of these matches:\n\n1. **Re-cloning is prohibitively expensive** — a multi-gigabyte game checkout, a monorepo with heavy LFS assets, or anything where the per-task `git clone` would dominate the actual work. You trade concurrency for a clone-free run.\n2. **Your changes are fine-grained and you want to review them locally as they happen** — you're iterating on a single component, you want to flip between the agent's edits and your editor every few minutes, and you'd rather have your existing checkout be the source of truth than a per-task worktree you have to dig out of `~/multica_workspaces/`.\n\nThe trade-off you accept in both cases is the same: **this version ships no file-level write lock.** The per-directory serial gate (one task at a time on the same folder) is the only protection against agents in two different issues touching the same files at the same time. If you point two issues' agents at the same `local_directory`, their tasks queue rather than parallelise — that's by design. If you need real parallelism on the same codebase, stay on `github_repo`.\n\n### Attaching a local directory\n\nThe folder picker lives in the **Desktop app** only — the web app has no way to read OS paths, so the \"Add local directory\" button is hidden there. On Desktop:\n\n1. Open the project → **Resources** panel.\n2. Click **Add local directory**. A native folder picker opens.\n3. Pick the folder. The path is bound to **the daemon currently registered by this Desktop install** — the resource record stores both the path and that daemon's ID.\n\nOn Desktop the button stays visible but is **disabled with a hint** when the daemon on this machine is offline, or when the project already has a `local_directory` bound to this daemon — so you can see *why* it's unavailable. (On the web app the button is hidden outright, since there's no folder picker available there at all.) To bind another machine's directory, install Desktop on that machine and add the resource from there.\n\nFrom the CLI (works on web-only environments too, as long as you supply the daemon ID yourself):\n\n```bash\nmultica project resource add <project-id> \\\n  --type local_directory \\\n  --local-path /Users/me/code/big-game \\\n  --daemon-id <daemon-uuid> \\\n  --ref-label \"main checkout\"        # optional\n\nmultica project resource update <project-id> <resource-id> \\\n  --local-path /Users/me/code/big-game-new\n```\n\n`--daemon-id` comes from `multica daemon list`. The CLI also accepts the generic `--ref '<json>'` escape hatch if you'd rather pass the payload directly.\n\n### Path rules\n\nThe path you attach must clear both an attach-time and a per-task validation. Both are enforced by the daemon that owns the resource — the server only stores the JSON. A path that breaks any rule fails the task with a typed error and leaves your directory untouched:\n\n- Must be **absolute**.\n- Must **exist** and be a **directory** (not a file, symlink to a file, or device node).\n- Must be **readable and writable** by the daemon process.\n- Cannot be a system root or an entire user profile — `/`, `/Users`, `/home`, `/root`, `/etc`, `/tmp`, `/var`, `/usr`, `/opt`, `/Users/Shared`, your own `$HOME`, any Windows drive root (`C:\\`, `D:\\`, …), or `C:\\Users` / `C:\\ProgramData` / `C:\\Program Files` / `C:\\Program Files (x86)` / `C:\\Windows`.\n- A symlink that resolves to any of the above is rejected, and so is the canonical form of an OS-aliased path (e.g. on macOS, typing `/private/tmp` is rejected the same way as `/tmp`).\n\nThe blacklist is intentionally aggressive — picking your home directory would put Multica's runtime files at the root of your account, which is never what you want. Pick a sub-folder (typically your actual project checkout) instead.\n\n### One per (project, daemon)\n\nA project may hold **at most one `local_directory` per daemon**. Attempting to add a second one on the same daemon returns a `409` from the API; the Desktop button hides itself when the limit is already reached and surfaces a tooltip explaining why.\n\nDifferent daemons are independent — a shared project can have one `local_directory` per teammate's machine, each binding the same project to a different folder on a different host. When the daemon claims a task, it picks the row that matches its own ID and ignores the rest.\n\n### Mixing resource types, and multiple `local_directory` resources\n\nTwo cross-resource configurations show up in practice:\n\n- **`github_repo` + `local_directory` on the same project.** On the daemon that has a matching `local_directory` binding, the local directory **takes precedence**: the agent runs in your folder, and the daemon does not create or use a `github_repo` worktree for that task. (The per-workspace repo cache may still sync as usual — that's a background behaviour unrelated to this task's working tree.) The `github_repo` URL still appears in `.multica/project/resources.json` and in the agent's `## Repositories` section for reference — but the working tree the agent edits is your local one, not a worktree. On a daemon that has **no** `local_directory` row for this project (different machine, or before that teammate attached one), the task falls back to the usual `github_repo` worktree flow. Effectively the local directory is a per-daemon override of the worktree path.\n- **Two `local_directory` resources on the same project.** Because each `local_directory` is bound to exactly one daemon, this only happens across two different machines (the API rejects two on the same daemon at attach time, see above). Tasks are routed by the agent's runtime assignment, not by which daemon has a local directory: a task lands on the daemon that owns the receiving agent's runtime, that daemon picks the `local_directory` row matching its own ID, and ignores the rest. There is no load-balancing — if you want a specific machine to run a task, dispatch the agent that's bound to that machine's runtime.\n\nA daemon that has no `local_directory` row for a project that has one bound elsewhere is **not** blocked — its tasks simply proceed via the project's other resources (typically the `github_repo` fallback). The `local_directory` only matters for the daemon it's bound to.\n\n### Running tasks against a local directory\n\nWhen a task is dispatched on an issue whose project has a `local_directory` bound to the receiving daemon, the daemon:\n\n1. Re-validates the path (rules above).\n2. Acquires a per-directory lock keyed on the symlink-resolved real path — so two routes to the same folder (one via a symlink, one direct) still serialise.\n3. Writes the agent's `CLAUDE.md` / `AGENTS.md` (and `.multica/project/resources.json`) **into the user's directory**. The agent works there, just as if you'd opened the folder yourself.\n4. Keeps Multica's runtime artefacts (`output/`, `logs/`, `.gc_meta.json`) in a separate envRoot **outside** the user's directory.\n\nIf a second task for the same directory arrives while the first is running, it parks with status **Waiting for local directory** (等待本地目录释放). The status is visible everywhere the task is — the chat task pill, the agent banner, the execution log, and the activity indicator — and the parked task counts toward the agent's \"queued\" presence. Cancelling the parked task releases its slot immediately; cancelling the running task lets the next one promote.\n\nThe wait is not a timeout — a parked task stays parked until either the lock releases or the user / agent cancels it.\n\n### What Multica will and won't touch in your directory\n\n- **Will write** `CLAUDE.md` / `AGENTS.md` (or the equivalent for your agent's provider) and `.multica/project/resources.json` at the directory root, so the agent has its meta-skill and resource list. Add these to your `.gitignore` if you don't want them committed.\n- **Will write** whatever code edits the agent decides to make — exactly the same way as if you'd run the agent locally yourself.\n- **Will never physically delete** the directory or anything inside it. Garbage collection is path-aware: for `local_directory` envRoots it cleans only its own `output/` and `logs/` under `workspacesRoot`, and treats the user's directory as off-limits.\n\n### v1 limits (will tighten in follow-ups)\n\nThe first release deliberately ships with sharper edges than `github_repo`. Expect this list to shrink over time — what's documented here is what's true today:\n\n- **No automatic branch switching.** The agent runs in whatever branch you have checked out. Switch branches before dispatching if it matters.\n- **No dirty-tree protection or auto-commit.** Uncommitted changes are visible to the agent, may be modified in place, and won't be stashed. Treat the directory as a real working tree and commit before risky runs.\n- **No automatic PR.** When the task ends, the changes sit on whatever branch they were made on — nothing is pushed and no PR is opened. Push and open the PR yourself when you're ready.\n- **`waiting_local_directory` shows status, not the holder.** The badge tells you the task is parked; it doesn't surface which task or which file path is currently holding the directory.\n\nThese are tracked as the agent-task-lifecycle follow-up to the local-directory work; until that ships, treat `local_directory` as \"the agent runs in your folder, the same way you would.\"\n\n## Attaching repos at project creation\n\nIn the **Web** or **Desktop** app, opening *New project* now shows a **Repos** pill alongside Status / Priority / Lead. Selecting workspace-bound repos (or pasting an ad-hoc URL) attaches them as `github_repo` resources the moment the project is created.\n\nFrom the **CLI**:\n\n```bash\n# Create + attach in one shot. The server attaches resources in the same\n# transaction as the project create — invalid resources roll back the whole\n# operation, so you never end up with a project that has half its resources.\nmultica project create \\\n  --title \"Agent UX 2026\" \\\n  --repo https://github.com/multica-ai/multica\n\n# Manage resources later\nmultica project resource list <project-id>\nmultica project resource add  <project-id> --type github_repo --url <url>\nmultica project resource add  <project-id> --type github_repo --url <url> --ref <branch-or-sha>\nmultica project resource remove <project-id> <resource-id>\n\n# Generic escape hatch for any resource_type the server understands —\n# no CLI change needed when a new type ships:\nmultica project resource add <project-id> \\\n  --type notion_page \\\n  --ref '{\"page_id\":\"…\",\"title\":\"…\"}'\n```\n\n`--repo` may be repeated; each value is attached as a separate `github_repo` resource.\n\n## What the agent sees at runtime\n\nWhen the daemon spawns an agent for an issue inside a project, two things happen:\n\n### 1. `.multica/project/resources.json`\n\nA structured pass-through of the API response, written into the agent's working directory:\n\n```json\n{\n  \"project_id\": \"…\",\n  \"project_title\": \"Agent UX 2026\",\n  \"resources\": [\n    {\n      \"id\": \"…\",\n      \"resource_type\": \"github_repo\",\n      \"resource_ref\": {\n        \"url\": \"https://github.com/multica-ai/multica\",\n        \"default_branch_hint\": \"main\"\n      }\n    }\n  ]\n}\n```\n\nSkills, helper scripts, or the agent itself can parse this file when they need the *exact* set of resources for the run.\n\n### 2. A \"Project Context\" section in the meta-skill prompt\n\nThe agent's `CLAUDE.md` / `AGENTS.md` (depending on provider) now includes a human-readable summary:\n\n```\n## Project Context\n\nThis issue belongs to **Agent UX 2026**.\n\nProject resources (also written to `.multica/project/resources.json`):\n\n- **GitHub repo**: https://github.com/multica-ai/multica (default branch: `main`)\n\nResources are pointers — open them only when relevant to the task. For\n`github_repo` resources, use `multica repo checkout <url>` to fetch the code.\n```\n\nThe text is intentionally minimal. The full payload is on disk; the prompt only orients the agent so it knows the project exists and what's attached.\n\n### Failure mode\n\nResource fetch is **best-effort**. If the API call fails, the project section is omitted from the prompt and the file is not written, but the task still starts. Agents never block on missing project context.\n\n## Adding a new resource type\n\nThe whole point of the abstraction is that new types are cheap. The full path:\n\n1. **Server validator** (`server/internal/handler/project_resource.go`) — add a case in `validateAndNormalizeResourceRef` that parses and normalizes the new payload.\n2. **Daemon meta-skill formatter** (`server/internal/daemon/execenv/runtime_config.go`) — add a case in `formatProjectResource` so the agent prompt renders the new type as a readable bullet.\n3. **TypeScript types** (`packages/core/types/project.ts`) — extend `ProjectResourceType` and add the payload interface.\n4. **UI renderer** (`packages/views/projects/components/project-resources-section.tsx`) — add a case in `ResourceRow` for the new type.\n\nThere is **no schema migration**, no new sqlc query, no new endpoint, **and no CLI change** — the CLI's generic `--ref '<json>'` flag accepts any payload the validator understands, so day-one support for a new type is purely the four steps above. (You may *optionally* add a per-type CLI shortcut later; not required.)\n\nThe same `project_resource` table and the same three CRUD calls handle every type.\n\n## Workspace repos vs. project repos\n\nThe repo list shown to the agent (`## Repositories` block in `CLAUDE.md` / `AGENTS.md`) is chosen by the daemon claim handler with this precedence:\n\n- **Project has at least one `github_repo` resource** → only those repos are surfaced to the agent. Workspace-bound repos are intentionally hidden so the agent doesn't have to guess which one belongs to this issue.\n- **Project has no `github_repo` resources (or the issue isn't in a project)** → fall back to the workspace's repo list as before.\n\nThis keeps the agent's working set tight: when a project is explicit about its repos, that's the authoritative answer. The structured resource list at `.multica/project/resources.json` always carries the full set, so a skill that wants to inspect everything still can.\n\nThe daemon mirrors this on the checkout side: when a task arrives with project-scoped `github_repo` URLs, those URLs are merged into the per-workspace allowlist *and* synced into the local repo cache before the agent spawns. So a project repo URL that isn't bound at the workspace level is still a valid argument to `multica repo checkout` — the daemon won't reject it as \"not configured.\" If the project resource includes `ref`, that ref becomes the default for `multica repo checkout <url>` during that task; passing `--ref` to the checkout command overrides it. The allowlist split is internal: workspace-bound URLs and task-scoped URLs are tracked separately, so a workspace-repos refresh doesn't accidentally revoke a project URL mid-run.\n\n## What's intentionally **not** in scope here\n\n- **Cross-project sharing.** Each resource lives on exactly one project today.\n- **Per-skill resource scoping.** All resources are visible to every skill on the agent's run; type-aware filtering is a follow-up.\n- **Caching / sync.** `github_repo` is just metadata — checkout still happens via `multica repo checkout` on demand. Cached document text for Notion / Google Docs will arrive with those types.\n\nThese are deliberate omissions — the goal of the first cut is to validate the abstraction with the smallest set of moving parts."
        }
      ]
    },
    {
      "label": "Agents",
      "labelKey": "agents",
      "pages": [
        {
          "slug": "agents",
          "title": "Agents",
          "content": "An agent is a **first-class member** of a Multica [workspace](/workspaces) — like a human, it can be [assigned issues](/assigning-issues), speak up in [comments](/comments), be [`@`-mentioned](/mentioning-agents), and lead a [project](/projects). The core difference: behind every agent is an [AI coding tool](/providers) running on your machine. Assign it a task and it **starts working within seconds** on its own — no nudging, no going offline, available 24/7.\n\n## What an agent can do\n\nAgents use the same \"member\" surface as humans, and the UI barely distinguishes them:\n\n- **[Be assigned issues](/assigning-issues)** — once set as the assignee, it starts working automatically\n- **[Be `@`-mentioned](/mentioning-agents)** — write `@agent-name` in a comment and it wakes up to read that comment\n- **Post [comments](/comments)** — it reports progress and replies to people under the issue\n- **Lead a [project](/projects)** — it can be set as project lead, same as a human\n- **Open [issues](/issues) itself** — while running a task, if it spots a related problem, it can create a new issue directly\n\nFrom the collaboration view, an agent is just a member of the workspace — its name sits in the same member list as humans, usually with a small robot icon in front.\n\n## How it differs from a human\n\nA few key differences only surface once you actually start using agents:\n\n- **It starts on its own** — after you assign it an issue or `@` it, Multica dispatches the task to its runtime immediately. Unlike a human, it doesn't wait to see the message and respond. For trigger details, see [Assigning issues to agents](/assigning-issues) and [@-mentioning agents in comments](/mentioning-agents).\n- **It doesn't receive notifications** — an agent never shows up on the other side of your [inbox](/inbox), and it's not in the audience for `@all`. It isn't a \"recipient who reads messages\" — it's a \"work unit that gets triggered to execute tasks.\"\n- **It's bound to one AI coding tool** — every agent is tied to a runtime (runtime = daemon × one AI coding tool; see [Daemon and runtimes](/daemon-runtimes)). If the tool is offline, the agent can't work; new tasks wait until the runtime comes back.\n- **It can be archived** — archive an agent you don't use anymore and it disappears from everyday views; restore it whenever you want. Archiving cancels any tasks currently running.\n\n## Who can assign an agent\n\nWhen you create an agent, you pick a **visibility** that controls who can assign it to an issue or set it as project lead:\n\n- **Workspace** — any member of the workspace can assign it\n- **Private** — only workspace owners, admins, or the agent's creator can assign it\n\nNew agents default to **private**. To make one available to the whole workspace, set visibility to `workspace` at creation time, or change it later in the agent's config. For the full role-permission matrix, see [Members and roles](/members-roles).\n\n> **Private means \"restricted who can assign,\" not \"hidden from everyone else.\"** Every member of the workspace sees a private agent's name and description in the agents list — they just can't see its config details (custom environment variables, MCP config, and other sensitive fields are masked). If you need \"visible to only one person,\" that's not currently possible.\n\n## Next steps\n\n- [Create and configure an agent](/agents-create) — how to build one\n- [Skills](/skills) — attach knowledge packs to an agent\n- [Squads](/squads) — group agents under a leader so the right one picks up the right issue\n- [Daemon and runtimes](/daemon-runtimes) — what an agent needs to actually run"
        },
        {
          "slug": "agents-create",
          "title": "Create and configure an agent",
          "content": "Creating an [agent](/agents) takes only two things: **a name** and **a choice of [AI coding tool](/providers)**. Everything else is optional — system instructions, model, environment variables, CLI arguments, visibility, concurrency limit — the defaults work fine. Get it running first and tune later; every field can be changed at any time.\n\n## Create an agent\n\nPrerequisite: you already have at least one supported [AI coding tool](/providers) installed on your machine (Claude Code, Codex, etc.) and a [daemon](/daemon-runtimes) running. If you're not there yet, start with [Cloud quickstart](/cloud-quickstart) or [Self-host quickstart](/self-host-quickstart).\n\nOnce that's in place, go to the **Agents** page in your workspace and click **+ New**, or use the CLI:\n\n```bash\nmultica agent create\n```\n\nThe form has only two required fields: **name** (unique within the workspace) and **runtime** (= pick an AI coding tool). Every other field is covered section by section below.\n\n## Pick an AI coding tool\n\nEach runtime is backed by a specific AI coding tool. Multica supports 14 of them. The most common choices:\n\n| Tool | Good for |\n|---|---|\n| **Claude Code** | Anthropic's official tool, most complete feature set; **best first pick** |\n| **Codex** | OpenAI, the mainstream alternative |\n| **Cursor** | Users in the Cursor editor ecosystem |\n| **Copilot** | Teams leveraging their GitHub account entitlements |\n| **CodeBuddy** | Teams using Tencent's Claude Code-compatible CLI |\n\nThe other nine (Antigravity, Hermes, Kimi, Kiro CLI, OpenCode, Pi, OpenClaw, Qoder, Trae CLI), along with each tool's full capability matrix (session resume, MCP, skill injection path, model selection), are covered in [AI coding tools comparison](/providers).\n\n## Writing system instructions\n\n**System instructions** (`instructions`) are prepended to every task, telling the agent what role it plays and what rules to follow:\n\n```text\nYou're a frontend code-review agent. When an issue comes in, read the diff first. Focus only on:\n- Styling issues (tailwind class names, box model)\n- Accessibility (a11y)\nDon't change code — leave suggestions in a comment.\n```\n\nWhen left blank (the default), the agent uses the native behavior of its underlying AI coding tool with no extra constraints.\n\n## Picking a model\n\nMost AI coding tools support model selection (for example, Claude Code lets you pick between Sonnet and Opus). Leave it blank and the tool's own default is used; pick one explicitly and that's what runs. Each tool's supported models are listed in [AI coding tools comparison](/providers).\n\nChanging the model **only applies to new tasks**. Already-dispatched tasks continue with the model that was locked in at dispatch time.\n\n## Custom environment variables (custom_env)\n\n**Custom environment variables** (`custom_env`) let you inject extra env vars at task execution time — typical uses are API keys or switching the upstream endpoint:\n\n```\nANTHROPIC_API_KEY = sk-...\nANTHROPIC_BASE_URL = https://my-proxy.example.com\n```\n\nSystem-critical variables cannot be overridden: `PATH`, `HOME`, `USER`, `SHELL`, `TERM`, `CODEX_HOME`, and any key starting with `MULTICA_*` are silently ignored by the daemon (with a warn log — no error).\n\n> **Values in `custom_env` are stored in plaintext in Multica's server database.** Agent list/get responses no longer carry env values at all — only an opaque count. Reading values requires a workspace owner or admin to hit the dedicated, audited `GET /api/agents/{id}/env` endpoint (CLI: `multica agent env get <id>`). Agents running tasks can NOT use their host's owner credentials to reveal env on other agents — the endpoint denies agent-actor sessions.\n>\n> **Don't put high-value secrets in `custom_env`** (production database passwords, root-level tokens, etc.). Use **dedicated, limited-scope credentials** for agents (read-only API keys, single-scope PATs), and rotate them regularly. Database backups and DB audits remain a meaningful exposure surface.\n\n## Custom CLI arguments (custom_args)\n\n**Custom CLI arguments** (`custom_args`) is a string array appended one-by-one to the AI coding tool's command line:\n\n```json\n[\"--max-turns\", \"100\", \"--append-system-prompt\", \"always respond in Chinese\"]\n```\n\nThe final command comes out as:\n\n```bash\nclaude --model <model> --max-turns 100 --append-system-prompt \"always respond in Chinese\" [...]\n```\n\nArguments are passed as-is, not through a shell (no injection risk), but whether a given flag is recognized is up to the AI coding tool itself — tools differ substantially here.\n\n> `custom_env` and `custom_args` have no hard caps, but in practice **keep each under 10 entries**. Too many makes the command line long, slows startup, and gets harder to maintain.\n\n## Visibility\n\n- **Workspace** (`workspace`) — any member of the workspace can assign it\n- **Private** (`private`) — only workspace owners, admins, or the agent's creator can assign it\n\nNew agents default to `private`.\n\n**Private does not mean hidden** — every member sees a private agent's name and description in the list, they just can't read sensitive config (env values never appear in agent list/get responses; MCP config is masked for non-owners). Full meaning in [Agents → Who can assign an agent](/agents#who-can-assign-an-agent).\n\n## Concurrency limit\n\n**Concurrency limit** (`max_concurrent_tasks`) controls how many tasks this agent can run in parallel at once. The default is **6**. New tasks that hit the cap queue up — they aren't rejected.\n\nThis is only the \"agent layer\" of a two-tier limit — the daemon itself enforces a broader cap (default 20), and whichever is tighter wins. Details in [Daemon and runtimes → How many tasks can run in parallel](/daemon-runtimes#how-many-tasks-can-run-in-parallel).\n\nChanging this value **does not cancel tasks already running** — it only applies to the next task about to be picked up.\n\n## Attaching domain expertise: Skills\n\nA created agent can have **Skills** attached — **knowledge packs** (`SKILL.md` + supporting files) automatically delivered to the AI coding tool at task execution time. You can create a new skill, import from GitHub or ClawHub, or scan one from an existing skill directory on your machine. See [Skills](/skills).\n\n## Archive and restore\n\nAgents you no longer use can be **archived** — they disappear from everyday views, but their historical data (tasks run, comments posted) is fully preserved. **Restore** them anytime to put them back to work.\n\n> **Archiving immediately cancels every unfinished task belonging to the agent** — running, dispatched, and queued tasks are all marked `cancelled` and won't continue. If you have an important task in flight, let it finish before archiving.\n\nArchived agents can't be assigned new tasks.\n\n## Next steps\n\n- [Skills](/skills) — attach knowledge packs to an agent\n- [AI coding tools comparison](/providers) — full capability matrix across all 14 tools\n- [Assigning issues to agents](/assigning-issues) — put your new agent to work"
        },
        {
          "slug": "skills",
          "title": "Skills",
          "content": "A Skill is a **knowledge pack** for an [agent](/agents) — a `SKILL.md` plus optional supporting files (scripts, configs, reference templates) that tell the agent \"when you hit this kind of task, think and act like this.\" Multica adopts the [Anthropic Agent Skills](https://agentskills.io) open standard, so any compliant Skill — from Anthropic's official repository, ClawHub, or skills.sh — can be imported directly.\n\n## Workspace skills vs. local skills\n\nMultica supports two skill sources:\n\n- **Workspace skill** — stored in Multica's cloud. Once attached to an agent, it's synced down to your daemon at task execution time. This is the **standard way to share skills across a team**.\n- **Local skill** — lives in a directory on your machine. On your request, the [daemon](/daemon-runtimes) scans your machine, and you manually pick which ones to bring into the workspace. The daemon checks **two roots, in priority order**: first the runtime's own skill directory (each AI coding tool has a conventional default path, e.g. Claude Code's `~/.claude/skills/`), then the cross-tool universal directory `~/.agents/skills/` — a shared location used by ecosystems like Codex and Gemini CLI. When the same skill name exists in both, the **provider-specific directory wins**, so the universal root only ever surfaces *additional* skills and never changes what an existing skill resolves to.\n\nMost of the time you want **workspace skills**: import once, every teammate's agent can use it. Local skills are a fit when you want to test locally first, or when the content involves sensitive local material.\n\n## Repository-scoped skills\n\nRepo-scoped skills are expected behavior. Some AI coding tools discover **project-level skills committed inside a repository**, such as `.claude/skills/`, `.cursor/skills/`, `.opencode/skills/`, or `.agents/skills/`. When a Multica task checks out that repository, those files remain in the workdir and the underlying tool can load them through its native discovery rules. You do **not** need to import those repo skills into Multica just to use them in that repo; Multica does **not** import them into the workspace skill registry automatically.\n\nWorkspace skills still sync into the same provider-native location for tools that support project-level discovery. If a workspace skill would collide with an existing repo skill directory, Multica writes the workspace copy to a sibling name such as `review-helper-multica` instead of overwriting the repo's files. The tool may then see both skills, with the workspace copy carrying the adjusted name.\n\n## Importing a skill\n\nWorkspace skills come from four sources:\n\n- **New** — write the `SKILL.md` and related files directly in the UI\n- **From GitHub** — paste a repo URL (e.g. `https://github.com/owner/repo/tree/main/skills/my-skill`) and Multica pulls the `SKILL.md` and every file in that directory\n- **From ClawHub** — search and import from the [ClawHub](https://clawhub.io) public marketplace, with version selection\n- **From local** — the daemon scans skill directories on your machine, and you pick which to bring into the workspace\n\nBoth individual files and whole skill packs have size caps (single-file cap around 1 MB when importing from GitHub). The exact rules appear in the import dialog — exceeding them returns an error.\n\n## Attaching to an agent\n\nOnce imported, a skill has to be **attached to a specific agent** to take effect. One agent can have multiple skills attached, and one skill can be attached to multiple agents.\n\nAfter attaching, the agent picks up its skills the next time it starts a task — each AI coding tool has its own skill discovery path (Claude Code uses `.claude/skills/`, Cursor uses `.cursor/skills/`, Antigravity uses `.agents/skills/`, etc.), and Multica drops files in the right place automatically. **However, three tools (Gemini, Hermes, OpenClaw) currently use the generic fallback path `.agent_context/skills/` — whether these tools actually read skills from that path depends on the tool itself.** Full path mapping and the native-discovery vs. fallback distinction is in [AI coding tools comparison → Where skill files go](/providers#where-skill-files-go).\n\nAfter you edit a skill's contents, **only newly created tasks pick up the new version** — tasks already running continue with the old skill.\n\n## Safety of third-party skills\n\nSkills imported from GitHub or ClawHub may include scripts and executable content. Multica itself **does not sign, audit, or sandbox them** — skill contents are handed to the corresponding AI coding tool as-is, and whether the tool treats them as executable is up to the tool.\n\n> **Before importing a third-party skill, review the `SKILL.md` and every file that ships with it.**\n>\n> In February 2026 the \"ClawHavoc\" incident — malicious instructions planted in a popular skill pack stole API keys from affected users. ClawHub has since added VirusTotal scanning, but **automated scans are not a substitute for your own review**.\n>\n> **Only import from sources you trust.** For projects involving sensitive data, consider using only local skills you wrote yourself.\n\n## Skills vs. MCP\n\nBoth augment what an agent can do, but in different directions:\n\n- **Skill** = a structured **knowledge pack** (static content + instructions). The agent reads a skill to learn \"when I see problem X, here's how to think and what to do.\"\n- **MCP** (Model Context Protocol) = a **tool channel**. The agent uses MCP to connect to external services (databases, filesystems, third-party APIs) and **invoke** them.\n\nThe two are complementary. In Multica today, MCP support is **provider-specific**: Claude Code, Codex, Cursor, Hermes, Kimi, Kiro CLI, OpenCode, and OpenClaw consume `mcp_config`; other tools receive the field but don't actually use it. A dedicated MCP section will come in a later release.\n\n---\n\nBy now you know what an agent is, how to create one, and how to attach skills. The next question: **where does it actually run, and why does my agent sometimes get stuck?** The next chapter covers the execution architecture — daemons, runtimes, and how tasks work together.\n\n## Next steps\n\n- [Daemon and runtimes](/daemon-runtimes) — where agents actually run, and how to tell online from offline\n- [Executing tasks](/tasks) — the full lifecycle of one \"agent work session\"\n- [AI coding tools comparison](/providers) — full comparison of all 14 tools (including each one's skill injection path)"
        },
        {
          "slug": "squads",
          "title": "Squads",
          "content": "A squad is a **named group of [agents](/agents) and human [members](/members-roles)**, with one designated **leader agent**. The squad is itself a first-class assignee: pick it from any **Assignee** picker and the leader takes the trigger, reads the issue, then `@`-mentions the squad member best suited to do the work. Squads let you assemble specialists once and dispatch them **by topic instead of by name** — the team grows, the routing stays the same.\n\n## What a squad is, in mechanics\n\n- **One leader, many members.** The leader must be an agent; members can be agents or human members. A squad with only the leader is allowed (the leader briefing notes \"no other members\"), and the same agent can sit in multiple squads.\n- **Assignable everywhere a person is.** Squads appear in the Assignee picker, the @-mention picker, and the quick-create modal — anywhere you'd pick an agent or member, you can pick a squad.\n- **Soft-deleted via archive.** Archive a squad and it disappears from pickers and lists; any issue currently assigned to it is **transferred to the leader agent** so the work doesn't go silent. Archived squads can't be assigned to new issues.\n\n## When to use a squad versus a single agent\n\n| Pick a squad when… | Pick a single agent when… |\n|---|---|\n| You have several specialists and don't know which one fits this issue in advance | The work is well-scoped to one specialty and you know who should do it |\n| You want one stable assignee (the squad) while the actual responder changes per issue | You want the agent's name on the issue and clear individual accountability |\n| You want a `@FrontendTeam` style routing target in comments | One-on-one `@agent-name` is enough |\n\nThe squad doesn't add capability — it adds **routing**. The members are still ordinary agents; the leader's only job is to pick the right one.\n\n## Permissions\n\n| Action | Who can do it |\n|---|---|\n| Create / update / archive a squad | Workspace **owner** or **admin** |\n| Add or remove members, change roles | Workspace **owner** or **admin** |\n| Assign an issue to a squad | Any workspace member (same as assigning to an agent) |\n| `@`-mention a squad in a comment | Any workspace member |\n| Record a squad-leader evaluation | The squad leader agent only (via CLI) |\n\nThe full role matrix lives in [Members and roles](/members-roles).\n\n## Create a squad\n\nIn the sidebar, open **Squads → New squad** and fill in:\n\n- **Name** — e.g. `Frontend Team`, `Bug Triage`. Doesn't need to be unique within the workspace.\n- **Description** (optional) — a short blurb shown on the squad card and detail page.\n- **Leader** — pick an existing agent. The leader is added to the squad automatically with role `leader`.\n\nAfter creation, open the squad's detail page to:\n\n- **Add members** — pick agents or human members, optionally give each a short role description (e.g. \"owns the migrations\", \"reviewer of last resort\"). The leader uses these roles when deciding who to delegate to.\n- **Write instructions** — squad-level guidance the leader sees on every run (more below).\n- **Set an avatar** — picked from the same picker used for agents.\n\nCLI equivalent:\n\n```bash\nmultica squad create --name \"Frontend Team\" --leader frontend-lead-agent\nmultica squad member add <squad-id> --member-id <agent-or-user-uuid> --type agent --role \"Owns Tailwind / shadcn surface\"\n```\n\n## How a squad-assigned issue runs\n\nWhen a non-Backlog issue is assigned to a squad, Multica immediately enqueues a `task` for the **leader agent** (not for every member). The flow then looks like this:\n\n1. **Leader claims the task.** The agent runtime picks up the task on its next poll, same as any other agent assignment.\n2. **Leader is briefed.** On claim, Multica appends three sections to the leader's system prompt — see [What the leader sees on every turn](#what-the-leader-sees-on-every-turn) below.\n3. **Leader posts one delegation comment.** The comment `@`-mentions the chosen member(s) using the exact mention markdown from the roster — that mention triggers a new `task` for each mentioned agent.\n4. **Leader records its evaluation** via `multica squad activity <issue-id> action --reason \"...\"`. This writes an entry to the issue's activity timeline so humans can see the leader actually evaluated the trigger.\n5. **Leader stops.** The leader does not do the implementation itself. When the delegated member posts back, the leader is re-triggered to read the update and either delegate the next step, escalate, or stay silent.\n\nIf the issue is in **Backlog**, the leader is not triggered — Backlog is a parking lot, same rule as for direct agent assignment.\n\n### What the leader sees on every turn\n\nOn each squad-leader run, three blocks are appended to the leader's instructions:\n\n- **Squad Operating Protocol** — a hard-coded rule set: read the issue, delegate by `@`-mention, be terse (don't restate the issue body — the assignee can read it), record an evaluation every turn, and **stop after dispatching**. This protocol is system-managed and not editable.\n- **Squad Roster** — the leader's self-row plus one row per non-archived member. Each row carries the exact mention markdown (`[@Name](mention://agent/<uuid>)` or `[@Name](mention://member/<uuid>)`) the leader should paste — typing a plain `@name` won't trigger anyone.\n- **Squad Instructions** — your custom guidance for this squad (set on the squad detail page or via `multica squad update --instructions`). Use this for routing rules (\"send DB work to Alice, frontend to Bob\"), escalation policies, or anything else the leader needs to know that isn't already in the issue.\n\n## When the leader is re-triggered\n\nAfter the first dispatch, the leader is woken up automatically by **most subsequent comments** on the issue. The exact rules:\n\n| Event | Leader triggered? |\n|---|---|\n| A non-member (human reporter, external agent) posts a comment | **Yes** |\n| A squad member posts a progress update with no `@mention` | **Yes** — the leader re-evaluates whether the next step is needed |\n| Anyone posts a comment that explicitly `@`-mentions another agent / member / squad / `@all` | **No** — the explicit `@` is the routing signal; the leader gets out of the way |\n| The leader's own comment (self-trigger) | **No** — guarded to prevent a loop |\n| A comment containing only an issue cross-reference (`[MUL-123](mention://issue/...)`) | **Yes** — issue references aren't routing |\n\nDedup applies on top of these rules: if the leader already has a `queued` or `dispatched` task on this issue, a new trigger won't enqueue a duplicate.\n\n> **Why the leader doesn't trigger when a member posts an `@`-mention.** Once a squad member directly `@`s someone, that comment is a deliberate hand-off — having the leader wake up to \"observe\" the routing would just produce a no-op turn and clutter the timeline. Agent-authored comments are the exception: when an agent posts a result that `@`s another agent, the leader still wakes up so it can coordinate the thread.\n\n## `@`-mention a squad in a comment\n\nSquads appear in the `@` picker alongside members and agents. Mentioning a squad inserts `[@SquadName](mention://squad/<uuid>)` and triggers the **squad leader** as if you had assigned the issue to the squad — without changing the assignee or the status. Use this when you want the squad to pick someone for a question or sub-task while keeping the current owner.\n\nThe same anti-loop rules apply: the leader skips itself, and an explicit member `@`-mention in the same comment will route to that member directly.\n\n## Reassign or archive a squad\n\n**Reassigning an issue away from a squad** behaves like any other assignee change: all of the issue's active tasks (including the leader's) are cancelled, and the new assignee — agent, member, or another squad — is enqueued. There is no separate \"remove squad without changing assignee\" action; pick a different assignee.\n\n**Archiving a squad** (`multica squad delete <id>`, or the Archive button on the detail page):\n\n1. **Transfers issues currently assigned to the squad to the leader agent**, so the work continues against a concrete agent instead of going silent.\n2. Marks the squad with `archived_at` / `archived_by` — the row is preserved so historical activity entries still resolve, but the squad disappears from lists, pickers, and the @-mention dropdown.\n3. **Rejects future assignments** to this squad with `cannot assign to an archived squad`.\n\nThere is currently no unarchive command; create a new squad if you need the routing back.\n\n## Squad operations from the CLI\n\n| Command | Purpose |\n|---|---|\n| `multica squad list` | List squads in the workspace |\n| `multica squad get <id>` | Show one squad's name, leader, description, instructions |\n| `multica squad create --name \"...\" --leader <agent>` | Create a squad (owner / admin) |\n| `multica squad update <id> [--name X] [--description X] [--instructions X] [--leader Y] [--avatar-url Z]` | Update one or more fields |\n| `multica squad delete <id>` | Archive (soft-delete) — transfers assigned issues to the leader |\n| `multica squad member list <id>` | List a squad's members |\n| `multica squad member add <id> --member-id <uuid> --type agent\\|member [--role \"...\"]` | Add a member (owner / admin) |\n| `multica squad member set-role <id> --member-id <uuid> --member-type agent\\|member --role \"...\"` | Change a member's role without removing it |\n| `multica squad member remove <id> --member-id <uuid> --type agent\\|member` | Remove a member (the leader cannot be removed — change leader first) |\n| `multica squad activity <issue-id> <action\\|no_action\\|failed> --reason \"...\"` | Recorded by the leader agent at the end of every turn |\n\n`--leader` accepts an agent name or UUID; for everything else, IDs come from `multica agent list --output json`, `multica workspace member list --output json`, and `multica squad list --output json`.\n\n## Next\n\n- [Assign issues to agents](/assigning-issues) — same flow, applies to squad assignees too\n- [`@`-mention agents in comments](/mentioning-agents) — the `@` picker also surfaces squads\n- [Agents](/agents) — what an agent is, the building block of every squad\n- [Members and roles](/members-roles) — the full owner / admin / member permission matrix"
        }
      ]
    },
    {
      "label": "How agents run",
      "labelKey": "how_agents_run",
      "pages": [
        {
          "slug": "daemon-runtimes",
          "title": "Daemon and runtimes",
          "content": "In Multica, [agents](/agents) do **not** run on our servers — they run on your own machines, driven by a small program called the **daemon** that invokes the [AI coding tools](/providers) installed locally. The Multica server only coordinates: it stores [issues](/issues), queues [tasks](/tasks), and dispatches them to the right **runtime** (runtime = daemon × one AI coding tool).\n\nThis structure is the biggest difference between Multica and Linear / Jira: **your API keys, toolchain, and code directories stay on your machine** — the Multica server never sees any of them. That means \"my agent isn't working\" is almost always a local problem — the daemon isn't running, an AI tool isn't installed, a key has expired. Check locally first; see [Troubleshooting](/troubleshooting) for a guide.\n\n## Starting the daemon\n\nThe daemon is part of the Multica CLI. Once you've installed the [Multica CLI](/cli), run on your own machine:\n\n```bash\nmultica daemon start\n```\n\nOn startup it does four things:\n\n1. Reads the credentials saved when you logged in\n2. Detects AI coding tools installed on your `PATH` (14 built-in: [Antigravity](/providers#antigravity), [Claude Code](/providers#claude-code), [CodeBuddy](/providers#codebuddy), [Codex](/providers#codex), [Cursor](/providers#cursor), [Copilot](/providers#copilot), [Hermes](/providers#hermes), [Kimi](/providers#kimi), [Kiro CLI](/providers#kiro-cli), [OpenCode](/providers#opencode), [OpenClaw](/providers#openclaw), [Pi](/providers#pi), [Qoder](/providers#qoder), [Trae CLI](/providers#trae))\n3. Registers itself with the server, along with a runtime for each detected tool\n4. Keeps **polling every 3 seconds** for tasks to pick up, and **sends a heartbeat every 15 seconds**\n\nCommon commands:\n\n| Command | Purpose |\n|---|---|\n| `multica daemon start` | Start (background by default; add `--foreground` to run in the foreground) |\n| `multica daemon stop` | Stop |\n| `multica daemon restart` | Restart |\n| `multica daemon status` | Show status |\n| `multica daemon logs` | Show logs (add `-f` to follow) |\n\nFull CLI reference in [CLI commands](/cli).\n\n## Why one machine has multiple runtimes\n\nA runtime is not a server and not a container — it's the combination of \"**daemon × one AI coding tool**\". For example: you start the daemon on a MacBook with both Claude Code and Codex installed, and you're a member of two workspaces. Multica then registers 4 runtimes:\n\n```mermaid\ngraph TD\n    D[\"Your daemon<br/>MacBook\"]\n    D --> R1[\"Runtime<br/>Workspace A × Claude Code\"]\n    D --> R2[\"Runtime<br/>Workspace A × Codex\"]\n    D --> R3[\"Runtime<br/>Workspace B × Claude Code\"]\n    D --> R4[\"Runtime<br/>Workspace B × Codex\"]\n```\n\nKey points:\n\n- **One daemon can map to multiple runtimes** — one per combination of installed tool and workspace you belong to\n- **The same daemon, workspace, and tool produces exactly one runtime** — restarting the daemon never creates duplicate records\n- The **Runtimes** page in the Multica UI lists these rows\n\n## Custom runtime profiles\n\nBuilt-in provider detection covers the common tools, but teams can also define\n**custom runtime profiles** for AI CLIs that speak one of Multica's supported\nprotocol families and need a workspace-specific launch command. Profiles are\nmanaged from the Runtimes UI or the CLI:\n\n```bash\nmultica runtime profile list\nmultica runtime profile create --display-name \"Composer\" --protocol-family codex --command-name agent\nmultica runtime profile update <profile-id> --command-name agent\nmultica runtime profile delete <profile-id>\n```\n\nThe command is argv-oriented, not a shell string. Multica stores an executable\nname plus fixed arguments, then the daemon launches it directly with\n`exec.Command(command_name, fixed_args...)`. Plain arguments, quotes, and\nbackslash escaping are supported; pipes, redirects, `&&`, `;`, backticks, and\n`$VAR` / `$(...)` expansion are not. Use a wrapper script when the runtime needs\nshell behavior. Today the Runtimes UI owns command-and-argument parsing; the CLI\nprofile commands manage the profile row and local path overrides.\n\nIf a desktop-launched daemon cannot find a command that works in your terminal,\npin the absolute path on that machine:\n\n```bash\nmultica runtime profile set-path <profile-id> --path /abs/path/to/agent\nmultica runtime profile unset-path <profile-id>\n```\n\nProfile command or argument edits apply to newly claimed tasks after the daemon\nre-registers. Running tasks keep the launch arguments they started with. For\nmixed deployments, upgrade the server before rolling out newer daemons: the\nserver-side Runtimes UI stores `fixed_args`, and the server is what surfaces\n`failed_profiles` registration reports. Older components may ignore fields they\ndo not understand instead of failing loudly, so treating the server upgrade as\nthe first step keeps the rollout observable.\n\n> **Cloud runtimes are coming**, currently in a waitlist phase. Once available, you'll be able to execute agent tasks directly on Multica Cloud without running a local daemon.\n\n## When a runtime is marked offline\n\nMultica uses heartbeats to decide whether a runtime is online. Three key numbers:\n\n| Event | Threshold |\n|---|---|\n| Daemon heartbeat frequency | Every **15 seconds** |\n| Marked as missing | No heartbeat for **45 seconds** (3 missed beats) |\n| Auto-deleted | Missing with no associated agents for over **7 days** |\n\nMissing is not permanent — as soon as the daemon sends another heartbeat it returns to online, and the runtime record is preserved. Restarting the daemon does not lose runtimes.\n\n> **Tasks running on a missing runtime are marked as failed** (failure reason `runtime_offline`). For retryable sources (issues, chat), Multica automatically requeues them; Autopilot-triggered tasks are not retried automatically. See [Tasks → Which failures retry automatically](/tasks#which-failures-retry-automatically-which-dont).\n\n## How many tasks can run in parallel\n\nMultica enforces concurrency limits at two layers:\n\n- **Daemon layer**: **20 concurrent tasks** by default (tunable via env var `MULTICA_DAEMON_MAX_CONCURRENT_TASKS`)\n- **Agent layer**: **6 concurrent tasks per agent** by default (configured per-agent)\n\nThe tighter of the two wins. If your daemon is already running 20 tasks, new tasks wait even if an agent still has headroom.\n\nIf you see tasks stuck in `queued` without moving to `dispatched`, one of these two limits is usually saturated.\n\n## What happens to in-flight tasks after a daemon crash\n\nWhen the daemon crashes or is force-killed, the tasks it had picked up are left in `dispatched` or `running`. On the next start, the daemon tells the server: \"these tasks are no longer mine, please mark them failed.\" The server flips them to `failed` with reason `runtime_recovery` — for retryable sources, the tasks are automatically requeued.\n\nEven if this step fails due to a network issue, there's a server-side scan **every 30 seconds** as a backstop: any runtime without a heartbeat for over 45 seconds is marked missing, and its tasks are reclaimed along with it.\n\n## Troubleshooting agents that aren't working\n\nWhen you hit a \"my agent isn't working\" problem, run this three-step checklist first:\n\n1. Run `multica daemon status` — confirm the daemon is running and online\n2. Run `multica daemon logs -f` — check for errors\n3. Open the **Runtimes** page in the Multica UI — confirm your runtime shows \"online\"\n\nMore scenarios in [Troubleshooting](/troubleshooting).\n\n## Next\n\n- [Tasks](/tasks) — the full lifecycle of a task once the daemon picks it up\n- [Providers Matrix](/providers) — capability differences across the 14 AI coding tools"
        },
        {
          "slug": "install-agent-runtime",
          "title": "Install an agent runtime",
          "content": "A **runtime** in Multica is the daemon on your machine paired with one AI coding tool the daemon found on your `PATH`. If the onboarding \"Connect a runtime\" step shows **No supported tools detected**, it means the daemon scanned `PATH` and didn't find any of the 14 tools it knows how to drive. Install one (or several) of the tools below, then come back to the step and re-scan — the runtime will show up within a few seconds.\n\nThis page is the install-side companion to:\n\n- [Daemon and runtimes](/daemon-runtimes) — how detection works\n- [AI coding tools matrix](/providers) — what each tool can and can't do (session resumption, MCP, model selection)\n\n> The Multica server never sees your API keys or the tools themselves. Everything below — installation, authentication, model access — lives on your local machine. If something fails, it's almost always a local problem.\n\n## Before you start\n\nTwo prerequisites apply to **every** tool below:\n\n1. **The Multica daemon must be running.** Run `multica daemon start` after installing the [Multica CLI](/cli). Without a running daemon there is nothing to detect tools.\n2. **The tool's binary must be reachable on `PATH`.** The daemon shells out to each tool by name (see the **Daemon looks for** column in each section). If `which <name>` doesn't find it in your terminal, the daemon won't find it either. After installing, open a fresh terminal (or restart the daemon) so the new `PATH` entry is picked up.\n\nAfter installing a tool, restart the daemon:\n\n```bash\nmultica daemon restart\n```\n\nThe daemon re-scans `PATH` on every start.\n\n## The 14 supported tools\n\nListed roughly from most to least common. Pick whichever ones you already have credentials for — you don't need all 14.\n\n### Claude Code (Anthropic)\n\nThe most complete integration. Session resumption works, MCP works, and it consumes the `mcp_config` field on agents (see the [matrix](/providers#mcp-configuration-provider-specific-support)).\n\n| | |\n|---|---|\n| Daemon looks for | `claude` |\n| Install | Follow the official guide at [claude.com/claude-code](https://www.claude.com/claude-code). The standard route is the npm package `@anthropic-ai/claude-code` (Node.js 18+ required). |\n| Authentication | Run `claude` once and follow the in-CLI login flow, or set `ANTHROPIC_API_KEY`. |\n| Notes | First-choice recommendation for new users. |\n\n### Codex (OpenAI)\n\nJSON-RPC 2.0 transport with finer-grained approval gates. MCP config is written into the per-task `$CODEX_HOME/config.toml`. **Session resumption works** through Codex app-server `thread/resume`; stale or missing threads fall back to a fresh thread.\n\n| | |\n|---|---|\n| Daemon looks for | `codex` |\n| Install | Follow the official guide at github.com/openai/codex. The standard route is the npm package `@openai/codex`. |\n| Authentication | `codex login` (browser-based) or `OPENAI_API_KEY`. |\n\n### Cursor (Anysphere)\n\nThe CLI counterpart to the Cursor editor. **Session resumption works** with current Cursor Agent releases: Multica reads `session_id` from the stream-json events and passes it back with `--resume <id>`.\n\n| | |\n|---|---|\n| Daemon looks for | `cursor-agent` |\n| Install | Install the [Cursor editor](https://cursor.com/) and then the CLI per their docs at [docs.cursor.com](https://docs.cursor.com/). The binary name is `cursor-agent`, not `cursor`. |\n| Authentication | Sign in through the Cursor editor; the CLI reuses that session. |\n\n### GitHub Copilot\n\nModel routing goes through your GitHub account entitlement — the tool doesn't pick a model itself; GitHub decides which model you get.\n\n| | |\n|---|---|\n| Daemon looks for | `copilot` |\n| Install | See GitHub's CLI docs at github.com/github/copilot-cli. |\n| Authentication | Browser-based GitHub login through the CLI. |\n| Notes | Requires an active GitHub Copilot subscription on the signed-in account. |\n\n### OpenCode (SST)\n\nOpen-source CLI agent. Dynamically discovers available models from its own configuration file — good fit for users who want to bring their own model catalog. Consumes the agent's `mcp_config` field through `OPENCODE_CONFIG_CONTENT`.\n\n| | |\n|---|---|\n| Daemon looks for | `opencode` |\n| Install | Follow the official guide at [opencode.ai](https://opencode.ai/) or the GitHub repo at github.com/sst/opencode. The typical route is the install script or the npm package. |\n| Authentication | Configure your model provider(s) per OpenCode's docs (Anthropic, OpenAI, etc.). |\n\n### Kiro CLI (Amazon)\n\nACP-over-stdio transport. Session resumption works through ACP `session/load`; MCP config is passed through ACP `mcpServers`; skills are copied into `.kiro/skills/`.\n\n| | |\n|---|---|\n| Daemon looks for | `kiro-cli` |\n| Install | See the Kiro docs at [kiro.dev](https://kiro.dev/). The binary name is `kiro-cli`, not `kiro`. |\n| Authentication | AWS-account-based; follow Kiro's own onboarding. |\n\n### Kimi (Moonshot)\n\nACP-protocol agent, primarily aimed at the Chinese market. MCP config is passed through ACP `mcpServers`; skills live under `.kimi/skills/` (native discovery).\n\n| | |\n|---|---|\n| Daemon looks for | `kimi` |\n| Install | Follow the official guide at github.com/MoonshotAI/kimi-cli. |\n| Authentication | Moonshot API key, configured per the vendor's docs. |\n\n### Hermes (Nous Research)\n\nACP-protocol agent (shares the transport with Kimi). Session resumption works, and MCP config is passed through ACP `mcpServers`. The skill injection path falls back to the generic `.agent_context/skills/` — verify your skills are loading before relying on them.\n\n| | |\n|---|---|\n| Daemon looks for | `hermes` |\n| Install | See Nous Research's repository at github.com/NousResearch for the latest CLI distribution. |\n| Authentication | Per the vendor's docs. |\n\n### OpenClaw\n\nOpen-source CLI agent orchestrator. MCP config is materialized through Multica's per-task config wrapper. **Model is bound at the agent layer** (`openclaw agents add --model`) — it can't be overridden per task, and you can't pass `--model` or `--system-prompt` from Multica.\n\n| | |\n|---|---|\n| Daemon looks for | `openclaw` |\n| Install | See the project at github.com/openclaw-org/openclaw (community-maintained). |\n| Authentication | Configure the underlying model provider per OpenClaw's docs. |\n\n### Pi (Inflection AI)\n\nMinimalist. **Session resumption is unusual** — the resume id is the path to a session file on disk, not a string id.\n\n| | |\n|---|---|\n| Daemon looks for | `pi` |\n| Install | See Inflection's CLI docs at [pi.ai](https://pi.ai/). |\n| Authentication | Per the vendor's docs. |\n\n### CodeBuddy (Tencent)\n\nA Claude Code–compatible CLI agent. Multica drives it with the same stream-json protocol as Claude Code: session resumption works via `--resume`, MCP config is passed through `--mcp-config`, and skills land in `.claude/skills/`. Models are discovered dynamically.\n\n| | |\n|---|---|\n| Daemon looks for | `codebuddy` |\n| Install | See the official CLI docs at [codebuddy.ai/cli](https://www.codebuddy.ai/cli). |\n| Authentication | Per the vendor's docs. |\n\n### Qoder (Alibaba)\n\nAgentic coding CLI using the ACP protocol over stdio (shares the transport with Hermes, Kimi, and Kiro CLI). Session resumption works through ACP `session/resume`, MCP config is passed through ACP `mcpServers`, model selection is discovered dynamically, and skills are copied into `.qoder/skills/`.\n\n| | |\n|---|---|\n| Daemon looks for | `qodercli` |\n| Install | See the official CLI docs at [qoder.com/cli](https://qoder.com/cli). |\n| Authentication | Per the vendor's docs. |\n\n### Trae CLI (ByteDance)\n\nByteDance's official TRAE CLI (`traecli`, paired with the Trae IDE — **not** the open-source `bytedance/trae-agent`). It is ACP-native, so Multica drives it over stdio via `traecli acp serve --yolo`, sharing the transport with Hermes, Kimi, Kiro CLI, and Qoder. Session resumption works through ACP `session/load`, MCP config is passed through ACP `mcpServers`, model selection is discovered dynamically (switched per task via `session/set_model`), and skills are copied into `.traecli/skills/`.\n\n| | |\n|---|---|\n| Daemon looks for | `traecli` |\n| Install | See the official CLI docs at [docs.trae.cn/cli](https://docs.trae.cn/cli). |\n| Authentication | Run `traecli` once interactively to complete the browser-based enterprise login (the token persists in `~/.trae` plus the OS keyring). Logging into the Trae **IDE** does not log in the CLI — they are separate. |\n| Notes | traecli reads project rules from `.trae/rules/` and has no `--system-prompt` flag, so Multica delivers its runtime brief inline in the prompt. Set a daemon-wide default model with `MULTICA_TRAECLI_MODEL`. |\n\n### Antigravity (Google)\n\nGoogle's Antigravity CLI (`agy`). Pairs with Google's Antigravity service and runs Gemini-backed models. Multica launches it with `agy -p`, the daemon-compatible non-interactive mode; current Antigravity CLI releases can execute tools from that mode, while `agy -i` requires an attached TTY. Session resumption works through `--conversation <id>`, captured by the daemon from the CLI log file. Model selection is managed inside the Antigravity CLI itself — Multica disables the per-agent model picker for this provider. Skills are written to `.agents/skills/` (the CLI inherits Gemini CLI's workspace skill layout — see [Antigravity docs](https://antigravity.google/docs/gcli-migration)).\n\n| | |\n|---|---|\n| Daemon looks for | `agy` |\n| Install | Follow the official guide at [antigravity.google/docs/cli-overview](https://antigravity.google/docs/cli-overview). The CLI ships pre-built — run `agy install` once to wire up PATH and shell aliases. |\n| Authentication | Run `agy` once interactively and complete the Google account login, or sign in via the Antigravity desktop app — the CLI reuses the keyring entry the GUI writes. |\n| Notes | The CLI emits plain assistant text on stdout, not a structured event stream; intermediate \"I will run X\" lines and the final reply are both relayed to Multica as text, and per-tool telemetry is not available today. |\n\n## After installing\n\n1. **Confirm the binary is on `PATH`.** Open a fresh terminal and run `which <name>` (for example `which claude`, `which cursor-agent`, `which kiro-cli`, `which agy`). If it prints a path, the daemon will find it. If it prints nothing, fix your shell `PATH` first (the typical cause is a per-shell rc file that wasn't reloaded).\n2. **Restart the daemon.** `multica daemon restart`. The daemon only scans `PATH` at startup.\n3. **Check the Runtimes page.** In the Multica UI, the **Runtimes** page should now list one row per `(workspace × tool)` combination. If the row says \"offline\", see [Daemon and runtimes → When a runtime is marked offline](/daemon-runtimes#when-a-runtime-is-marked-offline).\n4. **Go back to onboarding.** The \"Connect a runtime\" step polls and will pick up the new runtime within a few seconds — no need to refresh.\n\n## Troubleshooting\n\n- **`which` finds the binary but the daemon doesn't.** The daemon was started with an older `PATH`. Restart it.\n- **The binary exists but launching fails.** Run the tool's own `--version` or `--help` once from the terminal — most failures here are missing auth, expired tokens, or a Node.js / runtime mismatch.\n- **The Runtimes page shows the row, but tasks fail immediately.** Check `multica daemon logs -f` while triggering a task. The daemon surfaces the tool's own error output.\n\nFor broader symptoms, see the [Troubleshooting guide](/troubleshooting).\n\n## Next\n\n- [Daemon and runtimes](/daemon-runtimes) — how detection, heartbeats, and offline handling work\n- [AI coding tools matrix](/providers) — capability differences once a tool is connected\n- [Creating and configuring agents](/agents-create) — pick a tool for your agent and start running tasks"
        },
        {
          "slug": "tasks",
          "title": "Tasks",
          "content": "A **task** is the unit of every [agent](/agents) run — [assigning an issue to an agent](/assigning-issues), [@-mentioning an agent in a comment](/mentioning-agents), sending a message in [chat](/chat), or an [Autopilot](/autopilots) firing on schedule all produce a task. Multica puts it in a queue; a [daemon](/daemon-runtimes) picks it up and hands it off to the corresponding [AI coding tool](/providers), then writes the result back to the server when it finishes.\n\nTasks and [issues](/issues) are two different objects. A single issue can be assigned, @-mentioned, and manually rerun many times — each produces a **new** task.\n\n## The states a task goes through\n\n```mermaid\ngraph LR\n    Q[\"Queued<br/>queued\"] -->|daemon picks up| D[\"Dispatched<br/>dispatched\"]\n    D -->|agent starts| R[\"Running<br/>running\"]\n    R -->|success| C[\"Completed<br/>completed\"]\n    R -->|error or timeout| F[\"Failed<br/>failed\"]\n    Q -->|user cancels| X[\"Cancelled<br/>cancelled\"]\n    D -->|user cancels| X\n    R -->|user cancels| X\n    F -.retryable reason.-> Q\n```\n\n- **Queued** — the task was just created and is waiting for a daemon to pick it up\n- **Dispatched** — a daemon has claimed it and is starting the AI coding tool\n- **Running** — the AI coding tool is actually doing the work\n- **Completed** — finished successfully; the output (comments, code commits, status changes) is written back to the server\n- **Failed** — aborted with an error or timeout; if the failure reason is retryable, the task automatically returns to `queued` for another attempt\n- **Cancelled** — the user cancelled it\n\n## What happens when a task times out\n\nThe Multica server scans every 30 seconds. Two kinds of timeout trigger a failure:\n\n| Situation | Timeout |\n|---|---|\n| Dispatched but never started (daemon picked it up but didn't launch the AI tool) | **5 minutes** |\n| Running too long | **2.5 hours** |\n\nBoth timeouts use failure reason `timeout` and **retry automatically** (next section). For the related runtime-missing check, see [Daemon and runtimes → When a runtime is marked offline](/daemon-runtimes#when-a-runtime-is-marked-offline).\n\n## Which failures retry automatically, which don't\n\nFailures fall into two categories: **retryable** and **non-retryable**.\n\n**Retryable** (Multica automatically requeues):\n\n- `runtime_offline` — the daemon went missing after the task was dispatched\n- `runtime_recovery` — the daemon crashed and restarted, reclaiming tasks it didn't finish\n- `timeout` — runtime or dispatch timeout\n\n**Non-retryable** (the task stays in failed):\n\n- `agent_error` — the AI coding tool itself reported an error (API error, quota exceeded, internal bug). Underlying problems aren't retried — that would loop forever.\n\nAutomatic retry also has two extra conditions:\n\n1. **At most 2 attempts** — 1 original + 1 retry. If the retry also fails, no further retries, even if the reason is retryable.\n2. **Only for issue- and chat-triggered tasks** — Autopilot-triggered tasks do **not** retry automatically.\n\n> **Autopilot tasks don't retry automatically** by design. An Autopilot has its own firing cadence (e.g. daily); automatic retries on failure would overlap with the next scheduled run. If you need an immediate re-run after failure, use a manual rerun (next section).\n>\n> **How you'll know an Autopilot task failed**: a notification lands in your [Inbox](/inbox), and the associated issue's status reverts from `in_progress` back to `todo`. The [Autopilots](/autopilots) page also shows the latest run result per autopilot.\n\n## Manual rerun vs. automatic retry\n\nA **manual rerun** is one you trigger from the CLI or the API (`POST /api/issues/{id}/rerun`):\n\n```bash\nmultica issue rerun <issue-id>\n```\n\nBehavior:\n\n- By default, targets the issue's **current agent assignee** — useful when you want the rerun to follow the current assignment regardless of who ran the prior task.\n- The execution-log retry button on a specific row sends that row's task ID alongside, so the rerun targets **the agent that ran that exact task** — not the current assignee. This makes per-row retry meaningful for squad workers, parallel @-mention agents, or rows whose agent has since been displaced by a reassignment.\n- **Cancels** the target agent's queued or running task on this issue (if any). Tasks owned by other agents on the same issue (e.g. parallel @-mention runs) are left alone.\n- Creates a **brand-new** task — attempt count resets to 1, even if the original task hit the attempt ceiling.\n- Starts a **fresh agent session** — the prior session ID is **not** inherited. A manual rerun means you've judged the previous output bad, so resuming the same conversation would replay the same poisoned state. (Automatic retry, by contrast, does inherit the session — that path is for infrastructure failures, not bad output.)\n\nComparison:\n\n| Dimension | Automatic retry | Manual rerun |\n|---|---|---|\n| Trigger | System, based on failure reason | You, manually |\n| Ceiling | 2 attempts | No limit |\n| Applicable sources | Issues, chat | Issues with an agent assignee |\n| Agent picked | Same agent as the failed task | Source task's agent (UI per-row retry) or issue's current assignee (CLI / no task_id) |\n| Session inheritance | Yes (resumes prior session) | No (fresh session) |\n\n## How a failed task affects issue status\n\nIf an issue-triggered task fails (and no automatic retry succeeds) because the issue was assigned to an agent, **the issue's status automatically rolls back from `in_progress` to `todo`** — so when you open the board you immediately see \"this one needs another look.\" See [Issues and projects](/issues).\n\n## Can a task continue from the previous context\n\nYes — as long as the AI coding tool supports session resumption.\n\nMultica pins the session ID **twice** during a task: once at the start (when the AI tool returns its first system message), and once at the end (on completion or failure). The first lets the daemon recover if it crashes mid-run; the second is reserved for the next **automatic retry**, where that ID is passed back so the agent can pick up the previous conversation and file state. **Manual rerun deliberately skips this** and starts a fresh session — see [Manual rerun vs. automatic retry](#manual-rerun-vs-automatic-retry).\n\nBut **which AI coding tools actually support this** varies a lot:\n\n- ✅ **Real support** — Antigravity, Claude Code, Codex, Copilot, Cursor, Hermes, Kimi, Kiro CLI, OpenCode, OpenClaw, Pi\n- ❌ **No support** — Gemini\n\nSee [Providers Matrix → Session resumption](/providers#session-resumption-who-really-supports-it).\n\n## Next\n\n- [Providers Matrix](/providers) — capability differences across the 14 AI coding tools (including the exact session-resumption status)\n- [Assigning issues to agents](/assigning-issues) / [@-mentioning agents in comments](/mentioning-agents) / [Chat](/chat) / [Autopilots](/autopilots) — the four ways to trigger a task"
        },
        {
          "slug": "providers",
          "title": "AI coding tools matrix",
          "content": "Multica ships with built-in support for **14 AI coding tools**. They all implement the same interface — queue, dispatch, execute, return results — so you can drive any of them from the same Multica board. **But the capability details diverge significantly**: whether session resumption actually works, whether MCP is supported, where skill files live, how models are selected. This page is the full matrix.\n\nFor guidance on picking a tool when creating an agent, see [Creating and configuring agents](/agents-create).\n\n## Capability matrix\n\n| Tool | Vendor | Session resumption | MCP | Skill injection path | Model selection |\n|---|---|---|---|---|---|\n| **Antigravity** | Google | ✅ (`--conversation <id>`) | ❌ | `.agents/skills/` | Dynamic discovery (`agy models`) |\n| **Claude Code** | Anthropic | ✅ | ✅ | `.claude/skills/` | Static + flag |\n| **CodeBuddy** | Tencent | ✅ | ✅ | `.claude/skills/` | Dynamic discovery |\n| **Codex** | OpenAI | ✅ | ✅ | `$CODEX_HOME/skills/` | Static |\n| **Copilot** | GitHub | ✅ | ❌ | `.github/skills/` | Static (determined by account entitlement) |\n| **Cursor** | Anysphere | ✅ | ✅ | `.cursor/skills/` | Dynamic discovery |\n| **Hermes** | Nous Research | ✅ | ✅ | `.agent_context/skills/` (fallback) | Dynamic discovery |\n| **Kimi** | Moonshot | ✅ | ✅ | `.kimi/skills/` | Dynamic discovery |\n| **Kiro CLI** | Amazon | ✅ | ✅ | `.kiro/skills/` | Dynamic discovery |\n| **OpenCode** | SST | ✅ | ✅ | `.opencode/skills/` | Dynamic discovery + variants |\n| **OpenClaw** | Open source | ✅ | ✅ | `.agent_context/skills/` (fallback) | Bound to the agent, can't be switched per task |\n| **Pi** | Inflection AI | ✅ (session is a file path) | ❌ | `.pi/skills/` | Dynamic discovery |\n| **Qoder** | Alibaba | ✅ | ✅ | `.qoder/skills/` | Dynamic discovery |\n| **Trae** | ByteDance | ✅ (ACP `session/load`) | ✅ | `.traecli/skills/` | Dynamic discovery |\n\n## What each tool is for\n\n### Antigravity\n\nFrom Google. CLI binary name is `agy`. Pairs with Google's Antigravity service and ships with a Gemini-backed default model. Multica launches Antigravity with `agy -p` because that is the daemon-compatible non-interactive mode; `agy -i` needs an attached TTY and is not suitable for background task execution. Current Antigravity CLI releases can still execute tools from this mode, but stdout is plain assistant text rather than a structured event stream, so Multica relays the transcript as text and cannot show per-tool telemetry for Antigravity today. **Session resumption works** via `--conversation <id>`; the daemon captures the conversation UUID from the CLI's log file. **Model selection works** via the `--model` flag (added in agy 1.0.6): the daemon enumerates the catalog with `agy models` and ships the chosen value verbatim. Note these are human display strings such as `Claude Opus 4.6 (Thinking)`, not `provider/model` slugs — and agy silently no-ops on a value it doesn't recognise, so prefer picking from the discovered list over typing a custom one. Skills land in `.agents/skills/` (the CLI inherits Gemini CLI's workspace skill layout — see [Antigravity migration docs](https://antigravity.google/docs/gcli-migration)).\n\n### Claude Code\n\nFrom Anthropic. **First choice for new users** — the most complete feature set: session resumption actually works, it reads MCP configuration, and it supports fine-tuning flags like `--max-turns` and `--append-system-prompt`. Requires an Anthropic API key.\n\n### CodeBuddy\n\nFrom Tencent. A Claude Code–compatible CLI agent — Multica drives it with the same stream-json protocol as Claude Code, so session resumption works (via `--resume`), MCP config is passed through `--mcp-config`, and skills use Claude Code's `.claude/skills/` layout. Models are discovered dynamically.\n\n### Codex\n\nFrom OpenAI. Uses JSON-RPC 2.0, has stronger statefulness, and a finer-grained approve mechanism (manual approval for `exec_command` and `patch_apply`). MCP config is materialized into the per-task `$CODEX_HOME/config.toml`. **Session resumption works** through Codex app-server `thread/resume`; if the saved thread is missing or stale, Multica falls back to a fresh thread so the task can still run.\n\n### Copilot\n\nFrom GitHub. Model routing goes through your GitHub account entitlement — the tool doesn't select a model itself; GitHub decides which model you get. Placing skills in `.github/skills/` is GitHub CLI's native discovery mechanism.\n\n### Cursor\n\nFrom Anysphere, the CLI counterpart to the Cursor editor. **Session resumption works** with current Cursor Agent releases: the stream-json event includes a `session_id`, and Multica passes it back with `--resume <id>` on the next run. MCP config is materialized into the task workspace's `.cursor/mcp.json`, with Cursor's project approval file written under a per-task `CURSOR_DATA_DIR` so managed MCP servers do not depend on the user's global Cursor approvals.\n\n### Hermes\n\nFrom Nous Research. Uses the ACP protocol (shares a transport with Kimi). Session resumption works, and MCP config is passed through ACP `mcpServers`. But the **skill injection path is the generic fallback** (`.agent_context/skills/`), not a dedicated one — if the Hermes CLI itself doesn't read this path, skills may not take effect. Verify by testing.\n\n**Selecting a Hermes profile.** To launch Hermes under a specific profile, set the agent's `custom_args` to the profile flag and the profile name as two separate entries — for example, for a profile named `research`:\n\n```json\n[\"-p\", \"research\"]\n```\n\nDon't combine them into one string like `\"-p research\"`; Multica passes each array item as a separate argv entry. `custom_args` is configured per agent — see [Creating and configuring agents](/agents-create).\n\n### Kimi\n\nFrom Moonshot, aimed at the Chinese market. Shares the ACP protocol with Hermes, including MCP config through ACP `mcpServers`, but the skill path `.kimi/skills/` is Kimi CLI's native discovery mechanism — different from Hermes's fallback.\n\n### Kiro CLI\n\nFrom Amazon. Uses ACP over stdio via `kiro-cli acp`. Session resumption works through ACP `session/load`, MCP config is passed through ACP `mcpServers`, model selection works through `session/set_model`, and skills are copied into `.kiro/skills/` for native project-level discovery.\n\n### OpenCode\n\nFrom SST, open source. Dynamically discovers available models and model variants (scans the CLI's configuration file). Session resumption works, and it consumes the agent's `mcp_config` field — Multica injects it inline through the `OPENCODE_CONFIG_CONTENT` environment variable, so the agent's MCP servers reach OpenCode without writing anything into the task workdir's `opencode.json` (the agent or the user keep ownership of that file). When a model exposes variants, Multica shows them as the agent thinking selector and passes the selected value through `opencode run --variant`. **Suitable for tinkerers who want to customize their model catalog.**\n\n### OpenClaw\n\nOpen-source project, a CLI agent orchestrator. MCP config is materialized through Multica's per-task OpenClaw config wrapper. **Model is bound at the agent layer** (`openclaw agents add --model`) — it can't be overridden per task. Configuration is strictly controlled: users can't pass `--model` or `--system-prompt`; the agent-registration config decides.\n\n### Pi\n\nFrom Inflection AI, minimalist. **Session resumption is unusual** — the session ID is a file path on disk (`~/.pi/...`) rather than a string ID. In other tools, the resume id is a string returned by the CLI; in Pi, the resume id is the session file itself.\n\n### Qoder\n\nFrom Alibaba. An agentic coding CLI. Uses the ACP protocol over stdio (shares a transport with Hermes, Kimi, and Kiro CLI). Session resumption works through ACP `session/resume`, MCP config is passed through ACP `mcpServers`, model selection is discovered dynamically, and skills are copied into `.qoder/skills/` for native discovery.\n\n### Trae\n\nFrom ByteDance — the official **TRAE CLI** (`traecli`, documented at [docs.trae.cn/cli](https://docs.trae.cn/cli); this is the product paired with the Trae IDE, **not** the open-source `bytedance/trae-agent`). traecli is ACP-native, so Multica drives it over the standard ACP JSON-RPC transport via `traecli acp serve --yolo`, exactly like Kiro and Qoder — `--yolo` puts it in bypass-permissions mode so headless runs don't block on tool-approval prompts.\n\n- **Session resumption works** through ACP `session/load` (traecli advertises `loadSession: true` from `initialize`).\n- **Model selection is dynamic**: the model catalog is discovered from `session/new` (`models.availableModels`) and switched per task via `session/set_model`. Set the daemon-wide default with `MULTICA_TRAECLI_MODEL`.\n- **MCP is supported** — config is passed through ACP `mcpServers`; traecli advertises `mcpCapabilities: {http, sse}`, and unsupported transports are filtered out before `session/new`.\n- **Authentication** is traecli's own enterprise login: run `traecli` once interactively to complete the browser login (the token persists in `~/.trae` + the OS keyring). The IDE login is separate — logging into the Trae IDE does **not** log in the CLI.\n- **Skills** are written to `.traecli/skills/` (project) for native discovery; global skills live in `~/.traecli/skills`.\n- The Multica runtime brief is also inlined into the prompt (traecli has no `--system-prompt` flag and reads project rules from `.trae/rules/`, not `AGENTS.md`), so the workflow instructions reach the agent regardless.\n\n> The capabilities above were captured from the real `traecli` v0.120.42 binary: `initialize` → `loadSession:true` + `mcpCapabilities{http,sse}`; `session/new` → `result.sessionId` + `models.availableModels`; `session/prompt` streams `session/update` notifications with `sessionUpdate: agent_message_chunk`.\n\n## Session resumption: who really supports it\n\nThe session resumption mechanism is covered in [Tasks](/tasks#can-a-task-continue-from-the-previous-context). **Every supported tool resumes sessions** — pass the resume id and the task continues from the previous context. The one quirk is Pi, whose resume id is a session file path on disk rather than a string id (see [Pi](#pi) above).\n\n## MCP configuration: provider-specific support\n\n**Of the 14 tools, eleven consume `mcp_config`: Claude Code, CodeBuddy, Codex, Cursor, Hermes, Kimi, Kiro CLI, OpenCode, OpenClaw, Qoder, and Trae**. The other three (Antigravity, Copilot, Pi) accept the field but **ignore it** — no error, no warning, the config just has no effect.\n\nThe runtime paths are provider-specific: Claude Code and CodeBuddy receive it through `--mcp-config` paired with `--strict-mcp-config`; Codex writes a daemon-managed `mcp_servers` block into the per-task `$CODEX_HOME/config.toml`; Cursor writes `.cursor/mcp.json` plus per-task project approvals under `CURSOR_DATA_DIR`; Hermes, Kimi, Kiro CLI, Qoder, and Trae receive ACP `mcpServers`; OpenCode receives inline config through `OPENCODE_CONFIG_CONTENT`; OpenClaw receives `mcp.servers` through Multica's per-task config wrapper. OpenCode's path does **not** rewrite the project's `opencode.json`.\n\n> If you set `mcp_config` in an agent configuration but pick a tool not marked ✅ in the MCP column, your MCP servers have **no effect** on that agent. MCP integration is provider-specific.\n\n## Where skill files go\n\nEach tool uses **its own** skill discovery path. Before a task runs, the Multica daemon copies the workspace's skill files into the corresponding path:\n\n| Tool | Path | Native discovery? |\n|---|---|---|\n| Claude Code | `.claude/skills/` | ✅ Native |\n| CodeBuddy | `.claude/skills/` | ✅ Native |\n| Codex | `$CODEX_HOME/skills/` | ✅ Native |\n| Copilot | `.github/skills/` | ✅ Native |\n| Cursor | `.cursor/skills/` | ✅ Native |\n| Kimi | `.kimi/skills/` | ✅ Native |\n| Kiro CLI | `.kiro/skills/` | ✅ Native |\n| OpenCode | `.opencode/skills/` | ✅ Native |\n| Pi | `.pi/skills/` | ✅ Native |\n| Qoder | `.qoder/skills/` | ✅ Native |\n| Antigravity | `.agents/skills/` | ✅ Native (inherits Gemini CLI's workspace layout — see [Antigravity docs](https://antigravity.google/docs/gcli-migration)) |\n| Hermes | `.agent_context/skills/` | ⚠️ Generic fallback |\n| OpenClaw | `.agent_context/skills/` | ⚠️ Generic fallback |\n\nWhether a fallback-path tool actually reads this directory depends on the tool's own documentation — no guarantees. If your skills aren't taking effect for Hermes / OpenClaw, check this first.\n\nFor native project-level paths, repo-scoped discovery is expected: if the checked-out repository already contains a matching directory, the underlying tool can discover those committed skills on its own. You do not need to import those repo skills into Multica just to use them in that repo. Multica keeps the repo files intact. If a workspace skill has the same natural directory name, the daemon writes the workspace copy to a collision-free sibling such as `review-helper-multica`.\n\nFor creating and using skills, see [Skills](/skills).\n\n## Next\n\n- [Creating and configuring agents](/agents-create) — pick a tool for your agent\n- [Tasks](/tasks) — task lifecycle and session-resumption mechanics\n- [Daemon and runtimes](/daemon-runtimes) — where the tools run and how they connect to Multica\n- [Install an agent runtime](/install-agent-runtime) — installation and authentication for each of the 14 supported tools"
        }
      ]
    },
    {
      "label": "Collaborating with agents",
      "labelKey": "collaborating_with_agents",
      "pages": [
        {
          "slug": "assigning-issues",
          "title": "Assign issues to agents",
          "content": "Assign an [issue](/issues) to an [agent](/agents) and it works as the **official assignee** until the work is done — it can read the full issue context (description + all [comments](/comments)) and change status, post comments, and edit fields. This is the **most common and heaviest** of Multica's four trigger paths. The same flow also accepts a [squad](/squads) as the assignee — Multica then triggers the squad's **leader agent** instead.\n\n| Path | When to use | Changes the issue | Context | Priority | Auto retry |\n|---|---|---|---|---|---|\n| **Assign** | Hand an agent ownership | Changes assignee | Issue + all comments | Inherits from issue | ✓ |\n| [**@-mention**](/mentioning-agents) | Pull it in to take a look | No changes | Issue + trigger comment | Inherits from issue | ✓ |\n| [**Chat**](/chat) | One-to-one conversation outside any issue | No issue involved | Current conversation history | Fixed medium | ✓ |\n| [**Autopilots**](/autopilots) | Scheduled or manual automation | Depends on mode | Depends on mode | Set by autopilot | ✗ |\n\n\"Auto retry\" refers to retries after infrastructure failures (runtime offline, timeout). Business errors on the agent side (for example, the model reporting an error) are not retried. See [**Tasks**](/tasks) for details.\n\n## Assign from the UI\n\nOn the issue detail page, click the **Assignee** picker. It lists every member in the workspace, all non-archived agents, and every non-archived [squad](/squads). Pick an agent (or squad) and the issue is assigned right away.\n\nA few rules:\n\n- **Workspace agents** can be assigned by any member; **private agents** can only be assigned by their owner or a workspace admin.\n- You can only assign to agents that have **an online runtime** — agents with no one running them show as unavailable in the picker.\n- When the issue status is **Backlog**, assigning **does not trigger the agent** — Backlog is a parking lot; the agent only gets enqueued once you move the issue to Todo or In Progress.\n\n## Assign from the CLI\n\nThe command-line equivalent:\n\n```bash\nmultica issue assign MUL-42 --to alice\nmultica issue assign MUL-42 --to-id 5fb87ac7-23b5-4a7a-81fa-ed295a54545d\n```\n\n`--to` takes a member username or an agent name (fuzzy match). When names overlap — e.g. an agent `J` alongside `Cursor - J` — pass `--to-id <uuid>` instead, using the `user_id` (member) or `id` (agent) from `multica workspace member list --output json` / `multica agent list --output json`. UUID matching is strict and unambiguous, which is what you want from scripts and from agents driving the CLI. `--to` and `--to-id` are mutually exclusive.\n\nUnassign:\n\n```bash\nmultica issue assign MUL-42 --unassign\n```\n\n## What happens after assignment\n\nWhen a non-Backlog issue is assigned to an agent, Multica immediately does the following in the background:\n\n1. Enqueues a `queued` `task` with priority inherited from the issue, routed to the runtime where the agent lives.\n2. The agent's daemon picks up the `task` on its next poll and transitions it to `dispatched`.\n3. The agent starts working and the `task` moves to `running`; on completion it becomes `completed` or `failed`.\n4. During execution the agent can change the issue's status, post comments, and edit fields — these actions appear under the agent's identity.\n\n**If the agent is offline**, the `task` waits in the queue — **it times out and fails after 5 minutes** with reason `runtime_offline`. For retryable sources (assign, @-mention, chat), Multica automatically re-enqueues it. See [**Tasks**](/tasks) for the full retry rules.\n\nAssigning also auto-subscribes the agent to the issue — but in Multica **agents do not receive inbox notifications** (only members do). This subscription is internal bookkeeping with no user-visible side effect.\n\n## Reassign or unassign\n\nWhen you change the assignee from Agent A to Agent B:\n\n1. **Everything A has in flight is cancelled** — every `task` in `queued`, `dispatched`, or `running` state is marked `cancelled`.\n2. **B is enqueued a new `task` immediately** (if the issue is not in Backlog and B has an online runtime).\n\n> **Reassignment cancels every active `task` on this issue — not just the old assignee's.** If another agent is working on this issue because of an @-mention, its `task` is cancelled too. There is currently no UI action to cancel a single agent's `task` in isolation.\n\nUnassigning (`--unassign` or picking \"none\" in the picker) marks all active `task` entries as `cancelled` and **does not enqueue a new one**. Existing subscriptions are not cleared automatically — the old assignee stays on the subscription list (but still receives no inbox notifications).\n\n## Why only one active `task` per agent per issue\n\n**A single agent can have at most one `queued` or `dispatched` `task` on the same issue at any time.** A unique index at the database level plus the claim logic enforces this — it prevents duplicate enqueues and concurrent executions overwriting each other.\n\nBut **different agents can work on the same issue in parallel** — for example, Agent A is the assignee and Agent B is @-mentioned; both `task` entries can coexist, each running on its own runtime. See [**Tasks**](/tasks) for the full serial/concurrent rules.\n\n## Next\n\n- [**@-mention an agent in a comment**](/mentioning-agents) — a lighter trigger that leaves assignee and status untouched\n- [**Squads**](/squads) — assign to a group of agents and let the leader decide who picks it up\n- [**Chat**](/chat) — one-to-one conversation outside any issue\n- [**Autopilots**](/autopilots) — let agents start work automatically on a schedule"
        },
        {
          "slug": "mentioning-agents",
          "title": "@-mention agents in comments",
          "content": "`@`-mentioning an [agent](/agents) in a [comment](/comments) is the lighter trigger — **no assignee change, no status change**, just a nudge to have the agent take a look at the current [issue](/issues). Compared to [**assigning**](/assigning-issues) (turning the agent into the owner and handing over the issue), @-mention fits \"take a look at this section,\" \"give me another angle,\" or \"pull them in for a quick discussion.\"\n\n## Mention an agent in a comment\n\nSame as mentioning a member — type `@` to open the picker and select an agent. Once the comment is posted, Multica immediately enqueues a `task` for each mentioned agent with **that comment** as its trigger context. When the agent receives the task it can read:\n\n- The full issue (description + every historical comment)\n- The trigger comment itself — as the starting point for this run\n\nThe `@mention` Markdown syntax, the picker, and `@all` semantics are covered in [**Comments**](/comments).\n\n> **You can also `@`-mention a [squad](/squads) in a comment.** The same picker surfaces squads alongside members and agents; selecting one inserts `[@SquadName](mention://squad/<uuid>)` and triggers the squad's **leader agent** to coordinate a response — assignee and status stay untouched.\n\n## How it differs from assignment\n\nBoth put the agent to work, but the mechanics are entirely different:\n\n| Dimension | Assign | @-mention |\n|---|---|---|\n| Changes `assignee` | ✓ | ✗ |\n| Changes `status` | ✗ | ✗ |\n| Enqueues a `task` | Immediately (non-Backlog) | Immediately |\n| Trigger comment ID | Optional | Always carries the current comment |\n| Agents targeted per action | 1 (one assignee) | Many (a comment can @ multiple) |\n| Priority | Inherits from issue | Inherits from issue |\n\nThe rule of thumb is simple: **use assignment when you want the agent to \"own this issue from now on\"; use @-mention when you want it to \"take a look at the current context.\"**\n\n## What happens when you @ multiple agents\n\nIf one comment @-mentions several agents, each one is enqueued an independent `task` on its own runtime — **they run in parallel** without blocking each other.\n\nIf an agent already has a `queued` or `dispatched` `task` on the same issue (for example, it was just mentioned and has not started yet), the new mention is **deduplicated** and no duplicate `task` is enqueued. Deduplication is **scoped to a single comment** — two different comments seconds apart that both @ the same agent will both enqueue a `task`.\n\n> **Adding an @ by editing a comment does not re-trigger.** If you remember to add `@agent` only after posting, editing in the `@` only changes what is displayed — it **does not** deliver a new `task` to that agent. To trigger it, post a new comment or assign the issue to it.\n\n## `@all` does not trigger any agent\n\nWhen you call everyone with `@all`, **only workspace members land in the inbox — agents are not included in the `@all` expansion.** This is by design: agents do not receive inbox notifications, so `@all` has no meaning for them. To put an agent to work, mention it by name.\n\n## Agents @-mentioning themselves does not loop\n\nAgents can post comments while executing, and those comments may contain `@mention`s. Multica has a hardcoded guard: **if the comment author is the same as the agent targeted by an `@` mention, that mention is skipped** — there is no \"agent A @ agent A → new task → @ agent A again\" infinite loop.\n\nThis guard **only blocks direct self-references.** Agent A @-mentioning agent B works normally; if B then @-mentions A in its reply, A is triggered again — in other words, **indirect recursion is not blocked**. When writing agent instructions, be careful not to let a group of agents @-mention each other in a cycle.\n\n## Next\n\n- [**Squads**](/squads) — `@`-mention a squad to have the leader route the question to the right member\n- [**Chat**](/chat) — one-to-one conversation outside any issue\n- [**Autopilots**](/autopilots) — let agents start work automatically on a schedule\n- [**Comments**](/comments) — `@mention` syntax, the picker, and `@all` semantics"
        },
        {
          "slug": "chat",
          "title": "Chat",
          "content": "**Chat is a one-to-one conversation between you and an [agent](/agents)** — stepping outside the [issue](/issues) board. The agent sees no issues and cannot change any issue, and the entire conversation is **fully private** (nobody else in the [workspace](/workspaces), including admins, can see it). It fits discussing an approach with an agent, brainstorming, or asking a question that does not belong to any issue.\n\n## Why not just @-mention the agent?\n\n[@-mention](/mentioning-agents) **pulls the agent into** an issue's context — it reads the issue description and every historical comment, and it can change the issue. Chat flips this: **it pulls you out of** the issue — the agent only sees this single conversation, has no awareness of any issue, and has no entry point to modify one.\n\nTwo rules of thumb:\n\n- You want feedback grounded in the context of a specific issue → [@-mention](/mentioning-agents)\n- You want to discuss a topic unrelated to any issue (or you do not want anyone else to see the discussion) → Chat\n\n## Start a conversation\n\nOpen **Chat** from the sidebar, pick an agent, and start a new conversation. The interface feels like any messaging app: you send a message, the agent replies. Each message triggers a run in the background (an enqueued `task`), so replies may take a few seconds.\n\n## What an agent can and cannot do in chat\n\nAgents run in a **fully sandboxed** mode inside a conversation.\n\n**Can do:**\n\n- Answer the questions in your current message\n- Use its configured [skills](/skills) and MCP\n- Read and write files in its own working directory\n- Call `multica` CLI commands that do not need issue context (for example, querying basic workspace info)\n\n**Cannot do:**\n\n- **See any issue** — the prompt the agent receives has no issue IDs, and commands like `multica issue list` return empty\n- **Change any issue** — without issue context, API calls are blocked by permission checks\n- **See other conversations** — conversations are fully isolated\n- **@-mention anyone or any agent** — chat is a private space with no path to notify others\n\n## How multi-turn context is preserved\n\nChat maintains multi-turn context via **provider session resumption** — the agent establishes a provider session on its first reply (for example, a Claude session), and the session ID is stored. On the next message, the task dispatch passes that ID back so the agent **resumes from where it left off** without re-reading history every time.\n\nIf **one turn fails**, Multica looks up the previous task that had established a session ID (whether that task succeeded or failed) and tries to resume — a single failure in the middle does not drop the memory of the whole conversation.\n\nNote: not every provider actually implements session resumption — see the [**Providers Matrix**](/providers) for support status.\n\n## Archive a conversation\n\nConversations you no longer want to see can be archived — right-click in the conversation list or use the \"Archive\" button on the detail page. After archiving:\n\n- The conversation disappears from the active list (you can still find it in the \"Archived\" view)\n- Historical messages, session ID, and the working directory are all preserved — nothing is deleted\n\n> **There is no \"restore\" button after archiving.** There is currently no entry point to move an archived conversation back to active. If you want to continue the thread later, you will need to start a new conversation. To revisit content in an archived conversation, open the \"Archived\" view and read through the history.\n\n## Next\n\n- [**Autopilots**](/autopilots) — let agents start work automatically on a schedule\n- [**Assign issues to agents**](/assigning-issues) — bring the topic back onto the issue board"
        },
        {
          "slug": "autopilots",
          "title": "Autopilots",
          "content": "Autopilots let [agents](/agents) **start work automatically on a schedule** — configure a cron expression and a timezone, and Multica dispatches a [`task`](/tasks) on its own, without you triggering anything. It fits periodic checks, recurring reports, and overnight cleanup jobs — the \"standing order\" shape of work. Compared to the other three trigger paths ([assigning](/assigning-issues), [@-mention](/mentioning-agents), and [chat](/chat), where you are the one kicking things off), the core difference with Autopilots is that they are **time-driven**.\n\n## Configure an autopilot\n\nCreate a new autopilot on the workspace's **Autopilot** page. You set:\n\n- **Name** — display name\n- **Agent** — who the run is dispatched to\n- **Priority** — inherited by the `task` it produces (same semantics as issue priority)\n- **Description / prompt** — the work description the agent receives each run\n- **Execution mode** — see below\n- **Triggers** — at least one `schedule` (cron + timezone) or `webhook`\n\n## Pick an execution mode\n\nAn autopilot has two execution modes. **Start with \"create issue\" mode.**\n\n- **Create issue mode** (`create_issue`) — default, **recommended**. Each trigger first creates an issue in the workspace (the title currently supports a single placeholder, `{{date}}`, which interpolates to the UTC date in `YYYY-MM-DD` format; any other `{{...}}` token is rejected at create-time so a typo cannot silently land as the literal string in your issue titles), then assigns the issue to the agent through the normal assignment flow. All work lands on the issue board with the same history, comments, and status as a manually assigned issue.\n- **Run-only mode** (`run_only`) — skips issue creation and enqueues a `task` directly. The run is invisible on the board — you can only see it in the autopilot's run history.\n\n## Run it on a schedule\n\nEvery autopilot needs at least one `schedule` trigger. Cron uses the **standard 5-field format** (minute hour day month weekday), with **1-minute** minimum granularity (no seconds). Timezone is IANA-formatted (for example, `Asia/Shanghai`) and determines which timezone the cron expression is interpreted in.\n\nA few examples:\n\n- `0 9 * * 1-5`, `Asia/Shanghai` — 9 AM Beijing time on weekdays\n- `*/30 * * * *`, `UTC` — every 30 minutes\n- `0 3 * * *`, `UTC` — every day at 3 AM UTC\n\nThe Multica server scans for due triggers every **30 seconds** — **the actual fire time can lag by up to 30 seconds**, not down to the second. If the server is restarted across a fire time, it catches up missed triggers on startup (nothing is lost, but they fire right away).\n\n## Trigger once manually\n\nTo avoid waiting for cron while debugging an autopilot, trigger it manually:\n\n- UI: click \"Run now\" on the autopilot detail page\n- CLI:\n\n```bash\nmultica autopilot trigger <autopilot-id>\n```\n\nA manual trigger goes through the exact same execution flow as a `schedule` trigger — only the `source` field on the run record is marked `manual`.\n\n## Trigger from a webhook\n\nAutopilots can also fire on inbound HTTP webhooks. Add a **Webhook** trigger\non the autopilot detail page; Multica generates a unique URL of the shape:\n\n```\nhttps://<your-multica-host>/api/webhooks/autopilots/awt_…\n```\n\nPOST any JSON to that URL — Multica records a run with `source = webhook`,\nstores the body as the run's `trigger_payload`, and dispatches the agent\nexactly the way a schedule trigger would.\n\n```bash\ncurl -X POST \"$MULTICA_WEBHOOK_URL\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"event\":\"demo.received\",\"eventPayload\":{\"message\":\"hello\"}}'\n```\n\nIn **create issue mode**, the inbound payload is appended to the new issue's\ndescription so the agent can read it inline. In **run-only mode**, the\npayload is part of the run context the daemon hands the agent.\n\n### Payload shape\n\nYou can send your own envelope:\n\n```json\n{ \"event\": \"github.pull_request.opened\", \"eventPayload\": { } }\n```\n\n…or any JSON object/array. Multica normalizes it into an internal envelope:\n\n```json\n{\n  \"event\": \"<inferred>\",\n  \"eventPayload\": <your body>,\n  \"request\": { \"receivedAt\": \"<rfc3339>\", \"contentType\": \"application/json\" }\n}\n```\n\nWhen you don't provide an `event` field, Multica infers it from common\nheaders and body fields (`X-GitHub-Event` + body `action`,\n`X-Gitlab-Event`, `X-Event-Type`, body `event`/`type`/`action`). When\nnothing matches, the event is `webhook.received`.\n\nWhen configuring GitHub or similar sources, set the content type to\n`application/json` — form-encoded webhook payloads are not accepted.\n\n### Event filters\n\nA new webhook trigger fires on every inbound POST, which is fine for a\nsingle-purpose URL but noisy for sources that fan out many event types\n(GitHub being the obvious one — a single repo webhook can deliver\n`push`, `pull_request`, `workflow_run`, `check_suite`, and more). The\n**Event filters** section on a webhook trigger lets you restrict which\nevents actually dispatch a run; everything else is recorded in delivery\nhistory with `status = ignored` and `reason = event_filtered`, and no\nrun or issue is created.\n\nEach row is one rule: an **event name** plus an optional\ncomma-separated **actions** list. Multica allows a webhook if **any**\nrow matches; leave the section empty to accept everything (the\npre-filter behavior).\n\nExamples:\n\n| Event name     | Actions             | Matches                                                                  |\n| -------------- | ------------------- | ------------------------------------------------------------------------ |\n| `workflow_run` | `completed, failed` | `workflow_run` events with `action: completed` or `action: failed` only  |\n| `workflow_run` | _(empty)_           | every `workflow_run` event, regardless of action                         |\n| `push`         | _(empty)_           | every `push` event                                                       |\n\n#### Where the event name and action come from\n\nMultica derives the `event` name and `action` from the inbound request\nin this order — **the first match wins**.\n\n**1. Body envelope.** If the body is a JSON object with a string\n`event` field, that value is the event name directly. An optional\n`eventPayload` object then supplies action candidates from its\n`action` / `state` / `conclusion` / `status` fields.\n\n```bash\ncurl -X POST \"$MULTICA_WEBHOOK_URL\" \\\n  -H 'Content-Type: application/json' \\\n  -d '{\"event\":\"trigger\",\"eventPayload\":{\"action\":\"true\"}}'\n# inferred: event = trigger, action candidate = true\n```\n\n**2. Headers.** When no body envelope is present, Multica reads the\nfollowing well-known provider headers:\n\n- `X-GitHub-Event: <event>` — combined with the top-level body\n  `action` field (when present) to form `github.<event>.<action>`.\n- `X-Gitlab-Event: <event>` — becomes `gitlab.<event>`.\n- `X-Event-Type: <event>` — passed through verbatim.\n\n```bash\n# GitHub-style: header gives the event name, body gives the action.\ncurl -X POST \"$MULTICA_WEBHOOK_URL\" \\\n  -H 'X-GitHub-Event: workflow_run' \\\n  -H 'Content-Type: application/json' \\\n  -d '{\"action\":\"completed\"}'\n# inferred: event = github.workflow_run.completed\n#        → matches a filter row of workflow_run / completed\n\n# Generic event-type header — no body fields needed.\ncurl -X POST \"$MULTICA_WEBHOOK_URL\" \\\n  -H 'X-Event-Type: trigger.true' \\\n  -H 'Content-Type: application/json' \\\n  -d '{}'\n# inferred: event = trigger.true → matches trigger / true\n```\n\n**3. Body fallback.** If neither a body envelope nor a known header is\npresent, Multica falls back to top-level body string fields in this\norder: `event` → `type` → `action`.\n\n```bash\ncurl -X POST \"$MULTICA_WEBHOOK_URL\" \\\n  -H 'Content-Type: application/json' \\\n  -d '{\"type\":\"trigger\",\"action\":\"true\"}'\n# inferred: event = trigger (from `type`), action candidate = true\n```\n\n**4. Default.** If nothing above matches, the event is\n`webhook.received` and there are no action candidates.\n\n**Action candidates, in full.** Once the event is determined, Multica\nconsiders every value below as a possible action match:\n\n- The event-name suffix, when the event has the form\n  `provider.event.<action>` (e.g. `github.workflow_run.completed` →\n  `completed`).\n- The body fields `action`, `state`, `conclusion`, and `status` —\n  **only when they are JSON strings**. A boolean (`{\"action\": true}`)\n  or a number does not qualify, so a filter expecting\n  `event=trigger, action=true` will never match a body of\n  `{\"trigger\": true}` because `true` is a bool, not a string.\n\n**Common gotcha.** A filter row like `Event name: trigger` /\n`Actions: true` does **not** mean \"fire when the body has\n`trigger: true`\" — Event filters match the *inferred event and\naction*, not arbitrary body fields. Send `trigger.true` via\n`X-Event-Type` (or use the body envelope shown above) to hit it.\nSurrounding whitespace in saved filter rows (`\" workflow_run \"`) is\nstored verbatim and will never match — trim before saving.\n\n#### Quick test\n\nOnce a filter is configured, you can confirm both branches with `curl`:\n\n```bash\n# Allowed — header drives event=workflow_run, body drives action=completed\ncurl -X POST \"$MULTICA_WEBHOOK_URL\" \\\n  -H 'X-GitHub-Event: workflow_run' \\\n  -H 'Content-Type: application/json' \\\n  -d '{\"action\":\"completed\"}'\n# → 200 {\"status\":\"accepted\", ...}\n\n# Filtered — same event, action not in allowlist\ncurl -X POST \"$MULTICA_WEBHOOK_URL\" \\\n  -H 'X-GitHub-Event: workflow_run' \\\n  -H 'Content-Type: application/json' \\\n  -d '{\"action\":\"in_progress\"}'\n# → 200 {\"status\":\"ignored\",\"reason\":\"event_filtered\"}\n```\n\n### URL is a bearer secret\n\nThe generated URL **is** the credential. Anyone with it can fire the\nautopilot. Treat it like a token:\n\n- **Don't paste it into public issue threads, screenshots, or chat history.**\n- **Rotate it if it leaks** — click \"Rotate URL\" on the trigger row, or run\n  `multica autopilot trigger-rotate-url <autopilot-id> <trigger-id>`. The\n  old URL stops working immediately.\n- For sources that require strong source authentication, wait for\n  per-trigger HMAC signature verification; this v1 URL is bearer-only.\n- Workspace members who can view the autopilot can read its webhook URLs\n  for now — tighter per-role secret visibility is a follow-up.\n\n### Status-code semantics\n\nMultica returns `200 OK` with a `status` field for normal no-op outcomes so\nyour provider's webhook-retry machinery doesn't keep hammering the URL:\n\n- `{\"status\":\"accepted\",\"run_id\":\"…\",\"autopilot_id\":\"…\",\"trigger_id\":\"…\"}`\n  — a run was dispatched.\n- `{\"status\":\"skipped\",\"run_id\":\"…\",\"reason\":\"agent runtime is offline at dispatch time\"}`\n  — for `run_only` autopilots, the assignee's runtime is offline and no task\n  was queued. `create_issue` autopilots still create the tracked issue so the\n  work stays visible and can be claimed when the runtime returns.\n- `{\"status\":\"ignored\",\"reason\":\"trigger_disabled\"}` — the trigger is disabled.\n- `{\"status\":\"ignored\",\"reason\":\"autopilot_paused\"}` — the autopilot is paused.\n- `{\"status\":\"ignored\",\"reason\":\"autopilot_archived\"}` — the autopilot is archived.\n\nNon-2xx responses cover real failures:\n\n- `400` — invalid JSON, scalar body, or empty body.\n- `404` — unknown token (`{\"error\":\"webhook not found\"}`).\n- `413` — payload exceeded 256 KiB.\n- `429` — per-token rate limit exceeded (defaults to 60 req/min).\n\n### Self-hosted: configure your public URL\n\nWhen `MULTICA_PUBLIC_URL` is set on the server (e.g. `https://multica.example.com`),\nthe trigger response includes an absolute `webhook_url` and the UI shows a\nready-to-copy URL. Without it, the UI composes the URL from the client's\nAPI origin — which is fine for desktop and same-origin web, but not for\ncustom self-hosted reverse proxies. Multica deliberately does not derive\nthe public host from `Host` / `X-Forwarded-Host` headers so a misconfigured\nreverse proxy cannot trick the server into minting webhook URLs pointing at\nan attacker-controlled host.\n\n## View run history\n\nEvery trigger produces a **run record**, visible on the \"History\" tab of the autopilot detail page:\n\n- Trigger source (`schedule` / `manual` / `webhook`)\n- Start time, completion time\n- Status (`issue_created` / `running` / `completed` / `failed` / `skipped`)\n- The linked issue (create issue mode) or `task` (run-only mode)\n- Failure reason (if failed or skipped)\n\n## What happens when an autopilot fails\n\n> **Autopilot failures are not auto-retried and do not send inbox notifications.** A failure leaves a `failed` entry in run history — no system-level re-enqueue like assign or @-mention, and no notification to anyone. If the autopilot is periodic, **the next cron fire will trigger a new run**, but the failed work is not automatically re-run.\n>\n> If an autopilot is important, design your own monitoring — for example, have the agent post a comment on success, and catch failures by noticing missing comments.\n\nWhy no auto-retry: autopilots are already periodic, so adding system-level retries stacks on top of the next scheduled run and creates duplicate executions. Leaving the schedule entirely to cron keeps it clean.\n\n## What's not yet available\n\n**API-kind triggers are not wired up.** The trigger schema reserves an `api`\nkind, but no ingress route fires it; the UI shows a Deprecated badge for\nexisting rows and offers no copy/rotate affordances. Per-trigger HMAC\nsignature verification, IP allowlists, and provider-specific event presets\nare tracked as follow-ups; v1 URLs are bearer-only.\n\n## Next\n\n- [**Assign issues to agents**](/assigning-issues) — a one-shot hand-off of an issue to an agent\n- [**@-mention agents in comments**](/mentioning-agents) — pull an agent in to take a look from a comment\n- [**Chat**](/chat) — one-to-one conversation outside any issue"
        }
      ]
    },
    {
      "label": "Inbox",
      "labelKey": "inbox",
      "pages": [
        {
          "slug": "inbox",
          "title": "Inbox and subscriptions",
          "content": "The inbox is where Multica **interrupts** you — [issues](/issues) assigned to you, [`@` mentions](/comments), and activity on issues you're subscribed to all land here.\n\nYou control which issue activity reaches you by **subscribing** and **unsubscribing**.\n\n## What shows up in your inbox\n\nThe following events deliver a notification to your inbox:\n\n- **Issue assigned / unassigned / reassigned** — you're notified when you're the new (or former) assignee\n- **Status, priority, or due date change on an issue you're subscribed to**\n- **New comment on an issue you're subscribed to**\n- **You're `@`-mentioned in a comment** — delivered whether or not you're subscribed\n- **Someone reacts to your issue or comment**\n- **An agent [task](/tasks) you assigned fails**\n\n## `@all` notifies the entire workspace\n\n`@all` is a special target — it pushes a notification to **every member** of the workspace.\n\n> **Use `@all` sparingly.** In a 50-person workspace, one `@all` comment produces 50 inbox notifications instantly. Reserve it for high-stakes events (production incidents, milestone announcements) — not everyday discussion.\n\n## Agents never receive notifications\n\nAgents **never** get inbox notifications — not even when they're the assignee, creator, or `@`-mentioned in a comment.\n\nThis isn't a bug: agents don't read an inbox. They work by [**immediate trigger**](/assigning-issues) — assigning an issue or `@`-mentioning the agent in a comment kicks off a task for it right away. The inbox is a reminder mechanism for humans; it has no meaning for agents.\n\n## Subscription rules\n\nYou're **auto-subscribed** to an issue in four situations:\n\n- You **created** it\n- You were **assigned** to it\n- You **commented** on it\n- You were **`@`-mentioned** on it or in one of its comments\n\nAuto-subscription happens once — being both the creator and a mentionee doesn't subscribe you twice.\n\n> **Reassignment doesn't auto-unsubscribe you.** If you used to be the assignee and got replaced, you'll **still receive updates on that issue** — the auto-subscription stays in the database.\n>\n> To stop getting notified, open the issue and unsubscribe manually.\n\nYou can also **manually subscribe** to any issue (even unrelated ones), or **manually unsubscribe** from any auto-subscription. In the UI, use the right panel on the issue page; in the CLI, use `multica issue subscriber add/remove`.\n\n## Sub-issue status changes bubble up to the parent\n\nWhen a sub-issue's **status** changes, subscribers of the parent issue are notified too — even if they haven't subscribed to the sub-issue.\n\nThis applies to **status only**: comment, priority, and due date changes on sub-issues do **not** bubble up.\n\n## Next\n\n- [Comments and mentions](/comments) — how `@` mentions work and the gotchas\n- [Assigning issues to agents](/assigning-issues) — how agents are triggered (and why they don't read the inbox)"
        }
      ]
    },
    {
      "label": "Integrations",
      "labelKey": "integrations",
      "pages": [
        {
          "slug": "channels",
          "title": "Chat integrations (channels)",
          "content": "A **channel** connects a Multica [agent](/agents) to a chat platform so your team can work with it where they already talk. Today there are two channels — [Lark (飞书)](/lark-bot-integration) and [Slack](/slack-bot-integration) — and both run on the **same engine**: a platform-neutral core plus a thin per-platform adapter. Adding a platform is \"implement the adapter,\" not \"rebuild the pipeline.\"\n\nAn **installation** is the unit that ties it together: one bot bound to one `(workspace, agent)`. Inbound messages are routed to an installation, then through a shared pipeline; the agent's reply is sent back to the same chat.\n\n## Architecture\n\n```mermaid\nflowchart LR\n  subgraph P[\"Chat platforms\"]\n    LK[\"Lark / 飞书\"]\n    SL[\"Slack\"]\n  end\n  subgraph ENG[\"Channel engine (platform-neutral)\"]\n    direction TB\n    SUP[\"Supervisor<br/>one live connection per installation\"]\n    ROU[\"Router pipeline:<br/>route → dedup → auth → session → trigger\"]\n  end\n  LK -->|long connection| SUP\n  SL -->|Socket Mode| SUP\n  SUP -->|raw event| ADP[\"Per-platform adapter<br/>translate + ResolverSet\"]\n  ADP --> ROU\n  ROU -->|agent task| RUN[\"Daemon runs the agent\"]\n  RUN -->|reply| OUT[\"Per-platform outbound<br/>(bot token → platform API)\"]\n  OUT --> P\n```\n\n## The inbound pipeline (generic)\n\nEvery inbound message — Lark or Slack — runs through the same ordered steps in the engine `Router`. A platform adapter only supplies the per-platform pieces (the `ResolverSet`); the policy lives in the engine.\n\n1. **Route to installation** — map the event to a `channel_installation` (→ workspace + agent). Lark routes by `app_id`; Slack routes by the app id carried on the event.\n2. **Addressing filter** — in a group/channel, only messages that **@-mention the bot** continue; idle group chatter is dropped (not read).\n3. **Dedup** — a two-phase `(installation, message_id)` claim guarantees exactly-once processing, even across server replicas.\n4. **Identity + authorization** — resolve the sender's platform user id to a Multica user (the [account binding](#authorization)), then re-check workspace membership. Unbound senders get a \"link your account\" prompt; non-members are dropped.\n5. **Session** — find or create a [chat session](/chat) for this conversation and append the message (see [Sessions](#sessions-and-context)).\n6. **Trigger** — enqueue an agent [task](/tasks); a [daemon](/daemon-runtimes) runs the agent and the reply is sent back into the chat.\n\n## Sessions and context\n\nThe agent's context is the **chat-session transcript** — the messages that have been ingested into that session over time. This transcript model is generic (shared by every channel). What differs per platform is the **session-isolation key** the adapter composes:\n\n| Platform | Isolation key | Effect |\n|---|---|---|\n| **Lark / 飞书** | the chat id | One session per chat/group — consecutive turns in the same chat accumulate into one transcript (multi-turn memory). |\n| **Slack** | DM: the channel; channel: `channel + thread root` | Each DM is one session; **each @bot thread is its own session**, so two threads in one channel don't mix. |\n\n> In a group, only messages that **@-mention the bot** are ingested. Neither channel reads the channel's other (un-@'d) messages or scrollback today, so the agent won't see messages it wasn't addressed in. Fetching surrounding history as context is a planned enhancement.\n\n## Authorization\n\nTwo independent gates protect a bot in a shared group — both enforced in the engine for every message, identically for Lark and Slack:\n\n- **Account binding (authentication)** — the sender's platform user id must be linked to a Multica user. The first time someone messages the bot they get a one-time link to bind their identity to **their own** Multica account; until then no agent runs.\n- **Workspace membership (authorization)** — the bound Multica user must be a member of the installation's workspace, re-checked on every message. Non-members are silently dropped.\n\nSo adding a bot to a public channel is safe: only workspace members who have bound their identity can drive the agent, and each sender is checked independently. See the per-platform pages for the user-facing prompts.\n\n## The two channels\n\n> **Lark (飞书) — scan to install.** A workspace admin binds an agent by scanning a QR with the Lark app; no developer console steps. One Bot per agent. See [Lark Bot integration](/lark-bot-integration).\n\n> **Slack — bring your own app.** A workspace admin creates a Slack app, installs it to their Slack workspace, and pastes its bot token + app-level token into Multica. Each agent gets its own Slack app, so several agents can each have a distinct bot in one Slack workspace. See [Slack Bot integration](/slack-bot-integration) for the manifest and step-by-step setup.\n\n## Self-host\n\nEach channel is **off until you set its at-rest encryption key** (the key encrypts each bot's tokens before they touch the database):\n\n```dotenv\nMULTICA_LARK_SECRET_KEY=<base64-encoded 32-byte key>\nMULTICA_SLACK_SECRET_KEY=<base64-encoded 32-byte key>\n```\n\nOn Multica Cloud both are already configured. See [Environment variables](/environment-variables) for the full reference.\n\n## Next\n\n- [Lark Bot integration](/lark-bot-integration) — scan-to-install, DM / @-mention / `/issue`\n- [Slack Bot integration](/slack-bot-integration) — bring-your-own-app setup (manifest + tokens), per-agent bots\n- [Agents](/agents) · [Chat](/chat) · [Tasks](/tasks)"
        },
        {
          "slug": "github-integration",
          "title": "GitHub integration",
          "content": "Connect a GitHub account or organization once in **Settings → GitHub**. After that, any pull request whose branch name, title, or body contains an issue identifier (for example `MUL-123`) is **auto-linked** to that [issue](/issues), appears under **Pull requests** in the issue sidebar, and — when the PR is merged — moves the issue to **Done**.\n\nThere is no per-issue setup. The whole flow is identifier-driven.\n\n## What the integration does\n\n| Surface | Behavior |\n|---|---|\n| **Settings → GitHub** | Workspace admins see the GitHub tab with a master toggle, **Connect GitHub** button, and feature switches (PR sidebar, Co-authored-by, auto-link). After install you bounce back to the GitHub tab. |\n| **Issue sidebar → Pull requests** | Every PR auto-linked to this issue, with title, repo, state (`Open` / `Draft` / `Merged` / `Closed`), and author. Click a row to jump to the PR on GitHub. |\n| **Webhook (background)** | On every `pull_request` event, Multica upserts the PR row, scans the PR for issue identifiers, and (re)builds the link rows. Idempotent — replaying a delivery is a no-op. |\n| **Auto-status on merge** | When a PR transitions to `merged`, every linked issue not already `Done` or `Cancelled` is moved to `Done`. The status change is timeline-logged with source `github_pr_merged`. |\n\nOnly the PR itself is mirrored. Commits, branch refs without an open PR, and CI check states are **not** modeled. The integration is intentionally narrow.\n\n## How identifiers are matched\n\nThe webhook extracts identifiers from three fields, in this order: **PR head branch**, **PR title**, **PR body**. The matcher is:\n\n- Case-insensitive — `mul-123`, `MUL-123`, `Mul-123` all match.\n- Bounded — a `\\b` on the left and a digit anchor on the right keep it from grabbing version numbers like `v1.2-3` or email-style strings.\n- Workspace-scoped — only matches the workspace's own [issue prefix](/workspaces). `FOO-1` in a workspace whose prefix is `MUL` is ignored, even if the integer matches another issue.\n- Deduplicated — listing `MUL-1, MUL-1` in the body links the issue once.\n\nYou can reference **multiple issues** in one PR. `Closes MUL-1, MUL-2` links the PR to both, and merging it advances both to `Done`.\n\n## The auto-merge-to-Done rule\n\nWhen a PR's `merged` field flips to `true`, every linked issue is evaluated:\n\n| Issue current status | Result |\n|---|---|\n| `done` | No change (already terminal). |\n| `cancelled` | **No change** — cancelled means the user explicitly abandoned the work; the integration does not override that signal. |\n| Anything else (`todo`, `in_progress`, `in_review`, `blocked`, `backlog`) | Moved to `done`. |\n\nClosing a PR **without** merging it only updates the PR card's state to `Closed`. The linked issues stay where they were — the user is the one who decides what closing-without-merge means.\n\n> The action is attributed to the `system` actor on the timeline. Subscribers of the issue receive an inbox notification for the status change, the same way they would if a human had moved it.\n\n## What's not auto-linked\n\n- **Identifiers in commit messages** — only branch / title / body are scanned. A commit titled `MUL-123: fix login` does not auto-link unless the same string also appears in the PR title or body.\n- **Identifiers in PR comments** — only the PR's own metadata is scanned; later GitHub comments are ignored.\n- **PRs in repos the App isn't installed on** — without the App, Multica never receives the webhook.\n- **Manually linking a PR to an issue** — there is no UI for this yet. If your team's convention puts identifiers in a place Multica isn't reading, add them to the PR title or body.\n\n## Disconnecting\n\nIn **Settings → GitHub** there is no installation list — you manage existing installations from GitHub directly:\n\n- **From GitHub** — uninstall the Multica GitHub App at `https://github.com/settings/installations` (personal) or `https://github.com/organizations/<org>/settings/installations` (org). Multica receives the `installation.deleted` webhook and drops the row in real time; any open Settings tab updates without a refresh.\n- **Disconnect from inside Multica is admin-only** — the Disconnect control on the GitHub tab is hidden for non-admins. It stays available even when the master GitHub switch is off, so admins can still revoke a stale installation after one-click-disabling the feature.\n\nAfter disconnect, mirrored PR rows stay in the database so historical issue sidebars still show what was linked, but no new webhook events from that installation will be accepted.\n\n## Permissions and visibility\n\n- **Connect / disconnect** require workspace **owner or admin**. Members see the card description but no Connect button.\n- The **Pull requests** sidebar on an issue is visible to anyone who can read the issue — same permissions as the rest of issue detail.\n- The GitHub App requests **read-only** access to pull requests and metadata. Multica never pushes commits, comments, or status checks back to GitHub.\n\n## Self-host setup\n\nIf you're running Multica on Multica Cloud, the integration is already configured — skip this section.\n\nFor self-host, you create one GitHub App, point it at your server, and set two environment variables. The whole flow is below.\n\n### 1. Create a GitHub App\n\nGo to one of:\n\n- Personal account → `https://github.com/settings/apps/new`\n- Organization → `https://github.com/organizations/<org>/settings/apps/new`\n\nFill in:\n\n| Field | Value |\n|---|---|\n| **GitHub App name** | Anything recognizable, e.g. `Multica` or `Multica (staging)`. |\n| **Homepage URL** | Your Multica frontend, e.g. `https://multica.example.com`. |\n| **Callback URL** | Leave blank — Multica doesn't use OAuth user identity. |\n| **Setup URL** | `https://<api-host>/api/github/setup`. **Check \"Redirect on update\"**. |\n| **Webhook → Active** | Enabled. |\n| **Webhook URL** | `https://<api-host>/api/webhooks/github`. |\n| **Webhook secret** | Generate a long random string (e.g. `openssl rand -hex 32`). You'll paste the same value into Multica's env in step 2. |\n| **Permissions → Repository → Pull requests** | **Read-only**. |\n| **Permissions → Repository → Metadata** | Read-only (mandatory). |\n| **Subscribe to events** | Tick **Pull request**. |\n| **Where can this GitHub App be installed?** | Your choice. `Only on this account` is fine for single-org setups. |\n\nAfter **Create GitHub App**, note two things from the App's detail page:\n\n- The **public link** at the top — its tail is the slug. `https://github.com/apps/multica-acme` → slug = `multica-acme`.\n- The **webhook secret** you just generated (you can't read it back from GitHub later — save it now).\n\n> **Webhook secret ≠ Client secret.** The App settings page has both fields stacked together. The **Webhook secret** is what signs `pull_request` payloads — that's the one Multica needs. The **Client secret** is for OAuth and is not used by this integration. Mixing them up produces a confusing `401 invalid signature` on every webhook delivery.\n\n### 2. Set environment variables\n\nOn the API server:\n\n```dotenv\nGITHUB_APP_SLUG=multica-acme\nGITHUB_WEBHOOK_SECRET=<the webhook secret you generated>\n\n# Optional but recommended — populates the connected account name on\n# install instead of waiting for the first webhook to refresh it:\nGITHUB_APP_ID=<numeric App ID from the App's settings page>\nGITHUB_APP_PRIVATE_KEY=<full PEM block, including BEGIN/END lines>\n```\n\n`GITHUB_APP_SLUG` and `GITHUB_WEBHOOK_SECRET` are required. If either is missing:\n\n- `Connect GitHub` in Settings is **disabled** and shows a \"not configured\" hint.\n- The `/api/webhooks/github` endpoint returns **`503 github webhooks not configured`** — Multica refuses to process events with no secret, rather than silently treating every signature as valid.\n\n`GITHUB_APP_ID` and `GITHUB_APP_PRIVATE_KEY` are **optional**. They let the setup callback call GitHub's App-authenticated `/app/installations/{id}` endpoint to fetch the real organization or user name during install. Without them, the connection card briefly shows `Connected to unknown` until GitHub delivers the `installation.created` webhook (typically within a few seconds), at which point Multica refreshes the row and broadcasts a realtime update so any open Settings tab updates without a manual refresh. Generate the private key under **Private keys → Generate a private key** on the App's settings page; paste the full PEM block (including the `-----BEGIN/END RSA PRIVATE KEY-----` lines) into the env var, preserving newlines.\n\n`FRONTEND_ORIGIN` must also be set (it already is for any production self-host); the setup callback bounces the user back to `<FRONTEND_ORIGIN>/settings?tab=github` after install.\n\nRestart the API after setting the env vars.\n\n### 3. Run migrations\n\nThe integration ships its tables in migration `079_github_integration`. If you're upgrading an older deployment:\n\n```bash\nmake migrate-up\n```\n\nThree tables get created: `github_installation`, `github_pull_request`, `issue_pull_request`. They cascade-delete with their workspace, so removing a workspace cleans them up automatically.\n\n### 4. Connect from the UI\n\nIn Multica:\n\n1. Open **Settings → GitHub** as an owner or admin.\n2. Click **Connect GitHub**. GitHub opens in a new tab.\n3. Pick the repositories to grant access to and **Install**.\n4. GitHub redirects back to `<api-host>/api/github/setup`, which records the installation and bounces you to `<FRONTEND_ORIGIN>/settings?tab=github&github_connected=1`.\n\nAfter that, open any PR whose branch / title / body contains an issue identifier — within a few seconds the Pull requests block appears on that issue's detail page.\n\n### 5. Verify with a curl probe\n\nIf GitHub's **Recent Deliveries** page reports `401 invalid signature` after install, the two sides have different secrets. The fastest way to find out which side is wrong is to bypass GitHub:\n\n```bash\nSECRET=\"<the value you put in GITHUB_WEBHOOK_SECRET>\"\nBODY='{\"zen\":\"test\"}'\nSIG=$(printf '%s' \"$BODY\" | openssl dgst -sha256 -hmac \"$SECRET\" -hex | awk '{print $NF}')\ncurl -i -X POST https://<api-host>/api/webhooks/github \\\n  -H \"X-Hub-Signature-256: sha256=$SIG\" \\\n  -H \"X-GitHub-Event: ping\" \\\n  -H \"Content-Type: application/json\" \\\n  -d \"$BODY\"\n```\n\n| HTTP status | Meaning | Fix |\n|---|---|---|\n| `200` `{\"ok\":\"pong\"}` | Server's loaded secret matches your `$SECRET`. The mismatch is on GitHub. | Edit the App → Webhook secret → **paste the same value** → **Save changes** (clicking out of the field without Save keeps the old secret). Redeliver. |\n| `401 invalid signature` | Server's loaded secret is **not** what you think it is. | Confirm the env var landed in the running process (e.g. `kubectl exec` → `echo -n \"$GITHUB_WEBHOOK_SECRET\" | wc -c`). Re-deploy. |\n| `503 github webhooks not configured` | `GITHUB_WEBHOOK_SECRET` is empty in the process. | Set the env var, restart the API. |\n\n## Limitations\n\nA few rough edges to be aware of today:\n\n- **No manual link UI yet** — the only way to link a PR is to have the identifier in its branch, title, or body.\n- **No CI / check state** — only the PR itself is mirrored. Build status, review comments, and reviewers are not surfaced in Multica.\n- **No workspace-level config** for the merge → Done rule — it's a fixed default (`merged → done`, unless `cancelled`). Workspace-customizable mappings are a future addition.\n- **Multi-PR-to-one-issue is conservative on merge** — if two PRs both reference `MUL-123` and the first one merges, the issue is moved to `Done` immediately. A follow-up change to wait for all linked PRs to resolve before advancing is in progress.\n\n## Next\n\n- [Issues](/issues) — the issue identifiers (`MUL-123`) referenced from PRs\n- [Workspaces](/workspaces) — where the workspace-specific issue prefix is set\n- [Environment variables](/environment-variables) — full env reference, including the GitHub variables above"
        },
        {
          "slug": "lark-bot-integration",
          "title": "Lark Bot integration",
          "content": "Bind any [agent](/agents) to a Lark (飞书) Bot and your team can work with it from inside Lark — DM the Bot, @-mention it in a group, or type `/issue` to file a [Multica issue](/issues) without opening the app. The agent's replies stream back into the chat as a live card that updates while it works.\n\nEach Bot is bound **one-to-one** to a single Multica agent. Binding a second agent creates a second Bot; one agent never has two Bots.\n\n## What the integration does\n\n| Surface | Behavior |\n|---|---|\n| **Agent → Integrations** | The agent detail page has an **Integrations** tab (and a matching section in the left sidebar). Owners and admins see **Bind to Lark** there; once bound it flips to a **Connected to Lark** badge with a **Manage in Lark** link. |\n| **DM the Bot** | A workspace member messages the Bot directly in Lark. Each conversation becomes a Multica [chat](/chat) session with the agent; the agent answers in-thread. |\n| **@-mention in a group** | Add the Bot to a Lark group and @-mention it. Only the mentioning message is read — the Bot does not listen to the whole group. |\n| **`/issue` command** | Typing `/issue <title>` (optionally with a body) creates a new Multica issue in the workspace, attributed to you. |\n| **Live reply card** | The Bot posts an interactive card and keeps patching it as the agent runs — progress, the final answer, or an error. |\n\n## Bind an agent (owner / admin)\n\nBinding uses a scan-to-install flow — no app secrets to copy, no developer console steps.\n\n1. Open the agent in **Agents → _your agent_**.\n2. Go to the **Integrations** tab (or use the **Integrations** section in the left sidebar) and click **Bind to Lark**.\n3. A QR code appears. On your phone, open **Lark → Scan**, then authorize the new PersonalAgent Bot.\n4. When the scan completes the dialog closes and the agent shows **Connected to Lark**. Your own Lark identity is bound to your Multica account automatically, so you can start chatting with the Bot right away.\n\n> The QR is single-use and expires after a short window. If it lapses before you authorize, click **Scan again** for a fresh code.\n\nOnce an agent is connected, the **Bind to Lark** button is replaced by a **Manage in Lark** link. Use it to open the Bot's app page in Lark when you need to adjust scopes, rename it, or request additional permissions — re-scanning is intentionally disabled so you don't strand the existing Bot.\n\n## Use the Bot (members)\n\n### First message: bind your Lark identity\n\nThe first time you message the Bot, it replies with a card asking you to **bind your Lark identity**. Tap the link, sign in to Multica, and your Lark account is linked to your Multica membership. This is what lets the agent act as you — for example, `/issue` files the issue under your name.\n\n> Only people who are **members of the workspace** can use the Bot. If you aren't a member, or you skip the identity bind, the Bot won't respond — your message is dropped (and recorded for audit, without its contents).\n\n### Chat and `/issue`\n\n- **Ask the agent anything** — DM the Bot or @-mention it in a group. The conversation is a normal agent chat session; the agent replies in the card.\n- **File an issue** — send `/issue Fix the login redirect` and Multica creates that issue in the workspace, assigned the way any new issue would be. Add more lines after the title for a description.\n- **Watch it work** — the reply card patches itself while the agent runs, so you see progress and the result in place.\n\nIf the agent is **offline** (its runtime isn't connected) or **archived**, the Bot replies with a short status notice instead of silently dropping your message.\n\n## Manage and disconnect\n\nWorkspace-wide management lives in **Settings → Integrations**:\n\n- **Connected bots** lists every Bot in the workspace and the agent each one is bound to. This list is visible to all members.\n- **Disconnect** is **owner / admin only**. Disconnecting stops the Bot from receiving Lark messages and tears down its connection; the installation record is kept for audit, and you can re-bind the same agent later.\n\n## Permissions\n\n- **Bind / disconnect** require workspace **owner** or **admin**. Members see the connected-bots list but no bind or disconnect controls.\n- **Talking to the Bot** requires being a workspace member with a bound Lark identity. Everyone else is dropped.\n- The integration never stores message bodies for dropped messages — only a drop reason, for audit.\n\n## Self-host setup\n\nOn Multica Cloud the integration is already available — skip this section.\n\nFor self-host, Lark is **off until you set an at-rest encryption key**. The key encrypts each Bot's app secret before it touches the database.\n\n1. Generate a 32-byte key and set it on the API server:\n\n   ```dotenv\n   MULTICA_LARK_SECRET_KEY=<base64-encoded 32-byte key>\n   ```\n\n2. Restart the API. Until the key is set, **Settings → Integrations** shows a \"Lark integration not enabled\" notice and the **Bind to Lark** entry points stay hidden.\n\n> **Feishu and Lark international, side by side.** The cloud each Bot belongs to — mainland Feishu (`open.feishu.cn`) or Lark international (`open.larksuite.com`) — is detected automatically when you scan the QR, stored on the installation, and used for every call to that Bot. A single deployment serves both at once, so teams on either tenant can bind without any extra configuration.\n>\n> The `MULTICA_LARK_HTTP_BASE_URL` / `MULTICA_LARK_CALLBACK_BASE_URL` env vars remain only as an optional deployment-wide override (a proxy or a mock); leave them unset for normal operation so each installation keeps reaching its own cloud.\n>\n> **Upgrading from a single-cloud setup?** If you ran an international-Lark deployment by setting those vars to `https://open.larksuite.com`, the server relabels your existing installations to the Lark region on first boot after upgrade — you can then clear the override. Mainland deployments need no action.\n\n## Next\n\n- [Agents](/agents) — each Bot is bound to exactly one agent\n- [Chat](/chat) — what a Bot conversation maps to inside Multica\n- [Issues](/issues) — what `/issue` creates\n- [Environment variables](/environment-variables) — full self-host configuration reference"
        },
        {
          "slug": "slack-bot-integration",
          "title": "Slack Bot integration",
          "content": "Connect any [agent](/agents) to a Slack bot and your team can work with it from inside Slack — DM the bot, @-mention it in a channel, or type `/issue` to file a [Multica issue](/issues) without opening the app.\n\nSlack uses a **bring-your-own-app (BYO)** model: a workspace admin creates a Slack app, installs it to their Slack workspace, and pastes its tokens into Multica. Each agent gets **its own** Slack app — so several agents can each have a distinct, separately @-mentionable bot in the same Slack workspace. (This differs from [Lark](/lark-bot-integration), where binding is a scan-to-install flow.)\n\nThe whole setup is below and takes about five minutes. You'll end up with two tokens to paste into Multica:\n\n- a **Bot token** — starts with `xoxb-`\n- an **App-level token** — starts with `xapp-`\n\n## Set up your Slack app\n\n### 1. Create the app from a manifest\n\n1. Go to [https://api.slack.com/apps](https://api.slack.com/apps) and click **Create New App**.\n2. Choose **From a manifest**.\n3. Pick the Slack workspace to install the app into.\n4. Switch to the **YAML** tab, paste the manifest below, review, and create the app.\n\n```yaml\ndisplay_information:\n  name: Multica\nfeatures:\n  app_home:\n    home_tab_enabled: false\n    messages_tab_enabled: true\n    messages_tab_read_only_enabled: false\n  bot_user:\n    display_name: Multica\n    always_online: true\n  slash_commands:\n    - command: /issue\n      description: Create a Multica issue\n      usage_hint: \"[title]\"\noauth_config:\n  scopes:\n    bot:\n      - app_mentions:read\n      - channels:history\n      - groups:history\n      - im:history\n      - mpim:history\n      - chat:write\n      - reactions:write\n      - users:read\n      - commands\nsettings:\n  event_subscriptions:\n    bot_events:\n      - app_mention\n      - message.im\n      - message.channels\n      - message.groups\n      - message.mpim\n  interactivity:\n    is_enabled: false\n  org_deploy_enabled: false\n  socket_mode_enabled: true\n  token_rotation_enabled: false\n```\n\nThis manifest configures everything Multica needs, so you don't set anything by hand:\n\n| Section | Why it's there |\n|---|---|\n| `app_home.messages_tab_enabled: true` | Lets members open the bot and **DM** it. Without it, the bot can't be messaged directly. |\n| `bot_user` | Creates the bot identity that gets @-mentioned and posts replies. |\n| `chat:write` | Post the agent's replies back into Slack. |\n| `reactions:write` | Add a 👀 reaction to your message while the agent is working, removed when it replies. Without this scope the indicator is silently skipped — everything else still works. |\n| `app_mentions:read` + `app_mention` event | Receive @-mentions in channels. |\n| `im:history` + `message.im` | Receive **DMs** to the bot (every DM message is read). |\n| `channels:history` / `groups:history` / `mpim:history` + the matching `message.*` events | Receive messages in public channels, private channels, and group DMs. In these, the bot only acts on messages that **@-mention** it. |\n| `users:read` | Required so Multica can verify (via `bots.info`) that your two tokens belong to the same app. |\n| `commands` | The bot scope that enables the `/issue` slash command (pairs with `features.slash_commands`). Without it, updating the manifest and reinstalling won't grant the command. |\n| `socket_mode_enabled: true` | The bot connects out over Socket Mode — **no public URL / request URL needed**. |\n| `interactivity.is_enabled: false` | Multica's prompts are plain links, not buttons, so interactivity isn't needed. |\n| `slash_commands` (`/issue`) | Registers the `/issue` slash command so anyone can file a Multica issue from the message box. Delivered over Socket Mode — no request URL. |\n\nThere is **no OAuth redirect URL**, because BYO doesn't use OAuth.\n\n> Want a specific name in Slack? Change `display_information.name` and `features.bot_user.display_name` (e.g. to your agent's name) before creating, or edit it later under **App Home**. Slack shows the bot by its **bot display name**, which can differ from the app name.\n\n### 2. Install the app and copy the Bot token\n\n1. In the app's left nav, open **Install App** (or **OAuth & Permissions**).\n2. Click **Install to Workspace** and approve.\n3. Copy the **Bot User OAuth Token** — it starts with `xoxb-`. This is your **Bot token**.\n\n### 3. Create the App-level token\n\nThe app-level token authorizes the Socket Mode connection. It can only be created in the console (it isn't part of OAuth).\n\n1. Open **Basic Information → App-Level Tokens** and click **Generate Token and Scopes**.\n2. Give it any name.\n3. Click **Add Scope** and pick **`connections:write`** from the list (it's a picker — select it, don't type it).\n4. Click **Generate**, then copy the token — it starts with `xapp-`. This is your **App-level token**.\n\n### 4. Connect it in Multica\n\n1. Open the agent in **Agents → _your agent_** → the **Integrations** tab (or the **Integrations** section in the left sidebar).\n2. Click **Connect Slack**.\n3. Paste the **Bot token** (`xoxb-`) and the **App-level token** (`xapp-`), then click **Connect**.\n4. The agent shows **Connected to Slack**. The bot is now listening over its own Socket Mode connection.\n\n> The two tokens must be from the **same** Slack app, and that app maps to exactly **one** agent. Connecting an app that's already connected to a different agent or workspace is refused. To move an app to another agent, disconnect it first; re-connecting an agent with a **new** app updates that agent's bot in place.\n\n> Setting this up for **multiple agents**? Repeat the whole flow once per agent — each agent gets its own Slack app and its own pair of tokens, and they show up as separate bots in your Slack workspace.\n\n## What the integration does\n\n| Surface | Behavior |\n|---|---|\n| **Agent → Integrations** | Owners and admins see **Connect Slack**; once connected it flips to a **Connected to Slack** badge with a **Disconnect** control. |\n| **DM the bot** | A workspace member messages the bot directly. The conversation becomes a Multica [chat](/chat) session with the agent; every DM message is read. |\n| **@-mention in a channel** | Invite the bot (`/invite @your-bot`) and @-mention it. Only the mentioning message is read — the bot does not listen to the whole channel. Each @bot **thread** is its own session. |\n| **`/issue` slash command** | Type `/issue <description>` (in a channel or a DM) and the agent turns your plain-language description into a well-formed Multica issue, attributed to you. It replies privately to acknowledge — you get a Multica notification when the issue is ready. No @-mention needed. |\n| **Reply** | The agent's answer is posted back into the same DM or thread. |\n\n## Use the bot (members)\n\n### First message: link your account\n\nThe first time you @-mention or DM the bot, it replies with a **link your account** prompt. Tap the link, sign in to Multica, and your Slack identity is bound to your Multica membership — this is what lets the agent act as you (e.g. `/issue` files under your name). The link is single-use and expires in about 15 minutes; just message the bot again for a fresh one.\n\nYou only link **once per Slack workspace**. If the same Multica workspace runs several bots in one Slack workspace (one app per agent), the first bot you link teaches the rest: messaging a second bot reuses that link automatically, no re-prompt. (Linking again is only needed for a bot in a *different* Slack workspace, or a bot connected to a *different* Multica workspace.)\n\n> Only **members of the workspace** can use the bot. If you aren't a member, or you skip the identity link, the bot won't run — your message is dropped (recorded for audit, without its contents).\n\n### Chat and `/issue`\n\n- **In a channel** — the bot isn't auto-joined. Run `/invite @your-bot` once, then `@your-bot <your message>`. Re-mention it for each follow-up (the bot only reads messages that mention it).\n- **In a DM** — open the bot from the Slack sidebar's **Apps** section and message it directly; no mention needed.\n- **File an issue** — use the `/issue` slash command, e.g. `/issue the login redirect is broken on Safari`. Describe it in plain language; the agent writes a proper title and structured description for you and files the issue. It works in a channel or a DM (no @-mention needed) and replies privately to acknowledge — you'll get a Multica notification when the issue is created. First-time users get a one-time link to connect their account.\n\n## Manage and disconnect\n\nWorkspace-wide management lives in **Settings → Integrations**:\n\n- **Connected bots** lists every bot in the workspace and the agent each is bound to (visible to all members).\n- **Disconnect** is **owner / admin only**. It stops the bot from receiving Slack messages and tears down its connection; the installation record is kept for audit, and you can re-connect later.\n\n## Permissions\n\n- **Connect / disconnect** require workspace **owner** or **admin**.\n- **Talking to the bot** requires being a workspace member with a linked Slack identity. Everyone else is dropped.\n- Message bodies for dropped messages are never stored — only a drop reason, for audit.\n\n## Self-host setup\n\nOn Multica Cloud the integration is already available — skip this section.\n\nFor self-host, Slack is **off until you set an at-rest encryption key**. The key encrypts each app's bot + app-level tokens before they touch the database. BYO needs **no** OAuth client id/secret and **no** deployment-level app token — each installation uses the tokens the admin pastes.\n\n1. Generate a 32-byte key and set it on the API server:\n\n   ```dotenv\n   MULTICA_SLACK_SECRET_KEY=<base64-encoded 32-byte key>\n   ```\n\n   For example: `openssl rand -base64 32`.\n\n2. Restart the API. Until the key is set, **Settings → Integrations** shows a \"Slack integration not enabled\" notice and the **Connect Slack** entry points stay hidden.\n\n> The key must decode to exactly 32 bytes — `openssl rand -base64 32` does this. Treat it as a long-lived secret: rotating or losing it makes already-stored tokens undecryptable, forcing every bot to reconnect. The \"link your account\" link is built from your web app URL (`MULTICA_APP_URL`, falling back to `FRONTEND_ORIGIN`) — a normal deployment already sets this, so there's nothing extra to configure.\n\n## Next\n\n- [Chat integrations](/channels) — how the channel engine, sessions, and authorization work\n- [Agents](/agents) · [Chat](/chat) · [Issues](/issues)\n- [Environment variables](/environment-variables) — full self-host configuration reference"
        }
      ]
    },
    {
      "label": "Self-hosting & ops",
      "labelKey": "self_hosting_ops",
      "pages": [
        {
          "slug": "environment-variables",
          "title": "Environment variables",
          "content": "A self-hosted Multica [server](/self-host-quickstart) reads its configuration from environment variables at startup — database, sign-in, email, storage, signup allowlists all live here. This page groups every variable by purpose: each section spells out **what happens if you leave it unset** and **which ones you must set in production**. For how to actually configure the auth-related ones, see [Sign-in and signup configuration](/auth-setup).\n\n## Core server variables\n\nThese are the core variables you must think about before deploying — some have defaults that let the server start, but in production you should set the required ones explicitly.\n\n| Variable | Default | Required in production? |\n|---|---|---|\n| `DATABASE_URL` | `postgres://multica:multica@localhost:5432/multica?sslmode=disable` | **Yes** |\n| `PORT` | `8080` | No (unless you change the port) |\n| `JWT_SECRET` | `multica-dev-secret-change-in-production` | **Yes** (the default is unsafe) |\n| `APP_ENV` | empty | **Yes** (must be `production`) |\n| `FRONTEND_ORIGIN` | empty | **Yes** (self-host must set its own domain) |\n| `MULTICA_DEV_VERIFICATION_CODE` | empty | No (must stay empty in production) |\n\n> **Keep `MULTICA_DEV_VERIFICATION_CODE` empty in production.** A fixed local test code is disabled by default, but if you opt in with `MULTICA_DEV_VERIFICATION_CODE=888888`, anyone who can request a code can sign in with that fixed value while `APP_ENV` is non-production. The shortcut is ignored when `APP_ENV=production`.\n\n### Database connection pool\n\n| Variable | Default | Description |\n|---|---|---|\n| `DATABASE_MAX_CONNS` | `25` | pgxpool max connections. The daemon polls frequently (every 3s) and uses connections; larger deployments may need a higher value |\n| `DATABASE_MIN_CONNS` | `5` | Minimum idle connections |\n\n**When unset**, the values above are used — **not** pgx's built-in 4/NumCPU defaults, which previously caused pool exhaustion in production.\n\n## Email configuration\n\nMultica supports two delivery backends — [Resend](https://resend.com/) for cloud deployments, or an SMTP relay for internal / on-premise networks. `SMTP_HOST` takes priority over `RESEND_API_KEY` when both are set.\n\n### Resend\n\n| Variable | Default | Description |\n|---|---|---|\n| `RESEND_API_KEY` | empty | Resend API key |\n| `RESEND_FROM_EMAIL` | `noreply@multica.ai` | Sender address (must be a domain verified in your Resend account; also reused as the `From:` header when SMTP is in use) |\n\n### SMTP relay\n\n| Variable | Default | Description |\n|---|---|---|\n| `SMTP_HOST` | empty | SMTP relay hostname. Setting this activates SMTP mode and overrides Resend |\n| `SMTP_PORT` | `25` | SMTP port. Use `587` for STARTTLS submission, or `465` for SMTPS (implicit TLS, auto-enabled) |\n| `SMTP_USERNAME` | empty | SMTP username. Leave empty for unauthenticated relay |\n| `SMTP_PASSWORD` | empty | SMTP password |\n| `SMTP_TLS` | `starttls` | TLS mode. `implicit` (aliases `smtps`, `ssl`) forces an immediate TLS handshake on connect (SMTPS); port `465` auto-enables it. Unset / `starttls` upgrades via STARTTLS after connect |\n| `SMTP_TLS_INSECURE` | `false` | Set `true` to skip TLS certificate verification (private CA / self-signed only) |\n| `SMTP_EHLO_NAME` | machine hostname | EHLO/HELO name announced to the relay. Set a real FQDN when a strict relay (e.g. Google Workspace `smtp-relay.gmail.com`) rejects the default greeting from a public IP — otherwise the relay drops the connection and it surfaces as an opaque `EOF` on a later command |\n\nSTARTTLS is upgraded automatically when the server advertises it. The dial timeout is 10s and the whole SMTP session has a 30s deadline, so a black-holed relay can't hang the auth handler.\n\n**Behavior when neither is set**: the server does not error, but every email that should have been sent (verification codes, invite links) **is written to the server's stdout only**. Convenient for local development — copy the code out of the server logs; **in production, forgetting to set this creates a silent black hole**, with users never receiving email and no error surfaced.\n\n## Google OAuth configuration\n\nOptional. Leave unset for email + verification code only; configure it to add \"Sign in with Google\" on the sign-in page.\n\n| Variable | Default | Description |\n|---|---|---|\n| `GOOGLE_CLIENT_ID` | empty | Google Cloud OAuth client ID |\n| `GOOGLE_CLIENT_SECRET` | empty | Google Cloud OAuth secret |\n| `GOOGLE_REDIRECT_URI` | `http://localhost:3000/auth/callback` | OAuth callback URL (self-host: replace with your frontend domain) |\n\n**Takes effect at runtime**: the frontend reads these settings via `/api/config` at runtime, so **changing them requires no frontend rebuild or redeploy** — restart the server and they apply.\n\nFull setup (including Google Cloud Console steps) is in [Sign-in and signup configuration](/auth-setup#google-oauth-configuration).\n\n## File storage configuration\n\nMultica stores user-uploaded attachments (images and files in comments). **S3 is preferred**; if S3 is not configured, it falls back to local disk.\n\n### S3 / S3-compatible storage\n\n| Variable | Default | Description |\n|---|---|---|\n| `S3_BUCKET` | empty | **Bucket name only** (for example `my-bucket`). Do **not** include the `.s3.<region>.amazonaws.com` suffix — the server constructs the public host from `S3_BUCKET` + `S3_REGION`. Setting this enables S3 storage |\n| `S3_REGION` | `us-west-2` | AWS region. Must match the bucket's actual region — it is used both for SDK signing and for building the public URL |\n| `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY` | empty | Static credentials. When both are unset, the AWS SDK default credential chain is used (IAM role / environment credentials) |\n| `AWS_ENDPOINT_URL` | empty | Custom S3-compatible endpoint (for example [MinIO](https://min.io/)). Setting this switches to path-style URLs |\n| `ATTACHMENT_DOWNLOAD_MODE` | `auto` | Attachment download path: `auto`, `cloudfront`, `presign`, or `proxy`. In `auto`, CloudFront is preferred when fully configured; internal/private endpoint hosts use the server proxy; public S3-compatible endpoints use presigned GET URLs when supported |\n| `ATTACHMENT_DOWNLOAD_URL_TTL` | `30m` | TTL for CloudFront signed URLs and S3 presigned download URLs. Accepts Go duration strings |\n\n**When `S3_BUCKET` is unset**: the server logs `\"S3_BUCKET not set, cloud upload disabled\"` at startup, and all uploads fall back to local disk.\n\n**Stored object URLs** are constructed in this order of priority:\n\n1. `https://<CLOUDFRONT_DOMAIN>/<key>` if `CLOUDFRONT_DOMAIN` is set.\n2. `<AWS_ENDPOINT_URL>/<S3_BUCKET>/<key>` (path-style) if `AWS_ENDPOINT_URL` is set.\n3. `https://<S3_BUCKET>.s3.<S3_REGION>.amazonaws.com/<key>` (virtual-hosted-style). When `S3_BUCKET` contains dots, the server falls back to `https://s3.<S3_REGION>.amazonaws.com/<S3_BUCKET>/<key>` (path-style) because the AWS-issued wildcard TLS certificate does not validate dotted bucket hosts.\n\nAPI `download_url` values use `GET /api/attachments/{id}/download` unless CloudFront signing is configured. The endpoint redirects to CloudFront/S3 presigned URLs when safe, or streams through the server for private/internal endpoints such as `http://rustfs:9000`. For Docker/VPC-only object stores, set `ATTACHMENT_DOWNLOAD_MODE=proxy` if auto detection is not conservative enough for your network.\n\n### Local disk (when S3 is not configured)\n\n| Variable | Default | Description |\n|---|---|---|\n| `LOCAL_UPLOAD_DIR` | `./data/uploads` | Local storage directory |\n| `LOCAL_UPLOAD_BASE_URL` | empty (returns relative paths) | Public base URL — leave unset and the frontend can't resolve a full URL for attachments |\n\n### CloudFront (optional)\n\nIf you front S3 with CloudFront, three variables apply: `CLOUDFRONT_DOMAIN`, `CLOUDFRONT_KEY_PAIR_ID`, `CLOUDFRONT_PRIVATE_KEY` (or `CLOUDFRONT_PRIVATE_KEY_SECRET` to read from Secrets Manager). Skip them if you don't use CloudFront — they don't conflict with S3 configuration.\n\n### Cookie domain\n\n| Variable | Default | Description |\n|---|---|---|\n| `COOKIE_DOMAIN` | empty | Scope of the session cookie |\n\n- **Empty**: the cookie is valid only on the exact host visited (correct for single-host deployments)\n- **Set to `.example.com`**: the cookie is shared across subdomains (so `app.example.com` and `api.example.com` share a sign-in session)\n- Warning: it cannot be an IP address (browsers ignore it)\n\n## Restricting who can sign up\n\nThree allowlist layers combine by priority. **If any layer is set to a non-empty value, emails that don't match are rejected** — even `ALLOW_SIGNUP=true` won't override that.\n\n| Variable | Default | Description |\n|---|---|---|\n| `ALLOWED_EMAILS` | empty | Explicit email allowlist (comma-separated). When non-empty, only listed emails can sign up |\n| `ALLOWED_EMAIL_DOMAINS` | empty | Domain allowlist (comma-separated). When non-empty, only listed domains can sign up |\n| `ALLOW_SIGNUP` | `true` | Signup master switch. Set `false` to disable signup entirely |\n\n**The counterintuitive part**: `ALLOWED_EMAIL_DOMAINS=company.io` + `ALLOW_SIGNUP=true` does **not** mean \"allow company.io or everyone\" — it means **only allow company.io**. The allowlist layers are AND semantics — the full decision tree is in [Sign-in and signup configuration → Signup allowlists](/auth-setup#restricting-who-can-sign-up).\n\n**Invite flows themselves do not check the signup allowlist** — but the invitee must still be able to **sign in** before accepting the invite. If they already have a Multica account (for example from another workspace), they can accept directly, unaffected by the allowlist; **if they have never signed up**, the first step of sign-in (requesting a verification code) still passes through the allowlist check, and an email rejected by `ALLOW_SIGNUP=false` or by `ALLOWED_EMAILS` / `ALLOWED_EMAIL_DOMAINS` **cannot finish signup, and therefore cannot accept the invite**.\n\n## Locking down workspace creation\n\n`ALLOW_SIGNUP=false` blocks new accounts, but it does **not** block an already-signed-in user from creating another workspace via `POST /api/workspaces`. On a self-hosted instance where every issue, repo, and agent must be visible to the platform admin, set `DISABLE_WORKSPACE_CREATION=true` to close that gap.\n\n| Variable | Default | Description |\n|---|---|---|\n| `DISABLE_WORKSPACE_CREATION` | `false` | When `true`, every call to `POST /api/workspaces` returns `403 workspace creation is disabled for this instance`. The web UI hides every \"Create workspace\" affordance via `/api/config`. There is no role/owner exception — the gate is global per instance |\n\nRecommended bootstrap sequence:\n\n1. Start the instance with `DISABLE_WORKSPACE_CREATION` unset (the default).\n2. Sign in as the admin and create the shared workspace.\n3. Set `DISABLE_WORKSPACE_CREATION=true` and restart the backend. From this point on, users join via invitation only.\n\nIf you also want to keep `ALLOW_SIGNUP=true` so invited users can finish signup with their first verification code, combine `DISABLE_WORKSPACE_CREATION=true` with `ALLOWED_EMAIL_DOMAINS` / `ALLOWED_EMAILS` to scope which addresses can sign up. Setting `ALLOW_SIGNUP=false` will additionally block pending invitees from creating their account at all — useful only on instances where every member already has a Multica account.\n\n## Rate limiting (optional Redis)\n\nPublic auth endpoints — `/auth/send-code`, `/auth/verify-code`, `/auth/google` — have per-IP fixed-window rate limiting in front of them. The limiter is backed by Redis. When `REDIS_URL` is unset the middleware is a **no-op** (fail-open) and the backend logs `rate limiting disabled: REDIS_URL not configured` at startup.\n\n| Variable | Default | Description |\n|---|---|---|\n| `REDIS_URL` | empty | Redis connection URL (for example `redis://localhost:6379/0`). When unset, rate limiting on auth endpoints is disabled. The same Redis is also used by the realtime hub fan-out, the PAT cache, and the daemon-token cache — they all fall back to in-memory / direct-DB mode when unset |\n| `REDIS_DISABLE_CLIENT_NAME` | `false` | Set to `true` to skip the `CLIENT SETNAME` handshake on every Redis connection. **Required** for managed Redis providers that block the `CLIENT` command, such as GCP Memorystore or AWS ElastiCache with restricted ACLs. When enabled, connections lose their descriptive name in `CLIENT LIST` output but gain compatibility with restricted providers |\n| `RATE_LIMIT_AUTH` | `5` | Max requests per IP per minute against `/auth/send-code` and `/auth/google` |\n| `RATE_LIMIT_AUTH_VERIFY` | `20` | Max requests per IP per minute against `/auth/verify-code` |\n| `RATE_LIMIT_TRUSTED_PROXIES` | empty | Comma-separated CIDRs whose `X-Forwarded-For` header the limiter is allowed to trust. Empty (the default) means **never trust XFF** — the limiter only uses the direct connection's `RemoteAddr` |\n\nWhen a request is over the limit, the server replies with `429 Too Many Requests`, `Retry-After: 60`, and body `{\"error\":\"too many requests\"}`.\n\n> **Behind a reverse proxy you must set `RATE_LIMIT_TRUSTED_PROXIES`.** Otherwise every real user shares the proxy's IP from the backend's point of view, the whole deployment ends up in one bucket, and `/auth/send-code` becomes 5 req/min for the entire site. Typical values: `127.0.0.1/32,::1/128` for a same-host Caddy / Nginx; the CDN's published ranges for Cloudflare / ALB / CloudFront. Only IPs whose `RemoteAddr` falls inside one of these CIDRs may use `X-Forwarded-For` to identify the client.\n\nThis separate `RATE_LIMIT_TRUSTED_PROXIES` is **not** the same as `MULTICA_TRUSTED_PROXIES`, which controls the autopilot-webhook limiter (`/api/webhooks/autopilots/{token}`). Each limiter parses its own list, so a deployment behind a proxy should set both.\n\n## Daemon tuning parameters\n\nThe daemon runs on the user's local machine, and its config is read from local environment variables too. The common ones:\n\n| Variable | Default | Description |\n|---|---|---|\n| `MULTICA_SERVER_URL` | `ws://localhost:8080/ws` | Server address (self-host: replace with your domain) |\n| `MULTICA_DAEMON_HEARTBEAT_INTERVAL` | `15s` | Heartbeat interval |\n| `MULTICA_DAEMON_POLL_INTERVAL` | `3s` | Task polling interval |\n| `MULTICA_DAEMON_MAX_CONCURRENT_TASKS` | `20` | Max concurrent tasks |\n| `MULTICA_<PROVIDER>_PATH` | matches the CLI name | Path to each AI coding tool's executable (for example `MULTICA_CLAUDE_PATH`) |\n| `MULTICA_<PROVIDER>_MODEL` | empty | Default model for each AI coding tool |\n| `MULTICA_<PROVIDER>_ARGS` | empty | Daemon-wide default CLI arguments for a backend, applied to every task before each agent's own `custom_args`. Supported for `MULTICA_CLAUDE_ARGS`, `MULTICA_CODEX_ARGS`, and `MULTICA_CODEBUDDY_ARGS` |\n\nFor a full explanation of how each parameter affects daemon behavior, see [Daemon and runtimes](/daemon-runtimes).\n\n### Default agent arguments (`MULTICA_<PROVIDER>_ARGS`)\n\nThese set a **fleet-wide default** layer of CLI flags for a backend — a convenient way to apply a default cost or resource baseline (for example `--max-turns`) across every agent on a daemon without editing each agent's `custom_args` individually. This is a default layer, not a hard ceiling: per-agent `custom_args` are appended afterward and can override it (see **Precedence** below).\n\n- **Precedence:** the default args are applied first, then each agent's own `custom_args` are appended after. For flags that take a value, the downstream CLI's own argument parser decides the winner (last occurrence wins for most tools), so an individual agent can raise a daemon default but the default still applies wherever the agent doesn't override it.\n- **Parsing:** the value is split with POSIX shell-word rules, so quoting works — `MULTICA_CLAUDE_ARGS='--append-system-prompt \"multi word\"'` parses into two tokens.\n- **Safety:** both the default-args and per-agent `custom_args` layers pass through the same blocked-flags filter, so protocol-critical flags (such as `-p`, `--output-format`, `--input-format`, `--permission-mode`, `--mcp-config` for Claude, and `--listen` for Codex) cannot be injected through either layer.\n- **Unset/empty** means no change to behavior.\n\n## Frontend access control\n\n| Variable | Default | Description |\n|---|---|---|\n| `FRONTEND_ORIGIN` | empty | Frontend address. Invite email links, the CORS allowlist, and the cookie domain are all derived from this. When unset, invite email links fall back to the hosted domain `https://app.multica.ai` — self-host must set this explicitly |\n| `MULTICA_APP_URL` | empty | Frontend URL for CLI login flow. Also used by the web UI to show self-host daemon setup commands with your app domain; for same-origin deployments this is also used as daemon `server_url` when `MULTICA_PUBLIC_URL` is unset |\n| `MULTICA_PUBLIC_URL` | empty | Public API URL, without trailing slash. Used for autopilot webhook URLs and by the web UI as the daemon `server_url` |\n| `CORS_ALLOWED_ORIGINS` | empty | Additional allowed CORS origins (comma-separated) |\n| `ALLOWED_ORIGINS` | empty | WebSocket-specific origin allowlist (comma-separated); when unset, fallback order is `CORS_ALLOWED_ORIGINS` → `FRONTEND_ORIGIN` → `localhost:3000/5173/5174` |\n\n> **Leaving `FRONTEND_ORIGIN` unset creates two silent failures**: (1) invite email links point at `https://app.multica.ai` (the hosted domain), and clicking them doesn't bring users back to your self-hosted instance; (2) WebSocket Origin checks fall back to `localhost:3000 / 5173 / 5174`, so every WebSocket connection in a production deployment is rejected and the frontend appears to \"lose real-time updates.\"\n\n## GitHub integration\n\nThe [GitHub PR ↔ issue integration](/github-integration) needs two variables. Set both to enable Connect GitHub in Settings and accept incoming webhooks. Two additional variables are optional but populate the connected account name on install.\n\n| Variable | Default | Description |\n|---|---|---|\n| `GITHUB_APP_SLUG` | empty | The slug of your GitHub App (the tail of `https://github.com/apps/<slug>`). Drives the Settings → GitHub install button URL |\n| `GITHUB_WEBHOOK_SECRET` | empty | The Webhook secret you set on the GitHub App. Used for HMAC-SHA256 verification of every `pull_request` / `installation` delivery, and as the HMAC key for the setup-callback state token |\n| `GITHUB_APP_ID` | empty | Optional. Numeric App ID from the App's settings page. Combined with `GITHUB_APP_PRIVATE_KEY`, lets the setup callback fetch the connected account name from GitHub immediately on install |\n| `GITHUB_APP_PRIVATE_KEY` | empty | Optional. Full PEM block of the App's RSA private key (including `-----BEGIN/END-----` lines, newlines preserved). Used to mint the short-lived JWT GitHub requires for App-authenticated REST calls |\n\n**Behavior when either of the required variables is unset:**\n\n- `Connect GitHub` in Settings → GitHub is **disabled** and shows a \"not configured\" hint to admins.\n- The `/api/webhooks/github` endpoint returns **`503 github webhooks not configured`** — Multica refuses to process events with no secret rather than treating every signature as valid.\n\n**Behavior when the optional `GITHUB_APP_ID` / `GITHUB_APP_PRIVATE_KEY` are unset:**\n\n- The connection card briefly shows `Connected to unknown` after install. Multica refreshes the row to the real org/user name as soon as GitHub delivers the `installation.created` webhook (typically within a few seconds), and broadcasts a realtime update so any open Settings → GitHub tab reflects the change without a manual refresh.\n\n**Note:** `GITHUB_WEBHOOK_SECRET` is reused as the signing key for the install-flow state token, so operators only need to manage one secret. It is **not** the GitHub App's *Client* secret — Client secrets are OAuth-related and not used by this integration. See [GitHub integration → Self-host setup](/github-integration#self-host-setup) for the full walkthrough.\n\n## Usage analytics\n\nBy default, the server reports to Multica's official PostHog instance. To opt out, set `ANALYTICS_DISABLED=true`.\n\n| Variable | Default | Description |\n|---|---|---|\n| `ANALYTICS_DISABLED` | `false` | Set `true` to disable backend analytics entirely |\n| `POSTHOG_API_KEY` | built-in default key | Set when pointing at your own PostHog instance |\n| `POSTHOG_HOST` | `https://us.i.posthog.com` | Change to your own host if you self-host PostHog |\n\n## Next\n\n- [Sign-in and signup configuration](/auth-setup) — how to actually configure the auth-related variables above and where the traps are\n- [GitHub integration](/github-integration) — how to set up the GitHub App that backs `GITHUB_APP_SLUG` / `GITHUB_WEBHOOK_SECRET`\n- [Troubleshooting](/troubleshooting) — symptoms and fixes for common misconfigurations\n- [Daemon and runtimes](/daemon-runtimes) — what the `MULTICA_DAEMON_*` parameters actually do"
        },
        {
          "slug": "auth-setup",
          "title": "Sign-in and signup configuration",
          "content": "Multica supports two sign-in methods: **email + verification code** (default) and **Google OAuth** (optional). On successful sign-in, the server issues a JWT cookie with a 30-day lifetime. This page covers how to configure each method, how to restrict who can sign up, and the single biggest trap for self-hosted deployments.\n\nFor the list of environment variables referenced below, see [Environment variables](/environment-variables); for token usage and lifecycle details, see [Authentication and tokens](/auth-tokens).\n\n## How email + verification code sign-in works\n\nThe user enters an email on the sign-in page → the server sends a 6-digit code → the user enters it → the server verifies it → a JWT cookie is issued. Standard flow. Two delivery backends are supported — pick whichever fits your deployment:\n\n### Option A: Resend (recommended for cloud / public-internet deployments)\n\n1. Create a [Resend](https://resend.com/) account and verify your domain\n2. Create an API key\n3. Set the environment variables:\n\n    ```bash\n    RESEND_API_KEY=re_xxxxxxxxxxxxxxxx\n    RESEND_FROM_EMAIL=noreply@yourdomain.com  # must be a domain verified in Resend\n    ```\n\n4. Restart the server\n\n### Option B: SMTP relay (for self-hosted / on-premise deployments)\n\nUse this when the deployment can't reach `api.resend.com` or you already have an internal mail relay (Microsoft Exchange, Postfix, on-prem SendGrid, etc.). `SMTP_HOST` takes priority over `RESEND_API_KEY` when both are set — if `SMTP_HOST` is non-empty the server always goes through SMTP, even if `RESEND_API_KEY` is also configured, so verification and invite mail never leaves the internal network.\n\nThe SMTP path supports the three relay modes most on-premise mail servers (notably Microsoft Exchange's receive connectors) expose:\n\n| Mode | Port | Auth | TLS |\n|---|---|---|---|\n| Anonymous internal relay | `25` | none — submission is trusted by IP / subnet | none on the wire (internal segment only) |\n| Authenticated submission | `587` | `SMTP_USERNAME` + `SMTP_PASSWORD` | STARTTLS, upgraded automatically |\n| Implicit TLS (SMTPS) | `465` | optional (`SMTP_USERNAME` + `SMTP_PASSWORD`) | TLS handshake on connect — auto-enabled on port `465`, or force on a non-standard port with `SMTP_TLS=implicit` |\n\n**Anonymous Exchange relay on port 25** — the typical \"internal SMTP relay\" / Exchange anonymous receive connector that accepts mail from a trusted subnet without credentials:\n\n```bash\nSMTP_HOST=exchange.internal.example.com\nSMTP_PORT=25\nSMTP_USERNAME=\nSMTP_PASSWORD=\nSMTP_TLS_INSECURE=false\nRESEND_FROM_EMAIL=noreply@yourdomain.com  # reused as the From: header\n```\n\n**Authenticated submission on port 587** — for relays that require a service account; STARTTLS is upgraded automatically when the server advertises it:\n\n```bash\nSMTP_HOST=smtp.internal.example.com\nSMTP_PORT=587\nSMTP_USERNAME=multica\nSMTP_PASSWORD=...\nSMTP_TLS_INSECURE=false        # set true only for self-signed / private CA\nRESEND_FROM_EMAIL=noreply@yourdomain.com\n```\n\n**Implicit TLS (SMTPS) on port 465** — for providers that only offer SMTPS and don't advertise STARTTLS (e.g. Aliyun / Tencent enterprise mail). Port `465` auto-enables implicit TLS; `SMTP_TLS=implicit` (aliases: `smtps`, `ssl`) forces it on a non-standard SMTPS port:\n\n```bash\nSMTP_HOST=smtp.qiye.aliyun.com\nSMTP_PORT=465                  # implicit TLS auto-enabled on 465\nSMTP_USERNAME=multica@yourdomain.com\nSMTP_PASSWORD=...\nSMTP_TLS=implicit              # optional on 465; required on a non-standard SMTPS port\nRESEND_FROM_EMAIL=noreply@yourdomain.com\n```\n\n**Strict public relays (e.g. Google Workspace `smtp-relay.gmail.com`)** additionally require a valid EHLO name. They reject the default `localhost` greeting from a public IP, and the relay drops the connection — which surfaces as an opaque `EOF` on a later command (`smtp auth: EOF`) rather than at the greeting. Set `SMTP_EHLO_NAME` to the FQDN the relay expects; it defaults to the machine hostname, which inside a container is usually not a valid FQDN:\n\n```bash\nSMTP_HOST=smtp-relay.gmail.com\nSMTP_PORT=587\nSMTP_EHLO_NAME=mail.yourdomain.com   # FQDN the relay accepts; defaults to the (non-FQDN) container hostname\nRESEND_FROM_EMAIL=noreply@yourdomain.com\n```\n\nAt startup the server prints which provider it picked, including the negotiated TLS mode — for example `EmailService: SMTP relay exchange.internal.example.com:25 (starttls) from=noreply@example.com` or `… smtp.qiye.aliyun.com:465 (implicit-tls) from=…` (or `Resend API` / `DEV mode`). The password is never logged. If you don't see the SMTP line after restart, `SMTP_HOST` didn't reach the process — check the container env (`docker compose -f docker-compose.selfhost.yml exec backend env | grep SMTP`).\n\n**What happens if you set neither**: the server doesn't error, but **every email that should have been sent is written to the server's stdout only**. Handy for local development (copy the code from the logs); in production it's a black hole.\n\n## Fixed local testing codes\n\n> **Do not enable a fixed verification code on a publicly reachable instance.**\n>\n> The old behavior where non-production instances accepted `888888` by default has been removed. Unless you explicitly configure it, typing `888888` is treated like any other wrong code.\n>\n> Local development without any email backend configured (no Resend, no SMTP) should use the generated code printed in server logs. If you need deterministic local/private automation, set `MULTICA_DEV_VERIFICATION_CODE` to a 6-digit value such as `888888`, and keep `APP_ENV` non-production:\n>\n> ```bash\n> APP_ENV=development\n> MULTICA_DEV_VERIFICATION_CODE=888888\n> ```\n>\n> This shortcut is ignored when `APP_ENV=production`.\n\nProduction deployments should leave `MULTICA_DEV_VERIFICATION_CODE` empty and set `APP_ENV=production`. If you deploy via `make selfhost` / `docker-compose.selfhost.yml`, `APP_ENV` defaults to `production`.\n\n## Google OAuth configuration\n\nOptional. Without it, only email + verification code is available; with it, the sign-in page gets a \"Sign in with Google\" button.\n\n1. Create an OAuth 2.0 client in the [Google Cloud Console](https://console.cloud.google.com/)\n2. Set the **Authorized redirect URIs** to your Multica frontend address plus `/auth/callback`, for example:\n\n    ```text\n    https://multica.yourdomain.com/auth/callback\n    ```\n\n3. Once you have the client ID and client secret, set three environment variables:\n\n    ```bash\n    GOOGLE_CLIENT_ID=xxxxx.apps.googleusercontent.com\n    GOOGLE_CLIENT_SECRET=GOCSPX-xxxxxxxxxxxxxxx\n    GOOGLE_REDIRECT_URI=https://multica.yourdomain.com/auth/callback\n    ```\n\n4. Restart the server.\n\n**Takes effect at runtime**: the frontend reads these settings at runtime via `/api/config` — after changing them, restart the server and the frontend picks up the new values with no rebuild or redeploy.\n\n> **The redirect URI must match exactly in both the Google Console and `GOOGLE_REDIRECT_URI`** — including protocol (`http` vs `https`), trailing slash, and port. Any mismatch and Google rejects the entire OAuth flow; the error shown to the user is `redirect_uri_mismatch`.\n\n## Restricting who can sign up\n\nThree environment variables combine by priority:\n\n```mermaid\ngraph TD\n    Start[New user first sign-in] --> A{Email in<br/>ALLOWED_EMAILS?}\n    A -- Yes --> Allow[Allow signup]\n    A -- No --> B{Domain in<br/>ALLOWED_EMAIL_DOMAINS?}\n    B -- Yes --> Allow\n    B -- No --> C{Any allowlist<br/>non-empty?}\n    C -- Yes --> Block[Reject]\n    C -- No --> D{ALLOW_SIGNUP<br/>= true?}\n    D -- Yes --> Allow\n    D -- No --> Block\n```\n\n**Existing users can always sign in again** — the signup allowlist only applies to **first-time signup**, not returning users.\n\n- **`ALLOWED_EMAILS`** (highest priority) — explicit email allowlist, comma-separated. **When non-empty, only listed emails can sign up.**\n- **`ALLOWED_EMAIL_DOMAINS`** — domain allowlist, comma-separated (for example `company.io,partner.com`).\n- **`ALLOW_SIGNUP`** — master switch, default `true`. Set `false` to disable signup entirely.\n\n> **The three layers are AND semantics, not OR.** A common wrong intuition is that `ALLOWED_EMAIL_DOMAINS=company.io` + `ALLOW_SIGNUP=true` means \"allow company.io plus everyone else.\" It does **not**. If any layer has a non-empty value, **emails not matching it are rejected outright** — `ALLOW_SIGNUP=true` does not override that.\n>\n> To actually \"allow everyone,\" leave all three variables empty (or keep `ALLOW_SIGNUP=true`).\n\n**Typical configurations**:\n\n| Goal | Configuration |\n|---|---|\n| Internal only, employees of `company.io` | `ALLOWED_EMAIL_DOMAINS=company.io` |\n| Internal + a few external collaborators | `ALLOWED_EMAIL_DOMAINS=company.io` + collaborator addresses added to `ALLOWED_EMAILS` |\n| Disable self-serve signup entirely, invite-only | `ALLOW_SIGNUP=false` |\n| Open signup (not recommended for production) | All three empty |\n\n## Can you still invite people when signup is disabled?\n\n**Only people who already have a Multica account.** Accepting an invite doesn't check the signup allowlist — if the invitee has signed up already (for example in another workspace), clicking the invite link and signing in lets them accept.\n\n**But people who have never signed up cannot be rescued by an invite.** Before accepting, they must sign in, and the first step of sign-in (requesting the verification code) passes through the signup allowlist check. If `ALLOW_SIGNUP=false`, or their email isn't in `ALLOWED_EMAILS` / `ALLOWED_EMAIL_DOMAINS`, they **cannot complete signup**, and therefore cannot accept the invite.\n\nTo invite an external collaborator who hasn't signed up yet: temporarily add their email to `ALLOWED_EMAILS`, wait for them to sign up and accept the invite, then remove the entry.\n\nFor how to create and use invites, see [Members and roles](/members-roles).\n\n## Next\n\n- [Environment variables](/environment-variables) — full definitions of every variable used on this page\n- [Authentication and tokens](/auth-tokens) — JWT / PAT / daemon token categories and usage\n- [Troubleshooting](/troubleshooting) — verification code not received, OAuth `redirect_uri_mismatch`, signup rejected"
        },
        {
          "slug": "troubleshooting",
          "title": "Troubleshooting",
          "content": "Look up issues by symptom. Each entry gives you **symptom / likely causes / how to diagnose / how to fix**. If your situation isn't listed, open an issue on GitHub.\n\n## Daemon can't connect to the server\n\n**Symptom**: [`multica daemon`](/cli)'s `status` command shows `offline` or `connection refused`; the server logs show no `/api/daemon/register` or `/api/daemon/heartbeat` requests. For how the daemon mechanism works, see [Daemon and runtimes](/daemon-runtimes).\n\n**Likely causes**:\n\n1. **`MULTICA_SERVER_URL` points at the wrong address** — default is `ws://localhost:8080/ws`; self-host must change it to your server address\n2. **Network / firewall blocking** — the daemon and server aren't on the same network, or outbound traffic is blocked\n3. **Token expired or invalid** — you never ran `multica login`, or the PAT was revoked\n4. **Server rejected registration** — the account you signed in with isn't in the target workspace (register returns 403)\n5. **DNS resolution failure** — the hostname doesn't resolve on the daemon machine\n\n**How to diagnose**:\n\n```bash\nmultica daemon logs --lines 100    # look for daemon-side errors\necho $MULTICA_SERVER_URL          # confirm the address is set\ncurl -i http://<server-host>:8080/health   # hit the server directly\ncurl -i http://<server-host>:8080/readyz  # include DB + migration readiness\ncat ~/.multica/config.json        # verify api_token exists\nmultica workspace list            # confirm you're a member of the target workspace\n```\n\n**How to fix**: address each cause above. The two most common fixes are **changing `MULTICA_SERVER_URL` and restarting the daemon** (`multica daemon restart`) and **signing in again** (`multica logout && multica login`).\n\n## Tasks stuck in `queued`\n\n**Symptom**: after assigning an issue to an agent, the issue status flips to `in_progress` immediately, but a long time passes with no sign of agent execution on the page; `multica daemon status` shows the daemon `online`.\n\n**Likely causes** (ordered by frequency):\n\n1. **Agent concurrency limit reached** — this agent's `max_concurrent_tasks` (default 6) is fully occupied by other running tasks\n2. **Another task from the same agent is still running on the same issue** — same agent × same issue is forced to run sequentially (prevents duplicate execution)\n3. **Agent has been archived** — after archival, new tasks still enqueue but can't be claimed, and they time out after 5 minutes (code-issue G-01)\n4. **Daemon hasn't registered this runtime in the current workspace** — restart the daemon or reselect the runtime in the UI\n5. **Daemon disconnected** — no heartbeat in the last 45 seconds. `daemon status` reporting `online` may reflect a very recent disconnect\n\n**How to diagnose**:\n\n```bash\nmultica daemon status --output json       # runtime list + last_seen_at\nmultica agent list                         # check agent archived state\nmultica issue show <issue-id>             # inspect task history\n```\n\nOn the server side (self-host), grep for `\"no_tasks\"` / `\"no_capacity\"` to see the claim outcome.\n\n**How to fix**:\n\n- Concurrency full → wait for running tasks to finish, or `multica agent update <id> --max-concurrent-tasks 10` to raise the ceiling\n- Same-issue serialization → wait for the previous task to finish, or reassign to a different agent\n- Agent archived → `multica agent restore <id>`\n- Runtime not registered → `multica daemon restart`, and the daemon will re-register\n\n## WebSocket can't connect\n\n**Symptom**: the browser console logs `WebSocket is closed`; the page doesn't show real-time updates (task progress, comments, inbox), and a refresh is needed to see them; backend tasks still execute.\n\n**Likely causes**:\n\n1. **Origin check failure** — your frontend domain isn't in the server's CORS allowlist. The default allowlist only includes `localhost:3000/5173/5174`; self-hosting on the public internet requires `FRONTEND_ORIGIN`\n2. **Protocol mismatch** — frontend on `https://` needs `wss://`; HTTP uses `ws://`\n3. **Reverse proxy doesn't enable WebSocket upgrade** — Nginx / Envoy / HAProxy don't forward the `Upgrade` header by default\n4. **JWT cookie expired or missing** — no re-sign-in after the 30-day expiry\n\n**How to diagnose**:\n\n- Browser DevTools → Network → filter by \"WS\" and check connection state and status code\n- Grep server logs for `\"rejected origin\"` / `\"websocket\"` — an origin issue spells itself out\n- `curl -i http://<server-host>:8080/ws` should return `101 Switching Protocols` (with the `Upgrade` header)\n\n**How to fix**:\n\n- Wrong origin → set `FRONTEND_ORIGIN=https://multica.yourdomain.com` in the server's `.env` (or comma-separated `CORS_ALLOWED_ORIGINS`) and restart the server\n- Protocol mismatch → make sure `FRONTEND_ORIGIN`'s protocol matches the frontend's\n- Reverse proxy → in Nginx, add `proxy_http_version 1.1; proxy_set_header Upgrade $http_upgrade; proxy_set_header Connection \"upgrade\";`\n- Cookie expired → refresh the page and sign in again\n\n## Emails not received\n\n**Symptom**: after submitting an email during sign-in or invite acceptance, neither the inbox nor the spam folder has the verification code.\n\n**First, confirm which provider the server thinks is active.** At startup the backend prints one of:\n\n- `EmailService: SMTP relay <host>:<port> from=<addr>` — using SMTP (`SMTP_HOST` non-empty wins over Resend)\n- `EmailService: Resend API from=<addr>` — using Resend\n- `EmailService: DEV mode — codes printed to stdout …` — no provider configured\n\n```bash\ndocker compose -f docker-compose.selfhost.yml logs backend | grep \"EmailService:\"\n```\n\nIf the line you expected isn't there, the environment didn't reach the process — check `.env` and `docker compose -f docker-compose.selfhost.yml exec backend env | grep -E 'RESEND_|SMTP_'`. Credentials are never logged on this startup line.\n\n### When Resend is the active provider\n\n**Likely causes**:\n\n1. **`RESEND_API_KEY` not set** — the server silently falls back and **writes the code to its own stdout** without error. Easy to trip over in production\n2. **Resend API key invalid / out of quota** — server logs show `\"failed to send verification code\"`\n3. **`RESEND_FROM_EMAIL`'s domain not verified in Resend** — Resend refuses to send\n4. **Email was sent but flagged as spam by the recipient's ISP** — check the Resend dashboard and the spam folder\n\n**How to diagnose**:\n\n- Grep server logs for `\"[DEV] Verification code for\"` — if present, Resend isn't configured and the code was written to stdout\n- [Resend dashboard](https://resend.com/) → Emails for send history\n- Confirm `RESEND_FROM_EMAIL`'s domain appears in the Resend console's \"Verified Domains\" list\n\n**How to fix**:\n\n- Missing API key → follow [Sign-in and signup configuration → How email works](/auth-setup#how-email--verification-code-sign-in-works) to configure and restart the server\n- Domain not verified → run the DNS verification flow in the Resend console (add SPF / DKIM records)\n- In an emergency (internal testing) → copy the code printed under `[DEV]` from the server logs\n\n### When SMTP is the active provider\n\nThe SMTP path wraps every failure with the stage it failed at, so the server logs already tell you where the relay rejected the session. Grep for `\"failed to send verification email\"` / `\"failed to send invitation email\"` and check the wrapped error:\n\n| Logged error | What it means | How to fix |\n|---|---|---|\n| `smtp dial <host>:<port>: dial tcp …: connect: connection refused` / `i/o timeout` | The backend container can't reach the relay — wrong host, wrong port, firewall, or the relay isn't listening | Verify `SMTP_HOST` / `SMTP_PORT` resolve from inside the container (`docker compose -f docker-compose.selfhost.yml exec backend nslookup <host>` and `nc -vz <host> <port>`); open the firewall from the host running Multica to the relay |\n| `smtp starttls: x509: certificate signed by unknown authority` (or `certificate is not valid for any names`) | The relay uses a private CA / self-signed cert and the container's trust store rejects it | Either install the CA into the container, or set `SMTP_TLS_INSECURE=true` only after confirming the relay is reachable on a trusted segment |\n| `smtp auth: 535 5.7.8 Authentication credentials invalid` (or `534`/`530`) | `SMTP_USERNAME` / `SMTP_PASSWORD` are wrong, or the relay requires a different auth mechanism than `PLAIN` | Re-confirm the service-account credentials with your mail admin; for Exchange anonymous internal relay leave both empty (`SMTP_USERNAME=`, `SMTP_PASSWORD=`) |\n| `smtp MAIL FROM: 550 5.7.1 Client does not have permissions to send as this sender` | The relay won't accept `RESEND_FROM_EMAIL` as the envelope sender — typical Exchange \"anonymous users not allowed\" or DMARC alignment issue | Set `RESEND_FROM_EMAIL` to a domain the relay accepts; on Exchange, grant the source IP `ms-Exch-SMTP-Accept-Any-Sender` on the receive connector |\n| `smtp RCPT TO <addr>: 550 5.7.1 Unable to relay` | The relay's receive connector doesn't allow your subnet to relay to external recipients (most common for anonymous internal relays talking to outside domains) | Either restrict invites to internal recipients, or add the Multica host's subnet to the Exchange \"Anonymous Users → Relay\" permission list |\n| `smtp DATA` / `smtp write body` / `smtp end data` | Session was accepted but the relay dropped the body — usually message-size limits, content filtering, or a connection reset mid-stream | Check the relay's logs for the same `Message-ID` (logged as `<unixnano>@<host>`); raise the message size limit if needed |\n\n`MAIL FROM`, `RCPT TO`, and `DATA` errors are always logged with the relay's response code so you can match them against Exchange / Postfix logs on the other side. Verification codes and invite tokens are **never** included in the wrapped error.\n\n**How to diagnose**:\n\n- Grep `\"EmailService: SMTP relay\"` once at startup, then `\"failed to send\"` for runtime failures\n- From inside the backend container, sanity-check connectivity: `docker compose -f docker-compose.selfhost.yml exec backend sh -c 'nc -vz $SMTP_HOST $SMTP_PORT'`\n- Confirm the env reached the process: `docker compose -f docker-compose.selfhost.yml exec backend env | grep SMTP_` (password will be in the output — only run on a trusted shell)\n\n**How to fix**:\n\n- Wrong host / port → adjust `SMTP_HOST` / `SMTP_PORT` and restart the backend; for the supported relay modes see [Auth setup → Option B: SMTP relay](/auth-setup)\n- Cert mismatch → install the relay's CA into the container, or temporarily `SMTP_TLS_INSECURE=true` on a trusted segment\n- Auth failure → re-check credentials; for anonymous internal relay leave `SMTP_USERNAME` and `SMTP_PASSWORD` empty\n- `Unable to relay` → either restrict to internal recipients or grant the Multica host's IP relay permission on the Exchange receive connector\n\n## Fixed local test code doesn't work\n\n**Symptom**: on a self-hosted instance, you try to sign in with a fixed local test code such as `888888` and it's rejected with `invalid or expired code`.\n\n**Likely causes** (mutually exclusive):\n\n1. **`MULTICA_DEV_VERIFICATION_CODE` is empty** — fixed codes are disabled by default\n2. **`APP_ENV=production`** — this is the **correct** production configuration; fixed local test codes are ignored in production\n3. **The configured code is not 6 digits** — the shortcut only accepts a 6-digit value\n\n**How to diagnose**:\n\n```bash\ncat .env | grep -E 'APP_ENV|MULTICA_DEV_VERIFICATION_CODE'\ndocker exec <container> env | grep -E 'APP_ENV|MULTICA_DEV_VERIFICATION_CODE'\n```\n\nCheck your inbox (including spam) for the real verification code.\n\n**How to fix**:\n\n- In production, leave `MULTICA_DEV_VERIFICATION_CODE` empty — configure Resend and use real codes\n- For local development or internal testing, either copy the generated code from server logs or set `APP_ENV=development` plus `MULTICA_DEV_VERIFICATION_CODE=888888` — never enable a fixed code on a public instance (see [Sign-in and signup configuration → Fixed local testing codes](/auth-setup#fixed-local-testing-codes))\n\n## Usage dashboard stays at zero\n\n**Symptom**: agents complete tasks, raw token usage is written to the database, but **Settings → Usage** and **Settings → Runtime** show 0 input / output / cost across the board. This is silent — there is no error in the backend logs.\n\n**Likely causes**:\n\n1. **`rollup_task_usage_hourly()` is never being claimed** — the Usage / Runtime dashboards read from the derived `task_usage_hourly` table, populated by that function. Since MUL-2957 the backend runs the rollup in-process via the DB-backed scheduler (`sys_cron_executions`); a stale build, a missing migration `113`, or a sustained backend outage with no replicas left running can leave the table without a recent SUCCESS row.\n2. **`pg_cron` is configured for compatibility but pointing at the wrong database** — `pg_cron.database_name` defaults to `postgres`; if your Multica database has a different name, the scheduled job never sees `rollup_task_usage_hourly()`. The in-process scheduler does not depend on this, but if you removed the in-process scheduler and rely on `pg_cron`, the DB name must match.\n3. **The handler is being claimed but silently erroring** — e.g. the SQL function is missing because migrations were partially applied, or DB role / search_path is misconfigured. Check the FAILED audit rows in `sys_cron_executions`.\n\n**How to diagnose**:\n\n```sql\n-- Confirm raw events exist but the hourly table is empty.\nSELECT count(*) AS raw_rows FROM task_usage;\nSELECT count(*) AS hourly_rows FROM task_usage_hourly;\n\n-- Inspect the in-process scheduler's audit log.\nSELECT plan_time, status, attempt, runner_id,\n       error_code, error_msg, started_at, finished_at\n  FROM sys_cron_executions\n WHERE job_name = 'rollup_task_usage_hourly'\n ORDER BY plan_time DESC\n LIMIT 20;\n\n-- Watermark — if this is 1970-01-01, the rollup has never run.\nSELECT watermark_at FROM task_usage_hourly_rollup_state;\n\n-- Compatibility path: if you previously registered pg_cron, confirm\n-- it is (or isn't) available and pointing at the right database.\nSELECT * FROM pg_available_extensions WHERE name = 'pg_cron';\nSHOW shared_preload_libraries;\nSELECT jobname, schedule, database, active FROM cron.job;\n```\n\n**How to fix**:\n\n- Confirm the scheduler is actually running on at least one backend replica — every 30 seconds it should add a SUCCESS row to `sys_cron_executions` for `rollup_task_usage_hourly`.\n- Call the rollup once by hand to verify the SQL path: `SELECT rollup_task_usage_hourly();` — refresh the dashboard; if numbers appear, the SQL function is fine and the issue is on the scheduler claim path.\n- If migration `113_sys_cron_executions` has not applied yet, restart the backend so migrations run, or invoke `migrate up` manually.\n- If you have legacy `pg_cron` history that pre-dates the in-process scheduler, the SQL function still holds advisory lock 4246 internally and the two paths cannot double-write — see [Self-host quickstart → Usage rollup](/self-host-quickstart#7-usage-rollup-no-operator-action-required) for the optional `cron.unschedule` cleanup.\n\n## Migration `103` fails with `refusing to drop legacy daily rollups`\n\n**Symptom**: upgrading from `v0.3.4` to `v0.3.5+`, the backend container fails to start (or `migrate up` aborts) with:\n\n```text\nERROR: refusing to drop legacy daily rollups:\n  task_usage_hourly_rollup_state.watermark_at (1970-01-01 ...) trails\n  task_usage latest event (...) by more than 01:00:00 — backfill is\n  incomplete or pg_cron is not running. Run cmd/backfill_task_usage_hourly\n  (and let pg_cron catch up) before re-running migrate\n```\n\n**Likely cause**: this is migration `103`'s fail-closed guard. It refuses to drop the legacy daily rollups until `task_usage_hourly` has caught up with raw `task_usage`. The guard fires whenever existing rows are present and the rollup watermark still sits at the epoch — i.e. nothing has rolled history into the hourly table yet.\n\nSince MUL-2957 the migrate command runs an idempotent monthly-slice backfill (under advisory lock 4246) automatically immediately before applying migration `103`, so v0.3.4 → v0.3.5+ direct upgrades complete in a single `migrate up` invocation. If you are still seeing this error you are either on a pre-MUL-2957 binary or the hook itself failed — check the migrate logs for an earlier `task_usage hourly rollup hook` line.\n\n**How to fix**:\n\n1. If you are on a pre-MUL-2957 binary and cannot upgrade the binary first, run the standalone backfill against the same database (idempotent, safe to interrupt, safe to re-run):\n\n   ```bash\n   # Docker Compose\n   docker compose -f docker-compose.selfhost.yml exec backend \\\n     ./backfill_task_usage_hourly --sleep-between-slices=2s\n\n   # Kubernetes\n   kubectl -n multica exec deploy/multica-backend -- \\\n     ./backfill_task_usage_hourly --sleep-between-slices=2s\n   ```\n\n2. Re-run the upgrade — restarting the backend container is enough, migrations run on startup. The guard now sees a current watermark and lets `103` apply.\n3. The in-process scheduler then keeps the watermark advancing — see [Self-host quickstart → Usage rollup](/self-host-quickstart#7-usage-rollup-no-operator-action-required).\n\n`--sleep-between-slices=2s` is a polite default on production databases with years of history. Use `--months-back N --force-partial` if you only want to keep the last N months and are willing to permanently abandon older buckets.\n\n## Port conflicts\n\n**Symptom**: `multica server` or `multica daemon start` fails with `address already in use`.\n\n**Likely causes**:\n\n1. **Server port taken** (default `8080`)\n2. **Daemon health port taken** (default `19514`, offset by a hash per profile)\n3. **Web dev server port conflict** (`3000` / `5173`)\n4. **Insufficient privileges for the port** (binding a privileged port `< 1024` requires sudo)\n\n**How to diagnose**:\n\n```bash\nlsof -i :8080        # macOS / Linux\nnetstat -ano | findstr :8080    # Windows\n```\n\n**How to fix**:\n\n- Kill the conflicting process (`kill -9 <PID>`), or change ports via `PORT=9000`\n- To use 80 / 443 → don't bind directly; put a reverse proxy (Nginx / Caddy) in front, forwarding to a high port\n\n## Where to find logs\n\n| Component | Location | Command |\n|---|---|---|\n| **Daemon** | `~/.multica/daemon.log` (background mode) or foreground stdout | `multica daemon logs -f --lines 100` |\n| **Server (Docker)** | Container stdout | `docker logs -f <container>` |\n| **Server (systemd)** | journal | `journalctl -u multica-server -f` |\n| **Frontend (dev)** | Terminal running `pnpm dev` | Read directly |\n| **Frontend (browser)** | DevTools → Console | Press `F12` |\n\nFor more detailed daemon logs, move it from background to foreground: `multica daemon stop && multica daemon start --foreground`."
        }
      ]
    },
    {
      "label": "Reference",
      "labelKey": "reference",
      "pages": [
        {
          "slug": "cli",
          "title": "CLI command reference",
          "content": "The Multica CLI mirrors almost everything the Web UI can do (create [issues](/issues), assign [agents](/agents), start the [daemon](/daemon-runtimes), and more). This page lists every top-level command with a one-line description. For the full set of flags and examples, run `multica <command> --help`.\n\n## Getting authenticated\n\nRun this the first time you use the CLI to obtain a **personal access token (PAT)**:\n\n```bash\nmultica login\n```\n\nYour browser opens automatically. After you approve in the web app, the CLI saves the PAT (prefixed with `mul_`) to `~/.multica/config.json`. Every subsequent command authenticates with that PAT.\n\n> For CI or headless environments, skip the browser flow: create a PAT in the web app under **Settings → Personal Access Tokens**, then run `multica login --token <mul_...>` to supply it directly.\n\nFor the difference between token types, see [Authentication and tokens](/auth-tokens).\n\n## Auth and setup\n\n| Command | Purpose |\n|---|---|\n| `multica login` | Log in and save a PAT |\n| `multica auth status` | Show current login status, user, and workspace |\n| `multica auth logout` | Clear the local PAT |\n| `multica setup cloud` | One-shot setup for Multica Cloud (login + install daemon) |\n| `multica setup self-host` | One-shot setup for a self-hosted backend |\n\n## Workspaces and members\n\n| Command | Purpose |\n|---|---|\n| `multica workspace list` | List every workspace you can access |\n| `multica workspace get <slug>` | Show details for one workspace |\n| `multica workspace member list` | List members of the current workspace |\n| `multica workspace update <id> --name \"...\" [--description \"...\"] [--context \"...\"] [--issue-prefix \"...\"]` | Update workspace metadata (admin/owner). Long fields accept `--description-stdin` / `--context-stdin`. |\n\n## Issues and projects\n\n> `list` commands (`multica issue list`, `autopilot list`, `project list`, etc.) print short, copy-paste-ready IDs by default — issue keys like `MUL-123` for issues, short UUID prefixes for the rest. The `<id>` argument on the follow-up commands below accepts either the short ID or the full UUID, so the typical flow is `multica issue list` → copy the key → `multica issue get MUL-123`. Pass `--full-id` to a list command when you need the canonical UUID.\n\n| Command | Purpose |\n|---|---|\n| `multica issue list` | List issues (prints copy-paste-ready issue keys) |\n| `multica issue get <id>` | Show a single issue (accepts an issue key or a UUID) |\n| `multica issue create --title \"...\"` | Create a new issue |\n| `multica issue update <id> ...` | Update an issue (status, priority, assignee, etc.) |\n| `multica issue assign <id> --to <name>` | Assign to a member, agent, or squad (assigning to an agent triggers a run) |\n| `multica issue status <id> <status>` | Shortcut to change status |\n| `multica issue search <query>` | Keyword search |\n| `multica issue children <id>` | List sub-issues grouped by stage |\n| `multica issue pull-requests <id>` | List linked pull requests and their status |\n| `multica issue runs <id>` | Show agent runs on an issue |\n| `multica issue run-messages <task-id>` | Show messages for one execution |\n| `multica issue usage <id>` | Show aggregated token usage for an issue |\n| `multica issue rerun <id>` | Re-enqueue a fresh task for the issue's current agent assignee |\n| `multica issue cancel-task <task-id>` | Cancel a queued or running task |\n| `multica issue comment <id> ...` | Nested: view / post comments |\n| `multica issue comment resolve/unresolve <comment-id>` | Mark a comment thread resolved or unresolved |\n| `multica issue subscriber <id> ...` | Nested: subscribe / unsubscribe |\n| `multica issue metadata <id> ...` | Nested: read / write issue metadata |\n| `multica issue label <id> ...` | Nested: manage labels on an issue |\n| `multica project list/get/create/update/delete/status` | Project CRUD |\n| `multica label list/create/update/delete` | Workspace label CRUD |\n\n## Agents and skills\n\n| Command | Purpose |\n|---|---|\n| `multica agent list` | List the workspace's agents |\n| `multica agent get <slug>` | Show an agent's configuration |\n| `multica agent create ...` | Create an agent |\n| `multica agent update <slug> ...` | Update an agent |\n| `multica agent archive <slug>` | Archive |\n| `multica agent restore <slug>` | Restore an archived agent |\n| `multica agent tasks <slug>` | Show an agent's task history |\n| `multica agent avatar <slug>` | Upload an agent avatar |\n| `multica agent env <slug> ...` | Read or replace an agent's custom environment variables |\n| `multica agent skills ...` | Nested: attach / detach skills |\n| `multica skill list/get/create/update/delete` | Skill CRUD |\n| `multica skill import ...` | Import a skill from GitHub, ClawHub, or the local machine |\n| `multica skill files ...` | Nested: manage a skill's files |\n\n### Skill import conflicts\n\n`multica skill import --url <url>` defaults to `--on-conflict fail`. If a skill\nwith the same name already exists, the command exits with a structured\n`conflict` result and does not change the workspace.\n\nUse `--on-conflict overwrite` when you created the existing skill and want to\nreplace its content while preserving its ID and agent bindings. Use\n`--on-conflict rename` to import a copy with an automatic suffix such as `-2`.\nUse `--on-conflict skip` to leave the existing skill untouched and report\n`skipped`.\n\n```bash\nmultica skill import --url https://skills.sh/acme/repo/review-helper\nmultica skill import --url https://skills.sh/acme/repo/review-helper --on-conflict overwrite\nmultica skill import --url https://skills.sh/acme/repo/review-helper --on-conflict rename\nmultica skill import --url https://skills.sh/acme/repo/review-helper --on-conflict skip\n```\n\n## Squads\n\n| Command | Purpose |\n|---|---|\n| `multica squad list` | List squads in the workspace |\n| `multica squad get <id>` | Show a single squad |\n| `multica squad create --name \"...\" --leader <agent>` | Create a squad (owner / admin) |\n| `multica squad update <id> ...` | Update name, description, instructions, leader, or avatar |\n| `multica squad delete <id>` | Archive (soft-delete) — transfers assigned issues to the leader |\n| `multica squad member list/add/remove/set-role <squad-id>` | Manage squad members and update roles in place |\n| `multica squad activity <issue-id> <action\\|no_action\\|failed> --reason \"...\"` | Used by squad leader agents to record an evaluation per turn |\n\nSee [Squads](/squads) for the full model.\n\n## Autopilots\n\n| Command | Purpose |\n|---|---|\n| `multica autopilot list` | List every autopilot in the workspace |\n| `multica autopilot get <id>` | Show a single autopilot |\n| `multica autopilot create ...` | Create an autopilot |\n| `multica autopilot update <id> ...` | Update |\n| `multica autopilot delete <id>` | Delete |\n| `multica autopilot runs <id>` | Show run history |\n| `multica autopilot trigger <id>` | Trigger a run manually |\n| `multica autopilot trigger-add/update/delete <id>` | Manage schedule or webhook triggers |\n| `multica autopilot trigger-rotate-url <id> <trigger-id>` | Rotate a webhook trigger URL |\n\n`multica autopilot create/update` also accepts `--subscriber` to set default\nsubscribers for issues created by a `create_issue` autopilot; update accepts\n`--clear-subscribers` to remove them.\n\n## Daemon and runtimes\n\n| Command | Purpose |\n|---|---|\n| `multica daemon start` | Start the daemon (background by default; add `--foreground` to run in the foreground) |\n| `multica daemon stop` | Stop the daemon |\n| `multica daemon restart` | Restart the daemon |\n| `multica daemon status` | Check whether the daemon is online and its concurrency |\n| `multica daemon logs` | View daemon logs |\n| `multica runtime list` | List runtimes in the current workspace |\n| `multica runtime usage` | Show resource usage |\n| `multica runtime activity` | Recent activity log |\n| `multica runtime update <id> ...` | Initiate a CLI update on a runtime |\n| `multica runtime delete <id> [--cascade]` | Delete a runtime, optionally archiving bound agents |\n| `multica runtime profile ...` | Manage custom runtime profiles and local path overrides |\n\n## Miscellaneous\n\n| Command | Purpose |\n|---|---|\n| `multica repo checkout <url>` | Clone a repo locally for agents to use |\n| `multica config` | View or edit local CLI configuration |\n| `multica version` | Print the CLI version |\n| `multica update` | Upgrade the CLI to the latest release |\n| `multica attachment download <id>` | Download an attachment from an issue or comment |\n\n## Getting full flags\n\nEvery command supports `--help`:\n\n```bash\nmultica issue create --help\nmultica agent update --help\n```\n\nv2 will ship a dedicated detailed reference page for each command.\n\n## Next steps\n\n- [Authentication and tokens](/auth-tokens) — PAT vs. JWT vs. daemon token\n- [Daemon and runtimes](/daemon-runtimes) — how the `daemon` commands work under the hood\n- [Creating and configuring agents](/agents-create) — all options for `multica agent create`"
        },
        {
          "slug": "auth-tokens",
          "title": "Authentication and tokens",
          "content": "Multica has three kinds of tokens, one for each context: the browser Web UI, the command line and scripts, and the daemon. All three represent the same you, but their scopes and lifetimes differ.\n\n## The three tokens\n\n| Token | Format | Where it's used | Lifetime |\n|---|---|---|---|\n| **JWT cookie** | `multica_auth` cookie (HttpOnly) | Web browser | 30 days |\n| **Personal access token (PAT)** | Prefixed with `mul_` | CLI, scripts, direct API calls | No expiry by default; when you create one via the API you can pass `expires_in_days` |\n| **Daemon token** | Prefixed with `mdt_` | Daemon-to-server communication | Managed by the daemon itself |\n\nIn day-to-day use you'll only touch the first two directly. The **[daemon](/daemon-runtimes) token** is created and refreshed automatically by `multica daemon login` — you don't have to think about it.\n\n## What each token can hit\n\n| API route | JWT cookie | PAT | Daemon token |\n|---|---|---|---|\n| `/api/user/*` (user-level actions) | ✓ | ✓ | ✗ |\n| `/api/workspaces/:id/*` (workspace-level) | ✓ | ✓ | ✗ |\n| `/api/daemon/*` (daemon-only) | ✗ | ✓ | ✓ |\n| WebSocket `/ws` (real-time push) | ✓ (cookie) | ✓ (authenticates via first message) | ✗ |\n\n**A PAT can hit almost anything** — it represents \"the full you.\" A daemon token can only do what the daemon needs: fetch tasks and report results.\n\n**Both can hit `/api/daemon/*`, but their scopes differ.** A PAT represents an **entire user** — once authenticated, it can see every workspace you belong to. A daemon token is pinned to a single workspace at creation time and can only touch resources in that workspace. In production, run your daemon with a daemon token — don't take the shortcut of using a PAT, or you'll be granting far more privilege than the daemon needs.\n\n## Logging in\n\n### Email + verification code\n\n1. Enter your email; the server sends a 6-digit code.\n2. Enter the code; the server issues a JWT cookie (browser) or exchanges it for a PAT (CLI).\n\n> **Self-hosting operators, take note**: keep `MULTICA_DEV_VERIFICATION_CODE` empty on public deployments. If you enable a fixed local test code, anyone who can request a code can sign in with that value while `APP_ENV` is non-production. See [Self-host auth configuration](/auth-setup).\n\n### Google OAuth\n\nClick **Sign in with Google** and go through the standard OAuth callback. Self-hosting requires `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, and the redirect URI to be configured — see [Self-host auth configuration](/auth-setup).\n\n## Creating, viewing, and revoking a PAT\n\n**Creating** a PAT can be done two ways:\n\n- **Web UI**: Settings → Personal Access Tokens → New token\n- **CLI**: `multica login` creates one automatically if there's no local PAT yet\n\n> **The full PAT is displayed exactly once when it's created.** After you refresh or close the dialog, you won't be able to see it again.\n>\n> Multica stores only the hash of the PAT in the database — not even the server can retrieve the original. Copy and save it immediately. If you lose it, your only option is to revoke it and create a new one.\n\n**Viewing** existing PATs (name, creation time, last-used time — **not** the full token) lives under Settings → Personal Access Tokens.\n\n**Revoking** a PAT: click Revoke in the list. Revocation takes effect immediately — the next request made with that PAT will be rejected with a 401.\n\n## Logging out only deletes the local token\n\nWhen you run `multica auth logout` or click log out in the Web UI:\n\n- **The local token is cleared** — the CLI removes the PAT from `~/.multica/config.json`; the browser deletes the cookie.\n- **The PAT is still valid on the server** — if someone obtained your PAT before you logged out (for example, by copying it to another machine), they **can still use it**.\n\n> **If you suspect your PAT has leaked, don't just log out.** Go to Settings → Personal Access Tokens and **revoke** the token. Only revocation invalidates a leaked token immediately.\n\n## Next steps\n\n- [CLI command reference](/cli) — authentication is automatic for every CLI command\n- [Self-host auth configuration](/auth-setup) — how to configure email, OAuth, and signup allowlists when self-hosting\n- [Daemon and runtimes](/daemon-runtimes) — where the daemon token comes from"
        },
        {
          "slug": "mobile-app",
          "title": "Mobile app (iOS)",
          "content": "Multica's iOS client is open-source and lives in the main repo alongside web, desktop, and backend. It isn't on the App Store yet — until that changes, anyone who wants it on their iPhone builds from source. The build takes about 10–20 minutes the first time and ~2 minutes after that, and it talks to the same backend as [multica.ai](https://multica.ai) so your existing account just works.\n\n> This page is for **personal use**. App developers should read `apps/mobile/README.md` in the repo — it covers the dev / staging variants and the full script matrix.\n\n## What you need\n\n- A **Mac** with Xcode installed (free from the App Store).\n- A free **Apple ID** added under Xcode → Settings → Accounts. A paid Apple Developer Program account is optional and only extends the 7-day signing window to 1 year — see [7-day limit](#7-day-signing-limit) below.\n- An **iPhone** connected via USB cable, with [Developer Mode enabled](https://docs.expo.dev/guides/ios-developer-mode/) (Settings → Privacy & Security → Developer Mode).\n- The Multica source code checked out:\n\n  ```bash\n  git clone https://github.com/multica-ai/multica.git\n  cd multica\n  pnpm install\n  ```\n\nIf anything in that list is missing, walk through Expo's [Set up your environment](https://docs.expo.dev/get-started/set-up-your-environment/) (pick **Development build → iOS Device**) — it's the canonical setup guide for everything except the repo checkout.\n\n## Build it\n\nOne command:\n\n```bash\npnpm ios:mobile:device:prod:release\n```\n\nXcode signs the build with the \"Personal Team\" your Apple ID automatically owns — this team is created silently the first time you sign into Xcode with any Apple ID, so it's there even if you don't remember setting anything up. This is a **Release build**: no Metro dependency, splash → app, exactly like an App Store install.\n\nThe first build downloads CocoaPods + compiles React Native from source — expect 10–20 minutes. Subsequent builds reuse Xcode's cache.\n\nThat's it for the typical path. If signing fails, jump to [Troubleshooting](#troubleshooting).\n\n## 7-day signing limit\n\nA free Apple ID signs builds for **7 days**. After that, the app refuses to launch on your iPhone and shows an \"untrusted developer\" error. Plug back into your Mac and re-run the same command to re-sign — your data stays put because it lives on the backend, not in the app.\n\nThe only way to extend this is an **Apple Developer Program account** ($99/yr from [developer.apple.com](https://developer.apple.com)). Signing is then valid for 1 year between renewals, and you can also distribute to other devices via TestFlight.\n\n## Updating\n\nThere is no auto-update yet. When the Multica codebase moves forward, pull and rebuild:\n\n```bash\ngit pull\npnpm install\npnpm ios:mobile:device:prod:release\n```\n\nSubsequent builds are fast because Xcode caches the native compile.\n\n## Why no App Store yet\n\nThe iOS app is still moving fast — the team prefers ship-and-iterate over App Store review cycles right now. A TestFlight beta is the most likely next step before a full App Store release. Until then, the self-build path above is the only way to use Multica on iOS.\n\nIf you'd like to be notified when TestFlight opens, watch the GitHub repo.\n\n## Troubleshooting\n\n**\"No matching provisioning profiles found\"** — Xcode refuses to sign the default bundle id `ai.multica.mobile` with your Apple ID. Rare, but happens if someone has registered that prefix on Apple's developer portal. Pick any reverse-domain you control (`com.yourname.multica` is fine), export it, and re-run:\n\n```bash\nexport EXPO_BUNDLE_IDENTIFIER_PROD=com.yourname.multica\npnpm ios:mobile:device:prod:release\n```\n\nThe id doesn't have to mean anything — Apple just needs it to be unclaimed by other teams.\n\n**\"Could not launch &lt;app&gt;\" / \"Untrusted Developer\"** — either you've hit the 7-day limit (re-run the build) or you need to manually trust the developer profile on your iPhone: Settings → General → VPN & Device Management → tap your Apple ID → Trust.\n\n**Build hangs on `Pod install` or compiles forever** — first build is genuinely 10–20 minutes because CocoaPods downloads dependencies and Xcode compiles React Native from source. Subsequent builds are much faster.\n\n**App can't reach the backend** — confirm `apps/mobile/.env.production` hasn't been modified (it ships with `EXPO_PUBLIC_API_URL=https://api.multica.ai`). If you changed it, restore with `git checkout apps/mobile/.env.production`."
        }
      ]
    }
  ],
  "vi": [
    {
      "label": null,
      "labelKey": null,
      "pages": [
        {
          "slug": "index",
          "title": "Chào mừng",
          "content": "Multica là nền tảng cộng tác công việc nơi con người và các AI [agent](/agents) làm việc cùng nhau trong một [không gian làm việc](/workspaces). Bạn có thể [giao issue cho agent](/assigning-issues) giống như giao việc cho đồng đội — agent sẽ thực thi công việc, báo cáo tiến độ và trả lời trong phần bình luận. Bạn cũng có thể [mở cửa sổ chat và trò chuyện trực tiếp](/chat), nhờ agent soạn issue, trả lời câu hỏi hoặc xử lý một yêu cầu lẻ.\n\nTrang này giải thích agent chạy ở đâu và các cách để bắt đầu dùng Multica.\n\n## Agent chạy ở đâu\n\nAgent **không** thực thi task trên máy chủ của Multica. Hiện Multica hỗ trợ một mô hình runtime:\n\n- **[Daemon](/daemon-runtimes) cục bộ** — bạn chạy `multica daemon` trên máy của mình, daemon sẽ điều khiển các [công cụ lập trình AI](/providers) đã cài sẵn trên máy. Hiện có 14 công cụ được hỗ trợ: [Antigravity](/providers#antigravity), [Claude Code](/providers#claude-code), [CodeBuddy](/providers#codebuddy), [Codex](/providers#codex), [Cursor](/providers#cursor), [Copilot](/providers#copilot), [Hermes](/providers#hermes), [Kimi](/providers#kimi), [Kiro CLI](/providers#kiro-cli), [OpenCode](/providers#opencode), [OpenClaw](/providers#openclaw), [Pi](/providers#pi), [Qoder](/providers#qoder), [Trae CLI](/providers#trae). API key, bộ công cụ và thư mục mã nguồn của bạn luôn nằm trên máy của bạn.\n\n> **Cloud runtime sắp ra mắt**, hiện đang trong giai đoạn danh sách chờ. Khi khả dụng, bạn sẽ không cần daemon cục bộ — task của agent sẽ chạy trực tiếp trên Multica Cloud.\n\n## Hai cách dùng Multica\n\nCả hai lựa chọn dưới đây đều là **lựa chọn backend** — nơi máy chủ Multica chạy.\n\n  **01 — [Multica Cloud](/cloud-quickstart) · Danh sách chờ**\n\nBackend được quản lý sẵn. Cài CLI, chạy daemon trên máy của bạn và kết nối tới máy chủ do Multica vận hành. Mất khoảng 5 phút.\n  **02 — [Tự vận hành](/self-host-quickstart) · Docker · Helm**\n\nChạy toàn bộ backend trên máy chủ của bạn với Docker Compose. Cơ sở dữ liệu, máy chủ và lưu trữ đều nằm trên hạ tầng của bạn.\n\n## Bước tiếp theo\n\n  \n    [Cách Multica hoạt động](/how-multica-works) — đọc trong 30 giây, làm rõ dứt điểm ý \"máy chủ không chạy agent, agent chạy trên máy của bạn\".\n  \n  \n    Chọn một trong hai cách ở trên — [Multica Cloud](/cloud-quickstart) là nhanh nhất, chạy được trong khoảng 5 phút.\n  \n  \n    Tạo một [issue](/issues) và chọn agent làm người phụ trách thay vì một đồng đội. Chờ agent hoàn thành."
        },
        {
          "slug": "how-multica-works",
          "title": "Cách Multica hoạt động",
          "content": "Multica là một nền tảng **phân tán**. Giao diện web bạn nhìn thấy chỉ là mặt tiền — công việc thật sự do ba thành phần đảm nhận: **Multica server** nắm dữ liệu ([không gian làm việc](/workspaces), [issue](/issues), [thành viên](/members-roles), hàng đợi [task](/tasks), v.v.); **[daemon](/daemon-runtimes)** chạy trên máy của bạn, nhận task và điều khiển công cụ lập trình AI; còn **[công cụ lập trình AI](/providers)** (Claude Code, Codex và các CLI cục bộ khác) là thành phần thực sự viết mã. Đây là khác biệt lớn nhất giữa Multica với Linear hay Jira — **[agent](/agents) không chạy trên máy chủ của chúng tôi, chúng chạy trên máy của bạn**.\n\n## Ba thành phần cốt lõi\n\n- **Multica server** — không gian làm việc, danh sách issue và các luồng bình luận bạn thấy đều nằm trong cơ sở dữ liệu của nó. Nó cũng là hub WebSocket đẩy cập nhật thời gian thực giữa bạn và đồng đội. Nó **không** thực thi bất kỳ task nào của agent.\n- **Daemon** — một phần của Multica CLI, chạy trên máy của bạn. Khi khởi động, nó phát hiện các công cụ lập trình AI đã cài trên máy, đăng ký với server, rồi bắt đầu poll task mỗi 3 giây và gửi heartbeat mỗi 15 giây.\n- **Công cụ lập trình AI** — một trong 14 công cụ (hoặc nhiều công cụ chạy song song): [Antigravity](/providers#antigravity), [Claude Code](/providers#claude-code), [CodeBuddy](/providers#codebuddy), [Codex](/providers#codex), [Cursor](/providers#cursor), [Copilot](/providers#copilot), [Hermes](/providers#hermes), [Kimi](/providers#kimi), [Kiro CLI](/providers#kiro-cli), [OpenCode](/providers#opencode), [OpenClaw](/providers#openclaw), [Pi](/providers#pi), [Qoder](/providers#qoder), [Trae CLI](/providers#trae). Sau khi daemon nhận task, nó dùng các công cụ này để thực sự làm việc.\n\nVì bộ công cụ nằm hoàn toàn trên máy bạn, **API key, thư mục mã nguồn và các công cụ đã cấp quyền** chỉ được dùng trên máy của bạn — Multica server không bao giờ nhìn thấy chúng. Điều này đúng cho cả tự vận hành lẫn dùng Cloud.\n\n## Vòng đời của một task\n\nLấy kịch bản phổ biến nhất — bạn giao một issue cho agent:\n\n1. Bạn bấm giao việc trên giao diện web. Trình duyệt gửi một request HTTP tới Multica server.\n2. Server đặt người phụ trách issue đó là agent, đồng thời tạo một task thực thi trong hàng đợi với trạng thái `queued`.\n3. Daemon trên máy bạn nhận task ở lần poll kế tiếp (trong vòng 3 giây). Trạng thái task chuyển thành `dispatched`.\n4. Daemon tạo một thư mục làm việc cách ly trên máy và gọi công cụ lập trình AI tương ứng. Trạng thái task chuyển thành `running`.\n5. AI viết mã trên máy, chạy test và gửi bình luận ngược về server.\n6. Kết thúc thực thi. Daemon báo kết quả (thành công / thất bại) về server, trạng thái task chuyển thành `completed` hoặc `failed`. Bạn thấy tiến độ cập nhật thời gian thực trên giao diện web (qua WebSocket).\n\nChi tiết cơ chế xem tại [Daemon và runtime](/daemon-runtimes) và [Task](/tasks).\n\n## Bốn cách để agent bắt đầu làm việc\n\nKhông chỉ có \"giao issue\" — Multica có 4 cách kích hoạt, mỗi cách ứng với một kiểu cộng tác:\n\n| Cách | Tình huống điển hình | Tài liệu |\n|---|---|---|\n| **Giao issue** | Phổ biến nhất. Giao issue cho agent và nó tự bắt đầu | [Giao issue](/assigning-issues) |\n| **@nhắc agent trong bình luận** | \"Xem giúp mình cái này\" — không đổi người phụ trách hay trạng thái, chỉ gửi một bình luận | [Nhắc agent](/mentioning-agents) |\n| **Chat trực tiếp** | Cuộc trò chuyện độc lập, không gắn với issue — hỏi đáp, nhờ soạn issue | [Chat](/chat) |\n| **Tự động hóa (theo lịch)** | Chỉ dẫn cố định — kiểu \"sáng thứ Hai hằng tuần làm bản tóm tắt standup\" | [Tự động hóa](/autopilots) |\n\n## Runtime: chạy ở đâu, bao nhiêu công cụ\n\nMột **runtime** là cặp \"daemon × một công cụ lập trình AI\". Nếu daemon trên một máy có cả Claude Code lẫn Codex và tham gia hai không gian làm việc, Multica sẽ đăng ký 4 runtime độc lập (2 không gian làm việc × 2 công cụ).\n\nHiện tại chỉ hỗ trợ mô hình **daemon cục bộ**. Cloud runtime (không cần máy của bạn phải bật) **sắp ra mắt**, hiện đang trong giai đoạn danh sách chờ.\n\n## Bước tiếp theo\n\n- [Bắt đầu nhanh với Cloud](/cloud-quickstart) — kết nối Multica Cloud trong 5 phút\n- [Bắt đầu nhanh tự vận hành](/self-host-quickstart) — chạy backend của riêng bạn\n- [Daemon và runtime](/daemon-runtimes) — đi sâu vào thành phần nền tảng của kiến trúc"
        },
        {
          "slug": "cloud-quickstart",
          "title": "Bắt đầu nhanh với Cloud",
          "content": "Trang này dẫn bạn đi trọn vẹn Multica Cloud — **đăng ký → cài [CLI](/cli) → khởi động [daemon](/daemon-runtimes) → tạo [agent](/agents) → giao [task](/tasks) đầu tiên**. Mất khoảng 5 phút.\n\nMột điều kiện tiên quyết: bạn đã cài sẵn ít nhất một [công cụ lập trình AI](/providers) trên máy ([Antigravity](/providers#antigravity), [Claude Code](/providers#claude-code), [CodeBuddy](/providers#codebuddy), [Codex](/providers#codex), [Cursor](/providers#cursor), [Copilot](/providers#copilot), [Hermes](/providers#hermes), [Kimi](/providers#kimi), [Kiro CLI](/providers#kiro-cli), [OpenCode](/providers#opencode), [OpenClaw](/providers#openclaw), [Pi](/providers#pi), [Qoder](/providers#qoder) hoặc [Trae CLI](/providers#trae)). Daemon tự phát hiện chúng khi khởi động và từ chối chạy nếu không tìm thấy công cụ nào.\n\n## 1. Tạo tài khoản\n\nĐăng ký tại [multica.ai](https://multica.ai). Bạn có thể đăng nhập bằng email (mã xác thực 6 chữ số) hoặc Google.\n\nSau khi đăng ký, bạn được đưa vào một không gian làm việc mặc định (tạo từ tên tài khoản). Bạn có thể đổi tên sau, hoặc tạo không gian làm việc mới.\n\n## 2. Cài Multica CLI\n\n**macOS / Linux (khuyên dùng Homebrew)**:\n\n```bash\nbrew install multica-ai/tap/multica\n```\n\n**macOS / Linux (không dùng Homebrew)**:\n\n```bash\ncurl -fsSL https://raw.githubusercontent.com/multica-ai/multica/main/scripts/install.sh | bash\n```\n\n**Windows (PowerShell)**:\n\n```powershell\nirm https://raw.githubusercontent.com/multica-ai/multica/main/scripts/install.ps1 | iex\n```\n\nKiểm tra lại bản cài:\n\n```bash\nmultica version\n```\n\n## 3. Đăng nhập + khởi động daemon\n\nMột lệnh duy nhất lo cả đăng nhập lẫn khởi động daemon:\n\n```bash\nmultica setup\n```\n\n`multica setup` sẽ:\n\n1. Cấu hình CLI kết nối tới Multica Cloud\n2. Mở trình duyệt để đăng nhập (cùng mã xác thực email / Google OAuth như trên web)\n3. Lưu PAT được tạo vào `~/.multica/config.json`\n4. **Tự động khởi động daemon** — daemon bắt đầu poll task mỗi 3 giây và gửi heartbeat mỗi 15 giây\n\nKiểm tra daemon đang chạy:\n\n```bash\nmultica daemon status\n```\n\n`online` nghĩa là nó đã đăng ký với server.\n\n## 4. Kiểm tra runtime đã online\n\nTrên giao diện web, vào **Cài đặt → Runtime**. Daemon bạn vừa khởi động sẽ hiện thành một hoặc nhiều runtime hoạt động — mỗi runtime ứng với một công cụ lập trình AI cài trên máy.\n\nNếu nó hiện offline, đừng hoảng — xem [Khắc phục sự cố → Daemon không kết nối được server](/troubleshooting#daemon-cant-connect-to-the-server).\n\n## 5. Tạo agent\n\nTrên giao diện web, vào **Cài đặt → Agent** và bấm **New Agent**:\n\n- **Tên** — tên hiển thị của agent trên bảng và trong bình luận. Chọn tên bạn thích\n- **Provider** — chọn một công cụ lập trình AI bạn đã cài trên máy (danh sách chỉ hiện các công cụ mà runtime phát hiện được)\n- **Model** (tùy chọn) — lựa chọn model bên trong công cụ đó (danh sách tĩnh hoặc khám phá động, tùy provider)\n- **Chỉ dẫn** (tùy chọn) — system prompt cho agent này\n\nSau khi tạo, agent xuất hiện trong danh sách thành viên của không gian làm việc và có thể được giao việc như một thành viên bình thường.\n\n## 6. Giao task đầu tiên\n\nTạo issue trên giao diện web, hoặc từ CLI:\n\n```bash\nmultica issue create --title \"Add an ASCII architecture diagram to the README\"\n```\n\nGiao issue cho agent bạn vừa tạo — bấm vào avatar của nó trên web, hoặc dùng CLI:\n\n```bash\nmultica issue assign MUL-1 --to my-agent-name\n```\n\n`--to` nhận **tên** của agent hoặc thành viên. Khớp theo chuỗi con cũng được — nếu agent tên `my-code-reviewer`, gõ `reviewer` là đủ. Nếu không gian làm việc có tên trùng nhau, dùng `--to-id <uuid>` thay thế (loại trừ lẫn nhau với `--to`); tra UUID bằng `multica agent list --output json` hoặc `multica workspace member list --output json`.\n\n**Điều gì diễn ra tiếp theo phía daemon**:\n\n1. Nó nhận task trong vòng 3 giây (trạng thái chuyển từ `queued` sang `dispatched`)\n2. Nó gọi công cụ lập trình AI tương ứng để bắt đầu làm việc (trạng thái thành `running`)\n3. AI làm việc trên máy bạn — có thể đọc thư mục mã nguồn, chạy lệnh, sửa file\n4. Khi xong, nó báo kết quả về Multica (trạng thái thành `completed` hoặc `failed`, tùy việc tự động thử lại có kích hoạt hay không)\n\nGiao diện web cập nhật **thời gian thực** (qua WebSocket) — không cần tải lại trang.\n\n## Bước tiếp theo\n\n- [Daemon và runtime](/daemon-runtimes) — daemon vận hành ra sao và runtime nghĩa là gì\n- [Task](/tasks) — vòng đời task và quy tắc thử lại\n- [So sánh các công cụ lập trình AI](/providers) — khác biệt năng lực giữa 14 công cụ\n- [Bắt đầu nhanh tự vận hành](/self-host-quickstart) — chạy backend của riêng bạn"
        },
        {
          "slug": "self-host-quickstart",
          "title": "Bắt đầu nhanh tự vận hành",
          "content": "Trang này hướng dẫn bạn chạy **server** Multica (backend + frontend + PostgreSQL) trên máy hoặc máy chủ của mình với Docker. Khi hoàn tất, dữ liệu hoàn toàn nằm trong tầm kiểm soát của bạn — bao gồm [không gian làm việc](/workspaces), [issue](/issues), [bình luận](/comments) và cấu hình [agent](/agents).\n\nViệc **thực thi** của agent vẫn dựa vào [daemon](/daemon-runtimes) bạn chạy trên máy cùng các [công cụ lập trình AI](/providers) cài trên máy đó — y hệt như dùng Cloud. Tự vận hành chỉ thay tầng server, không thay tầng thực thi.\n\n## Điều kiện tiên quyết\n\n- **Docker** đã cài và chạy được `docker compose`\n- **Git** (tùy chọn, nhưng nên có để kéo mã nguồn)\n- Một máy có thể chạy liên tục (máy cục bộ / mạng nội bộ / máy chủ cloud đều được)\n- Ít nhất một công cụ lập trình AI cài trên **máy chạy daemon** (không nhất thiết là máy chạy server — laptop dev của bạn là được)\n\n## 1. Kéo dự án và khởi động backend\n\n> **Đã dùng Kubernetes?** Bỏ qua Docker và dùng Helm chart — nhảy tới [Triển khai Kubernetes](#kubernetes-deployment-alternative) bên dưới, rồi quay lại [Bước 4](#4-first-login--create-a-workspace) để đăng nhập lần đầu.\n\n```bash\ngit clone https://github.com/multica-ai/multica.git\ncd multica\nmake selfhost\n```\n\n`make selfhost` sẽ:\n\n1. Sinh `.env` từ `.env.example` nếu chưa có, với **JWT_SECRET và mật khẩu Postgres ngẫu nhiên**\n2. Kéo các Docker image chính thức (PostgreSQL, Multica backend, Multica frontend)\n3. Bật toàn bộ dịch vụ bằng `docker-compose.selfhost.yml`\n4. Chờ tới khi endpoint `/health` của backend sẵn sàng\n\nVới probe production sau khi khởi động, dùng `/readyz` khi bạn muốn phép kiểm tra thất bại khi có vấn đề về cơ sở dữ liệu hoặc migration.\n\nContainer backend **tự chạy migration cơ sở dữ liệu** khi khởi động (`docker/entrypoint.sh` chạy `./migrate up` trước khi server bật) — bạn sẽ thấy log migration trong log backend. Nâng cấp phiên bản cũng được xử lý theo cùng cách.\n\n> **Image chưa được phát hành?** Nếu `make selfhost` không kéo được image, có thể bạn đang ở một tag phiên bản chưa phát hành. Chuyển sang bản phát hành ổn định, hoặc build từ mã nguồn: `make selfhost-build`.\n\nKhi đã chạy:\n\n- **Frontend**: [http://localhost:3000](http://localhost:3000)\n- **Backend**: [http://localhost:8080](http://localhost:8080)\n\n> **Các cổng chỉ lắng nghe trên `127.0.0.1`.** `docker-compose.selfhost.yml` gắn mọi cổng công bố vào loopback — `ss -tlnp` sẽ không hiện `0.0.0.0:8080`, và các dịch vụ chủ ý không truy cập được từ máy khác. Secret và thông tin đăng nhập Postgres không bao giờ được phơi ra internet. Để truy cập từ máy khác, đặt một reverse proxy có TLS phía trước — xem [Bước 5b — Khác máy: đặt reverse proxy phía trước](#5b-cross-machine-front-with-a-reverse-proxy).\n\n## 2. Quan trọng: giữ nguyên các cơ chế an toàn production\n\n> **`docker-compose.selfhost.yml` mặc định đặt `APP_ENV` là `production`** và để trống `MULTICA_DEV_VERIFICATION_CODE`, nên không có mã cố định nào trên các máy chủ công khai.\n>\n> Chỉ đặt `MULTICA_DEV_VERIFICATION_CODE` cho môi trường cục bộ hoặc test tự động riêng tư. Nếu bật mã cố định khi `APP_ENV` không phải production, bất kỳ ai xin được mã đều có thể đăng nhập bằng giá trị cố định đó. Xem [Thiết lập xác thực → Mã kiểm thử cục bộ cố định](/auth-setup#fixed-local-testing-codes).\n>\n> Trước mọi lần triển khai công khai, đảm bảo `.env` có `APP_ENV=production` và `MULTICA_DEV_VERIFICATION_CODE` để trống.\n\n## 3. Cấu hình dịch vụ email (tùy chọn nhưng nên làm)\n\nKhông cấu hình email thì người dùng của bạn không nhận được mã xác thực qua email; server sẽ in mã ra stdout thay thế.\n\nHỗ trợ hai backend gửi mail — chọn cái phù hợp với mạng của bạn:\n\n**Phương án A — Resend (triển khai cloud / internet công cộng):**\n\n1. Đăng ký tại [Resend](https://resend.com/) và lấy API key\n2. Xác minh một tên miền gửi thư bạn sở hữu\n3. Đặt trong `.env`:\n\n    ```bash\n    RESEND_API_KEY=re_xxxxxxxxxxxx\n    RESEND_FROM_EMAIL=noreply@yourdomain.com\n    ```\n\n**Phương án B — SMTP relay (mạng nội bộ / on-premise):**\n\nDùng khi môi trường triển khai không truy cập được `api.resend.com`, hoặc bạn đã có relay mail nội bộ (Microsoft Exchange, Postfix, SendGrid on-prem, v.v.). Khi đặt cả hai, `SMTP_HOST` được ưu tiên hơn Resend, nên mail xác thực và mail mời luôn đi qua relay nội bộ. STARTTLS tự nâng cấp khi được quảng bá; cổng `465` (SMTPS / TLS ngầm định) tự bật bắt tay TLS ngay, còn `SMTP_TLS=implicit` (bí danh: `smtps`, `ssl`) ép bật trên cổng SMTPS không chuẩn.\n\nVới **relay nội bộ Exchange ẩn danh (cổng 25)** — host được tin cậy theo IP và gửi không cần thông tin đăng nhập:\n\n```bash\nSMTP_HOST=exchange.internal.example.com\nSMTP_PORT=25\nSMTP_USERNAME=\nSMTP_PASSWORD=\nSMTP_TLS_INSECURE=false\nRESEND_FROM_EMAIL=noreply@yourdomain.com  # dùng lại làm header From:\n```\n\nVới **gửi có xác thực (cổng 587, STARTTLS)** — relay yêu cầu tài khoản dịch vụ; STARTTLS tự nâng cấp khi được quảng bá:\n\n```bash\nSMTP_HOST=smtp.internal.example.com\nSMTP_PORT=587\nSMTP_USERNAME=multica\nSMTP_PASSWORD=...\nSMTP_TLS_INSECURE=false        # chỉ đặt true với CA riêng / chứng chỉ tự ký\nRESEND_FROM_EMAIL=noreply@yourdomain.com\n```\n\nVới **TLS ngầm định / SMTPS (cổng 465)** — các nhà cung cấp như mail doanh nghiệp Aliyun / Tencent không quảng bá STARTTLS. Cổng `465` tự bật TLS ngầm định nên `SMTP_TLS` ở đây là tùy chọn:\n\n```bash\nSMTP_HOST=smtp.qiye.aliyun.com\nSMTP_PORT=465\nSMTP_USERNAME=multica@yourdomain.com\nSMTP_PASSWORD=...\nSMTP_TLS=implicit              # tùy chọn trên 465; bắt buộc trên cổng SMTPS không chuẩn\nRESEND_FROM_EMAIL=noreply@yourdomain.com\n```\n\nVới **relay công cộng nghiêm ngặt (ví dụ Google Workspace `smtp-relay.gmail.com`)** vốn từ chối lời chào `localhost` mặc định từ IP công cộng, đặt `SMTP_EHLO_NAME` thành FQDN mà relay mong đợi — nếu không, kết nối bị ngắt và hiện thành lỗi `EOF` khó hiểu ở một lệnh sau đó. Mặc định là hostname của container, thường không phải FQDN hợp lệ:\n\n```bash\nSMTP_HOST=smtp-relay.gmail.com\nSMTP_PORT=587\nSMTP_EHLO_NAME=mail.yourdomain.com   # FQDN mà relay chấp nhận; mặc định là hostname container (không phải FQDN)\nRESEND_FROM_EMAIL=noreply@yourdomain.com\n```\n\nRồi khởi động lại: `docker compose -f docker-compose.selfhost.yml restart backend`. Khi khởi động lại, backend in ra provider nó chọn và chế độ TLS đã thương lượng (`EmailService: SMTP relay <host>:<port> (starttls|implicit-tls) from=…` / `Resend API` / `DEV mode`) — thông tin đăng nhập không bao giờ bị ghi log, nên dòng này an toàn để chia sẻ khi cần hỗ trợ.\n\nCấu hình xác thực thêm (OAuth, danh sách cho phép đăng ký) và tham chiếu đầy đủ biến SMTP, xem [Thiết lập xác thực](/auth-setup) và [Biến môi trường → Email](/environment-variables#email-configuration).\n\n## 4. Đăng nhập lần đầu + tạo không gian làm việc\n\nMở [http://localhost:3000](http://localhost:3000):\n\n- Nhập email của bạn\n- Lấy mã xác thực từ backend email đã cấu hình (Resend hoặc SMTP relay); nếu chưa cấu hình, chép mã từ stdout của container server — tìm dòng `[DEV] Verification code`\n- Đừng dùng `888888` trừ khi bạn đã chủ động đặt `MULTICA_DEV_VERIFICATION_CODE=888888` trên một máy chủ riêng tư không phải production\n- Đăng nhập và tạo không gian làm việc đầu tiên\n\n## 5. Trỏ CLI về server của bạn\n\nCách cài CLI giống hệt [Bắt đầu nhanh với Cloud → 2. Cài CLI](/cloud-quickstart#2-install-the-multica-cli) — Homebrew / script / PowerShell, chọn một.\n\n### 5a. Cùng máy\n\nNếu CLI và server chạy trên cùng một máy, giá trị mặc định đã dùng được ngay:\n\n```bash\nmultica setup self-host\n```\n\nLệnh này trỏ CLI về `http://localhost:8080` (backend) và `http://localhost:3000` (frontend), dẫn bạn qua đăng nhập bằng trình duyệt, lưu PAT trên máy và **tự khởi động daemon**.\n\n### 5b. Khác máy: đặt reverse proxy phía trước\n\nVì bộ compose chỉ lắng nghe trên `127.0.0.1`, daemon trên máy khác không thể truy cập thẳng `http://<server-ip>:8080` — và bạn cũng không nên muốn thế, vì secret của server sẽ bị phơi ra internet. Đặt một reverse proxy trên máy chủ để chấm dứt TLS và chuyển tiếp về `127.0.0.1:8080` (backend) và `127.0.0.1:3000` (frontend), rồi trỏ CLI về URL HTTPS công khai:\n\n```bash\nmultica setup self-host \\\n  --server-url https://<your-domain> \\\n  --app-url https://<your-domain>\n```\n\n> Thích biến môi trường hơn cờ lệnh? `setup self-host` đọc `MULTICA_SERVER_URL` và `MULTICA_APP_URL` khi cờ tương ứng bị bỏ trống — cờ vẫn được ưu tiên hơn biến môi trường. `MULTICA_SERVER_URL` cũng chấp nhận dạng `ws://…/ws` dành cho daemon từ [Biến môi trường](/environment-variables) và tự chuẩn hóa về HTTP base.\n\nMột Caddyfile tối giản phục vụ cả frontend lẫn backend (kèm hỗ trợ WebSocket, thứ mà cả daemon lẫn web app đều cần) trên một hostname duy nhất:\n\n```nginx\nmultica.example.com {\n    # Route WebSocket — phải đứng trước catch-all\n    @ws path /ws /ws/*\n    handle @ws {\n        reverse_proxy 127.0.0.1:8080 {\n            flush_interval -1\n        }\n    }\n\n    # API backend\n    handle /api/* {\n        reverse_proxy 127.0.0.1:8080\n    }\n\n    # Còn lại → frontend\n    reverse_proxy 127.0.0.1:3000\n}\n```\n\nSau khi bật proxy, đặt `FRONTEND_ORIGIN=https://multica.example.com` trong `.env` của server rồi khởi động lại backend — nếu không, phép kiểm tra origin của WebSocket sẽ từ chối trình duyệt ([Khắc phục sự cố → WebSocket không kết nối được](/troubleshooting#websocket-cant-connect)).\n\n[Cloudflare Tunnel](https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/) cũng là một lựa chọn tốt — nó cho bạn TLS và hostname công khai mà không phải mở cổng nào trên máy. Cấu hình Nginx tương đương (tách hostname `app.` / `api.`, `proxy_set_header Upgrade` cho WebSocket) cũng dùng tốt; yêu cầu then chốt là chấm dứt TLS và chuyển tiếp header `Upgrade` trên `/ws`.\n\n## 6. Tạo agent + giao task đầu tiên\n\nCùng quy trình với Cloud — xem [Bắt đầu nhanh với Cloud → Bước 5-6](/cloud-quickstart#5-create-an-agent).\n\n## 7. Tổng hợp usage (không cần thao tác vận hành)\n\n> Bảng điều khiển Usage / Runtime đọc từ bảng dẫn xuất `task_usage_hourly` do `rollup_task_usage_hourly()` ghi. Từ MUL-2957, backend chạy rollup này ngay trong tiến trình qua bộ lập lịch dựa trên DB — không cần `pg_cron` nữa, và cron ngoài / systemd timer cũng không còn là cách khuyến nghị. Image `pgvector/pgvector:pg17` đi kèm dùng được luôn, không cần chỉnh.\n\nBộ lập lịch trong tiến trình tick mỗi 30 giây và nhận một kế hoạch 5 phút theo UTC qua bảng `sys_cron_executions`. Nhiều bản sao backend vẫn an toàn — khóa duy nhất `(job_name, scope_kind, scope_id, plan_time)` đảm bảo mỗi kế hoạch chỉ một bản thắng. Triển khai mới không cần thiết lập gì.\n\n**Tương thích — các đăng ký `pg_cron` sẵn có.** Nếu trước đây bạn đã đăng ký rollup bằng job `pg_cron` (`SELECT cron.schedule('rollup_task_usage_hourly', '*/5 * * * *', …)`), bạn không cần gỡ — hàm SQL giữ advisory lock 4246 bên trong, nên bộ lập lịch của app và `pg_cron` không thể ghi trùng. Để xóa mục thừa:\n\n```sql\nSELECT cron.unschedule('rollup_task_usage_hourly')\n  FROM cron.job WHERE jobname = 'rollup_task_usage_hourly';\n```\n\n**Nâng cấp từ `v0.3.4 → v0.3.5+`.** Bản trước yêu cầu người vận hành chạy tay `cmd/backfill_task_usage_hourly` trước khi áp migration 103, nếu không guard fail-closed của migration sẽ chặn `migrate up`. Từ MUL-2957 việc này là tự động: lệnh migrate chạy backfill theo lát tháng có tính idempotent (dưới advisory lock 4246) ngay trước khi áp migration 103, rồi tiếp tục. Bạn vẫn có thể chạy backfill độc lập trên DB bận để giảm áp lực đọc bằng `--sleep-between-slices=2s`, nhưng không còn bắt buộc.\n\nTham chiếu đầy đủ — gồm ghi chú vận hành và hình dạng triển khai Kubernetes — nằm trong `SELF_HOSTING_ADVANCED.md → Usage Dashboard Rollup` của repo.\n\n## Triển khai Kubernetes (phương án thay thế)\n\nNếu bạn đã vận hành cụm Kubernetes, repo cũng kèm Helm chart tại `deploy/helm/multica/`. Nó tương đương `make selfhost` cho k8s — cùng image backend, image frontend và Postgres `pgvector/pgvector:pg17`, đóng gói thành Deployment / Service / Ingress với một `ConfigMap` render từ `values.yaml`. Được viết cho k3s + Traefik + `local-path` và sẽ chạy trên bất kỳ cụm nào có Ingress controller cùng StorageClass `ReadWriteOnce` mặc định.\n\nChart **không template giá trị secret**. Nó tham chiếu một Secret tên `multica-secrets` theo tên, nên các khóa JWT / DB / Resend / Google thật không bao giờ phải nằm trong git hay `values.yaml`. Tạo namespace + Secret một lần bằng kubectl:\n\n```bash\nkubectl create namespace multica\n\nkubectl -n multica create secret generic multica-secrets \\\n  --from-literal=JWT_SECRET=\"$(openssl rand -hex 32)\" \\\n  --from-literal=POSTGRES_PASSWORD=\"$(openssl rand -hex 16)\" \\\n  --from-literal=RESEND_API_KEY=\"\" \\\n  --from-literal=GOOGLE_CLIENT_SECRET=\"\" \\\n  --from-literal=CLOUDFRONT_PRIVATE_KEY=\"\" \\\n  --from-literal=MULTICA_DEV_VERIFICATION_CODE=\"\"\n```\n\nRồi cài chart:\n\n```bash\ngit clone https://github.com/multica-ai/multica.git\ncd multica\nhelm install multica deploy/helm/multica -n multica\n```\n\nMặc định giả định hostname `multica.dev.lan` (web) và `api.multica.dev.lan` (backend). Thêm chúng vào `/etc/hosts` (hoặc DNS cục bộ) trỏ về IP node bất kỳ nơi Ingress truy cập được. Để dùng hostname khác, chép `deploy/helm/multica/values.yaml`, sửa `ingress.frontend.host` / `ingress.backend.host` và các mục tương ứng `backend.config.appUrl` / `frontendOrigin` / `localUploadBaseUrl` / `googleRedirectUri`, rồi cài với `-f my-values.yaml`.\n\nTrên cụm nguội, backend có thể ở trạng thái `Running` nhưng chưa `Ready` trong vài phút khi chờ Postgres và chạy migration — startupProbe hấp thụ khoảng này nên pod sẽ không restart. Khi đã `Ready`:\n\n```bash\ncurl -H \"Host: api.multica.dev.lan\" http://<ingress-ip>/healthz\n# {\"status\":\"ok\",\"checks\":{\"db\":\"ok\",\"migrations\":\"ok\"}}\n```\n\nRồi mở `http://multica.dev.lan` và tiếp tục ở [Bước 4 — Đăng nhập lần đầu](#4-first-login--create-a-workspace) phía trên. Trỏ CLI về hostname Ingress của bạn:\n\n```bash\nmultica setup self-host \\\n  --server-url http://api.multica.dev.lan \\\n  --app-url http://multica.dev.lan\n```\n\nĐể kéo image mới nhất mà không đổi chart: `kubectl -n multica rollout restart deploy/multica-backend deploy/multica-frontend`. Để ghim một bản phát hành Multica cụ thể, đặt `images.backend.tag` / `images.frontend.tag` trong values file rồi `helm upgrade`. `helm -n multica uninstall multica` gỡ workload nhưng giữ PVC và Secret; `kubectl delete namespace multica` xóa sạch tất cả.\n\nTham chiếu đầy đủ — ba chế độ đăng nhập, workaround ExternalName `backend` cho `REMOTE_API_URL` bị nướng lúc build trong image web, giới hạn tài nguyên và TLS — nằm trong `SELF_HOSTING.md` của repo.\n\n## Sự cố thường gặp\n\n- **Backend không khởi động**: xem log container bằng `docker compose -f docker-compose.selfhost.yml logs backend`; thường do `DATABASE_URL` hoặc `JWT_SECRET` sai trong `.env`\n- **Không nhận được mã xác thực**: chưa cấu hình backend email nào (cả Resend lẫn SMTP) → tìm `[DEV] Verification code` trong `docker compose logs backend`\n- **WebSocket không kết nối**: với triển khai công khai bạn phải đặt `FRONTEND_ORIGIN` đúng tên miền frontend thật; xem [Khắc phục sự cố → WebSocket không kết nối được](/troubleshooting#websocket-wont-connect)\n- **Bảng Usage / Runtime toàn số 0**: `rollup_task_usage_hourly()` không được lập lịch — xem [Bước 7](#7-usage-rollup-no-operator-action-required) phía trên và [Khắc phục sự cố → Bảng usage hiện 0](/troubleshooting#usage-dashboard-stays-at-zero)\n- **`migrate up` lỗi `refusing to drop legacy daily rollups`**: guard đường nâng cấp `v0.3.4 → v0.3.5+`. Từ MUL-2957, lệnh migrate tự chạy backfill trước khi áp migration 103 — xem [Bước 7](#7-usage-rollup-no-operator-action-required)\n\n## Bước tiếp theo\n\n- [Biến môi trường](/environment-variables) — tham chiếu env đầy đủ\n- [Thiết lập xác thực](/auth-setup) — Resend / OAuth / danh sách cho phép đăng ký chi tiết\n- [Tích hợp GitHub](/github-integration) — kết nối GitHub App để PR tự liên kết issue và merge thì đóng issue\n- [Khắc phục sự cố](/troubleshooting) — bắt đầu từ đây khi có trục trặc"
        }
      ]
    },
    {
      "label": "Workspace & team",
      "labelKey": "workspace_team",
      "pages": [
        {
          "slug": "workspaces",
          "title": "Không gian làm việc",
          "content": "Không gian làm việc là **nơi khép kín để một nhóm cộng tác trong Multica** — mọi [issue](/issues), [thành viên](/members-roles), [bình luận](/comments) và [agent](/agents) đều thuộc về một không gian làm việc. Danh sách issue, danh sách thành viên và cấu hình agent bạn thấy sau khi đăng nhập đều gói trong không gian làm việc hiện tại; **chuyển không gian làm việc là thay toàn bộ khung nhìn**.\n\n## Tạo không gian làm việc\n\nBa thứ được quyết định khi bạn tạo một không gian làm việc:\n\n- **Tên** — tên hiển thị mà thành viên nhìn thấy. Cho phép dấu cách và ký tự ngoài ASCII. Có thể đổi sau.\n- **Slug** — chuỗi dùng trong URL của không gian làm việc. Chỉ chữ thường và chữ số (nối bằng `-`). **Không đổi được sau khi tạo**, nên hãy chọn kỹ. Nếu slug đã có người dùng hoặc trùng từ khóa hệ thống, màn hình tạo sẽ yêu cầu chọn slug khác.\n- **Tiền tố issue** — tiền tố cho mọi số issue trong không gian làm việc (phần `MUL` trong `MUL-123`). Chữ hoa và chữ số, tối đa 10 ký tự.\n\n> **Tránh đổi tiền tố issue.** Số issue được hiển thị theo tiền tố hiện tại — đổi nó thì `MUL-5` lập tức thành `NEW-5`. Mọi liên kết bên ngoài, lượt nhắc trên Slack và tham chiếu lịch sử trong bình luận đều gãy so với số cũ. Hãy coi tiền tố issue là \"đặt lúc tạo, không bao giờ đụng vào.\"\n\nBạn có thể tạo không gian làm việc từ giao diện web hoặc dòng lệnh:\n\n```bash\nmultica workspace create\n```\n\n## Số issue\n\nMỗi issue tạo trong không gian làm việc được tự gán số theo dạng `<tiền tố>-<số>` — `MUL-1`, `MUL-2`, `MUL-3`. Vài tính chất:\n\n- **Tuần tự và duy nhất trong một không gian làm việc** — mỗi không gian làm việc có bộ đếm riêng; không ảnh hưởng lẫn nhau.\n- **Không tự gán được** — khi tạo issue bạn chỉ nhập tiêu đề; số do hệ thống cấp.\n- **Không tái sử dụng khi xóa** — xóa `MUL-5` thì issue mới kế tiếp là `MUL-6`, không phải `MUL-5`.\n\n## Xóa không gian làm việc\n\nChỉ owner mới xóa được cả không gian làm việc. Xóa là **không thể hoàn tác**.\n\n> Xóa không gian làm việc sẽ quét sạch cùng lúc:\n>\n> - Mọi issue, dự án, bình luận và cảm xúc\n> - Mọi tệp đính kèm\n> - Mọi tư cách thành viên và lời mời đang chờ\n> - Mọi cấu hình agent cùng lịch sử task của nó\n>\n> **Dữ liệu không thể khôi phục.** Xuất những gì cần giữ trước khi xóa.\n\nNếu bạn là owner cuối cùng của không gian làm việc và muốn rời đi, hãy chuyển vai trò owner cho thành viên khác trước, rồi để owner mới (hoặc bạn) quyết định có xóa hay không. Xem [Thành viên và vai trò](/members-roles).\n\n## Tiếp theo\n\n- [Thành viên và vai trò](/members-roles) — cách thêm người vào không gian làm việc, và mỗi vai trò trong ba vai trò làm được gì\n- [Issue và dự án](/issues) — các đối tượng công việc cốt lõi trong không gian làm việc"
        },
        {
          "slug": "members-roles",
          "title": "Thành viên và vai trò",
          "content": "Ai trong [không gian làm việc](/workspaces) cũng có một vai trò, và vai trò quyết định họ làm được gì. Multica có ba: **owner** (chủ không gian làm việc), **admin** và **member**. Hầu hết công việc hằng ngày — tạo [issue](/issues), viết [bình luận](/comments), dùng [agent](/agents) — cả ba vai trò đều làm được. **Khác biệt tập trung ở quản lý nhóm.**\n\n## Quyền hạn trong một bảng\n\nBảng dưới liệt kê các khác biệt quan trọng nhất trong các thao tác quản lý nhóm:\n\n| Thao tác | owner | admin | member |\n|---|---|---|---|\n| Mời admin hoặc member mới | ✓ | ✓ | ✗ |\n| **Mời owner mới** | ✓ | ✗ | ✗ |\n| Hạ cấp / gỡ admin hoặc member | ✓ | ✓ | ✗ |\n| **Hạ cấp / gỡ một owner khác** | ✓ | ✗ | ✗ |\n| Xóa không gian làm việc | ✓ | ✗ | ✗ |\n\n**Member không mời được ai** — mời người là quyền của tầng admin. **Chỉ owner mới nâng được ai đó lên owner** — admin nâng và hạ được member hoặc admin khác, nhưng không tạo được owner mới. Tương tự, admin gỡ được member hoặc admin khác nhưng **không đụng được owner hiện có**. Mục đích là đảm bảo tầng cao nhất chỉ được cấp bởi người đã nắm nó — quyền không rò rỉ lên trên.\n\n> Phạm vi hiển thị của agent có hai kiểu: \"workspace\" và \"private\". Agent private chỉ có owner và admin giao được vào issue — điều này bảo vệ những cấu hình dành cho một nhóm người cụ thể. Xem [Agent](/agents).\n\n## Mời thành viên mới\n\nMultica mời thành viên mới qua email:\n\n1. Trong trang cài đặt không gian làm việc, bấm **Invite member**, nhập email và chọn vai trò.\n2. Multica gửi email mời chứa một liên kết duy nhất.\n3. Người nhận bấm liên kết, đăng nhập (hoặc đăng ký) và **chấp nhận lời mời** để tham gia không gian làm việc.\n\nEmail được mời **không cần đăng ký Multica từ trước** — nếu chưa có tài khoản, tài khoản được tạo khi lời mời được chấp nhận.\n\nNếu email mời gửi không tới (sai địa chỉ, dịch vụ mail trục trặc), bản ghi lời mời vẫn được giữ; bạn có thể gửi lại email từ cài đặt không gian làm việc, hoặc chia sẻ liên kết mời qua kênh khác.\n\nLời mời **có hiệu lực 7 ngày**. Sau đó, bấm liên kết sẽ hiện thông báo \"hết hạn\" và người mời cần gửi lời mời mới.\n\n## Luôn có ít nhất một owner\n\nMỗi không gian làm việc **luôn phải có ít nhất một owner**. Ràng buộc này tự chặn hai thao tác:\n\n- Owner cuối cùng không tự hạ cấp được.\n- Owner hoặc admin khác không gỡ được owner cuối cùng.\n\n> Nếu bạn là owner cuối cùng và sắp rời nhóm, **hãy chuyển vai trò owner cho thành viên khác trước**, rồi mới rời hoặc bàn giao không gian làm việc. Nếu không, thao tác sẽ bị từ chối.\n\n## Gỡ thành viên\n\nOwner và admin gỡ được thành viên khác khỏi không gian làm việc. Thành viên bị gỡ mất quyền truy cập ngay lập tức; issue, bình luận và nội dung khác họ đã tạo vẫn được giữ trong không gian làm việc.\n\n## Tiếp theo\n\n- [Issue và dự án](/issues) — thứ mà thành viên làm việc trên đó\n- [Bình luận và nhắc tên](/comments) — cộng tác bên dưới một issue"
        },
        {
          "slug": "issues",
          "title": "Issue và dự án",
          "content": "Issue là một đơn vị công việc khép kín trong Multica — một bug, một tính năng mới, một việc cần làm. Mỗi issue có **tiêu đề**, **mô tả** (hỗ trợ Markdown), **trạng thái**, **độ ưu tiên**, **người phụ trách**, và có thể thuộc về một **dự án**. Nếu bạn từng dùng Linear hay Jira thì hình dạng y hệt.\n\n**Nét đặc trưng của Multica là người phụ trách issue có thể là con người hoặc một [agent](/agents)** — và ta bắt đầu từ đó.\n\n## Giao issue cho agent\n\n[Giao](/assigning-issues) issue cho agent là trao việc đó cho nó. Agent **tự khởi động** — thực thi trong vài giây, báo tiến độ trong bình luận, và chuyển trạng thái sang done khi xong. Khác biệt duy nhất so với giao việc cho đồng đội là agent không offline, không cần nhắc và sẵn sàng 24/7.\n\n> Về danh tính, cấu hình và nơi agent chạy, xem [Agent](/agents).\n\nAgent private chỉ có owner và admin của không gian làm việc giao được vào issue. Về quyền theo vai trò, xem [Thành viên và vai trò](/members-roles).\n\n## Trạng thái\n\nMultica có bảy trạng thái. **Trạng thái nào cũng chuyển thẳng sang trạng thái khác được** — Multica không áp đặt quy trình, và sẽ không cản bạn nhảy từ `backlog` thẳng tới `done`.\n\n| Trạng thái | Ý nghĩa |\n|---|---|\n| `backlog` | Chưa lên lịch |\n| `todo` | Đã lên lịch, sẵn sàng bắt đầu |\n| `in_progress` | Đang làm |\n| `in_review` | Chờ review |\n| `done` | Hoàn thành |\n| `blocked` | Kẹt vì yếu tố bên ngoài |\n| `cancelled` | Đã hủy |\n\nKhi issue được giao cho agent, agent tự chuyển trạng thái từ `backlog` / `todo` sang `in_progress`, rồi sang `done` khi hoàn tất. Bạn cũng đổi tay được bất cứ lúc nào.\n\n## Độ ưu tiên\n\nĐộ ưu tiên có năm mức, dùng để sắp thứ tự danh sách issue mặc định:\n\n| Độ ưu tiên | Dùng khi |\n|---|---|\n| `No priority` | Chưa quyết (mặc định) |\n| `Urgent` | Khẩn cấp |\n| `High` | Cao |\n| `Medium` | Trung bình |\n| `Low` | Thấp |\n\n## Số issue\n\nMỗi issue có một số duy nhất trong không gian làm việc theo dạng `<tiền tố>-<số>` — ví dụ `MUL-123`. Số do hệ thống cấp lúc tạo và **không bao giờ đổi**. Xem [Không gian làm việc → Số issue](/workspaces#issue-numbers).\n\n## Bình luận\n\nLuồng bình luận dưới issue là nơi cộng tác diễn ra — trả lời bình luận, `@` một người hoặc agent, thả cảm xúc.\n\n`@` một agent trong bình luận và **nó tự kích hoạt** — đây là cách thứ hai để khởi động agent, bên cạnh \"giao việc\". Xem [Bình luận và nhắc tên](/comments) và [Nhắc agent trong bình luận](/mentioning-agents).\n\n## Xóa issue\n\n> Xóa issue sẽ **ngay lập tức** dọn sạch mọi bình luận, cảm xúc và tệp đính kèm bên dưới, cùng mọi task agent đang xếp hàng (task đang chạy bị hủy). **Không thể hoàn tác.**\n>\n> Nếu chỉ muốn issue khuất mắt, **đổi trạng thái sang `cancelled` an toàn hơn xóa** — dữ liệu còn nguyên và bạn kéo lại được sau.\n\n## Dự án\n\nDự án là vật chứa gom nhiều issue lại với nhau. Một issue thuộc tối đa một dự án, hoặc không thuộc dự án nào.\n\nDự án có **lead** riêng — **giống người phụ trách issue, lead có thể là con người hoặc agent**.\n\nXóa dự án **không xóa các issue bên trong**: các issue đó chỉ tách khỏi dự án và vẫn ở trong không gian làm việc.\n\n## Tiếp theo\n\n- [Bình luận và nhắc tên](/comments) — cộng tác bên dưới một issue\n- [Agent](/agents) — hiểu \"giao cho agent\" thực sự hoạt động ra sao"
        },
        {
          "slug": "projects",
          "title": "Dự án",
          "content": "**Dự án** trong Multica là vật chứa cho các [issue](/issues) liên quan. Dùng nó khi một khối công việc lớn hơn một issue nhưng nhỏ hơn cả không gian làm việc — một đợt ra mắt, một cuộc di trú, một tính năng nhiều phần, một cuộc điều tra rẽ thành nhiều nhánh.\n\nMỗi dự án có tên, biểu tượng, mô tả, một **lead** (thành viên hoặc [agent](/agents)), một **trạng thái** (`planned` / `in_progress` / `paused` / `completed` / `cancelled`), một **độ ưu tiên** (`urgent` / `high` / `medium` / `low` / `none`), và một tỉ lệ **tiến độ** tự suy ra từ trạng thái các issue đã liên kết.\n\n## Dự án quan hệ với issue thế nào\n\nDự án và issue là hai đối tượng độc lập với quan hệ nhiều-về-một: một issue thuộc **tối đa một** dự án; một dự án chứa **bao nhiêu issue cũng được**. Liên kết và gỡ liên kết đảo ngược được bất cứ lúc nào — kéo trong khung bảng, hoặc dùng bộ chọn dự án ở bảng thuộc tính bên phải của issue.\n\nThanh tiến độ của dự án được tính từ các issue liên kết — càng nhiều issue chạm `done`, thanh càng đầy. Issue `cancelled` bị loại khỏi phép đếm; issue ở `backlog` tính vào mẫu số nhưng không tính vào tử số.\n\n## Ghim vào thanh bên\n\nBấm biểu tượng ghim ở góc trên phải của dự án để thêm nó vào danh sách ghim trên thanh bên. Dự án đã ghim luôn cách một cú bấm dù bạn đang ở đâu trong không gian làm việc; mỗi người trong nhóm ghim độc lập — ghim là của cá nhân.\n\nLiên kết **Không gian làm việc → Dự án** trên thanh bên luôn hiện mọi dự án trong không gian làm việc; ghim chỉ là lối tắt cá nhân đặt bên trên.\n\n## Đính kèm tài nguyên\n\nMỗi dự án có mục **Tài nguyên** để bạn đính kèm các repository GitHub. Sau khi đính kèm, mọi [agent](/agents) được giao issue trong dự án này có thể đọc và ghi các repo đó khi thực thi task — Multica truyền URL repo làm ngữ cảnh cho [daemon](/daemon-runtimes).\n\nTài nguyên tính theo dự án; nếu nhiều dự án dùng chung một repo, đính kèm vào từng dự án.\n\n## Xóa dự án\n\nXóa dự án **không xóa các issue của nó**. Các issue liên kết chỉ bị gỡ liên kết và quay về danh sách issue phẳng của không gian làm việc. Đây là chủ ý — công việc từng thuộc một dự án hiếm khi là đồ bỏ, kể cả khi khung dự án thay đổi.\n\n> Nếu muốn xóa cả công việc, hãy lưu trữ hoặc xóa các issue trước, rồi mới xóa dự án.\n\n## Lead của dự án\n\nLead là người — hoặc agent — chịu trách nhiệm về dự án. Đây là tín hiệu mềm, không phải kiểm soát truy cập: thành viên nào của không gian làm việc cũng sửa được dự án bất kể ai là lead. Lead của dự án có thể là:\n\n- Một thành viên không gian làm việc (đồng đội con người)\n- Một [agent](/agents) — hữu ích khi công việc của dự án chủ yếu ủy thác cho agent (ví dụ \"Phân loại bug hằng tuần\" do một agent phân loại đứng đầu)\n\n## Tiếp theo\n\n- [Issue](/issues) — đơn vị công việc sống bên trong dự án\n- [Agent làm lead dự án](/agents) — khi nào agent là người phụ trách phù hợp\n- [Cách Multica hoạt động](/how-multica-works) — bức tranh rộng hơn"
        },
        {
          "slug": "comments",
          "title": "Bình luận và nhắc tên",
          "content": "Mỗi [issue](/issues) có một luồng bình luận. Đăng bình luận, trả lời ai đó, `@` một [thành viên](/members-roles) hoặc [agent](/agents), thả cảm xúc — những thao tác quen thuộc ở mọi trình quản lý công việc bạn từng dùng. Khác biệt duy nhất: **nhắc một agent bằng `@` sẽ kích hoạt nó bắt đầu làm việc.**\n\n## Đăng bình luận\n\nGõ vào ô nhập ở cuối trang chi tiết issue và bấm **Gửi**. Bình luận hiện trong luồng ngay lập tức. Bình luận hỗ trợ Markdown — tiêu đề, danh sách, khối mã, liên kết, đủ cả.\n\n## Trả lời bình luận\n\nBấm **Trả lời** ở góc trên phải của bình luận bất kỳ để mở ô nhập lồng bên dưới. Câu trả lời hiển thị như con của bình luận đó, tạo thành luồng hội thoại. Trả lời có thể có trả lời riêng, lồng sâu bao nhiêu tùy bạn.\n\nDanh sách issue chỉ hiện số bình luận cấp cao nhất; mở issue mới thấy cả cây hội thoại.\n\n## Cảm xúc\n\nMỗi bình luận có nút cảm xúc ở góc trên phải cho các tín hiệu nhanh (👍, 👀, 🎉) — không cần đăng bình luận \"+1\" để đồng tình.\n\n## Nhắc bằng `@`\n\nGõ `@` trong bình luận sẽ mở bộ chọn. Chọn một thành viên hoặc agent, và `@` kèm slug của đối tượng được chèn vào (`@alice` hoặc `@reviewer-bot`). Bên được nhắc nhận thông báo trong [hộp thư đến](/inbox) của họ.\n\n**Nếu bạn nhắc một agent, nó tự kích hoạt** — xem [Nhắc agent trong bình luận](/mentioning-agents).\n\nNhắc cùng một người nhiều lần trong một bình luận vẫn chỉ sinh **một** thông báo.\n\n### `@all` thông báo cả không gian làm việc\n\n`@all` là đích đặc biệt: nó đẩy thông báo tới mọi thành viên của không gian làm việc. Cả người lẫn agent đều dùng được `@all` — nghĩa là một agent báo tiến độ cũng có thể `@all`, nên hãy nhắc agent trong phần chỉ dẫn của nó là dùng tiết chế.\n\n> **Dùng `@all` cẩn thận.** Trong không gian làm việc đông người, một lần `@all` sinh chừng đó thông báo hộp thư ngay lập tức. Dành nó cho những gì mọi người thật sự cần biết — không phải cập nhật thường nhật.\n\n## Tham chiếu issue\n\nĐể liên kết issue khác, chọn nó từ bộ chọn nhắc trong bình luận. Multica lưu liên kết issue dưới dạng mention tường minh `[MUL-123](mention://issue/<uuid>)`. Liên kết issue chỉ là tham chiếu chéo: không thông báo cho ai và không kích hoạt agent.\n\nGõ trần một mã issue, ví dụ `MUL-123`, thì nó vẫn là văn bản thuần. Điều này cũng giữ tên nhánh và đường dẫn, như `feature/MUL-123`, không bị viết lại bên trong bình luận.\n\n> Nhấn mạnh Markdown theo quy tắc CommonMark. Khi chữ đậm kết thúc bằng dấu câu hoặc dấu nháy đóng và ngay sau đó là một trợ từ tiếng Hàn, dấu `**` đóng có thể không được nhận ra.\n>\n> Ưu tiên đưa dấu nháy ra ngoài phần in đậm:\n>\n> ```markdown\n> \"**무엇을 먼저 정해두고 시작할지**\"가\n> ```\n>\n> thay vì:\n>\n> ```markdown\n> **\"무엇을 먼저 정해두고 시작할지\"**가\n> ```\n\n## Sửa và xóa bình luận\n\nChỉ tác giả bình luận mới sửa hoặc xóa được nó.\n\nXóa một bình luận cũng **xóa mọi trả lời** bên dưới (kể cả trả lời của trả lời). Chỉ muốn đổi nội dung thì dùng sửa.\n\n> **Thêm `@` khi sửa bình luận không kích hoạt agent.** Kích hoạt chỉ nổ vào lúc bình luận được **tạo** — sửa để thêm `@` mới, hay đổi đích, không gửi thông báo mới và không đánh thức agent. Muốn gọi agent bạn đã bỏ lỡ, **đăng bình luận mới** có `@` nó.\n\n---\n\nTất cả những gì vừa nói là \"thế giới con người\" — không gian làm việc, thành viên, issue, dự án, bình luận. Nếu bạn từng dùng Linear hay Jira, chẳng có gì lạ lẫm.\n\nNhưng nét đặc trưng của Multica vẫn chưa xuất hiện: **coi agent là thành viên hạng nhất của không gian làm việc**. Đó là phần tiếp theo.\n\n## Tiếp theo\n\n- [Agent](/agents) — chúng là gì, và khác con người ở đâu\n- [Nhắc agent trong bình luận](/mentioning-agents) — dùng `@` trong bình luận để khởi động agent"
        },
        {
          "slug": "project-resources",
          "title": "Tài nguyên dự án",
          "content": "**Tài nguyên dự án** là một con trỏ có kiểu — URL repo Git, đường dẫn trên máy bạn, mai này là trang Notion — đính vào một [dự án](/workspaces). Khi một [agent](/agents) chạy trên issue thuộc dự án đó, daemon tự ghi danh sách tài nguyên của dự án vào thư mục làm việc của agent và vào prompt [meta-skill](/skills) của nó.\n\nKết quả: agent biết cần checkout repo nào (hoặc làm việc trong thư mục cục bộ nào), và tài liệu nào là \"tham chiếu chính\" của dự án, mà không ai phải chép-dán ngữ cảnh vào phần thân issue.\n\n## Mô hình tư duy\n\nDự án không còn chỉ là cái nhãn. Nó là một **vật chứa tài nguyên** nhỏ:\n\n- Một dự án có 0..N **tài nguyên**.\n- Một tài nguyên có `resource_type` (ví dụ `github_repo`, `local_directory`) và `resource_ref` (payload JSON định kiểu theo `resource_type`).\n- Kiểu tài nguyên mới chỉ cần thêm một chuỗi + một handler. **Không migration schema. Không viết lại frontend.**\n\nHình dạng này là chủ ý — đúng mẫu Multica đã dùng cho provider của agent: một discriminator `type` và một payload có kiểu. Nó giữ schema ổn định, nên bổ sung \"trang Notion\", \"Google Doc\", \"file tải lên\" hay \"URL ngoài\" sau này chỉ là thay đổi cộng thêm rất nhỏ.\n\nHiện có hai kiểu tài nguyên: [`github_repo`](#resource-type-github_repo) (clone theo từng task vào worktree cách ly) và [`local_directory`](#resource-type-local_directory) (chạy trực tiếp trong một thư mục trên máy của một daemon cụ thể).\n\n## Kiểu tài nguyên: `github_repo`\n\nKiểu mặc định — checkout theo từng task vào worktree cách ly:\n\n```json\n{\n  \"resource_type\": \"github_repo\",\n  \"resource_ref\": {\n    \"url\": \"https://github.com/owner/repo\",\n    \"ref\": \"release/v2\",\n    \"default_branch_hint\": \"main\"\n  }\n}\n```\n\n`ref` là tùy chọn — nếu có, `multica repo checkout <url>` dùng nó làm nhánh, tag hoặc commit mặc định cho task trong dự án này. Một lệnh `multica repo checkout <url> --ref <other-ref>` tường minh vẫn thắng cho lần checkout đó.\n\n`default_branch_hint` là ngữ cảnh prompt tùy chọn. Nó không dùng để checkout; dùng `ref` khi dự án cần ghim nhánh, tag hoặc SHA.\n\n## Kiểu tài nguyên: `local_directory`\n\nCho các repo không thể clone lại theo từng task một cách hợp lý — checkout game nhiều gigabyte, monorepo lớn, hay bất kỳ dự án nào mà mô hình worktree-mỗi-task gây khổ sở — dự án có thể trỏ vào **một thư mục sẵn có trên máy của một [daemon](/daemon-runtimes) cụ thể**. Agent chạy **ngay trong thư mục đó**, không clone, không copy, không worktree.\n\n```json\n{\n  \"resource_type\": \"local_directory\",\n  \"resource_ref\": {\n    \"local_path\": \"/Users/me/code/big-game\",\n    \"daemon_id\": \"0001234e-…\",\n    \"label\": \"main checkout\"\n  }\n}\n```\n\nĐánh đổi so với `github_repo` là chủ ý: chỉ daemon được gắn mới nhận được task trên thư mục đó, và các task trên cùng thư mục chạy **tuần tự** thay vì song song. Đổi lại bạn giữ nguyên checkout, nhánh và trạng thái dở dang hiện có — Multica không bao giờ clone lại.\n\n### Khi nào chọn `local_directory` thay vì `github_repo`\n\n| Mối quan tâm | `github_repo` (worktree) | `local_directory` |\n| --- | --- | --- |\n| Chi phí checkout mỗi task | Clone mới + worktree | Không — agent chạy tại chỗ |\n| Đồng thời trên cùng repo | Nhiều task song song | Mỗi thư mục một task một lúc |\n| Nhánh / trạng thái dở dang | Mỗi task một nhánh mới từ mặc định | Nguyên trạng thư mục hiện tại |\n| Chạy ở đâu được | Daemon bất kỳ | Đúng một daemon (cái được gắn) |\n| Dung lượng đĩa | Một worktree mỗi task | Không tốn thêm — thư mục sẵn có |\n\nChọn `local_directory` khi **một trong hai** điều này đúng:\n\n1. **Clone lại đắt tới mức không chấp nhận được** — checkout game nhiều gigabyte, monorepo nặng LFS, hay bất kỳ trường hợp nào `git clone` mỗi task lấn át chính công việc. Bạn đổi tính song song lấy lần chạy không cần clone.\n2. **Thay đổi của bạn nhỏ lẻ và bạn muốn review ngay trên máy khi chúng diễn ra** — bạn đang lặp trên một component, muốn nhảy qua lại giữa chỉnh sửa của agent và editor mỗi vài phút, và muốn checkout hiện có là nguồn sự thật thay vì worktree mỗi task phải đào từ `~/multica_workspaces/`.\n\nĐánh đổi bạn chấp nhận ở cả hai trường hợp là một: **phiên bản này không có khóa ghi mức file.** Cổng tuần tự theo thư mục (mỗi thư mục một task một lúc) là lớp bảo vệ duy nhất chống chuyện agent của hai issue đụng cùng file cùng lúc. Trỏ agent của hai issue vào cùng một `local_directory` thì task của chúng xếp hàng thay vì chạy song song — đó là chủ ý. Cần song song thật trên cùng codebase thì ở lại `github_repo`.\n\n### Đính thư mục cục bộ\n\nBộ chọn thư mục chỉ có trong **app Desktop** — web không có cách đọc đường dẫn hệ điều hành, nên nút \"Add local directory\" bị ẩn ở đó. Trên Desktop:\n\n1. Mở dự án → khung **Tài nguyên**.\n2. Bấm **Add local directory**. Bộ chọn thư mục native mở ra.\n3. Chọn thư mục. Đường dẫn được gắn với **daemon hiện đang đăng ký bởi bản Desktop này** — bản ghi tài nguyên lưu cả đường dẫn lẫn ID daemon đó.\n\nTrên Desktop, nút vẫn hiện nhưng **bị vô hiệu kèm gợi ý** khi daemon trên máy này offline, hoặc khi dự án đã có `local_directory` gắn với daemon này — để bạn thấy *vì sao* nó không dùng được. (Trên web, nút bị ẩn hẳn vì hoàn toàn không có bộ chọn thư mục.) Muốn gắn thư mục của máy khác, cài Desktop trên máy đó và thêm tài nguyên từ đó.\n\nTừ CLI (dùng được cả trong môi trường chỉ có web, miễn bạn tự cung cấp ID daemon):\n\n```bash\nmultica project resource add <project-id> \\\n  --type local_directory \\\n  --local-path /Users/me/code/big-game \\\n  --daemon-id <daemon-uuid> \\\n  --ref-label \"main checkout\"        # tùy chọn\n\nmultica project resource update <project-id> <resource-id> \\\n  --local-path /Users/me/code/big-game-new\n```\n\n`--daemon-id` lấy từ `multica daemon list`. CLI cũng nhận lối thoát chung `--ref '<json>'` nếu bạn muốn truyền thẳng payload.\n\n### Quy tắc đường dẫn\n\nĐường dẫn bạn đính phải vượt qua kiểm tra lúc đính và kiểm tra theo từng task. Cả hai do daemon sở hữu tài nguyên thi hành — server chỉ lưu JSON. Đường dẫn phạm bất kỳ quy tắc nào sẽ làm task thất bại với lỗi có kiểu và không đụng gì tới thư mục của bạn:\n\n- Phải là đường dẫn **tuyệt đối**.\n- Phải **tồn tại** và là **thư mục** (không phải file, symlink tới file, hay device node).\n- Phải **đọc và ghi được** bởi tiến trình daemon.\n- Không được là gốc hệ thống hay cả một hồ sơ người dùng — `/`, `/Users`, `/home`, `/root`, `/etc`, `/tmp`, `/var`, `/usr`, `/opt`, `/Users/Shared`, chính `$HOME` của bạn, gốc ổ đĩa Windows bất kỳ (`C:\\`, `D:\\`, …), hay `C:\\Users` / `C:\\ProgramData` / `C:\\Program Files` / `C:\\Program Files (x86)` / `C:\\Windows`.\n- Symlink phân giải về bất kỳ mục nào ở trên đều bị từ chối, dạng chính tắc của đường dẫn bí danh hệ điều hành cũng vậy (ví dụ trên macOS, gõ `/private/tmp` bị từ chối y như `/tmp`).\n\nDanh sách chặn cố tình gắt — chọn thư mục home sẽ đặt file runtime của Multica ngay gốc tài khoản của bạn, điều không bao giờ nên. Hãy chọn thư mục con (thường là checkout dự án thật của bạn).\n\n### Một cho mỗi (dự án, daemon)\n\nMột dự án giữ **tối đa một `local_directory` cho mỗi daemon**. Thêm cái thứ hai trên cùng daemon sẽ nhận `409` từ API; nút trên Desktop tự ẩn khi đã chạm giới hạn và hiện tooltip giải thích.\n\nCác daemon khác nhau độc lập với nhau — một dự án dùng chung có thể có một `local_directory` cho máy của từng đồng đội, mỗi cái gắn cùng dự án với một thư mục khác trên một máy khác. Khi daemon nhận task, nó chọn dòng khớp ID của chính nó và bỏ qua phần còn lại.\n\n### Trộn kiểu tài nguyên, và nhiều `local_directory`\n\nHai cấu hình chéo tài nguyên hay gặp trong thực tế:\n\n- **`github_repo` + `local_directory` trên cùng dự án.** Trên daemon có gắn `local_directory` khớp, thư mục cục bộ **được ưu tiên**: agent chạy trong thư mục của bạn, daemon không tạo hay dùng worktree `github_repo` cho task đó. (Cache repo theo không gian làm việc vẫn có thể đồng bộ như thường — đó là hành vi nền không liên quan cây làm việc của task này.) URL `github_repo` vẫn xuất hiện trong `.multica/project/resources.json` và mục `## Repositories` của agent để tham chiếu — nhưng cây làm việc agent sửa là thư mục cục bộ của bạn, không phải worktree. Trên daemon **không** có dòng `local_directory` cho dự án này (máy khác, hoặc trước khi đồng đội đó đính), task quay về luồng worktree `github_repo` như thường. Về bản chất, thư mục cục bộ là lớp ghi đè đường worktree theo từng daemon.\n- **Hai `local_directory` trên cùng dự án.** Vì mỗi `local_directory` gắn với đúng một daemon, chuyện này chỉ xảy ra trên hai máy khác nhau (API từ chối hai cái trên cùng daemon lúc đính, xem trên). Task được định tuyến theo phân công runtime của agent, không theo daemon nào có thư mục cục bộ: task rơi vào daemon sở hữu runtime của agent nhận việc, daemon đó chọn dòng `local_directory` khớp ID mình và bỏ qua phần còn lại. Không có cân bằng tải — muốn một máy cụ thể chạy task thì phái agent gắn với runtime của máy đó.\n\nDaemon không có dòng `local_directory` cho một dự án đã gắn ở nơi khác thì **không** bị chặn — task của nó cứ đi tiếp qua các tài nguyên khác của dự án (thường là fallback `github_repo`). `local_directory` chỉ có ý nghĩa với daemon nó được gắn.\n\n### Chạy task trên thư mục cục bộ\n\nKhi task được phát trên issue mà dự án của nó có `local_directory` gắn với daemon nhận, daemon sẽ:\n\n1. Kiểm tra lại đường dẫn (quy tắc ở trên).\n2. Lấy khóa theo thư mục dựa trên đường dẫn thật đã phân giải symlink — nên hai lối vào cùng thư mục (một qua symlink, một trực tiếp) vẫn tuần tự hóa.\n3. Ghi `CLAUDE.md` / `AGENTS.md` của agent (và `.multica/project/resources.json`) **vào thư mục của bạn**. Agent làm việc tại đó, y như bạn tự mở thư mục.\n4. Giữ các artefact runtime của Multica (`output/`, `logs/`, `.gc_meta.json`) trong một envRoot riêng **bên ngoài** thư mục của bạn.\n\nNếu task thứ hai cho cùng thư mục tới khi task đầu đang chạy, nó đỗ lại với trạng thái **Waiting for local directory**. Trạng thái hiện ở mọi nơi có task — pill task trong chat, banner agent, nhật ký thực thi và chỉ báo hoạt động — và task đang đỗ được tính vào diện \"đang xếp hàng\" của agent. Hủy task đang đỗ giải phóng chỗ ngay; hủy task đang chạy cho task kế được lên.\n\nChờ không có timeout — task đỗ tới khi khóa được nhả hoặc người dùng / agent hủy nó.\n\n### Multica sẽ và sẽ không đụng gì trong thư mục của bạn\n\n- **Sẽ ghi** `CLAUDE.md` / `AGENTS.md` (hoặc tương đương theo provider của agent) và `.multica/project/resources.json` ở gốc thư mục, để agent có meta-skill và danh sách tài nguyên. Thêm chúng vào `.gitignore` nếu không muốn commit.\n- **Sẽ ghi** những sửa đổi mã mà agent quyết định làm — y hệt như bạn tự chạy agent trên máy.\n- **Không bao giờ xóa vật lý** thư mục hay bất cứ thứ gì bên trong. Dọn rác nhận biết đường dẫn: với envRoot của `local_directory`, nó chỉ dọn `output/` và `logs/` của chính nó dưới `workspacesRoot`, và coi thư mục của bạn là vùng cấm.\n\n### Giới hạn v1 (sẽ siết dần)\n\nBản đầu chủ ý sắc cạnh hơn `github_repo`. Danh sách này sẽ ngắn dần — ghi ở đây là những gì đúng hôm nay:\n\n- **Không tự chuyển nhánh.** Agent chạy trên nhánh bạn đang checkout. Chuyển nhánh trước khi phái việc nếu điều đó quan trọng.\n- **Không bảo vệ cây dở dang, không auto-commit.** Thay đổi chưa commit hiện ra với agent, có thể bị sửa tại chỗ và không được stash. Hãy coi thư mục là cây làm việc thật và commit trước những lần chạy rủi ro.\n- **Không tự mở PR.** Task kết thúc thì thay đổi nằm trên nhánh nơi chúng được tạo — không push, không mở PR. Tự push và mở PR khi bạn sẵn sàng.\n- **`waiting_local_directory` hiện trạng thái, không hiện ai đang giữ.** Huy hiệu cho biết task đang đỗ; nó không chỉ ra task nào hay đường dẫn nào đang giữ thư mục.\n\nNhững mục này được theo dõi trong phần tiếp nối agent-task-lifecycle của tính năng thư mục cục bộ; trước khi nó ra, hãy coi `local_directory` là \"agent chạy trong thư mục của bạn, theo đúng cách bạn sẽ chạy.\"\n\n## Đính repo lúc tạo dự án\n\nTrong app **Web** hoặc **Desktop**, mở *New project* giờ có pill **Repos** cạnh Trạng thái / Độ ưu tiên / Lead. Chọn các repo đã gắn không gian làm việc (hoặc dán một URL bất kỳ) sẽ đính chúng làm tài nguyên `github_repo` ngay khi dự án được tạo.\n\nTừ **CLI**:\n\n```bash\n# Tạo + đính trong một phát. Server đính tài nguyên trong cùng transaction\n# với việc tạo dự án — tài nguyên không hợp lệ sẽ rollback toàn bộ,\n# nên không bao giờ có dự án chỉ đính được một nửa.\nmultica project create \\\n  --title \"Agent UX 2026\" \\\n  --repo https://github.com/multica-ai/multica\n\n# Quản lý tài nguyên về sau\nmultica project resource list <project-id>\nmultica project resource add  <project-id> --type github_repo --url <url>\nmultica project resource add  <project-id> --type github_repo --url <url> --ref <branch-or-sha>\nmultica project resource remove <project-id> <resource-id>\n\n# Lối thoát chung cho mọi resource_type server hiểu —\n# không cần đổi CLI khi có kiểu mới:\nmultica project resource add <project-id> \\\n  --type notion_page \\\n  --ref '{\"page_id\":\"…\",\"title\":\"…\"}'\n```\n\n`--repo` lặp lại được; mỗi giá trị được đính thành một tài nguyên `github_repo` riêng.\n\n## Agent thấy gì lúc chạy\n\nKhi daemon khởi tạo agent cho issue trong một dự án, hai điều xảy ra:\n\n### 1. `.multica/project/resources.json`\n\nBản chuyển tiếp có cấu trúc của phản hồi API, ghi vào thư mục làm việc của agent:\n\n```json\n{\n  \"project_id\": \"…\",\n  \"project_title\": \"Agent UX 2026\",\n  \"resources\": [\n    {\n      \"id\": \"…\",\n      \"resource_type\": \"github_repo\",\n      \"resource_ref\": {\n        \"url\": \"https://github.com/multica-ai/multica\",\n        \"default_branch_hint\": \"main\"\n      }\n    }\n  ]\n}\n```\n\nSkill, script phụ trợ, hoặc chính agent có thể parse file này khi cần tập tài nguyên *chính xác* của lần chạy.\n\n### 2. Mục \"Project Context\" trong prompt meta-skill\n\n`CLAUDE.md` / `AGENTS.md` của agent (tùy provider) giờ có phần tóm tắt dễ đọc:\n\n```\n## Project Context\n\nThis issue belongs to **Agent UX 2026**.\n\nProject resources (also written to `.multica/project/resources.json`):\n\n- **GitHub repo**: https://github.com/multica-ai/multica (default branch: `main`)\n\nResources are pointers — open them only when relevant to the task. For\n`github_repo` resources, use `multica repo checkout <url>` to fetch the code.\n```\n\nĐoạn chữ cố tình tối giản. Payload đầy đủ nằm trên đĩa; prompt chỉ định hướng để agent biết dự án tồn tại và có gì đính kèm.\n\n### Khi lỗi\n\nLấy tài nguyên là **best-effort**. Nếu gọi API thất bại, phần dự án bị bỏ khỏi prompt và file không được ghi, nhưng task vẫn chạy. Agent không bao giờ bị chặn vì thiếu ngữ cảnh dự án.\n\n## Thêm kiểu tài nguyên mới\n\nToàn bộ ý nghĩa của lớp trừu tượng này là kiểu mới rất rẻ. Đường đi đầy đủ:\n\n1. **Validator phía server** (`server/internal/handler/project_resource.go`) — thêm một case trong `validateAndNormalizeResourceRef` để parse và chuẩn hóa payload mới.\n2. **Formatter meta-skill của daemon** (`server/internal/daemon/execenv/runtime_config.go`) — thêm case trong `formatProjectResource` để prompt của agent hiển thị kiểu mới thành gạch đầu dòng dễ đọc.\n3. **Kiểu TypeScript** (`packages/core/types/project.ts`) — mở rộng `ProjectResourceType` và thêm interface payload.\n4. **Renderer UI** (`packages/views/projects/components/project-resources-section.tsx`) — thêm case trong `ResourceRow` cho kiểu mới.\n\n**Không** migration schema, không truy vấn sqlc mới, không endpoint mới, **và không đổi CLI** — cờ chung `--ref '<json>'` của CLI nhận mọi payload mà validator hiểu, nên hỗ trợ ngày-một cho kiểu mới chỉ là bốn bước trên. (Bạn *có thể* thêm lối tắt CLI theo kiểu về sau; không bắt buộc.)\n\nCùng bảng `project_resource` và cùng ba lệnh CRUD phục vụ mọi kiểu.\n\n## Repo của không gian làm việc vs. repo của dự án\n\nDanh sách repo hiện cho agent (khối `## Repositories` trong `CLAUDE.md` / `AGENTS.md`) do trình xử lý nhận task của daemon chọn theo thứ tự ưu tiên:\n\n- **Dự án có ít nhất một tài nguyên `github_repo`** → chỉ các repo đó được đưa cho agent. Repo gắn không gian làm việc bị chủ ý ẩn để agent không phải đoán repo nào thuộc issue này.\n- **Dự án không có tài nguyên `github_repo` (hoặc issue không thuộc dự án nào)** → quay về danh sách repo của không gian làm việc như trước.\n\nĐiều này giữ tập làm việc của agent gọn: khi dự án đã nói rõ repo của nó, đó là câu trả lời có thẩm quyền. Danh sách tài nguyên có cấu trúc tại `.multica/project/resources.json` luôn mang tập đầy đủ, nên skill nào muốn soi tất cả vẫn được.\n\nDaemon phản chiếu điều này ở phía checkout: khi task tới với các URL `github_repo` thuộc phạm vi dự án, các URL đó được gộp vào allowlist theo không gian làm việc *và* đồng bộ vào cache repo cục bộ trước khi agent khởi chạy. Nên một URL repo dự án chưa gắn ở tầng không gian làm việc vẫn là đối số hợp lệ cho `multica repo checkout` — daemon không từ chối kiểu \"chưa cấu hình\". Nếu tài nguyên dự án có `ref`, ref đó thành mặc định cho `multica repo checkout <url>` trong task đó; truyền `--ref` cho lệnh checkout sẽ ghi đè. Việc tách allowlist là nội bộ: URL gắn không gian làm việc và URL theo task được theo dõi riêng, nên một lần refresh repo không gian làm việc không vô tình thu hồi URL dự án giữa chừng.\n\n## Những gì chủ ý **không** nằm trong phạm vi\n\n- **Chia sẻ chéo dự án.** Hôm nay mỗi tài nguyên thuộc đúng một dự án.\n- **Phạm vi tài nguyên theo skill.** Mọi tài nguyên hiện ra với mọi skill trong lần chạy của agent; lọc theo kiểu là việc tiếp nối.\n- **Cache / đồng bộ.** `github_repo` chỉ là metadata — checkout vẫn qua `multica repo checkout` khi cần. Văn bản tài liệu cache cho Notion / Google Docs sẽ tới cùng các kiểu đó.\n\nĐây là những thiếu sót có chủ đích — mục tiêu của bản đầu là kiểm chứng lớp trừu tượng với ít bộ phận chuyển động nhất."
        }
      ]
    },
    {
      "label": "Agents",
      "labelKey": "agents",
      "pages": [
        {
          "slug": "agents",
          "title": "Agent",
          "content": "Agent là **thành viên hạng nhất** của một [không gian làm việc](/workspaces) Multica — như con người, nó có thể được [giao issue](/assigning-issues), lên tiếng trong [bình luận](/comments), được [`@`nhắc](/mentioning-agents) và làm lead một [dự án](/projects). Khác biệt cốt lõi: đứng sau mỗi agent là một [công cụ lập trình AI](/providers) chạy trên máy của bạn. Giao việc cho nó và nó **tự bắt đầu trong vài giây** — không cần thúc, không offline, sẵn sàng 24/7.\n\n## Agent làm được gì\n\nAgent dùng cùng bề mặt \"thành viên\" như con người, và giao diện gần như không phân biệt:\n\n- **[Được giao issue](/assigning-issues)** — sau khi được đặt làm người phụ trách, nó tự bắt đầu làm\n- **[Được `@`nhắc](/mentioning-agents)** — viết `@agent-name` trong bình luận và nó thức dậy đọc bình luận đó\n- **Đăng [bình luận](/comments)** — nó báo tiến độ và trả lời mọi người dưới issue\n- **Làm lead một [dự án](/projects)** — đặt được làm lead dự án, như con người\n- **Tự mở [issue](/issues)** — trong lúc chạy task, nếu phát hiện vấn đề liên quan, nó tạo được issue mới trực tiếp\n\nTừ góc nhìn cộng tác, agent chỉ là một thành viên của không gian làm việc — tên nó nằm chung danh sách thành viên với con người, thường kèm biểu tượng robot nhỏ phía trước.\n\n## Khác con người ở đâu\n\nVài khác biệt then chốt chỉ lộ ra khi bạn thực sự dùng agent:\n\n- **Nó tự bắt đầu** — sau khi bạn giao issue hoặc `@` nó, Multica phát task tới runtime của nó ngay. Khác con người, nó không chờ thấy tin nhắn rồi mới phản hồi. Chi tiết kích hoạt xem [Giao issue cho agent](/assigning-issues) và [@nhắc agent trong bình luận](/mentioning-agents).\n- **Nó không nhận thông báo** — agent không bao giờ xuất hiện ở đầu kia [hộp thư đến](/inbox) của bạn, và không nằm trong nhóm nhận `@all`. Nó không phải \"người nhận đọc tin nhắn\" — nó là \"đơn vị công việc được kích hoạt để thực thi task.\"\n- **Nó gắn với một công cụ lập trình AI** — mỗi agent buộc vào một runtime (runtime = daemon × một công cụ lập trình AI; xem [Daemon và runtime](/daemon-runtimes)). Công cụ offline thì agent không làm được; task mới chờ tới khi runtime trở lại.\n- **Nó lưu trữ được** — lưu trữ agent không dùng nữa và nó biến khỏi các khung nhìn thường nhật; khôi phục lúc nào cũng được. Lưu trữ sẽ hủy các task đang chạy.\n\n## Ai giao được việc cho agent\n\nKhi tạo agent, bạn chọn một **phạm vi hiển thị** quyết định ai giao được nó vào issue hoặc đặt làm lead dự án:\n\n- **Workspace** — thành viên nào của không gian làm việc cũng giao được\n- **Private** — chỉ owner, admin, hoặc người tạo agent giao được\n\nAgent mới mặc định là **private**. Muốn cả không gian làm việc dùng được, đặt phạm vi hiển thị là `workspace` lúc tạo, hoặc đổi sau trong cấu hình agent. Ma trận quyền đầy đủ xem [Thành viên và vai trò](/members-roles).\n\n> **Private nghĩa là \"hạn chế ai được giao việc,\" không phải \"ẩn với người khác.\"** Mọi thành viên của không gian làm việc vẫn thấy tên và mô tả của agent private trong danh sách agent — họ chỉ không xem được chi tiết cấu hình (biến môi trường tùy chỉnh, cấu hình MCP và các trường nhạy cảm khác bị che). Nếu bạn cần \"chỉ một người thấy\" thì hiện chưa làm được.\n\n## Bước tiếp theo\n\n- [Tạo và cấu hình agent](/agents-create) — cách dựng một agent\n- [Skills](/skills) — gắn gói tri thức cho agent\n- [Đội](/squads) — gom agent dưới một leader để đúng agent nhận đúng issue\n- [Daemon và runtime](/daemon-runtimes) — agent cần gì để thực sự chạy"
        },
        {
          "slug": "agents-create",
          "title": "Tạo và cấu hình agent",
          "content": "Tạo một [agent](/agents) chỉ cần hai thứ: **một cái tên** và **một [công cụ lập trình AI](/providers)**. Còn lại đều tùy chọn — chỉ dẫn hệ thống, model, biến môi trường, đối số CLI, phạm vi hiển thị, giới hạn đồng thời — mặc định là dùng tốt. Cho chạy trước, tinh chỉnh sau; trường nào cũng đổi được bất cứ lúc nào.\n\n## Tạo agent\n\nĐiều kiện tiên quyết: bạn đã cài ít nhất một [công cụ lập trình AI](/providers) được hỗ trợ trên máy (Claude Code, Codex, v.v.) và một [daemon](/daemon-runtimes) đang chạy. Nếu chưa tới bước đó, bắt đầu với [Bắt đầu nhanh với Cloud](/cloud-quickstart) hoặc [Bắt đầu nhanh tự vận hành](/self-host-quickstart).\n\nXong xuôi, vào trang **Agent** trong không gian làm việc và bấm **+ New**, hoặc dùng CLI:\n\n```bash\nmultica agent create\n```\n\nBiểu mẫu chỉ có hai trường bắt buộc: **tên** (duy nhất trong không gian làm việc) và **runtime** (= chọn một công cụ lập trình AI). Các trường còn lại được nói từng mục bên dưới.\n\n## Chọn công cụ lập trình AI\n\nMỗi runtime được chống lưng bởi một công cụ lập trình AI cụ thể. Multica hỗ trợ 14 công cụ. Các lựa chọn phổ biến nhất:\n\n| Công cụ | Hợp với |\n|---|---|\n| **Claude Code** | Công cụ chính thức của Anthropic, bộ tính năng đầy đủ nhất; **lựa chọn đầu tiên tốt nhất** |\n| **Codex** | OpenAI, phương án chủ lưu thay thế |\n| **Cursor** | Người dùng hệ sinh thái editor Cursor |\n| **Copilot** | Đội tận dụng quyền lợi tài khoản GitHub |\n| **CodeBuddy** | Đội dùng CLI tương thích Claude Code của Tencent |\n\nChín công cụ còn lại (Antigravity, Hermes, Kimi, Kiro CLI, OpenCode, Pi, OpenClaw, Qoder, Trae CLI), cùng ma trận năng lực đầy đủ của từng công cụ (khôi phục phiên, MCP, đường chèn skill, chọn model), xem tại [So sánh công cụ lập trình AI](/providers).\n\n## Viết chỉ dẫn hệ thống\n\n**Chỉ dẫn hệ thống** (`instructions`) được gắn trước mỗi task, cho agent biết nó đóng vai gì và tuân thủ quy tắc nào:\n\n```text\nYou're a frontend code-review agent. When an issue comes in, read the diff first. Focus only on:\n- Styling issues (tailwind class names, box model)\n- Accessibility (a11y)\nDon't change code — leave suggestions in a comment.\n```\n\nĐể trống (mặc định), agent dùng hành vi gốc của công cụ lập trình AI bên dưới, không ràng buộc thêm.\n\n## Chọn model\n\nĐa số công cụ lập trình AI hỗ trợ chọn model (ví dụ Claude Code cho chọn giữa Sonnet và Opus). Để trống thì dùng mặc định của công cụ; chọn tường minh thì chạy đúng cái đó. Model được hỗ trợ của từng công cụ liệt kê tại [So sánh công cụ lập trình AI](/providers).\n\nĐổi model **chỉ áp dụng cho task mới**. Task đã dispatched tiếp tục với model đã chốt lúc dispatch.\n\n## Biến môi trường tùy chỉnh (custom_env)\n\n**Biến môi trường tùy chỉnh** (`custom_env`) cho phép tiêm thêm biến môi trường lúc thực thi task — cách dùng điển hình là API key hoặc chuyển endpoint upstream:\n\n```\nANTHROPIC_API_KEY = sk-...\nANTHROPIC_BASE_URL = https://my-proxy.example.com\n```\n\nCác biến trọng yếu của hệ thống không ghi đè được: `PATH`, `HOME`, `USER`, `SHELL`, `TERM`, `CODEX_HOME` và mọi khóa bắt đầu bằng `MULTICA_*` bị daemon lặng lẽ bỏ qua (có log cảnh báo — không báo lỗi).\n\n> **Giá trị trong `custom_env` được lưu dạng plaintext trong cơ sở dữ liệu server của Multica.** Phản hồi list/get agent không còn mang giá trị env — chỉ một con số đếm. Đọc giá trị đòi hỏi owner hoặc admin của không gian làm việc gọi endpoint chuyên biệt có kiểm toán `GET /api/agents/{id}/env` (CLI: `multica agent env get <id>`). Agent đang chạy task KHÔNG dùng được thông tin đăng nhập owner của máy chủ nó để lộ env của agent khác — endpoint từ chối phiên agent-actor.\n>\n> **Đừng đặt bí mật giá trị cao vào `custom_env`** (mật khẩu cơ sở dữ liệu production, token quyền root, v.v.). Dùng **thông tin đăng nhập chuyên biệt, phạm vi hẹp** cho agent (API key chỉ đọc, PAT một phạm vi), và xoay vòng đều đặn. Bản sao lưu và kiểm toán DB vẫn là bề mặt lộ đáng kể.\n\n## Đối số CLI tùy chỉnh (custom_args)\n\n**Đối số CLI tùy chỉnh** (`custom_args`) là một mảng chuỗi nối lần lượt vào dòng lệnh của công cụ lập trình AI:\n\n```json\n[\"--max-turns\", \"100\", \"--append-system-prompt\", \"always respond in Chinese\"]\n```\n\nLệnh cuối cùng thành ra:\n\n```bash\nclaude --model <model> --max-turns 100 --append-system-prompt \"always respond in Chinese\" [...]\n```\n\nĐối số được truyền nguyên vẹn, không qua shell (không rủi ro injection), nhưng cờ nào được nhận ra là chuyện của chính công cụ lập trình AI — các công cụ khác nhau đáng kể ở điểm này.\n\n> `custom_env` và `custom_args` không có trần cứng, nhưng thực tế **giữ mỗi cái dưới 10 mục**. Nhiều quá làm dòng lệnh dài, khởi động chậm và khó bảo trì.\n\n## Phạm vi hiển thị\n\n- **Workspace** (`workspace`) — thành viên nào của không gian làm việc cũng giao được\n- **Private** (`private`) — chỉ owner, admin, hoặc người tạo agent giao được\n\nAgent mới mặc định là `private`.\n\n**Private không có nghĩa là ẩn** — thành viên nào cũng thấy tên và mô tả của agent private trong danh sách, họ chỉ không đọc được cấu hình nhạy cảm (giá trị env không bao giờ xuất hiện trong phản hồi list/get; cấu hình MCP bị che với người không phải owner). Ý nghĩa đầy đủ tại [Agent → Ai giao được việc cho agent](/agents#who-can-assign-an-agent).\n\n## Giới hạn đồng thời\n\n**Giới hạn đồng thời** (`max_concurrent_tasks`) kiểm soát agent này chạy song song được bao nhiêu task một lúc. Mặc định là **6**. Task mới chạm trần thì xếp hàng — không bị từ chối.\n\nĐây mới là \"tầng agent\" của giới hạn hai tầng — bản thân daemon có trần rộng hơn (mặc định 20), và cái chặt hơn thắng. Chi tiết tại [Daemon và runtime → Bao nhiêu task chạy song song được](/daemon-runtimes#how-many-tasks-can-run-in-parallel).\n\nĐổi giá trị này **không hủy task đang chạy** — chỉ áp dụng cho task sắp được nhận kế tiếp.\n\n## Gắn chuyên môn: Skills\n\nAgent tạo xong có thể gắn **Skills** — các **gói tri thức** (`SKILL.md` + file hỗ trợ) tự động chuyển tới công cụ lập trình AI lúc thực thi task. Bạn có thể tạo skill mới, nhập từ GitHub hoặc ClawHub, hoặc quét từ một thư mục skill sẵn có trên máy. Xem [Skills](/skills).\n\n## Lưu trữ và khôi phục\n\nAgent không dùng nữa có thể **lưu trữ** — chúng biến khỏi các khung nhìn thường nhật, nhưng dữ liệu lịch sử (task đã chạy, bình luận đã đăng) được giữ nguyên vẹn. **Khôi phục** bất cứ lúc nào để đưa chúng trở lại làm việc.\n\n> **Lưu trữ lập tức hủy mọi task chưa xong của agent** — task đang chạy, đã dispatched và đang xếp hàng đều bị đánh dấu `cancelled` và không tiếp tục. Có task quan trọng đang chạy thì để nó xong rồi hãy lưu trữ.\n\nAgent đã lưu trữ không được giao task mới.\n\n## Bước tiếp theo\n\n- [Skills](/skills) — gắn gói tri thức cho agent\n- [So sánh công cụ lập trình AI](/providers) — ma trận năng lực đầy đủ của cả 14 công cụ\n- [Giao issue cho agent](/assigning-issues) — đưa agent mới của bạn vào việc"
        },
        {
          "slug": "skills",
          "title": "Skills",
          "content": "Skill là một **gói tri thức** cho [agent](/agents) — một `SKILL.md` cùng các file hỗ trợ tùy chọn (script, cấu hình, mẫu tham chiếu) nói với agent \"gặp loại task này thì tư duy và hành động thế này.\" Multica theo chuẩn mở [Anthropic Agent Skills](https://agentskills.io), nên bất kỳ Skill nào tuân chuẩn — từ kho chính thức của Anthropic, ClawHub, hay skills.sh — đều nhập thẳng được.\n\n## Skill của không gian làm việc vs. skill cục bộ\n\nMultica hỗ trợ hai nguồn skill:\n\n- **Skill của không gian làm việc** — lưu trên cloud của Multica. Sau khi gắn vào agent, nó được đồng bộ xuống daemon của bạn lúc thực thi task. Đây là **cách chuẩn để chia sẻ skill trong nhóm**.\n- **Skill cục bộ** — nằm trong một thư mục trên máy bạn. Theo yêu cầu của bạn, [daemon](/daemon-runtimes) quét máy và bạn tự chọn cái nào đưa vào không gian làm việc. Daemon kiểm **hai gốc, theo thứ tự ưu tiên**: trước là thư mục skill riêng của runtime (mỗi công cụ lập trình AI có đường dẫn mặc định quy ước, ví dụ `~/.claude/skills/` của Claude Code), sau là thư mục vạn năng liên công cụ `~/.agents/skills/` — vị trí chung mà các hệ sinh thái như Codex và Gemini CLI dùng. Khi cùng tên skill có ở cả hai, **thư mục riêng của provider thắng**, nên gốc vạn năng chỉ bổ sung skill *thêm* chứ không đổi cách một skill sẵn có được phân giải.\n\nĐa phần bạn sẽ muốn **skill của không gian làm việc**: nhập một lần, agent của mọi đồng đội dùng được. Skill cục bộ hợp khi bạn muốn thử trên máy trước, hoặc nội dung liên quan tài liệu cục bộ nhạy cảm.\n\n## Skill theo phạm vi repository\n\nSkill trong phạm vi repo là hành vi được kỳ vọng. Một số công cụ lập trình AI khám phá **skill mức dự án commit ngay trong repository**, như `.claude/skills/`, `.cursor/skills/`, `.opencode/skills/` hay `.agents/skills/`. Khi một task của Multica checkout repository đó, các file này vẫn nằm trong workdir và công cụ bên dưới nạp được qua quy tắc khám phá gốc của nó. Bạn **không** cần nhập các skill repo đó vào Multica chỉ để dùng trong repo đó; Multica **không** tự nhập chúng vào sổ skill của không gian làm việc.\n\nSkill của không gian làm việc vẫn đồng bộ vào đúng vị trí native của provider với các công cụ hỗ trợ khám phá mức dự án. Nếu một skill không gian làm việc đụng độ với thư mục skill sẵn có của repo, Multica ghi bản của không gian làm việc sang tên anh em như `review-helper-multica` thay vì ghi đè file của repo. Công cụ khi đó có thể thấy cả hai skill, bản của không gian làm việc mang tên đã điều chỉnh.\n\n## Nhập skill\n\nSkill của không gian làm việc tới từ bốn nguồn:\n\n- **Tạo mới** — viết `SKILL.md` và các file liên quan ngay trong UI\n- **Từ GitHub** — dán URL repo (ví dụ `https://github.com/owner/repo/tree/main/skills/my-skill`) và Multica kéo `SKILL.md` cùng mọi file trong thư mục đó\n- **Từ ClawHub** — tìm và nhập từ chợ công khai [ClawHub](https://clawhub.io), có chọn phiên bản\n- **Từ máy** — daemon quét các thư mục skill trên máy bạn, và bạn chọn cái nào đưa vào không gian làm việc\n\nCả file lẻ lẫn nguyên gói skill đều có trần dung lượng (trần một file khoảng 1 MB khi nhập từ GitHub). Quy tắc chính xác hiện trong hộp thoại nhập — vượt trần sẽ báo lỗi.\n\n## Gắn vào agent\n\nNhập xong, skill phải được **gắn vào một agent cụ thể** mới có hiệu lực. Một agent gắn được nhiều skill, và một skill gắn được nhiều agent.\n\nSau khi gắn, agent nhận skill ở lần bắt đầu task kế tiếp — mỗi công cụ lập trình AI có đường khám phá skill riêng (Claude Code dùng `.claude/skills/`, Cursor dùng `.cursor/skills/`, Antigravity dùng `.agents/skills/`, v.v.), và Multica tự thả file vào đúng chỗ. **Tuy nhiên, ba công cụ (Gemini, Hermes, OpenClaw) hiện dùng đường dự phòng chung `.agent_context/skills/` — các công cụ này có thật sự đọc skill từ đường đó không là tùy công cụ.** Bản đồ đường dẫn đầy đủ và phân biệt khám-phá-native vs. dự phòng nằm ở [So sánh công cụ lập trình AI → File skill đi đâu](/providers#where-skill-files-go).\n\nSau khi bạn sửa nội dung skill, **chỉ task tạo mới nhận bản mới** — task đang chạy tiếp tục với skill cũ.\n\n## An toàn với skill bên thứ ba\n\nSkill nhập từ GitHub hay ClawHub có thể chứa script và nội dung thực thi được. Bản thân Multica **không ký, không kiểm toán, không sandbox chúng** — nội dung skill được trao nguyên vẹn cho công cụ lập trình AI tương ứng, còn công cụ có coi chúng là thực thi được hay không là chuyện của công cụ.\n\n> **Trước khi nhập skill bên thứ ba, hãy review `SKILL.md` và từng file đi kèm.**\n>\n> Tháng 2/2026, sự cố \"ClawHavoc\" — chỉ dẫn độc hại cài trong một gói skill phổ biến đã đánh cắp API key của người dùng bị ảnh hưởng. ClawHub từ đó đã thêm quét VirusTotal, nhưng **quét tự động không thay được việc bạn tự review**.\n>\n> **Chỉ nhập từ nguồn bạn tin.** Với dự án có dữ liệu nhạy cảm, cân nhắc chỉ dùng skill cục bộ do chính bạn viết.\n\n## Skill vs. MCP\n\nCả hai đều mở rộng khả năng của agent, nhưng theo hai hướng khác nhau:\n\n- **Skill** = một **gói tri thức** có cấu trúc (nội dung tĩnh + chỉ dẫn). Agent đọc skill để học \"gặp vấn đề X thì tư duy thế nào, làm gì.\"\n- **MCP** (Model Context Protocol) = một **kênh công cụ**. Agent dùng MCP để kết nối dịch vụ ngoài (cơ sở dữ liệu, hệ thống file, API bên thứ ba) và **gọi** chúng.\n\nHai thứ bổ trợ nhau. Trong Multica hiện tại, hỗ trợ MCP là **theo từng provider**: Claude Code, Codex, Cursor, Hermes, Kimi, Kiro CLI, OpenCode và OpenClaw dùng `mcp_config`; các công cụ khác nhận trường này nhưng không thực dùng. Mục MCP chuyên biệt sẽ tới trong bản phát hành sau.\n\n---\n\nTới đây bạn đã biết agent là gì, tạo ra sao, gắn skill thế nào. Câu hỏi kế tiếp: **nó thực sự chạy ở đâu, và vì sao agent của tôi thỉnh thoảng bị kẹt?** Chương tiếp theo nói về kiến trúc thực thi — daemon, runtime và cách task vận hành cùng nhau.\n\n## Bước tiếp theo\n\n- [Daemon và runtime](/daemon-runtimes) — agent thực chạy ở đâu, và phân biệt online với offline\n- [Thực thi task](/tasks) — vòng đời đầy đủ của một \"phiên làm việc agent\"\n- [So sánh công cụ lập trình AI](/providers) — so sánh đầy đủ 14 công cụ (kèm đường chèn skill của từng cái)"
        },
        {
          "slug": "squads",
          "title": "Đội",
          "content": "Đội (squad) là một **nhóm có tên gồm các [agent](/agents) và [thành viên](/members-roles) con người**, với một **agent leader** được chỉ định. Bản thân đội là một người phụ trách hạng nhất: chọn nó từ bất kỳ bộ chọn **Người phụ trách** nào và leader nhận kích hoạt, đọc issue, rồi `@`nhắc thành viên đội phù hợp nhất để làm việc. Đội cho phép bạn tập hợp chuyên gia một lần và phái việc **theo chủ đề thay vì theo tên** — nhóm lớn dần, cách định tuyến giữ nguyên.\n\n## Đội là gì, về mặt cơ chế\n\n- **Một leader, nhiều thành viên.** Leader phải là agent; thành viên có thể là agent hoặc con người. Đội chỉ có mỗi leader vẫn hợp lệ (phần brief của leader ghi \"không có thành viên khác\"), và cùng một agent có thể ở nhiều đội.\n- **Giao được ở mọi nơi giao được cho người.** Đội xuất hiện trong bộ chọn Người phụ trách, bộ chọn @nhắc và modal tạo nhanh — chỗ nào chọn được agent hay thành viên là chọn được đội.\n- **Xóa mềm qua lưu trữ.** Lưu trữ đội và nó biến khỏi bộ chọn lẫn danh sách; issue nào đang giao cho nó được **chuyển sang agent leader** để công việc không rơi vào im lặng. Đội đã lưu trữ không được giao issue mới.\n\n## Khi nào dùng đội, khi nào dùng agent đơn\n\n| Chọn đội khi… | Chọn agent đơn khi… |\n|---|---|\n| Bạn có nhiều chuyên gia và không biết trước ai hợp với issue này | Công việc gọn trong một chuyên môn và bạn biết ai nên làm |\n| Bạn muốn một người phụ trách ổn định (đội) trong khi người thực trả lời đổi theo issue | Bạn muốn tên agent nằm trên issue và trách nhiệm cá nhân rõ ràng |\n| Bạn muốn một đích định tuyến kiểu `@FrontendTeam` trong bình luận | `@agent-name` một-một là đủ |\n\nĐội không thêm năng lực — nó thêm **định tuyến**. Thành viên vẫn là agent bình thường; việc duy nhất của leader là chọn đúng người.\n\n## Quyền hạn\n\n| Thao tác | Ai làm được |\n|---|---|\n| Tạo / cập nhật / lưu trữ đội | **Owner** hoặc **admin** của không gian làm việc |\n| Thêm hoặc gỡ thành viên, đổi vai trò | **Owner** hoặc **admin** của không gian làm việc |\n| Giao issue cho đội | Thành viên bất kỳ (như giao cho agent) |\n| `@`nhắc đội trong bình luận | Thành viên bất kỳ |\n| Ghi đánh giá của leader | Chỉ agent leader của đội (qua CLI) |\n\nMa trận vai trò đầy đủ tại [Thành viên và vai trò](/members-roles).\n\n## Tạo đội\n\nTrên thanh bên, mở **Đội → New squad** và điền:\n\n- **Tên** — ví dụ `Frontend Team`, `Bug Triage`. Không cần duy nhất trong không gian làm việc.\n- **Mô tả** (tùy chọn) — đoạn ngắn hiện trên thẻ đội và trang chi tiết.\n- **Leader** — chọn một agent sẵn có. Leader tự được thêm vào đội với vai trò `leader`.\n\nTạo xong, mở trang chi tiết đội để:\n\n- **Thêm thành viên** — chọn agent hoặc thành viên con người, tùy chọn ghi mô tả vai trò ngắn cho từng người (ví dụ \"phụ trách migration\", \"reviewer chốt chặn\"). Leader dùng các vai trò này khi quyết định ủy thác cho ai.\n- **Viết chỉ dẫn** — hướng dẫn mức đội mà leader thấy ở mỗi lượt chạy (nói thêm bên dưới).\n- **Đặt avatar** — chọn từ cùng bộ chọn dùng cho agent.\n\nCLI tương đương:\n\n```bash\nmultica squad create --name \"Frontend Team\" --leader frontend-lead-agent\nmultica squad member add <squad-id> --member-id <agent-or-user-uuid> --type agent --role \"Owns Tailwind / shadcn surface\"\n```\n\n## Issue giao cho đội chạy thế nào\n\nKhi một issue không ở Backlog được giao cho đội, Multica lập tức xếp hàng một `task` cho **agent leader** (không phải cho mọi thành viên). Luồng tiếp theo:\n\n1. **Leader nhận task.** Runtime của agent nhận task ở lần poll kế tiếp, như mọi lần giao cho agent.\n2. **Leader được brief.** Lúc nhận, Multica nối ba khối vào system prompt của leader — xem [Leader thấy gì mỗi lượt](#what-the-leader-sees-on-every-turn) bên dưới.\n3. **Leader đăng một bình luận ủy thác.** Bình luận `@`nhắc thành viên được chọn bằng đúng markdown mention từ danh sách đội — mention đó kích hoạt một `task` mới cho từng agent được nhắc.\n4. **Leader ghi đánh giá** qua `multica squad activity <issue-id> action --reason \"...\"`. Lệnh này ghi một mục vào dòng thời gian hoạt động của issue để con người thấy leader đã thực sự đánh giá lượt kích hoạt.\n5. **Leader dừng.** Leader không tự triển khai. Khi thành viên được ủy thác báo lại, leader được kích hoạt lại để đọc cập nhật và hoặc ủy thác bước kế, hoặc leo thang, hoặc im lặng.\n\nNếu issue ở **Backlog**, leader không được kích hoạt — Backlog là bãi đỗ, cùng quy tắc với giao trực tiếp cho agent.\n\n### Leader thấy gì mỗi lượt\n\nỞ mỗi lượt chạy của leader đội, ba khối được nối vào chỉ dẫn của leader:\n\n- **Squad Operating Protocol** — bộ quy tắc cứng: đọc issue, ủy thác bằng `@`nhắc, viết gọn (đừng kể lại thân issue — người nhận tự đọc được), ghi đánh giá mỗi lượt, và **dừng sau khi phái việc**. Giao thức này do hệ thống quản lý, không sửa được.\n- **Squad Roster** — dòng của chính leader cùng một dòng cho mỗi thành viên chưa lưu trữ. Mỗi dòng mang đúng markdown mention (`[@Name](mention://agent/<uuid>)` hoặc `[@Name](mention://member/<uuid>)`) mà leader nên dán — gõ trần `@name` không kích hoạt ai cả.\n- **Squad Instructions** — hướng dẫn tùy chỉnh của bạn cho đội này (đặt trên trang chi tiết đội hoặc qua `multica squad update --instructions`). Dùng cho quy tắc định tuyến (\"việc DB gửi Alice, frontend gửi Bob\"), chính sách leo thang, hay bất cứ gì leader cần biết mà issue chưa nói.\n\n## Khi nào leader được kích hoạt lại\n\nSau lần phái việc đầu, leader được đánh thức tự động bởi **đa số bình luận tiếp theo** trên issue. Quy tắc chính xác:\n\n| Sự kiện | Leader có kích hoạt? |\n|---|---|\n| Người ngoài đội (người báo cáo, agent ngoài) đăng bình luận | **Có** |\n| Thành viên đội đăng cập nhật tiến độ không có `@mention` | **Có** — leader đánh giá lại xem có cần bước kế không |\n| Ai đó đăng bình luận `@`nhắc tường minh một agent / thành viên / đội khác / `@all` | **Không** — `@` tường minh chính là tín hiệu định tuyến; leader tránh đường |\n| Bình luận của chính leader (tự kích hoạt) | **Không** — được chặn để tránh vòng lặp |\n| Bình luận chỉ chứa tham chiếu chéo issue (`[MUL-123](mention://issue/...)`) | **Có** — tham chiếu issue không phải định tuyến |\n\nKhử trùng lặp áp lên trên các quy tắc này: nếu leader đã có `task` ở `queued` hoặc `dispatched` trên issue này, kích hoạt mới không xếp hàng bản trùng.\n\n> **Vì sao leader không kích hoạt khi thành viên đăng bình luận có `@`nhắc.** Khi một thành viên đội trực tiếp `@` ai đó, bình luận đó là một cú bàn giao có chủ đích — đánh thức leader để \"quan sát\" việc định tuyến chỉ sinh một lượt vô nghĩa và làm rối dòng thời gian. Bình luận do agent viết là ngoại lệ: khi một agent đăng kết quả có `@` agent khác, leader vẫn thức dậy để điều phối luồng.\n\n## `@`nhắc đội trong bình luận\n\nĐội xuất hiện trong bộ chọn `@` cạnh thành viên và agent. Nhắc một đội sẽ chèn `[@SquadName](mention://squad/<uuid>)` và kích hoạt **leader của đội** như thể bạn giao issue cho đội — mà không đổi người phụ trách hay trạng thái. Dùng khi muốn đội cử người cho một câu hỏi hay việc phụ trong khi giữ nguyên người đang phụ trách.\n\nCùng bộ quy tắc chống vòng lặp: leader bỏ qua chính nó, và một `@`nhắc thành viên tường minh trong cùng bình luận sẽ định tuyến thẳng tới thành viên đó.\n\n## Giao lại hoặc lưu trữ đội\n\n**Giao issue rời khỏi đội** hành xử như mọi lần đổi người phụ trách: mọi task đang hoạt động của issue (kể cả của leader) bị hủy, và người phụ trách mới — agent, thành viên, hay đội khác — được xếp hàng. Không có thao tác \"gỡ đội mà không đổi người phụ trách\"; hãy chọn người phụ trách khác.\n\n**Lưu trữ đội** (`multica squad delete <id>`, hoặc nút Archive trên trang chi tiết):\n\n1. **Chuyển các issue đang giao cho đội sang agent leader**, để công việc tiếp tục với một agent cụ thể thay vì rơi vào im lặng.\n2. Đánh dấu đội bằng `archived_at` / `archived_by` — dòng được giữ để các mục hoạt động lịch sử vẫn phân giải được, nhưng đội biến khỏi danh sách, bộ chọn và dropdown @nhắc.\n3. **Từ chối các lần giao sau** cho đội này với `cannot assign to an archived squad`.\n\nHiện chưa có lệnh bỏ lưu trữ; cần định tuyến trở lại thì tạo đội mới.\n\n## Thao tác đội từ CLI\n\n| Lệnh | Mục đích |\n|---|---|\n| `multica squad list` | Liệt kê đội trong không gian làm việc |\n| `multica squad get <id>` | Xem tên, leader, mô tả, chỉ dẫn của một đội |\n| `multica squad create --name \"...\" --leader <agent>` | Tạo đội (owner / admin) |\n| `multica squad update <id> [--name X] [--description X] [--instructions X] [--leader Y] [--avatar-url Z]` | Cập nhật một hoặc nhiều trường |\n| `multica squad delete <id>` | Lưu trữ (xóa mềm) — chuyển issue đã giao sang leader |\n| `multica squad member list <id>` | Liệt kê thành viên đội |\n| `multica squad member add <id> --member-id <uuid> --type agent\\|member [--role \"...\"]` | Thêm thành viên (owner / admin) |\n| `multica squad member set-role <id> --member-id <uuid> --member-type agent\\|member --role \"...\"` | Đổi vai trò thành viên mà không gỡ |\n| `multica squad member remove <id> --member-id <uuid> --type agent\\|member` | Gỡ thành viên (leader không gỡ được — đổi leader trước) |\n| `multica squad activity <issue-id> <action\\|no_action\\|failed> --reason \"...\"` | Do agent leader ghi ở cuối mỗi lượt |\n\n`--leader` nhận tên agent hoặc UUID; các mục còn lại lấy ID từ `multica agent list --output json`, `multica workspace member list --output json` và `multica squad list --output json`.\n\n## Tiếp theo\n\n- [Giao issue cho agent](/assigning-issues) — cùng luồng, áp dụng cả cho đội làm người phụ trách\n- [`@`nhắc agent trong bình luận](/mentioning-agents) — bộ chọn `@` cũng hiện đội\n- [Agent](/agents) — agent là gì, viên gạch của mọi đội\n- [Thành viên và vai trò](/members-roles) — ma trận quyền owner / admin / member đầy đủ"
        }
      ]
    },
    {
      "label": "How agents run",
      "labelKey": "how_agents_run",
      "pages": [
        {
          "slug": "daemon-runtimes",
          "title": "Daemon và runtime",
          "content": "Trong Multica, [agent](/agents) **không** chạy trên máy chủ của chúng tôi — chúng chạy trên máy của bạn, được điều khiển bởi một chương trình nhỏ gọi là **daemon** chuyên gọi các [công cụ lập trình AI](/providers) cài trên máy. Multica server chỉ điều phối: lưu [issue](/issues), xếp hàng [task](/tasks) và phát chúng tới đúng **runtime** (runtime = daemon × một công cụ lập trình AI).\n\nCấu trúc này là khác biệt lớn nhất giữa Multica với Linear / Jira: **API key, bộ công cụ và thư mục mã nguồn ở lại trên máy của bạn** — Multica server không bao giờ nhìn thấy chúng. Vì thế \"agent của tôi không chạy\" gần như luôn là vấn đề cục bộ — daemon chưa chạy, công cụ AI chưa cài, key hết hạn. Kiểm tra trên máy trước; xem [Khắc phục sự cố](/troubleshooting).\n\n## Khởi động daemon\n\nDaemon là một phần của Multica CLI. Sau khi cài [Multica CLI](/cli), chạy trên máy của bạn:\n\n```bash\nmultica daemon start\n```\n\nKhi khởi động, nó làm bốn việc:\n\n1. Đọc thông tin đăng nhập đã lưu khi bạn đăng nhập\n2. Phát hiện các công cụ lập trình AI trên `PATH` (14 công cụ có sẵn: [Antigravity](/providers#antigravity), [Claude Code](/providers#claude-code), [CodeBuddy](/providers#codebuddy), [Codex](/providers#codex), [Cursor](/providers#cursor), [Copilot](/providers#copilot), [Hermes](/providers#hermes), [Kimi](/providers#kimi), [Kiro CLI](/providers#kiro-cli), [OpenCode](/providers#opencode), [OpenClaw](/providers#openclaw), [Pi](/providers#pi), [Qoder](/providers#qoder), [Trae CLI](/providers#trae))\n3. Đăng ký chính nó với server, kèm một runtime cho mỗi công cụ phát hiện được\n4. **Poll mỗi 3 giây** để nhận task, và **gửi heartbeat mỗi 15 giây**\n\nCác lệnh thường dùng:\n\n| Lệnh | Mục đích |\n|---|---|\n| `multica daemon start` | Khởi động (mặc định chạy nền; thêm `--foreground` để chạy tiền cảnh) |\n| `multica daemon stop` | Dừng |\n| `multica daemon restart` | Khởi động lại |\n| `multica daemon status` | Xem trạng thái |\n| `multica daemon logs` | Xem log (thêm `-f` để theo dõi) |\n\nTham chiếu CLI đầy đủ tại [Lệnh CLI](/cli).\n\n## Vì sao một máy có nhiều runtime\n\nRuntime không phải server, cũng không phải container — nó là tổ hợp \"**daemon × một công cụ lập trình AI**\". Ví dụ: bạn khởi động daemon trên một chiếc MacBook cài cả Claude Code lẫn Codex, và bạn là thành viên của hai không gian làm việc. Multica sẽ đăng ký 4 runtime:\n\n```mermaid\ngraph TD\n    D[\"Daemon của bạn<br/>MacBook\"]\n    D --> R1[\"Runtime<br/>Không gian làm việc A × Claude Code\"]\n    D --> R2[\"Runtime<br/>Không gian làm việc A × Codex\"]\n    D --> R3[\"Runtime<br/>Không gian làm việc B × Claude Code\"]\n    D --> R4[\"Runtime<br/>Không gian làm việc B × Codex\"]\n```\n\nĐiểm chính:\n\n- **Một daemon có thể ứng với nhiều runtime** — mỗi runtime cho một tổ hợp công cụ đã cài × không gian làm việc bạn tham gia\n- **Cùng daemon, không gian làm việc và công cụ thì chỉ có đúng một runtime** — khởi động lại daemon không bao giờ tạo bản ghi trùng\n- Trang **Runtime** trong giao diện Multica liệt kê các dòng này\n\n## Runtime profile tùy chỉnh\n\nCơ chế phát hiện provider có sẵn đã phủ các công cụ phổ biến, nhưng đội của bạn cũng có thể định nghĩa **runtime profile tùy chỉnh** cho các AI CLI nói một trong các họ giao thức Multica hỗ trợ và cần lệnh khởi chạy riêng theo không gian làm việc. Profile được quản lý từ giao diện Runtime hoặc CLI:\n\n```bash\nmultica runtime profile list\nmultica runtime profile create --display-name \"Composer\" --protocol-family codex --command-name agent\nmultica runtime profile update <profile-id> --command-name agent\nmultica runtime profile delete <profile-id>\n```\n\nLệnh theo hướng argv, không phải chuỗi shell. Multica lưu tên file thực thi cùng các đối số cố định, rồi daemon khởi chạy trực tiếp bằng `exec.Command(command_name, fixed_args...)`. Hỗ trợ đối số thường, dấu nháy và escape bằng backslash; không hỗ trợ pipe, redirect, `&&`, `;`, backtick và mở rộng `$VAR` / `$(...)`. Dùng script bọc khi runtime cần hành vi shell. Hiện giao diện Runtime đảm nhận phân tích lệnh-và-đối-số; các lệnh profile của CLI quản lý bản ghi profile và ghi đè đường dẫn cục bộ.\n\nNếu daemon không tìm thấy một lệnh vốn chạy được trong terminal của bạn, ghim đường dẫn tuyệt đối trên máy đó:\n\n```bash\nmultica runtime profile set-path <profile-id> --path /abs/path/to/agent\nmultica runtime profile unset-path <profile-id>\n```\n\nSửa lệnh hoặc đối số của profile chỉ áp dụng cho task nhận mới sau khi daemon đăng ký lại. Task đang chạy giữ nguyên đối số khởi chạy ban đầu. Với triển khai hỗn hợp, nâng cấp server trước khi tung daemon mới: giao diện Runtime phía server lưu `fixed_args`, và server là nơi hiển thị báo cáo đăng ký `failed_profiles`. Các thành phần cũ có thể lặng lẽ bỏ qua trường chúng không hiểu thay vì báo lỗi rõ ràng, nên coi nâng cấp server là bước đầu tiên giúp đợt rollout luôn quan sát được.\n\n> **Cloud runtime sắp ra mắt**, hiện trong giai đoạn danh sách chờ. Khi khả dụng, bạn sẽ chạy được task của agent trực tiếp trên Multica Cloud mà không cần daemon cục bộ.\n\n## Khi runtime bị đánh dấu offline\n\nMultica dùng heartbeat để quyết định runtime có online hay không. Ba con số then chốt:\n\n| Sự kiện | Ngưỡng |\n|---|---|\n| Tần suất heartbeat của daemon | Mỗi **15 giây** |\n| Bị đánh dấu missing | Không có heartbeat trong **45 giây** (lỡ 3 nhịp) |\n| Tự động xóa | Missing và không gắn agent nào trong hơn **7 ngày** |\n\nMissing không phải vĩnh viễn — daemon gửi heartbeat tiếp theo là nó trở lại online, và bản ghi runtime được giữ nguyên. Khởi động lại daemon không làm mất runtime.\n\n> **Task đang chạy trên runtime missing bị đánh dấu failed** (lý do `runtime_offline`). Với các nguồn cho phép thử lại (issue, chat), Multica tự xếp hàng lại; task do Autopilot kích hoạt không tự thử lại. Xem [Task → Lỗi nào tự thử lại](/tasks#which-failures-retry-automatically-which-dont).\n\n## Bao nhiêu task chạy song song được\n\nMultica giới hạn đồng thời ở hai tầng:\n\n- **Tầng daemon**: mặc định **20 task đồng thời** (chỉnh bằng biến môi trường `MULTICA_DAEMON_MAX_CONCURRENT_TASKS`)\n- **Tầng agent**: mặc định **6 task đồng thời mỗi agent** (cấu hình theo từng agent)\n\nGiới hạn chặt hơn thắng. Nếu daemon đã chạy 20 task, task mới phải chờ dù agent vẫn còn chỗ.\n\nNếu bạn thấy task kẹt ở `queued` không sang `dispatched`, thường là một trong hai giới hạn này đã bão hòa.\n\n## Task đang chạy ra sao sau khi daemon sập\n\nKhi daemon sập hoặc bị kill, các task nó đã nhận bị bỏ lại ở `dispatched` hoặc `running`. Ở lần khởi động kế tiếp, daemon báo server: \"các task này không còn của tôi, hãy đánh dấu failed.\" Server chuyển chúng sang `failed` với lý do `runtime_recovery` — với các nguồn cho phép thử lại, task được tự xếp hàng lại.\n\nKể cả khi bước này thất bại do lỗi mạng, phía server có một lượt quét **mỗi 30 giây** làm lưới đỡ: runtime nào không có heartbeat quá 45 giây bị đánh dấu missing, và các task của nó được thu hồi theo.\n\n## Chẩn đoán agent không hoạt động\n\nKhi gặp vấn đề \"agent của tôi không chạy\", làm checklist ba bước này trước:\n\n1. Chạy `multica daemon status` — xác nhận daemon đang chạy và online\n2. Chạy `multica daemon logs -f` — kiểm tra lỗi\n3. Mở trang **Runtime** trong giao diện Multica — xác nhận runtime của bạn hiện \"online\"\n\nThêm tình huống khác tại [Khắc phục sự cố](/troubleshooting).\n\n## Tiếp theo\n\n- [Task](/tasks) — vòng đời đầy đủ của task sau khi daemon nhận\n- [Ma trận provider](/providers) — khác biệt năng lực giữa 14 công cụ lập trình AI"
        },
        {
          "slug": "install-agent-runtime",
          "title": "Cài đặt runtime cho agent",
          "content": "Một **runtime** trong Multica là daemon trên máy của bạn ghép với một công cụ lập trình AI mà daemon tìm thấy trên `PATH`. Nếu bước onboarding \"Connect a runtime\" hiện **No supported tools detected**, nghĩa là daemon đã quét `PATH` và không thấy công cụ nào trong 14 công cụ nó biết điều khiển. Cài một (hoặc vài) công cụ bên dưới, rồi quay lại bước đó và quét lại — runtime sẽ hiện lên trong vài giây.\n\nTrang này là phần cài đặt, đi kèm với:\n\n- [Daemon và runtime](/daemon-runtimes) — cơ chế phát hiện hoạt động ra sao\n- [Ma trận công cụ lập trình AI](/providers) — mỗi công cụ làm được và không làm được gì (khôi phục phiên, MCP, chọn model)\n\n> Multica server không bao giờ nhìn thấy API key hay bản thân các công cụ. Mọi thứ bên dưới — cài đặt, xác thực, quyền truy cập model — đều nằm trên máy của bạn. Nếu có lỗi, gần như chắc chắn là lỗi cục bộ.\n\n## Trước khi bắt đầu\n\nHai điều kiện áp dụng cho **mọi** công cụ bên dưới:\n\n1. **Multica daemon phải đang chạy.** Chạy `multica daemon start` sau khi cài [Multica CLI](/cli). Không có daemon thì không có gì để phát hiện công cụ.\n2. **Binary của công cụ phải nằm trên `PATH`.** Daemon gọi từng công cụ theo tên (xem cột **Daemon tìm** trong mỗi mục). Nếu `which <name>` không tìm thấy trong terminal của bạn thì daemon cũng không tìm thấy. Sau khi cài, mở terminal mới (hoặc khởi động lại daemon) để `PATH` mới được nhận.\n\nSau khi cài một công cụ, khởi động lại daemon:\n\n```bash\nmultica daemon restart\n```\n\nDaemon quét lại `PATH` ở mỗi lần khởi động.\n\n## 14 công cụ được hỗ trợ\n\nLiệt kê xấp xỉ từ phổ biến nhất tới ít phổ biến nhất. Chọn công cụ nào bạn đã có sẵn tài khoản — không cần đủ cả 14.\n\n### Claude Code (Anthropic)\n\nTích hợp hoàn chỉnh nhất. Khôi phục phiên hoạt động, MCP hoạt động, và nó dùng trường `mcp_config` trên agent (xem [ma trận](/providers#mcp-configuration-provider-specific-support)).\n\n| | |\n|---|---|\n| Daemon tìm | `claude` |\n| Cài đặt | Theo hướng dẫn chính thức tại [claude.com/claude-code](https://www.claude.com/claude-code). Cách chuẩn là gói npm `@anthropic-ai/claude-code` (cần Node.js 18+). |\n| Xác thực | Chạy `claude` một lần và làm theo luồng đăng nhập trong CLI, hoặc đặt `ANTHROPIC_API_KEY`. |\n| Ghi chú | Khuyến nghị hàng đầu cho người mới. |\n\n### Codex (OpenAI)\n\nGiao vận JSON-RPC 2.0 với các cổng phê duyệt chi tiết hơn. Cấu hình MCP được ghi vào `$CODEX_HOME/config.toml` theo từng task. **Khôi phục phiên hoạt động** qua `thread/resume` của Codex app-server; thread cũ hoặc mất sẽ tự chuyển sang thread mới.\n\n| | |\n|---|---|\n| Daemon tìm | `codex` |\n| Cài đặt | Theo hướng dẫn chính thức tại github.com/openai/codex. Cách chuẩn là gói npm `@openai/codex`. |\n| Xác thực | `codex login` (qua trình duyệt) hoặc `OPENAI_API_KEY`. |\n\n### Cursor (Anysphere)\n\nBản CLI của trình soạn thảo Cursor. **Khôi phục phiên hoạt động** với các bản Cursor Agent hiện tại: Multica đọc `session_id` từ các sự kiện stream-json và truyền lại bằng `--resume <id>`.\n\n| | |\n|---|---|\n| Daemon tìm | `cursor-agent` |\n| Cài đặt | Cài [trình soạn thảo Cursor](https://cursor.com/) rồi cài CLI theo tài liệu tại [docs.cursor.com](https://docs.cursor.com/). Tên binary là `cursor-agent`, không phải `cursor`. |\n| Xác thực | Đăng nhập qua trình soạn thảo Cursor; CLI dùng lại phiên đó. |\n\n### GitHub Copilot\n\nViệc định tuyến model đi qua quyền lợi tài khoản GitHub của bạn — công cụ không tự chọn model; GitHub quyết định bạn được model nào.\n\n| | |\n|---|---|\n| Daemon tìm | `copilot` |\n| Cài đặt | Xem tài liệu CLI của GitHub tại github.com/github/copilot-cli. |\n| Xác thực | Đăng nhập GitHub qua trình duyệt từ CLI. |\n| Ghi chú | Cần gói GitHub Copilot còn hiệu lực trên tài khoản đã đăng nhập. |\n\n### OpenCode (SST)\n\nCLI agent mã nguồn mở. Tự khám phá các model khả dụng từ file cấu hình riêng — phù hợp cho người muốn tự mang danh mục model. Dùng trường `mcp_config` của agent qua `OPENCODE_CONFIG_CONTENT`.\n\n| | |\n|---|---|\n| Daemon tìm | `opencode` |\n| Cài đặt | Theo hướng dẫn chính thức tại [opencode.ai](https://opencode.ai/) hoặc repo github.com/sst/opencode. Cách thông dụng là script cài hoặc gói npm. |\n| Xác thực | Cấu hình nhà cung cấp model theo tài liệu của OpenCode (Anthropic, OpenAI, v.v.). |\n\n### Kiro CLI (Amazon)\n\nGiao vận ACP qua stdio. Khôi phục phiên hoạt động qua ACP `session/load`; cấu hình MCP truyền qua ACP `mcpServers`; skill được chép vào `.kiro/skills/`.\n\n| | |\n|---|---|\n| Daemon tìm | `kiro-cli` |\n| Cài đặt | Xem tài liệu Kiro tại [kiro.dev](https://kiro.dev/). Tên binary là `kiro-cli`, không phải `kiro`. |\n| Xác thực | Theo tài khoản AWS; làm theo onboarding của Kiro. |\n\n### Kimi (Moonshot)\n\nAgent giao thức ACP, chủ yếu nhắm thị trường Trung Quốc. Cấu hình MCP truyền qua ACP `mcpServers`; skill nằm trong `.kimi/skills/` (khám phá native).\n\n| | |\n|---|---|\n| Daemon tìm | `kimi` |\n| Cài đặt | Theo hướng dẫn chính thức tại github.com/MoonshotAI/kimi-cli. |\n| Xác thực | API key của Moonshot, cấu hình theo tài liệu nhà cung cấp. |\n\n### Hermes (Nous Research)\n\nAgent giao thức ACP (chung giao vận với Kimi). Khôi phục phiên hoạt động, cấu hình MCP truyền qua ACP `mcpServers`. Đường chèn skill dùng đường dự phòng chung `.agent_context/skills/` — hãy kiểm tra skill có nạp không trước khi phụ thuộc vào chúng.\n\n| | |\n|---|---|\n| Daemon tìm | `hermes` |\n| Cài đặt | Xem repository của Nous Research (github.com/NousResearch) để lấy bản CLI mới nhất. |\n| Xác thực | Theo tài liệu nhà cung cấp. |\n\n### OpenClaw\n\nBộ điều phối CLI agent mã nguồn mở. Cấu hình MCP được vật thể hóa qua lớp bọc cấu hình theo từng task của Multica. **Model gắn ở tầng agent** (`openclaw agents add --model`) — không ghi đè được theo task, và bạn không truyền được `--model` hay `--system-prompt` từ Multica.\n\n| | |\n|---|---|\n| Daemon tìm | `openclaw` |\n| Cài đặt | Xem dự án github.com/openclaw-org/openclaw (do cộng đồng bảo trì). |\n| Xác thực | Cấu hình nhà cung cấp model bên dưới theo tài liệu của OpenClaw. |\n\n### Pi (Inflection AI)\n\nTối giản. **Khôi phục phiên hơi khác thường** — id khôi phục là đường dẫn tới file phiên trên đĩa, không phải chuỗi id.\n\n| | |\n|---|---|\n| Daemon tìm | `pi` |\n| Cài đặt | Xem tài liệu CLI của Inflection tại [pi.ai](https://pi.ai/). |\n| Xác thực | Theo tài liệu nhà cung cấp. |\n\n### CodeBuddy (Tencent)\n\nCLI agent tương thích Claude Code. Multica điều khiển nó bằng đúng giao thức stream-json như Claude Code: khôi phục phiên qua `--resume`, cấu hình MCP truyền qua `--mcp-config`, skill nằm trong `.claude/skills/`. Model được khám phá động.\n\n| | |\n|---|---|\n| Daemon tìm | `codebuddy` |\n| Cài đặt | Xem tài liệu CLI chính thức tại [codebuddy.ai/cli](https://www.codebuddy.ai/cli). |\n| Xác thực | Theo tài liệu nhà cung cấp. |\n\n### Qoder (Alibaba)\n\nCLI lập trình agentic dùng giao thức ACP qua stdio (chung giao vận với Hermes, Kimi và Kiro CLI). Khôi phục phiên hoạt động qua ACP `session/resume`, cấu hình MCP truyền qua ACP `mcpServers`, model được khám phá động, và skill được chép vào `.qoder/skills/`.\n\n| | |\n|---|---|\n| Daemon tìm | `qodercli` |\n| Cài đặt | Xem tài liệu CLI chính thức tại [qoder.com/cli](https://qoder.com/cli). |\n| Xác thực | Theo tài liệu nhà cung cấp. |\n\n### Trae CLI (ByteDance)\n\nTRAE CLI chính thức của ByteDance (`traecli`, đi cặp với Trae IDE — **không phải** bản mã nguồn mở `bytedance/trae-agent`). Nó là ACP-native, nên Multica điều khiển qua stdio bằng `traecli acp serve --yolo`, chung giao vận với Hermes, Kimi, Kiro CLI và Qoder. Khôi phục phiên hoạt động qua ACP `session/load`, cấu hình MCP truyền qua ACP `mcpServers`, model được khám phá động (đổi theo task qua `session/set_model`), và skill được chép vào `.traecli/skills/`.\n\n| | |\n|---|---|\n| Daemon tìm | `traecli` |\n| Cài đặt | Xem tài liệu CLI chính thức tại [docs.trae.cn/cli](https://docs.trae.cn/cli). |\n| Xác thực | Chạy `traecli` một lần ở chế độ tương tác để hoàn tất đăng nhập doanh nghiệp qua trình duyệt (token lưu trong `~/.trae` và keyring hệ điều hành). Đăng nhập vào Trae **IDE** không đăng nhập CLI — hai thứ tách biệt. |\n| Ghi chú | traecli đọc quy tắc dự án từ `.trae/rules/` và không có cờ `--system-prompt`, nên Multica đưa phần chỉ dẫn runtime thẳng vào prompt. Đặt model mặc định toàn daemon bằng `MULTICA_TRAECLI_MODEL`. |\n\n### Antigravity (Google)\n\nAntigravity CLI của Google (`agy`). Ghép với dịch vụ Antigravity của Google và chạy các model nền Gemini. Multica khởi chạy bằng `agy -p`, chế độ không tương tác tương thích daemon; các bản Antigravity CLI hiện tại thực thi được công cụ từ chế độ đó, còn `agy -i` cần TTY. Khôi phục phiên hoạt động qua `--conversation <id>`, do daemon bắt từ file log của CLI. Việc chọn model được quản lý bên trong Antigravity CLI — Multica tắt bộ chọn model theo agent cho provider này. Skill được ghi vào `.agents/skills/` (CLI kế thừa bố cục skill của Gemini CLI — xem [tài liệu Antigravity](https://antigravity.google/docs/gcli-migration)).\n\n| | |\n|---|---|\n| Daemon tìm | `agy` |\n| Cài đặt | Theo hướng dẫn chính thức tại [antigravity.google/docs/cli-overview](https://antigravity.google/docs/cli-overview). CLI phát hành dạng pre-built — chạy `agy install` một lần để nối PATH và alias shell. |\n| Xác thực | Chạy `agy` một lần ở chế độ tương tác và hoàn tất đăng nhập tài khoản Google, hoặc đăng nhập qua app Antigravity — CLI dùng lại mục keyring mà GUI ghi. |\n| Ghi chú | CLI in văn bản trợ lý thuần ra stdout, không phải luồng sự kiện có cấu trúc; các dòng trung gian \"I will run X\" lẫn câu trả lời cuối đều được chuyển về Multica dưới dạng văn bản, và hiện chưa có telemetry theo công cụ. |\n\n## Sau khi cài\n\n1. **Xác nhận binary nằm trên `PATH`.** Mở terminal mới và chạy `which <name>` (ví dụ `which claude`, `which cursor-agent`, `which kiro-cli`, `which agy`). Nếu in ra đường dẫn, daemon sẽ tìm thấy. Nếu không in gì, sửa `PATH` của shell trước (nguyên nhân điển hình là file rc chưa được nạp lại).\n2. **Khởi động lại daemon.** `multica daemon restart`. Daemon chỉ quét `PATH` lúc khởi động.\n3. **Kiểm tra trang Runtime.** Trong giao diện Multica, trang **Runtime** giờ sẽ liệt kê một dòng cho mỗi tổ hợp `(không gian làm việc × công cụ)`. Nếu dòng ghi \"offline\", xem [Daemon và runtime → Khi runtime bị đánh dấu offline](/daemon-runtimes#when-a-runtime-is-marked-offline).\n4. **Quay lại onboarding.** Bước \"Connect a runtime\" tự poll và sẽ nhận runtime mới trong vài giây — không cần tải lại trang.\n\n## Khắc phục sự cố\n\n- **`which` tìm thấy binary nhưng daemon thì không.** Daemon được khởi động với `PATH` cũ. Khởi động lại nó.\n- **Binary tồn tại nhưng khởi chạy lỗi.** Chạy `--version` hoặc `--help` của chính công cụ đó từ terminal — đa số lỗi ở đây là thiếu xác thực, token hết hạn, hoặc lệch phiên bản Node.js / runtime.\n- **Trang Runtime hiện dòng, nhưng task lỗi ngay lập tức.** Xem `multica daemon logs -f` trong lúc kích hoạt task. Daemon hiển thị chính thông báo lỗi của công cụ.\n\nCác triệu chứng rộng hơn, xem [Hướng dẫn khắc phục sự cố](/troubleshooting).\n\n## Tiếp theo\n\n- [Daemon và runtime](/daemon-runtimes) — cơ chế phát hiện, heartbeat và xử lý offline\n- [Ma trận công cụ lập trình AI](/providers) — khác biệt năng lực khi công cụ đã kết nối\n- [Tạo và cấu hình agent](/agents-create) — chọn công cụ cho agent và bắt đầu chạy task"
        },
        {
          "slug": "tasks",
          "title": "Task",
          "content": "Một **task** là đơn vị của mỗi lượt [agent](/agents) chạy — [giao issue cho agent](/assigning-issues), [@nhắc agent trong bình luận](/mentioning-agents), gửi tin nhắn trong [chat](/chat), hay một [Autopilot](/autopilots) nổ theo lịch đều sinh ra một task. Multica đưa nó vào hàng đợi; một [daemon](/daemon-runtimes) nhận và chuyển cho [công cụ lập trình AI](/providers) tương ứng, rồi ghi kết quả về server khi xong.\n\nTask và [issue](/issues) là hai đối tượng khác nhau. Một issue có thể được giao, @nhắc và chạy lại thủ công nhiều lần — mỗi lần sinh một task **mới**.\n\n## Các trạng thái một task đi qua\n\n```mermaid\ngraph LR\n    Q[\"Xếp hàng<br/>queued\"] -->|daemon nhận| D[\"Đã phát<br/>dispatched\"]\n    D -->|agent bắt đầu| R[\"Đang chạy<br/>running\"]\n    R -->|thành công| C[\"Hoàn thành<br/>completed\"]\n    R -->|lỗi hoặc timeout| F[\"Thất bại<br/>failed\"]\n    Q -->|người dùng hủy| X[\"Đã hủy<br/>cancelled\"]\n    D -->|người dùng hủy| X\n    R -->|người dùng hủy| X\n    F -.lý do cho phép thử lại.-> Q\n```\n\n- **Queued** — task vừa được tạo, chờ daemon nhận\n- **Dispatched** — một daemon đã nhận và đang khởi chạy công cụ lập trình AI\n- **Running** — công cụ lập trình AI đang thực sự làm việc\n- **Completed** — hoàn thành; kết quả (bình luận, commit mã, thay đổi trạng thái) được ghi về server\n- **Failed** — bị dừng vì lỗi hoặc timeout; nếu lý do lỗi cho phép thử lại, task tự quay về `queued` để chạy thêm lần nữa\n- **Cancelled** — người dùng đã hủy\n\n## Điều gì xảy ra khi task timeout\n\nMultica server quét mỗi 30 giây. Hai loại timeout gây thất bại:\n\n| Tình huống | Timeout |\n|---|---|\n| Đã dispatched nhưng không khởi chạy (daemon nhận nhưng chưa bật công cụ AI) | **5 phút** |\n| Chạy quá lâu | **2,5 giờ** |\n\nCả hai đều dùng lý do lỗi `timeout` và **tự thử lại** (mục kế tiếp). Về phép kiểm tra runtime missing liên quan, xem [Daemon và runtime → Khi runtime bị đánh dấu offline](/daemon-runtimes#when-a-runtime-is-marked-offline).\n\n## Lỗi nào tự thử lại, lỗi nào không\n\nLỗi chia hai loại: **cho phép thử lại** và **không cho phép thử lại**.\n\n**Cho phép thử lại** (Multica tự xếp hàng lại):\n\n- `runtime_offline` — daemon mất tích sau khi task đã dispatched\n- `runtime_recovery` — daemon sập rồi khởi động lại, thu hồi các task chưa xong\n- `timeout` — timeout khi chạy hoặc khi dispatch\n\n**Không cho phép thử lại** (task nằm nguyên ở failed):\n\n- `agent_error` — bản thân công cụ lập trình AI báo lỗi (lỗi API, hết quota, bug nội bộ). Lỗi gốc không được thử lại — vì sẽ lặp vô hạn.\n\nTự thử lại còn hai điều kiện phụ:\n\n1. **Tối đa 2 lần chạy** — 1 lần gốc + 1 lần thử lại. Nếu lần thử lại vẫn lỗi thì thôi, kể cả khi lý do cho phép thử lại.\n2. **Chỉ với task kích hoạt từ issue và chat** — task do Autopilot kích hoạt **không** tự thử lại.\n\n> **Task của Autopilot không tự thử lại** là chủ ý thiết kế. Autopilot có nhịp chạy riêng (ví dụ hằng ngày); tự thử lại khi lỗi sẽ chồng lên lần chạy kế theo lịch. Cần chạy lại ngay sau lỗi thì dùng chạy lại thủ công (mục kế tiếp).\n>\n> **Làm sao biết task của Autopilot lỗi**: một thông báo về [Hộp thư đến](/inbox), và trạng thái issue liên quan lùi từ `in_progress` về `todo`. Trang [Tự động hóa](/autopilots) cũng hiện kết quả lần chạy mới nhất của từng autopilot.\n\n## Chạy lại thủ công vs. tự thử lại\n\n**Chạy lại thủ công** là do bạn kích hoạt từ CLI hoặc API (`POST /api/issues/{id}/rerun`):\n\n```bash\nmultica issue rerun <issue-id>\n```\n\nHành vi:\n\n- Mặc định nhắm vào **agent đang phụ trách issue** — hữu ích khi bạn muốn lần chạy lại bám theo phân công hiện tại, bất kể ai chạy task trước.\n- Nút thử lại trên một dòng trong nhật ký thực thi gửi kèm ID task của dòng đó, nên lần chạy lại nhắm vào **agent đã chạy đúng task đó** — không phải người phụ trách hiện tại. Điều này khiến thử lại theo dòng có ý nghĩa với worker của đội, các agent @nhắc song song, hoặc dòng có agent đã bị thay do phân công lại.\n- **Hủy** task đang xếp hàng hoặc đang chạy của agent mục tiêu trên issue này (nếu có). Task của agent khác trên cùng issue (ví dụ các lượt @nhắc song song) được để yên.\n- Tạo một task **hoàn toàn mới** — bộ đếm lần chạy về 1, kể cả khi task gốc đã chạm trần.\n- Bắt đầu **phiên agent mới** — ID phiên trước **không** được kế thừa. Chạy lại thủ công nghĩa là bạn đã đánh giá kết quả trước là tệ, nên nối lại cuộc trò chuyện cũ chỉ phát lại đúng trạng thái hỏng đó. (Ngược lại, tự thử lại kế thừa phiên — đường đó dành cho lỗi hạ tầng, không phải kết quả tệ.)\n\nSo sánh:\n\n| Khía cạnh | Tự thử lại | Chạy lại thủ công |\n|---|---|---|\n| Kích hoạt | Hệ thống, theo lý do lỗi | Bạn, thủ công |\n| Trần | 2 lần chạy | Không giới hạn |\n| Nguồn áp dụng | Issue, chat | Issue có agent phụ trách |\n| Agent được chọn | Cùng agent với task lỗi | Agent của task nguồn (thử lại theo dòng trên UI) hoặc người phụ trách hiện tại (CLI / không có task_id) |\n| Kế thừa phiên | Có (nối phiên trước) | Không (phiên mới) |\n\n## Task lỗi ảnh hưởng trạng thái issue thế nào\n\nNếu task kích hoạt từ issue thất bại (và không lần tự thử lại nào thành công) vì issue được giao cho agent, **trạng thái issue tự lùi từ `in_progress` về `todo`** — mở bảng là bạn thấy ngay \"cái này cần xem lại\". Xem [Issue và dự án](/issues).\n\n## Task có tiếp tục được từ ngữ cảnh trước không\n\nCó — miễn công cụ lập trình AI hỗ trợ khôi phục phiên.\n\nMultica ghim ID phiên **hai lần** trong một task: một lần lúc bắt đầu (khi công cụ AI trả về system message đầu tiên), một lần lúc kết thúc (hoàn thành hoặc thất bại). Lần đầu giúp daemon phục hồi nếu sập giữa chừng; lần sau dành cho lượt **tự thử lại** kế tiếp, nơi ID đó được truyền lại để agent nối tiếp cuộc trò chuyện và trạng thái file trước đó. **Chạy lại thủ công chủ ý bỏ qua bước này** và mở phiên mới — xem [Chạy lại thủ công vs. tự thử lại](#manual-rerun-vs-automatic-retry).\n\nNhưng **công cụ nào thực sự hỗ trợ** thì khác nhau nhiều:\n\n- ✅ **Hỗ trợ thật** — Antigravity, Claude Code, Codex, Copilot, Cursor, Hermes, Kimi, Kiro CLI, OpenCode, OpenClaw, Pi\n- ❌ **Không hỗ trợ** — Gemini\n\nXem [Ma trận provider → Khôi phục phiên](/providers#session-resumption-who-really-supports-it).\n\n## Tiếp theo\n\n- [Ma trận provider](/providers) — khác biệt năng lực giữa 14 công cụ lập trình AI (kèm hiện trạng khôi phục phiên chính xác)\n- [Giao issue cho agent](/assigning-issues) / [@nhắc agent trong bình luận](/mentioning-agents) / [Chat](/chat) / [Tự động hóa](/autopilots) — bốn cách kích hoạt một task"
        },
        {
          "slug": "providers",
          "title": "Ma trận công cụ lập trình AI",
          "content": "Multica hỗ trợ sẵn **14 công cụ lập trình AI**. Tất cả triển khai cùng một giao diện — xếp hàng, phát, thực thi, trả kết quả — nên bạn điều khiển được con nào cũng từ cùng một bảng Multica. **Nhưng chi tiết năng lực khác nhau đáng kể**: khôi phục phiên có thật sự chạy không, MCP có được hỗ trợ không, file skill nằm đâu, model được chọn thế nào. Trang này là ma trận đầy đủ.\n\nĐể chọn công cụ khi tạo agent, xem [Tạo và cấu hình agent](/agents-create).\n\n## Ma trận năng lực\n\n| Công cụ | Nhà cung cấp | Khôi phục phiên | MCP | Đường chèn skill | Chọn model |\n|---|---|---|---|---|---|\n| **Antigravity** | Google | ✅ (`--conversation <id>`) | ❌ | `.agents/skills/` | Khám phá động (`agy models`) |\n| **Claude Code** | Anthropic | ✅ | ✅ | `.claude/skills/` | Tĩnh + cờ |\n| **CodeBuddy** | Tencent | ✅ | ✅ | `.claude/skills/` | Khám phá động |\n| **Codex** | OpenAI | ✅ | ✅ | `$CODEX_HOME/skills/` | Tĩnh |\n| **Copilot** | GitHub | ✅ | ❌ | `.github/skills/` | Tĩnh (theo quyền lợi tài khoản) |\n| **Cursor** | Anysphere | ✅ | ✅ | `.cursor/skills/` | Khám phá động |\n| **Hermes** | Nous Research | ✅ | ✅ | `.agent_context/skills/` (dự phòng) | Khám phá động |\n| **Kimi** | Moonshot | ✅ | ✅ | `.kimi/skills/` | Khám phá động |\n| **Kiro CLI** | Amazon | ✅ | ✅ | `.kiro/skills/` | Khám phá động |\n| **OpenCode** | SST | ✅ | ✅ | `.opencode/skills/` | Khám phá động + biến thể |\n| **OpenClaw** | Mã nguồn mở | ✅ | ✅ | `.agent_context/skills/` (dự phòng) | Gắn ở tầng agent, không đổi theo task |\n| **Pi** | Inflection AI | ✅ (phiên là đường dẫn file) | ❌ | `.pi/skills/` | Khám phá động |\n| **Qoder** | Alibaba | ✅ | ✅ | `.qoder/skills/` | Khám phá động |\n| **Trae** | ByteDance | ✅ (ACP `session/load`) | ✅ | `.traecli/skills/` | Khám phá động |\n\n## Từng công cụ dùng cho việc gì\n\n### Antigravity\n\nCủa Google. Tên binary CLI là `agy`. Ghép với dịch vụ Antigravity của Google, model mặc định nền Gemini. Multica khởi chạy Antigravity bằng `agy -p` vì đó là chế độ không tương tác tương thích daemon; `agy -i` cần TTY và không hợp thực thi task nền. Các bản Antigravity CLI hiện tại vẫn thực thi được công cụ từ chế độ này, nhưng stdout là văn bản trợ lý thuần chứ không phải luồng sự kiện có cấu trúc, nên Multica chuyển tiếp bản ghi dưới dạng văn bản và hiện chưa hiển thị được telemetry theo công cụ cho Antigravity. **Khôi phục phiên hoạt động** qua `--conversation <id>`; daemon bắt UUID cuộc trò chuyện từ file log của CLI. **Chọn model hoạt động** qua cờ `--model` (thêm từ agy 1.0.6): daemon liệt kê danh mục bằng `agy models` và gửi nguyên giá trị đã chọn. Lưu ý đây là chuỗi hiển thị cho người đọc như `Claude Opus 4.6 (Thinking)`, không phải slug `provider/model` — và agy lặng lẽ bỏ qua giá trị nó không nhận ra, nên ưu tiên chọn từ danh sách khám phá được thay vì tự gõ. Skill nằm trong `.agents/skills/` (CLI kế thừa bố cục skill của Gemini CLI — xem [tài liệu di trú Antigravity](https://antigravity.google/docs/gcli-migration)).\n\n### Claude Code\n\nCủa Anthropic. **Lựa chọn đầu tiên cho người mới** — bộ tính năng đầy đủ nhất: khôi phục phiên chạy thật, đọc cấu hình MCP, và hỗ trợ các cờ tinh chỉnh như `--max-turns` và `--append-system-prompt`. Cần API key của Anthropic.\n\n### CodeBuddy\n\nCủa Tencent. CLI agent tương thích Claude Code — Multica điều khiển nó bằng đúng giao thức stream-json như Claude Code, nên khôi phục phiên hoạt động (qua `--resume`), cấu hình MCP truyền qua `--mcp-config`, và skill dùng bố cục `.claude/skills/` của Claude Code. Model khám phá động.\n\n### Codex\n\nCủa OpenAI. Dùng JSON-RPC 2.0, tính trạng thái mạnh hơn, và cơ chế phê duyệt chi tiết hơn (phê duyệt tay cho `exec_command` và `patch_apply`). Cấu hình MCP được vật thể hóa vào `$CODEX_HOME/config.toml` theo từng task. **Khôi phục phiên hoạt động** qua `thread/resume` của Codex app-server; nếu thread đã lưu mất hoặc cũ, Multica quay về thread mới để task vẫn chạy được.\n\n### Copilot\n\nCủa GitHub. Định tuyến model qua quyền lợi tài khoản GitHub — công cụ không tự chọn model; GitHub quyết định bạn được model nào. Đặt skill trong `.github/skills/` là cơ chế khám phá gốc của GitHub CLI.\n\n### Cursor\n\nCủa Anysphere, bản CLI của trình soạn thảo Cursor. **Khôi phục phiên hoạt động** với các bản Cursor Agent hiện tại: sự kiện stream-json chứa `session_id`, và Multica truyền lại bằng `--resume <id>` ở lần chạy sau. Cấu hình MCP được vật thể hóa vào `.cursor/mcp.json` trong workspace của task, kèm file phê duyệt dự án của Cursor ghi dưới `CURSOR_DATA_DIR` theo từng task để các MCP server được quản lý không phụ thuộc phê duyệt Cursor toàn cục của người dùng.\n\n### Hermes\n\nCủa Nous Research. Dùng giao thức ACP (chung giao vận với Kimi). Khôi phục phiên hoạt động, cấu hình MCP truyền qua ACP `mcpServers`. Nhưng **đường chèn skill là dự phòng chung** (`.agent_context/skills/`), không phải đường riêng — nếu bản thân Hermes CLI không đọc đường này, skill có thể không hiệu lực. Kiểm chứng bằng cách thử.\n\n**Chọn profile Hermes.** Để khởi chạy Hermes dưới một profile cụ thể, đặt `custom_args` của agent thành cờ profile và tên profile như hai mục riêng — ví dụ với profile tên `research`:\n\n```json\n[\"-p\", \"research\"]\n```\n\nĐừng gộp thành một chuỗi kiểu `\"-p research\"`; Multica truyền mỗi phần tử mảng như một mục argv riêng. `custom_args` cấu hình theo từng agent — xem [Tạo và cấu hình agent](/agents-create).\n\n### Kimi\n\nCủa Moonshot, nhắm thị trường Trung Quốc. Chung giao thức ACP với Hermes, gồm cấu hình MCP qua ACP `mcpServers`, nhưng đường skill `.kimi/skills/` là cơ chế khám phá gốc của Kimi CLI — khác đường dự phòng của Hermes.\n\n### Kiro CLI\n\nCủa Amazon. Dùng ACP qua stdio bằng `kiro-cli acp`. Khôi phục phiên qua ACP `session/load`, cấu hình MCP qua ACP `mcpServers`, chọn model qua `session/set_model`, và skill được chép vào `.kiro/skills/` để khám phá gốc mức dự án.\n\n### OpenCode\n\nCủa SST, mã nguồn mở. Khám phá động các model và biến thể model khả dụng (quét file cấu hình của CLI). Khôi phục phiên hoạt động, và nó dùng trường `mcp_config` của agent — Multica tiêm inline qua biến môi trường `OPENCODE_CONFIG_CONTENT`, nên MCP server của agent tới được OpenCode mà không ghi gì vào `opencode.json` trong workdir của task (agent hoặc người dùng giữ quyền sở hữu file đó). Khi một model có biến thể, Multica hiện chúng làm bộ chọn thinking của agent và truyền giá trị đã chọn qua `opencode run --variant`. **Hợp với người thích vọc, muốn tùy biến danh mục model.**\n\n### OpenClaw\n\nDự án mã nguồn mở, một bộ điều phối CLI agent. Cấu hình MCP được vật thể hóa qua lớp bọc cấu hình OpenClaw theo từng task của Multica. **Model gắn ở tầng agent** (`openclaw agents add --model`) — không ghi đè được theo task. Cấu hình được kiểm soát chặt: người dùng không truyền được `--model` hay `--system-prompt`; cấu hình đăng ký agent quyết định.\n\n### Pi\n\nCủa Inflection AI, tối giản. **Khôi phục phiên hơi khác thường** — ID phiên là đường dẫn file trên đĩa (`~/.pi/...`) thay vì chuỗi ID. Ở công cụ khác, resume id là chuỗi do CLI trả về; ở Pi, resume id là chính file phiên.\n\n### Qoder\n\nCủa Alibaba. CLI lập trình agentic. Dùng giao thức ACP qua stdio (chung giao vận với Hermes, Kimi và Kiro CLI). Khôi phục phiên qua ACP `session/resume`, cấu hình MCP qua ACP `mcpServers`, chọn model khám phá động, và skill được chép vào `.qoder/skills/` để khám phá gốc.\n\n### Trae\n\nCủa ByteDance — **TRAE CLI** chính thức (`traecli`, tài liệu tại [docs.trae.cn/cli](https://docs.trae.cn/cli); đây là sản phẩm đi cặp với Trae IDE, **không phải** bản mã nguồn mở `bytedance/trae-agent`). traecli là ACP-native, nên Multica điều khiển qua giao vận ACP JSON-RPC chuẩn bằng `traecli acp serve --yolo`, y như Kiro và Qoder — `--yolo` đặt nó ở chế độ bỏ qua phê duyệt để các lần chạy headless không kẹt ở lời nhắc phê duyệt công cụ.\n\n- **Khôi phục phiên hoạt động** qua ACP `session/load` (traecli công bố `loadSession: true` từ `initialize`).\n- **Chọn model là động**: danh mục model khám phá từ `session/new` (`models.availableModels`) và đổi theo task qua `session/set_model`. Đặt mặc định toàn daemon bằng `MULTICA_TRAECLI_MODEL`.\n- **MCP được hỗ trợ** — cấu hình truyền qua ACP `mcpServers`; traecli công bố `mcpCapabilities: {http, sse}`, các giao vận không hỗ trợ bị lọc trước `session/new`.\n- **Xác thực** là đăng nhập doanh nghiệp của chính traecli: chạy `traecli` một lần ở chế độ tương tác để hoàn tất đăng nhập qua trình duyệt (token lưu ở `~/.trae` + keyring hệ điều hành). Đăng nhập IDE là chuyện riêng — đăng nhập Trae IDE **không** đăng nhập CLI.\n- **Skill** được ghi vào `.traecli/skills/` (dự án) để khám phá gốc; skill toàn cục nằm ở `~/.traecli/skills`.\n- Phần chỉ dẫn runtime của Multica cũng được nhúng thẳng vào prompt (traecli không có cờ `--system-prompt` và đọc quy tắc dự án từ `.trae/rules/`, không phải `AGENTS.md`), nên chỉ dẫn quy trình vẫn tới được agent.\n\n> Các năng lực trên được ghi nhận từ binary `traecli` v0.120.42 thật: `initialize` → `loadSession:true` + `mcpCapabilities{http,sse}`; `session/new` → `result.sessionId` + `models.availableModels`; `session/prompt` stream các thông báo `session/update` với `sessionUpdate: agent_message_chunk`.\n\n## Khôi phục phiên: ai hỗ trợ thật\n\nCơ chế khôi phục phiên được nói ở [Task](/tasks#can-a-task-continue-from-the-previous-context). **Mọi công cụ được hỗ trợ đều khôi phục phiên** — truyền resume id và task tiếp tục từ ngữ cảnh trước. Điểm dị biệt duy nhất là Pi, có resume id là đường dẫn file phiên trên đĩa thay vì chuỗi id (xem [Pi](#pi) ở trên).\n\n## Cấu hình MCP: hỗ trợ theo từng provider\n\n**Trong 14 công cụ, mười một con dùng `mcp_config`: Claude Code, CodeBuddy, Codex, Cursor, Hermes, Kimi, Kiro CLI, OpenCode, OpenClaw, Qoder và Trae**. Ba con còn lại (Antigravity, Copilot, Pi) nhận trường này nhưng **bỏ qua** — không lỗi, không cảnh báo, cấu hình đơn giản là không có tác dụng.\n\nĐường runtime là theo từng provider: Claude Code và CodeBuddy nhận qua `--mcp-config` đi cặp `--strict-mcp-config`; Codex ghi khối `mcp_servers` do daemon quản lý vào `$CODEX_HOME/config.toml` theo từng task; Cursor ghi `.cursor/mcp.json` cùng phê duyệt dự án theo task dưới `CURSOR_DATA_DIR`; Hermes, Kimi, Kiro CLI, Qoder và Trae nhận ACP `mcpServers`; OpenCode nhận cấu hình inline qua `OPENCODE_CONFIG_CONTENT`; OpenClaw nhận `mcp.servers` qua lớp bọc cấu hình theo task của Multica. Đường của OpenCode **không** viết lại `opencode.json` của dự án.\n\n> Nếu bạn đặt `mcp_config` trong cấu hình agent nhưng chọn công cụ không có ✅ ở cột MCP, các MCP server của bạn **không có tác dụng** với agent đó. Tích hợp MCP là theo từng provider.\n\n## File skill đi đâu\n\nMỗi công cụ dùng đường khám phá skill **của riêng nó**. Trước khi task chạy, daemon Multica chép file skill của không gian làm việc vào đường tương ứng:\n\n| Công cụ | Đường | Khám phá gốc? |\n|---|---|---|\n| Claude Code | `.claude/skills/` | ✅ Gốc |\n| CodeBuddy | `.claude/skills/` | ✅ Gốc |\n| Codex | `$CODEX_HOME/skills/` | ✅ Gốc |\n| Copilot | `.github/skills/` | ✅ Gốc |\n| Cursor | `.cursor/skills/` | ✅ Gốc |\n| Kimi | `.kimi/skills/` | ✅ Gốc |\n| Kiro CLI | `.kiro/skills/` | ✅ Gốc |\n| OpenCode | `.opencode/skills/` | ✅ Gốc |\n| Pi | `.pi/skills/` | ✅ Gốc |\n| Qoder | `.qoder/skills/` | ✅ Gốc |\n| Antigravity | `.agents/skills/` | ✅ Gốc (kế thừa bố cục workspace của Gemini CLI — xem [tài liệu Antigravity](https://antigravity.google/docs/gcli-migration)) |\n| Hermes | `.agent_context/skills/` | ⚠️ Dự phòng chung |\n| OpenClaw | `.agent_context/skills/` | ⚠️ Dự phòng chung |\n\nCông cụ dùng đường dự phòng có thật sự đọc thư mục này không thì tùy tài liệu của chính công cụ — không đảm bảo. Nếu skill không hiệu lực với Hermes / OpenClaw, kiểm tra chỗ này trước.\n\nVới các đường gốc mức dự án, khám phá trong phạm vi repo là hành vi được kỳ vọng: nếu repository đã checkout có sẵn thư mục khớp, công cụ bên dưới tự khám phá các skill đã commit đó. Bạn không cần nhập các skill repo đó vào Multica chỉ để dùng trong repo đó. Multica giữ nguyên file của repo. Nếu một skill của không gian làm việc trùng tên thư mục tự nhiên, daemon ghi bản của không gian làm việc sang tên không đụng độ như `review-helper-multica`.\n\nVề tạo và dùng skill, xem [Skills](/skills).\n\n## Tiếp theo\n\n- [Tạo và cấu hình agent](/agents-create) — chọn công cụ cho agent của bạn\n- [Task](/tasks) — vòng đời task và cơ chế khôi phục phiên\n- [Daemon và runtime](/daemon-runtimes) — các công cụ chạy ở đâu và kết nối Multica ra sao\n- [Cài đặt runtime cho agent](/install-agent-runtime) — cài đặt và xác thực cho từng công cụ trong 14 công cụ được hỗ trợ"
        }
      ]
    },
    {
      "label": "Collaborating with agents",
      "labelKey": "collaborating_with_agents",
      "pages": [
        {
          "slug": "assigning-issues",
          "title": "Giao issue cho agent",
          "content": "Giao một [issue](/issues) cho [agent](/agents) và nó làm việc với tư cách **người phụ trách chính thức** tới khi xong — nó đọc được toàn bộ ngữ cảnh issue (mô tả + mọi [bình luận](/comments)) và đổi trạng thái, đăng bình luận, sửa trường. Đây là đường kích hoạt **phổ biến và nặng nhất** trong bốn đường của Multica. Cùng luồng này cũng nhận một [đội](/squads) làm người phụ trách — khi đó Multica kích hoạt **agent leader** của đội.\n\n| Đường | Dùng khi | Đổi issue | Ngữ cảnh | Độ ưu tiên | Tự thử lại |\n|---|---|---|---|---|---|\n| **Giao việc** | Trao quyền sở hữu cho agent | Đổi người phụ trách | Issue + mọi bình luận | Kế thừa từ issue | ✓ |\n| [**@nhắc**](/mentioning-agents) | Kéo vào xem giúp | Không đổi gì | Issue + bình luận kích hoạt | Kế thừa từ issue | ✓ |\n| [**Chat**](/chat) | Trò chuyện một-một ngoài issue | Không dính issue | Lịch sử hội thoại hiện tại | Cố định trung bình | ✓ |\n| [**Tự động hóa**](/autopilots) | Tự động theo lịch hoặc thủ công | Tùy chế độ | Tùy chế độ | Do autopilot đặt | ✗ |\n\n\"Tự thử lại\" nghĩa là thử lại sau lỗi hạ tầng (runtime offline, timeout). Lỗi nghiệp vụ phía agent (ví dụ model báo lỗi) không được thử lại. Chi tiết xem [**Task**](/tasks).\n\n## Giao từ giao diện\n\nTrên trang chi tiết issue, bấm bộ chọn **Người phụ trách**. Nó liệt kê mọi thành viên trong không gian làm việc, mọi agent chưa lưu trữ và mọi [đội](/squads) chưa lưu trữ. Chọn một agent (hoặc đội) là issue được giao ngay.\n\nVài quy tắc:\n\n- **Agent workspace** ai cũng giao được; **agent private** chỉ người tạo hoặc admin của không gian làm việc giao được.\n- Chỉ giao được cho agent có **runtime đang online** — agent không ai chạy sẽ hiện là không khả dụng trong bộ chọn.\n- Khi issue ở trạng thái **Backlog**, giao việc **không kích hoạt agent** — Backlog là bãi đỗ; agent chỉ được xếp hàng khi bạn chuyển issue sang Todo hoặc In Progress.\n\n## Giao từ CLI\n\nLệnh tương đương:\n\n```bash\nmultica issue assign MUL-42 --to alice\nmultica issue assign MUL-42 --to-id 5fb87ac7-23b5-4a7a-81fa-ed295a54545d\n```\n\n`--to` nhận username của thành viên hoặc tên agent (khớp mờ). Khi tên chồng nhau — ví dụ agent `J` bên cạnh `Cursor - J` — dùng `--to-id <uuid>` với `user_id` (thành viên) hoặc `id` (agent) từ `multica workspace member list --output json` / `multica agent list --output json`. Khớp UUID là nghiêm ngặt và không mơ hồ — đúng thứ bạn cần cho script và cho agent điều khiển CLI. `--to` và `--to-id` loại trừ lẫn nhau.\n\nBỏ giao:\n\n```bash\nmultica issue assign MUL-42 --unassign\n```\n\n## Điều gì xảy ra sau khi giao\n\nKhi một issue không ở Backlog được giao cho agent, Multica lập tức làm các việc sau ở nền:\n\n1. Xếp hàng một `task` trạng thái `queued` với độ ưu tiên kế thừa từ issue, định tuyến tới runtime nơi agent sống.\n2. Daemon của agent nhận `task` ở lần poll kế tiếp và chuyển nó sang `dispatched`.\n3. Agent bắt đầu làm và `task` sang `running`; kết thúc thì thành `completed` hoặc `failed`.\n4. Trong lúc thực thi, agent có thể đổi trạng thái issue, đăng bình luận, sửa trường — các hành động này hiện dưới danh tính của agent.\n\n**Nếu agent offline**, `task` chờ trong hàng — **quá 5 phút thì timeout và fail** với lý do `runtime_offline`. Với nguồn cho phép thử lại (giao việc, @nhắc, chat), Multica tự xếp hàng lại. Xem [**Task**](/tasks) cho bộ quy tắc thử lại đầy đủ.\n\nGiao việc cũng tự đăng ký agent theo dõi issue — nhưng trong Multica **agent không nhận thông báo hộp thư** (chỉ thành viên nhận). Đăng ký này là sổ sách nội bộ, không có hiệu ứng nhìn thấy được.\n\n## Giao lại hoặc bỏ giao\n\nKhi bạn đổi người phụ trách từ Agent A sang Agent B:\n\n1. **Mọi thứ A đang chạy bị hủy** — mọi `task` ở trạng thái `queued`, `dispatched` hoặc `running` bị đánh dấu `cancelled`.\n2. **B được xếp hàng một `task` mới ngay** (nếu issue không ở Backlog và B có runtime online).\n\n> **Giao lại hủy mọi `task` đang hoạt động trên issue này — không chỉ của người phụ trách cũ.** Nếu một agent khác đang làm trên issue vì được @nhắc, `task` của nó cũng bị hủy. Hiện chưa có thao tác UI để hủy riêng `task` của một agent.\n\nBỏ giao (`--unassign` hoặc chọn \"none\" trong bộ chọn) đánh dấu mọi `task` đang hoạt động là `cancelled` và **không xếp hàng cái mới**. Các đăng ký theo dõi sẵn có không bị xóa tự động — người phụ trách cũ vẫn trong danh sách theo dõi (nhưng vẫn không nhận thông báo hộp thư).\n\n## Vì sao mỗi agent chỉ một `task` hoạt động trên mỗi issue\n\n**Một agent chỉ có tối đa một `task` ở `queued` hoặc `dispatched` trên cùng một issue tại một thời điểm.** Một unique index ở tầng cơ sở dữ liệu cộng logic nhận task thi hành điều này — nó chặn xếp hàng trùng và các lần thực thi song song ghi đè lẫn nhau.\n\nNhưng **các agent khác nhau làm song song trên cùng issue được** — ví dụ Agent A là người phụ trách và Agent B được @nhắc; hai `task` cùng tồn tại, mỗi cái chạy trên runtime riêng. Xem [**Task**](/tasks) cho bộ quy tắc tuần tự/song song đầy đủ.\n\n## Tiếp theo\n\n- [**@nhắc agent trong bình luận**](/mentioning-agents) — kích hoạt nhẹ hơn, giữ nguyên người phụ trách và trạng thái\n- [**Đội**](/squads) — giao cho một nhóm agent và để leader quyết ai nhận\n- [**Chat**](/chat) — trò chuyện một-một ngoài issue\n- [**Tự động hóa**](/autopilots) — để agent tự bắt đầu việc theo lịch"
        },
        {
          "slug": "mentioning-agents",
          "title": "@nhắc agent trong bình luận",
          "content": "`@`nhắc một [agent](/agents) trong [bình luận](/comments) là cách kích hoạt nhẹ hơn — **không đổi người phụ trách, không đổi trạng thái**, chỉ là cái huých để agent vào xem [issue](/issues) hiện tại. So với [**giao việc**](/assigning-issues) (biến agent thành chủ sở hữu và trao issue), @nhắc hợp với \"xem giúp đoạn này\", \"cho mình góc nhìn khác\", hay \"kéo vào bàn nhanh một chút\".\n\n## Nhắc agent trong bình luận\n\nGiống nhắc thành viên — gõ `@` để mở bộ chọn và chọn agent. Bình luận đăng xong, Multica lập tức xếp hàng một `task` cho từng agent được nhắc với **bình luận đó** làm ngữ cảnh kích hoạt. Khi nhận task, agent đọc được:\n\n- Toàn bộ issue (mô tả + mọi bình luận lịch sử)\n- Chính bình luận kích hoạt — làm điểm xuất phát cho lượt chạy này\n\nCú pháp Markdown của `@mention`, bộ chọn và ngữ nghĩa `@all` nói ở [**Bình luận**](/comments).\n\n> **Bạn cũng `@`nhắc được một [đội](/squads) trong bình luận.** Cùng bộ chọn hiện đội cạnh thành viên và agent; chọn một đội sẽ chèn `[@SquadName](mention://squad/<uuid>)` và kích hoạt **agent leader** của đội để điều phối phản hồi — người phụ trách và trạng thái giữ nguyên.\n\n## Khác giao việc thế nào\n\nCả hai đều đưa agent vào việc, nhưng cơ chế khác hẳn:\n\n| Khía cạnh | Giao việc | @nhắc |\n|---|---|---|\n| Đổi `assignee` | ✓ | ✗ |\n| Đổi `status` | ✗ | ✗ |\n| Xếp hàng `task` | Ngay lập tức (không ở Backlog) | Ngay lập tức |\n| ID bình luận kích hoạt | Tùy chọn | Luôn mang bình luận hiện tại |\n| Số agent mỗi thao tác | 1 (một người phụ trách) | Nhiều (một bình luận @ được nhiều agent) |\n| Độ ưu tiên | Kế thừa từ issue | Kế thừa từ issue |\n\nQuy tắc ngón tay cái rất gọn: **giao việc khi muốn agent \"sở hữu issue này từ giờ\"; @nhắc khi muốn nó \"xem ngữ cảnh hiện tại\".**\n\n## Điều gì xảy ra khi @ nhiều agent\n\nNếu một bình luận @nhắc nhiều agent, mỗi agent được xếp hàng một `task` độc lập trên runtime riêng — **chúng chạy song song** không chặn nhau.\n\nNếu agent đã có `task` ở `queued` hoặc `dispatched` trên cùng issue (ví dụ vừa được nhắc và chưa bắt đầu), lượt nhắc mới bị **khử trùng lặp** và không xếp hàng `task` trùng. Khử trùng lặp **tính trong một bình luận** — hai bình luận khác nhau cách nhau vài giây cùng @ một agent thì cả hai đều xếp hàng `task`.\n\n> **Thêm @ bằng cách sửa bình luận không kích hoạt lại.** Nếu bạn nhớ ra `@agent` sau khi đã đăng, sửa để thêm `@` chỉ đổi phần hiển thị — nó **không** gửi `task` mới cho agent đó. Muốn kích hoạt, đăng bình luận mới hoặc giao issue cho nó.\n\n## `@all` không kích hoạt agent nào\n\nKhi bạn gọi mọi người bằng `@all`, **chỉ thành viên nhận thông báo hộp thư — agent không nằm trong phép mở rộng của `@all`.** Đây là chủ ý: agent không nhận thông báo hộp thư, nên `@all` vô nghĩa với chúng. Muốn agent làm việc, nhắc đích danh nó.\n\n## Agent tự @nhắc mình không tạo vòng lặp\n\nAgent có thể đăng bình luận trong lúc thực thi, và bình luận đó có thể chứa `@mention`. Multica có lớp chặn cứng: **nếu tác giả bình luận trùng với agent được `@` nhắc, mention đó bị bỏ qua** — không có vòng lặp vô hạn \"agent A @ agent A → task mới → lại @ agent A\".\n\nLớp chặn này **chỉ chặn tự tham chiếu trực tiếp.** Agent A @nhắc agent B vẫn bình thường; nếu B rồi @nhắc lại A trong câu trả lời, A được kích hoạt lại — nói cách khác, **đệ quy gián tiếp không bị chặn**. Khi viết chỉ dẫn cho agent, cẩn thận đừng để một nhóm agent @nhắc nhau thành vòng.\n\n## Tiếp theo\n\n- [**Đội**](/squads) — `@`nhắc một đội để leader định tuyến câu hỏi tới đúng thành viên\n- [**Chat**](/chat) — trò chuyện một-một ngoài issue\n- [**Tự động hóa**](/autopilots) — để agent tự bắt đầu việc theo lịch\n- [**Bình luận**](/comments) — cú pháp `@mention`, bộ chọn và ngữ nghĩa `@all`"
        },
        {
          "slug": "chat",
          "title": "Chat",
          "content": "**Chat là cuộc trò chuyện một-một giữa bạn và một [agent](/agents)** — bước ra ngoài bảng [issue](/issues). Agent không thấy issue nào và không đổi được issue nào, và toàn bộ cuộc trò chuyện **hoàn toàn riêng tư** (không ai khác trong [không gian làm việc](/workspaces), kể cả admin, thấy được). Nó hợp để bàn hướng làm với agent, brainstorm, hoặc hỏi một câu không thuộc issue nào.\n\n## Sao không @nhắc agent luôn?\n\n[@nhắc](/mentioning-agents) **kéo agent vào** ngữ cảnh của một issue — nó đọc mô tả issue và mọi bình luận lịch sử, và có thể đổi issue. Chat đảo ngược: **nó kéo bạn ra khỏi** issue — agent chỉ thấy đúng cuộc trò chuyện này, không biết gì về issue nào, và không có lối vào để sửa issue.\n\nHai quy tắc ngón tay cái:\n\n- Muốn phản hồi bám ngữ cảnh của một issue cụ thể → [@nhắc](/mentioning-agents)\n- Muốn bàn một chủ đề không liên quan issue nào (hoặc không muốn ai khác thấy cuộc bàn luận) → Chat\n\n## Bắt đầu cuộc trò chuyện\n\nMở **Chat** từ thanh bên, chọn một agent và bắt đầu cuộc trò chuyện mới. Giao diện như mọi app nhắn tin: bạn gửi tin, agent trả lời. Mỗi tin nhắn kích hoạt một lượt chạy ở nền (một `task` được xếp hàng), nên câu trả lời có thể mất vài giây.\n\n## Agent làm được và không làm được gì trong chat\n\nAgent chạy ở chế độ **sandbox hoàn toàn** trong cuộc trò chuyện.\n\n**Làm được:**\n\n- Trả lời câu hỏi trong tin nhắn hiện tại của bạn\n- Dùng các [skill](/skills) và MCP đã cấu hình\n- Đọc ghi file trong thư mục làm việc của chính nó\n- Gọi các lệnh CLI `multica` không cần ngữ cảnh issue (ví dụ truy vấn thông tin cơ bản của không gian làm việc)\n\n**Không làm được:**\n\n- **Thấy issue** — prompt agent nhận không có ID issue nào, và lệnh như `multica issue list` trả về rỗng\n- **Đổi issue** — không có ngữ cảnh issue, các lệnh gọi API bị chặn bởi kiểm tra quyền\n- **Thấy cuộc trò chuyện khác** — các cuộc trò chuyện cách ly hoàn toàn\n- **@nhắc ai hay agent nào** — chat là không gian riêng, không có đường thông báo người khác\n\n## Ngữ cảnh nhiều lượt được giữ thế nào\n\nChat giữ ngữ cảnh nhiều lượt qua **khôi phục phiên của provider** — agent lập một phiên provider ở câu trả lời đầu (ví dụ một phiên Claude), và ID phiên được lưu. Ở tin nhắn kế, lượt phát task truyền ID đó lại để agent **tiếp nối từ chỗ dừng** mà không phải đọc lại lịch sử mỗi lần.\n\nNếu **một lượt lỗi**, Multica tra ngược task trước đó từng lập ID phiên (dù task đó thành công hay thất bại) và thử nối lại — một lần lỗi giữa chừng không làm mất trí nhớ của cả cuộc trò chuyện.\n\nLưu ý: không phải provider nào cũng thực sự có khôi phục phiên — xem [**Ma trận provider**](/providers) cho hiện trạng hỗ trợ.\n\n## Lưu trữ cuộc trò chuyện\n\nCuộc trò chuyện không muốn thấy nữa có thể lưu trữ — chuột phải trong danh sách hoặc dùng nút \"Archive\" trên trang chi tiết. Sau khi lưu trữ:\n\n- Cuộc trò chuyện biến khỏi danh sách hoạt động (vẫn tìm được trong khung nhìn \"Archived\")\n- Tin nhắn lịch sử, ID phiên và thư mục làm việc đều được giữ — không gì bị xóa\n\n> **Không có nút \"khôi phục\" sau khi lưu trữ.** Hiện chưa có lối đưa cuộc trò chuyện đã lưu trữ về lại hoạt động. Muốn tiếp tục mạch chuyện, bạn phải mở cuộc trò chuyện mới. Muốn xem lại nội dung đã lưu trữ, mở khung nhìn \"Archived\" và đọc lịch sử.\n\n## Tiếp theo\n\n- [**Tự động hóa**](/autopilots) — để agent tự bắt đầu việc theo lịch\n- [**Giao issue cho agent**](/assigning-issues) — đưa chủ đề trở lại bảng issue"
        },
        {
          "slug": "autopilots",
          "title": "Tự động hóa",
          "content": "Tự động hóa (autopilot) để [agent](/agents) **tự bắt đầu việc theo lịch** — cấu hình một biểu thức cron cùng múi giờ, và Multica tự phát một [`task`](/tasks) mà bạn không phải kích hoạt gì. Nó hợp với kiểm tra định kỳ, báo cáo lặp lại và các việc dọn dẹp qua đêm — dạng công việc \"lệnh thường trực\". So với ba đường kích hoạt còn lại ([giao việc](/assigning-issues), [@nhắc](/mentioning-agents) và [chat](/chat), nơi bạn là người khởi động), khác biệt cốt lõi của tự động hóa là **thời gian điều khiển**.\n\n## Cấu hình autopilot\n\nTạo autopilot mới trên trang **Autopilot** của không gian làm việc. Bạn đặt:\n\n- **Tên** — tên hiển thị\n- **Agent** — lượt chạy được phát cho ai\n- **Độ ưu tiên** — `task` sinh ra kế thừa nó (cùng ngữ nghĩa với độ ưu tiên issue)\n- **Mô tả / prompt** — mô tả công việc agent nhận mỗi lượt chạy\n- **Chế độ thực thi** — xem bên dưới\n- **Trigger** — ít nhất một `schedule` (cron + múi giờ) hoặc `webhook`\n\n## Chọn chế độ thực thi\n\nAutopilot có hai chế độ thực thi. **Bắt đầu với chế độ \"tạo issue\".**\n\n- **Chế độ tạo issue** (`create_issue`) — mặc định, **khuyến nghị**. Mỗi lượt kích hoạt trước tiên tạo một issue trong không gian làm việc (tiêu đề hiện hỗ trợ một placeholder duy nhất, `{{date}}`, nội suy thành ngày UTC dạng `YYYY-MM-DD`; mọi token `{{...}}` khác bị từ chối ngay lúc tạo để lỗi gõ không lặng lẽ thành chuỗi thô trong tiêu đề issue của bạn), rồi giao issue cho agent qua luồng giao việc bình thường. Mọi công việc nằm trên bảng issue với đầy đủ lịch sử, bình luận và trạng thái như issue giao tay.\n- **Chế độ chỉ chạy** (`run_only`) — bỏ qua tạo issue và xếp hàng `task` trực tiếp. Lượt chạy vô hình trên bảng — chỉ thấy trong lịch sử chạy của autopilot.\n\n## Chạy theo lịch\n\nMỗi autopilot cần ít nhất một trigger `schedule`. Cron dùng **định dạng 5 trường chuẩn** (phút giờ ngày tháng thứ), độ mịn tối thiểu **1 phút** (không có giây). Múi giờ theo định dạng IANA (ví dụ `Asia/Ho_Chi_Minh`) và quyết định biểu thức cron được hiểu theo múi giờ nào.\n\nVài ví dụ:\n\n- `0 9 * * 1-5`, `Asia/Ho_Chi_Minh` — 9 giờ sáng giờ Việt Nam các ngày trong tuần\n- `*/30 * * * *`, `UTC` — mỗi 30 phút\n- `0 3 * * *`, `UTC` — mỗi ngày 3 giờ sáng UTC\n\nMultica server quét trigger tới hạn mỗi **30 giây** — **thời điểm nổ thật có thể trễ tới 30 giây**, không chính xác tới giây. Nếu server khởi động lại qua một thời điểm nổ, nó đuổi kịp các trigger bị lỡ khi bật (không mất gì, nhưng chúng nổ ngay lập tức).\n\n## Kích hoạt một lần thủ công\n\nĐể khỏi chờ cron khi đang debug autopilot, kích hoạt tay:\n\n- UI: bấm \"Run now\" trên trang chi tiết autopilot\n- CLI:\n\n```bash\nmultica autopilot trigger <autopilot-id>\n```\n\nKích hoạt tay đi đúng luồng thực thi như trigger `schedule` — chỉ trường `source` trên bản ghi chạy được đánh `manual`.\n\n## Kích hoạt từ webhook\n\nAutopilot cũng nổ theo webhook HTTP gửi vào. Thêm trigger **Webhook** trên trang chi tiết autopilot; Multica sinh một URL duy nhất dạng:\n\n```\nhttps://<your-multica-host>/api/webhooks/autopilots/awt_…\n```\n\nPOST JSON bất kỳ vào URL đó — Multica ghi một lượt chạy với `source = webhook`, lưu body làm `trigger_payload` của lượt chạy, và phát agent y hệt trigger theo lịch.\n\n```bash\ncurl -X POST \"$MULTICA_WEBHOOK_URL\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"event\":\"demo.received\",\"eventPayload\":{\"message\":\"hello\"}}'\n```\n\nỞ **chế độ tạo issue**, payload gửi vào được nối vào mô tả của issue mới để agent đọc trực tiếp. Ở **chế độ chỉ chạy**, payload là một phần ngữ cảnh chạy mà daemon trao cho agent.\n\n### Hình dạng payload\n\nBạn có thể gửi phong bì của riêng mình:\n\n```json\n{ \"event\": \"github.pull_request.opened\", \"eventPayload\": { } }\n```\n\n…hoặc bất kỳ object/array JSON nào. Multica chuẩn hóa thành phong bì nội bộ:\n\n```json\n{\n  \"event\": \"<inferred>\",\n  \"eventPayload\": <your body>,\n  \"request\": { \"receivedAt\": \"<rfc3339>\", \"contentType\": \"application/json\" }\n}\n```\n\nKhi bạn không cung cấp trường `event`, Multica suy ra từ các header và trường body phổ biến (`X-GitHub-Event` + `action` trong body, `X-Gitlab-Event`, `X-Event-Type`, `event`/`type`/`action` trong body). Không khớp gì thì event là `webhook.received`.\n\nKhi cấu hình GitHub hay nguồn tương tự, đặt content type là `application/json` — payload webhook mã hóa form không được chấp nhận.\n\n### Bộ lọc sự kiện\n\nTrigger webhook mới nổ với mọi POST gửi vào — ổn cho URL một mục đích nhưng ồn ào với các nguồn tỏa nhiều loại sự kiện (GitHub là ví dụ rõ nhất — một webhook repo có thể gửi `push`, `pull_request`, `workflow_run`, `check_suite` và hơn nữa). Mục **Event filters** trên trigger webhook cho bạn giới hạn sự kiện nào thực sự phát lượt chạy; phần còn lại được ghi vào lịch sử giao nhận với `status = ignored` và `reason = event_filtered`, không tạo lượt chạy hay issue nào.\n\nMỗi dòng là một quy tắc: một **tên sự kiện** cùng danh sách **actions** tùy chọn phân tách bởi dấu phẩy. Multica cho qua nếu **bất kỳ** dòng nào khớp; để trống cả mục để nhận tất cả (hành vi trước khi có bộ lọc).\n\nVí dụ:\n\n| Tên sự kiện     | Actions             | Khớp                                                                     |\n| -------------- | ------------------- | ------------------------------------------------------------------------ |\n| `workflow_run` | `completed, failed` | Chỉ các sự kiện `workflow_run` với `action: completed` hoặc `action: failed` |\n| `workflow_run` | _(trống)_           | Mọi sự kiện `workflow_run`, bất kể action                                 |\n| `push`         | _(trống)_           | Mọi sự kiện `push`                                                        |\n\n#### Tên sự kiện và action lấy từ đâu\n\nMultica suy `event` và `action` từ request gửi vào theo thứ tự này — **khớp đầu tiên thắng**.\n\n**1. Phong bì body.** Nếu body là object JSON có trường `event` dạng chuỗi, giá trị đó là tên sự kiện luôn. Object `eventPayload` tùy chọn khi đó cấp ứng viên action từ các trường `action` / `state` / `conclusion` / `status` của nó.\n\n```bash\ncurl -X POST \"$MULTICA_WEBHOOK_URL\" \\\n  -H 'Content-Type: application/json' \\\n  -d '{\"event\":\"trigger\",\"eventPayload\":{\"action\":\"true\"}}'\n# suy ra: event = trigger, ứng viên action = true\n```\n\n**2. Header.** Không có phong bì body thì Multica đọc các header provider quen thuộc:\n\n- `X-GitHub-Event: <event>` — ghép với trường `action` cấp cao nhất của body (nếu có) thành `github.<event>.<action>`.\n- `X-Gitlab-Event: <event>` — thành `gitlab.<event>`.\n- `X-Event-Type: <event>` — truyền qua nguyên vẹn.\n\n```bash\n# Kiểu GitHub: header cho tên sự kiện, body cho action.\ncurl -X POST \"$MULTICA_WEBHOOK_URL\" \\\n  -H 'X-GitHub-Event: workflow_run' \\\n  -H 'Content-Type: application/json' \\\n  -d '{\"action\":\"completed\"}'\n# suy ra: event = github.workflow_run.completed\n#        → khớp dòng lọc workflow_run / completed\n\n# Header event-type chung — không cần trường body nào.\ncurl -X POST \"$MULTICA_WEBHOOK_URL\" \\\n  -H 'X-Event-Type: trigger.true' \\\n  -H 'Content-Type: application/json' \\\n  -d '{}'\n# suy ra: event = trigger.true → khớp trigger / true\n```\n\n**3. Body dự phòng.** Nếu không có phong bì body lẫn header quen thuộc, Multica lùi về các trường chuỗi cấp cao nhất của body theo thứ tự: `event` → `type` → `action`.\n\n```bash\ncurl -X POST \"$MULTICA_WEBHOOK_URL\" \\\n  -H 'Content-Type: application/json' \\\n  -d '{\"type\":\"trigger\",\"action\":\"true\"}'\n# suy ra: event = trigger (từ `type`), ứng viên action = true\n```\n\n**4. Mặc định.** Không gì khớp thì event là `webhook.received` và không có ứng viên action.\n\n**Ứng viên action, đầy đủ.** Khi event đã xác định, Multica xét mọi giá trị dưới đây làm ứng viên khớp action:\n\n- Hậu tố tên sự kiện, khi event có dạng `provider.event.<action>` (ví dụ `github.workflow_run.completed` → `completed`).\n- Các trường body `action`, `state`, `conclusion` và `status` — **chỉ khi chúng là chuỗi JSON**. Boolean (`{\"action\": true}`) hay số không tính, nên bộ lọc mong `event=trigger, action=true` không bao giờ khớp body `{\"trigger\": true}` vì `true` là bool, không phải chuỗi.\n\n**Bẫy thường gặp.** Dòng lọc kiểu `Event name: trigger` / `Actions: true` **không** có nghĩa \"nổ khi body có `trigger: true`\" — Event filters khớp *event và action đã suy ra*, không phải trường body tùy ý. Gửi `trigger.true` qua `X-Event-Type` (hoặc dùng phong bì body ở trên) để trúng nó. Khoảng trắng bao quanh trong dòng lọc đã lưu (`\" workflow_run \"`) được lưu nguyên vẹn và không bao giờ khớp — cắt trước khi lưu.\n\n#### Thử nhanh\n\nCấu hình lọc xong, xác nhận cả hai nhánh bằng `curl`:\n\n```bash\n# Cho qua — header cho event=workflow_run, body cho action=completed\ncurl -X POST \"$MULTICA_WEBHOOK_URL\" \\\n  -H 'X-GitHub-Event: workflow_run' \\\n  -H 'Content-Type: application/json' \\\n  -d '{\"action\":\"completed\"}'\n# → 200 {\"status\":\"accepted\", ...}\n\n# Bị lọc — cùng event, action không trong danh sách cho phép\ncurl -X POST \"$MULTICA_WEBHOOK_URL\" \\\n  -H 'X-GitHub-Event: workflow_run' \\\n  -H 'Content-Type: application/json' \\\n  -d '{\"action\":\"in_progress\"}'\n# → 200 {\"status\":\"ignored\",\"reason\":\"event_filtered\"}\n```\n\n### URL là một bí mật bearer\n\nURL được sinh **chính là** thông tin xác thực. Ai có nó đều nổ được autopilot. Đối xử với nó như một token:\n\n- **Đừng dán vào luồng issue công khai, ảnh chụp màn hình hay lịch sử chat.**\n- **Xoay nó nếu lộ** — bấm \"Rotate URL\" trên dòng trigger, hoặc chạy `multica autopilot trigger-rotate-url <autopilot-id> <trigger-id>`. URL cũ ngừng hoạt động ngay.\n- Với nguồn cần xác thực nguồn mạnh, chờ xác minh chữ ký HMAC theo trigger; URL v1 này chỉ có bearer.\n- Thành viên xem được autopilot hiện đọc được các URL webhook của nó — kiểm soát bí mật chặt hơn theo vai trò là việc tiếp nối.\n\n### Ngữ nghĩa mã trạng thái\n\nMultica trả `200 OK` kèm trường `status` cho các kết quả no-op bình thường để bộ máy retry webhook của provider không dội URL liên tục:\n\n- `{\"status\":\"accepted\",\"run_id\":\"…\",\"autopilot_id\":\"…\",\"trigger_id\":\"…\"}` — một lượt chạy đã được phát.\n- `{\"status\":\"skipped\",\"run_id\":\"…\",\"reason\":\"agent runtime is offline at dispatch time\"}` — với autopilot `run_only`, runtime của người nhận offline và không task nào xếp hàng. Autopilot `create_issue` vẫn tạo issue theo dõi để công việc luôn nhìn thấy và được nhận khi runtime trở lại.\n- `{\"status\":\"ignored\",\"reason\":\"trigger_disabled\"}` — trigger đang tắt.\n- `{\"status\":\"ignored\",\"reason\":\"autopilot_paused\"}` — autopilot đang tạm dừng.\n- `{\"status\":\"ignored\",\"reason\":\"autopilot_archived\"}` — autopilot đã lưu trữ.\n\nPhản hồi non-2xx cho lỗi thật:\n\n- `400` — JSON sai, body vô hướng, hoặc body rỗng.\n- `404` — token lạ (`{\"error\":\"webhook not found\"}`).\n- `413` — payload vượt 256 KiB.\n- `429` — vượt hạn mức theo token (mặc định 60 req/phút).\n\n### Tự vận hành: cấu hình URL công khai\n\nKhi `MULTICA_PUBLIC_URL` được đặt trên server (ví dụ `https://multica.example.com`), phản hồi trigger có `webhook_url` tuyệt đối và UI hiện URL sẵn sàng chép. Không có nó, UI ghép URL từ origin API của client — ổn với desktop và web cùng origin, nhưng không ổn với reverse proxy tự vận hành tùy chỉnh. Multica chủ ý không suy host công khai từ header `Host` / `X-Forwarded-Host` để reverse proxy cấu hình sai không lừa được server đúc URL webhook trỏ về host của kẻ tấn công.\n\n## Xem lịch sử chạy\n\nMỗi lượt kích hoạt sinh một **bản ghi chạy**, xem ở tab \"History\" trên trang chi tiết autopilot:\n\n- Nguồn kích hoạt (`schedule` / `manual` / `webhook`)\n- Thời điểm bắt đầu, thời điểm hoàn thành\n- Trạng thái (`issue_created` / `running` / `completed` / `failed` / `skipped`)\n- Issue liên kết (chế độ tạo issue) hoặc `task` (chế độ chỉ chạy)\n- Lý do thất bại (nếu failed hoặc skipped)\n\n## Điều gì xảy ra khi autopilot thất bại\n\n> **Autopilot thất bại không được tự thử lại và không gửi thông báo hộp thư.** Thất bại để lại một mục `failed` trong lịch sử chạy — không có xếp hàng lại cấp hệ thống như giao việc hay @nhắc, và không thông báo cho ai. Nếu autopilot có chu kỳ, **lần nổ cron kế sẽ kích hoạt lượt chạy mới**, nhưng phần việc thất bại không tự chạy lại.\n>\n> Nếu autopilot quan trọng, tự thiết kế giám sát — ví dụ để agent đăng bình luận khi thành công, và bắt thất bại bằng cách nhận ra bình luận vắng mặt.\n\nVì sao không tự thử lại: autopilot vốn đã định kỳ, nên thêm thử lại cấp hệ thống sẽ chồng lên lần chạy kế theo lịch và tạo thực thi trùng. Để lịch hoàn toàn cho cron giữ mọi thứ sạch sẽ.\n\n## Những gì chưa có\n\n**Trigger loại API chưa được nối dây.** Schema trigger có dự trữ loại `api`, nhưng không route ingress nào nổ nó; UI hiện huy hiệu Deprecated cho các dòng sẵn có và không có nút chép/xoay. Xác minh chữ ký HMAC theo trigger, danh sách IP cho phép và preset sự kiện theo provider được theo dõi làm việc tiếp nối; URL v1 chỉ có bearer.\n\n## Tiếp theo\n\n- [**Giao issue cho agent**](/assigning-issues) — trao một issue cho agent một lần\n- [**@nhắc agent trong bình luận**](/mentioning-agents) — kéo agent vào xem từ một bình luận\n- [**Chat**](/chat) — trò chuyện một-một ngoài issue"
        }
      ]
    },
    {
      "label": "Inbox",
      "labelKey": "inbox",
      "pages": [
        {
          "slug": "inbox",
          "title": "Hộp thư đến và theo dõi",
          "content": "Hộp thư đến là nơi Multica **ngắt lời** bạn — [issue](/issues) giao cho bạn, [nhắc `@`](/comments) và hoạt động trên các issue bạn theo dõi đều đổ về đây.\n\nBạn kiểm soát hoạt động issue nào chạm tới mình bằng cách **theo dõi** và **bỏ theo dõi**.\n\n## Những gì xuất hiện trong hộp thư\n\nCác sự kiện sau gửi thông báo vào hộp thư của bạn:\n\n- **Issue được giao / bỏ giao / giao lại** — bạn được báo khi là người phụ trách mới (hoặc cũ)\n- **Trạng thái, độ ưu tiên hoặc hạn chót đổi trên issue bạn theo dõi**\n- **Bình luận mới trên issue bạn theo dõi**\n- **Bạn được `@`nhắc trong bình luận** — gửi tới dù bạn có theo dõi hay không\n- **Ai đó thả cảm xúc vào issue hoặc bình luận của bạn**\n- **Một [task](/tasks) agent do bạn giao thất bại**\n\n## `@all` thông báo cả không gian làm việc\n\n`@all` là đích đặc biệt — nó đẩy thông báo tới **mọi thành viên** của không gian làm việc.\n\n> **Dùng `@all` tiết kiệm.** Trong không gian làm việc 50 người, một bình luận `@all` sinh 50 thông báo hộp thư ngay lập tức. Dành nó cho sự kiện hệ trọng (sự cố production, công bố cột mốc) — không phải bàn luận thường ngày.\n\n## Agent không bao giờ nhận thông báo\n\nAgent **không bao giờ** nhận thông báo hộp thư — kể cả khi là người phụ trách, người tạo, hay được `@`nhắc trong bình luận.\n\nĐây không phải bug: agent không đọc hộp thư. Chúng làm việc theo cơ chế [**kích hoạt tức thời**](/assigning-issues) — giao issue hoặc `@`nhắc agent trong bình luận là khởi động task cho nó ngay. Hộp thư là cơ chế nhắc dành cho con người; với agent nó vô nghĩa.\n\n## Quy tắc theo dõi\n\nBạn được **tự động theo dõi** một issue trong bốn tình huống:\n\n- Bạn **tạo** nó\n- Bạn được **giao** nó\n- Bạn **bình luận** trên nó\n- Bạn được **`@`nhắc** trên nó hoặc trong một bình luận của nó\n\nTự theo dõi chỉ xảy ra một lần — vừa là người tạo vừa được nhắc không khiến bạn theo dõi hai lần.\n\n> **Giao lại không tự bỏ theo dõi cho bạn.** Nếu bạn từng là người phụ trách và bị thay, bạn **vẫn nhận cập nhật trên issue đó** — bản ghi tự theo dõi vẫn nằm trong cơ sở dữ liệu.\n>\n> Muốn ngừng nhận thông báo, mở issue và bỏ theo dõi thủ công.\n\nBạn cũng có thể **theo dõi thủ công** bất kỳ issue nào (kể cả không liên quan), hoặc **bỏ theo dõi thủ công** bất kỳ lượt tự theo dõi nào. Trên UI, dùng khung bên phải của trang issue; trên CLI, dùng `multica issue subscriber add/remove`.\n\n## Đổi trạng thái issue con nổi lên issue cha\n\nKhi **trạng thái** của issue con đổi, người theo dõi issue cha cũng được báo — kể cả khi họ không theo dõi issue con.\n\nĐiều này áp dụng cho **trạng thái mà thôi**: thay đổi bình luận, độ ưu tiên và hạn chót trên issue con **không** nổi lên.\n\n## Tiếp theo\n\n- [Bình luận và nhắc tên](/comments) — nhắc `@` hoạt động ra sao và các bẫy\n- [Giao issue cho agent](/assigning-issues) — agent được kích hoạt thế nào (và vì sao chúng không đọc hộp thư)"
        }
      ]
    },
    {
      "label": "Integrations",
      "labelKey": "integrations",
      "pages": [
        {
          "slug": "channels",
          "title": "Tích hợp chat (kênh)",
          "content": "Một **kênh** nối một [agent](/agents) Multica với một nền tảng chat để nhóm của bạn làm việc với nó ngay nơi họ vẫn trò chuyện. Hiện có hai kênh — [Lark (飞书)](/lark-bot-integration) và [Slack](/slack-bot-integration) — và cả hai chạy trên **cùng một bộ máy**: lõi trung lập nền tảng cộng một adapter mỏng theo nền tảng. Thêm nền tảng mới là \"viết adapter\", không phải \"xây lại pipeline\".\n\nMột **installation** là đơn vị gắn kết mọi thứ: một bot buộc vào một cặp `(không gian làm việc, agent)`. Tin nhắn gửi vào được định tuyến tới một installation, rồi qua pipeline chung; câu trả lời của agent được gửi lại đúng cuộc chat.\n\n## Kiến trúc\n\n```mermaid\nflowchart LR\n  subgraph P[\"Nền tảng chat\"]\n    LK[\"Lark / 飞书\"]\n    SL[\"Slack\"]\n  end\n  subgraph ENG[\"Bộ máy kênh (trung lập nền tảng)\"]\n    direction TB\n    SUP[\"Supervisor<br/>một kết nối sống cho mỗi installation\"]\n    ROU[\"Pipeline router:<br/>route → dedup → auth → session → trigger\"]\n  end\n  LK -->|kết nối dài| SUP\n  SL -->|Socket Mode| SUP\n  SUP -->|sự kiện thô| ADP[\"Adapter theo nền tảng<br/>dịch + ResolverSet\"]\n  ADP --> ROU\n  ROU -->|task cho agent| RUN[\"Daemon chạy agent\"]\n  RUN -->|trả lời| OUT[\"Gửi ra theo nền tảng<br/>(bot token → API nền tảng)\"]\n  OUT --> P\n```\n\n## Pipeline nhận tin (chung)\n\nMọi tin nhắn gửi vào — Lark hay Slack — chạy qua cùng các bước theo thứ tự trong `Router` của bộ máy. Adapter nền tảng chỉ cấp các mảnh theo nền tảng (`ResolverSet`); chính sách nằm trong bộ máy.\n\n1. **Định tuyến tới installation** — ánh xạ sự kiện tới một `channel_installation` (→ không gian làm việc + agent). Lark định tuyến theo `app_id`; Slack theo app id mang trên sự kiện.\n2. **Lọc theo cách xưng hô** — trong nhóm/kênh, chỉ tin nhắn **@nhắc bot** mới đi tiếp; chuyện phiếm trong nhóm bị bỏ (không đọc).\n3. **Khử trùng lặp** — cơ chế nhận hai pha theo `(installation, message_id)` đảm bảo xử lý đúng-một-lần, kể cả qua nhiều bản sao server.\n4. **Danh tính + ủy quyền** — phân giải id người gửi trên nền tảng về một người dùng Multica ([liên kết tài khoản](#authorization)), rồi kiểm tra lại tư cách thành viên không gian làm việc. Người chưa liên kết nhận lời nhắc \"liên kết tài khoản\"; người ngoài bị bỏ.\n5. **Phiên** — tìm hoặc tạo một [phiên chat](/chat) cho cuộc trò chuyện này và nối tin nhắn vào (xem [Phiên](#sessions-and-context)).\n6. **Kích hoạt** — xếp hàng một [task](/tasks) cho agent; một [daemon](/daemon-runtimes) chạy agent và câu trả lời được gửi lại vào cuộc chat.\n\n## Phiên và ngữ cảnh\n\nNgữ cảnh của agent là **bản ghi phiên chat** — các tin nhắn đã nạp vào phiên đó theo thời gian. Mô hình bản ghi này là chung (mọi kênh dùng). Khác nhau theo nền tảng là **khóa cách ly phiên** mà adapter ghép:\n\n| Nền tảng | Khóa cách ly | Hiệu ứng |\n|---|---|---|\n| **Lark / 飞书** | id cuộc chat | Một phiên cho mỗi chat/nhóm — các lượt liên tiếp trong cùng chat dồn vào một bản ghi (nhớ nhiều lượt). |\n| **Slack** | DM: kênh; kênh: `kênh + gốc thread` | Mỗi DM là một phiên; **mỗi thread @bot là phiên riêng**, nên hai thread trong một kênh không trộn lẫn. |\n\n> Trong nhóm, chỉ tin nhắn **@nhắc bot** được nạp. Hiện cả hai kênh đều không đọc các tin nhắn khác (không @) hay lịch sử cũ của kênh, nên agent không thấy tin nhắn nó không được gọi tên. Lấy lịch sử xung quanh làm ngữ cảnh là cải tiến đã lên kế hoạch.\n\n## Ủy quyền\n\nHai cổng độc lập bảo vệ bot trong nhóm chung — cả hai do bộ máy thi hành cho mọi tin nhắn, giống hệt cho Lark và Slack:\n\n- **Liên kết tài khoản (xác thực)** — id người gửi trên nền tảng phải được liên kết với một người dùng Multica. Lần đầu ai đó nhắn bot, họ nhận một liên kết một lần để buộc danh tính với tài khoản Multica **của chính họ**; trước đó không agent nào chạy.\n- **Tư cách thành viên (ủy quyền)** — người dùng Multica đã liên kết phải là thành viên của không gian làm việc của installation, kiểm tra lại ở mỗi tin nhắn. Người ngoài bị bỏ lặng lẽ.\n\nNên thêm bot vào kênh công khai vẫn an toàn: chỉ thành viên đã liên kết danh tính mới điều khiển được agent, và từng người gửi được kiểm tra độc lập. Xem trang từng nền tảng cho các lời nhắc phía người dùng.\n\n## Hai kênh\n\n> **Lark (飞书) — quét để cài.** Admin của không gian làm việc buộc một agent bằng cách quét QR bằng app Lark; không cần bước developer console nào. Mỗi agent một Bot. Xem [Tích hợp Lark Bot](/lark-bot-integration).\n\n> **Slack — tự mang app.** Admin của không gian làm việc tạo một app Slack, cài vào Slack workspace của mình, rồi dán bot token + app-level token vào Multica. Mỗi agent có app Slack riêng, nên nhiều agent có thể mỗi con một bot riêng trong một Slack workspace. Xem [Tích hợp Slack Bot](/slack-bot-integration) để lấy manifest và các bước cài đặt.\n\n## Tự vận hành\n\nMỗi kênh **tắt cho tới khi bạn đặt khóa mã hóa at-rest của nó** (khóa mã hóa token của từng bot trước khi chạm cơ sở dữ liệu):\n\n```dotenv\nMULTICA_LARK_SECRET_KEY=<khóa 32 byte mã hóa base64>\nMULTICA_SLACK_SECRET_KEY=<khóa 32 byte mã hóa base64>\n```\n\nTrên Multica Cloud cả hai đã cấu hình sẵn. Xem [Biến môi trường](/environment-variables) cho tham chiếu đầy đủ.\n\n## Tiếp theo\n\n- [Tích hợp Lark Bot](/lark-bot-integration) — quét để cài, DM / @nhắc / `/issue`\n- [Tích hợp Slack Bot](/slack-bot-integration) — tự mang app (manifest + token), bot theo từng agent\n- [Agent](/agents) · [Chat](/chat) · [Task](/tasks)"
        },
        {
          "slug": "github-integration",
          "title": "Tích hợp GitHub",
          "content": "Kết nối một tài khoản hoặc tổ chức GitHub một lần trong **Cài đặt → GitHub**. Sau đó, bất kỳ pull request nào có tên nhánh, tiêu đề hoặc phần thân chứa mã issue (ví dụ `MUL-123`) được **tự liên kết** vào [issue](/issues) đó, xuất hiện dưới mục **Pull requests** ở thanh bên của issue, và — khi PR được merge — chuyển issue sang **Done**.\n\nKhông cần thiết lập theo từng issue. Toàn bộ luồng chạy theo mã định danh.\n\n## Tích hợp làm gì\n\n| Bề mặt | Hành vi |\n|---|---|\n| **Cài đặt → GitHub** | Admin của không gian làm việc thấy tab GitHub với công tắc tổng, nút **Connect GitHub** và các công tắc tính năng (thanh bên PR, Co-authored-by, tự liên kết). Cài xong bạn được đưa về tab GitHub. |\n| **Thanh bên issue → Pull requests** | Mọi PR tự liên kết vào issue này, kèm tiêu đề, repo, trạng thái (`Open` / `Draft` / `Merged` / `Closed`) và tác giả. Bấm một dòng để nhảy tới PR trên GitHub. |\n| **Webhook (nền)** | Với mỗi sự kiện `pull_request`, Multica upsert dòng PR, quét PR tìm mã issue và dựng (lại) các dòng liên kết. Idempotent — phát lại một lượt giao nhận là no-op. |\n| **Tự đổi trạng thái khi merge** | Khi PR chuyển sang `merged`, mọi issue liên kết chưa ở `Done` hoặc `Cancelled` được chuyển sang `Done`. Thay đổi trạng thái được ghi vào dòng thời gian với nguồn `github_pr_merged`. |\n\nChỉ bản thân PR được phản chiếu. Commit, ref nhánh không có PR mở, và trạng thái CI check **không** được mô hình hóa. Tích hợp chủ ý giữ hẹp.\n\n## Mã định danh được khớp thế nào\n\nWebhook trích mã từ ba trường, theo thứ tự: **nhánh head của PR**, **tiêu đề PR**, **thân PR**. Bộ khớp:\n\n- Không phân biệt hoa thường — `mul-123`, `MUL-123`, `Mul-123` đều khớp.\n- Có biên — `\\b` bên trái và neo chữ số bên phải giữ nó khỏi vồ số phiên bản như `v1.2-3` hay chuỗi kiểu email.\n- Theo phạm vi không gian làm việc — chỉ khớp [tiền tố issue](/workspaces) của chính không gian làm việc. `FOO-1` trong không gian làm việc có tiền tố `MUL` bị bỏ qua, kể cả khi phần số trùng một issue khác.\n- Khử trùng lặp — ghi `MUL-1, MUL-1` trong thân chỉ liên kết issue một lần.\n\nBạn có thể nhắc **nhiều issue** trong một PR. `Closes MUL-1, MUL-2` liên kết PR với cả hai, và merge nó đưa cả hai lên `Done`.\n\n## Quy tắc tự merge-thành-Done\n\nKhi trường `merged` của PR lật sang `true`, mọi issue liên kết được đánh giá:\n\n| Trạng thái hiện tại của issue | Kết quả |\n|---|---|\n| `done` | Không đổi (đã kết thúc). |\n| `cancelled` | **Không đổi** — cancelled nghĩa là người dùng đã chủ động bỏ việc; tích hợp không ghi đè tín hiệu đó. |\n| Còn lại (`todo`, `in_progress`, `in_review`, `blocked`, `backlog`) | Chuyển sang `done`. |\n\nĐóng PR **mà không** merge chỉ cập nhật trạng thái thẻ PR thành `Closed`. Các issue liên kết đứng nguyên — người dùng là người quyết định đóng-không-merge nghĩa là gì.\n\n> Hành động được ghi cho actor `system` trên dòng thời gian. Người theo dõi issue nhận thông báo hộp thư cho thay đổi trạng thái, y như khi một con người chuyển nó.\n\n## Những gì không tự liên kết\n\n- **Mã trong thông điệp commit** — chỉ nhánh / tiêu đề / thân được quét. Commit tên `MUL-123: fix login` không tự liên kết trừ khi chuỗi đó cũng xuất hiện trong tiêu đề hoặc thân PR.\n- **Mã trong bình luận của PR** — chỉ metadata của chính PR được quét; các bình luận GitHub về sau bị bỏ qua.\n- **PR trong repo chưa cài App** — không có App, Multica không bao giờ nhận webhook.\n- **Liên kết PR vào issue thủ công** — chưa có UI cho việc này. Nếu quy ước của nhóm đặt mã ở chỗ Multica không đọc, thêm nó vào tiêu đề hoặc thân PR.\n\n## Ngắt kết nối\n\nTrong **Cài đặt → GitHub** không có danh sách installation — bạn quản lý các installation sẵn có từ GitHub trực tiếp:\n\n- **Từ GitHub** — gỡ Multica GitHub App tại `https://github.com/settings/installations` (cá nhân) hoặc `https://github.com/organizations/<org>/settings/installations` (tổ chức). Multica nhận webhook `installation.deleted` và xóa dòng theo thời gian thực; tab Cài đặt đang mở cập nhật không cần tải lại.\n- **Ngắt từ trong Multica chỉ dành cho admin** — nút Disconnect trên tab GitHub bị ẩn với người không phải admin. Nó vẫn khả dụng kể cả khi công tắc tổng GitHub đang tắt, để admin vẫn thu hồi được installation cũ sau khi tắt nhanh tính năng.\n\nSau khi ngắt, các dòng PR đã phản chiếu vẫn nằm trong cơ sở dữ liệu để thanh bên issue lịch sử vẫn hiện những gì từng liên kết, nhưng sự kiện webhook mới từ installation đó không được chấp nhận nữa.\n\n## Quyền và khả năng nhìn thấy\n\n- **Kết nối / ngắt kết nối** cần **owner hoặc admin** của không gian làm việc. Member thấy mô tả thẻ nhưng không có nút Connect.\n- Thanh bên **Pull requests** trên issue hiện với bất kỳ ai đọc được issue — cùng quyền với phần còn lại của chi tiết issue.\n- GitHub App xin quyền **chỉ đọc** với pull request và metadata. Multica không bao giờ đẩy commit, bình luận hay status check ngược về GitHub.\n\n## Thiết lập tự vận hành\n\nNếu bạn dùng Multica Cloud, tích hợp đã cấu hình sẵn — bỏ qua mục này.\n\nVới tự vận hành, bạn tạo một GitHub App, trỏ nó về server của mình và đặt hai biến môi trường. Toàn bộ luồng bên dưới.\n\n### 1. Tạo GitHub App\n\nVào một trong hai:\n\n- Tài khoản cá nhân → `https://github.com/settings/apps/new`\n- Tổ chức → `https://github.com/organizations/<org>/settings/apps/new`\n\nĐiền:\n\n| Trường | Giá trị |\n|---|---|\n| **GitHub App name** | Gì đó dễ nhận, ví dụ `Multica` hoặc `Multica (staging)`. |\n| **Homepage URL** | Frontend Multica của bạn, ví dụ `https://multica.example.com`. |\n| **Callback URL** | Để trống — Multica không dùng danh tính OAuth. |\n| **Setup URL** | `https://<api-host>/api/github/setup`. **Tích \"Redirect on update\"**. |\n| **Webhook → Active** | Bật. |\n| **Webhook URL** | `https://<api-host>/api/webhooks/github`. |\n| **Webhook secret** | Sinh một chuỗi ngẫu nhiên dài (ví dụ `openssl rand -hex 32`). Bạn sẽ dán đúng giá trị này vào env của Multica ở bước 2. |\n| **Permissions → Repository → Pull requests** | **Read-only**. |\n| **Permissions → Repository → Metadata** | Read-only (bắt buộc). |\n| **Subscribe to events** | Tích **Pull request**. |\n| **Where can this GitHub App be installed?** | Tùy bạn. `Only on this account` là ổn cho thiết lập một tổ chức. |\n\nSau **Create GitHub App**, ghi lại hai thứ từ trang chi tiết App:\n\n- **Public link** ở trên cùng — đuôi của nó là slug. `https://github.com/apps/multica-acme` → slug = `multica-acme`.\n- **Webhook secret** vừa sinh (về sau không đọc lại được từ GitHub — lưu ngay).\n\n> **Webhook secret ≠ Client secret.** Trang cài đặt App xếp hai trường này cạnh nhau. **Webhook secret** là thứ ký payload `pull_request` — đó là cái Multica cần. **Client secret** dành cho OAuth và tích hợp này không dùng. Nhầm hai cái sẽ sinh `401 invalid signature` khó hiểu ở mọi lượt giao webhook.\n\n### 2. Đặt biến môi trường\n\nTrên API server:\n\n```dotenv\nGITHUB_APP_SLUG=multica-acme\nGITHUB_WEBHOOK_SECRET=<webhook secret bạn đã sinh>\n\n# Tùy chọn nhưng nên có — điền tên tài khoản kết nối ngay khi cài\n# thay vì chờ webhook đầu tiên refresh:\nGITHUB_APP_ID=<App ID dạng số từ trang cài đặt App>\nGITHUB_APP_PRIVATE_KEY=<nguyên khối PEM, gồm cả dòng BEGIN/END>\n```\n\n`GITHUB_APP_SLUG` và `GITHUB_WEBHOOK_SECRET` là bắt buộc. Thiếu một trong hai:\n\n- Nút `Connect GitHub` trong Cài đặt bị **vô hiệu** và hiện gợi ý \"not configured\".\n- Endpoint `/api/webhooks/github` trả **`503 github webhooks not configured`** — Multica từ chối xử lý sự kiện khi không có secret, thay vì lặng lẽ coi mọi chữ ký là hợp lệ.\n\n`GITHUB_APP_ID` và `GITHUB_APP_PRIVATE_KEY` là **tùy chọn**. Chúng cho phép callback thiết lập gọi endpoint `/app/installations/{id}` xác thực bằng App của GitHub để lấy tên tổ chức hoặc người dùng thật ngay lúc cài. Không có chúng, thẻ kết nối hiện tạm `Connected to unknown` cho tới khi GitHub giao webhook `installation.created` (thường trong vài giây), lúc đó Multica refresh dòng và phát cập nhật realtime để tab Cài đặt đang mở tự cập nhật. Sinh private key ở **Private keys → Generate a private key** trên trang cài đặt App; dán nguyên khối PEM (gồm cả dòng `-----BEGIN/END RSA PRIVATE KEY-----`) vào biến env, giữ nguyên xuống dòng.\n\n`FRONTEND_ORIGIN` cũng phải được đặt (mọi bản tự vận hành production đều đã có); callback thiết lập đưa người dùng về `<FRONTEND_ORIGIN>/settings?tab=github` sau khi cài.\n\nKhởi động lại API sau khi đặt biến env.\n\n### 3. Chạy migration\n\nTích hợp mang bảng của nó trong migration `079_github_integration`. Nếu bạn nâng cấp từ bản cũ:\n\n```bash\nmake migrate-up\n```\n\nBa bảng được tạo: `github_installation`, `github_pull_request`, `issue_pull_request`. Chúng cascade-delete cùng không gian làm việc, nên xóa không gian làm việc tự dọn chúng.\n\n### 4. Kết nối từ UI\n\nTrong Multica:\n\n1. Mở **Cài đặt → GitHub** với vai trò owner hoặc admin.\n2. Bấm **Connect GitHub**. GitHub mở trong tab mới.\n3. Chọn repository muốn cấp quyền và **Install**.\n4. GitHub chuyển hướng về `<api-host>/api/github/setup`, nơi ghi lại installation và đưa bạn về `<FRONTEND_ORIGIN>/settings?tab=github&github_connected=1`.\n\nSau đó, mở bất kỳ PR nào có nhánh / tiêu đề / thân chứa mã issue — trong vài giây, khối Pull requests xuất hiện trên trang chi tiết issue đó.\n\n### 5. Kiểm chứng bằng curl\n\nNếu trang **Recent Deliveries** của GitHub báo `401 invalid signature` sau khi cài, hai bên đang khác secret. Cách nhanh nhất để biết bên nào sai là bỏ qua GitHub:\n\n```bash\nSECRET=\"<giá trị bạn đặt vào GITHUB_WEBHOOK_SECRET>\"\nBODY='{\"zen\":\"test\"}'\nSIG=$(printf '%s' \"$BODY\" | openssl dgst -sha256 -hmac \"$SECRET\" -hex | awk '{print $NF}')\ncurl -i -X POST https://<api-host>/api/webhooks/github \\\n  -H \"X-Hub-Signature-256: sha256=$SIG\" \\\n  -H \"X-GitHub-Event: ping\" \\\n  -H \"Content-Type: application/json\" \\\n  -d \"$BODY\"\n```\n\n| Mã HTTP | Ý nghĩa | Cách sửa |\n|---|---|---|\n| `200` `{\"ok\":\"pong\"}` | Secret server đang nạp khớp `$SECRET` của bạn. Lệch nằm phía GitHub. | Sửa App → Webhook secret → **dán đúng giá trị** → **Save changes** (bấm ra ngoài ô mà không Save sẽ giữ secret cũ). Redeliver. |\n| `401 invalid signature` | Secret server đang nạp **không** như bạn nghĩ. | Xác nhận biến env đã vào tiến trình đang chạy (ví dụ `kubectl exec` → `echo -n \"$GITHUB_WEBHOOK_SECRET\" \\| wc -c`). Deploy lại. |\n| `503 github webhooks not configured` | `GITHUB_WEBHOOK_SECRET` rỗng trong tiến trình. | Đặt biến env, khởi động lại API. |\n\n## Hạn chế\n\nVài cạnh thô cần biết hôm nay:\n\n- **Chưa có UI liên kết thủ công** — cách duy nhất để liên kết PR là có mã trong nhánh, tiêu đề hoặc thân của nó.\n- **Không có trạng thái CI / check** — chỉ bản thân PR được phản chiếu. Trạng thái build, bình luận review và reviewer không hiện trong Multica.\n- **Không có cấu hình mức không gian làm việc** cho quy tắc merge → Done — nó là mặc định cố định (`merged → done`, trừ `cancelled`). Ánh xạ tùy biến theo không gian làm việc là bổ sung tương lai.\n- **Nhiều PR về một issue xử lý bảo thủ khi merge** — nếu hai PR cùng nhắc `MUL-123` và cái đầu merge, issue được chuyển `Done` ngay. Thay đổi tiếp theo để chờ mọi PR liên kết xong mới chuyển đang được làm.\n\n## Tiếp theo\n\n- [Issue](/issues) — mã issue (`MUL-123`) được nhắc từ PR\n- [Không gian làm việc](/workspaces) — nơi đặt tiền tố issue riêng của không gian làm việc\n- [Biến môi trường](/environment-variables) — tham chiếu env đầy đủ, gồm các biến GitHub ở trên"
        },
        {
          "slug": "lark-bot-integration",
          "title": "Tích hợp Lark Bot",
          "content": "Buộc bất kỳ [agent](/agents) nào vào một Bot Lark (飞书) và nhóm của bạn làm việc với nó ngay trong Lark — DM cho Bot, @nhắc nó trong nhóm, hoặc gõ `/issue` để tạo một [issue Multica](/issues) mà không phải mở app. Câu trả lời của agent stream ngược về cuộc chat dưới dạng thẻ động cập nhật trong lúc nó làm việc.\n\nMỗi Bot buộc **một-một** với một agent Multica. Buộc agent thứ hai sẽ tạo Bot thứ hai; một agent không bao giờ có hai Bot.\n\n## Tích hợp làm gì\n\n| Bề mặt | Hành vi |\n|---|---|\n| **Agent → Integrations** | Trang chi tiết agent có tab **Integrations** (và mục tương ứng ở thanh bên trái). Owner và admin thấy **Bind to Lark** ở đó; buộc xong nó chuyển thành huy hiệu **Connected to Lark** kèm liên kết **Manage in Lark**. |\n| **DM cho Bot** | Một thành viên nhắn thẳng cho Bot trong Lark. Mỗi cuộc trò chuyện thành một phiên [chat](/chat) Multica với agent; agent trả lời trong luồng. |\n| **@nhắc trong nhóm** | Thêm Bot vào nhóm Lark và @nhắc nó. Chỉ tin nhắn có nhắc mới được đọc — Bot không nghe cả nhóm. |\n| **Lệnh `/issue`** | Gõ `/issue <tiêu đề>` (tùy chọn kèm phần thân) tạo một issue Multica mới trong không gian làm việc, đứng tên bạn. |\n| **Thẻ trả lời động** | Bot đăng một thẻ tương tác và liên tục vá nó trong lúc agent chạy — tiến độ, câu trả lời cuối, hoặc lỗi. |\n\n## Buộc một agent (owner / admin)\n\nViệc buộc dùng luồng quét-để-cài — không chép app secret, không bước developer console.\n\n1. Mở agent trong **Agent → _agent của bạn_**.\n2. Vào tab **Integrations** (hoặc mục **Integrations** ở thanh bên trái) và bấm **Bind to Lark**.\n3. Một mã QR hiện ra. Trên điện thoại, mở **Lark → Scan**, rồi cấp quyền cho Bot PersonalAgent mới.\n4. Quét xong, hộp thoại đóng và agent hiện **Connected to Lark**. Danh tính Lark của chính bạn được buộc tự động vào tài khoản Multica, nên bạn chat được với Bot ngay.\n\n> Mã QR dùng một lần và hết hạn sau khoảng ngắn. Nếu nó hết hạn trước khi bạn cấp quyền, bấm **Scan again** để lấy mã mới.\n\nKhi agent đã kết nối, nút **Bind to Lark** được thay bằng liên kết **Manage in Lark**. Dùng nó để mở trang app của Bot trong Lark khi cần chỉnh scope, đổi tên, hoặc xin thêm quyền — quét lại bị chủ ý vô hiệu để bạn không bỏ rơi Bot hiện có.\n\n## Dùng Bot (thành viên)\n\n### Tin nhắn đầu tiên: buộc danh tính Lark\n\nLần đầu bạn nhắn Bot, nó trả lời bằng một thẻ đề nghị **buộc danh tính Lark của bạn**. Bấm liên kết, đăng nhập Multica, và tài khoản Lark của bạn được nối với tư cách thành viên Multica. Đây là thứ cho phép agent hành động thay bạn — ví dụ, `/issue` tạo issue đứng tên bạn.\n\n> Chỉ **thành viên của không gian làm việc** dùng được Bot. Nếu bạn không phải thành viên, hoặc bỏ qua bước buộc danh tính, Bot sẽ không trả lời — tin nhắn của bạn bị bỏ (và ghi lại để kiểm toán, không kèm nội dung).\n\n### Chat và `/issue`\n\n- **Hỏi agent bất cứ gì** — DM cho Bot hoặc @nhắc nó trong nhóm. Cuộc trò chuyện là một phiên chat agent bình thường; agent trả lời trong thẻ.\n- **Tạo issue** — gửi `/issue Fix the login redirect` và Multica tạo issue đó trong không gian làm việc, được giao như mọi issue mới. Thêm dòng sau tiêu đề để làm mô tả.\n- **Xem nó làm việc** — thẻ trả lời tự vá trong lúc agent chạy, nên bạn thấy tiến độ và kết quả tại chỗ.\n\nNếu agent **offline** (runtime của nó không kết nối) hoặc **đã lưu trữ**, Bot trả lời một thông báo trạng thái ngắn thay vì lặng lẽ bỏ tin nhắn của bạn.\n\n## Quản lý và ngắt kết nối\n\nQuản lý toàn không gian làm việc nằm ở **Cài đặt → Integrations**:\n\n- **Connected bots** liệt kê mọi Bot trong không gian làm việc và agent mà từng Bot buộc vào. Danh sách này mọi thành viên đều thấy.\n- **Disconnect** chỉ dành cho **owner / admin**. Ngắt kết nối làm Bot ngừng nhận tin nhắn Lark và gỡ kết nối của nó; bản ghi installation được giữ để kiểm toán, và bạn có thể buộc lại chính agent đó về sau.\n\n## Quyền\n\n- **Buộc / ngắt** cần **owner** hoặc **admin** của không gian làm việc. Member thấy danh sách bot đã kết nối nhưng không có nút buộc hay ngắt.\n- **Nói chuyện với Bot** cần là thành viên không gian làm việc có danh tính Lark đã buộc. Ai khác bị bỏ.\n- Tích hợp không bao giờ lưu nội dung tin nhắn bị bỏ — chỉ lý do bỏ, để kiểm toán.\n\n## Thiết lập tự vận hành\n\nTrên Multica Cloud tích hợp đã sẵn sàng — bỏ qua mục này.\n\nVới tự vận hành, Lark **tắt cho tới khi bạn đặt khóa mã hóa at-rest**. Khóa mã hóa app secret của từng Bot trước khi chạm cơ sở dữ liệu.\n\n1. Sinh một khóa 32 byte và đặt trên API server:\n\n   ```dotenv\n   MULTICA_LARK_SECRET_KEY=<khóa 32 byte mã hóa base64>\n   ```\n\n2. Khởi động lại API. Chừng nào khóa chưa đặt, **Cài đặt → Integrations** hiện thông báo \"Lark integration not enabled\" và các lối vào **Bind to Lark** vẫn ẩn.\n\n> **Feishu và Lark quốc tế, song song.** Đám mây mà từng Bot thuộc về — Feishu đại lục (`open.feishu.cn`) hay Lark quốc tế (`open.larksuite.com`) — được phát hiện tự động khi bạn quét QR, lưu trên installation và dùng cho mọi lệnh gọi tới Bot đó. Một bản triển khai phục vụ cả hai cùng lúc, nên đội ở tenant nào cũng buộc được mà không cần cấu hình thêm.\n>\n> Các biến env `MULTICA_LARK_HTTP_BASE_URL` / `MULTICA_LARK_CALLBACK_BASE_URL` chỉ còn là ghi đè toàn triển khai tùy chọn (proxy hoặc mock); để trống trong vận hành bình thường để mỗi installation tự tới đúng đám mây của nó.\n>\n> **Nâng cấp từ thiết lập một đám mây?** Nếu bạn từng chạy bản Lark quốc tế bằng cách đặt các biến đó về `https://open.larksuite.com`, server sẽ gắn lại nhãn các installation sẵn có sang vùng Lark ở lần khởi động đầu sau nâng cấp — sau đó bạn xóa được phần ghi đè. Triển khai đại lục không cần làm gì.\n\n## Tiếp theo\n\n- [Agent](/agents) — mỗi Bot buộc với đúng một agent\n- [Chat](/chat) — cuộc trò chuyện với Bot ánh xạ thành gì trong Multica\n- [Issue](/issues) — `/issue` tạo ra gì\n- [Biến môi trường](/environment-variables) — tham chiếu cấu hình tự vận hành đầy đủ"
        },
        {
          "slug": "slack-bot-integration",
          "title": "Tích hợp Slack Bot",
          "content": "Nối bất kỳ [agent](/agents) nào với một bot Slack và nhóm của bạn làm việc với nó ngay trong Slack — DM cho bot, @nhắc nó trong kênh, hoặc gõ `/issue` để tạo một [issue Multica](/issues) mà không phải mở app.\n\nSlack dùng mô hình **tự-mang-app (BYO)**: admin của không gian làm việc tạo một app Slack, cài vào Slack workspace của mình và dán token của nó vào Multica. Mỗi agent có **app Slack riêng** — nên nhiều agent có thể mỗi con một bot riêng, @nhắc riêng, trong cùng một Slack workspace. (Khác với [Lark](/lark-bot-integration), nơi việc buộc là luồng quét-để-cài.)\n\nToàn bộ thiết lập ở bên dưới, mất khoảng năm phút. Kết quả là hai token để dán vào Multica:\n\n- một **Bot token** — bắt đầu bằng `xoxb-`\n- một **App-level token** — bắt đầu bằng `xapp-`\n\n## Thiết lập app Slack của bạn\n\n### 1. Tạo app từ manifest\n\n1. Vào [https://api.slack.com/apps](https://api.slack.com/apps) và bấm **Create New App**.\n2. Chọn **From a manifest**.\n3. Chọn Slack workspace để cài app vào.\n4. Chuyển sang tab **YAML**, dán manifest bên dưới, xem lại và tạo app.\n\n```yaml\ndisplay_information:\n  name: Multica\nfeatures:\n  app_home:\n    home_tab_enabled: false\n    messages_tab_enabled: true\n    messages_tab_read_only_enabled: false\n  bot_user:\n    display_name: Multica\n    always_online: true\n  slash_commands:\n    - command: /issue\n      description: Create a Multica issue\n      usage_hint: \"[title]\"\noauth_config:\n  scopes:\n    bot:\n      - app_mentions:read\n      - channels:history\n      - groups:history\n      - im:history\n      - mpim:history\n      - chat:write\n      - reactions:write\n      - users:read\n      - commands\nsettings:\n  event_subscriptions:\n    bot_events:\n      - app_mention\n      - message.im\n      - message.channels\n      - message.groups\n      - message.mpim\n  interactivity:\n    is_enabled: false\n  org_deploy_enabled: false\n  socket_mode_enabled: true\n  token_rotation_enabled: false\n```\n\nManifest này cấu hình đủ mọi thứ Multica cần, nên bạn không phải chỉnh tay gì:\n\n| Mục | Vì sao cần |\n|---|---|\n| `app_home.messages_tab_enabled: true` | Cho thành viên mở bot và **DM** nó. Thiếu nó, bot không nhắn trực tiếp được. |\n| `bot_user` | Tạo danh tính bot được @nhắc và đăng trả lời. |\n| `chat:write` | Đăng câu trả lời của agent ngược vào Slack. |\n| `reactions:write` | Thêm cảm xúc 👀 vào tin nhắn của bạn khi agent đang làm, gỡ khi nó trả lời. Thiếu scope này, chỉ báo bị bỏ qua lặng lẽ — mọi thứ khác vẫn chạy. |\n| `app_mentions:read` + sự kiện `app_mention` | Nhận @nhắc trong kênh. |\n| `im:history` + `message.im` | Nhận **DM** gửi bot (mọi tin DM đều được đọc). |\n| `channels:history` / `groups:history` / `mpim:history` + các sự kiện `message.*` tương ứng | Nhận tin trong kênh công khai, kênh riêng và DM nhóm. Ở đó, bot chỉ hành động với tin **@nhắc** nó. |\n| `users:read` | Cần để Multica xác minh (qua `bots.info`) rằng hai token thuộc cùng một app. |\n| `commands` | Scope bot bật lệnh gạch chéo `/issue` (đi cặp với `features.slash_commands`). Thiếu nó, cập nhật manifest và cài lại cũng không được cấp lệnh. |\n| `socket_mode_enabled: true` | Bot kết nối ra ngoài qua Socket Mode — **không cần URL công khai / request URL**. |\n| `interactivity.is_enabled: false` | Lời nhắc của Multica là liên kết thuần, không phải nút, nên không cần interactivity. |\n| `slash_commands` (`/issue`) | Đăng ký lệnh `/issue` để ai cũng tạo được issue Multica từ ô nhắn tin. Giao qua Socket Mode — không cần request URL. |\n\n**Không có OAuth redirect URL**, vì BYO không dùng OAuth.\n\n> Muốn tên riêng trong Slack? Đổi `display_information.name` và `features.bot_user.display_name` (ví dụ thành tên agent của bạn) trước khi tạo, hoặc sửa sau ở **App Home**. Slack hiển thị bot theo **tên hiển thị của bot**, có thể khác tên app.\n\n### 2. Cài app và chép Bot token\n\n1. Ở nav trái của app, mở **Install App** (hoặc **OAuth & Permissions**).\n2. Bấm **Install to Workspace** và phê duyệt.\n3. Chép **Bot User OAuth Token** — bắt đầu bằng `xoxb-`. Đây là **Bot token** của bạn.\n\n### 3. Tạo App-level token\n\nApp-level token ủy quyền cho kết nối Socket Mode. Chỉ tạo được trong console (nó không thuộc OAuth).\n\n1. Mở **Basic Information → App-Level Tokens** và bấm **Generate Token and Scopes**.\n2. Đặt tên tùy ý.\n3. Bấm **Add Scope** và chọn **`connections:write`** từ danh sách (đó là bộ chọn — chọn nó, đừng gõ).\n4. Bấm **Generate**, rồi chép token — bắt đầu bằng `xapp-`. Đây là **App-level token** của bạn.\n\n### 4. Kết nối trong Multica\n\n1. Mở agent trong **Agent → _agent của bạn_** → tab **Integrations** (hoặc mục **Integrations** ở thanh bên trái).\n2. Bấm **Connect Slack**.\n3. Dán **Bot token** (`xoxb-`) và **App-level token** (`xapp-`), rồi bấm **Connect**.\n4. Agent hiện **Connected to Slack**. Bot giờ đang nghe qua kết nối Socket Mode của riêng nó.\n\n> Hai token phải từ **cùng một** app Slack, và app đó ánh xạ tới đúng **một** agent. Kết nối một app đã nối với agent hoặc không gian làm việc khác sẽ bị từ chối. Muốn chuyển app sang agent khác, ngắt kết nối trước; kết nối lại một agent với app **mới** sẽ cập nhật bot của agent đó tại chỗ.\n\n> Thiết lập cho **nhiều agent**? Lặp lại toàn bộ luồng cho từng agent — mỗi agent có app Slack riêng và cặp token riêng, chúng hiện thành các bot tách biệt trong Slack workspace của bạn.\n\n## Tích hợp làm gì\n\n| Bề mặt | Hành vi |\n|---|---|\n| **Agent → Integrations** | Owner và admin thấy **Connect Slack**; nối xong nó chuyển thành huy hiệu **Connected to Slack** kèm nút **Disconnect**. |\n| **DM cho bot** | Một thành viên nhắn thẳng cho bot. Cuộc trò chuyện thành một phiên [chat](/chat) Multica với agent; mọi tin DM đều được đọc. |\n| **@nhắc trong kênh** | Mời bot (`/invite @your-bot`) và @nhắc nó. Chỉ tin có nhắc mới được đọc — bot không nghe cả kênh. Mỗi **thread** @bot là một phiên riêng. |\n| **Lệnh gạch chéo `/issue`** | Gõ `/issue <mô tả>` (trong kênh hoặc DM) và agent biến mô tả bằng lời thường của bạn thành một issue Multica chỉn chu, đứng tên bạn. Nó trả lời riêng tư để xác nhận — bạn nhận thông báo Multica khi issue sẵn sàng. Không cần @nhắc. |\n| **Trả lời** | Câu trả lời của agent được đăng ngược vào đúng DM hoặc thread. |\n\n## Dùng bot (thành viên)\n\n### Tin nhắn đầu tiên: liên kết tài khoản\n\nLần đầu bạn @nhắc hoặc DM bot, nó trả lời với lời nhắc **liên kết tài khoản**. Bấm liên kết, đăng nhập Multica, và danh tính Slack của bạn được buộc với tư cách thành viên Multica — đây là thứ cho agent hành động thay bạn (ví dụ `/issue` tạo issue đứng tên bạn). Liên kết dùng một lần và hết hạn sau khoảng 15 phút; nhắn bot lần nữa để lấy cái mới.\n\nBạn chỉ liên kết **một lần cho mỗi Slack workspace**. Nếu cùng một không gian làm việc Multica chạy nhiều bot trong một Slack workspace (mỗi agent một app), bot đầu tiên bạn liên kết sẽ dạy các con còn lại: nhắn bot thứ hai tự dùng lại liên kết đó, không hỏi lại. (Chỉ cần liên kết lại với bot ở Slack workspace *khác*, hoặc bot nối với không gian làm việc Multica *khác*.)\n\n> Chỉ **thành viên của không gian làm việc** dùng được bot. Nếu bạn không phải thành viên, hoặc bỏ qua liên kết danh tính, bot sẽ không chạy — tin nhắn của bạn bị bỏ (ghi lại để kiểm toán, không kèm nội dung).\n\n### Chat và `/issue`\n\n- **Trong kênh** — bot không tự vào. Chạy `/invite @your-bot` một lần, rồi `@your-bot <tin nhắn>`. Nhắc lại nó cho mỗi câu tiếp theo (bot chỉ đọc tin có nhắc nó).\n- **Trong DM** — mở bot từ mục **Apps** ở thanh bên Slack và nhắn trực tiếp; không cần nhắc.\n- **Tạo issue** — dùng lệnh `/issue`, ví dụ `/issue the login redirect is broken on Safari`. Mô tả bằng lời thường; agent viết tiêu đề chuẩn và mô tả có cấu trúc rồi tạo issue. Dùng được trong kênh hoặc DM (không cần @nhắc) và trả lời riêng tư để xác nhận — bạn nhận thông báo Multica khi issue được tạo. Người dùng lần đầu nhận liên kết một lần để nối tài khoản.\n\n## Quản lý và ngắt kết nối\n\nQuản lý toàn không gian làm việc nằm ở **Cài đặt → Integrations**:\n\n- **Connected bots** liệt kê mọi bot trong không gian làm việc và agent từng con buộc vào (mọi thành viên thấy được).\n- **Disconnect** chỉ dành cho **owner / admin**. Nó làm bot ngừng nhận tin Slack và gỡ kết nối; bản ghi installation giữ lại để kiểm toán, và bạn nối lại được về sau.\n\n## Quyền\n\n- **Kết nối / ngắt** cần **owner** hoặc **admin** của không gian làm việc.\n- **Nói chuyện với bot** cần là thành viên có danh tính Slack đã liên kết. Ai khác bị bỏ.\n- Nội dung tin nhắn bị bỏ không bao giờ được lưu — chỉ lý do bỏ, để kiểm toán.\n\n## Thiết lập tự vận hành\n\nTrên Multica Cloud tích hợp đã sẵn sàng — bỏ qua mục này.\n\nVới tự vận hành, Slack **tắt cho tới khi bạn đặt khóa mã hóa at-rest**. Khóa mã hóa bot + app-level token của từng app trước khi chạm cơ sở dữ liệu. BYO **không** cần OAuth client id/secret và **không** cần app token mức triển khai — mỗi installation dùng token admin dán vào.\n\n1. Sinh một khóa 32 byte và đặt trên API server:\n\n   ```dotenv\n   MULTICA_SLACK_SECRET_KEY=<khóa 32 byte mã hóa base64>\n   ```\n\n   Ví dụ: `openssl rand -base64 32`.\n\n2. Khởi động lại API. Chừng nào khóa chưa đặt, **Cài đặt → Integrations** hiện thông báo \"Slack integration not enabled\" và các lối vào **Connect Slack** vẫn ẩn.\n\n> Khóa phải giải mã ra đúng 32 byte — `openssl rand -base64 32` làm đúng vậy. Đối xử với nó như bí mật sống lâu: xoay hoặc mất nó khiến token đã lưu không giải mã được, buộc mọi bot kết nối lại. Liên kết \"liên kết tài khoản\" được dựng từ URL web app của bạn (`MULTICA_APP_URL`, lùi về `FRONTEND_ORIGIN`) — triển khai bình thường đã đặt sẵn, không cần cấu hình thêm.\n\n## Tiếp theo\n\n- [Tích hợp chat](/channels) — bộ máy kênh, phiên và ủy quyền hoạt động ra sao\n- [Agent](/agents) · [Chat](/chat) · [Issue](/issues)\n- [Biến môi trường](/environment-variables) — tham chiếu cấu hình tự vận hành đầy đủ"
        }
      ]
    },
    {
      "label": "Self-hosting & ops",
      "labelKey": "self_hosting_ops",
      "pages": [
        {
          "slug": "environment-variables",
          "title": "Biến môi trường",
          "content": "Một Multica [server](/self-host-quickstart) tự vận hành đọc cấu hình từ biến môi trường lúc khởi động — cơ sở dữ liệu, đăng nhập, email, lưu trữ, danh sách cho phép đăng ký đều nằm đây. Trang này nhóm mọi biến theo mục đích: mỗi mục nói rõ **điều gì xảy ra nếu bạn bỏ trống** và **biến nào bắt buộc phải đặt ở production**. Cách cấu hình cụ thể các biến liên quan xác thực, xem [Thiết lập đăng nhập và đăng ký](/auth-setup).\n\n## Biến lõi của server\n\nĐây là các biến lõi cần cân nhắc trước khi triển khai — vài biến có mặc định đủ để server chạy, nhưng ở production bạn nên đặt tường minh các biến bắt buộc.\n\n| Biến | Mặc định | Bắt buộc ở production? |\n|---|---|---|\n| `DATABASE_URL` | `postgres://multica:multica@localhost:5432/multica?sslmode=disable` | **Có** |\n| `PORT` | `8080` | Không (trừ khi đổi cổng) |\n| `JWT_SECRET` | `multica-dev-secret-change-in-production` | **Có** (mặc định không an toàn) |\n| `APP_ENV` | trống | **Có** (phải là `production`) |\n| `FRONTEND_ORIGIN` | trống | **Có** (tự vận hành phải đặt tên miền riêng) |\n| `MULTICA_DEV_VERIFICATION_CODE` | trống | Không (phải để trống ở production) |\n\n> **Giữ `MULTICA_DEV_VERIFICATION_CODE` trống ở production.** Mã kiểm thử cục bộ cố định mặc định là tắt, nhưng nếu bạn bật với `MULTICA_DEV_VERIFICATION_CODE=888888`, bất kỳ ai xin được mã đều đăng nhập được bằng giá trị cố định đó khi `APP_ENV` không phải production. Lối tắt này bị bỏ qua khi `APP_ENV=production`.\n\n### Pool kết nối cơ sở dữ liệu\n\n| Biến | Mặc định | Mô tả |\n|---|---|---|\n| `DATABASE_MAX_CONNS` | `25` | Số kết nối tối đa của pgxpool. Daemon poll thường xuyên (mỗi 3s) và dùng kết nối; triển khai lớn có thể cần giá trị cao hơn |\n| `DATABASE_MIN_CONNS` | `5` | Số kết nối nhàn rỗi tối thiểu |\n\n**Khi bỏ trống**, các giá trị trên được dùng — **không phải** mặc định 4/NumCPU của pgx, thứ từng gây cạn pool ở production.\n\n## Cấu hình email\n\nMultica hỗ trợ hai backend gửi mail — [Resend](https://resend.com/) cho triển khai cloud, hoặc SMTP relay cho mạng nội bộ / on-premise. `SMTP_HOST` được ưu tiên hơn `RESEND_API_KEY` khi đặt cả hai.\n\n### Resend\n\n| Biến | Mặc định | Mô tả |\n|---|---|---|\n| `RESEND_API_KEY` | trống | API key của Resend |\n| `RESEND_FROM_EMAIL` | `noreply@multica.ai` | Địa chỉ gửi (phải là tên miền đã xác minh trong tài khoản Resend của bạn; cũng dùng lại làm header `From:` khi dùng SMTP) |\n\n### SMTP relay\n\n| Biến | Mặc định | Mô tả |\n|---|---|---|\n| `SMTP_HOST` | trống | Hostname của SMTP relay. Đặt biến này kích hoạt chế độ SMTP và ghi đè Resend |\n| `SMTP_PORT` | `25` | Cổng SMTP. Dùng `587` cho gửi STARTTLS, hoặc `465` cho SMTPS (TLS ngầm định, tự bật) |\n| `SMTP_USERNAME` | trống | Username SMTP. Để trống cho relay không xác thực |\n| `SMTP_PASSWORD` | trống | Mật khẩu SMTP |\n| `SMTP_TLS` | `starttls` | Chế độ TLS. `implicit` (bí danh `smtps`, `ssl`) ép bắt tay TLS ngay khi kết nối (SMTPS); cổng `465` tự bật. Bỏ trống / `starttls` nâng cấp qua STARTTLS sau khi kết nối |\n| `SMTP_TLS_INSECURE` | `false` | Đặt `true` để bỏ kiểm chứng chứng chỉ TLS (chỉ với CA riêng / tự ký) |\n| `SMTP_EHLO_NAME` | hostname của máy | Tên EHLO/HELO công bố với relay. Đặt FQDN thật khi relay nghiêm ngặt (ví dụ Google Workspace `smtp-relay.gmail.com`) từ chối lời chào mặc định từ IP công cộng — nếu không relay ngắt kết nối và hiện thành lỗi `EOF` khó hiểu ở một lệnh sau |\n\nSTARTTLS tự nâng cấp khi server quảng bá. Timeout quay số là 10s và cả phiên SMTP có hạn 30s, nên một relay \"hố đen\" không treo được handler xác thực.\n\n**Hành vi khi không đặt gì**: server không báo lỗi, nhưng mọi email đáng lẽ được gửi (mã xác thực, liên kết mời) **chỉ được ghi ra stdout của server**. Tiện cho phát triển cục bộ — chép mã từ log server; **ở production, quên đặt sẽ tạo hố đen im lặng**: người dùng không bao giờ nhận email và không lỗi nào hiện ra.\n\n## Cấu hình Google OAuth\n\nTùy chọn. Bỏ trống nếu chỉ dùng email + mã xác thực; cấu hình để thêm \"Đăng nhập với Google\" trên trang đăng nhập.\n\n| Biến | Mặc định | Mô tả |\n|---|---|---|\n| `GOOGLE_CLIENT_ID` | trống | OAuth client ID của Google Cloud |\n| `GOOGLE_CLIENT_SECRET` | trống | OAuth secret của Google Cloud |\n| `GOOGLE_REDIRECT_URI` | `http://localhost:3000/auth/callback` | URL callback OAuth (tự vận hành: thay bằng tên miền frontend của bạn) |\n\n**Có hiệu lực lúc chạy**: frontend đọc các thiết lập này qua `/api/config` lúc runtime, nên **đổi chúng không cần build hay deploy lại frontend** — khởi động lại server là áp dụng.\n\nThiết lập đầy đủ (gồm các bước Google Cloud Console) ở [Thiết lập đăng nhập và đăng ký](/auth-setup#google-oauth-configuration).\n\n## Cấu hình lưu trữ file\n\nMultica lưu tệp đính kèm người dùng tải lên (ảnh và file trong bình luận). **Ưu tiên S3**; nếu chưa cấu hình S3 thì lùi về đĩa cục bộ.\n\n### S3 / lưu trữ tương thích S3\n\n| Biến | Mặc định | Mô tả |\n|---|---|---|\n| `S3_BUCKET` | trống | **Chỉ tên bucket** (ví dụ `my-bucket`). **Đừng** kèm hậu tố `.s3.<region>.amazonaws.com` — server tự dựng host công khai từ `S3_BUCKET` + `S3_REGION`. Đặt biến này bật lưu trữ S3 |\n| `S3_REGION` | `us-west-2` | Vùng AWS. Phải khớp vùng thật của bucket — dùng cho cả ký SDK lẫn dựng URL công khai |\n| `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY` | trống | Thông tin đăng nhập tĩnh. Khi cả hai trống, chuỗi credential mặc định của AWS SDK được dùng (IAM role / credential môi trường) |\n| `AWS_ENDPOINT_URL` | trống | Endpoint tương thích S3 tùy chỉnh (ví dụ [MinIO](https://min.io/)). Đặt nó chuyển sang URL kiểu path-style |\n| `ATTACHMENT_DOWNLOAD_MODE` | `auto` | Đường tải tệp đính kèm: `auto`, `cloudfront`, `presign`, hoặc `proxy`. Ở `auto`, CloudFront được ưu tiên khi cấu hình đủ; endpoint nội bộ/riêng dùng proxy của server; endpoint tương thích S3 công cộng dùng presigned GET URL khi hỗ trợ |\n| `ATTACHMENT_DOWNLOAD_URL_TTL` | `30m` | TTL cho URL ký CloudFront và URL presigned S3. Nhận chuỗi duration của Go |\n\n**Khi `S3_BUCKET` trống**: server log `\"S3_BUCKET not set, cloud upload disabled\"` lúc khởi động, và mọi upload lùi về đĩa cục bộ.\n\n**URL của object đã lưu** được dựng theo thứ tự ưu tiên:\n\n1. `https://<CLOUDFRONT_DOMAIN>/<key>` nếu đặt `CLOUDFRONT_DOMAIN`.\n2. `<AWS_ENDPOINT_URL>/<S3_BUCKET>/<key>` (path-style) nếu đặt `AWS_ENDPOINT_URL`.\n3. `https://<S3_BUCKET>.s3.<S3_REGION>.amazonaws.com/<key>` (virtual-hosted-style). Khi `S3_BUCKET` chứa dấu chấm, server lùi về `https://s3.<S3_REGION>.amazonaws.com/<S3_BUCKET>/<key>` (path-style) vì chứng chỉ TLS wildcard của AWS không hợp lệ với host bucket có dấu chấm.\n\nGiá trị `download_url` của API dùng `GET /api/attachments/{id}/download` trừ khi cấu hình ký CloudFront. Endpoint chuyển hướng tới URL presigned CloudFront/S3 khi an toàn, hoặc stream qua server cho endpoint riêng/nội bộ như `http://rustfs:9000`. Với kho object chỉ trong Docker/VPC, đặt `ATTACHMENT_DOWNLOAD_MODE=proxy` nếu tự phát hiện chưa đủ bảo thủ cho mạng của bạn.\n\n### Đĩa cục bộ (khi chưa cấu hình S3)\n\n| Biến | Mặc định | Mô tả |\n|---|---|---|\n| `LOCAL_UPLOAD_DIR` | `./data/uploads` | Thư mục lưu cục bộ |\n| `LOCAL_UPLOAD_BASE_URL` | trống (trả đường dẫn tương đối) | URL gốc công khai — bỏ trống thì frontend không dựng được URL đầy đủ cho tệp đính kèm |\n\n### CloudFront (tùy chọn)\n\nNếu bạn đặt CloudFront trước S3, có ba biến: `CLOUDFRONT_DOMAIN`, `CLOUDFRONT_KEY_PAIR_ID`, `CLOUDFRONT_PRIVATE_KEY` (hoặc `CLOUDFRONT_PRIVATE_KEY_SECRET` để đọc từ Secrets Manager). Không dùng CloudFront thì bỏ qua — chúng không xung đột với cấu hình S3.\n\n### Domain của cookie\n\n| Biến | Mặc định | Mô tả |\n|---|---|---|\n| `COOKIE_DOMAIN` | trống | Phạm vi của cookie phiên |\n\n- **Trống**: cookie chỉ hợp lệ đúng trên host được truy cập (đúng cho triển khai một host)\n- **Đặt `.example.com`**: cookie chia sẻ giữa các subdomain (`app.example.com` và `api.example.com` dùng chung phiên đăng nhập)\n- Cảnh báo: không được là địa chỉ IP (trình duyệt bỏ qua)\n\n## Giới hạn ai được đăng ký\n\nBa lớp danh sách cho phép kết hợp theo ưu tiên. **Nếu lớp nào được đặt giá trị không rỗng, email không khớp bị từ chối** — kể cả `ALLOW_SIGNUP=true` cũng không vượt được.\n\n| Biến | Mặc định | Mô tả |\n|---|---|---|\n| `ALLOWED_EMAILS` | trống | Danh sách email tường minh (phân tách phẩy). Khi không rỗng, chỉ email trong danh sách đăng ký được |\n| `ALLOWED_EMAIL_DOMAINS` | trống | Danh sách tên miền (phân tách phẩy). Khi không rỗng, chỉ tên miền trong danh sách đăng ký được |\n| `ALLOW_SIGNUP` | `true` | Công tắc tổng đăng ký. Đặt `false` để tắt hẳn đăng ký |\n\n**Phần dễ hiểu nhầm**: `ALLOWED_EMAIL_DOMAINS=company.io` + `ALLOW_SIGNUP=true` **không** nghĩa là \"cho company.io hoặc mọi người\" — nó nghĩa là **chỉ cho company.io**. Các lớp danh sách theo ngữ nghĩa AND — cây quyết định đầy đủ ở [Thiết lập đăng nhập và đăng ký → Danh sách cho phép đăng ký](/auth-setup#restricting-who-can-sign-up).\n\n**Bản thân luồng mời không kiểm tra danh sách đăng ký** — nhưng người được mời vẫn phải **đăng nhập được** trước khi chấp nhận lời mời. Nếu họ đã có tài khoản Multica (ví dụ từ không gian làm việc khác), họ chấp nhận thẳng, không bị ảnh hưởng bởi danh sách; **nếu chưa từng đăng ký**, bước đầu của đăng nhập (xin mã xác thực) vẫn qua kiểm tra danh sách, và email bị `ALLOW_SIGNUP=false` hay `ALLOWED_EMAILS` / `ALLOWED_EMAIL_DOMAINS` từ chối **không hoàn tất được đăng ký, do đó không chấp nhận được lời mời**.\n\n## Khóa việc tạo không gian làm việc\n\n`ALLOW_SIGNUP=false` chặn tài khoản mới, nhưng **không** chặn người đã đăng nhập tạo thêm không gian làm việc qua `POST /api/workspaces`. Trên máy chủ tự vận hành nơi mọi issue, repo và agent phải trong tầm mắt của admin nền tảng, đặt `DISABLE_WORKSPACE_CREATION=true` để đóng khe hở đó.\n\n| Biến | Mặc định | Mô tả |\n|---|---|---|\n| `DISABLE_WORKSPACE_CREATION` | `false` | Khi `true`, mọi lệnh gọi `POST /api/workspaces` trả `403 workspace creation is disabled for this instance`. Web UI ẩn mọi nút \"Tạo không gian làm việc\" qua `/api/config`. Không có ngoại lệ theo vai trò/owner — cổng chặn là toàn máy chủ |\n\nTrình tự khởi tạo khuyến nghị:\n\n1. Khởi động máy chủ với `DISABLE_WORKSPACE_CREATION` bỏ trống (mặc định).\n2. Đăng nhập admin và tạo không gian làm việc dùng chung.\n3. Đặt `DISABLE_WORKSPACE_CREATION=true` và khởi động lại backend. Từ đây, người dùng chỉ vào qua lời mời.\n\nNếu bạn cũng muốn giữ `ALLOW_SIGNUP=true` để người được mời hoàn tất đăng ký với mã xác thực đầu tiên, kết hợp `DISABLE_WORKSPACE_CREATION=true` với `ALLOWED_EMAIL_DOMAINS` / `ALLOWED_EMAILS` để khoanh vùng địa chỉ nào đăng ký được. Đặt `ALLOW_SIGNUP=false` sẽ chặn luôn cả người được mời đang chờ tạo tài khoản — chỉ hữu ích trên máy chủ mà mọi thành viên đã có tài khoản Multica.\n\n## Giới hạn tần suất (Redis tùy chọn)\n\nCác endpoint xác thực công khai — `/auth/send-code`, `/auth/verify-code`, `/auth/google` — có giới hạn tần suất theo IP theo cửa sổ cố định. Bộ giới hạn dùng Redis. Khi `REDIS_URL` trống, middleware là **no-op** (fail-open) và backend log `rate limiting disabled: REDIS_URL not configured` lúc khởi động.\n\n| Biến | Mặc định | Mô tả |\n|---|---|---|\n| `REDIS_URL` | trống | URL kết nối Redis (ví dụ `redis://localhost:6379/0`). Bỏ trống thì giới hạn tần suất trên endpoint xác thực bị tắt. Cùng Redis này cũng được dùng bởi fan-out của hub realtime, cache PAT và cache daemon-token — tất cả lùi về chế độ in-memory / truy DB trực tiếp khi trống |\n| `REDIS_DISABLE_CLIENT_NAME` | `false` | Đặt `true` để bỏ handshake `CLIENT SETNAME` trên mỗi kết nối Redis. **Bắt buộc** với các Redis được quản lý chặn lệnh `CLIENT`, như GCP Memorystore hay AWS ElastiCache với ACL hạn chế. Khi bật, kết nối mất tên mô tả trong `CLIENT LIST` nhưng tương thích với các nhà cung cấp hạn chế |\n| `RATE_LIMIT_AUTH` | `5` | Số request tối đa mỗi IP mỗi phút với `/auth/send-code` và `/auth/google` |\n| `RATE_LIMIT_AUTH_VERIFY` | `20` | Số request tối đa mỗi IP mỗi phút với `/auth/verify-code` |\n| `RATE_LIMIT_TRUSTED_PROXIES` | trống | Các CIDR (phân tách phẩy) mà bộ giới hạn được phép tin header `X-Forwarded-For` của chúng. Trống (mặc định) nghĩa là **không bao giờ tin XFF** — bộ giới hạn chỉ dùng `RemoteAddr` của kết nối trực tiếp |\n\nKhi request vượt giới hạn, server trả `429 Too Many Requests`, `Retry-After: 60`, và body `{\"error\":\"too many requests\"}`.\n\n> **Sau reverse proxy, bạn phải đặt `RATE_LIMIT_TRUSTED_PROXIES`.** Nếu không, mọi người dùng thật chia chung IP của proxy dưới góc nhìn backend, cả triển khai dồn vào một xô, và `/auth/send-code` thành 5 req/phút cho cả site. Giá trị điển hình: `127.0.0.1/32,::1/128` cho Caddy / Nginx cùng máy; dải IP công bố của CDN cho Cloudflare / ALB / CloudFront. Chỉ IP có `RemoteAddr` nằm trong các CIDR này mới được dùng `X-Forwarded-For` để nhận diện client.\n\n`RATE_LIMIT_TRUSTED_PROXIES` này **không** giống `MULTICA_TRUSTED_PROXIES`, thứ điều khiển bộ giới hạn webhook autopilot (`/api/webhooks/autopilots/{token}`). Mỗi bộ giới hạn parse danh sách riêng, nên triển khai sau proxy nên đặt cả hai.\n\n## Tham số tinh chỉnh daemon\n\nDaemon chạy trên máy của người dùng, cấu hình của nó cũng đọc từ biến môi trường cục bộ. Các biến hay dùng:\n\n| Biến | Mặc định | Mô tả |\n|---|---|---|\n| `MULTICA_SERVER_URL` | `ws://localhost:8080/ws` | Địa chỉ server (tự vận hành: thay bằng tên miền của bạn) |\n| `MULTICA_DAEMON_HEARTBEAT_INTERVAL` | `15s` | Chu kỳ heartbeat |\n| `MULTICA_DAEMON_POLL_INTERVAL` | `3s` | Chu kỳ poll task |\n| `MULTICA_DAEMON_MAX_CONCURRENT_TASKS` | `20` | Số task đồng thời tối đa |\n| `MULTICA_<PROVIDER>_PATH` | trùng tên CLI | Đường dẫn tới file thực thi của từng công cụ lập trình AI (ví dụ `MULTICA_CLAUDE_PATH`) |\n| `MULTICA_<PROVIDER>_MODEL` | trống | Model mặc định cho từng công cụ lập trình AI |\n| `MULTICA_<PROVIDER>_ARGS` | trống | Đối số CLI mặc định toàn daemon cho một backend, áp cho mọi task trước `custom_args` riêng của từng agent. Hỗ trợ `MULTICA_CLAUDE_ARGS`, `MULTICA_CODEX_ARGS` và `MULTICA_CODEBUDDY_ARGS` |\n\nGiải thích đầy đủ từng tham số ảnh hưởng hành vi daemon thế nào, xem [Daemon và runtime](/daemon-runtimes).\n\n### Đối số mặc định cho agent (`MULTICA_<PROVIDER>_ARGS`)\n\nCác biến này đặt lớp cờ CLI **mặc định toàn đội** cho một backend — cách tiện để áp mặt bằng chi phí hoặc tài nguyên mặc định (ví dụ `--max-turns`) cho mọi agent trên một daemon mà không phải sửa `custom_args` từng con. Đây là lớp mặc định, không phải trần cứng: `custom_args` theo agent được nối vào sau và ghi đè được (xem **Thứ tự ưu tiên** bên dưới).\n\n- **Thứ tự ưu tiên:** đối số mặc định áp trước, rồi `custom_args` riêng của từng agent nối sau. Với cờ nhận giá trị, trình parse của chính CLI hạ nguồn quyết định (đa số công cụ lấy lần xuất hiện cuối), nên một agent riêng lẻ nâng được mặc định của daemon, nhưng mặc định vẫn áp ở nơi agent không ghi đè.\n- **Parse:** giá trị được tách theo quy tắc shell-word POSIX, nên trích dẫn hoạt động — `MULTICA_CLAUDE_ARGS='--append-system-prompt \"multi word\"'` parse thành hai token.\n- **An toàn:** cả lớp đối số mặc định lẫn `custom_args` theo agent đều qua cùng bộ lọc cờ bị chặn, nên cờ trọng yếu của giao thức (như `-p`, `--output-format`, `--input-format`, `--permission-mode`, `--mcp-config` với Claude, và `--listen` với Codex) không tiêm được qua lớp nào.\n- **Trống** nghĩa là không đổi hành vi.\n\n## Kiểm soát truy cập frontend\n\n| Biến | Mặc định | Mô tả |\n|---|---|---|\n| `FRONTEND_ORIGIN` | trống | Địa chỉ frontend. Liên kết trong email mời, danh sách CORS và domain cookie đều suy từ đây. Bỏ trống thì liên kết email mời lùi về tên miền hosted `https://app.multica.ai` — tự vận hành phải đặt tường minh |\n| `MULTICA_APP_URL` | trống | URL frontend cho luồng đăng nhập CLI. Web UI cũng dùng để hiện lệnh thiết lập daemon tự vận hành với tên miền của bạn; với triển khai cùng origin, nó cũng được dùng làm `server_url` cho daemon khi `MULTICA_PUBLIC_URL` trống |\n| `MULTICA_PUBLIC_URL` | trống | URL API công khai, không có gạch chéo cuối. Dùng cho URL webhook autopilot và bởi web UI làm `server_url` cho daemon |\n| `CORS_ALLOWED_ORIGINS` | trống | Các origin CORS được phép thêm (phân tách phẩy) |\n| `ALLOWED_ORIGINS` | trống | Danh sách origin riêng cho WebSocket (phân tách phẩy); khi trống, thứ tự lùi là `CORS_ALLOWED_ORIGINS` → `FRONTEND_ORIGIN` → `localhost:3000/5173/5174` |\n\n> **Bỏ trống `FRONTEND_ORIGIN` tạo hai lỗi im lặng**: (1) liên kết email mời trỏ về `https://app.multica.ai` (tên miền hosted), bấm vào không đưa người dùng về máy chủ tự vận hành của bạn; (2) kiểm tra Origin của WebSocket lùi về `localhost:3000 / 5173 / 5174`, nên mọi kết nối WebSocket ở production bị từ chối và frontend trông như \"mất cập nhật thời gian thực.\"\n\n## Tích hợp GitHub\n\n[Tích hợp GitHub PR ↔ issue](/github-integration) cần hai biến. Đặt cả hai để bật Connect GitHub trong Cài đặt và nhận webhook. Hai biến bổ sung là tùy chọn nhưng điền được tên tài khoản kết nối ngay lúc cài.\n\n| Biến | Mặc định | Mô tả |\n|---|---|---|\n| `GITHUB_APP_SLUG` | trống | Slug của GitHub App của bạn (đuôi của `https://github.com/apps/<slug>`). Điều khiển URL nút cài ở Cài đặt → GitHub |\n| `GITHUB_WEBHOOK_SECRET` | trống | Webhook secret bạn đặt trên GitHub App. Dùng cho xác minh HMAC-SHA256 của mọi lượt giao `pull_request` / `installation`, và làm khóa HMAC cho state token của callback thiết lập |\n| `GITHUB_APP_ID` | trống | Tùy chọn. App ID dạng số từ trang cài đặt App. Kết hợp với `GITHUB_APP_PRIVATE_KEY`, cho callback thiết lập lấy tên tài khoản kết nối từ GitHub ngay lúc cài |\n| `GITHUB_APP_PRIVATE_KEY` | trống | Tùy chọn. Nguyên khối PEM của RSA private key của App (gồm dòng `-----BEGIN/END-----`, giữ xuống dòng). Dùng để đúc JWT ngắn hạn mà GitHub yêu cầu cho các lệnh REST xác thực bằng App |\n\n**Hành vi khi thiếu một trong hai biến bắt buộc:**\n\n- Nút `Connect GitHub` trong Cài đặt → GitHub bị **vô hiệu** và hiện gợi ý \"not configured\" cho admin.\n- Endpoint `/api/webhooks/github` trả **`503 github webhooks not configured`** — Multica từ chối xử lý sự kiện khi không có secret thay vì coi mọi chữ ký là hợp lệ.\n\n**Hành vi khi `GITHUB_APP_ID` / `GITHUB_APP_PRIVATE_KEY` tùy chọn bỏ trống:**\n\n- Thẻ kết nối hiện tạm `Connected to unknown` sau khi cài. Multica cập nhật dòng thành tên org/người dùng thật ngay khi GitHub giao webhook `installation.created` (thường trong vài giây), và phát cập nhật realtime để tab Cài đặt → GitHub đang mở phản ánh thay đổi mà không cần tải lại.\n\n**Lưu ý:** `GITHUB_WEBHOOK_SECRET` được dùng lại làm khóa ký cho state token của luồng cài, nên người vận hành chỉ cần quản lý một secret. Nó **không phải** *Client* secret của GitHub App — Client secret thuộc OAuth và tích hợp này không dùng. Xem [Tích hợp GitHub → Thiết lập tự vận hành](/github-integration#self-host-setup) cho hướng dẫn đầy đủ.\n\n## Phân tích sử dụng\n\nMặc định, server báo về máy chủ PostHog chính thức của Multica. Để tắt, đặt `ANALYTICS_DISABLED=true`.\n\n| Biến | Mặc định | Mô tả |\n|---|---|---|\n| `ANALYTICS_DISABLED` | `false` | Đặt `true` để tắt hẳn analytics phía backend |\n| `POSTHOG_API_KEY` | key mặc định có sẵn | Đặt khi trỏ về máy chủ PostHog của riêng bạn |\n| `POSTHOG_HOST` | `https://us.i.posthog.com` | Đổi sang host của bạn nếu tự vận hành PostHog |\n\n## Tiếp theo\n\n- [Thiết lập đăng nhập và đăng ký](/auth-setup) — cách cấu hình cụ thể các biến xác thực ở trên và bẫy nằm đâu\n- [Tích hợp GitHub](/github-integration) — cách dựng GitHub App đứng sau `GITHUB_APP_SLUG` / `GITHUB_WEBHOOK_SECRET`\n- [Khắc phục sự cố](/troubleshooting) — triệu chứng và cách sửa cho các cấu hình sai phổ biến\n- [Daemon và runtime](/daemon-runtimes) — các tham số `MULTICA_DAEMON_*` thực sự làm gì"
        },
        {
          "slug": "auth-setup",
          "title": "Thiết lập đăng nhập và đăng ký",
          "content": "Multica hỗ trợ hai cách đăng nhập: **email + mã xác thực** (mặc định) và **Google OAuth** (tùy chọn). Đăng nhập thành công, server cấp một cookie JWT hạn 30 ngày. Trang này nói cách cấu hình từng phương thức, cách giới hạn ai được đăng ký, và cái bẫy lớn nhất với triển khai tự vận hành.\n\nDanh sách biến môi trường được nhắc bên dưới, xem [Biến môi trường](/environment-variables); chi tiết dùng token và vòng đời, xem [Xác thực và token](/auth-tokens).\n\n## Đăng nhập email + mã xác thực hoạt động thế nào\n\nNgười dùng nhập email trên trang đăng nhập → server gửi mã 6 chữ số → người dùng nhập mã → server xác minh → cookie JWT được cấp. Luồng chuẩn. Hỗ trợ hai backend gửi mail — chọn cái hợp với triển khai của bạn:\n\n### Phương án A: Resend (khuyên dùng cho triển khai cloud / internet công cộng)\n\n1. Tạo tài khoản [Resend](https://resend.com/) và xác minh tên miền của bạn\n2. Tạo một API key\n3. Đặt biến môi trường:\n\n    ```bash\n    RESEND_API_KEY=re_xxxxxxxxxxxxxxxx\n    RESEND_FROM_EMAIL=noreply@yourdomain.com  # phải là tên miền đã xác minh trong Resend\n    ```\n\n4. Khởi động lại server\n\n### Phương án B: SMTP relay (cho triển khai tự vận hành / on-premise)\n\nDùng khi môi trường không truy cập được `api.resend.com` hoặc bạn đã có relay mail nội bộ (Microsoft Exchange, Postfix, SendGrid on-prem, v.v.). `SMTP_HOST` được ưu tiên hơn `RESEND_API_KEY` khi đặt cả hai — hễ `SMTP_HOST` không rỗng, server luôn đi SMTP, kể cả khi `RESEND_API_KEY` cũng được cấu hình, nên mail xác thực và mail mời không bao giờ rời mạng nội bộ.\n\nĐường SMTP hỗ trợ ba chế độ relay mà đa số mail server on-premise (đặc biệt receive connector của Microsoft Exchange) cung cấp:\n\n| Chế độ | Cổng | Xác thực | TLS |\n|---|---|---|---|\n| Relay nội bộ ẩn danh | `25` | không — gửi được vì tin theo IP / subnet | không trên đường truyền (chỉ phân đoạn nội bộ) |\n| Gửi có xác thực | `587` | `SMTP_USERNAME` + `SMTP_PASSWORD` | STARTTLS, tự nâng cấp |\n| TLS ngầm định (SMTPS) | `465` | tùy chọn (`SMTP_USERNAME` + `SMTP_PASSWORD`) | Bắt tay TLS khi kết nối — tự bật trên cổng `465`, hoặc ép trên cổng khác chuẩn với `SMTP_TLS=implicit` |\n\n**Relay Exchange ẩn danh trên cổng 25** — kiểu \"relay SMTP nội bộ\" / receive connector ẩn danh của Exchange nhận mail từ subnet tin cậy không cần thông tin đăng nhập:\n\n```bash\nSMTP_HOST=exchange.internal.example.com\nSMTP_PORT=25\nSMTP_USERNAME=\nSMTP_PASSWORD=\nSMTP_TLS_INSECURE=false\nRESEND_FROM_EMAIL=noreply@yourdomain.com  # dùng lại làm header From:\n```\n\n**Gửi có xác thực trên cổng 587** — cho relay yêu cầu tài khoản dịch vụ; STARTTLS tự nâng cấp khi server quảng bá:\n\n```bash\nSMTP_HOST=smtp.internal.example.com\nSMTP_PORT=587\nSMTP_USERNAME=multica\nSMTP_PASSWORD=...\nSMTP_TLS_INSECURE=false        # chỉ đặt true với chứng chỉ tự ký / CA riêng\nRESEND_FROM_EMAIL=noreply@yourdomain.com\n```\n\n**TLS ngầm định (SMTPS) trên cổng 465** — cho nhà cung cấp chỉ có SMTPS, không quảng bá STARTTLS (ví dụ mail doanh nghiệp Aliyun / Tencent). Cổng `465` tự bật TLS ngầm định; `SMTP_TLS=implicit` (bí danh: `smtps`, `ssl`) ép nó trên cổng SMTPS khác chuẩn:\n\n```bash\nSMTP_HOST=smtp.qiye.aliyun.com\nSMTP_PORT=465                  # TLS ngầm định tự bật trên 465\nSMTP_USERNAME=multica@yourdomain.com\nSMTP_PASSWORD=...\nSMTP_TLS=implicit              # tùy chọn trên 465; bắt buộc trên cổng SMTPS khác chuẩn\nRESEND_FROM_EMAIL=noreply@yourdomain.com\n```\n\n**Relay công cộng nghiêm ngặt (ví dụ Google Workspace `smtp-relay.gmail.com`)** còn yêu cầu tên EHLO hợp lệ. Chúng từ chối lời chào `localhost` mặc định từ IP công cộng và ngắt kết nối — hiện thành lỗi `EOF` khó hiểu ở một lệnh sau (`smtp auth: EOF`) chứ không phải ngay lời chào. Đặt `SMTP_EHLO_NAME` thành FQDN relay mong đợi; mặc định là hostname của máy, trong container thường không phải FQDN hợp lệ:\n\n```bash\nSMTP_HOST=smtp-relay.gmail.com\nSMTP_PORT=587\nSMTP_EHLO_NAME=mail.yourdomain.com   # FQDN relay chấp nhận; mặc định là hostname container (không phải FQDN)\nRESEND_FROM_EMAIL=noreply@yourdomain.com\n```\n\nKhi khởi động, server in ra provider nó chọn, kèm chế độ TLS đã thương lượng — ví dụ `EmailService: SMTP relay exchange.internal.example.com:25 (starttls) from=noreply@example.com` hoặc `… smtp.qiye.aliyun.com:465 (implicit-tls) from=…` (hay `Resend API` / `DEV mode`). Mật khẩu không bao giờ bị log. Không thấy dòng SMTP sau khi khởi động lại nghĩa là `SMTP_HOST` chưa vào tiến trình — kiểm tra env của container (`docker compose -f docker-compose.selfhost.yml exec backend env | grep SMTP`).\n\n**Điều gì xảy ra nếu không đặt gì**: server không báo lỗi, nhưng **mọi email đáng lẽ được gửi chỉ được ghi ra stdout của server**. Tiện cho phát triển cục bộ (chép mã từ log); ở production là một hố đen.\n\n## Mã kiểm thử cục bộ cố định\n\n> **Đừng bật mã xác thực cố định trên máy chủ truy cập được công khai.**\n>\n> Hành vi cũ mà máy chủ non-production mặc định chấp nhận `888888` đã bị gỡ. Trừ khi bạn cấu hình tường minh, gõ `888888` bị coi như mọi mã sai khác.\n>\n> Phát triển cục bộ không cấu hình backend email nào (không Resend, không SMTP) nên dùng mã sinh ra in trong log server. Nếu cần tự động hóa cục bộ/riêng tư có tính tất định, đặt `MULTICA_DEV_VERIFICATION_CODE` thành giá trị 6 chữ số như `888888`, và giữ `APP_ENV` không phải production:\n>\n> ```bash\n> APP_ENV=development\n> MULTICA_DEV_VERIFICATION_CODE=888888\n> ```\n>\n> Lối tắt này bị bỏ qua khi `APP_ENV=production`.\n\nTriển khai production nên để `MULTICA_DEV_VERIFICATION_CODE` trống và đặt `APP_ENV=production`. Nếu bạn triển khai qua `make selfhost` / `docker-compose.selfhost.yml`, `APP_ENV` mặc định là `production`.\n\n## Cấu hình Google OAuth\n\nTùy chọn. Không có nó, chỉ có email + mã xác thực; có nó, trang đăng nhập thêm nút \"Đăng nhập với Google\".\n\n1. Tạo một OAuth 2.0 client trong [Google Cloud Console](https://console.cloud.google.com/)\n2. Đặt **Authorized redirect URIs** thành địa chỉ frontend Multica của bạn cộng `/auth/callback`, ví dụ:\n\n    ```text\n    https://multica.yourdomain.com/auth/callback\n    ```\n\n3. Có client ID và client secret rồi, đặt ba biến môi trường:\n\n    ```bash\n    GOOGLE_CLIENT_ID=xxxxx.apps.googleusercontent.com\n    GOOGLE_CLIENT_SECRET=GOCSPX-xxxxxxxxxxxxxxx\n    GOOGLE_REDIRECT_URI=https://multica.yourdomain.com/auth/callback\n    ```\n\n4. Khởi động lại server.\n\n**Có hiệu lực lúc chạy**: frontend đọc các thiết lập này lúc runtime qua `/api/config` — sau khi đổi, khởi động lại server là frontend nhận giá trị mới, không cần build hay deploy lại.\n\n> **Redirect URI phải khớp chính xác ở cả Google Console lẫn `GOOGLE_REDIRECT_URI`** — gồm giao thức (`http` vs `https`), gạch chéo cuối và cổng. Lệch bất kỳ đâu là Google từ chối cả luồng OAuth; lỗi hiện cho người dùng là `redirect_uri_mismatch`.\n\n## Giới hạn ai được đăng ký\n\nBa biến môi trường kết hợp theo ưu tiên:\n\n```mermaid\ngraph TD\n    Start[Người dùng mới đăng nhập lần đầu] --> A{Email nằm trong<br/>ALLOWED_EMAILS?}\n    A -- Có --> Allow[Cho đăng ký]\n    A -- Không --> B{Tên miền nằm trong<br/>ALLOWED_EMAIL_DOMAINS?}\n    B -- Có --> Allow\n    B -- Không --> C{Có danh sách nào<br/>không rỗng?}\n    C -- Có --> Block[Từ chối]\n    C -- Không --> D{ALLOW_SIGNUP<br/>= true?}\n    D -- Có --> Allow\n    D -- Không --> Block\n```\n\n**Người dùng hiện có luôn đăng nhập lại được** — danh sách cho phép chỉ áp cho **đăng ký lần đầu**, không áp cho người quay lại.\n\n- **`ALLOWED_EMAILS`** (ưu tiên cao nhất) — danh sách email tường minh, phân tách phẩy. **Khi không rỗng, chỉ email trong danh sách đăng ký được.**\n- **`ALLOWED_EMAIL_DOMAINS`** — danh sách tên miền, phân tách phẩy (ví dụ `company.io,partner.com`).\n- **`ALLOW_SIGNUP`** — công tắc tổng, mặc định `true`. Đặt `false` để tắt hẳn đăng ký.\n\n> **Ba lớp theo ngữ nghĩa AND, không phải OR.** Trực giác sai phổ biến là `ALLOWED_EMAIL_DOMAINS=company.io` + `ALLOW_SIGNUP=true` nghĩa là \"cho company.io cộng mọi người khác.\" **Không phải.** Hễ một lớp có giá trị không rỗng, **email không khớp bị từ chối thẳng** — `ALLOW_SIGNUP=true` không ghi đè được.\n>\n> Muốn thật sự \"cho mọi người\", để trống cả ba biến (hoặc giữ `ALLOW_SIGNUP=true`).\n\n**Cấu hình điển hình**:\n\n| Mục tiêu | Cấu hình |\n|---|---|\n| Chỉ nội bộ, nhân viên `company.io` | `ALLOWED_EMAIL_DOMAINS=company.io` |\n| Nội bộ + vài cộng tác viên ngoài | `ALLOWED_EMAIL_DOMAINS=company.io` + địa chỉ cộng tác viên thêm vào `ALLOWED_EMAILS` |\n| Tắt hẳn tự đăng ký, chỉ qua lời mời | `ALLOW_SIGNUP=false` |\n| Đăng ký mở (không khuyến nghị cho production) | Cả ba trống |\n\n## Tắt đăng ký rồi thì còn mời người được không?\n\n**Chỉ người đã có tài khoản Multica.** Chấp nhận lời mời không kiểm tra danh sách đăng ký — người được mời đã đăng ký rồi (ví dụ ở không gian làm việc khác) thì bấm liên kết mời, đăng nhập là chấp nhận được.\n\n**Nhưng người chưa từng đăng ký thì lời mời không cứu được.** Trước khi chấp nhận, họ phải đăng nhập, và bước đầu của đăng nhập (xin mã xác thực) đi qua kiểm tra danh sách đăng ký. Nếu `ALLOW_SIGNUP=false`, hoặc email của họ không nằm trong `ALLOWED_EMAILS` / `ALLOWED_EMAIL_DOMAINS`, họ **không hoàn tất được đăng ký**, do đó không chấp nhận được lời mời.\n\nĐể mời một cộng tác viên ngoài chưa đăng ký: tạm thêm email của họ vào `ALLOWED_EMAILS`, chờ họ đăng ký và chấp nhận lời mời, rồi gỡ mục đó.\n\nVề cách tạo và dùng lời mời, xem [Thành viên và vai trò](/members-roles).\n\n## Tiếp theo\n\n- [Biến môi trường](/environment-variables) — định nghĩa đầy đủ mọi biến dùng trên trang này\n- [Xác thực và token](/auth-tokens) — các loại JWT / PAT / daemon token và cách dùng\n- [Khắc phục sự cố](/troubleshooting) — không nhận được mã xác thực, OAuth `redirect_uri_mismatch`, đăng ký bị từ chối"
        },
        {
          "slug": "troubleshooting",
          "title": "Khắc phục sự cố",
          "content": "Tra vấn đề theo triệu chứng. Mỗi mục gồm **triệu chứng / nguyên nhân khả dĩ / cách chẩn đoán / cách sửa**. Nếu tình huống của bạn không có ở đây, hãy liên hệ đội hỗ trợ.\n\n## Daemon không kết nối được server\n\n**Triệu chứng**: lệnh `status` của [`multica daemon`](/cli) hiện `offline` hoặc `connection refused`; log server không có request `/api/daemon/register` hay `/api/daemon/heartbeat` nào. Cơ chế daemon hoạt động ra sao, xem [Daemon và runtime](/daemon-runtimes).\n\n**Nguyên nhân khả dĩ**:\n\n1. **`MULTICA_SERVER_URL` trỏ sai địa chỉ** — mặc định là `ws://localhost:8080/ws`; tự vận hành phải đổi sang địa chỉ server của bạn\n2. **Mạng / firewall chặn** — daemon và server không cùng mạng, hoặc lưu lượng ra ngoài bị chặn\n3. **Token hết hạn hoặc không hợp lệ** — bạn chưa từng chạy `multica login`, hoặc PAT đã bị thu hồi\n4. **Server từ chối đăng ký** — tài khoản bạn đăng nhập không thuộc không gian làm việc mục tiêu (register trả 403)\n5. **Lỗi phân giải DNS** — hostname không phân giải được trên máy chạy daemon\n\n**Cách chẩn đoán**:\n\n```bash\nmultica daemon logs --lines 100    # tìm lỗi phía daemon\necho $MULTICA_SERVER_URL          # xác nhận địa chỉ đã đặt\ncurl -i http://<server-host>:8080/health   # gọi thẳng server\ncurl -i http://<server-host>:8080/readyz  # kèm độ sẵn sàng DB + migration\ncat ~/.multica/config.json        # kiểm tra api_token tồn tại\nmultica workspace list            # xác nhận bạn là thành viên không gian làm việc mục tiêu\n```\n\n**Cách sửa**: xử lý từng nguyên nhân ở trên. Hai cách sửa phổ biến nhất là **đổi `MULTICA_SERVER_URL` và khởi động lại daemon** (`multica daemon restart`) và **đăng nhập lại** (`multica logout && multica login`).\n\n## Task kẹt ở `queued`\n\n**Triệu chứng**: giao issue cho agent xong, trạng thái issue lật sang `in_progress` ngay, nhưng mãi không thấy dấu hiệu agent thực thi trên trang; `multica daemon status` hiện daemon `online`.\n\n**Nguyên nhân khả dĩ** (xếp theo tần suất):\n\n1. **Chạm giới hạn đồng thời của agent** — `max_concurrent_tasks` của agent này (mặc định 6) đã bị các task đang chạy chiếm hết\n2. **Một task khác của cùng agent còn đang chạy trên cùng issue** — cùng agent × cùng issue bị ép chạy tuần tự (chống thực thi trùng)\n3. **Agent đã bị lưu trữ** — sau lưu trữ, task mới vẫn xếp hàng nhưng không được nhận, và timeout sau 5 phút (mã sự cố G-01)\n4. **Daemon chưa đăng ký runtime này trong không gian làm việc hiện tại** — khởi động lại daemon hoặc chọn lại runtime trên UI\n5. **Daemon mất kết nối** — không có heartbeat trong 45 giây gần nhất. `daemon status` báo `online` có thể phản ánh một cú rớt rất mới\n\n**Cách chẩn đoán**:\n\n```bash\nmultica daemon status --output json       # danh sách runtime + last_seen_at\nmultica agent list                         # kiểm tra trạng thái lưu trữ của agent\nmultica issue show <issue-id>             # soi lịch sử task\n```\n\nPhía server (tự vận hành), grep `\"no_tasks\"` / `\"no_capacity\"` để xem kết quả nhận task.\n\n**Cách sửa**:\n\n- Đầy đồng thời → chờ task đang chạy xong, hoặc `multica agent update <id> --max-concurrent-tasks 10` để nâng trần\n- Tuần tự cùng issue → chờ task trước xong, hoặc giao lại cho agent khác\n- Agent lưu trữ → `multica agent restore <id>`\n- Runtime chưa đăng ký → `multica daemon restart`, daemon sẽ đăng ký lại\n\n## WebSocket không kết nối được\n\n**Triệu chứng**: console trình duyệt log `WebSocket is closed`; trang không hiện cập nhật thời gian thực (tiến độ task, bình luận, hộp thư) và phải tải lại mới thấy; task phía backend vẫn chạy.\n\n**Nguyên nhân khả dĩ**:\n\n1. **Lỗi kiểm tra Origin** — tên miền frontend của bạn không nằm trong danh sách CORS của server. Danh sách mặc định chỉ có `localhost:3000/5173/5174`; tự vận hành trên internet công cộng bắt buộc đặt `FRONTEND_ORIGIN`\n2. **Lệch giao thức** — frontend chạy `https://` thì cần `wss://`; HTTP dùng `ws://`\n3. **Reverse proxy chưa bật nâng cấp WebSocket** — Nginx / Envoy / HAProxy mặc định không chuyển tiếp header `Upgrade`\n4. **Cookie JWT hết hạn hoặc thiếu** — chưa đăng nhập lại sau hạn 30 ngày\n\n**Cách chẩn đoán**:\n\n- DevTools trình duyệt → Network → lọc \"WS\" và xem trạng thái kết nối cùng mã trạng thái\n- Grep log server tìm `\"rejected origin\"` / `\"websocket\"` — vấn đề origin tự nói tên\n- `curl -i http://<server-host>:8080/ws` phải trả `101 Switching Protocols` (kèm header `Upgrade`)\n\n**Cách sửa**:\n\n- Sai origin → đặt `FRONTEND_ORIGIN=https://multica.yourdomain.com` trong `.env` của server (hoặc `CORS_ALLOWED_ORIGINS` phân tách phẩy) và khởi động lại server\n- Lệch giao thức → đảm bảo giao thức của `FRONTEND_ORIGIN` khớp với frontend\n- Reverse proxy → với Nginx, thêm `proxy_http_version 1.1; proxy_set_header Upgrade $http_upgrade; proxy_set_header Connection \"upgrade\";`\n- Cookie hết hạn → tải lại trang và đăng nhập lại\n\n## Không nhận được email\n\n**Triệu chứng**: sau khi nhập email lúc đăng nhập hoặc nhận lời mời, cả hộp thư lẫn thư mục spam đều không có mã xác thực.\n\n**Trước tiên, xác nhận server nghĩ provider nào đang hoạt động.** Lúc khởi động backend in một trong các dòng:\n\n- `EmailService: SMTP relay <host>:<port> from=<addr>` — đang dùng SMTP (`SMTP_HOST` không rỗng thắng Resend)\n- `EmailService: Resend API from=<addr>` — đang dùng Resend\n- `EmailService: DEV mode — codes printed to stdout …` — chưa cấu hình provider nào\n\n```bash\ndocker compose -f docker-compose.selfhost.yml logs backend | grep \"EmailService:\"\n```\n\nNếu không thấy dòng bạn mong đợi, biến môi trường chưa vào tiến trình — kiểm tra `.env` và `docker compose -f docker-compose.selfhost.yml exec backend env | grep -E 'RESEND_|SMTP_'`. Thông tin đăng nhập không bao giờ bị log ở dòng khởi động này.\n\n### Khi Resend là provider đang hoạt động\n\n**Nguyên nhân khả dĩ**:\n\n1. **Chưa đặt `RESEND_API_KEY`** — server lặng lẽ lùi về và **ghi mã ra stdout của chính nó** không báo lỗi. Rất dễ vấp ở production\n2. **API key Resend không hợp lệ / hết quota** — log server hiện `\"failed to send verification code\"`\n3. **Tên miền của `RESEND_FROM_EMAIL` chưa xác minh trong Resend** — Resend từ chối gửi\n4. **Email đã gửi nhưng bị ISP người nhận đánh dấu spam** — kiểm tra bảng điều khiển Resend và thư mục spam\n\n**Cách chẩn đoán**:\n\n- Grep log server tìm `\"[DEV] Verification code for\"` — nếu có, Resend chưa cấu hình và mã đã được ghi ra stdout\n- [Bảng điều khiển Resend](https://resend.com/) → Emails xem lịch sử gửi\n- Xác nhận tên miền của `RESEND_FROM_EMAIL` có trong danh sách \"Verified Domains\" của console Resend\n\n**Cách sửa**:\n\n- Thiếu API key → làm theo [Thiết lập đăng nhập và đăng ký → Email hoạt động thế nào](/auth-setup#how-email--verification-code-sign-in-works) và khởi động lại server\n- Tên miền chưa xác minh → chạy luồng xác minh DNS trong console Resend (thêm bản ghi SPF / DKIM)\n- Trường hợp khẩn (test nội bộ) → chép mã in dưới `[DEV]` từ log server\n\n### Khi SMTP là provider đang hoạt động\n\nĐường SMTP bọc mỗi lỗi kèm giai đoạn nó thất bại, nên log server đã nói relay từ chối phiên ở đâu. Grep `\"failed to send verification email\"` / `\"failed to send invitation email\"` và xem lỗi được bọc:\n\n| Lỗi trong log | Nghĩa là gì | Cách sửa |\n|---|---|---|\n| `smtp dial <host>:<port>: dial tcp …: connect: connection refused` / `i/o timeout` | Container backend không tới được relay — sai host, sai cổng, firewall, hoặc relay không lắng nghe | Kiểm chứng `SMTP_HOST` / `SMTP_PORT` phân giải từ trong container (`docker compose -f docker-compose.selfhost.yml exec backend nslookup <host>` và `nc -vz <host> <port>`); mở firewall từ máy chạy Multica tới relay |\n| `smtp starttls: x509: certificate signed by unknown authority` (hoặc `certificate is not valid for any names`) | Relay dùng CA riêng / chứng chỉ tự ký và kho tin cậy của container từ chối | Hoặc cài CA vào container, hoặc đặt `SMTP_TLS_INSECURE=true` chỉ sau khi xác nhận relay nằm trên phân đoạn tin cậy |\n| `smtp auth: 535 5.7.8 Authentication credentials invalid` (hoặc `534`/`530`) | `SMTP_USERNAME` / `SMTP_PASSWORD` sai, hoặc relay đòi cơ chế xác thực khác `PLAIN` | Xác nhận lại thông tin tài khoản dịch vụ với quản trị mail; với relay nội bộ ẩn danh Exchange, để trống cả hai (`SMTP_USERNAME=`, `SMTP_PASSWORD=`) |\n| `smtp MAIL FROM: 550 5.7.1 Client does not have permissions to send as this sender` | Relay không nhận `RESEND_FROM_EMAIL` làm người gửi envelope — điển hình Exchange \"anonymous users not allowed\" hoặc vấn đề khớp DMARC | Đặt `RESEND_FROM_EMAIL` về tên miền relay chấp nhận; trên Exchange, cấp `ms-Exch-SMTP-Accept-Any-Sender` cho IP nguồn trên receive connector |\n| `smtp RCPT TO <addr>: 550 5.7.1 Unable to relay` | Receive connector của relay không cho subnet của bạn relay tới người nhận ngoài (phổ biến nhất với relay nội bộ ẩn danh gửi ra tên miền ngoài) | Hoặc giới hạn lời mời cho người nhận nội bộ, hoặc thêm subnet của máy Multica vào danh sách quyền \"Anonymous Users → Relay\" của Exchange |\n| `smtp DATA` / `smtp write body` / `smtp end data` | Phiên được nhận nhưng relay bỏ phần thân — thường do giới hạn kích thước, lọc nội dung, hoặc reset kết nối giữa chừng | Xem log của relay với cùng `Message-ID` (log dạng `<unixnano>@<host>`); nâng giới hạn kích thước nếu cần |\n\nLỗi `MAIL FROM`, `RCPT TO` và `DATA` luôn được log kèm mã phản hồi của relay để bạn đối chiếu với log Exchange / Postfix phía bên kia. Mã xác thực và token mời **không bao giờ** nằm trong lỗi được bọc.\n\n**Cách chẩn đoán**:\n\n- Grep `\"EmailService: SMTP relay\"` một lần lúc khởi động, rồi `\"failed to send\"` cho lỗi runtime\n- Từ trong container backend, kiểm tra kết nối: `docker compose -f docker-compose.selfhost.yml exec backend sh -c 'nc -vz $SMTP_HOST $SMTP_PORT'`\n- Xác nhận env đã vào tiến trình: `docker compose -f docker-compose.selfhost.yml exec backend env | grep SMTP_` (mật khẩu sẽ hiện trong output — chỉ chạy trên shell tin cậy)\n\n**Cách sửa**:\n\n- Sai host / cổng → chỉnh `SMTP_HOST` / `SMTP_PORT` và khởi động lại backend; các chế độ relay được hỗ trợ xem [Thiết lập xác thực → Phương án B: SMTP relay](/auth-setup)\n- Lệch chứng chỉ → cài CA của relay vào container, hoặc tạm `SMTP_TLS_INSECURE=true` trên phân đoạn tin cậy\n- Lỗi xác thực → kiểm tra lại thông tin đăng nhập; với relay nội bộ ẩn danh, để trống `SMTP_USERNAME` và `SMTP_PASSWORD`\n- `Unable to relay` → hoặc giới hạn người nhận nội bộ, hoặc cấp quyền relay cho IP của máy Multica trên receive connector Exchange\n\n## Mã kiểm thử cục bộ cố định không hoạt động\n\n**Triệu chứng**: trên máy chủ tự vận hành, bạn thử đăng nhập bằng mã kiểm thử cố định như `888888` và bị từ chối với `invalid or expired code`.\n\n**Nguyên nhân khả dĩ** (loại trừ lẫn nhau):\n\n1. **`MULTICA_DEV_VERIFICATION_CODE` trống** — mã cố định mặc định là tắt\n2. **`APP_ENV=production`** — đây là cấu hình production **đúng**; mã kiểm thử cố định bị bỏ qua ở production\n3. **Mã cấu hình không phải 6 chữ số** — lối tắt chỉ nhận giá trị 6 chữ số\n\n**Cách chẩn đoán**:\n\n```bash\ncat .env | grep -E 'APP_ENV|MULTICA_DEV_VERIFICATION_CODE'\ndocker exec <container> env | grep -E 'APP_ENV|MULTICA_DEV_VERIFICATION_CODE'\n```\n\nKiểm tra hộp thư (cả spam) tìm mã xác thực thật.\n\n**Cách sửa**:\n\n- Ở production, để `MULTICA_DEV_VERIFICATION_CODE` trống — cấu hình Resend và dùng mã thật\n- Cho phát triển cục bộ hay test nội bộ, hoặc chép mã sinh ra từ log server, hoặc đặt `APP_ENV=development` cộng `MULTICA_DEV_VERIFICATION_CODE=888888` — không bao giờ bật mã cố định trên máy chủ công khai (xem [Thiết lập đăng nhập và đăng ký → Mã kiểm thử cục bộ cố định](/auth-setup#fixed-local-testing-codes))\n\n## Bảng usage đứng ở số 0\n\n**Triệu chứng**: agent hoàn thành task, usage token thô được ghi vào cơ sở dữ liệu, nhưng **Cài đặt → Usage** và **Cài đặt → Runtime** hiện 0 input / output / chi phí toàn bộ. Lỗi này im lặng — log backend không có lỗi nào.\n\n**Nguyên nhân khả dĩ**:\n\n1. **`rollup_task_usage_hourly()` không bao giờ được nhận** — bảng điều khiển Usage / Runtime đọc từ bảng dẫn xuất `task_usage_hourly`, do hàm đó ghi. Từ MUL-2957, backend chạy rollup trong tiến trình qua bộ lập lịch dựa trên DB (`sys_cron_executions`); build cũ, thiếu migration `113`, hoặc backend sập kéo dài không còn bản sao nào chạy có thể khiến bảng không có dòng SUCCESS gần đây.\n2. **`pg_cron` cấu hình cho tương thích nhưng trỏ sai cơ sở dữ liệu** — `pg_cron.database_name` mặc định là `postgres`; nếu database Multica của bạn tên khác, job theo lịch không bao giờ thấy `rollup_task_usage_hourly()`. Bộ lập lịch trong tiến trình không phụ thuộc cái này, nhưng nếu bạn gỡ nó và dựa vào `pg_cron`, tên DB phải khớp.\n3. **Handler được nhận nhưng lỗi lặng lẽ** — ví dụ hàm SQL thiếu do migration áp một phần, hoặc role DB / search_path cấu hình sai. Xem các dòng audit FAILED trong `sys_cron_executions`.\n\n**Cách chẩn đoán**:\n\n```sql\n-- Xác nhận sự kiện thô tồn tại nhưng bảng theo giờ rỗng.\nSELECT count(*) AS raw_rows FROM task_usage;\nSELECT count(*) AS hourly_rows FROM task_usage_hourly;\n\n-- Soi nhật ký audit của bộ lập lịch trong tiến trình.\nSELECT plan_time, status, attempt, runner_id,\n       error_code, error_msg, started_at, finished_at\n  FROM sys_cron_executions\n WHERE job_name = 'rollup_task_usage_hourly'\n ORDER BY plan_time DESC\n LIMIT 20;\n\n-- Watermark — nếu là 1970-01-01, rollup chưa từng chạy.\nSELECT watermark_at FROM task_usage_hourly_rollup_state;\n\n-- Đường tương thích: nếu trước đây bạn đăng ký pg_cron, xác nhận\n-- nó có (hoặc không) khả dụng và trỏ đúng cơ sở dữ liệu.\nSELECT * FROM pg_available_extensions WHERE name = 'pg_cron';\nSHOW shared_preload_libraries;\nSELECT jobname, schedule, database, active FROM cron.job;\n```\n\n**Cách sửa**:\n\n- Xác nhận bộ lập lịch thực đang chạy trên ít nhất một bản sao backend — mỗi 30 giây nó phải thêm một dòng SUCCESS vào `sys_cron_executions` cho `rollup_task_usage_hourly`.\n- Gọi tay rollup một lần để kiểm chứng đường SQL: `SELECT rollup_task_usage_hourly();` — làm mới bảng điều khiển; nếu số hiện lên, hàm SQL ổn và vấn đề nằm ở đường nhận của bộ lập lịch.\n- Nếu migration `113_sys_cron_executions` chưa áp, khởi động lại backend để migration chạy, hoặc gọi `migrate up` thủ công.\n- Nếu bạn có lịch sử `pg_cron` cũ trước bộ lập lịch trong tiến trình, hàm SQL vẫn giữ advisory lock 4246 bên trong và hai đường không thể ghi trùng — xem [Bắt đầu nhanh tự vận hành → Tổng hợp usage](/self-host-quickstart#7-usage-rollup-no-operator-action-required) cho bước dọn `cron.unschedule` tùy chọn.\n\n## Migration `103` lỗi `refusing to drop legacy daily rollups`\n\n**Triệu chứng**: nâng cấp từ `v0.3.4` lên `v0.3.5+`, container backend không khởi động được (hoặc `migrate up` dừng) với:\n\n```text\nERROR: refusing to drop legacy daily rollups:\n  task_usage_hourly_rollup_state.watermark_at (1970-01-01 ...) trails\n  task_usage latest event (...) by more than 01:00:00 — backfill is\n  incomplete or pg_cron is not running. Run cmd/backfill_task_usage_hourly\n  (and let pg_cron catch up) before re-running migrate\n```\n\n**Nguyên nhân khả dĩ**: đây là guard fail-closed của migration `103`. Nó từ chối xóa rollup theo ngày cũ tới khi `task_usage_hourly` đuổi kịp `task_usage` thô. Guard nổ khi có dòng dữ liệu sẵn và watermark của rollup vẫn ở epoch — tức là chưa gì cuộn lịch sử vào bảng theo giờ.\n\nTừ MUL-2957, lệnh migrate tự chạy backfill theo lát tháng idempotent (dưới advisory lock 4246) ngay trước khi áp migration `103`, nên nâng cấp thẳng v0.3.4 → v0.3.5+ hoàn tất trong một lần `migrate up`. Nếu bạn vẫn thấy lỗi này thì hoặc đang dùng binary trước MUL-2957, hoặc chính hook đó thất bại — xem log migrate tìm dòng `task_usage hourly rollup hook` sớm hơn.\n\n**Cách sửa**:\n\n1. Nếu đang dùng binary trước MUL-2957 và chưa nâng binary được, chạy backfill độc lập trên cùng database (idempotent, ngắt an toàn, chạy lại an toàn):\n\n   ```bash\n   # Docker Compose\n   docker compose -f docker-compose.selfhost.yml exec backend \\\n     ./backfill_task_usage_hourly --sleep-between-slices=2s\n\n   # Kubernetes\n   kubectl -n multica exec deploy/multica-backend -- \\\n     ./backfill_task_usage_hourly --sleep-between-slices=2s\n   ```\n\n2. Chạy lại nâng cấp — khởi động lại container backend là đủ, migration chạy lúc khởi động. Guard giờ thấy watermark hiện hành và cho `103` áp.\n3. Bộ lập lịch trong tiến trình sau đó giữ watermark tiến — xem [Bắt đầu nhanh tự vận hành → Tổng hợp usage](/self-host-quickstart#7-usage-rollup-no-operator-action-required).\n\n`--sleep-between-slices=2s` là mặc định lịch sự trên database production nhiều năm lịch sử. Dùng `--months-back N --force-partial` nếu chỉ muốn giữ N tháng gần nhất và chấp nhận bỏ vĩnh viễn các bucket cũ hơn.\n\n## Xung đột cổng\n\n**Triệu chứng**: `multica server` hoặc `multica daemon start` lỗi `address already in use`.\n\n**Nguyên nhân khả dĩ**:\n\n1. **Cổng server bị chiếm** (mặc định `8080`)\n2. **Cổng health của daemon bị chiếm** (mặc định `19514`, lệch theo hash mỗi profile)\n3. **Xung đột cổng dev server web** (`3000` / `5173`)\n4. **Thiếu quyền cho cổng** (bind cổng đặc quyền `< 1024` cần sudo)\n\n**Cách chẩn đoán**:\n\n```bash\nlsof -i :8080        # macOS / Linux\nnetstat -ano | findstr :8080    # Windows\n```\n\n**Cách sửa**:\n\n- Kill tiến trình xung đột (`kill -9 <PID>`), hoặc đổi cổng qua `PORT=9000`\n- Muốn dùng 80 / 443 → đừng bind trực tiếp; đặt reverse proxy (Nginx / Caddy) phía trước, chuyển tiếp về cổng cao\n\n## Tìm log ở đâu\n\n| Thành phần | Vị trí | Lệnh |\n|---|---|---|\n| **Daemon** | `~/.multica/daemon.log` (chế độ nền) hoặc stdout tiền cảnh | `multica daemon logs -f --lines 100` |\n| **Server (Docker)** | stdout của container | `docker logs -f <container>` |\n| **Server (systemd)** | journal | `journalctl -u multica-server -f` |\n| **Frontend (dev)** | Terminal chạy `pnpm dev` | Đọc trực tiếp |\n| **Frontend (trình duyệt)** | DevTools → Console | Bấm `F12` |\n\nMuốn log daemon chi tiết hơn, chuyển từ nền sang tiền cảnh: `multica daemon stop && multica daemon start --foreground`."
        }
      ]
    },
    {
      "label": "Reference",
      "labelKey": "reference",
      "pages": [
        {
          "slug": "cli",
          "title": "Tham chiếu lệnh CLI",
          "content": "Multica CLI phản chiếu gần như mọi thứ Web UI làm được (tạo [issue](/issues), giao [agent](/agents), khởi động [daemon](/daemon-runtimes) và hơn nữa). Trang này liệt kê mọi lệnh cấp cao với mô tả một dòng. Bộ cờ và ví dụ đầy đủ, chạy `multica <command> --help`.\n\n## Xác thực lần đầu\n\nChạy lệnh này ở lần đầu dùng CLI để lấy **personal access token (PAT)**:\n\n```bash\nmultica login\n```\n\nTrình duyệt của bạn tự mở. Sau khi bạn phê duyệt trong web app, CLI lưu PAT (tiền tố `mul_`) vào `~/.multica/config.json`. Mọi lệnh sau đó xác thực bằng PAT này.\n\n> Với CI hoặc môi trường không màn hình, bỏ qua luồng trình duyệt: tạo PAT trong web app ở **Cài đặt → Personal Access Tokens**, rồi chạy `multica login --token <mul_...>` để đưa trực tiếp.\n\nKhác biệt giữa các loại token, xem [Xác thực và token](/auth-tokens).\n\n## Xác thực và thiết lập\n\n| Lệnh | Mục đích |\n|---|---|\n| `multica login` | Đăng nhập và lưu PAT |\n| `multica auth status` | Xem trạng thái đăng nhập, người dùng và không gian làm việc hiện tại |\n| `multica auth logout` | Xóa PAT cục bộ |\n| `multica setup cloud` | Thiết lập một phát cho Multica Cloud (đăng nhập + cài daemon) |\n| `multica setup self-host` | Thiết lập một phát cho backend tự vận hành |\n\n## Không gian làm việc và thành viên\n\n| Lệnh | Mục đích |\n|---|---|\n| `multica workspace list` | Liệt kê mọi không gian làm việc bạn truy cập được |\n| `multica workspace get <slug>` | Xem chi tiết một không gian làm việc |\n| `multica workspace member list` | Liệt kê thành viên của không gian làm việc hiện tại |\n| `multica workspace update <id> --name \"...\" [--description \"...\"] [--context \"...\"] [--issue-prefix \"...\"]` | Cập nhật metadata không gian làm việc (admin/owner). Trường dài nhận `--description-stdin` / `--context-stdin`. |\n\n## Issue và dự án\n\n> Các lệnh `list` (`multica issue list`, `autopilot list`, `project list`, v.v.) mặc định in ID ngắn sẵn sàng chép-dán — mã issue kiểu `MUL-123` cho issue, tiền tố UUID ngắn cho phần còn lại. Đối số `<id>` ở các lệnh tiếp theo nhận cả ID ngắn lẫn UUID đầy đủ, nên luồng điển hình là `multica issue list` → chép mã → `multica issue get MUL-123`. Truyền `--full-id` cho lệnh list khi cần UUID chính tắc.\n\n| Lệnh | Mục đích |\n|---|---|\n| `multica issue list` | Liệt kê issue (in mã issue chép-dán được) |\n| `multica issue get <id>` | Xem một issue (nhận mã issue hoặc UUID) |\n| `multica issue create --title \"...\"` | Tạo issue mới |\n| `multica issue update <id> ...` | Cập nhật issue (trạng thái, độ ưu tiên, người phụ trách, v.v.) |\n| `multica issue assign <id> --to <name>` | Giao cho thành viên, agent hoặc đội (giao cho agent kích hoạt một lượt chạy) |\n| `multica issue status <id> <status>` | Lối tắt đổi trạng thái |\n| `multica issue search <query>` | Tìm theo từ khóa |\n| `multica issue children <id>` | Liệt kê issue con nhóm theo giai đoạn |\n| `multica issue pull-requests <id>` | Liệt kê pull request liên kết và trạng thái |\n| `multica issue runs <id>` | Xem các lượt chạy agent trên một issue |\n| `multica issue run-messages <task-id>` | Xem tin nhắn của một lần thực thi |\n| `multica issue usage <id>` | Xem usage token tổng hợp của một issue |\n| `multica issue rerun <id>` | Xếp hàng một task mới cho agent đang phụ trách issue |\n| `multica issue cancel-task <task-id>` | Hủy task đang xếp hàng hoặc đang chạy |\n| `multica issue comment <id> ...` | Lồng: xem / đăng bình luận |\n| `multica issue comment resolve/unresolve <comment-id>` | Đánh dấu luồng bình luận đã/chưa xử lý |\n| `multica issue subscriber <id> ...` | Lồng: theo dõi / bỏ theo dõi |\n| `multica issue metadata <id> ...` | Lồng: đọc / ghi metadata của issue |\n| `multica issue label <id> ...` | Lồng: quản lý nhãn trên issue |\n| `multica project list/get/create/update/delete/status` | CRUD dự án |\n| `multica label list/create/update/delete` | CRUD nhãn của không gian làm việc |\n\n## Agent và skill\n\n| Lệnh | Mục đích |\n|---|---|\n| `multica agent list` | Liệt kê agent của không gian làm việc |\n| `multica agent get <slug>` | Xem cấu hình một agent |\n| `multica agent create ...` | Tạo agent |\n| `multica agent update <slug> ...` | Cập nhật agent |\n| `multica agent archive <slug>` | Lưu trữ |\n| `multica agent restore <slug>` | Khôi phục agent đã lưu trữ |\n| `multica agent tasks <slug>` | Xem lịch sử task của agent |\n| `multica agent avatar <slug>` | Tải avatar cho agent |\n| `multica agent env <slug> ...` | Đọc hoặc thay biến môi trường tùy chỉnh của agent |\n| `multica agent skills ...` | Lồng: gắn / gỡ skill |\n| `multica skill list/get/create/update/delete` | CRUD skill |\n| `multica skill import ...` | Nhập skill từ GitHub, ClawHub hoặc máy cục bộ |\n| `multica skill files ...` | Lồng: quản lý file của skill |\n\n### Xung đột khi nhập skill\n\n`multica skill import --url <url>` mặc định `--on-conflict fail`. Nếu skill trùng tên đã tồn tại, lệnh thoát với kết quả `conflict` có cấu trúc và không đổi gì trong không gian làm việc.\n\nDùng `--on-conflict overwrite` khi bạn đã tạo skill sẵn có và muốn thay nội dung nhưng giữ ID cùng các gắn kết agent. Dùng `--on-conflict rename` để nhập một bản sao với hậu tố tự động như `-2`. Dùng `--on-conflict skip` để giữ nguyên skill sẵn có và báo `skipped`.\n\n```bash\nmultica skill import --url https://skills.sh/acme/repo/review-helper\nmultica skill import --url https://skills.sh/acme/repo/review-helper --on-conflict overwrite\nmultica skill import --url https://skills.sh/acme/repo/review-helper --on-conflict rename\nmultica skill import --url https://skills.sh/acme/repo/review-helper --on-conflict skip\n```\n\n## Đội\n\n| Lệnh | Mục đích |\n|---|---|\n| `multica squad list` | Liệt kê đội trong không gian làm việc |\n| `multica squad get <id>` | Xem một đội |\n| `multica squad create --name \"...\" --leader <agent>` | Tạo đội (owner / admin) |\n| `multica squad update <id> ...` | Cập nhật tên, mô tả, chỉ dẫn, leader hoặc avatar |\n| `multica squad delete <id>` | Lưu trữ (xóa mềm) — chuyển issue đã giao sang leader |\n| `multica squad member list/add/remove/set-role <squad-id>` | Quản lý thành viên đội và cập nhật vai trò tại chỗ |\n| `multica squad activity <issue-id> <action\\|no_action\\|failed> --reason \"...\"` | Agent leader dùng để ghi đánh giá mỗi lượt |\n\nMô hình đầy đủ xem [Đội](/squads).\n\n## Tự động hóa\n\n| Lệnh | Mục đích |\n|---|---|\n| `multica autopilot list` | Liệt kê mọi autopilot trong không gian làm việc |\n| `multica autopilot get <id>` | Xem một autopilot |\n| `multica autopilot create ...` | Tạo autopilot |\n| `multica autopilot update <id> ...` | Cập nhật |\n| `multica autopilot delete <id>` | Xóa |\n| `multica autopilot runs <id>` | Xem lịch sử chạy |\n| `multica autopilot trigger <id>` | Kích hoạt một lượt chạy thủ công |\n| `multica autopilot trigger-add/update/delete <id>` | Quản lý trigger theo lịch hoặc webhook |\n| `multica autopilot trigger-rotate-url <id> <trigger-id>` | Xoay URL của trigger webhook |\n\n`multica autopilot create/update` cũng nhận `--subscriber` để đặt người theo dõi mặc định cho issue do autopilot `create_issue` tạo; update nhận `--clear-subscribers` để gỡ.\n\n## Daemon và runtime\n\n| Lệnh | Mục đích |\n|---|---|\n| `multica daemon start` | Khởi động daemon (mặc định chạy nền; thêm `--foreground` để chạy tiền cảnh) |\n| `multica daemon stop` | Dừng daemon |\n| `multica daemon restart` | Khởi động lại daemon |\n| `multica daemon status` | Kiểm tra daemon online chưa và mức đồng thời |\n| `multica daemon logs` | Xem log daemon |\n| `multica runtime list` | Liệt kê runtime trong không gian làm việc hiện tại |\n| `multica runtime usage` | Xem mức dùng tài nguyên |\n| `multica runtime activity` | Nhật ký hoạt động gần đây |\n| `multica runtime update <id> ...` | Khởi phát cập nhật CLI trên một runtime |\n| `multica runtime delete <id> [--cascade]` | Xóa runtime, tùy chọn lưu trữ các agent gắn kèm |\n| `multica runtime profile ...` | Quản lý runtime profile tùy chỉnh và ghi đè đường dẫn cục bộ |\n\n## Khác\n\n| Lệnh | Mục đích |\n|---|---|\n| `multica repo checkout <url>` | Clone repo về máy cho agent dùng |\n| `multica config` | Xem hoặc sửa cấu hình CLI cục bộ |\n| `multica version` | In phiên bản CLI |\n| `multica update` | Nâng CLI lên bản mới nhất |\n| `multica attachment download <id>` | Tải tệp đính kèm từ issue hoặc bình luận |\n\n## Xem đủ cờ\n\nMọi lệnh đều hỗ trợ `--help`:\n\n```bash\nmultica issue create --help\nmultica agent update --help\n```\n\nv2 sẽ có trang tham chiếu chi tiết riêng cho từng lệnh.\n\n## Bước tiếp theo\n\n- [Xác thực và token](/auth-tokens) — PAT vs. JWT vs. daemon token\n- [Daemon và runtime](/daemon-runtimes) — các lệnh `daemon` hoạt động thế nào bên dưới\n- [Tạo và cấu hình agent](/agents-create) — mọi tùy chọn của `multica agent create`"
        },
        {
          "slug": "auth-tokens",
          "title": "Xác thực và token",
          "content": "Multica có ba loại token, mỗi loại cho một ngữ cảnh: Web UI trên trình duyệt, dòng lệnh và script, và daemon. Cả ba đại diện cho cùng một bạn, nhưng phạm vi và vòng đời khác nhau.\n\n## Ba loại token\n\n| Token | Định dạng | Dùng ở đâu | Vòng đời |\n|---|---|---|---|\n| **Cookie JWT** | cookie `multica_auth` (HttpOnly) | Trình duyệt web | 30 ngày |\n| **Personal access token (PAT)** | Tiền tố `mul_` | CLI, script, gọi API trực tiếp | Mặc định không hết hạn; tạo qua API có thể truyền `expires_in_days` |\n| **Daemon token** | Tiền tố `mdt_` | Giao tiếp daemon-với-server | Daemon tự quản lý |\n\nDùng hằng ngày bạn chỉ đụng hai loại đầu. **Token của [daemon](/daemon-runtimes)** được `multica daemon login` tự tạo và làm mới — bạn không phải nghĩ về nó.\n\n## Mỗi token gọi được gì\n\n| Route API | Cookie JWT | PAT | Daemon token |\n|---|---|---|---|\n| `/api/user/*` (hành động mức người dùng) | ✓ | ✓ | ✗ |\n| `/api/workspaces/:id/*` (mức không gian làm việc) | ✓ | ✓ | ✗ |\n| `/api/daemon/*` (chỉ daemon) | ✗ | ✓ | ✓ |\n| WebSocket `/ws` (đẩy thời gian thực) | ✓ (cookie) | ✓ (xác thực qua tin nhắn đầu) | ✗ |\n\n**PAT gọi được gần như mọi thứ** — nó đại diện \"toàn bộ bạn.\" Daemon token chỉ làm được việc daemon cần: lấy task và báo kết quả.\n\n**Cả hai đều gọi được `/api/daemon/*`, nhưng phạm vi khác nhau.** PAT đại diện **cả một người dùng** — xác thực xong, nó thấy mọi không gian làm việc bạn thuộc về. Daemon token bị ghim vào một không gian làm việc lúc tạo và chỉ đụng được tài nguyên trong đó. Ở production, chạy daemon bằng daemon token — đừng đi tắt dùng PAT, kẻo cấp cho daemon nhiều quyền hơn nó cần rất nhiều.\n\n## Đăng nhập\n\n### Email + mã xác thực\n\n1. Nhập email; server gửi mã 6 chữ số.\n2. Nhập mã; server cấp cookie JWT (trình duyệt) hoặc đổi lấy PAT (CLI).\n\n> **Người vận hành tự host lưu ý**: giữ `MULTICA_DEV_VERIFICATION_CODE` trống trên triển khai công khai. Nếu bật mã kiểm thử cố định, bất kỳ ai xin được mã đều đăng nhập được bằng giá trị đó khi `APP_ENV` không phải production. Xem [Thiết lập xác thực tự vận hành](/auth-setup).\n\n### Google OAuth\n\nBấm **Đăng nhập với Google** và đi qua callback OAuth chuẩn. Tự vận hành cần cấu hình `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET` và redirect URI — xem [Thiết lập xác thực tự vận hành](/auth-setup).\n\n## Tạo, xem và thu hồi PAT\n\n**Tạo** PAT có hai cách:\n\n- **Web UI**: Cài đặt → Personal Access Tokens → New token\n- **CLI**: `multica login` tự tạo nếu chưa có PAT cục bộ\n\n> **PAT đầy đủ chỉ hiển thị đúng một lần lúc tạo.** Tải lại hay đóng hộp thoại rồi, bạn không xem lại được.\n>\n> Multica chỉ lưu hash của PAT trong cơ sở dữ liệu — chính server cũng không khôi phục được bản gốc. Chép và lưu ngay. Mất nó thì lựa chọn duy nhất là thu hồi và tạo cái mới.\n\n**Xem** các PAT hiện có (tên, thời điểm tạo, lần dùng cuối — **không** phải token đầy đủ) nằm ở Cài đặt → Personal Access Tokens.\n\n**Thu hồi** PAT: bấm Revoke trong danh sách. Thu hồi có hiệu lực ngay — request kế tiếp bằng PAT đó bị từ chối với 401.\n\n## Đăng xuất chỉ xóa token cục bộ\n\nKhi bạn chạy `multica auth logout` hoặc bấm đăng xuất trên Web UI:\n\n- **Token cục bộ bị xóa** — CLI gỡ PAT khỏi `~/.multica/config.json`; trình duyệt xóa cookie.\n- **PAT vẫn hợp lệ trên server** — nếu ai đó lấy được PAT của bạn trước khi bạn đăng xuất (ví dụ chép sang máy khác), họ **vẫn dùng được**.\n\n> **Nghi PAT bị lộ thì đừng chỉ đăng xuất.** Vào Cài đặt → Personal Access Tokens và **thu hồi** token. Chỉ thu hồi mới vô hiệu hóa ngay một token bị lộ.\n\n## Bước tiếp theo\n\n- [Tham chiếu lệnh CLI](/cli) — xác thực là tự động cho mọi lệnh CLI\n- [Thiết lập xác thực tự vận hành](/auth-setup) — cấu hình email, OAuth và danh sách đăng ký khi tự vận hành\n- [Daemon và runtime](/daemon-runtimes) — daemon token từ đâu ra"
        },
        {
          "slug": "mobile-app",
          "title": "Ứng dụng di động (iOS)",
          "content": "Client iOS của Multica là mã nguồn mở và nằm trong repo chính cùng web, desktop và backend. Nó chưa có trên App Store — cho tới khi điều đó thay đổi, ai muốn dùng trên iPhone thì build từ mã nguồn. Lần build đầu mất khoảng 10–20 phút và ~2 phút cho các lần sau, và nó nói chuyện với cùng backend như [multica.ai](https://multica.ai) nên tài khoản sẵn có của bạn dùng được luôn.\n\n> Trang này dành cho **dùng cá nhân**. Lập trình viên của app nên đọc `apps/mobile/README.md` trong repo — nó nói về các biến thể dev / staging và ma trận script đầy đủ.\n\n## Bạn cần gì\n\n- Một chiếc **Mac** cài Xcode (miễn phí từ App Store).\n- Một **Apple ID** miễn phí thêm ở Xcode → Settings → Accounts. Tài khoản Apple Developer Program trả phí là tùy chọn, chỉ để kéo dài cửa sổ ký 7 ngày lên 1 năm — xem [giới hạn 7 ngày](#7-day-signing-limit) bên dưới.\n- Một chiếc **iPhone** nối qua cáp USB, đã [bật Developer Mode](https://docs.expo.dev/guides/ios-developer-mode/) (Cài đặt → Quyền riêng tư & Bảo mật → Developer Mode).\n- Mã nguồn Multica đã checkout:\n\n  ```bash\n  git clone https://github.com/multica-ai/multica.git\n  cd multica\n  pnpm install\n  ```\n\nThiếu gì trong danh sách đó, hãy đi qua hướng dẫn [Set up your environment](https://docs.expo.dev/get-started/set-up-your-environment/) của Expo (chọn **Development build → iOS Device**) — đó là hướng dẫn thiết lập chuẩn cho mọi thứ trừ phần checkout repo.\n\n## Build\n\nMột lệnh:\n\n```bash\npnpm ios:mobile:device:prod:release\n```\n\nXcode ký bản build bằng \"Personal Team\" mà Apple ID của bạn tự sở hữu — team này được tạo âm thầm ở lần đầu bạn đăng nhập Xcode bằng bất kỳ Apple ID nào, nên nó có sẵn dù bạn không nhớ đã thiết lập gì. Đây là **bản Release**: không phụ thuộc Metro, splash → app, y như bản cài từ App Store.\n\nLần build đầu tải CocoaPods + biên dịch React Native từ mã nguồn — chờ 10–20 phút. Các lần sau dùng lại cache của Xcode.\n\nĐường điển hình chỉ có vậy. Nếu ký thất bại, nhảy tới [Khắc phục sự cố](#troubleshooting).\n\n## Giới hạn ký 7 ngày\n\nApple ID miễn phí ký bản build trong **7 ngày**. Sau đó, app từ chối khởi động trên iPhone và hiện lỗi \"untrusted developer\". Cắm lại vào Mac và chạy lại đúng lệnh trên để ký lại — dữ liệu của bạn còn nguyên vì nó nằm trên backend, không nằm trong app.\n\nCách duy nhất để kéo dài là tài khoản **Apple Developer Program** ($99/năm từ [developer.apple.com](https://developer.apple.com)). Khi đó chữ ký có hiệu lực 1 năm giữa các lần gia hạn, và bạn còn phân phối được cho thiết bị khác qua TestFlight.\n\n## Cập nhật\n\nChưa có tự cập nhật. Khi codebase Multica tiến lên, pull và build lại:\n\n```bash\ngit pull\npnpm install\npnpm ios:mobile:device:prod:release\n```\n\nCác lần build sau nhanh vì Xcode cache phần biên dịch native.\n\n## Vì sao chưa lên App Store\n\nApp iOS vẫn đang đi nhanh — đội ưu tiên ship-và-lặp hơn chu kỳ duyệt App Store lúc này. Bản beta TestFlight nhiều khả năng là bước kế tiếp trước khi phát hành App Store đầy đủ. Trước đó, đường tự build ở trên là cách duy nhất để dùng Multica trên iOS.\n\n## Khắc phục sự cố\n\n**\"No matching provisioning profiles found\"** — Xcode từ chối ký bundle id mặc định `ai.multica.mobile` với Apple ID của bạn. Hiếm, nhưng xảy ra nếu ai đó đã đăng ký tiền tố đó trên cổng developer của Apple. Chọn bất kỳ reverse-domain nào bạn kiểm soát (`com.yourname.multica` là ổn), export rồi chạy lại:\n\n```bash\nexport EXPO_BUNDLE_IDENTIFIER_PROD=com.yourname.multica\npnpm ios:mobile:device:prod:release\n```\n\nId không cần mang nghĩa gì — Apple chỉ cần nó chưa bị team khác nhận.\n\n**\"Could not launch &lt;app&gt;\" / \"Untrusted Developer\"** — hoặc bạn chạm giới hạn 7 ngày (build lại), hoặc cần tự tin cậy hồ sơ developer trên iPhone: Cài đặt → Cài đặt chung → VPN & Quản lý thiết bị → chạm Apple ID của bạn → Tin cậy.\n\n**Build treo ở `Pod install` hoặc biên dịch mãi** — lần build đầu thật sự mất 10–20 phút vì CocoaPods tải phụ thuộc và Xcode biên dịch React Native từ mã nguồn. Các lần sau nhanh hơn nhiều.\n\n**App không tới được backend** — xác nhận `apps/mobile/.env.production` chưa bị sửa (nó đi kèm `EXPO_PUBLIC_API_URL=https://api.multica.ai`). Nếu bạn đã đổi, khôi phục bằng `git checkout apps/mobile/.env.production`."
        }
      ]
    }
  ]
};
